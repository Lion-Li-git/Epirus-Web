# 踩过的坑 / 规矩（Epirus-Web）—— 只留**会复发**的

> 为什么单独一个文件：这些不是"版本变化记录"（那些在 `CHANGELOG.md`），而是**做这件事时反复踩到的规矩**。
> 原先它们堆在 `HANDOFF.md` §5 里，让交接文档越滚越长；v1.5.18 起挪到这里，HANDOFF 只记"现在 + 下一步"。
> 编号沿用历史编号（1–20 来自 v1.5.2~v1.5.12 时期，21–25 是 v1.5.18 新增），**别因为编号大就跳过**。

## A. 工具 / 流程

1. **别把"等待"写进 tool call**（用户 2026-09-12 指出，千问定位）：我写了个 15 分钟的轮询循环，
   导致 **DSH 整个卡死**、实验被带走。**改成"一次只看一眼"（`timeoutMs ≤ 60s`），让实验自己写日志**。
2. **长任务先报【预估】、跑中报【进度】**（用户要求）。基准吞吐：**5 人 250 代热启动 ≈ 1 分钟**、
   **一个 1400 局考卷 ≈ 8 秒**。
3. **补丁与提交必须 `&&` 串联**，并在提交前 `grep` 校验 —— 我两次因为 Python 补丁失败
   （SyntaxError / 锚点不匹配）而**提交了声明里有、磁盘上没有**的 CHANGELOG 条目。
4. **别在同一条命令里叠多个 heredoc**：`python - <<'PY' … PY` 后面再跟 `git commit -F - <<'MSG'`，
   第一个 heredoc 会吃掉后面全部内容（本轮犯过，什么都没提交）。
5. **`git commit -m "…"` 的信息里不要有内层双引号**（会截断成 pathspec）。
6. **`sed -i 's/…v1.4.6/…/'` 不匹配时不报错** ⇒ 版本号会静默漂移（README 曾停在 v1.4.6、index.html 停在 v1.4.1）。
   **改用任意版本的正则** `s/v[0-9.]+/vX.Y.Z/`；且 **np-test D8** 已在守护三方一致。
7. **node 的 `/tmp` = `D:\tmp`**（不是 Git Bash 的 `%TEMP%`）⇒ 给 node 传 /tmp 下的文件必须 `$(cygpath -w …)`。
   这是会话内**第 4 次**踩。
8. **`.cmd` 里不能写中文注释**（GBK 码页会把行拆成非法命令）⇒ 两个 `.cmd` 都是纯 ASCII。
9. **按命令行模式杀进程会杀掉自己**（我的 kill 命令里含匹配串，把当前 shell 也匹配上了）。
   要按 pid 精确杀，或用不会出现在杀手指令里的模式。
10. **跨 heredoc 写 JS 正则会丢反斜杠**（`[\\/]` 变成 `[\/]`）⇒ 写完必须回读字节验证；
    实在要用就换成**不含反斜杠**的写法（如 `/^.*[^0-9A-Za-z_.-]/`）。

## B. 测量 / 方法（都是花钱买来的）

11. **n=1 不得下结论** —— 曾**三次**同类翻车：v1.4.1 把 38.4%→21.8% 归因于轮换（真因是"从零冷启动 vs 热启动谱系"）；
    v1.4.9 想据 `wall2-31` 换冠军（多 seed 后不显著）；v1.4.9 的反弹墙效应（n=6 仍不显著）。
    **单跑极差**：标准考卷 3.8~8.5pt、**反弹墙 100pt**。
12. **每个新口径都要带自检计数 + 反证对照**。范例：`--ban=sword`（使用率 0%）应给出 **≈0 的变化**
    （实测 −0.1pt）⇒ 证明消融器材本身不改变行为。没有反证对照的 Δ 不可信。
13. **"白名单/清单两处各写一遍"必出事**（漏加名字）—— 已踩**三次**（v1.3.59 漏 server、v1.4.8 漏 worker、
    v1.4.14 漏 `opp-pool.mjs` 本身）。现在由 **single source（`server/opp-pool.mjs`）+ np-test D7/D9** 守住。
    v1.5.18 又抓到三处同型（反摆烂 env / 体检指标 / 2P 基准表）⇒ 各配 D24 / D27 / `tools/p2-baselines.mjs`。
14. **口径会决定结论**（最贵的教训之一）：`枪` 的 mono-spam Δ = **−15.5pt**（读作"坑"）而消融 `Δ_lost` = **+0.5~3.8pt**
    （读作"该留着"）；`雷击之枪` mono −44pt（读作"没用"）而消融 −2.6pt + 环场 **+20pt**。
    **已在用的技能看消融；反制卡看场**。
15. **没有门禁会自己变绿**（v1.4.15）：`tools/smoke.mjs` 从 **v1.4.0 起就是红的**，4 个断言全在
    「长程（5 血）模式必须能在页面选到」这条线上 —— 而 v1.4.0 的交付清单里**只落了 `ui.js` 的消费者，
    没落 `index.html` 的 `<option>`**（"两处各写一半"）。它躺了 5 个版本。
    ⇒ **交付清单里"文件 A + 文件 B"这种条目，做完必须由一条会失败的用例去点名**。
16. **`workers` 是会改变结果的行为输入**（v1.5.0）：同 seed/池/起点，`EPIRUS_WORKERS` 16 → 12 训出的权重**不同**
    （sha1 `A4A1A894…` vs `92AF513B…`）。机理：worker 是独立沙箱，个体随机流在 worker 内推进量随
    `chunk = ceil(pop/workers)` 变化。⇒ **同 seed / 同池 / 同起点 / 同 worker 数**才是可复现；
    想"少用几个 worker 省机器"就等于**悄悄换了一个冠军**。
17. **给训练池加"对手名"时又漏了一处（同型第三次）**（v1.5.2）：只改了「开跑前校验」和「串行回退」，
    漏了**终局名人堂评估**那段 —— 它自己又写了一遍"名字→函数"映射，跑到 250 代才炸成 `sel is not a function`。
    ⇒ **规矩**：训练侧"名字→函数"**只允许一个入口**（`server/opp-champs.mjs` 的 `makeOppSelResolver`），
    且**开跑前把每个名字真解一次**。守门 `np-test D12`。附带：worker 里的硬错误必须 **reject 穿透**
    （否则 Promise 永不 resolve，只会挂到 30 分钟墙上时钟报"训练超时"，真因被埋掉）。
18. **"冠军的行为"与"某个风格脚本的行为"必须分清**（v1.5.13，用户实测踩到）：多人局难度下拉的**默认**是
    `st:combocounter` **脚本**，不是冠军 ⇒ 用户拿它当"冠军"看，于是"枪使用率很高"与 skill report 对不上；
    冠军 `eco-34` 的枪使用率是 **0%**。⇒ 想看冠军就把难度选成「**冠军（最强）**」；skill report 的「使用率」
    是**富裕经济**口径（每回合把 ep 补到 10），与真实对局不同 —— 它的「Δ_lost 强度」列才是主口径。
19. **规则文档里"对谁"没写清时，实现很容易只对 2 人局正确**（v1.5.13 修用户报的狙击枪 bug）：
    旧实现只查"被击方这回合用了什么技能"、不看打谁 ⇒ 多人局里目标用枪打第三个人也会废掉狙击，
    而 2 人局恰好等价 ⇒ `spec R13/57` 一直绿。守门 `np-test D20`。**教训**：规则条目里凡出现
    "该玩家/攻击者/被作用者"这类指代，实现处要写一句"到底指向谁"，并配一条**多人局**用例。
20. **"按模式猜默认值"必须先证明该模式真的会触发那条规则**（v1.5.12 回滚 v1.5.11）：
    干净配对（`lngC` 自动 0.5 vs `lngD` 显式 0，6 seed，其余全同）产出**逐字节相同** ⇒ 长程里它是**空操作**。

## C. v1.5.18 新增

21. **"守门"要看清它守的是"文件"还是"入口"**：`np-test` 的 `REPRO2` 原先只做**字符串存在性检查**
    （对硬编码的 5 个文件 grep `seedSandbox|setRng`）⇒ "同一个文件里 `runTrainN` 有播种、`runTrain` 没有"
    照样通过 —— 而那条未播种的路产出的正是线上 2P 包。现在的判据是 **调用点反查 + 按函数体切分 + 实测幂等**。
22. **阈值先量样本敏感性**：G（有效技能数）在同一个冠军上 **n5=3.08 / n10=2.99 / n20=4.24 / n40=4.54**
    （罕见技能没出现时熵被低估）⇒ "用 10 局卡 `G<3`"是把**样本量**读成"打法坍缩"。
    `promote-champion` 的自对局局数默认已 10→20；数据记在 `tools/audit-lib.mjs` 顶部。
23. **可证明是空操作的改动不要落地**：复核 R2 建议把"复制避雷针"提前到 ② 之前，但 ① 层的
    "当回合有雷系 ⇒ 全部无效（全场）"已经覆盖了它 ⇒ 写出来就是第二个"有实现、有测试、无入口"。
    正确做法：把**事实**钉成守门（`np-test D31`），而不是把**推理**变成代码。
24. **审计/复核者给的命令也可能脏工作区**：`tools/battle-test.mjs` 原先覆写**被 git 跟踪**的
    `results/battle-0*.png` ⇒ 一次"只读复核"照抄验收单里的命令就把工作区弄脏了。已改到
    `docs/artifacts/battle-out/`（未跟踪）。**写工具时默认"只读"就该真的只读。**
25. **一条用例"绿"不等于它锁住了规则**：`tests/spec.js` 那条名叫「三判定全胜=爆头」的用例，
    构造是 `seqRng([0.1,0.1,0.1])` ⇒ 把 `judge3` 改成 `judge`（只判一次）**照样绿**。
    写用例时要问："**把实现改坏成什么样，这条还会绿？**" —— 会绿就说明它锁住的是输出，不是那个"3"。

## 26. `git add -A` 会把"工作区里早就没了"的文件提交成删除 —— 提交前必看 `D` 行

**事故**：v1.5.21 提交时用了 `git add -A`，把 `results/` 里**早已不在工作区**的历史文件
（22/34/57 回合对局记录、`epirus-champion (6/7/8).json`、`page-verify-v1516/1517.png`、`battle-0{1..4}.png`）
一并提交成删除（`18 files changed, … 6018 deletions`），**用户的对局记录当场从仓库里消失**，
只能 `git checkout <上一个提交> -- results/` 恢复（已补一次"恢复提交"）。
**排查**：`tools/*.mjs` 只 `rmSync` 自己 `mkdtempSync` 出来的临时 user-data-dir，**不是工具删的** ——
工作区里那批文件在提交之前就已经不在了（很可能是一次手工清理）。
**规矩**：提交前先 `git status --porcelain`，**只要看到 `D`（deleted）行就先确认"这是我故意删的吗"**；
不是就 `git checkout <last-good> -- <path>` 恢复后再提交。**宁可分两次提交，也不要让 `-A` 替你做决定。**
（同族教训见第 3 条"补丁必须 && 串联 + grep 校验"：都是"别让一条命令替你做它没被告知的决定"。）

## 27. 新守门一律**追加在文件末尾**；门禁结果只看**退出码/尾部输出**，绝不看静默的 grep

**事故（连环两次）**：v1.5.33 的 D44 与 v1.5.34 的 D45 都用"锚点插入"写进了**别的测试块中间** ⇒
`tools/np-test.mjs` 多出一个 `});` ⇒ **SyntaxError，整个守门脚本崩掉**。
更糟的是：两次提交时我的命令都是 `node tools/np-test.mjs | grep -E "✘|通过 " | tail -2`，
脚本崩溃时 grep **匹配不到任何东西 ⇒ 静默无输出**，而我把"没有输出"读成了"通过" ⇒ 坏文件被提交并推送了两次。

**规矩**：
1. **新测试追加在文件末尾**（用 `cat >>` 或"取文件尾部锚点"），**不要**按中间某行做插入 —— 那种锚点在语义上分不清
   "块内"还是"块边界"（D38/D39、D44/D45 都栽在这上面）；
2. 门禁结果**必须看退出码**（`node tools/np-test.mjs > /tmp/x 2>&1; echo $?; tail -3 /tmp/x`），
   **绝不**用 `grep | tail` 的"有/无输出"当判据 —— 静默既可能是"没有失败项"、也可能是"脚本根本没跑起来"；
3. 同族教训（见第 26 条）：**别让一条命令替你做它没被告知的决定**。

---

## D. v1.5.94–99（第九轮复核 + 教师示范接线）新增

28. **"接线在文件里" ≠ "接线在跑的那条路径上"** —— 同一个故障本轮连续出现**三次**，每次都把整臂变成空操作：
    - **env 拷贝**：`worker_threads` 的 `process.env` 是**创建时的拷贝**；而 `EPIRUS_IMIT_GENS` 是服务端在
      `runTrain` 里**事后**派生再写进 env 的 ⇒ worker 永远读到 0
      （`v7ringT` / `v7ringT2` 两批产物与对照 **逐位相同** = no-op）。
    - **函数分叉（真根因）**：runner 的训练 URL 带 `n=5` ⇒ 请求走 **`runTrainN`**（多人路径），
      而全部 IMIT 接线原先**只写在 `runTrain`**（2 人路径）⇒ N 人臂上 `imitB ≡ 0`。
    - **名字静默退回**：`EPIRUS_IMIT_TEACHER=deepsaver` 不是 `BOT_PICKS` 的键（正确名是 `pickDeepSaver`）⇒
      `accepted=false`，而库里 `IMIT_TEACHER || BOT_PICKS['heavyfire']` **静默退回默认教师** ⇒
      整臂**贴着错标签**跑完。
    ⇒ 四条规矩：① 回执必须打**在真正执行的那条路径**上（worker 侧也要打）；② 回执要含**生效值**
      （`gens / frac / teacher / accepted / override / β(gen0)`）；③ **最终反证 = 产物是否与对照逐位相同**
      （确定性流水线让它成为最强判据）；④ 解析与设值**只写一份**（`applyImitEnv`、`setImitPlanByName`），
      两条路径都调它。

29. **"样本敏感性"本身要按包验证，不能当常数用**：第 22 条那条曲线
    （同一个包 n5=3.08 / n10=2.99 / n20=4.24 / n40=4.54）只在**当时那个包**上成立；
    本轮 `v7role1-82` 在 **n=5~80 恒定 1.70~1.71** ⇒ 它的 `G<3` 是**真塌**，不是抽样假象。
    ⇒ 引用旧曲线之前，先在**手上这个包**上重测一遍。

30. **报均值必须同时报逐 seed 胜负**：6 seed 时"角色间熵 +51%"看着像真赢，
    扩到 9 seed 后翻成 **4 胜 5 负** —— 那个 +51% 全靠**对照的一个退化 seed**
    （角色间熵 0.002 = 蓄能囤积到 100%）撑着。⇒ 退化样本会污染均值；**逐 seed 胜负才是结论**。

31. **门的判据要写清"量的是什么"，而且"最坏的那一格/那一座"本身是小样本统计量**：
    - 座位门的判据是**极差**（<30pt），**不是**"有没有座位为 0"。`v7f5t-31` 在 n=400 时极差 26.9pt ✓ 过门，
      可它的**首座夺冠率是 0%**（其余四座 20~27%）。
    - **同一类错误本轮出现两次**：座位极差在默认 n=40.4pt（红）→ n=200 30.6pt（红，只超 0.6）→
      n=400/800 **26.9/26.3pt（绿）**；`G4` 的"最克那一格"在 **n=60 是「坦克线 57%」**，
      而 **n=120 与 n=200 都稳定指向「只枪 53%」** ⇒ **n=60 会选错格子**。
    ⇒ 两条硬规矩：① 阈值落在噪声带时，**判"过/不过"之前先把 n 拉大**；
      ② **报 `G4` 必须同时报 n**（否则"哪一格最克"这个结论本身不可信）。

33. **布尔化的经济判据会被"最小非零"刷分**（第十轮复核 §4-2）：`D78` 原先要求"得珠>0 **且** 花掉>0"，
    而候选② 得珠 835 / **花掉 1**（花/得 **0.12%**）就点亮了"闭环 ✓"，同时每局浪费 **20.9** 颗（≈在位包的 250 倍）。
    这是第七轮 §11 陷阱的**镜像**（当时是"干脆不用 ⇒ 浪费率 0% ⇒ 绿"）。
    ⇒ 规矩：**任何布尔化判据都要先问"最小非零能不能点亮它"**；经济类判据用**双条件**
    （花/得 ≥ 20% **且** 过期/局 ≤ 1），并且**把"囤积"与"使用"在同一个显示里分开**
    （`energy(含空转/过期珠)` —— 否则 25.4% 会被读成"开始用能量类"）。

34. **"偶然性"至少有两种，处置相反**（第十轮复核 §5）：用"解析候选熵 `H` + 采样背离率（1−p_max）"区分 ——
    - **策略内的偶然性**（高 `H`、背离 34%）：同一局面每局一个样 ⇒ **降温度/降 ε 可收窄**，代价是变呆；
      但它的**读数本身带采样噪声 ⇒ 必须报置信区间**。
    - **种子内的偶然性**（`H≈0`、块胜率 `[0,0,0,0]`）：网络已收敛到极峰，**确定地**产出这个形状 ⇒
      **降温度完全无效**，只能换配置 / 换 seed ⇒ **必须报"同配置换 seed 的复现率"**。
    ⇒ `H=0` 还意味着：**对已收敛到单卡的个体，熵/覆盖类项没有牙齿**（我的 `S` 是跨状态覆盖熵，同理会退化 ⇒
    要让 S 有牙齿得用**跨状态**的覆盖量（`roleOf` 8 角色 / 门禁 `G`），而不是同状态内的动作熵）。

35. **报"考卷分"也要报决策样本 n**（第十轮复核 §3-2 的扩展）：有的包里 A 卷只有 **136 个决策样本**
    （该场里它 0 号席常早亡）⇒ 与 `G4`/座位同理，**分数本身也是小样本统计量**。

32. **"产物=证据；没记账的产物等于没有结论"**：整晚 20 个产物零记录，其中一个座位极差 99pt。
    ⇒ 立了守门 **D82**：**24h 内落盘的 `.bak` 必须逐个在 CHANGELOG 被点名**（**缩写不算字面名字**）。
    它随后**多次抓到我自己**（漏点名、用 `v7f2t-{31,…}` 缩写）。

## E. v1.5.129 新增（第三方复核实锤 + 一次"同族的另一半"）

36. **修一个洞时要问"同族的另一半在哪"**（第三方复核 §2-1）：R10 当初裁定"**被无效化的聚能环不续计**"，
    修的是 `ringStreak`；而**同一句原则**落在 `lastSkill` 上的那一半**漏了 8 个版本** ——
    `setVoid` 只置 `voided`、**不动** `outcome`，于是"被雷击之枪废掉的那一手"照样写进 `lastSkill`，
    **白送 R32 激光眼连用**（2 ジ且免爆珠）。⇒ 本次立 **R61** + **D102**。
    - 教训：规则裁定往往是一条**通用原则**（"被废掉的一手不算数"），而实现里它可能有多处**影子记账点**
      （`ringStreak` / `lastSkill` / `cannonCount` …）。修一处时必须把**同族清单**列出来，并**逐条写明"动/不动"及理由**
      （本例：`lastSkill` 动、`cannonCount` 有 R43 明文**不动**）。
    - 而且**"不同族"也要能失败**：D102 的对照组（没被无效化 ⇒ 照旧写）与第 ③ 段（过载炮相位必须仍是 2）
      分别挡住两种假修法（"恒 null"与"把 `!voided` 套到所有计数器上"）。

37. **"已记录的例外"必须与"量具口径"绑定，否则它会变成永久免检**（本次 D67 的改判）：
    用户裁定把 D67 从"硬红"降为"已记录的 `--force` 例外"（动机：长期红会让**真红被已知红淹没**）。
    但例外若只认 `meta.auditForced` 一个布尔，**改完克制表之后旧记录会继续免检** —— 那正是本仓最忌讳的
    "门看着在、其实没判"。⇒ `gate-drafts` 打印**口径 id**（`G4POOL` = 克制表键序 + 阈值 的 sha1 前 8 位），
    `promote-champion` 把它写进 `meta.gate4Pool`，**D67 只在口径 id 相等时才认这条例外**。
    - 一般化：任何"记录在案所以放行"的门，都要能回答**"这条记录是关于哪一版量具的"**。
    - 副作用（**有意**）：本次加了「珠爆发」一格 ⇒ 口径 id 变 ⇒ **旧例外自动失效** ⇒ 逼出一次重记。

38. **判据的"座位数"必须匹配洞的"座位数"**（v1.5.129，本次最有价值的结构性发现）：
    复核 §3 的珠爆发线在 **1v1（2P standard）** 把线上冠军打到 **100% 胜 · 均 9 回合**；
    但把它做成 G4 的第 8 格之后，在 **5 席** harness 里它只有 **30%(long) / 15%(multi)** ——
    **不是最克的那一格**（最克仍是「只枪」75%/62%）。
    - ⇒ G4 结构上**只跑 5 席**，所以任何"1v1 专用洞"它**永远看不见**。两张池子测的是两件事：
      **2P 考卷（`tools/p2-baselines.mjs`，1v1）× G4（`tools/gate-drafts.mjs`，5 席）**。
    - 一般化：写"某某线能打穿它"之前，先问**这条线和这条判据是不是同一张桌子**。
      本次据此**两处都入册**（真正的信号在 2P 考卷那侧：冠军 **0.0%** 胜）。

39. **单点行为全对 ≠ 整条链转得起来**（v1.5.129，我自己踩的坑；与第 25 条同族）：
    我第一版 `pickBeadBurst` 的**每个分支单独看都对**（没珠就蓄、有珠就放炮、买不起就ジ），
    但整条链**死循环**：珠只活一回合（`resolve.js` 的 `keep = p.beadNew || null`）+ 蓄能花 1 ジ
    ⇒ 退化成 `蓄能 → ジ → 珠过期 → 蓄能 → …`，200 局里 `railgun` 出手 **0 次**、伤害 **0.00/局**、胜率 0%。
    - ⇒ 门里必须有一条**端到端**断言（"**链条真的转起来**"）：`np-test D103` 的 ①b 就是它
      （对手只出ジ ⇒ 脚本必须放得出电磁炮；第一版在这里读到 0）。单点断言**抓不到**这种病。
    - ⚠️ 附带的**态度**教训：**"没复现"的第一反应必须是查自己的实现，而不是宣布对方数据错**。
      我这次先量了出手构成（`ji:6000 charge:5800 railgun:0`）才找到根因 —— 若直接下"复核不可复现"
      的结论，就会把一份**真发现**误判成噪声（复核的 100% 与 9 回合在我修好后**逐项吻合**）。

40. **"状态空间被包含" ≠ "策略被包含"**（v1.5.129，回答用户的疑问："2P 的状态完全包含在 3P 里，
    为什么 3P 冠军跑去打 2P 会被暴打？"）。实测：`tools/probe-2p-mismatch.mjs`（n=200/格，座位轮换）：
    - **不是模式**：`multi@N=2` 与 `standard@N=2` 在这个 matchup 里**逐字节相同**
      （多出的 `holo/dualGun/mirror` 一次也没被选中；`suddenDeath 45` 在 6 回合的局里不可达）⇒ 模式被排除。
    - **是均衡错配**：3P 冠军 × 2P 冠军 @ 2 席 ⇒ 3P **16.5%**（6.3 回合）；它的出手是 `ジ 66% + 剑 + 狙击`、
      **防御类 0%**；而 2P 冠军 `ジ 52.7%`、**枪 526 次**（3P 只有 193 次 ⇒ 2.7× 密度差）。
    - **判别性证据**：一行**只枪**（最便宜的密度线）打 3P 冠军 **99.5%**、打 2P 冠军 **0.0%**
      （且对 2P 冠军造成伤害 **0.00/局**）⇒ "会不会防最便宜的密度线"就是两者的分水岭。
    - **反向对照**（把冠军丢进对方的席位数）：2P 冠军进 5 席 ⇒ **0% 胜 / 100% 平**（打不动任何人，
      最后靠终局收缩同时死光）；3P 冠军进 5 席 ⇒ **39.5%**（高于 20% 的公平份额）。
    - 3P 冠军**自己没坏**：自镜像 @ 2 席 = 50/50 · 15.8 回合。
    - ⇒ **策略是"相对某个对手总体"的均衡**，不是状态的函数。2 席与 5 席的差别不在"能看到什么"，
      而在**同一动作的机会成本**：5 席里防御是"给别人让路"、按ジ 等机会是理性；2 席里两者都是
      **把 tempo 送给唯一对手**。所以**同一个包不可能同时服务两套经济学** ——
      复核 §4-3 的"先训 2 席专属包、别用一个包通吃"是**对的方向**，本条给了它量化依据。

## F. v1.5.130 新增（"修一个洞、开两个洞"及其解：热启动）

41. **改造一个包的能力时，"任务变了"必须配"从好包出发"**（v1.5.130；一次 A/B 把两件事分离开）：
    目标 = 堵住 2P 冠军的「珠爆发」洞（`examGateOk:false`，该线 **100%** 打穿）。
    做法 = 把这条线加进 **2P 训练池**（`js/train/evo.js` 的 `BOT_PICKS` / `buildOpps`，w=2.0 ≈ **6.25%** 对局）。
    **同一条池子改动、同一个种子，只差种群起点**，结果完全不同：

    | 种群起点 | 珠爆发 | 旧能力 | 总评 |
    |---|---|---|---|
    | **全随机重开**（原 `makeTrainer`：16 个个体全是 `P.makePolicy(0.25)`） | 0% → **93%**（学会了） | `defend` 100%→**0%** · `reflectspam` 100%→**0%** | avg 89%→**85%** ⇒ **1 个洞变 2 个洞** |
    | **热启动**（`T.seedChampion(t, 现有冠军)`，围绕冠军变异） | 0% → **100%** | 全部保持（`defend`/`reflectspam`/`wall` 均 100%） | avg 89%→**98.3%** ⇒ **0 回归 + 1 修复** |

    - ⇒ **"池子给了梯度"只是必要条件**。从零重开时，寻找新能力会**顺带丢掉**旧能力 —— 那不是设计上的
      权衡，而是"**择优时没人在看旧能力**"。**热启动 + 不回归层**才把"改造"变成"增量"。
    - ⚠️ 同一个 bug 的另一面（工具缺陷）：`train-best` 的择优原先只有 `sc` + 容差带 + 发散度，
      而闸门罚项**在有洞时退化成常数**（`gateMin*0.4-1`，gateMin=0 ⇒ `sc ≡ -1`）⇒ 5 个候选 sc 全 -1
      ⇒ 容差带 = 全部 ⇒ 择优实际由**发散度**决定 ⇒ 它把上面那行"1 个洞变 2 个洞"的包**当成最优写进了线上**。
      ⇒ 现在择优抽成纯函数（`tools/pick-best.mjs`）、加**不回归层**，并立门 **D104**（喂合成候选表验行为）。
    - 一般化（直接回答"能不能把 2P 冠军的能力蒸到 NP 冠军"）：**能力转移的载体是"任务/对手线"，不是权重快照**；
      权重的角色是**起点**，它决定的是"学到新东西时会不会忘掉旧的"。**只搬权重而任务不变 ⇒ 搬来的是别人桌上的均衡**
      （见第 40 条：2P 包的权重丢进 5 席 = 0% 胜 / 100% 平）。

42. **文档里的读数不带指纹 ⇒ 表格会静默过期**（v1.5.131 实锤；与第 37 条"例外必须与量具口径绑定"同族）：
    `rules-fingerprint.mjs` 的立论是"记着旧指纹的成绩**自动变成已过期**"，而 **D16 只把它施加在两个包的 meta 上** ——
    **文档里的表不受任何门保护**。本次实测：`docs/AUDIT-2026-09-19-post-merge.md` 的 V2 列（**34.2%**，指纹 `61a7711c`）
    与今天**同工具 / 同 seed / 同装配 / 同包 / 同模式**的读数（**10.0%**，指纹 `00e732a7`）**差 24pt**，
    而中间只有一次引擎改动（R61）—— 我差一步就把那个 34.2% 当基线用了。
    ⇒ 规矩：**引用任何旧读数前，先问"它当时的指纹是什么"**；跨指纹的表要么重跑，要么在表头写明"指纹 + 日期"。
    ⚠️ 但要与**配对差**分开：同一批 run 内部的**配对差**（如 V2 +15.2pt / p=3e-22）**不受**这条影响
    （两臂同引擎、同 seed、逐局配对）；会过期的是**跨指纹的绝对水平**。

43. **给一条轴立门之前，先量"什么都不做"的水平**（v1.5.132 实测；与"阳性对照"同族，见 §F.41 那个 A/B）：
    本轮要给 V2/V4 立门时，我先量了**零权重常数策略**在同一量具上的读数：
    **V4 = 3.4%/席（远低于线上包的 16.1）⇒ 这条轴能分开好与坏**；
    **V2 = 10.2%，与线上冠军的 10.0% 一模一样 ⇒ 这条轴（现状下）没有判别力** ——
    我上一版草案里的 `V2 ≥ 8%` 于是**连垃圾都挡不住**（近恒真），差一步就在套件里立了一条"看着在、其实没判"的门。
    ⇒ 规矩两条：① **任何阈值都要配一个"地板参考"**（常数策略 / 零权重），并断言它**被挡下**；
    ② **凡是被挡不下的轴，只记录、不阻断**，并加一条"**判别力自检**"（现状必须与地板同档；哪天拉开了再升成阻断门）。
    副产品：这条参考还让旧读数更好读 —— R61 之前 V2 = **34.7%**（**明显高于**地板），现在 10.0% = **就是地板**；
    而 §4-4 的"禁狙击 +15.2pt"是把 V2 抬到 **25.2%**，即**抬到地板之上**。
    ✅ **当天就查清了**（独立 worktree @ `2ccd558`，同工具 / 同 seed / 同包，**引擎是唯一变量**）：
    V2 **34.7% → 10.0%** · V1 85.5% → 85.3% · V4 17.2 → 16.1%/席 ⇒ **那 24pt 就是 R61**；
    且 V2 里狙击出手 **1132 → 716 次**（−37%）⇒ 行为也变了 —— 不只是"数值漂移"。
    ⇒ 由此得**第二条更硬的规矩**：**引擎一改（指纹变），所有"在旧语义下训出来的包"都成了次优解**，
    而**只有当那条轴上有门时你才会发现它** —— V2/V4 **没有门** ⇒ 这次静默掉了 **24.7pt**（两个版本没人发现）。
    补救只有两条：**在当前引擎下重训**，或**明确接受并在表头写清**；但**判断"有没有变差"只能用同引擎的数字**
    （跨指纹的绝对值不可比 —— "34.2%" 与 "10.0%" 说的不是同一件事）。

44. **"权衡/互斥"这类结论必须做因果检验，而不是沿一个旋钮比较两个包**（v1.5.133；本仓栽过的方式很隐蔽）：
    `docs/HANDOFF-2026-09-19.md` §2.1 里有一条被引用了很多轮的结论 ——「**场B 清场 ↔ 抗只枪是单调互斥**」。
    它的证据是：`CLEAR_W` 0.05 的那个包 **G4 ✓ / 场B 0.00**，0.1 的那个包 **场B 1.0 / G4 76~67% ✗**。
    ⇒ 这条读法其实只证明了"**在这个标量化 `base + CLEAR_W·clears` 下，两个最优点各自只能拿一个**"，
    而它被当成了"**游戏里不存在两全的策略**"用了很多轮（连"别在 `CLEAR_W` 上滑"这种结论都由此而来）。
    **怎么判它是不是根本**：**同一个包、同一 seed 序列，只改一个决策规则**（与环消融同法），看两条轴能否同时保住。
    实测（`tools/probe-g4-anatomy.mjs` + `AGGR_HOOK=attack`）：`forceAttack`（**买得起枪却选ジ → 改判开枪**）
    ⇒ 只枪 **75% → 13%**（long）/ **62% → 17%**（multi），而 **场B 清场 1.00 → 1.00**（门槛 0.3）⇒ **两全存在**，
    那条"互斥"只是**旋钮的性质**。
    ⇒ 三条可搬走的规矩：
    ① **横跨两个训练设置的"互斥/权衡"结论，一律降级成"这个旋钮买不到两个都要"**；要下"根本"的判断，
       必须换成**同包同 seed 的单规则干预**（否则协议方差、seed 方差、权重整体漂移全都混在里面）。
    ② 干预要**双向**（这里：`forceAttack` 掉 62pt + `forceTurtle` 升到 100%），并每次报**改判次数**
       （=0 就是**空枪**：这条规则在这个装配里根本没被触发过 ⇒ 那行读数无信息，别读成"改了没用"）。
    ③ 逆命题同样要量：`forceTurtle` 把 **场B 清场打到 0.00**（挂门）⇒ **这条轴有判别力**，
       所以"场B 不掉"才是有内容的读数（否则可能只是量具死了）。
    附带（同一天的两件小事，都是"沉默失效"的形态）：
    · 标量化之前先想清楚**要的是"最优点"还是"两全点"** —— 若产品要两全，标量化的"互斥"是**假二选一**；
      真正的杠杆在**训练信号/训练形状**（这里下一根是"4 席自家族 + 1 席脚本外部人"，而 G4 恰恰是那个形状）。
    · **探针坏了要能被发现**：`tools/probe-aggr.mjs` 读的字段名与真源漂移了很久（读 `atkOld/dealt/taken/rounds`，
      真源给 `atkOldWhitelist/dealtPerGame/takenPerGame/roundsPerGame`）⇒ `undefined.toFixed()` 每行抛错被 catch
      吞成一行「读失败」，而它打印的正是被反复引用的两条轴；它还**从不打印门禁真正判的那一列**（场B 的 `清场/局`）。
      ⇒ 立门 **D106**（真跑一次探针 + 断言无「读失败」且含门禁那一列；**反证跑过**：改坏字段 ⇒ 152/154、exit 1）。
      通则：**打印器必须打印门禁判的那一列**，且"工具能不能用"要由**行为断言**（跑一次）来守，不是钉文本。
