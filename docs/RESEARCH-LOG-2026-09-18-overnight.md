# 通宵自主推进日志（2026-09-18 → 09-19 09:00 停工）

> **这份文件是防失忆用的唯一真相源。** 每完成一步就更新它。
> 如果上下文被压缩：**先重读本文件的 DONE / 不要重复 / NEXT 三节，再继续干活**，不要凭记忆重开实验。
> 约束（用户 2026-09-18 睡前授权）：可以长程训练与方向探索；**只提交并推送到 `qoder` 分支**；
> **绝不换包、绝不动 `js/bundled-champion-3p.js`、绝不 bump 版本号**（那是 ds 的发布动作）；
> 改动一律 env 开关且**默认关闭**；每步跑 `node tools/np-test.mjs`（期望 146/146）+
> `node tools/rules-fingerprint.mjs`（规则未动应仍为 `1391c094`）。

## 现场基线（09-18 23:40）
- `main` = `04e2e3f`（v1.5.115 代记）；`qoder` 已推 3 个 commit：`6848fa9` / `3527f20` / `0c916b3`
- 线上包 sha1 前 12 位 = **`268461f7c9f0`**（不许动）
- 工作区里 `results/*.txt` 是**用户自己的对局记录**，永远不要 `git add`（add 一律点名文件）

## DONE（已做完并**已提交/推送**，不要再做第二遍）
1. **第十三轮分析全部完成**，文档 `docs/OPTIMIZATION-ep-cliff.md`（已推 qoder）。三条实测结论：
   - 在位包 **看不到环**：9909 决策点、ep 直方 44/34/22/**0**/**0**%、峰 ep=**2**、环可负担 **0** 点；
   - arm A `v7ringA1-82` **富死**：ep≥4 占 68.5%、峰 ep 43/99、环可负担 71% 仍 0 次打环、终局余 ep 38.7/86.3；
   - **白送钱 >> 教它赚钱**：环×8 Δ+12.5pt CI[3.8,21.3] vs 加钱+18 Δ**+38.9pt** CI[24.6,53.3]、
     加钱+40 Δ+39.6pt（终局还攥着 38 点）⇒ 起作用的是"**瞬间付得起贵卡**"，不是平时有钱。
2. **根因两处已定位并实测**：`policy.js:202/:379` ep 编码在 **ep≥16 逐位冻结**（16/20/30/40 四档 p(ジ)=0.453、p(攻)=0.378 完全相同）；
   `evo.js` 经济 shaping 全是**封顶阶梯**（`economyStock` 挂 `maxEp` 且在 2C 夹住；`conv` 只数到 2 次 cost≥2）。
3. **5 个独立量具已入库**：`tools/probe-ring-afford.mjs`、`probe-ring-value.mjs`、`probe-cashout.mjs`、
   `probe-wealth-placebo.mjs`、`probe-ep-saturation.mjs`（REPO 路径走 `EPIRUS_REPO`，默认 `./`）。
4. **续跑量具的坑已修并写进文档**：`autoGameN` **不能从中间状态续跑**（重复一次 `startTurn`）⇒
   必须用 `probe-ring-value.mjs` 里的 `resumeN(state, chs, skipStart)`，忠实性自检 8/8 才可用。

## 门禁坑（本轮新踩，写死在这里，别踩第三遍）
- **D77 的 1200 字符硬窗口**：`setEconomyReward` 必须让 `ECON_REWARD_KEYS` 每个键的 `o.KEY != null`
  出现在**函数开头 1200 字符内**（门禁按 slice 查）。⇒ 新旋钮的 `if` 一定写在 setter **最前面**，
  长注释放到 `function` 之前（slice 从函数名开始，前面的注释不计）。
  我第一版写在后部 ⇒ `wallFilter/wallGames` 被顶出去 ⇒ 红；加别名键又会顶出更多 ⇒ **别名为省字符直接不设**。
- **D77 全文扫描**：`EPIRUS_*` 这个字面名**只允许出现在 `server/econ-env.mjs`**，在别的文件里
  哪怕只写在注释里也会被判"第二个读取点"。（本轮 `js/train/evo.js:675` 就因此红过一次。）
- **D82 记账**：24 小时内落盘的每个 `.bak` 必须在 `CHANGELOG.md` 里以**字面文件名**出现（缩写不算）。
  ⇒ **等全部臂跑完**再一次性写 CHANGELOG，把每个产物名逐个列全；否则臂还在长就会一直缺。
  现在缺的是 `v7l2c-82/91/93.bak`、`v7l2h-81.bak`（臂还在跑）。
- 失败标记是 **`✘`** 不是 `✗` ⇒ `grep '✗'` 抓不到红，要用 `grep '✘'`（我在 144/146 那一步被这个误导过一次，
  差点以为"没有红、是跳过"）。

## ⚠ 读数码坑（今天上午我自己差点读错过一次）
- **训练在跑时 `gate-landscape` 的输出整行是 `—` 且判 `✗ (其它)`，连线上包那行也是** ——
  那是 `.training.lock` 存在时它不去读 bundle 的**保护行为**，**不是**"这个包五道门全挂"。
  ⇒ 判产物门地形必须**等臂全部跑完**再跑（臂跑完时锁会被 `ring2-run.mjs` 的 finally 删掉）。
- `eval-5p.mjs` 的 `--field=ringwall` / `targeter` 这类专用场**自带 pickRandom 基线行**，
  一定先看基线再看冠军（本轮就是靠它发现 `ringwall` 没有判别力）。
- 失败标记是 `✘` 不是 `✗`（`grep '✗'` 会什么都抓不到，我已上当一次）。
- 比产物是否相同：`.bak` 的**文件 sha1 不同 ≠ 权重不同**（meta 里有时间戳/自评数字）
  ⇒ 只比 `EPIRUS_CHAMPION_3P` 的 `a/f/fa/h` 段。

## ✗ 已被否证的假设（**别再试**，都是我今天自己提出来又自己打掉的）
1. ~~"奖惩不对称（`STOCK_BONUS` 只有 0.05）是环学不出来的阻力"~~ ⇒ `v7l2s` 把它抬到 **0.15**，
   两个 seed 的**峰 ep 仍然=2**、环可负担 0.00%、兑现率 96~98%。**权重加三倍连门槛都碰不到。**
   结构性结论：多回合计划的代价在**胜率尺度（~1.0）**上，shaping 项都在 0.0X 尺度 ⇒
   **任何 0.0X 的常数都买不到一个多回合计划**；而能看到多回合后果的只有胜率项，它对环的梯度又是零。
2. ~~"在位包见人就狙所以被点名打死"~~ ⇒ 狙击只占 5.0% 出手；真因是**防御预算 12.6%**（随机是 70.8%）。
3. ~~"conv/余款惩罚被防御卡 captured"~~ ⇒ 那只龟兑现率 97%、cost≥2 出手 **0%** ⇒ 两项在它身上**零出力**；
   龟是**裸胜率项**在低压迫场里推出来的。
4. ~~"池子大半对手不打人"~~ ⇒ 只有 **3/17** 低压迫（farmer/guardspam/wall）；原话已收回。
   但捞到一条新的：`ringspam` 伤害 12.58/回合**却给随机席 54.2% 夺冠率** ⇒ **伤害高 ≠ 压迫强**。
5. ~~"反环能力有读数（53.3% vs 42.5%），值得把资源挪过去"~~ ⇒ 那两个数出自 `ringwall`，
   而 `ringwall` 在 n=200 下**冠军 45.0% < 随机 51.5%** ⇒ 该场无判别力，建议依据已撤销。
6. ~~"argmax 恒为狙击是因为狙击买不起"~~ ⇒ `SNIPE` 费用=2（不是 4），峰 ep 正好=2 ⇒ 一攒到就花掉。

## 不要重复做的事（都已经有结论了，重跑是浪费 GPU）
- ❌ **不要再跑"强迫打环/环奖励"臂** —— 本轮两条独立否定 + 之前四次失败同型（详见 `OPTIMIZATION-ep-cliff.md` §3/§5 L3）。
- ❌ 不要拿"环可负担率 76.8%"当读数 —— 那是 **ds 旧包/旧口径**；在位包实测是 **0%**。
- ❌ 不要报"每对 SE"当置信区间 —— 必须**按局 bootstrap**（30 快照只来自 6 局会造出假的 +15pt±2SE）。
- ❌ 不要用"打 ジ n 手"当安慰剂：那条线本来就在打 ジ ⇒ 与对照**逐字相同**，不是独立臂。
- ❌ 不要"修" `state.js:191` 与 `resolve.js:645` 两处 `ringStreak++` —— 查过是**误报**（分别管打环者本人与镜面复制者）。
- ❌ 不要重复跑 `probe-ep-saturation` / `probe-wealth-placebo` 的同一批读数来"再确认一遍"。

## NEXT（按顺序做，做完一件就划掉并把实测值写回 DONE）
- [x] **N1 做完**：L1 表征层验证 ⇒ `tools/probe-ep-encoding.mjs`。实测三条：
      M1 现状三种 ep 编码在 **ep≥16 的那 6 档（16/20/24/30/40/60）只产出 1 个不同值**（完全塌缩）；
      三个提案（log1p / √ / 两段线性+压缩）都是 **6/6 可区分**。M2 真实中局面上整条 213 维状态向量：
      arm A 192 输入 → 146 个不同向量（可区分度 76%）、线上包 → 109（57%）。
      M3 **精确改点定位**：状态侧只有下标 **[2, 18, 54]** 随自己席位 ep 变化（对手 ep 那几维要另改）。
      ⇒ 结论：换编码只解决"能不能表示"，"会不会用"仍需重训（换代代价 = FEAT_S 语义变 ⇒ 旧包不兼容）。
- [ ] **N1′（新发现，待做）**：`probe-leftover.mjs` 的判据读数显示**在位包不是"不会攒"，是"攒不到就花光"**：
      线上包 multi 兑现率 **94%**、余 ep/人 **0.9**、峰 ep 2；arm A multi 兑现率 **23%**、余 ep/人 **23.2**。
      ⇒ 两包的病**方向相反**，所以"余款惩罚"对线上包是**空操作**（没余款可罚），只对 arm A 型有效。
      待验：给在位包那一侧的处方是"低 ep 时的攒"，但 STOCK_BONUS 上限只有 0.05、而 tempo 损失在胜率尺度上是
      1.0 量级 ⇒ **推断：这个不对称才是环学不出来的真正阻力**（需要一次"提高 STOCK_BONUS 会不会买到环"的臂来证）。
- [ ] **N1″（原 N1）** L1 的**表征验证**：不改训练，只在 `probe-ep-saturation.mjs` 里加一个
      `EPIRUS_EP_FEAT_CAP=<新上限/编码>` 的**影子前向**（同一份参数、只换 ep 编码），看 16/20/30/40 四档
      是否不再逐位相同。⚠ 旧包参数是在旧编码下学出来的 ⇒ 换编码必然错乱，所以这里只看**输入是否可区分**，
      不看胜率；判据：p(ジ) 在 ep 阶梯上**不再逐位相同**即成立。
- [x] **N5′ 已修 `probe-convert` 的收入饥饿判据**（§1 报的那条量具 bug）：`realCost()` 改走 `computeCost`
      的实际 ep，并把"0 个可负担点"的卡也纳入 ⇒ 线上包现在正确报出
      `【收入饥饿】= 聚能环 0.0% · 摄魂指法 0.0% · 真正的落雷 0.0% · 避雷针 0.0% · 地雷 0.0%…`
      （原来恒为"无"，因为环声明的 `cost` 是 `null` 且 ready=0 时它连 `list` 都不进）。
- [ ] **N2** 实现 L2′：`evo.js` 里
      ① `rec` 加 `leftEp`（终局余款）与 `epGained/epSpent`；② `economyStock` 自变量 `maxEp`→`leftEp`（开关 `EPIRUS_HOARD_ON_LEFTOVER=1`）；
      ③ `conv` 改比率 `已花/已获得`（开关 `EPIRUS_CONV_RATIO=1`）；④ `HOARD` 饱和点 `2C`→可配 `EPIRUS_HOARD_CAP_MULT`（默认 2，臂上试 4）。
      全部默认关闭 ⇒ np-test 146/146、指纹 `1391c094` 不变；np-test 里给新纯函数补一条 D 断言（门槛语义单测，学 D15 的路子）。
- [ ] **N3** 跑臂：对照臂（开关全关，复现今天的行为作为**回归基准**）+ ② 单开 + ②③④ 全开。seed 31-33 起步。
- [ ] **N4** 判据（三条都要，缺一即否）：产物**终局余 ep 显著低于在位包**（现值：multi 38.7 / long 86.3）·
      **A 卷分数不掉** · **出手/回合上升**。另用 `probe-convert` / `skill-report` / `champ-audit` 交叉。
- [ ] **N5** 写 `docs/AUDIT-RESPONSE-2026-09-19-overnight.md`（给 ds 的交接：改了什么、实测、否定什么、他接手要注意什么），推送。

## ★ 今夜最重要的发现（09-19 00:17，实测）：**环的价值是"条件反号"的**
高 ep 窗口（`ep≥16 · hp 健康 · 近 2 回合没挨打 · 环可负担`）上做配对反事实
（`tools/probe-ring-highep.mjs`，24 快照/12 局，每格 n=192 对，按局 bootstrap 95%CI）：

| 被测包 | 对照胜率 | 环×4 | 环×8 | 白送+12 | 白送+18 |
|---|---|---|---|---|---|
| `v7ringA1-82`（arm A：会连打 40 手 ジ 的那种）**multi 3 血** | 9.4% | +10.4pt[0.0,21.4] | −3.6pt 跨0 | **+11.5pt**[3.1,19.3] | **+24.0pt**[13.0,34.9] |
| `v7ringA1-82`（同上）**long 5 血** | 12.0% | **+31.3pt**[11.5,51.0] | **+43.2pt**[19.8,64.6] | +34.9pt[7.3,58.9] | +13.0pt 跨0 |
| `v7l2c-82`（对照臂：兑现率 69%、会花钱的那种）**multi** | 0.0%（3 快照，样本太薄）| 0.0 跨0 | 0.0 跨0 | 0.0 跨0 | 0.0 跨0 |
| `v7l2c-82`（同上）**long** | **86.3%** | +2.5pt 跨0 | **−52.5pt**[−80.0,−25.0] | **+3.8pt**[1.3,6.3] | +3.8pt[1.3,6.3] |

⇒ **同一个动作，在两种包上符号相反，而且只在 5 血长程里相反**（3 血 multi 里环×8 根本不显著）：
  · 对"没别的事可做"的包（它本来就在连打 ジ），按 8 手环等于把闲置回合变现 ⇒ **+43pt**，
    量级与"白送 12 点"同级 ⇒ 环的收入在**这个窗口里是真钱**；
  · 对"会花钱"的包（该窗口里它已经赢 86%），交出 8 个回合 ⇒ **−52pt**，而白送钱仍是 **+3.8pt** ⇒
    环的**机会成本**（8 手不出手）才是它输掉的东西，不是它的收入。
⇒ 这把 §3 那句话（"环的好处全被多攒 ep 解释"）**收窄**了：正确的说法是
  **"环值不值，取决于被它挤掉的那几手本来值多少"** —— 一个只在退化状态下才划算的动作，
  健康种群永远不会选它。**这就是"恢复聚能环"反复失败（本轮 2 臂 + 之前 4 次）的统一解释。**
⇒ 推论（写给 ds，也写给我自己）：
  ① 不要再加"环奖励/环示范"这一族任何旋钮 —— 它优化的窗口只在"包已经很笨"时才存在；
  ② 想让环在**健康包**身上成立，只能改规则（降低 tempo 成本：例如"环与另一张 0 费资源卡共奏"
     或"链期间不被小雷点名"），而用户裁定过**规则不用改** ⇒ 该目标应**正式作废**，
     并把资源挪到"反环能力"（G4 那一格，在位包反环考卷 53.3% vs 旧包 42.5%，那条有读数）；
  ③ 这条也顺手解释了为什么 `白送钱` **每一格都不亏**（+3.8 ~ +34.9，四格全正或跨0但从不为负）：
     **缺的从来不是钱，是把钱花对的回合数**；而 3 血模式下"交出 8 个回合"这件事本身就买不起
     ⇒ **环的可行性是血量/模式的条件函数**（若用户哪天愿意改规则，"环只在长程成立"这条可以当设计依据）。
  ⚠ 采样口径记清楚：`multi` 那两行是同一窗口同一批 seed，但 3 血下"hp≥2 且近 2 回合没挨打且 ep≥16"
     的快照本来就少（`v7l2c-82` 只采到 3 个）⇒ **multi 的"不显著"是样本不足，不等于"无效应"**，别反过来用。

## ★ N4 第一份结果：**L2′-① 在这套配置下是空操作**（null result，有机理解释，不是"没生效"）
`v7l2c`（对照，开关全关）vs `v7l2h`（只开 `EPIRUS_HOARD_LEFTOVER=1`），同池同 seed：

| seed | 对照 | 处理 | 权重 sha1 | 判据读数 |
|---|---|---|---|---|
| 91 | `v7l2c-91` | `v7l2h-91` | **`bbf5494a0d2d` 两边逐位相同** | 兑现率 98%/99%、余 ep/人 0.2 —— 八个数字全等 |
| 93 | `v7l2c-93` | `v7l2h-93` | `f11cbc7b` vs `cdd1e94d`（不同）| 行为读数仍与对照**逐项相同**（兑现率 97%→90%、余 ep 0.5→1.2）|
| 81 | **被健康门槛拦下** | **过门落盘**（自评 0.4404）| — | 唯一一次两臂路径真的分叉 |
（接线凭据：`ring2-server-l2h.log` 里 **64 行** `[econ] worker 生效值 ... "hoardOnLeftover":true` ⇒ 开关确实到了 16 个 worker。）

**机理解释（这条比"没效果"本身更重要）**：这套配置**根本不产出囤积包** ——
对照组自己就兑现 **97~99%**、终局余 ep **0.2~0.5**，而惩罚门槛在 `C=10`
⇒ **罚不到任何东西** ⇒ fitness 全程一字相同 ⇒ seed 91 长出**同一个冠军**（权重逐位相同就是证据）。
⇒ 那么 arm A 那个"兑现率 23%、峰 ep 43"的囤积标本是怎么来的？—— **是 `IMIT_TEACHER=pickDeepSaver`
   + `IMIT_OVERRIDE=0.5` 教出来的**（deepSaver 本身就是"86% ジ 攒钱"那个脚本）。
   即：**囤积是教师注入的，不是池子或 shaping 逼出来的**。
⇒ **而且这条归因是实测的，不是推断的**：`v7ringA1-*`（arm A）与 `v7l2c-*`（今夜对照）
   的配置**只差 `EPIRUS_IMIT_*` 一组 env**（同池 = E+gunspam 17 名 · 同 `CLEAR_W=0.05` · 同 `SEAT_GAMES=24` ·
   同 250 代 · 同 seed 号），其余一字不差 ⇒ 两者的差就是教师的效应：

   | 读数 | `v7ringA1-82`（有 deepSaver 真示范 0.5） | `v7l2c-91/93`（无教师） |
   |---|---|---|
   | 终局兑现率（已花/已获得） | **23%** | **98% / 97%** |
   | 人均终局余 ep | **23.2** | 0.2 / 0.5 |
   | ep≥4 的决策点 | 68.0% | 0% |
   | 峰 ep | 43 | 2 |
   | 打环次数 | 0 | 0 |

   ⇒ **"ep≥3 主导 / 攥着钱不花"这个病是 deepSaver 教师注入的**（它就是"86% ジ 攒钱"那个脚本），
     不是池子、不是门槛、也不是 shaping 逼出来的。⇒ 也顺带说明：**判据①（降余 ep）在这套无教师配置上无物可测**。
⇒ 所以 L2′-① 的**正确实验对象**不是这套无教师配置，而是 **arm A 那套配置本身**：
   拿 arm A 的配方（含 deepSaver 真示范）+ 三个开关，看能不能把 23% 的兑现率治上去、
   同时 A 卷不掉。**已排进批次 2**（臂 `v7l2a`，它的对照就是已经训好的 `v7ringA1-*` 六个产物，不用重跑）。
⚠ 记一句防重复：`v7l2c-91` 与 `v7l2h-91` 权重逐位相同 ⇒ **不要拿它们当"两个独立产物"去做 A/B 统计**，
   那是一个样本；同理 `.bak` 的 sha1 不同**不代表**权重不同（meta 里带时间戳/自评数字）——
   要比权重必须把 `EPIRUS_CHAMPION_3P` 的 `a/f/fa/h` 段单独取出来比（本轮我用这个方法才看穿）。

## ★★ 今夜最大的发现（00:32）：**线上包在"会反制人"的场里低于随机** ⇒ 真正的弱点轴找到了；
### 而 `ringwall`（考卷2）根本判不了这件事 —— 顺带**推翻我昨天自己写进文档的一条建议**
`--mode=long --field=targeter`（4 席 `pickTargeter`：谁用狙击/刚放冷枪/攒满大雷就瞄谁），
`node tools/eval-5p.mjs 120 5 88100 <包> --mode=long --field=targeter` 的实测（1st 率）：

| 被测包 | 1st | top2 | 备注 |
|---|---|---|---|
| **pickRandom（基线）** | **24.2%** | 31.7% | 5 席基线本应 20% |
| **线上包 `v7new5_005-31`** | **9.2%** | 15.8% | **低于随机 15pt** ⇒ 它不是"强但可被克制"，是**在这个轴上主动比什么都不做还差** |
| `v7l2c-91`（对照） | 2.5% | 2.5% | 与 `v7l2h-91` **逐位相同权重** ⇒ 两行读数一字不差，正好当测量确定性检查 |
| `v7l2h-91`（处理） | 2.5% | 2.5% | 同上（这格证明"处理无效"是真的，不是噪声） |
| `v7l2c-93`（对照） | 14.2% | 45.0% | — |
| **`v7l2h-93`（处理：只开余款惩罚）** | **40.0%** | **66.7%** | **同 seed 唯一差 = 开关 ⇒ +25.8pt** |
| **`v7l2f-81`（全开）** | **51.7%** | 78.3% | 今夜最好的一个包 |
| `v7l2f-82`（全开） | 6.7% | 8.3% | — |
| `v7ringA1-82`（arm A） | 0.8% | 3.3% | — |

⇒ 两件事同时成立：
1. **这个场有真正的判别力**（读数从 0.8% 一路铺到 51.7%，而随机基线 24.2% 在中间），
   而**考卷2（`ringwall` = 4 席只攒不还手的 ringspam）没有**：n=200 下线上包 45.0% **输给随机 51.5%**
   ⇒ 一个"随机比冠军还高"的场，它的读数不能用来排任何产物的好坏
   —— **我昨天在 `OPTIMIZATION-ep-cliff.md` §5-L3 写的"资源挪到反环能力，那条有读数 53.3% vs 42.5%"是错的**，
   那两个数就出自 `ringwall` ⇒ 必须撤掉/改写（已在本轮更正提交里处理）。
2. **L2′-①（终局余款惩罚）在 seed 93 上把"被反制"这一轴从 14.2% 抬到 40.0%**（同 seed、同池、只差开关），
   seed 91 上则完全无效（两边长出同一个冠军）。⇒ 效应**存在但高度 seed 依赖**，n=2 配对远不够下结论。
   下一枪就是这个：**同配置铺 12 个 seed**，比较"处理 vs 对照"在 targeter 场上的**分布**，
   而不是比某一个 seed（METHODOLOGY §31 的同一条教训：小样本统计量不能当判据）。

## ★ 机制追踪（00:39）：线上包在 targeter 场里输的**原因不是我猜的那个**（猜错了，实测推翻）
`tools/probe-targeter-why.mjs`（60 局长程 · 4 席 `pickTargeter` · 逐回合归因）：

| 读数 | 线上包 | pickRandom 基线 |
|---|---|---|
| 胜率 / 死亡率 | 6.7% / **95%** | 26.7% / 78% |
| 死时回合 | 100.2 | 99.9 |
| 出手构成 | **ジ 62% · 激光剑 20%** · 全息 7% · 狙击 5% | ジ 21% · **八卦 20% · 防御 20% · 反弹 20%** |
| 防御族占比 | **12.6%** | **70.8%** |
| 狙击出手占比 | 5.0% | 0.1% |
| 挨打的回合比例 | 5.6% | 3.9% |
| 挨打回合平均被几席打 | 1.51 | 1.74 |
| 挨打后下一回合还手率 | 26.7% | 18.8% |

⇒ **我先前两条假设都被否证**：
  ① "它见人就狙 ⇒ 被点名打死" —— 错。它的狙击只占 5.0% 出手。⚠ 我最初补的那句解释（"狙击 4 费、它峰 ep=2 ⇒ argmax 落在买不起的卡上"）**也是错的**：`rules.js` 里 `SNIPE` 费用=**2**，所以 ep=2 就买得起；正确说法是"一攒到 2 立刻被狙击花掉 ⇒ 永远跨不到环要的 3"，这才与"兑现率 94%、峰 ep=2"自洽。
     而它 ep 峰值只有 2 ⇒ argmax 长期落在一个买不起的卡上**，实际退化成 ジ/剑 的乱攒乱打）；
  ② "它被 3~4 席集火" —— 也错。平均只被 1.51 席打，比随机（1.74）还低；它**还手率反而更高**（26.7% vs 18.8%）。
⇒ **真正的原因是防守预算**：随机把 **70.8%** 的出手给防御族，线上包只给 **12.6%** ⇒ 随机死亡率 78%、
  线上包 **95%**，而这类"对手也不猛"的场里活到最后就是赢。
⇒ **方法论警告（这条比单个读数更重要）**：**由"不施压的脚本"组成的专用场，量的其实是"谁最会龟"，
  不是"谁会打"** —— `ringwall`（4 席只攒不打）与 `targeter`（读招反制但同样低压迫）都有这个毛病，
  所以它们的"随机基线特别高"不是巧合。⇒ 用这类场排产物好坏时**必须**同时报随机基线（`tools/l2-eval.mjs` 已强制这么做），
  而且**别把"低于随机"直接读成"弱"**：至少要说清"它输在防守预算"。
⇒ 给 ds 的可执行问题：在位包的 F 门（被集火还手率 15%<20%，`v1.5.114` 换包时 `--force` 越过的那条）
  与本探针的 26.7% 是**不同口径**（这里按"挨打回合的下一回合是否反打到打人者"）⇒ 不矛盾，但值得统一；
  真正要问的是：**防守预算 12.6% 是否偏低**（用户裁定过"全息屏障不能给自己"，所以防御族里少了一张可用牌）。

## ★★ 第二个陷阱（00:41）：L2′ 在 targeter 轴上的"成功"**是把包养成了乌龟** ⇒ 判据必须加"防守预算"这一栏
把上面那张机制表按防御族占比排开（各 50 局，同一场同一 seed）：

| 包 | 防御族出手占比 | 该场胜率 | A 卷 |
|---|---|---|---|
| `v7l2c-91`（对照） | 17.5% | **0.0%** | 48.4% |
| 线上包 | 12.6% | 6.7% | 45.1% |
| `v7l2h-93`（只开余款惩罚） | 27.6% | 36.0% | 48.3% |
| pickRandom | 70.8% | 26.7% | 8.0% |
| **`v7l2f-81`（三开关全开）** | **82.3%** | **56.0%** | **36.4%**（比在位包低 10pt） |

⇒ 防御占比与胜负基本单调（随机略偏出，因为它的防守是不分对象的乱摁）。
⇒ **所以 `v7l2f-81` 那个 +27.5pt 不是"学会了资源线"，是"变成了乌龟"**：
  出手构成 八卦 28% + 防御 27% + 反弹 27%，只出手 9% 给ジ、9% 给枪。
⇒ 机理也说得通，而且是**奖励设计上的通病**：
  `conv` 比率问的是"钱花出去了没有"，`余款惩罚`问的是"结束时手里有没有剩"——
  **两张问的都是"花没花"，没一张问"花在进攻上没有"**；而防御卡**随时买得起、永远不亏**
  ⇒ 于是这两项一起被"最便宜的常驻出口"（防御族）**captured** ⇒ 典型的 reward hacking 形状。
⇒ **修法（今夜已实现成第三个开关）**：`EPIRUS_CONV_OFFENSE=1` ⇒ 兑现比率只统计
  **花在 `R.ATK_EFFECT`（会造成伤害的卡）上的 ep** / 已获得。判据也随之加一栏：
  **防守预算必须落在在位包附近（12.6%）与随机（70.8%）之间的合理带，不许 ≥50%**，
  否则"A 卷没掉 + targeter 变强"两条都可以被龟壳同时满足。
⇒ 顺带一条给用户的设计问题（不改规则也能问）：一个 82% 时间在摁防御的电脑对手，
  线下 4~7 人局里**根本没人想跟它玩** ⇒ "龟"即便赢指标也不该要。

## ⚠ 01:02 自我否证：上一条"conv 被防御 captured"的解释**错了**（见交接文档 §5 的更正块）
实测 `v7l2f-81`（82.3% 防御那只）：兑现率 **97%**、cost≥2 出手 **0%**、峰 ep **1**、余 ep/人 **0.2**
⇒ 两个新旋钮在它身上**既不给分也不罚分** ⇒ 龟是**裸胜率项**在低压迫场里推出来的，不是 shaping 喂出来的。
⇒ `EPIRUS_CONV_OFFENSE` 不是这个病的解药（它治的是"把乱花小钱也算成兑现"）。
真正的对策：① 把"防御族出手占比 ≤30%"做成**门**（不是奖励）；② 给训练场加压迫。
另外把"怕反制"从个案升级成**系统性**：50 局快筛 11 个产物，**9 个低于随机基线 28%**，
且这些包的防御族占比几乎全是 **0.0%**（ジ 57~71% + 狙击 13~28%）。

## ★★★ 01:07 头对头（`tools/head2head.mjs`，60 局轮座·同 seed 同盐·正反两向）
`v7l2f-81`（那只 82.3% 防御的龟）**真的比线上包强**：
  被测 1 席 vs 线上包 4 席 = **28.3%（multi）/ 51.7%（long）**（基线 20%）
  反向（线上包 1 席 vs 它 4 席）= 线上包只剩 **10.0% / 8.3%**
⇒ 两向一致 ⇒ 不是专用场骗人。**但我没换包**（那是 ds 的发布动作，用户也没让我换）；
  代价记清：它 cost≥2 一张不出（0.0%）、峰 ep 1、A 卷 36.4%（比在位包 −10pt）、
  且与用户要的"技能多样性"正面冲突 ⇒ 交给用户在"强度/多样性/好玩"之间定权重。
另：`skill-report.mjs` 的"使用="列此刻对**线上包也全是 0.0%** ⇒ 那是量具退化，别拿它当证据；
  出手构成一律以 `probe-targeter-why` / `probe-leftover` 为准。（跑它还会改 `docs/skill-report.html`，
  我已经 `git checkout --` 还原，别把那个生成文件的改动一起提交。）

## 01:17 champ-audit 把"要不要这只龟"从价值观问题变成账本问题（n=20/列，方向可信）
  列                    线上包        候选 v7l2f-81
  A 考卷 1st / 严胜     47.7/47.7     40.7/38.4      ← 线上包
  D 长程反弹墙          80%           **0%**          ← 它完全不会破墙
  H 混合场(4 风格同场)  60%           **0%**          ← 差距悬殊
  G 有效技能数          8.05(10种)    3.03(6种)       ← 多样性差 2.7 倍
  F 被集火还手          14%           **44%**         ← 唯一大赢的一格（就是 v1.5.114 --force 越过的那条）
  C 盾/局               2.5           0.1 ;座位极差 27pt → 24pt ;I 对被动 都 100%
⇒ **不是"强度 vs 好玩"的取舍：它只修了 F 一格，把 D/H/G 三格修坏了** ⇒ 不建议换包（我也没换）。
⇒ 同时给我自己上一条结论加限：`targeter` 是**好探针**（能照出"只会攒和狙"的偏科）但**不能当总判据**
  —— 0% 破墙、0% 混合场的包照样能在它上面刷高分。§5 里"正确的靶场是 targeter"要读成
  "当前最没被利用的探针"。
⇒ 留给明早的问题（值得单开一臂）：**能不能只要 F？** 即在训练场里放"会集火"的对手（`targeter`/`gunspam` 多席）
  而不是靠 shaping 把包养成龟 —— 脚本已备好（`probe-pool-pressure.mjs` 的 `HIGH` 池）。

## 01:12 两条降级（我自己的话，被自己的量具打回来的）
1. **"龟是低压迫训练场奖励出来的"—— 前提只成立一半。** `node tools/probe-pool-pressure.mjs 24 E17`：
   池 E17 里造成伤害/回合 <1.0 的只有 **3/17**（farmer 0.00 / guardspam 0.00 / wall 0.42）
   ⇒ "大半对手不打人"是**错的，收回**；不过 `ringspam` 很特别：伤害 12.58/回合**却给随机席 54.2% 夺冠率**
   ⇒ **"伤害高"≠"压迫强"**（它大多在打同类）。含≥1 个"留活路型"对手的抽样概率约 73%（已算），
   但"因此学会龟"**未证明** ⇒ "加压迫"从结论降级为**批次 4 的待验假设**。
2. **A 卷与头对头又打架**（`v7l2f-81` A 卷 36.4% 却头对头两向吊打在位包）⇒ 判据顺序改成
   **头对头第一、A 卷只当辅助**；"A 卷没掉"不再算充分条件。

## 排程协议（**必读：违反会静默污染产物归属**）
- ⛔ **同一时刻只允许一个 `ring2-run.mjs` 在跑**。它用**共享**的暂存文件
  `docs/artifacts/.training-in-3p.js` / `.training-out-3p.js` 传起点与产物 ⇒
  两个臂批并发 = A 臂的包可能被写进 B 臂的文件名里，而且**不会报错**（v1.5.55 的 prevSha 守卫只查连续同字节）。
- 我已启动过一个"排程器" `tools/l2-schedule.mjs`（等 `l2-batch2.log` 里的 `BATCH2 COMPLETE` ⇒
  自动跑批次 3 的 `o` 臂 + 评测矩阵 + 资源线矩阵，最后往 `l2-batch2.log` 追加 `NIGHT_DONE`）。
  它的启动命令返回了 exit=1（`&`  detached 的包装层退出），**不确定那个 node 进程还活着**。
- ⇒ 批次 2 结束后的正确做法：先 `test -f docs/artifacts/l2-schedule.log` 并看它有没有新行 ——
  **有** ⇒ 排程器活着，就让它跑完，我等 `NIGHT_DONE`，**绝不再手动起臂**；
  **没有** ⇒ 排程器已死，我自己按顺序起：
  `L2_ARMS=o L2_SEEDS=81,82,91,92,93 node tools/l2-arms.mjs`
  → `node tools/l2-eval.mjs 120`（三考卷 × 全产物，自带随机基线）
  → `node tools/probe-leftover.mjs 60 js/bundled-champion-3p.js docs/artifacts/v7l2*.bak`。
- 批次 3 的臂是 `v7l2o`（全开 + `EPIRUS_CONV_OFFENSE=1`，治龟壳）；它的判据三条一起看：
  **targeter 场仍高于随机** · **A 卷回到 ≥44%** · **防御族出手占比回到 12~30% 带内（不许 ≥50%）**。

## DOING（01:19 快照 · 重启前先读这一节）
- 正在跑的后台任务：
  ① **批次 2 臂批**（`tools/l2-arms.mjs`，日志 `docs/artifacts/l2-arms-status.log` + `ring2-status-l2{c,h,f}.log`）
     进度：`v7l2c` 新 seed 101~106 完 → `v7l2h` 101~106 完 → **`v7l2f` 101~106 正在跑** → 之后 `v7l2a`(教师+开关) 与 `v7l2s`(只抬攒钱上限)。
     完成标记：`docs/artifacts/l2-batch2.log` 里出现 `BATCH2 COMPLETE`。
  ② **排程器已废弃，不要再用**。第二个实例我用 TaskStop 杀掉了；第一个（16:55 detached，包装层 exit=1）
     死活不可知 ⇒ 我把 `tools/l2-arms.mjs` 里那只臂的 key 从 **`o` 改成 `o2`** 拆弹：
     万一那个 stray 排程器还活着，它写死的 `L2_ARMS=o` 会匹配不到任何臂 ⇒ 空转（它后续的 eval/leftover 只读，重复跑无害）。
     ⇒ **批次 3/4 由我手动起**：`L2_ARMS=o2,p L2_SEEDS=81,82,91,92,93 node tools/l2-arms.mjs`。
     ⇒ 教训（写死）：以后**不要**用 `&` 起需要独占共享资源的长任务 —— 起不活你不知道，活着了你也不知道。
     要么用可追踪的后台任务（能 TaskStop），要么干脆串行做。
  ③ **audit 汇总表**（`docs/artifacts/audit-table.log`）：对全部产物跑 `champ-audit`（20+20 局），
     用来回答"能不能只要 F"；与训练并行只是慢一点，不冲突（它不碰 `.training-*` 暂存）。
- 还没做的（按顺序）：
  1. 等 ①②③ 收尾 ⇒ 跑 `node tools/l2-eval.mjs 120`（三考卷 + 强制随机基线）与 `node tools/head2head.mjs 60`（全产物头对头）。
  2. **批次 4（验我那条降级后的假设）**：用 `tools/probe-pool-pressure.mjs` 里那档 `HIGH` 高压池训一臂，
     看"只要 F"能不能不靠养成龟拿到（判据：F↑ 且 D/H/G 不低于线上包）。
  3. 补 `CHANGELOG.md` 的 D82 记账段（**逐字列全** 24h 内所有 `.bak`，缩写不算）⇒ 目标 np-test **146/146**。
  4. 填交接文档 §8（批次 2/3 的 12-seed 分布 + `v7l2o`/`v7l2s`/`v7l2a` 结果），最后再推一次。
- 01:38 更新（头对头全量已跑完，见 `docs/artifacts/h2h-sweep.log` 与交接文档 §9）：
  最强挑战者 = **`v7l2c-82`**（正向 45.0%/46.7%，对基线 15%；A 卷 51.4 · F 34% · D 100% · 墙伤 17.9 · G 4.11/7种），
  次强 = `v7l2c-103`（43.3/41.7 · G 5.61/10种 · 座位极差 6pt）与 `v7l2s-91`（43.3/46.7）。
  ⚠ 三者都出自**没开任何新开关**的臂 ⇒ L2′ 目前没有被证实的强度贡献。
- 剩余步骤的**确切命令**（按顺序，串行；`promote-champion` 与 `gate-landscape` 都必须等锁消失）：
  1. `L2_ARMS=o2,p L2_SEEDS=81,82,91,92,93 node tools/l2-arms.mjs`         # 批次 3/4
  2. `node tools/l2-eval.mjs 120` ; `node tools/head2head.mjs 60`            # 全量评测（新产物出来后重跑）
  3. `node tools/probe-leftover.mjs 60 js/bundled-champion-3p.js docs/artifacts/v7l2o-81.bak ...`
  4. `node tools/promote-champion.mjs --dry docs/artifacts/v7l2c-82.bak`      # **只 dry**，看门禁怎么说
  5. `node tools/gate-landscape.mjs docs/artifacts/v7l2c-82.bak js/bundled-champion-3p.js`
  6. `node tools/l2-bookkeep.mjs` ⇒ 把名单贴进 `CHANGELOG.md` ⇒ `node tools/np-test.mjs` 目标 **146/146**
  7. 填交接文档 §8/§9 ⇒ commit+push ⇒ `UpdateGoal complete`
  ⚠ 已验证过的行为：`promote-champion` 在锁存在时**连 `--dry` 都拒绝**（正确，别绕）。
- ⛔ 全程红线：不换包、不动 `js/bundled-champion-3p.js`、不 bump 版本号、不 `git add results/`、只推 `qoder`。
- **N3 三臂正在跑**（`tools/l2-arms.mjs`，后台，约 35~40 分钟）：
  `v7l2c` 对照（开关全关）→ `v7l2h`（只开 `EPIRUS_HOARD_LEFTOVER=1`）→ `v7l2f`（+`CONV_RATIO=1`+`HOARD_CAP_MULT=4`）。
  共同配置 = 池 E+gunspam(17) · `CLEAR_W=0.05` · `TRAIN_SEAT_GAMES=24` · **无 imit 教师**
  （上一轮 arm A 开了 `IMIT_OVERRIDE=0.5` 会覆盖 50% 早期决策 ⇒ 会把 shaping 效应糊在教师示范里，测不干净）。
  进度看 `docs/artifacts/l2-arms-status.log`；每臂另有 `ring2-status-<tag>.log` / `ring2-server-<tag>.log`。
- ⚠ 已知现象（不是 bug）：对照臂 seed 81 被**健康门槛**拦下（`G 有效技能数 1.43 < 3`、以及一例 `回合 41.4 > 40`）
  ⇒ 无 imit 教师时这配置更容易长窄。若某臂 6 个 seed 全被拦，就改用 `EPIRUS_ALLOW_HEALTH_FAIL=1` 重跑**并显式记账**。
- **接线凭据**：worker 只在 `hasEconOverride` 为真时打 `[econ] worker 生效值` ⇒ 到 `v7l2h`/`v7l2f` 的
  server 日志里应有 **16 行**（这就是"开关真到了 worker"的判据，对照臂没有是正常的）。
