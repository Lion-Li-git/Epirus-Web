# 多人局（3–5 人）训练与测量现状复查 · 5P 版

> 交千问复查。上一版是 `docs/REVIEW-3P.md`（3P 时代）。本版覆盖 v1.3.57 → v1.4.6 的工作：
> 座位效应、首个 5 人冠军、skill-report 的测量缺陷、四类新口径、长程模式（5 血）、
> 以及我自己两次错误的归因更正。
>
> 快速入口：**双击 `tools\skill-report.cmd`** 生成当前冠军的技能表；
> **双击 `tools\skill-report-compare.cmd`** 生成多版本对比页。两者都会自动打开浏览器。

---

## 0. 一句话现状

- **线上多人冠军 = v1.3.58**（5P 考卷 1400 局 1st **38.4%**、top2 55.9%、3P 47.5%；座位极差 5.0pt）
- **新增长程模式（5 血，3-5 人）**：`MODES.long = {hp:5, maxRounds:100, drainHpMax:3}`
- **测量面从 1 个口径扩到 5 个**：`--inject` / `--grant` / `--plan`·`--combo` / `--smart` / `--ban`，
  并在 report 里给出每个技能"该用哪个口径"
- **P0 仍未解**：深经济锁死 —— 所有版本"富经济 1st − 原生 1st"缺口 **21~41pt**

---

## 1. 已解决

| 项 | 关键证据 | 版本 |
|---|---|---|
| 座位效应 + 平局 | `rankOf` 第 4 键 `dealt` + `mulberry32(seed)` 先洗牌再稳定排序；真实考卷场极差 **15.4→3.3pt**、对称场 **54.4→7.5pt**；np-test **L7** | v1.3.57 |
| 首个 5 人冠军 | 5P 38.4% / top2 55.9% / 3P 47.5%；座位 37.5/37.5/41.8/38.6/36.8（极差 5.0pt） | v1.3.58 |
| skill-report「一堆死技能」 | 三处测量缺陷（**强制命中率从未被测量** / `[probe,probe,probe]` 硬编码 3 探针 → N=5 两个僵尸座位 / 2 脚本填 4 座）；**死技能 22 → 1** | v1.3.59 |
| 条件触发卡的器材 | `--field`（focusfire/focusme/focusme2/tank/mine/minespam）/ `--regen` / `--smart` / `--combo` | v1.3.60 |
| 长程模式（5 血） | 全局 `MAX_ROUNDS=60` 会把 5 血局截断（3 血 43.3 回合 vs 5 血 49.8 且场均承伤 4.28<5）；配 100 后 **72.3 = 43.3×5/3** | v1.4.0 |
| 摄魂指法门槛平衡 | 放宽**反而更差**：3血 HP≤1 Δ+1.1pt；5血 HP≤1 **+1.8** / HP≤2 −0.3 / HP≤3 **−3.1**；"HP≤1"机会率两模式均 **0.3%** | v1.4.0 |
| 我的归因错误（第 1 次） | "38.4%→21.8% 是轮换造成的" **被证伪**：配对轮换恢复后 armA9f 只有 18.1%、训练自评同向；真因是**从零冷启动 vs 热启动谱系** | v1.4.2 |
| 多 seed 配对 A/B（n=4） | 12 对手均值 **37.8% vs 32.9%（+4.9pt，3/4 正）**、方差极差 **11.6→3.8pt**；配对 t≈1.57（p≈0.11）未达显著 | v1.4.4 |
| 四个新口径 | `--ban` 消融 / `--grant` 开珠（`onRoundStart` 钩子）/ `--plan` 连招 / `--smart` 条件注入 | v1.4.5 |
| report 覆盖表 + 一键入口 | 「该用口径」列 + 覆盖率卡片 + 覆盖表；`tools\skill-report.cmd` / `skills-report-compare.cmd` | v1.4.6 |

---

## 2. 尚存问题

### P0 深经济锁死（5 次失败后仍未解）

- **缺口在所有版本都在**：富经济 1st − 原生 1st = 21.1 / **34.8** / 23.2 / 35.0 / 40.8pt（v1.3.58/hA9/hB12/armB12f/armA9）
- **机制（v1.4.7 更正，原表述不完整）**：ジ 给 +1/回合是**最慢**的一条收入线
  （`js/core/resolve.js:501`）；但**还存在第二条复利线**——`聚能环` 首次 3 ジ → +1（净 −2），
  连续第 2 次 0 ジ → +2，**第 3 次起 0 ジ → +3/回合**（`state.js` computeCost + `resolve.js` ring 分支）。
  所以"任何 ≥2 ジ 的卡都要 2+ 回合攒钱"只对**不开环**的策略成立。
  ⚠️ 这条更正很关键：历次补贴（regen / ep 下限）**恰好绕开了这条复利线**，
  网络被白给的钱喂饱后环的相对价值归零 ⇒ "补贴教会它花钱"这个前提从头就是反的。详见 §5。
- **四类卡的瓶颈各不相同**（v1.4.0 / v1.4.5 实测）：

| 族 | 例 | 3 血 Δ | 5 血 Δ | 瓶颈 |
|---|---|---|---|---|
| 攒钱耗时型 | 地雷(3ジ) | −1.3pt | **+4.0pt** | 攒钱时间 ⇒ **5 血可解冻** |
| 反应型 | 转移伤害(2ジ) | −30.8pt | −28.1pt | 需同回合承伤 N≥2；实测"上回合承伤≥2"只成立 **0.2%** |
| 组合型 | 贴贴×天火 | 引爆 **0** 次 | 引爆 **0** 次 | 算术上限：符咒 `age≤3` vs 铺符速率 ≤1 张/2 回合 |
| 连招型 | 蓄能→电磁炮 | — | — | 可跑通（打出 4233 次）但 1st **26.8%**（top2 69.4%、5th 2.3%）= **活得久赢不了** |

- **电磁炮的反差（本轮最刺眼的数据）**：
  - 每回合白送 1 枚电珠 ⇒ 冠军 **38.4% → 51.2%（+12.8pt）**，比本轮任何训练/基因改动都大
  - 而它真实的获取路径（`蓄能`(1ジ) → 下回合 `电磁炮`(2ジ)）连招主体只有 26.8%
  - 机制：珠子时效 R9'（`resolve.js:790-796`）`keep = p.beadNew || null` ⇒ **只有"本回合新蓄"的珠才保留**，
    持珠那回合打 ジ 攒钱会把珠清空；而 `蓄能` **不产 ep** ⇒ 必须 `ep≥3 蓄能 → 下回合 ep=2+珠 → 开炮`
  - ⇒ **3 ジ / 2 伤**，被 `坦克` 的 **2 ジ / 2 伤**支配（虽然电磁炮穿防+穿反弹）
- **曾经的 5 次失败尝试**（完整证据链见 CHANGELOG v1.3.15~v1.3.18）：课程退火 / 永久回放切片 /
  探针进选择 / 两者并修 —— 分别证明"退火窗口 ≫ 漂移尺度"、"被看见 ≠ 被选择"、
  "判据与 min 缩放写错"、"探针构造性不可满足"

### P1 测量面仍缺

- **藤甲"火弱那一半"仍未测**：饱和铺雷场（4×`pickMineSpam`）里 `--inject=armor` 与 `--inject=reflect`
  给出**逐位相同**的结果（32.5/15.0/20.0/7.5/25.0）⇒ 该场防御族惰性（地雷伤害不吃格挡），
  只测到"龟 vs 不龟"。需要 `--smart=armor:<条件>`（只在"目标正在被别人打"时贴）
- **判定阈值是刀锋**：`use ≥ 4%` 决定"常用" ⇒ `枪`（v1.3.58 使用率 **3.8%**）在"坑/死技能"间摆动。
  边界技能应**读 Δ 不读标签**
- **单跑噪声 8.6pt**（同设置 3 次 firstRate 极差）；多 seed 只做了 4 个 ⇒ 定论需 8~12
- mono-spam 之外的 4 个口径**仍要手动跑**（report 只给出"该用哪个命令"，尚未自动执行）

### P2 训练

- **从零冷启动 350 代仍不如热启动谱系**（最好 33.3% vs 38.4%）
- 9 vs 12 对手：**+4.9pt 但未显著**，方差减半（11.6→3.8pt）
- **各 seed 落到的局部最优战术差异极大**：v1.3.58 激光剑流(25.2%) / hA9 狙击枪流(22.6%)+聚能环(5.5%) /
  hB12 激光剑+防御流 / armB12f 坦克流(27.0%) / armA9 枪流(36.8%，最弱)
- **消融（拿掉）实测各自主武器的贡献**：hA9 −狙击枪 **−9.5pt**、armB12f −坦克 **−4.0pt**、
  hA9 −聚能环 −1.6pt、hB12 −大雷 −1.6pt；对照 hA9 −激光剑（使用率 0.0%）**−0.1pt**

### P3 未做

- 5 血冠军尚未训练（现役是 3 血族，5 血下 1st 从 38.4% 掉到 32.6%）
- `--plan` 的更多连招：贴贴×N→天火（已证算术锁死，除非改 `age`）、ジ→转移（条件率 0.2%）
- `--smart=armor`（藤甲火弱半）
- `docs/skill-report-3p.html` 未按新口径重跑

---

## 3. 方法论（本轮新增，多数已变成会失败的用例）

1. **n=1 不得归因** —— 同类错误**第二次**（v1.3.12 据单次 17:62 收回 `WR_TOL`；v1.4.1 据两臂归因轮换）
2. **每个新口径都必须带自检计数 + 反证对照**：
   `forceHit`（强制命中率）/ 注入率 / 消融命中 / 连招打出次数；消融的对照 `ban=sword`（使用率 0%）= **−0.1pt**
3. **静默失败清单（本轮又抓 3 个）**：
   worker 对手池 `filter` **静默取子集**（漏名字 ⇒ "12 对手"臂实际只跑 9 个）；`usage()` 硬编码 3 探针
   ⇒ N=5 时 2 个僵尸座位；训练实验进行中 `js/bundled-champion-3p.js` 是**中途产物**（忘传 `--champ` 会测错对象）
4. **"能跑通" ≠ "值得用"**：蓄能→电磁炮打出 4233 次却 −11.6pt
5. **对照本身必须是对的**：`rank-diag` 的洗牌对照第一版把"按 key 分组"当成随机化，等于没消除偏置
6. **把教训变成会失败的用例**（Claude 的核心意见）：L1~L7 + 本轮 **D1~D4**
   （D4 = 对手轮换不得依赖个体下标；D3 = 终局上限按模式走）
7. **长任务先报时间预估，跑起来后定期报进度**（用户 2026-09-12 的要求）。
   每个 run 前写一行【预估】（依据是已实测的吞吐：5 人 250 代热启动 ≈ 1 分钟；
   一个 1400 局考卷 ≈ 8 秒），run 中按阶段打印带时间戳的进度行并在对话里反馈，
   不要"发一个 30 分钟的任务然后沉默"。
8. **路径坑（会话内第 4 次栽）**：node 在 Windows 上把 `/tmp/x` 解析成 `D:` + 反斜杠 + `tmp` + 反斜杠 + `x`，
   而 Git Bash 的 `/tmp` 是 `C:` + 反斜杠 + `Users` + 反斜杠 + `<user>` + 反斜杠 + `AppData\Local\Temp`
   ⇒ 给 node 传 /tmp 下的文件必须用 `$(cygpath -w /tmp/x)`。
   新增 `tools/ab-analyze.mjs` 只接受显式路径，就是为了不再靠记忆。
9. **`.cmd` 必须纯 ASCII**：中文注释在 GBK 控制台码页下会把行拆成非法命令（已写入脚本注释）

---

## 4. 请千问复查的问题

1. **深经济的真瓶颈是什么？** 我现在的表述是"ep 收入 +1/回合 × 存活期望 3 回合"。
   但 5 血只解冻了地雷（+4.0pt），转移/组合/连招都没解冻 —— 是否说明"存活期望"这个解释也不完整？
   三类失败各自的瓶颈（N≥2 供给、`age≤3`、3 ジ vs 2 ジ）是不是应该分别当作**规则问题**而不是训练问题？
2. **电磁炮那组反差怎么读？** 白送珠 +12.8pt、真实路径 −11.6pt。
   这是**平衡问题**（该降电磁炮成本 / 改珠时效让珠能跨回合）还是**探索问题**（该给网络"承诺级轨迹"）？
   我倾向前者，但 v1.3.14~18 的四次"训练侧解决"都失败了，想听你的判断。
3. **贴贴×天火**：`age≤3` + 1 张/2 回合 ⇒ 同目标永远叠不到 2 张活符咒。
   改 `age`（3→5）还是改贴贴成本（1→0）？哪个是更小的可验证改动能真正解开这条线？
4. **转移伤害**：条件率 0.2% 时它算"设计上不可用"吗？还是该改机制（例如"转移不消耗行动"/"承伤 N 全转"）？
5. **测量学**：既然多数技能是"**冠军已经在用**"，`--ban` 消融是不是该取代 `--inject` 成为**主口径**？
   以及：report 现在只给"该用哪条命令"，要不要做成**自动按覆盖表巡回执行**（每个技能自动挑口径）？
6. **5 血模式**要不要干脆作为**默认多人模式**（把 3 血留给 2 人对战）？依据：5 血下座位更中性、
   地雷这类卡解冻、平均 72.3 回合；代价是 AI 需重训、单局更长。

---

## 附：快速复现命令

```bat
rem 当前冠军的技能表（自动打开）
tools\skill-report.cmd

rem 某个存档版本的技能表
tools\skill-report.cmd docs\artifacts\champion-5p-hA9.bak

rem 多版本对比页（默认 5 个版本 + current）
tools\skill-report-compare.cmd

rem 消融：测"拿掉某个冠军已在用的技能"的损失
node tools\eval-5p.mjs 40 5 77000 docs\artifacts\champion-5p-hA9.bak --ban=snipe

rem 开珠：珠类门技能（电磁炮/激光眼）必须先开珠才能测
node tools\eval-5p.mjs 40 5 77000 docs\artifacts\champion-5p-v1.3.58.bak --grant=boom --inject=laserEye

rem 连招：蓄能→电磁炮序列主体
node tools\eval-5p.mjs 40 5 77000 docs\artifacts\champion-5p-v1.3.58.bak --plan=chargeRailgun

rem 前置条件场：集火 / 饱和铺雷
node tools\eval-5p.mjs 40 5 77000 docs\artifacts\champion-5p-v1.3.58.bak --field=focusme2 --inject=reflect
node tools\eval-5p.mjs 40 5 77000 docs\artifacts\champion-5p-v1.3.58.bak --field=minespam --inject=armor

rem 长程模式（5 血）+ 摄魂门槛扫描
node tools\eval-5p.mjs 40 5 77000 docs\artifacts\champion-5p-v1.3.58.bak --mode=long --drainHp=2 --inject=drain

rem 回归
node tools\spec-run.mjs && node tools\np-test.mjs
```

---

## 5. 第十轮独立复核（2026-09-12）的处置

复核文档：`results/Epirus-Web-第十轮复核-5P与长程.md`
（千问自建评测架 `r10-lib.mjs`，**不复用** `eval-5p`/`skill-report` 以免同享一个 bug；
未改本仓库任何文件，跑前备份 md5、跑后核对未变；~2 万局乱跑 0 异常 0 违反不变量。）

**总判：它对我方 P0 的重新定性成立，而且找到了比我列的 P0 更大的洞（0 ジ 反弹）。**

### 5.1 逐条处置

| # | 复核结论 | 我的复验 | 处置 |
|---|---|---|---|
| 1 | **P0 标题误标**：21~41pt 是"收入"缺口不是"货架"缺口；ep 阶梯峰值在 cap3(54.8%)，cap10 掉到 32.5%，掉下来是**雷击之枪**占比 1.1%→52.0% | 未复跑阶梯（成本高），但机制自洽：`雷击之枪` 0 伤害/2 ジ，我方 report 里 Δ=−44pt 且被归入"辅助/防御" | **接受改名**：`P0-a 收入不足` + `P0-b 陷阱卡（雷击之枪）` |
| 2 | **0 ジ 反弹统治多人局**：冠军对 4×反弹墙 0.0~0.4%、必死（3 次自弹=3 血）；纯激光剑/狙击 100% | ✅ **已复验（1400 局/臂）**：冠军 1st **0.0%**、场均承伤 **3.00**、**自己的攻击被弹回 3.00 次/局**、终局血量 **0.00**、平局率 0%；`--pure=sword` **100.0%**、`--pure=snipe` **100.0%**，同一批纯招在中性场只有 34.6% / 3.1% ⇒ **天花板真实存在** | **升级为 P0-c（最高优先）**，修法=训练池，**零规则改动** |
| 3 | **电磁炮不被坦克支配**（0.67 vs 0.50 伤害/ジ，且穿防+穿反弹） | ✅ 读码确认 `rules.js:60` `pierce:{defense,reflect}` ⇒ 我 v1.4.5 写的"3 ジ/2 伤被坦克 2 ジ/2 伤支配"**是错的** | **更正我的结论**（见 5.2）；改测"珠时效"（`beadNew` 不覆盖 + 珠上限 2） |
| 4 | 大雷 vs 坦克是 (经济, 考卷, 人数, 血量) 四元条件；v1.3.55 的"等 5 人局"是过度推广 | 部分独立成立：v1.4.0 我已测到 5 血下翻转（3 血 −13.6pt） | **接受**；结论必须写成条件表 |
| 5 | **聚能环是 0 成本 +3/回合的复利收入线**；我方"ep 收入只有 +1/回合"漏了它 | ✅ 读码确认；且**解释了方向性错误**（补贴绕开了这条线） | **已更正 REVIEW-5P §2-P0**（见上）；列为 `P0-d` |
| 6 | 长程模式 100 回合仍大面积截断（防守卷 90%），5 血比 3 血更糟 | 部分：我测过平均回合 43.3→72.3，**未测截断率** | **接受**：需突然死亡，或在结论里逐条标注截断率 |
| 7 | **训练考卷没跟着换 5 血**：`oneGameN` 支持 `opts.mode`，但 30 个调用点无一传 mode | ✅ grep 确认 | **待办**（4 个调用点 + 传参）——这是"5 血下轨迹是否解冻"必须先补的接线 |
| 8 | 小问题 8 条 | 逐条读码确认 | ①②④ 已修（见 5.3），其余进待办 |

### 5.2 我要更正的地方（都是我自己写的）

1. **REVIEW-5P §2-P0 的机制表述不完整**（已在正文更正）：漏了 `聚能环` 那条 **0 成本 +3/回合**的复利收入线。
   复核把它做成可执行轨迹（环→攒到 bank8 出大雷）：**5 人 5 血进攻卷上是最优解（22.7%）**，
   防守卷上仍不如坦克流；"只环不打"恒 **0%**（与 v1.3.x `save` 奖励失败一致 ⇒ 囤积 ≠ 强度）。
2. **CHANGELOG v1.4.5 §3 的"电磁炮被坦克支配"是错的**：电磁炮 0.67 伤害/ジ > 坦克 0.50，
   且穿防+穿反弹。真实约束是**时机**（珠只活一回合），不是价格。最小可验改动 = 让珠跨回合保留 1 层。

### 5.3 本轮已落的修复（都配了会失败的用例或反证）

| 修复 | 位置 | 守护 |
|---|---|---|
| `蓄能` desc 改为与 R9' 一致（仅保留到下一回合、同类最多 1 枚） | `js/core/rules.js:40`（`ui.js:131` 直接展示给玩家） | 读码 + 事件流 |
| 双枪第二发复用 `dgDmg`（两发同源，不再写死 `amt:1`） | `js/core/resolve.js:636` | **np-test D5**（反证：改回写死 1 立刻红 `got=1 want=2`） |
| 30 分钟墙上时钟上限改为 `EPIRUS_WALL_MS` 可关（默认不变） | `server/train-server.mjs` 两处 | — |
| `RULES-NP.md` N1 范围声明补 `long`（5 血） | `docs/RULES-NP.md:165` | — |
| **`--pure=<技能>` 纯招口径**（`--inject` 会从回落里漏出枪，测不了"单一卡片策略能否破墙"） | `tools/eval-5p.mjs` | 纯招自检（可负担率） |
| 四种防御墙 `--field=reflectwall/guardwall/baguawall/protowall` + 机制读数（被弹回/平局率/终局血量） | `tools/eval-5p.mjs` | — |

### 5.4 更新后的优先级

1. **P0-c 反弹墙**（零规则成本）：把 `reflectspam`/`guardspam`/`baguaspam`/`protowall` 放进**训练/评测池**
   —— 注意它们作为"玩家对手"确实无聊（v1.3.22 按熵=1.00 剔除，那是 UI 判断），但作为
   **exploit cursor** 正是缺的那类；且这四个名字**不在 server 的 `BOT_FN_N` 里**，只设 `EPIRUS_OPP_N` 不够。
   **验收：冠军在 `--field=reflectwall` 的 1st 从 0.0% 升到 ≥50%**（纯激光剑 100% ⇒ 天花板存在）。
2. **P0-a 收入不足 / P0-b 陷阱卡**：复查 `雷击之枪`（0 伤害 / 2 ジ）的定价。
3. **P0-d 聚能环线没被教**：它是唯一"钱生钱"机制，而历次补贴都在绕开它 ⇒ 补贴类实验应停。
4. **P1 5 血两处结构问题**：训练考卷换 5 血（接线）；截断率（突然死亡）。
5. 卫生项：`trainer.js startTraining` 死代码、`play.js:50` 珠型写死、
   `winner` vs `rankOf` ~15% 不一致（复核样本含高温网络，只当线索，待在真实考卷上重测）。

---

## 6. ring2 实验：把「聚能环经济流」放进训练池 —— 判定 **不加**（n=6 配对）

**问题（用户）**：把聚能环经济流（`ringspam`）放进训练池，冠军能学会反制环吗？（小雷的反制目标就是环）
**设计**：控制臂 = 12 对手（`ms2-p12-31..36`，上一轮已产出）；实验臂 = 同 12 对手 + `ringspam`
（`ring2-31..36`，其中 32–36 本轮新产出）。两臂**同起点、同 seed、同 gens/pop/games**
（250 代 / pop16 / gpo8 / 5 人 / 热启动），**唯一差别是池子**——所有产物 meta 的
`hotstartFrom` 恒为 `e379c62ccd2648fa`（= v1.3.58 权重），seed 与参数逐个一致。
**预注册验收**（HANDOFF §1）：`--mode=long --field=ringwall` 的 1st 提升，且标准考卷不劣化（多 seed 配对）。

**复跑（本轮实测 ≈ 6 分钟；高精度复核 ≈ 100 秒）**：
```bash
node tools/ring2-run.mjs                                                    # 训练 32..36 + 评估 12 臂 + 汇总
RING2_STAGE=eval RING2_RGAMES=400 RING2_TAG=r400 node tools/ring2-run.mjs   # 环场每臂 400 局
node tools/ab-analyze.mjs docs/artifacts/ring2-run.log p12 r17              # 配对统计（p12=控制臂, r17=实验臂）
```
（`tools/ring2-run.mjs` 是本轮新增的自跑器：把「等待」关在它自己的进程内 —— HANDOFF §5.1-1 那次
DSH 卡死就是会话内长轮询造成的；它还会备份/还原 `js/bundled-champion-3p.js` 与 `index.html`，
因为训练每轮都会改写这两个文件。产物 `docs/artifacts/ring2-*.{bak,log}` 均被 .gitignore 忽略。）

### 6.1 预注册口径（每臂每考卷 40 局）的配对结果

| seed | p12 自评 | r17 自评 | p12 标准 1st | r17 标准 1st | p12 环场 1st | r17 环场 1st |
|---|---|---|---|---|---|---|
| 31 | 0.3864 | 0.3603 | 37.2% | 38.8% | 60.0% | 22.5% |
| 32 | 0.4083 | 0.3205 | 40.4% | 36.1% | 2.5% | 20.0% |
| 33 | 0.3530 | 0.3647 | 36.6% | 34.9% | 7.5% | 10.0% |
| 34 | 0.4023 | 0.3603 | 37.1% | 31.2% | 2.5% | 2.5% |
| 35 | 0.3705 | 0.3929 | 36.6% | 36.3% | 27.5% | 40.0% |
| 36 | 0.3780 | 0.3712 | 37.0% | 37.0% | 85.0% | 27.5% |

| 指标（r17 − p12, n=6） | p12 均值 | r17 均值 | 均值Δ | sd | t | 符号 | 单跑极差 p12/r17 |
|---|---|---|---|---|---|---|---|
| 训练自评(×100) | 38.31 | 36.16 | −2.14 | 4.02 | −1.31 | 2正/4负 | 5.5 / 7.2 |
| 标准考卷 1st | 37.48% | 35.72% | **−1.77pt** | 2.83 | −1.53（边缘） | 1正/4负/1零 | 3.8 / 7.6 |
| 环场 1st（40 局） | 30.83% | 20.42% | −10.42pt | 30.10 | −0.85 | 3正/2负/1零 | **82.5 / 37.5** |

三条读数：
- **目标指标（环场）没有提升**，点估计为负且完全不显著（t=−0.85）；40 局下单跑极差 82.5pt，
  这个口径本身在 n=6 读不出东西。
- **标准考卷弱负**：Δ=−1.77pt、t=−1.53、6 个 seed 里 4 负 1 正 1 零。它没到 p<0.05，
  但方向与验收条件「不劣化」相反；且标准考卷的单跑极差只有 3.8pt ⇒ 这个位移与噪声同量级但更成系统。
- 训练自评的 −2.14 是**机械效应**（池里多了一个打不过的对手，1st 自然被摊薄），不当质量信号读。

### 6.2 高精度复核（环场每臂 400 局）：抬局数**压不掉** seed 间异质性

| seed | p12 40局 | p12 400局 | r17 40局 | r17 400局 | Δ40 | Δ400 |
|---|---|---|---|---|---|---|
| 31 | 60.0% | 70.3% | 22.5% | 17.3% | −37.5 | −53.0 |
| 32 | 2.5% | 12.3% | 20.0% | 15.0% | +17.5 | +2.7 |
| 33 | 7.5% | 12.0% | 10.0% | 12.8% | +2.5 | +0.8 |
| 34 | 2.5% | 11.0% | 2.5% | 11.0% | 0.0 | 0.0 |
| 35 | 27.5% | 29.3% | 40.0% | 37.5% | +12.5 | +8.2 |
| 36 | 85.0% | 81.5% | 27.5% | 25.3% | −57.5 | −56.2 |
| 均值 | 30.83% | **36.07%** | 20.42% | **19.82%** | −10.42 | **−16.25**（sd 29.86, se 12.19, t=−1.33） |

- 每臂读数的 40→400 漂移 ≤10.3pt（最极端的那几个 2.5% 被抬到 11–12%）⇒ 40 局确实有 ±10pt 抽样噪声。
- **但 400 局下臂内极差仍有 70.5pt（11.0%→81.5%）**，配对差分的 sd 几乎没动（30.10 → 29.86）
  ⇒ 这 70pt 是**真实的「这次搜索落到哪个局部最优」差异**，抬局数压不掉它。
  与 §3.4 对反弹墙的结论同构：**这类"条件场"指标要读出效应只能加 seed（或改训练信号），不是加局数。**
- 实验臂 6 个 seed 全部落在低带（11.0–37.5%）；控制臂有两个落在高带（70.3 / 81.5%）
  —— 加 `ringspam` 不是"平移"，而是**换了一次抽样**，且这一轮没抽到那两个抗环盆。
- 顺带确认：**评测工具是确定性的** —— 标准考卷 12 个读数在两轮里逐位相同。

### 6.3 单点诊断（环场 400 局）：现役冠军的基线 & 小雷的真实边际

| 主体 | 环场 1st（400 局） | 说明 |
|---|---|---|
| **现役冠军 v1.3.58** | **11.0%**（5th 76.3%） | §3.3 原写「2.5%」是 40 局读数，属噪声带内 |
| v1.3.58 + `--inject=miniT` | 25.8%（+14.8pt） | 小雷的反制线**真实但有限**；注入自检 31.9% |
| `ms2-p12-36`（控制臂 81.5%）+ `--inject=miniT` | 79.3%（−2.2pt） | 它抗环**不是靠小雷**（注入无边际） |
| `ms2-p12-31`（控制臂 70.3%）+ `--inject=miniT` | 8.8%（−61.5pt） | 贪心注入**摧毁**了它自己的抗环方案 |
| `pickRandom` 对照（同一环场） | 48.0% | 环场对随机玩家并不难 ⇒ 「冠军 11%」是**它的策略**输给环，不是环太强 |

最后两行是本轮最有用的一条机制证据：**贪心 `--inject` 不是"这张卡值多少"的干净器械**，
对已经有成型方案的冠军会测出"贪心误用有多糟"（§6.5 的 `--smart` 就是为这个写的）。

### 6.4 判定：**不把聚能环经济流放进训练默认池**

1. **预注册验收未通过**：目标指标（环场 1st）无提升，40 局 Δ=−10.42pt、400 局 Δ=−16.25pt，
   两次都不显著且点估计为负。
2. **标准考卷弱负**：Δ=−1.77pt、t=−1.53、4/6 seed 为负 1 个零。验收要求"不劣化"，这条没满足。
   （两个臂的差别只有池子，没有别的变量 —— 起点权重、seed、代数、pop、gpo 全部相同。）
3. **机制上也不该指望它有用**：池里加 1/13 个对手，先改掉**每代每个个体的得分**
   （同一策略对上 13 个对手 vs 12 个，评分配对变了）⇒ 精英选择变了 ⇒ 即便繁殖用的随机流
   是 `mulberry32(seed*100003+gen)` 逐代独立播种（`server/train-server.mjs:394`）、序列相同，
   它套到的父代也已经不同 ⇒ **整条搜索轨迹分叉**。所以这不是"教一个反制"，而是**换一次抽样**；
   n=6 下这个彩票的方差（sd≈30pt）盖过一切效应。
4. 若真要教冠军打环，**该动的是训练信号**（把环场写进适应度/课程），不是往池里掺一个名字。
   这条与 §3.6（摄魂门槛放宽反而更差）同属一类：**改池/改门槛 ≠ 教会一种策略**。

### 6.5 本轮的方法论收获（三条，都可复跑）

1. **"极端读数"先怀疑局数**：控制臂 40 局的两个 2.5%（seed 32/34）在 400 局下变成 **12.3% / 11.0%**，
   85%（seed 36）变成 81.5% ⇒ 极端值被抬回噪声带内，但**排序不变**。⇒ 40 局可用于排序，不要读绝对值。
2. **杠杆要选对**：同一指标上，「每臂局数 ×10」几乎不动配对 sd（30.10→29.86），
   而「多 2 个 seed」（n=4→6）直接决定结论能不能读。§3.4 早就写过，这轮再次坐实。
3. **纠正了一条已写进文档的数**：§3.3「4×ringspam 把现役冠军打到 1st 2.5%」是 40 局读数；
   400 局复核是 **11.0%**。这是又一次「把噪声当结论」（§5.2-11 已记过三次同类事故）。
   该结论的**定性**部分（5 血解冻轨迹策略、环在 3 血退化）不受影响。

---

## 7. 长程（5 血）冠军：训练出来了 —— 取舍是真的，"5 血收益"还没到显著

**背景**：§4-3 一直挂着"训练考卷没跟着换 5 血 ⇒ 5 血冠军尚未训练"。根因不是算法而是**接线**：
`oneGameN` 早就支持 `opts.mode`，但 19 个调用点里唯一传过它的是评测工具 `tools/eval-5p.mjs:305`
（所以 `--mode=long` 在评测里一直好使），训练侧 5 处全走默认 `'multi'`（3 血）。
v1.5.0 把线接通，并在 worker 线程 + 回执上补了两道守门（np-test **D10/D11**）。

**设计**：控制臂 = 12 对手池按 **3 血**训出的 6 个冠军（`ms2-p12-31..36`，上一轮产物，未重训）；
实验臂 = **完全相同的池 / 种子 / 起点 / worker 数 / gens / pop / gpo**，只把模式换成 **5 血**
（`long-31..36`）。7 个输入维度里 6 个逐位相同，产物 meta 全部核验过
（`mode=long`、`opps=12`、`workers=16`、`hotstartFrom=e379c62ccd2648fa`）。

**复跑**（实测：训练 11 分钟 + 评估 4.5 分钟）：
```bash
RING2_MODE=long RING2_POOL=A RING2_ARM=long RING2_SEEDS=31,32,33,34,35,36 \
  RING2_EXAM2FLAGS='--mode=long' RING2_TAG=long node tools/ring2-run.mjs
node tools/ab-analyze.mjs docs/artifacts/ring2-run-long.log p12 r17
```
（`p12` = 3 血臂、`r17` = 长程臂；`ring2-run-long.log` 头两行已写明。**注意**
`EPIRUS_WORKERS` 不能改 —— 见 HANDOFF §5.2-16。）

### 7.1 配对结果（n=6）与逐 seed 明细

| 指标（r17 − p12, n=6） | p12 均值 | r17 均值 | 均值Δ | sd | t | 符号 | 单跑极差 |
|---|---|---|---|---|---|---|---|
| **3 血标准考卷 1st** | 37.48% | 34.15% | **−3.33pt** | 4.42 | −1.85（边缘） | **1正/5负** | 3.8 / 10.3 |
| **5 血标准考卷 1st** | 30.72% | 37.82% | **+7.10pt** | 13.98 | 1.24（不显著） | 4正/2负 | 13.6 / 29.8 |
| 训练自评(×100) | 38.31 | 44.39 | +6.09 | 7.86 | 1.90（边缘） | 5正/1负 | 5.5 / 17.5 |

| seed | 3 血考卷 p12→r17（Δ） | 5 血考卷 p12→r17（Δ） |
|---|---|---|
| 31 | 37.2 → 33.0（**−4.2**） | 29.1 → 32.9（+3.8） |
| 32 | 40.4 → 32.5（**−7.9**） | 38.8 → 32.8（−6.0） |
| 33 | 36.6 → 41.4（+4.8） | 28.2 → **57.7**（**+29.5**） |
| 34 | 37.1 → 31.1（**−6.0**） | 25.2 → 38.5（+13.3） |
| 35 | 36.6 → 34.4（−2.2） | 36.4 → 27.9（−8.5） |
| 36 | 37.0 → 32.5（**−4.5**） | 26.6 → 37.1（+10.5） |

⇒ **取舍是真的**：5 血下更强（均值 +7.10pt、4/6 seed 为正），3 血下更弱（均值 −3.33pt、5/6 seed 为负）。
但两边都没到 p<0.05：5 血那边 sd=13.98（单跑极差 29.8pt）**读不出显著性**，3 血那边符号模式很强
（5/6 负）而 t 只有 −1.85。**"长程冠军在 5 血更强"目前是方向性结论，不是已证结论。**
（训练自评那行 +6.09 不可当质量信号读：它在**各自模式下**量，量纲不同。）

### 7.2 技能使用分布：**种类没变多，变的是"用钱的深度"**

同一张 5 血考卷、同 6 个 seed，只差训练模式（n=6 均值）：

| 读数（5 血考卷） | 3 血冠军 | 长程冠军 | 比值 | 逐 seed Δ 符号 |
|---|---|---|---|---|
| **出手种类** | 16.7 | 15.3 | **0.92×（略降）** | 2正/3负/1零 |
| **ep ≥ 3 的决策占比** | 19.5% | 47.8% | **2.4×** | **5正/1负** |
| **cost ≥ 3 出手占比** | 1.9% | 8.1% | **4.3×** | **5正/1负** |
| 决策时 ep 10+ 占比 | 5.8% | 22.2% | **3.8×** | — |
| 到达过的最高 ep（中位） | 52 | 139 | — | — |

所以"分布有没有增加"的准确答案是：**不是会的招更多，而是敢攒敢花**。长程冠军学到的是
**稳定的"攒钱换大件"轨迹**（5/6 seed 同向），而不是多样的出招集合。
唯一的反例是 **seed 35**：它的长程冠军没有转向深经济（ep≥3 8.1% / cost≥3 0.3%），
而它在 5 血考卷上也恰好是**退步**的（−8.5pt）—— 这条内部一致性反而支持上面的解释。

**这种风格会延续到 3 血**（同一批冠军、3 血考卷）：

| 读数（3 血考卷） | 3 血冠军 | 长程冠军 | 比值 |
|---|---|---|---|
| ep ≥ 3 决策占比 | 16.2% | **44.6%** | 2.8×（逐 seed 5正/1负） |
| cost ≥ 3 出手占比 | 1.45% | **7.5%** | 5.2× |
| 出手种类 | 16.0 | 13.5 | 0.84× |

⇒ 长程冠军在 3 血局里**仍然**按深经济打 —— 它在 5 血学到的是一套**跨模式的风格**，
而这套风格在 3 血（赛程短、回不了本）是亏的。这正是 3 血掉 3.33pt 的机制。

### 7.3 结论与建议（三条，都别越过证据）

1. **不要把线上默认（3 血）冠军换成长程冠军**：3 血考卷 1st 平均掉 3.33pt（5/6 seed 为负），
   而 5 血的收益（+7.10pt）在 n=6 下**不显著**。风险与收益不对称。
2. **但"按模式分冠军"是站得住的**：长程模式挂一个长程冠军、标准/多人（3 血）继续用 v1.3.58。
   代价是维护两套权重（`js/bundled-champion-3p.js` 目前只有一份）。
3. **绝不要按 5 血考卷挑 6 个里最好的那个**（例如挑 seed 33 的 57.7%）—— 那是**挑噪声的上尾**
   （§5.2-11 同类错误）。要真交付一个长程冠军，正确做法是：多 seed 取中位/合并，
   或把"5 血考卷的成绩"写进适应度做复合目标（这条也正是 §6.4 对环实验给出的结论：
   **改训练信号，不是改池子**）。

### 7.4 已知缺口（留给下个会话）

- `tools/skill-report.mjs` 内部**只按 3 血建局**（它调 `oneGameN` 时不传 mode）⇒ 你的一键技能表
  对长程冠军给出的其实是"3 血下的读数"。本节的技能分布是用 `eval-5p --mode=long` 量的。
- 5 血标准考卷有 **19% 平局率**（终局收缩导致全灭平局，§3.5 的已知边界）⇒ 1st 率是在更少的
  有效局上算的，跨模式比较时要记住这一点。

### 7.5 技能报告看到的：**环是关键，而它只在 5 血里被学出来**（v1.5.1）

先说口径（改完才敢比）：`skill-report` 现在**缺省跟随冠军自己的 `meta.mode`**（`--mode=` 可覆盖），
报告头与 JSON 都写出模式 —— 此前它只按 3 血建局，对 5 血冠军给出的其实是 3 血读数。
改动对无 mode 的老冠军**行为保持**：重跑 v1.3.58，`baseNative/baseRich` 与旧 JSON 完全相同
（0.28968 / 0.50132），逐技能 use/Δ 序列逐位一致。单冠军 ≈64 秒（3 血）/ ≈150 秒（5 血）。

**八个版本的画像**（同一台仪器：5 人 · 126 组对手场 × 6 局 = 756 局/条件 · 富裕经济把 ep 补到 10）：

| 冠军 | 原生 1st | 富经济 1st | 主招（用量） |
|---|---|---|---|
| v1.3.58（现役，**3 血口径**） | 29.0% | 50.1% | 激光剑 25.2% |
| hA9（3 血口径） | 31.3% | **66.1%** | 狙击枪 22.6% + 聚能环 5.5% |
| hB12（3 血口径） | 28.8% | 52.0% | 激光剑 17.7% + 枪 8.2% |
| armA9（3 血口径） | 22.6% | 63.4% | 枪 36.8%（Δ=−28，典型"坑"） |
| armB12f（3 血口径） | 27.8% | 62.8% | 坦克 27.0% |
| ms2-p12-31（3 血训练，**5 血口径**） | 33.2% | 57.5% | ジ 56.4% + 激光剑 17.6% + 防御/反弹/八卦各 ~5% |
| long-31（5 血训练，5 血口径） | 36.8% | 66.0% | ジ 76.5% + 激光剑 15.2% + 真正的落雷 3.7% |
| **long-33**（5 血训练，5 血口径） | **57.9%** | **70.0%** | **聚能环 38.2%** + 激光剑 19.5% + 狙击枪 9.1% |

（⚠ 只有标"5 血口径"的三行彼此可比；其余是 3 血口径。且 long-33 是**我按 5 血考卷挑出来的最好那个**
⇒ 它的数字带选择性偏差，见 §7.3 第 3 条。另外顺带看到：**每个冠军都是单一流派**（2-3 招占全部出手），
流派完全不同；而 `原型制御 / 全息屏障 / 真正的落雷 / 摄魂指法` 在**几乎每个**冠军上都是
"零使用但正 Δ" —— 这是系统性的"没学会的强招"。）

**最重要的发现：5 血训练自己把「聚能环」学出来了。**
long-33 把环当主业（**用量 38.2%**），消融证明它**承重**：拿掉环，1st 从 **57.9% → 38.6%
（Δ_lost=+19.3pt）**，仅次于ジ（54.4pt）。

**不是孤例、也不是纯噪声** —— 用**无选择偏差**的 12 臂 5 血考卷 stdout 看环用量（6+6 全量）：

| seed | 3 血训练 | 5 血训练 |
|---|---|---|
| 31 | ≈0 | 0.2% |
| 32 | 11.5% | 5.4% |
| 33 | ≈0 | **39.5%** |
| 34 | 0.1% | **24.8%** |
| 35 | 1.8% | ≈0 |
| 36 | 0.5% | ≈0 |

- 5 血训练 **3/6** 把环用起来；3 血训练 **1/6**（且只有 11.5%）。
- **两个重度用环的长程冠军正好是 5 血考卷上最强的两个**（seed 33 = 57.7% 全场第一、seed 34 = 38.5% 第二），
  另 4 个平均只有 32.7% ⇒ **差 +15.4pt**；3 血那侧唯一的环用户（seed 32）也正好是 3 血臂在 5 血考卷上的最高分（38.8%）。

⇒ **这解开了 §3.1 的 P0-d 悬案**：「聚能环复利线没被教会」**不是**因为补贴不够、也不是池里没有环，
而是**赛程太短、环回不了本**（3 血下没等复利生效就被打死）。换成 5 血，环自己就长出来了，
而且**用环的冠军在 5 血里明显更强**。这比 §7.1 的均值 +7.10pt 更有解释力：
5 血的收益主要是靠"学会环"实现的，而学不学得到仍受 seed 彩票影响（3/6）。

---

### 7.6 **更正**：那四个"没学会的强招"不是 AI 的盲区，是**仪器（富裕经济）的假信号**

§7.5 里我写过「`原型制御 / 全息屏障 / 真正的落雷 / 摄魂指法` 在几乎每个冠军上都是"零使用但正 Δ"
⇒ 系统性没学会的强招」。**这个说法是错的**，本轮把它查掉了 —— 两条独立证据：

**(1) `forceHit`（前置条件出现率）把两类完全不同的原因分开了**（9 份技能报告全量）：

| 技能 | 9 个冠军的使用率 | 前置条件出现率 | mono Δ（**富裕经济**口径） |
|---|---|---|---|
| 原型制御（1 费·自保） | **全部 0.0%** | 94–96% | +12 ~ +29pt |
| 全息屏障（1 费·自保） | **全部 0.0%** | 94–96% | +10 ~ +30pt |
| 真正的落雷（5 费） | 0–3.7% | 97% | −7 ~ +28pt |
| 摄魂指法（3 费·HP≤1） | 全部 0.0% | **3–42%** | +5 ~ +25pt |

**(2) 换成**原生经济**口径一量，结论就掉头了**（现役冠军 v1.3.58，标准考卷 1400 局，基线 **38.4%**）：

| 口径（`eval-5p --inject=`，能用就用） | 注入可用率 | 1st | Δ |
|---|---|---|---|
| 基线（自由发挥） | — | **38.4%** | — |
| `--inject=proto`（原型制御） | 47.4% | **3.9%** | **−34.5pt** |
| `--inject=holo`（全息屏障） | 46.9% | **9.1%** | **−29.3pt** |
| `--inject=bigT`（真正的落雷） | **0.3%** | 38.7% | +0.3pt（**几乎没生效**） |
| `--inject=drain`（摄魂指法） | **0.3%** | 39.5% | +1.1pt（**几乎没生效**） |

⇒ **富裕经济（每回合把 ep 补到 10）会把"1 费整回合免伤"这种便宜可重复卡高估成 +30pt**；
而真按"能用就用"执行时，原生经济下**掉 29–35pt**（钱全花在防守上、永远不还手）——
**冠军不选它们是完全正确的**。至于 `bigT / drain`：原生经济下"可用率"只有 **0.3%**
⇒ 本口径**没有测量功效**，既不能说它们没用、也不能说该用。

**方法论修正（写进 HANDOFF §6.2）**：`skill-report` 的 mono-Δ 列**不能单独读**（它是富裕经济口径）；
必须配一条原生经济的 `--inject` 读数；两者符号相反时，**原生经济那个才算数**。

**另外一条（用户裁定，v1.5.2）**：用户指出「全息屏障是给别人套原型制御」。
核对代码：`js/core/rules.js:61` 的目标字段是 **`self`**，且 `js/core/resolve.js:114`
把 `PROTO / HOLO` 判成同一个 `kind: 'proto'`（注释：全息屏障=原型制御 R18）
⇒ **当前实现里它是一张自保卡**，与"给别人套"的设计意图不一致。这**要么是规则实现缺一半、
要么是描述口径问题**，属于"需要用户裁定"的条目（与 §3.1 的 `贴贴×天火` 同类），
**不该按"AI 该学会它"来处理**。用户同时裁定：**结盟式打法不必强求**（当前策略没有结盟建模），
本条从"AI 盲区"里移出。

---

## 8. 风格对练：把四个「风格化冠军」放进训练池（v1.5.2）

**动机**（用户提出）：手上这几个冠军**风格极其鲜明** —— 激光剑流 / 狙击枪+环流 / 坦克流 / 枪+墙流
（画像见 §7.5 与 `docs/skill-report-cmp.html`）—— 它们本身就是最合适的**陪练**。
脚本对手打不出成体系的策略，所以"反制不同流派"这件事此前**根本没有靶子**。

**新能力**：训练池支持 `champ:<仓库相对路径>`，机制单一来源在 `server/opp-champs.mjs`：

```
?opps=random,defend,champ:docs/artifacts/champion-5p-hA9.bak,champ:docs/artifacts/champion-5p-armB12f.bak
```

为什么用**路径**当输入、不写死名单：§7 的约定是冠军包不入库（`*.bak` 被 gitignore），写死会让仓库
依赖本机文件；走 `?opps=` 则路径连同 seed/参数一起记进产物 meta ⇒ 天然可复现。
（顺带踩了同型事故第三次并改成结构性唯一入口，见 `docs/HANDOFF.md` §5.2-17 / np-test D12。）

**设计**（同池 / 同种子 / 同起点 / 同 worker 数，唯一差别就是多了 4 个冠军对手）：
- 控制臂 = `ms2-p12-31..36`（12 个脚本对手，上一轮产物，**未重训**）
- 实验臂 = `mix-31..36`（12 脚本 + 4 风格冠军 = 16 个对手）
- **判定 ①**：标准考卷（3 血 · 35 组合 × 40 局）有没有掉 —— 别为了反制风格把通用实力丢了；
- **判定 ②**：**风格考卷**（新工具 `tools/style-exam.mjs`）有没有涨 —— 每风格一场（其余座位全是该风格）
  + 混合风格场 + 脚本对照场；关键是**留出场**：环经济冠军 `long-31`/`long-33` **没进过池**，
  用来区分"学会了反制"与"记住了陪练"。

**口径**：每场 30 局、`seat = g % N`、局种子 `SEED + g*977 + total`（与 `eval-5p` 同一范式），
两臂同 seed ⇒ 逐局配对。复跑命令见 `docs/HANDOFF.md` §6.5。

### 8.1 结果

**判定① 标准考卷（3 血 · 35 组合 × 40 局 = 1400 局/臂；n=6 配对）** —— 均值基本持平，但**方差爆炸**：

| seed | 控制臂 `ms2-p12` | 实验臂 `mix` | Δ |
|---|---|---|---|
| 31 | 37.2% | 38.1% | +0.9 |
| 32 | 40.4% | 41.2% | +0.8 |
| 33 | 36.6% | 34.8% | −1.8 |
| 34 | 37.1% | **18.8%** | **−18.3** |
| 35 | 36.6% | 36.6% | 0.0 |
| 36 | 37.0% | 29.0% | −8.0 |

均值Δ = **−4.40pt**（sd 7.58, t=−1.42, 2正/3负/1零 ⇒ **不显著**）；两臂平均 37.48% → 33.08%。
**但单跑极差：控制臂 3.8pt、实验臂 22.4pt（6×）** —— 4 个 seed 几乎没动、2 个崩了。

**判定② 风格考卷（每场 30 局；n=4 配对，seeds 31-34）** —— **没涨**：

| 场 | 控制臂 | 实验臂 | 均值Δ | t | 符号 |
|---|---|---|---|---|---|
| 激光剑流 ×4 | 11.7% | 10.0% | −1.7 | −0.30 | 1正/2负 |
| 狙击枪+环流 ×4 | 45.0% | 25.8% | −19.2 | −2.30 | 1正/3负 |
| 坦克流 ×4 | 17.5% | 18.3% | +0.8 | 0.14 | 2正/2负 |
| 枪+墙流 ×4 | 7.5% | 10.8% | +3.3 | 1.41 | 2正/0负 |
| **`long-31` ×4（留出）** | 38.3% | 35.8% | −2.5 | −0.34 | 2正/2负 |
| **`long-33` ×4（留出）** | 46.7% | 23.3% | −23.3 | −1.95 | 1正/3负 |
| 混合风格场 | 20.0% | 30.8% | +10.8 | 1.49 | 3正/1负 |
| 脚本对照场 | 56.7% | 35.0% | −21.7 | −1.58 | 1正/3负 |
| **风格场平均（6 场）** | **26.7%** | **22.1%** | **−4.5** | **−4.21** | **0正/4负** |

### 8.2 结论

1. **假设不成立**：把四个风格化冠军放进池子，**没有**换来"对不同风格都有反制能力"——
   风格场平均 **−4.5pt**（t=−4.21，**4/4 seed 为负**），**留出场也没涨**（long-31 −2.5、long-33 −23.3）。
   即"打练过的对手"都没变好，"没练过的对手"也没变好。
2. **唯一的名义正值（混合场 +10.8pt, t=1.49）不可信**：那是 8 个场里的 1 个，多重比较下期待值就有 ~1 个，
   且它本身不显著。**不要拿它当"部分成功"**。
3. **真正的代价是方差**：标准考卷均值只降 4.40pt（不显著），但**单跑极差 3.8 → 22.4pt（6×）**——
   4 个 seed 几乎不动、2 个崩掉。更硬、更异质的对手池把 **seed 彩票放大了约 6 倍**。
4. **本轮分不开"被强敌带坏"与"练脚本的量变少"**（设计缺陷，必须写明）：16 个对手但每代仍只打 8 局
   ⇒ 每个对手的练习量从约 2.67 槽降到 2.0 槽（**−25%**）。要分离这两个机制，正确做法是
   **把每对手预算拉齐**：16 对手配 `gpo≈11-12`，并且**同时重训控制臂**（否则口径不可比）。
5. **方法论收获（与 §6.4 的环实验合成一条）**：这是**第二次**"改对手池"的干预落空——
   ring2 是往池里加 1 个脚本对手（+1/13），本轮是加 4 个**真实风格冠军**（+4/16，强度高得多）。
   两次都没换来能读出来的收益。⇒ **对手池是弱杠杆；该改的是训练信号**（适应度 / 课程 / 每对手预算）。
6. 因此**`mix-*` 不进线上**，`js/bundled-champion-3p.js` 仍是 v1.3.58。
   但**能力保留**（`champ:` 对手 + 风格考卷）—— 它是后面"复合适应度"实验的必要工具。

---

## 9. 复合适应度：把"风格表现"写进 fit（v1.5.2）

**动机**（用户提出，我给出实现路径）：§8 与 §6 两次"改对手池"都落空，而且 §8 还**摊薄**了每个对手的
练习量（16 对手仍只打 8 局 ⇒ 每对手 −25%）。⇒ 换一条路：**不改池子**，在**池子预算之外**给每个个体
**追加 k 局对风格冠军的局**，把它们的 1st 率按权重并进 `fit`：

```
fit = 池子那部分的适应度 + STYLE_W × (风格局 1st 率)
```

**为什么这是诚实的复合目标**：风格局走**原生规则**（`regen=0`、无补贴、无补 ep），量的是真实强度；
不是把人推向"只在特殊口径下成立"的策略（§5.2 的教训）。**追加**而非替换 ⇒ 池子练习量不变（D13 钉住这条）。

**接线（两端都要，§5.2-17 的教训）**：`evo.js` 加 `setStyleSlice(opps, w, games)`（调用方显式传入，
默认关闭 ⇒ 既有路径逐位不变）；`train-server` 解析 `?styleopps/?stylew/?stylegames`、走**统一解析器**
校验、写进产物 meta，并在**建 worker 之前**设好；`paralleltrain` 把名单/权重/局数**随消息下发**；
`train-worker` 在**自己沙箱**里再设一遍并回执 `styleGames`；服务端**校验回执**（不符立即中止）。
守门用例 **np-test D13**（62/62）：关闭时 0 局 / 打开时恰好 k 局 / `fit−fitNoDiv == w×styleRate` /
**池子那部分 fit 逐位不变**（追加而非替换）/ 两端都接线。

**设计（同池 / 同种子 / 同起点 / 同 worker 数，唯一差别是切片）**：
- 控制臂 = `ms2-p12-31..36`（12 个脚本对手，无切片，未重训）
- 实验臂 = `sty-31..36`（**同一批 12 个脚本对手** + 切片：**3** 个风格冠军 × 2 局/个体/代，w=0.5）
- **留出场**（没进切片）：**枪+墙流 `armA9`**、环经济 `long-31`、`long-33`
  ⇒ 这次能真正区分"学会了反制"与"记住了陪练"

复跑见 `docs/HANDOFF.md` §6.5。

### 9.1 结果

**判定① 标准考卷（3 血 · 1400 局/臂；n=6 配对）** —— **通用实力明显受损**：

| seed | 控制臂 `ms2-p12` | 实验臂 `sty` | Δ |
|---|---|---|---|
| 31 | 37.2% | 31.5% | −5.7 |
| 32 | 40.4% | 26.9% | −13.5 |
| 33 | 36.6% | 35.9% | −0.7 |
| 34 | 37.1% | 40.1% | +3.0 |
| 35 | 36.6% | 27.5% | −9.1 |
| 36 | 37.0% | 27.1% | −9.9 |

均值Δ = **−5.98pt**（sd 6.17, t=−2.38, **1正/5负**）；两臂平均 37.48% → 31.50%；单跑极差 3.8 → 13.2pt。

**判定② 风格考卷（每场 30 局；n=4）** —— **没有系统性收益**：

| 场 | 控制臂 | 实验臂 | Δ | t | 符号 |
|---|---|---|---|---|---|
| 激光剑流（**练过**） | 11.7% | 14.2% | +2.5 | 0.47 | 2正/2负 |
| 狙击枪+环流（**练过**） | 45.0% | 49.2% | +4.2 | 0.38 | 2正/1负 |
| 坦克流（**练过**） | 17.5% | 17.5% | 0.0 | 0.00 | 2正/1负 |
| 枪+墙流（**留出**） | 7.5% | 5.0% | −2.5 | −0.39 | 2正/2负 |
| `long-31`（**留出**） | 38.3% | 48.3% | +10.0 | 7.35 | 4正/0负 |
| `long-33`（**留出**） | 46.7% | 20.8% | −25.8 | −2.40 | 0正/3负 |
| 混合风格场（练过的 3 个） | 18.3% | 21.7% | +3.3 | 1.41 | 2正/0负 |
| 脚本对照场 | 56.7% | 57.5% | +0.8 | 1.00 | 1正/0负 |
| **风格场平均（6 场）** | **26.4%** | **25.2%** | **−1.2** | **−0.59** | **2正/1负** |

注意两件事：**"练过"的三场一个都没涨出显著**（+2.5 / +4.2 / 0.0，t≈0–0.5）——
而这三场**正是被写进 fit 的**；而**脚本对照场 +0.8**（t=1.00）说明**池子练习量没被摊薄**（切片确实是追加的，
设计目的达到了，问题在别处）。

### 9.2 结论

1. **第三条路也没走通**：把风格表现写进适应度，**付出了 −5.98pt 的通用实力**，却没换来对
   **它自己训练过的那三个风格**的优势（三场全不显著）；留出三场互相抵消（+10.0 / −25.8 / −2.5），
   风格场平均 −1.2pt（t=−0.59）。
2. **机制（这条最有价值，而且与项目自己踩过的坑同型）**：每代只追加 **2 局**风格局 ⇒ 风格 1st 率的
   估计**噪声极大**（30 局口径下单跑 sd 都有 2.7–21.5pt）⇒ **等于把噪声当分数做选择**：
   被选出来的是"这 2 局运气好"的个体，而不是真会打风格的个体。症状正是"噪声选择"的典型样子——
   **被优化的目标量（风格场）没涨，真实目标（标准考卷）被挤掉**。
   这条错误**项目里已经记过一次**：v1.3.18 的结论是「**探针是门槛，不是分数**」
   （`min(pairSc, probeSc)` 跨量纲把冠军推向探针，改成"probe ≥ 阈值才进排序、排序仍按 pairSc"）。
   本轮我把风格率当**分数**加进 fit，等于又犯一次。
3. **下一步（便宜且明确）**：① 先**量清楚** k 局风格率的噪声（k=2/4/8/16 各跑一批同 seed，看 sd）；
   ② 要么把 k 抬到与池子预算同量级（k≈8）再当分数用，要么**改成门槛/band**
   （`styleRate ≥ 阈值` 才进入择优，排序仍按池子分，照 v1.3.18 的修法）。
4. 线上冠军仍是 **v1.3.58**；`sty-*` 不进线上。能力（`champ:` 对手 + 风格考卷 + `setStyleSlice` 开关）
   与守门用例 **D13** 保留 —— 它们是下一轮"把噪声当门槛"实验的必要件。
5. **三次"想让它学会更多"的干预全部落回"无收益或有代价"**：ring2（池里加 1 个脚本对手）、
   mix（池里加 4 个真实风格冠军）、sty（把风格表现写进适应度）。
   共同教训：**在这套进化搜索里改目标函数，必须先看"目标量的信噪比"** ——
   信噪比不够时加进去的不是信号，而是选择噪声。

---

## 10. 奖励机制与训练池：两次**干净** A/B（v1.5.7）

**为什么非要"干净对照"**：v1.5.4 改 `全息屏障` 语义后，**整族旧产物都被打坏** —— 旧的对照臂
`ms2-p12`（12 脚本池 × 6 seed）标准考卷均值从 ~**37.5%** 掉到 **21.20%**。
拿它当对照去测任何东西都会被"HOLO 语义修正"污染（B1 第一版就吃到：表面 Δ+12.30pt 全是假信号）。
⇒ 做法：① 奖励 A/B 用 `EPIRUS_ECO_*` 在**同一引擎**上重跑旧门槛（对照臂 `rwd0`）；
② 池子 A/B 用 `eco`（12 对手）vs `def`（15 对手）—— **引擎/奖励/起点/种子/模式全同，只差池子**。

### 10.1 奖励改动（新门槛 5/10 + 熵 0.06）vs 旧门槛（4/10 + 熵 0）
| 指标 | `rwd0`（旧） | `eco`（新） | Δ |
|---|---|---|---|
| 标准考卷 1st | 29.93% | 33.50% | +3.57pt（t=0.91, 3正/3负, **不显著**）|
| **最高 ep 均值** | **44.8** | **12.8** | ↓↓ |
| **ep≥3 决策占比** | 34.3% | 5.8% | ↓↓ |
| cost≥3 出手占比 | 2.72% | 0.82% | ↓ |
| **出手种类（技能数）** | 13.7 | **14.8** | ↑ |

**结论①：熵奖励按预期提高了技能广度，但它与 ep 奖励互相打架。**
熵是按**动作分布**算的，而"攒钱"就是反复出ジ ⇒ 熵极低 ⇒ **熵奖励其实在惩罚攒钱**
（最高 ep 44.8→12.8、ep≥3 决策 34.3%→5.8%）。实力上没损失（+3.57 不显著），
但它会把**深经济**（§8/§9 与千问体检都指出：5 血里最强的那条线）**压掉** ⇒ 长程训练里很可能有害。
⇒ 两个修法：**(a)** `divW` 再降一个量级（0.006）；**(b)** 更对劲：**熵只统计"非ジ"的动作**
（ジ 是等待/攒钱，不该被算成"塔成一招"）。
（顺带解释了 Q3 当初为什么把熵移出目标函数 —— 不只"熵≠见过"，它还会与攒钱项对冲。）

### 10.2 池子加三个**单一防御 specialist**（15 对手）vs 12 对手
考卷2 = **长程口径**的 4 座反弹墙（`--mode=long --field=reflectwall`，即千问说的那个 regime）：
| 指标 | `eco`（12 对手） | `def`（15 对手） | Δ |
|---|---|---|---|
| 标准考卷 1st | 33.50% | 34.58% | +1.08pt（**不显著，没掉**）|
| 训练自评 | 35.62% | 42.92% | **+7.30（t=4.99，6正/0负）** |
| **长程反弹墙 1st** | 64.17% | **93.33%** | +29.17pt（t=1.24，**sd 57.8**）|
| 逐 seed（eco→def） | 100/**0**/100/85/**0**/100 | **100/100/100/100/60/100** | **灾难点（0%）消失** |

**结论②：千问的"缺的是**见过**，不是**选不出**"被支持。** 池里加 3 个单一防御 specialist 之后，
长程反弹墙从"2/6 个 seed 拿 **0%**"变成"**5/6 拿 100%、最差 60%**"，而标准考卷不但没掉还 +1.08pt。
**这修正了我 v1.5.2 的结论**：当时说"对手池是弱杠杆"，但那两次加的是**复杂策略对手**
（1 个脚本 / 4 个风格冠军）；这次加的是**"见过就能反制"的单一 specialist**
⇒ 精确说法是：**加复杂策略对手是弱杠杆，加单一防御 specialist 不是**。
⚠️ 判据要看"**有没有灾难点**"而不是均值：这个场本身双峰（sd 57.8），均值 Δ 不显著是噪声造成的。

---

## 11. 摆烂均衡、互套盾僵局、以及"考卷分会被熬骗"（v1.5.8，浏览器对战测试查出来的）

### 11.0 起因：用户要求"自己开浏览器做对战测试"
新增 **`tools/battle-test.mjs`**（真 Chrome + CDP）：多人 5 人 + 难度选「冠军」⇒ AI 座跑线上冠军；
P0（人类座）一直摆烂出局后页面走 `autoRunRest()` 自动打完；全程收 JS 错误 + 截图 + 断言"打架活跃度"。
**第一次跑就抓到东西**：

```
第 39..59 回合（观战）：🛡 玩家2 用【全息屏障】护住 玩家3
                        🛡 玩家3 用【全息屏障】护住 玩家2      ← 每回合互套盾，伤害恒为 0
```

### 11.1 事故一：页面测的根本不是"上线包"
`ui.js` 的 `loadMultiChamp()` **优先读 `localStorage['epirus.champion3p']`**，没有才回退内置 bundle。
⇒ 那局是**旧冠军 v1.3.58** 在打（正好对上它每局出 27.8 次盾的特征）。
**这是真实的交付问题**：换 bundle 对"曾在自己浏览器里训练/导入过冠军"的用户**无效**（需在发布说明里写明）。
工具已改成先清 localStorage 再刷新。

### 11.2 事故二：v1.5.4 **之前**训练的冠军会把盾套给对手
它们学的是"1 费自保"，而 v1.5.4 把这张卡改成了"给目标套盾"⇒ 出卡 = 护对手。
引擎量化（5 座全是同一冠军、20 局自对局）：v1.3.58 **全息屏障 27.8 次/局**；
而 v1.5.4 **之后**训练的臂（`eco-33`/`def-33`/`rwd0-33`）**0.0 次/局** ✓ ⇒ 修法是**用新语义重训**，不是改规则。

### 11.3 事故三（最要紧）：**考卷 1st 率会被"熬"骗**
新工具 **`tools/champ-audit.mjs`** 把"打架活跃度"变成硬指标，一量就露馅
（A=canonical 多人 3 血考卷 1st；B/C=5 座同一冠军的自对局；D=长程口径 4 座纯反弹）：

| 冠军 | A 考卷 | B 伤害/局 | B 重击/局 | C 盾/局 | 零伤害率 | 平局率 | 回合 | D 反弹墙 |
|---|---|---|---|---|---|---|---|---|
| **long-33**（v1.5.7 换成线上） | **45.3%** | **0.0** | 0.0 | 0.0 | **100%** | **100%** | 60.0 | 100% |
| **eco-34**（v1.5.8 改换上线） | **45.6%** | **13.6** | 0.0 | **0.0** | 0% | 0% | 28.7 | 85% |
| long-34 | 32.9% | 19.6 | 19.6 | 0.1 | 0% | 0% | 29.6 | 25% |
| long-31 / 32 / 36 | 30.6–35.7% | 13–15 | 4–8 | 0.0 | 0% | 0% | 22–49 | 25–45% |
| eco-36 | 37.7% | 13.4 | 0.0 | 0.0 | 0% | 0% | 34.6 | 100% |
| v1.3.58（旧线上） | 14.9% | 12.2 | **0.0** | **27.8** | 0% | 0% | 45.6 | 100% |

**long-33 在 5 座全是自己的自对局里 20/20 局零伤害、60 回合平局** —— 它只刷聚能环与ジ、从不出手，
那个 45.3% 是"熬到回合上限比血量"赢来的。千问体检 §5-4 早就点过这个口径陷阱
（"改报 **cost≥3 的落地伤害** 与**哨声前 HP 领先占比**，后者能量出'赢'还是'熬'"）——
**我没有把它变成硬指标就换了冠军，这是我的失误**（v1.5.7 的依据只有 1st 率）。

### 11.4 熵修复（v1.5.8）让摆烂**更严重**了 —— 说明熵只是二阶因素
用户要求"熵只统计非ジ动作"（因为旧口径在惩罚攒钱）。修法本身 ✓（D17 钉住"掺进任意多ジ不改熵"），
但对照一训就看出来：**修好的熵 ⇒ 更摆烂**：

| 臂 | A 考卷 | B 伤害/局 | 回合 | 说明 |
|---|---|---|---|---|
| `eco`（旧熵口径，含ジ） | 34.7% | 13.9 | **25.2** | 熵在惩罚攒钱 ⇒ 被"逼"着打 |
| `eco2`（修好的熵） | 31.4% | 12.4 | **56.0** | 攒钱不再被罚 ⇒ 摆烂回来了 |

⇒ **真正让冠军摆烂的是"胜利条件允许熬"**，而 fit 里的出手项太弱（`deal` 0.01 / `proact` 0.02）。
对策（v1.5.8 已实现待验）：**哨声惩罚** —— 终局时还有 ≥2 人活着（没人被淘汰）⇒ 胜利打折
（`EPIRUS_FIGHT_WHISTLE=0.5`），出手权重也做成可调（`EPIRUS_FIGHT_DEAL`）；守门 `np-test D18`。

### 11.5 结论（要变成规矩）
1. **单一 1st 率不能当验收指标**：换冠军必须过 `tools/champ-audit.mjs`（考卷 + 打架活跃度 + 病理 + 反弹墙）。
2. **自对局必须真的打起来**：零伤害率 / 平局率 / 回合数 三项都要看；`long-33` 是反例。
3. **换 bundle 不会影响 localStorage 里有冠军的用户** —— 发布说明必须写明（或提供"恢复内置冠军"按钮）。
4. 浏览器对战测试（`tools/battle-test.mjs`）值得进常规流程：它抓到的三件事，Node 沙箱一件都看不见。

### 11.6 第三种僵局：**没人进攻时，AI 一起摆架势**（修正后的对战测试才发现）
第一版对战测试让 P0 一直出ジ"摆烂" ⇒ 那等于**人为造出"没人进攻"的局面**。引擎复现
（1 座固定策略 + 4 座冠军，10 局）：

| P0 座 | 伤害/局 | 回合 | 平局 | 架势/局 |
|---|---|---|---|---|
| 只出ジ（摆烂） | **3.0** | 60.0 | **10/10** | **220.5** |
| `balanced` 脚本 | 13.1 | 24.1 | 0/10 | 13.3 |
| `aggro` 脚本 | 13.9 | 22.9 | 0/10 | 15.5 |
| `random` 脚本 | 12.0 | 39.9 | 1/10 | 60.5 |
| 五座全冠军 | 13.6 | 28.7 | 0/10 | 7.1 |

⇒ **冠军是愿意打的**（对面一旦出手就正常开打），僵局只出现在"所有人都只防守/等待"这种**退化输入**下。
**测试设计教训**：对战测试的人类座必须**真的进攻**，否则量到的是退化均衡 ——
`tools/battle-test.mjs` 已改：P0 每回合挑一个可负担的攻击技（枪/激光剑/坦克/狙击枪/电磁炮/落雷/摄魂指法），
在多人局弹出的目标框 `#modal-card #mbtn` 里选目标，打不出来时才退回蓄能/ジ。

**修正后的实测（上线冠军 eco-34）**：40 回合内 **21 次伤害、0 次全息屏障、0 JS 错误**，
28 回合终局 **4 人被 KO、胜者剩 1 HP**（截图 `results/battle-0{1,2,3}.png` 已人工复核：
开局页显示 多人/5 人/难度=冠军/程序 v1.5.8；终局页显示"玩家5 获胜 · 共 28 回合 · 四人 HP 0、玩家5 HP 1"）。

### 11.7 反摆烂的**根因修复**：哨声惩罚实测（v1.5.8）
做法：把"熬出来的胜利"打折 —— 终局还有 ≥2 人活着（没人被淘汰）⇒ 胜利 × (1−`WHISTLE_PEN`)，取 0.5。
对照设计：`eco2-*`（**修好的熵、无**哨声惩罚）vs `fgtA-*`（修好的熵 + 哨声惩罚 0.5），其余全同（同池/同种子/同起点）。

| 冠军 | A 考卷 | B 伤害/局 | B 重击/局 | 零伤害率 | 平局率 | 回合 |
|---|---|---|---|---|---|---|
| `eco2-33`（无哨声惩罚） | 31.4% | 12.7 | **0.0** | 0% | 0% | **55.2** |
| **`fgtA-32`** | 37.0% | **15.7** | **9.2** | 0% | 0% | **13.7** |
| `fgtA-36` | 25.0% | 15.0 | 7.8 | 0% | 0% | 15.3 |
| `fgtA-33` | 34.7% | 13.6 | 4.9 | 0% | 0% | 39.4 |
| `fgtA-34` | 37.0% | 11.0 | 0.1 | 0% | **20%** | 35.6 |
| `fgtA-31` / `fgtA-35` | 35.4% / 28.3% | ~12 | 0.0 | 0% | 0% | ~50 |
| `eco-34`（**当前线上**，无哨声惩罚） | **45.6%** | 13.6 | 0.0 | 0% | 0% | 28.7 |

**结论**：哨声惩罚**确实把"熬"改成了"打"** —— 回合数 55.2 → 13.7~39.4，**cost≥3 落地伤害从 0.0 变成 4.9~9.2**
（资源真的转成伤害，而不是攒着），且没有零伤害局。代价：考卷分中位下降（最好的 `fgtA-32` 37.0% < `eco-34` 45.6%），
且一个 seed（`fgtA-34`）出现 20% 平局率。
⇒ 它是**有效的"训练处方"**（想让冠军更果断、更快终局时就开），但**当前线上冠军维持 `eco-34`**：
考卷最高（45.6%）且自对局已经打得起来（13.6 伤害/局、0% 平局、28.7 回合）。
⇒ 更根本的修法在**规则层**（例如长局加入"伤害递增/毒圈"来消灭"熬"的收益）—— 那需要用户裁定。

### 11.8 哨声惩罚在**长程**是空操作（v1.5.12 回滚；证据是"两条臂产出逐字节相同"）
v1.5.11 我按"长程上限 140、终局收缩要到第 100 回合才起效 ⇒ 在那之前熬仍划算"的推理，
把哨声惩罚设成**长程默认开 0.5**（其余模式 0），用户说"按你的意思做"就上线了。随即做了干净配对验证：

| 臂 | 设置 |
|---|---|
| `lngC-*` | 长程，`whistlePen` **自动** = 0.5 |
| `lngD-*` | 长程，**显式** `EPIRUS_FIGHT_WHISTLE=0` |

其余全同（12 对手池 / 6 seed / 起点 v1.3.58 / 250 代 / 同引擎）。结果：
**6 个 seed 里能比的 5 个，两份产物逐字节相同**（`lngC-31 == lngD-31` … `-35`），
配对统计也完全一致（自评 Δ=+2.06、标准考卷 Δ=+9.23、反弹墙 Δ=−8.33 —— 因为两条臂本来就是同一个东西）。
而两份 meta 分别正确记着 `whistlePen:0.5/override:null` 与 `0/override:0` ⇒ **接线没问题**。

**原因**：终局收缩 + 5 血 ⇒ 长程局几乎总以"有人被淘汰"结束（v1.5.10 实测 12/12），
`aliveEnd >= 2` 的**哨兵局 ≈ 0%** ⇒ 惩罚从不触发 ⇒ 对选择零影响。
惩罚真正有用的是**多人 3 血**（上限 60、收缩不触发 ⇒ 哨兵局常见）：v1.5.9 实测 0.5 能把"熬"改成"打"
（回合 55→14~39、cost≥3 重击 0→5~9）。

**处置**：v1.5.12 **回滚**该默认（`WHISTLE_PEN` 回到 0，只认显式 env/参数），理由写进 `whistlePenNow()` 注释
（防止以后又凭推理加回来）。**教训归档**：「按模式猜默认值」必须先证明**该模式真的会触发那条规则** ——
这次的验证成本只有两条臂 ≈20 分钟；不验证的话，线上会长期挂着一个"看起来在防摆烂、其实从未生效"的开关。

**副产品**：这两条长程臂是"当前处方"下的长程冠军，体检显示**没有比 `eco-34` 更好的**
（标准考卷均值 30.4% < `eco-34` 的 42.9%）⇒ 上线冠军不变。

---

## 12. 冠军的"集体防御"是怎么来的（v1.5.13 后用户实测提出，已定位机制）

**用户观察**：跑了一局 34 回合（长程 5 血、难度=冠军）——"前期莫名其妙的集体防御，直到血线拉开差距
才开始进攻"。对局记录 `results/epirus-battle-34回合.txt`。

### 12.1 先看那局本身（记录量化）
- 你（玩家1）**23/34 回合出的是ジ**（68%），主动进攻 8 次 ⇒ AI 面对的是一个**基本不进攻**的对手。
- 场面：第 1-2 回合全员ジ → **第 3 回合集体爆发**（4 人同时激光剑，玩家4 一轮吃 3 点）→
  第 4~8 回合**集体转架势**（八卦阵/防御/反弹）→ 之后零星互殴 → 最后 10 回合又基本是ジ。
- 那波爆发的后果就是**互戒**：谁先出手谁被集火（玩家4 一轮掉 3 血）⇒ 全场改摆架势等便宜。

### 12.2 因果实验：**架势率由对手活跃度决定**（12 局/臂，长程 5 血，4 个 AI 座都是冠军）
| 人类座的行为 | AI 架势率 | AI ジ率 | AI 进攻率 | 伤害/局 | 回合 |
|---|---|---|---|---|---|
| **用户当时（68% ジ）** | **75.0%** | 17.1% | 7.9% | 23.8 | 82.6 |
| 全摆烂（100% ジ） | **95.8%** | 3.1% | 1.1% | 24.9 | 103.9 |
| 每回合都进攻 | **11.9%** | 59.2% | **28.9%** | 23.8 | **28.3** |

无人类座的对照：5 座全冠军（long 5血）架势 46.1% / 进攻 16.8% / 63.3 回合；
5 座全冠军（multi 3血）26.6% / 23.6% / 42.2；**2 座冠军单挑只有 10.0% 架势 / 30.2% 进攻** ✓。

### 12.3 机制（三条绑在一起）
1. **三张架势牌费用都是 0**（`防御/反弹/八卦阵` cost=0）⇒ 摆架势是**免费的免伤手段**；
   而进攻要花 1~2 ep（枪 1、剑 2）**还会把自己变成靶子** ⇒ 对称局面下"不打"是局部最优。
2. **胜利条件奖励不掉血**（活到最后 / 上限时血最多；多人局上限 60）⇒ 防守没有代价。
3. **适应度里没有"先手"激励**：`deal` 只有 0.01、`proact` 是**相对**全场均值的（全场都 0 伤害时它恒 0）
   ⇒ 大家都在摆架势时，谁都没有梯度去当那个先出手的人。
⇒ 结果是一个**多人局的互戒均衡**（不是 bug，是学出来的合理策略）；人类不出手时它被推到极端
（架势 75~96%）。**单挑（2 人）不会这样**（没有第三方可躲，架势只有 10%）—— 这也是 2P 一直没暴露的原因。

### 12.4 可选修法（给用户的决策项，未实施）
- **A. 训练侧加"先手/首次伤害"激励**（把 `deal` 提到 0.05、或给"本回合第一个造成伤害的人"额外权重）：
  直接改冠军策略，不动规则；验证要跑臂（≈25 分钟/臂）。
- **B. 规则侧让架势有代价**（例如**多人局**里 `防御/反弹/八卦阵` 收 1 ep，或"连续同一种架势失效"）：
  立刻生效、训练也跟着变；但要注意 **2P 冻结** ⇒ 只能挂多人局字段（像 `MULTI_ONLY` 那样）。
- **C. 规则侧"无进展加速收缩"**（若连续 K 回合没人掉血 ⇒ 提前进入终局收缩）：
  对**彻底摆烂**有效（long-33 那种 0 伤害局），但对本案（每几回合还是有人掉血）**作用有限** ✗ 实测：
  这局最长无伤害区间只有 **4 回合**（第 4-7 回合）⇒ K 取 8 根本不会触发。
- **D. 接受它**：AI 的行为在规则内是理性的，人类一进攻它们就正常开打（11.9% 架势 / 28.3 回合）✓。
  若目标是"陪练体验"，A 或 B 才有实质效果。

---

## 13. 选项 A（训练侧先手激励）实测：行为治好了，但考卷掉 14pt（v1.5.14）

**实施**（用户裁定选 A，见 §12.4）：`EPIRUS_FIGHT_DEAL`（出手权重 0.01→0.05）+ **新增**
`EPIRUS_FIGHT_FIRST`（**本局第一个造成伤害的席位**拿 0.08）。**默认都是关**（沿用 §5.2-20 的教训：
不猜默认值，先证明有效）。判"先手"的纯函数 `T.firstBloodSeat(events)` 只认带 `source` 的伤害事件
（终局收缩是 `source:null`，不算任何人的先手）；守门 `np-test D21`。
关键设计理由：**首伤奖励是"区分名次"的项**（每局只有一个席位拿到），而统一加在所有人身上的项
（如哨声惩罚）在对称局面里不改变排序 ⇒ 这是它可能有效的根据。

**臂**：`fstA`（多人 3 血）/ `fstB`（长程 5 血），各 6 seed × 250 代，12 脚本对手池，起点 v1.3.58。

### 13.1 行为：**5/6 seed 打掉了"集体防御"** ✓
| 冠军 | E 被动场架势 | F 活跃场进攻 | F 回合 | 考卷 1st |
|---|---|---|---|---|
| `eco-34`（现役，无激励） | **95%** ✗ | 24% | 28.1 | **42.9~45.6%** |
| `fstA-31/32/33/34/36` | **0~42%** ✓ | 10~37% | 11~32 | 26.0~31.7% |
| `fstB-31/32/33/34/35` | **0~1%** ✓ | 7~37% | 12~39 | 27.0~35.3% |
| `fstA-35` / `fstB-36` | 94% / 93% ✗ | — | — | 34.7% / 26.7% |

### 13.2 代价：考卷 **42.9% → 约 29~30%（均值）**，训练自评显著下降
`fstA` 标准考卷均值 29.3%（6 seed：24.3/30.4/30.7/29.4/33.4/27.7）；
`fstB` 30.3%。两臂的自评配对都显著下降（fstA Δ−8.24，1正/5负，p<0.05）。

### 13.3 怎么读这个代价（重要）
**这 14pt 本身就是证据**：在当前规则下，"进攻"会被惩罚（`防御/反弹/八卦阵` 费用为 0 ⇒ 免费免伤；
先动手的人还会被集火）⇒ 逼它主动就要牺牲分数。
⇒ 想要"又强又主动"，杠杆在**规则**（§12.4 的 B：让架势有代价），而不是适应度：
适应度只能改变策略的偏好，改变不了"进攻不划算"这个支付结构。
**当前处置**：激励保持**默认关**（产物 `fstA-*`/`fstB-*` 入库，未上线）；上线冠军仍是 `eco-34`。
推荐候选（若用户要"手感优先"）：**`fstB-34`**（考卷 35.3%、伤害 16.3/局含 14.6 重击、
被动场架势 1%、平局 0%、28.8 回合）。

### 13.4 工具：体检表新增 **E/F 两列**（`tools/champ-audit.mjs`）
E = **面对被动对手（只ジ）时的架势率**；F = **面对活跃对手时的进攻率与回合数**（`--mode=` 可切换模式）。
起因就是用户这次反馈：**这两列能把"集体防御"直接量出来**（`eco-34`：E 95% / F 24%），
而原来的 A~D 四列完全看不到这类问题。

## 14. 镜面反射回归原始规则：没有"无可复制"（v1.5.16，用户裁定）

### 14.1 起因：用户对 N14 的澄清
用户原话：「**没有"无可复制"这回事 —— 所有技能都能复制**」；架势 / 自增益 / 能量类**复制到使用者自己身上**，
同时对 t2 形成一个**"空指"**（指向保留、本身不产生效果）；**空指仍算"指向"，要参与连带 / 传导**。
用户给的例子：「a 用镜面反射复制 b 的【反弹】，指向 c —— 这个指向本身没有作用；但若这时候正好 d 用
【真正的落雷】打 a，则 **c 会受到大雷传导的影响**，而 **a 和 b 因为有反弹所以不受影响**」。
旧实现里 `copyEffect(key)` 返回 null（= 绝大多数非伤害技能）时只发一条 `mirrorNoEffect`「无可复制」
⇒ **复制了等于没复制**。

### 14.2 这个例子把"生效时机"钉死了，而它和代码的结算顺序冲突
- 引擎顺序：① 避雷针 → ② 小雷(pri5) → ③ **大雷(pri4)** → ④ 默认 pri3 → **④b 镜面反射(pri3)** → ⑤ 枪(pri2) → ⑥ 狙击(pri1)。
  大雷**先**结算，而且规则是"目标非防御类技能一律无效化" ⇒ 照字面实现，a 的镜面反射会被大雷**作废**、
  复制到的反弹来不及出现 ⇒ **a 应该被打中**，与用户例子相反。
- 但引擎里"架势"本来就是**声明式**的：`guardOf` 读的是**本回合的行动**，所以同回合摆的架势能挡住先结算的大雷
  （③ 的代码就是边结算边读 `guardOf(t)`）。按同一口径，**复制来的架势也必须在声明那一刻生效**。
- 落地（不引入新状态字段，避免"两处真相"）：`effectiveKeyOf(state,pid)`（镜面反射的**有效技能** = 它复制到的那张卡；
  t1 被作废 ⇒ null）→ `guardOf` 走有效技能 → `copiedGuardKind`（复制全息屏障 = 原型制御式自保）；
  大雷效果2 的"非防御类无效化"、小雷的防御族豁免、激光眼破防**也都改用有效技能**（否则复制来的盾会被这几处漏掉）。

### 14.3 空指参与连带：连带判定要认**两个**被作用者
大雷连带的"与目标产生交互的第三方"原来只认 `targetOf`（= t1）⇒ 用户例子里的 c 收不到连带。
改为在快照阶段就记 `snapT2`（镜面反射取 `mirrorRefs` 的 t2；双枪取 `target2`），判定
`qTargetsT = snapT[q]===t || snapT2[q]===t`、`tTargetsQ = tTgt===q || tTgt2===q`。
同时把 N22 那条精确化：**只有"自己的行动就是防御族、且那张防御有作用目标"（金刚盾 / 藤甲）**才把连带转给目标；
**复制来的架势没有对外目标**（空指不产生效果）⇒ **完全挡住** —— 这正是例子里 b 不掉血的原因。

### 14.4 三处"只改一半就会静默出错"的地方都做成单点真源
- `mirrorRefs`：t1/t2 的兜底钳制（目标阵亡 / 非法 ⇒ 改指向），结算 / 架势 / 无效化 / 连带**四处共用**；
- `guardKindOfKey`：技能→架势的唯一映射（原来只在 `guardOf` 里存在一个 switch）；
- `applyMirrorSelf`：自效果表，配 `np-test D23⑥` 用 `R.MIRROR_SELF` 对**全部 30 个技能**做穷举归类
  （可复制伤害 / 有自效果 / 仅空指 —— **新增技能不归类就红**）。

### 14.5 证据
- `np-test D23`（7 组，含**用户例子的端到端复现**）：a、b 都 3 血不掉，c 吃 2 点连带，事件 `bigTChain.to === 2`。
- **反证三次全红**（实测，各自对应不同断言）：① 让自效果表失效（复制【ジ】不给 ep）⇒ D23③ 红（got=3 want=4）；
  ② `guardOf` 改回只读 `a.key` ⇒ D23① 红（"复制到的反弹必须是真架势"实测 `null`）；
  ③ 去掉大雷的 `snapT2` ⇒ D23② 红（got=3 want=1，c 不掉血）。
- ⚠️ **一个反直觉点（写反证时要选对锚点）**：架势是**派生**的（`guardOf` 现算，不落状态字段）⇒
  删 `applyMirrorSelf` 的 default 分支**不会**让 D23 红 —— 它真正影响的是日志事件与无极变速第二回合的 `guardNext`
  （那次反证确实让**另一个**用例红了，不是 D23）。
- 回归：`spec 37/37`、`np-test 72/72`、`smoke SMOKE OK`、`battle-test BATTLE OK`。
- 规则指纹 `a69358d6` → `e2241072`（改了 `rules.js`/`resolve.js`）⇒ 按"训练重启前置铁律"重测重记了冠军成绩。

### 14.6 两个刻意的实现选择（写下来免得以后被当成 bug）
- 复制**蓄能**的珠类型取**复制者自己**的选择（`镜面反射` 那张卡的 `opt.bead`，缺省电珠），**不读 t1 的 opt**：
  t1 蓄哪种珠是隐藏信息（v1.5.15 刚修的三处泄漏之一）⇒ 读它等于给复制者开天眼。
- 复制**避雷针**只给"3 回合免雷窗口"（R31 情形B），**不追溯**本回合已在飞的雷：① 避雷针那段早于镜面反射结算，
  追溯会凭空改写已结算的结果。

### 14.7 v1.5.17 修正（用户**二次**裁定，推翻 §14.2）
用户原话：「我觉得你对优先级的理解是对的，**大雷优先于镜面反射**，所以**反弹并没有被复制成功**，是我错了，这里改过来吧。」
- §14.2 那套"声明即生效"（`effectiveKeyOf` + `guardOf` 从复制内容派生 + 大雷豁免镜面反射）**整段撤回**：
  大雷(pri4) 先结算、把镜面反射**无效化**（镜面反射是特殊类、不是防御族）⇒ **复制不会发生** ⇒ 目标该挨打就挨打。
- 现在的实现：复制来的架势写到**状态字段** `p.copiedGuard`（`applyMirrorSelf` 在 ④b 写入、`startTurn` 清空），
  `guardOf` 读状态 ⇒ 它**挡得住随后结算的 ⑤ 枪 / ⑥ 狙击，挡不住先前结算的大雷 / 小雷**。
- §14.3 的 `snapT2`（"空指参与连带"）**一并撤回**：这次施法都被废了，那个"空指"根本不会产生 ⇒
  连带判定回到原口径（只看 `targetOf`）。
- 证据（`np-test D23②` 改写）：a 复制 b 的反弹、指向 c；d 用大雷打 a ⇒ a 的镜面反射 `voided`、
  **a 实打实挨 2 点（3 → 1）**、**没有任何复制产生**；b 有自己的反弹 ⇒ 连带打不动它；c 不受影响。
- **教训（值得留档）**：**"为了让某个例子成立"去改结算语义，是危险动作**。§14.2 的推理链（"引擎里架势本来就是
  声明式的"）自身自洽，但它与"优先级原则"这条**更上位**的规则冲突。正确顺序是先问「这个例子在本规则下真的成立吗」，
  而不是先让它成立 —— 这次是用户自己回来纠正了这个前提。
- 反证三条（实测全红，各红在不同断言）：① 把 §14.2 的两处改回去（派生 + 豁免）⇒ D23② 红；
  ② 自效果表失效（复制ジ不给 ep）⇒ D23③ 红；③ t1 被作废仍去复制 ⇒ D23⑤ 红。

### 14.8 用户三次澄清（同日）：**同优先级时防御类先出现**
用户原话：「复制的来的防御反弹之类**可以**挡得住同优先级对应能挡住的激光剑或坦克。**同优先级的时候防御类先出现**。」
- §14.7 那版把架势推迟到 ④b（镜面反射那一层）才落位 ⇒ 它挡不住**同优先级(pri3)的激光剑 / 坦克** ✗。
  用户这条把"什么时候生效"再细化一次：不是"镜面反射这一层"，而是"**优先级 3 里防御类先出现**的那一刻"。
- 实现：新增 `mirrorGuardPass(state)`，在 **④ 默认优先级 3 的循环之前**调用，**只落防御族**的架势
  （`p.copiedGuard`、`guardSet{copied,early}`、无极变速的 `guardNext`）；其余自效果（ep / 珠 / 雷 / 净化）
  仍在 ④b 结算。`applyMirrorSelf` 加 `!me.copiedGuard` 兜底，避免重复落位与重复事件。
- 与 §14.7 不冲突：大雷(③) / 小雷(②) 都早于 `mirrorGuardPass` ⇒ 目标被作废时 `actionOf(m)` 为 null ⇒ 跳过 ⇒
  复制照样不会发生。
- 证据：`D23②c`（同优先级坦克被挡住并弹回：P0 不掉血、P2 掉 1）、`D23②d`（激光剑穿反弹 ⇒ P0 掉 1 ——
  防止把这个澄清误读成"复制来的防御无敌"）。**反证第四条**：删 `mirrorGuardPass` 的调用 ⇒ `D23②c` 红。
