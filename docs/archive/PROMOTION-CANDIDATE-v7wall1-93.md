# 上线候选评估 ②：`docs/artifacts/v7wall1-93.bak`（**形状最好的那个**）

> **一句话**：它是本轮**唯一"宽 + 五道门全过"**的产物 —— 比在位包更广（`G_eff` 4.84 vs 3.59、
> 最大单卡 45.4% vs 55.7%）、五道门全过；**唯一卡住的是 G4**（**四格压线**，见 §4-1）
> （被**坦克线** 57% 打穿；在位包则是被**只枪** 65% 打穿）。
> **换与不换是你的决定**；本文只摆读数、代价与回滚方法。线上包 `e4b4f780dda2` **未动**。

## 1. 它是怎么来的

配置：池 E（16 对手）· `EPIRUS_IMIT_FRAC=0.5` · `EPIRUS_IMIT_TEACHER=pickDeepSaver` ·
`EPIRUS_IMIT_OVERRIDE=1`（真示范）· **`EPIRUS_WALL_FILTER=1`**（成员级破墙硬过滤）· seed 31–93。
臂 `v7wall1`，本候选 = **seed 93**（该臂唯一过五道门的）。

## 2. 与在位包的逐项对照（20 局长程自对局 + 完整体检）

| 指标 | 在位包 | **`v7wall1-93`** |
|---|---|---|
| **五道门** | ✅ 全过 | **✅ 全过**（座位 28.9pt · `G` **4.99** · 墙 17.1/局 · 场A 28% · 场B 清场 0.88/局）|
| **G4 最克那一格** | **只枪 65% ✗**（n=60） | **只枪 53% ✗**（n=120 **与** n=200 都指向这格）|
| G5 破防 | ✓ | ✓（未出现在阻断项里）|
| 平铺广度 `G_eff` | 3.59 | **4.84** |
| **最大单卡占比** | 55.7%（枪）| **45.4%（狙击枪）** |
| 覆盖（4 类 / 8 功能角色）| 3 / 3 | 3 / **4** |
| 各类占比 | attack **77.1** · defense 22.0 · special 0.8 · energy 0.1 | attack **54.6** · energy 25.4 · defense 19.9 · special 0.1 ⚠️ 见 §4-3 |
| 珠经济闭环（第 6 道）| ✗ | ✗ **（"首次闭环"已撤销 —— 见 §4-2）**：得珠 835 / 过期 834 / **花掉 1** ⇒ 花/得 **0.12%**、过期 **20.9/局** |
| 每回合出手伤害 | 0.629 | 0.391 |
| A 卷 1st / 长程 ringwall | — | 41.8% / **60.0%** |
| 零出手局 / 零伤害局 | 0% / 0% | **0% / 0%**（不是退化包）|
| G6 靶向率（只记录）| ✗（24.2%）| 38.5%（**仍低于 40%** 的自定线）|

## 3. 为什么它比候选 ① 更值得讨论

| | 候选 ① `v7f5t-31` | **候选 ② `v7wall1-93`** |
|---|---|---|
| 五道门 | ✅（**要座位 n≥400** 才过）| **✅（默认 n 就过）** |
| 广度 | **窄**：`G_eff` 2.39、单卡 **80.9%** | **宽**：`G_eff` 4.84、单卡 **45.4%** |
| 与用户目标（技能多样性）| **违背**（它是在位包更极端的特化）| **一致**（attack 77%→55%，energy 0.1%→25%）|
| 珠经济 | ✗ | **✓** |
| G4 弱点 | 无（六条全过，但样本依赖）| **坦克线 57%** |

⇒ 候选 ① 是"用多样性换抗只枪"；**候选 ② 是"既更广、又只差一格"**。

## 4. 必须知道的风险

1. **G4 仍是红的，而且弱点与在位包是同一格**：n=60 时最克格显示"坦克线 57%"，但
   **n=120 与 n=200 都稳定指向「只枪(1ジ压制·打最肥)」53%**（基线 19% / 21%）
   ⇒ 真实弱点是**只枪**，53% **仍 >45% 门槛**，只是比在位包的 **65% 好 12pt**。
   ⇒ 教训：**"最克那一格"本身就是个小样本统计量** —— n=60 会选错格子，n≥120 才稳；**报 G4 必须同时报 n**。
2. **它是 6 个里的 1 个**：同臂其它 5 个卡在 `G<3`（1.01~3.30）/ 座位 43.5pt / 场B 0.00
   ⇒ **"过门"仍可能是 seed 运气**（与 §9 的判定同型）。**换之前必须做同配置的 seed 扩展**（正在跑）。
3. **输出偏低**：每回合出手伤害 0.391（在位包 0.629）⇒ 它靠"广"而不是靠"狠"。
4. **`G6` 靶向率 38.5%** 仍低于自定线 40%（**只记录不阻断**）。

## 5. 换 / 不换的操作（你决定后才做）

- 换：`node tools/promote-champion.mjs docs/artifacts/v7wall1-93.bak`（不带 `--dry` 才真写；
  门不过会**中止且不换**，要越过得 `--force`；G4 属**候选门**，可用 `--force` 越过并留痕）。
- 回滚：`git checkout -- js/bundled-champion-3p.js index.html`。
- ⚠️ 训练进行中不要换（`.training.lock` 会让 `promote-champion` 拒绝）。

## 6. 判定（**已完成**）：**不建议换** —— 与候选①同型，是单点运气

同配置补 seed 94–99（臂 `v7wall2`）后：**0/7 过五道门**，**过门率 1/13**（只有 seed 93）：

| 产物 | 卡在哪 | 读数 |
|---|---|---|
| `v7wall2-94` | 座位 + `G` | 48.9pt（某座 58%）· G 2.14 |
| `v7wall2-95` | 座位 + `G` | **87.6pt（某座 88%）** · G 1.37 |
| `v7wall2-96` | 座位 + `G` | **100pt（某座 100%）** · G 1.16 |
| `v7wall2-97` | 座位 | 35.5pt（但 **`G` 5.63** —— 很宽）|
| `v7wall2-98` | 座位 + `G` + 场B | 93.9pt（某座 95%）· G 1.93 |
| `v7wall2-99` / `-31` | `G` | 2.39 / 2.12 |

- ⇒ **结论：不建议换包。** 而且这条配置在成绩上**并不更好**：12 seed A 卷均值 **44.4%**（对照 45.4%）。
- ⚠️ 新 seed 还暴露一个形状：`WALL_FILTER` 臂在 94–98 上出现**极端座位偏置**（某座 88% / 95% / **100%**）
  ⇒ 成员级硬过滤会把种群推向**单座专精**（比"窄"更糟：连座位都不公平）。
- ⇒ **两个候选的最终判定一致**（① 窄、② 宽，都是单点运气）⇒ **本轮没有可推荐的换包**。
  真正的拦路虎始终是 **G4 的「只枪」格**（在位 65% / ② 53%，门槛 45%）。
