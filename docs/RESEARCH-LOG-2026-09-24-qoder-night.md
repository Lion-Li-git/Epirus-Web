# 状态灯（09-24 夜 · Qoder · 击杀奖励训练实验）

> 自包含防失忆日志。**每完成一格就更新 DONE/DOING/NEXT + 时间戳**。
> 分支：`qoder-killreward-0924`（基线 = main `b8628eb`）。**不动 main、不动两槽线上包、不提交 `results/*`。**

## 今晚要做什么（用户 09-23 夜原话拆成的任务）
1. 用**两条击杀奖励规则**各训一只新冠军（"现在的冠军并没有学过这个规则"）。
2. 三条细化必须落实：
   - **蓄能算进开销**：电磁炮 / 激光眼 需要珠子 ⇒ 等价于多耗 1 ep ⇒ 规则 1 的同优先级并列判定里它们**占优**。
   - **无来源的天火、地雷也要回 ep** ⇒ 归因不能只看 `damage.source`（那是 R57 的规则语义，**不许改**）⇒ 加独立归因字段。
   - **转移伤害造成的伤害 = 其转移的伤害**（口径有两种读法，见"待决"）。
3. 别的探索自由做，做到 **09-24 09:00**。

## 硬约束（照旧）
- 规则改动**只在内存补丁里**：`js/core/*` 一字不动 ⇒ **规则指纹 `ebdbff36` 不变**、门禁不红、线上不受影响。
- 规则的**实现必须单一来源**：`tools/kill-reward-lib.mjs` 同时给 `probe-kill-reward` 与 `train-3p` 用（本仓"两处各写一遍"栽过六次）。
- 臂后 `cp`/产物写入必须以 exit code 为条件；`.bak` 要在 CHANGELOG 逐名点名（D82）。
- 判据/换槽要用户点头才动；今晚只训练 + 量。

## DONE
- 00:30 分支 `qoder-killreward-0924` 建好（基线 main `b8628eb`，工作区干净）。
- 00:32 摸清发射端：`damage{to,amt,reason,via,source}`；地雷 `via='mine'` + `source:null`（`mineHit` 手里**有** `mineFrom`，只是没写进事件）；
  天火 `via='firestorm'` + `source:null`（**R57 规则语义，不许改成有来源**）；转移 `{type:'transfer', to:转移者, from:原攻击者, dest, amt}`。
- 01:00 九只臂跑完（3 规则档 × 3 种子 · 300 代）＋ 全部回评读数 ⇒ 见 §K-2。
- 01:22 **v1.5.194 记账完成**：CHANGELOG 首版 = README = index.html 三处一致；D82 的 9 个 `.bak` 逐名点名；
  `np-test 186/186` · `spec-run 52/52` · `smoke SMOKE OK` · 指纹仍 `ebdbff36`（引擎未动）。
- 01:22 顺手抓到自己写的一条**假断言**：D140 的 overkill 用例注释写"目标只有 2 血"、代码却 `mkState([3,3,3])`
  造了 3 血 ⇒ 门红着告诉我"lib 少付了"，实际是**测试的起始局面和它自己的注释不符**（lib 的裁剪是对的）。
  ⇒ 教训同族：断言失败先怀疑量具，别急着改被测物。

## DOING
- 提交 + 推分支 `qoder-killreward-0924`（**不推 main**）。

## NEXT（按顺序，到 09:00）
1. 量"省事等价做法"：**在现状规则下直接把破防场 1st 当选**跑一版，看能不能不靠新规则就拿到"会破防"的包（§K-2 结论 ②）。
2. 补种子：4/6 vs 0/3 这条是今晚唯一方向干净的结论，但 n=3 ⇒ 再加 3 个种子把它推成"能说的话"。
3. 对 `kr1s1-band2` 做**只读**的 promote 式五闸门禁审计（不动槽、不写产物）。
4. 早报。

## 待决（明早问用户）
- 转移伤害的归因：算给**转移者**（我现在的默认）还是算给**原攻击者**？两种都做了开关，默认 `owner`。
- 激光眼"续招"那次不吃珠（费用 2）⇒ 是否也按 +1 计？现在按"只要该次出手需要珠子才 +1"实现。

## K-1 00:55 · 接线完成 + **预注册**（跑臂之前写死，不许事后挪）
**已做**：
- `tools/kill-reward-lib.mjs` = 规则的**单一来源**（`patchResolve` / `patchPlay` / `makeKR`），`probe-kill-reward` 与 `train-3p` 共用；
  探针里原来那份内联实现**已删**（3455 字符）—— 两处各写一遍会导致"训出来的冠军和量出来的冠军不是一套规则"。
- `train-3p` 接上 `EPIRUS_KILL_REWARD=0|1|2`（非法档 ⇒ `exit 7`）+ **行为式读回**（跑一局看 `__krPaid` 在不在、
  事件是否带上归因字段，缺一个就 `exit 7`）+ `meta.recipe.killReward` 写进产物。
  ⚠️ 第一版的读回探针用了 `EPIRUS_CHAMPION_3P` —— 那一刻冠军包还没进沙箱 ⇒ unpack 拿到 null ⇒ `shapeOf(null)` 抛。
  改成"脚本池跑一局"（钩子与谁下注无关）。
- 三条细化落实方式：**不动 `damage.source`**（R57 规则语义 + 大雷连带输入 + "终局收缩"哨兵都依赖它），
  而是补三个独立归因字段 `mineFrom` / `fireFrom` / `transferBy`；蓄能按"该手需要珠子 ⇒ 开销 +1"计；
  转移伤害默认记给**转移者**（`--kr-transfer=source` 可切另一种读法）。
- **补丁行为中性已证**：mode 0（打了补丁但不挂钩子）vs 未打补丁，40 局逐位相同。
- **新坐实的一条**（破防场 200 局）：3000 个伤害事件**全部无归因**，致命一击的 `via` 是 **`终局收缩`**
  ⇒ 那场里根本没人击杀任何人 ⇒ 加了地雷/天火/转移归因之后**仍然是 0 发放**。§N49 的结论从"没触发"升级为"**结构上不可能触发**"。

**预注册（跑臂之前）**：
| # | 判据 | 通过线 |
|---|---|---|
| P1 | 同规则世界里，新冠军 vs 现役（现役没学过这条规则） | 配对 Δ 1st > +5pt 且过 1.96·SE |
| P2 | **回到现状规则**（关掉奖励）新冠军 vs 现役 | Δ ≥ −3pt（不显著变差）才算"学到了规则之外的东西" |
| P3 | 广度：净兑现 `landG`（现状口径，5 人 20 局） | ≥ 现役的 2.66 |
| P4 | 破防栏（4 席只防御不还手） | 新冠军不能比现役更差（现役 = 0% 胜 / 100% 平） |
| P5 | 奖励发放次数（新冠军是否真在"制造击杀"） | 比现役高 ⇒ 机制生效的直接证据 |
| 红线 | 不换槽 / 不 promote / 不动 main / 不提交 `results/*` | — |
**臂**：`mode ∈ {1,2}` × `seed ∈ {1,2,3}` × 300 代（3 人 · 8 局 · 种群 8），外加 `mode 0` 同规格三只作对照。

## K-2 01:35 · **今晚最有用的一条**：击杀奖励不能"防龟"，但能当**课程**——用它训出来的臂，回到现状规则下**会破防**
九只臂（300 代 × 3 人 × 8 局 × 种群 8）：`kr1s*` = 规则 1 世界训的、`kr2s*` = 规则 2 世界训的、`kr0s*` = **对照**（现状世界训的，同一套代码/种子）。
回评一律用 `probe-kill-reward`（与训练**同一份规则实现**），**规则关掉（档 0）= 纯现状世界**：

| 臂 | 训于 | **破防场 1st（规则关）** | 平局率 | 头对头 vs 现役（规则关） | 净兑现 `landG` | 奖励发放（规则开时） |
|---|---|---|---|---|---|---|
| 现役 `cmin4-31` | — | **0.0%** | 100% | 16.7% | 2.66（3 种） | 0.59 / 0.65 |
| `kr1s1` | 规则1 | **100.0%** | 0% | **26.0%** | **3.21（5 种）** | 0.85 |
| `kr1s2` | 规则1 | 16.7% | 83% | 26.7% | 2.69（3 种） | 0.99 |
| `kr1s3` | 规则1 | **81.3%** | 19% | 20.7% | 2.13（4 种） | 0.89 |
| `kr2s1` | 规则2 | **63.3%** | 37% | 21.3% | 2.00（2 种） | 0.93 |
| `kr2s2` | 规则2 | **93.3%** | 7% | 20.7% | 1.67（3 种） | 0.94 |
| `kr2s3` | 规则2 | 0.0% | 100% | 16.0% | 1.00（1 种） | 0.98 |
| `kr0s1` | **对照** | 0.0% | 100% | 14.0% | 1.00（1 种） | 0.84 |
| `kr0s2` | **对照** | 0.0% | 100% | **36.7%** | 1.00（1 种） | 0.87 |
| `kr0s3` | **对照** | 0.0% | 100% | 22.0% | 1.00（1 种） | 0.97 |

- **破防能力：规则臂 4/6 能破防（>50%），对照臂 0/3，现役 0/1。** 这是今晚唯一"方向干净"的发现，且**规则关掉之后仍然成立** ⇒ 它是包学到的能力，不是规则的副产品。
- **机制解释**：奖励击杀 ⇒ 训练压力偏向"能把人打死"的打法 ⇒ 而破防场要求的恰恰是"把防御者打死"（防御者不还手，只有杀得动才算赢）。
  所以这条规则**作为上线规则无效**（§N49：龟缩场里根本没有击杀可归因），**作为训练课程有效**。
- **但 P1 的另一半是否定的**：把"学过规则的包"放回规则世界，它的提升**不比对照臂更大**（对照 kr0s1 从 14.0% 跳到 46.7%，比任何规则臂都猛）
  ⇒ **"学没学过"几乎不重要，规则本身在挑"本来就爱杀人"的打法** ⇒ 别指望"先改规则再让 AI 学"，直接按"能不能破防/杀得动人"挑就行。
- **种子方差主导**：同一套代码三种子，头对头 14.0%~36.7%、净兑现全塌到 1 种（对照臂）⇒ 今晚所有单臂 Δ 都要按"n=3 的种子噪声"读，别当结论。
  尤其 `kr0s2` 头对头最高（36.7%）却广度最差（1 种）——**胜率与广度今晚再次证明不是一回事**（与 §N24/§N32 同向）。
- **预注册打分（对照 §K-1 的表）**：P1 ✗（规则臂不比对照强）· P2 **部分过**（`kr1s1` 现状世界 26.0% > 现役 16.7% 且广度 3.21 > 2.66）·
  P3 只有 `kr1s1`/`kr1s2` 过线 · P4 **规则臂 4/6 vs 对照 0/3** · P5 ✓（规则臂发放次数普遍更高，机制确实生效）。
- **⇒ 明早给用户的建议**：不要上线这两条规则（§N49 已证它在龟缩场结构上失效）；
  但可以把"击杀奖励世界"当**课程/筛选器**用 —— 或者更省事的等价做法：**直接在现状规则下按"破防场 1st"当选**（我今晚可以顺手量一版）。

## K-3 02:05 · **推翻我昨晚那条"课程"结论的机制归因**：不用改规则也能拿到"会破防"的包 —— 把龟缩对手放上训练桌就够了
跑了两批新臂（同规格 300 代 × 3 人 × 8 局 × 种群 8）：
- `kr{0,1,2}s4..6` = 补种子（对照/规则1/规则2）⇒ 三组各 n=6；
- `co1s1..9` = **现状规则 + `EPIRUS_COUNTER_OPPS=1`**（训练桌多三个判据原型：`cnt:guardSpam`「只防御不还手」/`cnt:gunSpam`/`cnt:snipeSpam`，v1.5.172 就有线上这个开关）⇒ **完全不改规则**；
- `cr1s1..9` = 上面那个开关 **+ 规则 1**（看"课程"在"上桌"之上还剩多少增量）。
回评口径与 §K-2 同一把尺（`probe-kill-reward` · 规则档 0=现状 · 240 局/格 · 同种子配对 · 种子 `20260923`）。

| 组 | 训法 | **破防场 1st（现状规则）** | 能破防（>50%） | 头对头 vs 现役（中位） | 净兑现 `landG`（中位） |
|---|---|---|---|---|---|
| 现役 | — | 0.0% | **0/1** | 17.1% | 2.66（3 种） |
| `kr0*` | 现状规则、普通桌 | 0,0,0,0,100,0 | **1/6** | 20.8% | 1.30 |
| `kr1*` | 规则 1 | 100,15,83,100,100,100 | **5/6** | 27.5% | 2.13 |
| `kr2*` | 规则 2 | 60,94,0,26,0,43 | 2/6 | 19.6% | 1.81 |
| **`co1*`** | **现状规则 + 龟缩对手上桌** | 100,98,88,5,100,100,100,100,0 | **7/9** | **32.1%** | 2.03 |
| `cr1*` | 上桌 + 规则 1 | 27,100,100,100,0,100,0,0,0 | 5/9（seed1-6 子集 5/6） | 24.6% | 2.05 |

- **同种子符号检验（破防场）**：`R1 vs 对照` 5胜0负1平 · `CO vs 对照` 5胜0负1平（seed1-6 子集）· `R1 vs R2` 5胜1负。
  ⇒ **"能破防"这件事确实能被训出来，但把它训出来的不是击杀奖励，而是"对手里有一只龟"** ——
  §K-2 我把机制归因写成"奖励击杀 ⇒ 压力偏向杀得动人"，**错了**：对照桌里没有不还手的防御者，
  规则档只是**间接**制造了"要杀人才有分"的压力；直接把龟放上桌，同样的压力免费就有了（还是**产品判据本身**）。
  这与 §N35 那次"`v7xn22a` 广行长成却死在 G4『只防御不还手 85% 击败』⇒ 判它的对手从不出现"是同一个病的**正解**。
- **加规则不再有增量**：`cr1`（上桌+规则1）破防 5/9，**不比 `co1`（只上桌，7/9）好**，头对头中位还更低 ⇒
  两条击杀规则这条线到此可以**结案**（连同 §N49"作为上线规则结构上失效"⇒ 两头都不成立）。
- **今晚最好的一粒**（但**必须换种子复评才算数**，因为我是拿同一把尺在 30 粒里挑最大 ⇒ 典型赢家诅咒）：
  `cr1s6-band1` 破防 100% / 头对头 **41.7%**（现役 17.1%）/ 净兑现 **2.66（5 种）**（现役 2.66 只有 3 种）；
  次选 `co1s8-band1`（100% / 31.3% / 2.03（4 种））、`co1s3-band4`（88% / 33.8% / 1.71）。

## K-4 03:05 · 两件事：① 我那片"破防场"的读数**含义读错了**（下限判据）② shipped G5 与它**互 not 蕴含**，而且 G5 看不见龟群场
**① 判胜口径**（`js/core/resolve.js` 的 `alive.length === 0` 分支）：龟群场里 4 席不还手 ⇒ 全员被终局收缩清场 ⇒ **按"累计有归因伤害"判胜**。
  所以 `破防 1st` 只等于"47 回合里蹭到至少一发"。诊断三局实录：现役包 vs 4 龟 —— `action 235 / guardSet 188 / blocked 23 / damage 15（全部 src=null 终局收缩）`
  ⇒ **它整局出手 23 次、全被防御挡、落地 0 点**（0.49 次出手/回合 = 它自己也在拖时间）。
  ⇒ 探针加两栏 `归因伤害/局` `终场存活`（门 **D141** 钉"与引擎同形 + 存在 + 不是死读数"）。中途我先用"局长 < 回合上限"当"提前收场"，
  读出来**人人 100%**（游戏在 47 回合被清场，上限是 100）⇒ 新栏必须先问"它能不能不等于 1"。
**② 与 G5 的关系**：G5 是「1 席龟 + 4 席被测 ⇒ 防席夺冠 ≤25%」，现役 **PASS（0%）**；我的龟群场是「1 席被测 + 4 席龟」，现役 **0.00 点/局、0% 活着收场**。
  ⇒ 两栏互 not 蕴含，且**现有门禁完全看不见龟群病**。33 粒新臂里两栏都过的只有三粒：
  `kr1s4-band2`(G5 0/0 · 9.70 点 · 存活 70%) · `co1s7-band6`(23/17 · 7.61 · 79%) · `cr1s4-band2`(13/13 · 7.59 · 98%)。
  三批独立种子都稳的那粒是 **`co1s8-band1`**：头对头 41.7/34.0/36.7%（现役 14.2/17.8/19.0）· 龟群 8.7 点/局 · 存活 78~83% · 脚本池 16.7~17.3%（现役 5.3~8.0）
  · `promote --dry` 唯一阻断 = **G5[multi] 防席夺冠 28% > 25%** ⇒ 这是判据取舍，**留给用户**（我没换槽、没 promote、没动 main）。
**③ 给 DS 的账（口径 bug，不是噪声）**：`EPIRUS_BREADTH_FLOOR` 量的是**训练桌 N=3**：`cb1s1-band1` 被线以"净 G(落地) 3.95（4 种）"选中，
  而同包 5 人桌 = **1.93**（3 人 3.95 / 4 人 3.47 / 5 人 1.93）。`mirrorHealth` 是**确定性**的（8 个种子读数一字不差）⇒ 差异全来自口径。
  后果：广度线可以放行一粒在 5 人桌上比现役还窄的包。要不要改口径 = 判据改动 ⇒ **不动，等裁定**。
**④ 248 粒海选**（把 `docs/artifacts` 全部"当选带"用换种子那把尺过一遍）：历史臂里 **39/87 能破龟群**，幅值前几名全是老包
  （`v7xn7a-31-band5/6` 11.15 · `v7xn16a-band1` 11.02 · `v7xn8c-31-band4` 10.40）⇒ **现役不是"最能破防"，只是"从没按这一栏选过"**。

## 状态（03:10）
- `np-test 187/187`（新增 D141）· `spec-run 52/52` · `smoke OK` · 指纹仍 `ebdbff36`。
- CHANGELOG v1.5.195 已记账（含 D82 的 33 粒当选带；165 粒落选带已删）· 三处版本号 = v1.5.195。
- 分支 `qoder-killreward-0924` 已推到 origin（v1.5.194 + v1.5.195 两枚提交）。**两槽线上包未动。**

## K-5 03:55 · 我自己差点被 n=60 骗了：`co1s8-band1` 在 **n=300 复测下 G4/G5 全格 PASS**（`--dry` 零阻断）
- 上一节我写"它卡在 G5[multi] 28% > 25%" —— 那是 **60 局**的读数。同一片场换 **300 局**：G5 = 4%(long)/22%(multi)、G4 最克格 43%/22% ⇒ **全过**，
  `promote --dry` 只剩两条"参照行"（**现役自己没过**的那两条）。G6 靶向率 62.4%（现役 18.5%）。
  ⇒ 教训写进 CHANGELOG：**阈值 25% 与 n=60 的 SE 同量级 ⇒ 这种门在 60 局上就是掷硬币**；`cr1s4-band2` 的 G4[珠爆发] 62%→66%(n=300) 才是真红。
- **但候选不是白给的**（canonical 5 人考卷，三个种子都稳）：现役 **54.4~57.1%** · `co1s8` **29.5~31.9%** · `co1s7` 28.2~30.1%。
- **顺手把"A 考卷掉了 25pt 是不是课程的代价"这个混淆量开了**：同规格 300 代臂的考卷散布 =
  对照 `kr0s1/5/6` **36.9 / 41.6 / 23.9** · 规则1 `kr1s1/4` **45.9 / 28.9** · 规则2 `kr2s2` **27.6** · 上桌 `co1s3/7/8` **33.9 / 29.8 / 29.5** · 上桌+规则 `cr1s4` **38.6**
  ⇒ **考卷低是"这一批臂只有 300 代 × 种群 8"（预算），不是"上桌"这门课的代价**（对照臂自己就散布 24~42%）。
  正在跑 3 只**大预算上桌臂**（`cbig1s1..3`：1500 代 × 种群 16 × 12 局）来把这条钉死 —— 如果它们考卷回到 45~55% 且仍破龟群，那"上桌"就没有任何已知代价。

## K-6 04:05 · 结构结论（123 粒包的相关性，不是猜）：**"破龟群"与"广度"不冲突，是"从来没人按这栏选过"**
拿 §K-4 那次 248 粒海选的读数算相关（`n=123` 有完整两栏的包）：
- `破龟群伤害` vs `净兑现 G`：**r = +0.048**（秩 +0.33）· vs `出手 G` −0.04 · vs `落地种数` +0.08 ⇒ **基本独立**，不存在"会打龟就一定窄"的取舍。
- `净兑现 G` vs `头对头`：**r = −0.27** ⇒ 又一次"胜率与广度不是一回事"（与 §N24/§N32/§K-2 同向）。
- 破龟群那 22 粒的净兑现均值 **1.46** vs 全体 **1.77** —— 差一点，但那是"这批臂都没被按广度选过"的共同结果，不是因果。
⇒ **给用户的判断**：想要"既破龟又宽"，不需要在两者之间挑，需要的是**把破龟群这一栏放进当选判据**（今天任何门都不看它）。
（独立性也解释了为什么 `cbig1s*` 那三只**大预算 + 上桌**的臂值得等：如果预算上来后考卷回到 45~55%，"上桌"就没有已知代价。）

## K-7 04:35 · 大预算"上桌"臂（1500 代 × 种群 16 × 12 局 · 3 只）：**考卷回到 48~50% ⇒ 证实 A 列的差是预算**；并且抓到一粒**我的新栏会误奖、而 G5 会拦下**的包
| 臂 | A 考卷（两种子） | G4 最克格 / G5 防席夺冠（**n=300**） | 破龟群：1st / **归因伤害** / 终场存活 | 头对头 vs 现役（3 批种子） | 净兑现 |
|---|---|---|---|---|---|
| 现役 | **54.4~57.1%** | 38%/35% · 0%/0% | 0% / **0.00** / 0% | 19.0 / 14.2 / 16.3%（基线） | 2.66（3 种） |
| `co1s8-band1`（300 代） | 29.5~31.9% | 43%/22% · 4%/22% ⇒ **全 PASS** | 100% / **8.7** / 78% | **36.7 / 34.0 / 41.7%** | 2.03（4 种） |
| `cbig1s1-band4` | **48.1~50.1%** | 「只防御不还手」**87%/90%** ⇒ G4 FAIL · G5 **86%/89%** ⇒ FAIL | 100% / **1.00** / **0%** | 22.0 / 27.9 / 25.0% | 1.79（3 种） |
| `cbig1s2-band1` | **49.1~49.5%** | 60%/45% · 12%/4% ⇒ **全 PASS** | 100% / **7.1** / **97%** | 11.3 / 14.6 / 20.4% | 2.15（3 种） |
| `cbig1s3-band3` | 37.2~38.2% | 22%/36% · 19%/**33%** ⇒ G5[multi] FAIL | 99~100% / **2.2** / 3% | **33.3 / 38.3 / 37.0%** | 2.66（3 种） |
- **最重要的一条反例 = `cbig1s1-band4`**：破龟群 1st 读 **100%**，但归因伤害只有 **1.00 点/局**、终场存活 **0%** —— 它没杀龟，只是"蹭到一发后跟龟一起被终局收缩清场、按伤害判胜赢了"。
  反过来 G5 里**一只龟能赢它 86~89%** ⇒ **shipped 门当场拦下**。⇒ 这正是 §K-4 加两栏的理由，也说明**新栏单独用也不够**：
  **`破龟群 1st`（下限）→ `归因伤害/终场存活`（幅值）→ `G5`（反向装配）**三条一起读才立得住。
- **预算假设证实**：同一条"上桌"课程，300 代臂考卷 29.5~33.9%，1500 代臂 **48.1~50.1%**（现役 54~57）⇒ **A 考卷那 25pt 差是预算不是课程**（对照臂 `kr0s*` 同规格散布 23.9~41.6% 也指向同一结论）。
- **今晚的取舍形状**：`cbig1s2-band1` = 考卷接近现役 + 全部 shipped 门过 + **真凿穿龟群（7.1 点 / 97% 活着）**，但头对头只与现役相当（11~20% vs 14~19%）；
  `co1s8-band1` = 头对头最好（34~42%）+ 门全过 + 龟群 8.7 点，但考卷只有 30%（预算）。⇒ **两粒都要，就得把预算再抬 + 多种子筛**（正在跑 seeds 4~9 六只 1500 代臂）。

## K-8 04:45 · 若要给"破龟群"设门，门线该画在哪：**总体是双峰的，所以这条线不是剃刀边**（124 粒当选带的分布）
- `归因伤害/局`：=0 的 **42 粒** · (0,1) 29 · [1,2) 8 · [2,4) 12 · [4,6) 15 · [6,8) 7 · ≥8 若干（老包 `v7xn7a-31-band5/6` 11.15、`v7xn16a-band1` 11.02、`v7xn8c-31-band4` 10.40、`kr1s4-band2` 9.70）。
- `终场存活`：**84 粒恰好 0%**，13 粒 <20%，5 粒 20~50%，**22 粒 ≥50%**（其中 20 粒 ≥80%）⇒ **要么完全打不穿、要么整局活着收场**，中间地带只有 5 粒。
- ⇒ 建议门线 **`归因伤害 ≥4/局 且 终场存活 ≥50%`（multi · 5 人 · 240 局）**：**124 粒里 22 粒（18%）过**，
  而**现役包 0.00 / 0% 会被当场拦下** ⇒ 这条门一加，"在位包自己红"是必然结果（与 G4[long] 那次一样属**参照行**）⇒ **只有用户能裁定要不要进门禁**，我不会自己加。
- 另一条更轻的做法（不动判据、只动当选面）：训练侧把 `破龟群幅值`当**第二排序键**（与 D124「当选面 3P 第二栏」同一形状），这样选出来的包天然带这能力，而不需要换包判据。

## K-9 05:30 · 九只大预算臂筛完：**今晚不换包的结论站住了**（详表见 CHANGELOG v1.5.196）
- 破龟群"真凿穿"（幅值 ≥4 且活着收场 ≥50%）只有 **3/9**：`cbig1s2-band1` 7.1/97% · `cbig1s5-band1` 9.8/92% · `cbig1s7-band4` 6.2/100%。
- 但这三粒在**本仓自己的"通吃＝最弱格"口径**下都不如现役（现役 15.0% · 11.7% · 7.5% · `co1s8` 9.2%）
  ⇒ **"没有一粒同时满足五栏"是可交付的结论**，不是我没找到：五栏里"通吃最弱格"与"破龟群幅值"今晚是**互相拉扯**的。
- `cbig1s1-band4` 那粒（破防 1st 100% / 1.00 点 / G5 被一只龟赢 86%）是**最好的一课**：如果我只读 1st 就报"找到破龟包了"，门会把我的结论当场打脸。
- 状态：`np-test 187/187` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36` · 两槽未动 · `results/*` 未提交。
