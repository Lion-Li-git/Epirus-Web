# 研究日志 · DS · 2026-09-21（分支 main @ 合并后）

> 承接：千问 09-20/09-21 两份账（`RESEARCH-LOG-2026-09-20-qoder.md` / `-09-21-qoder.md`）。
> 本文件是 **DS 自己动手那部分的预注册 + 结果**，规矩同它两班：**判据跑前写死**、产物点名、如实报否证。

## 0. 状态

- `main` 已合并 `qoder-bugs-0921`（16 提交）⇒ `main` = **`bf2c562`**，版本 **v1.5.140**，指纹 **`be6c2195`**。
- 合并后自检（DS 实跑）：D8 三方一致 ✓ · `spec 52/52` ✓ · `np-test 156/156` ✓ · `smoke OK` ✓ · `battle-test OK` ✓。
- 两槽：3P `v7n1-93`（考卷 43.1% · `forced=null` 自然过门 · G4 两模式 PASS · G5 两模式 **0%/0%**）；
  2P 内置冠军（考卷 **98.31%** · 最差基准 80.00% · 权重逐字节未变）。

## 1. 课题：**"教师示范为什么失败？"**（千问 §5/#21 结案后移交 DS 的特征面设计题）

### 1.1 它给的证据（我要检验的对象）

- `v7t1`（唯一变量 `BIGCARD_W=0.1`）：**贵卡出手全 0.0%**（电磁炮/大雷/雷击之枪/地雷，双口径）⇒ 否证；
  但不回退（G4 三粒最坏 58%、G5 0~7%）⇒ "奖励尾流买不到贵卡，也没赔上既得过"。
- `v7t2`（`heavyfire` 进四段教师计划）：**仍全 0.0%**，而**价值分反而更悬殊**（大雷富经济 +18.4pt、电磁炮 +15.6pt）
  ⇒ "越教越'知道好'，越不出手"。
- 它的结论：**特征/形状面设计题** —— "网络缺的不是分数，是'我 ep 富余 & 目标残血/防姿 ⇒ 贵卡价值兑现'的可学习连接"。

### 1.2 我的第一刀：**先量这些线到底值不值钱**（不先假设学生在犯错）

口径 = `duelAssembly`（与 `gate-drafts` 的 G4 逐字同口径）· `multi` · n=60 · seed0=90210 · **对手 = 现役在槽冠军 `v7n1-93`**：

| 线 | 脚本席胜 | 判读 |
|---|---|---|
| 基线（5 席同包 = 公平份额） | **18%** | 量具校准位（≈20%）✓ |
| 只枪线（旧的唯一红门 75%/62%） | **7%** | 新冠军已按住它 ⇒ 红门确实关了 |
| **纯攒大雷**（买得起才放，否则只攒） | **15%** | **低于基线 ⇒ 亏本** |
| **攒大雷 + 平时枪** | **7%** | 亏本 |
| 教师 `pickHeavyFire`（**v7t2 用的那条**） | 23% | 仅 +5pt |
| 教师 `pickDeepSaver`（**v7t1/标准配方用的那条**） | 20% | ≈ 基线（**等于没教**） |
| **`pickBeadBurst`（蓄珠 → 电磁炮）** | **30%** | **+12pt，全场最高** |
| 炮台线 `pickTankLine` | 23% | +5pt |
| 重狙击 `pickSnipeSpam` | 13% | 低于基线 |

**读出来的三件事**：
1. **大雷线是亏本的**（纯攒 15% / 攒+枪 7% < 基线 18%）⇒ 两个教师里 `heavyfire` 只有 +5pt、`deepSaver` ≈ 0pt
   ⇒ **它两次挑的教师，恰好是"平庸"和"没价值"的那两条**。
2. **真正值钱的贵卡线是"蓄珠→电磁炮"**（+12pt）——**从来没当过任何一臂的教师**；
   而现役冠军对电磁炮的使用率 **0.0%**（体检里那条"珠经济未闭环（花珠率 0）"）。
3. **交叉印证**：`gate-drafts` 自己给现役包的判词里，`G4[线上包/multi]` 的**最克格正是「珠爆发」30%**
   —— 与我这行 **30%** 是同一件事 ⇒ 现役冠军最薄的一环 = **珠经济**。

### 1.3 我的假设（与它不同）

不是"特征面缺连接"，而是 **"示范的是错的那条线"**：贵卡的价值集中在**珠经济（蓄珠→电磁炮）**这条**多步节奏**上，
而 (a) 它没被任何教师示范过；(b) 标量奖励（`BIGCARD_W`）只能奖励"出贵卡"这个**动作**、奖励不到"先攒后放"的**时序**
（这一点它 §3 自己也写了）。⇒ 那么**换对教师**就该有梯度，成本只要一臂。

### 1.4 预注册（**跑前写死**；臂 `v7bead1`）

**配方** = 千问 `v7u1` 配方**逐字**，**唯一变量**：三段教师计划里的 `pickBreakDef:0.33` → **`pickBeadBurst:0.33`**
（即 `EPIRUS_IMIT_PLAN='pickDeepSaver:0.33,pickAimDefender:0.34,pickBeadBurst:0.33'`，`IMIT_FRAC=0.25` 不动）。
`RING2_HOT=docs/artifacts/v7n1-93.bak` · 22 席池 · `CLEAR_W=0.1`/`DIV_W=0.3`/`SEAT_GAMES=24`/`S4_W=0.3`/`S4_GAMES=6`。
seeds 31,82,93。产物 `docs/artifacts/v7bead1-{31,82,93}.bak`（未 promote）。

**通过判据（跑前写死，n≥120）**
- ① **行为（这次的主判据，也是从未达成过的那条）**：`工具 skill-report` 下**电磁炮出手 > 0**
  **且** 蓄能/蓄珠链有实际落地（"花珠率 > 0"的机械证据）——现役包是 **0.0%/0.0%**；
- ② **不回退**：五道门全过 + G4 两模式全格 ≤60% + G5 两模式 ≤25%（口径同 v7n1/v7u1 终裁）；
- ③ **不靠变窄/变龟**（代价记账，不阻断）：`G ≥ 3`、座位极差 <30pt、场A ≥20%。

**通过** = ①+②；**否证** = ① 仍为 0 ⇒ **"示范通道（用对教师）也教不出珠经济"** ⇒ 那时才轮到它说的特征/形状面设计题，
而这条否证是**用对的教师**做出来的（比现在的两次更有分量）。
**风险登记**：换掉 `pickBreakDef` 段可能动 G5 轴（破防段是它 Q9/Q10 用来压 `只防御` 的）⇒ 若 ② 红在 G5，按"红格搬家"如实记。

## 6. v7bead2 预注册（**跑前写死**）：把"能放炮"这件事先做成可负担的

**沿着 §2 的诊断做的第二臂**（`v7bead1` 已证：教师对、但覆盖**永远示范不到放炮**）。读码定位到确切机制：

- `makeEconChooser`（`evo.js:695-704`，v1.5.96）的覆盖**只在教师动作"确实可负担"时才执行**；
  而 v1.5.99 的补贴局（`IMIT_SUB_ONLY`）补贴的是 **ep（白来的 ep）**，**不含珠** ⇒
  **"电磁炮"（`cost 2` + `energyNeeds {elec:1}`）在补贴局里仍然不可负担** ⇒ 覆盖只能示范"蓄能"
  ⇒ **93 粒"蓄能 5 次、得珠 5、全过期、放炮 0"就是这条机制的形状**（不是学生不肯学）。
- 因此本臂的**唯一改动 = 让"放炮"在补贴局里可负担**：新增 `EPIRUS_SUB_BEAD=1`
  （`evo.js` 的 `setSubBead`，默认关；只给**受评席**每回合补一颗珠，**只作用于 `regen>0` 的补贴局**，
  对手席不动 ⇒ 不改变补贴局的对手行为；`evo.js` 不在 `FINGERPRINT_FILES` ⇒ **零重记成本**），
  并配 `EPIRUS_IMIT_SUB_ONLY=1` + 计划里写 `only=railgun`（v1.5.98/99 现成的"只教目标卡"通道）。

**配方**：`v7bead1` 逐字 + `EPIRUS_SUB_BEAD=1` + `EPIRUS_IMIT_SUB_ONLY=1` +
`EPIRUS_IMIT_PLAN='pickDeepSaver:0.33,pickAimDefender:0.34,pickBeadBurst:0.33:railgun'`。
HOT=`docs/artifacts/v7n1-93.bak` · seeds 31,82,93 · 产物 `docs/artifacts/v7bead2-{31,82,93}.bak`（未 promote）。

**判据**：① **花珠率 > 0 且电磁炮出手 > 0**（本仓从未达成过；现役 0.0%/0.0%）· ② 五道门 + G4 两模式 ≤60% +
G5 ≤25%（n≥120）· ③ 代价记账（G≥3 / 座位<30pt / 场A≥20%）。
**非空枪自检**：worker 回执 `[imit] worker **消息**subBead 生效 = true` 必须出现（否则本臂没生效，读数作废）。
**通过** ⇒ "时序信用分配"这个定位成立且可训；**否证**（① 仍 0）⇒ 连"当场可放"都学不会 ⇒ 下一步只能动**奖励形状**
（给 `charge→fire` 序列单独计价，而不是给"出贵卡"这个动作）。

## 7. v7bead2 终裁（**也否证**）+ 适应度算术的定性结论（本次研究的收束）

**非空枪自检先过了** ✓：worker 回执 `[imit] worker **消息**subBead 生效 = true`（三份 worker 都亮）⇒ 旋钮端到端生效。

| 产物 | 判据① 花珠率 | 蓄能/局 | 得珠 | 花掉 | 判据②（五道门/G4/G5） |
|---|---|---|---|---|---|
| `v7bead2-31` | **0.0%** ✗ | 0.03 | 1 | 0 | ✗ **G4-multi 只枪 62%**（超阈 2pt） |
| `v7bead2-82` | **0.0%** ✗ | 0.00 | 0 | 0 | ✗ **场B 清场 0.17** |
| `v7bead2-93` | **0.0%** ✗ | 0.00 | 0 | 0 | ✅ 全过 |

⇒ **判据① 三粒全不中**，且两粒还留下了红灯 ⇒ 这次干预**无效且略有害**（读数为准，不粉饰）。

**为什么"连当场可放"也不够 —— 查了适应度算术，三条事实**：
1. **补贴局确实进 fit**（`evo.js:1187-1194`：只有**承诺局** `commitGame` 只记账不进 fit；`regen>0` 的补贴局走
   `else { fit += gFit; fitGames++; }`）⇒ 这一臂里"示范 + 可负担 + 进适应度"**三者同时在场**，种群**仍然**丢掉它。
2. **"花珠"本来就有专门的奖励项**：`BEAD_W = 0.05`（`evo.js:1156/2015`，`beadBonus = BEAD_W·min(1, beadSpent/2)`）
   —— 现役包花珠率仍是 **0.0%** ⇒ **"给钱太少/没给钱"这个解释不成立**。
3. **标度对比**（`evo.js:747/776/910/1911`）：`STOCK_BONUS 0.05` · `BEAD_W 0.05` · `DEAL_W 0.01` ·
   `BIGCARD_W 0`（v7t1 用 0.1）· `CLEAR_W 0`（各臂用 0.1）—— 而 `gFit` 的主项是 `base`（名次分，0~1 量级）
   与 `imitB·imit` ⇒ **珠/贵卡这些项全是 0.0x 量级的微扰**，压不过"赢名次"这条主梯度。
4. **旁证（本日实测）**：我 v1.5.133 查出的"只枪致命"用的正是**同一股主梯度**（即时伤害/密度 ⇒ 攒钱被罚）
   ⇒ 珠经济要的恰是它的反面（攒 1 回合、0 伤害）⇒ **同一股梯度一边教它"别攒"、一边要它"攒够再放"**。

**本次研究的结论（三层，按证据强度排）**：
- ⛔ **千问两条否证的证据链不成立**：`v7t2` 用的是 `pickHeavyFire`，实测**贵卡出手 0 次、最大 ep=2**
  ⇒ 那段"贵卡示范"**是空的**（v1.5.37 老教训第三次复发）；v7t1 用的标准教师示范的**几乎只有大雷**（亏本那张）。
- ✅ **我补的两条也否证**（这是本仓头两次"教师对 + 目标可负担"的实验）：`v7bead1`（换对教师）与
  `v7bead2`（再让"放炮"可负担）**花珠率都是 0.0%** ⇒ **示范通道、可观测性、可负担性都不是瓶颈**。
- 🎯 **剩下的解释（与全部读数一致）**：**珠经济的收益与"名次主梯度"直接冲突**，而它自己的奖励项只有 0.05 量级。
  ⇒ 千问"特征面/形状缺连接"的定位**方向不对**（信息在特征里、示范也到了），真命题是
  **"这条线在当前适应度算术下不划算/不被保留"**。

**下一步的两条（都不再假装是"教师问题"）**：
- (a) **把珠子项从"微扰"抬到"能对抗主项"的量级**（例如 `BEAD_W` 0.05 → 0.5，配 `SUB_BEAD` 让放炮随时可负担）
  —— 一次环境变量级的臂就能判：**若抬 10 倍仍 0% ⇒ 不是钱的问题，是结构**；若长出 ⇒ 之前只是"标度不够"。
  ⚠️ 代价必须记：高额贵卡奖励历史上买过"教条/塌宽度"（§4-2 与 WIDTH 的旧账）。
- (b) 若 (a) 否证 ⇒ 承认**珠经济是"针对某条脚本的窄反制"，不是通用能力**（我的价值表也支持：它只在
  G4 那种"1 席脚本 vs 4 席家族"的形状下 +12pt；在"1 席候选 vs 4 席池子"的训练形状里没有证据值这么多）
  ⇒ 那时的正解是**把它做成对手线/考卷的一条**（已经在 G4 里了），而**不是**逼冠军自己长出来。



## 2. v7bead1 终裁（**我的预注册否证被触发** —— 老实报）

服务端日志确认实验按设计跑（`plan=pickDeepSaver:0.33,pickAimDefender:0.34,pickBeadBurst:0.33`、`imitGens=62`）⇒ **教师确实换上了**，不是静默退回 heavyfire。

| 产物 | **花珠率**（判据①） | 蓄能/局 | 得珠 | **花掉** | 五道门 | G4/G5 行为门 |
|---|---|---|---|---|---|---|
| `v7bead1-31` | **0.0%** ✗ | 0.00 | 0 | 0 | ✅ 全过（G 3.04 · 场B 1/局） | ✅ 全过 |
| `v7bead1-82` | **0.0%** ✗ | 0.00 | 0 | 0 | ✗ **场B 清场 0.23 < 0.3** | ✅ 全过 |
| `v7bead1-93` | **0.0%** ✗ | **0.13** | **5** | **0**（浪费率 **100%**） | ✅ 全过（G 5.78 · 场B 4/局） | ✅ 全过 |

⇒ **判据① 三粒全不中** ⇒ 按预注册记 **否证**："**示范通道（即使教师是对的）也教不出珠经济**"。
**但 93 那粒给出了比"全 0"有用得多的定位**：它**蓄能 5 次（40 局里 = 0.13/局，现役是 0.00/局）、得珠 5 颗、
全部过期、一次没放** —— 即**出现了第一步（蓄珠）的微弱迹象，但第二步（放炮）从未发生**。
⚠️ 措辞要准：5 次/40 局**不叫"学会了蓄珠"**（现役 0 次，它是 5 次）；真正的读数是**"蓄了必过期"（浪费率 100%）** ⇒ 失败点是**两回合序列没接上**，不是"什么都没学到"。

## 3. 教师取证（这次研究最硬的一条：**v7t2 的那段"贵卡示范"是空的**）

口径：1 席教师 vs 4 席现役冠军（同 G4 装配）· multi · 40 局 · 逐键计数（**含"返回字符串"的选法** —— 第一版我漏了，差点把 `pickHeavyFire` 读成"什么都不做"）：

| 教师 | 胜 | 贵卡口径出手 | **观测到的最大 ep** | 出手构成 |
|---|---|---|---|---|
| **`pickHeavyFire`**（**v7t2 用的那段"贵卡教师"**） | 25% | **0（0%）** | **2** | `ji:342 tank:155` |
| `pickDeepSaver`（v7t1/标准配方） | 23% | 36（3.9%） | 5 | `guard:679 ji:218 **bigT:36**` |
| **`pickBeadBurst`**（本臂换上） | 28% | **113（19.9%）** | 3 | `ji:385 charge:69 **railgun:67** drain:46` |

- **`pickHeavyFire` 的最大 ep = 2** ⇒ 它**结构上不可能**示范任何 cost≥3 的卡（实测，不是读码推断；与它 `bots.js:314-319`
  "买得起就按 大雷→电磁炮→坦克→剑 出，否则ジ" + 每回合花光 ep 的实现一致）。
  ⇒ **千问 `v7t2` 那一臂并不是"示范通道的失败"，而是"教师示范了 0 次"** ✗ —— 它 §5 的结论
  （"越教越'知道好'，越不出手"）**建立在一个空示范上**。这是本仓 **v1.5.37 老教训的第三次复发**：
  *"教师的触发条件必须与那件事相关"*（旧 antiRing 的 `ep>=2` 在 45% 决策里成立，而真实滚环只有 0.15%）。
- `pickDeepSaver`（标准配方那条）示范的**几乎只有大雷**（36 次），而大雷正是我 §1.2 量出**亏本**的那张
  ⇒ 学生"不学"在这里是**正确适应**。
- 只有 `pickBeadBurst` 真的在走**有价值**的那条链（`charge→railgun`，113 次贵卡出手）⇒ **本臂是第一次"有效教师"的实验**。

## 4. 诊断：**不是特征看不见，是两回合的回报算不过来**

- 珠**在特征里**（`policy.js:203` 的 `me.elec/me.boom`、`:259` 的"对手持珠"、**`:240` 的"电磁炮前摇"** `o.elec>0 && o.ep>=2`
  —— 连"下回合能不能放炮"都是显式特征）；动作侧也有 `cost`/`energyNeeds`/可负担余量（`policy.js:376-380`）。
  ⇒ **"我 ep 富余 & 我有珠 ⇒ 该放炮"这条连接所需的信息全在特征里** ⇒ 千问"形状/特征面缺连接"的定位**不成立**（至少不是这个原因）。
- 真正的机制：这是一条**两回合计划**（蓄能花 1 ep、当回合 **0 伤害**，珠只活到下一回合末 ⇒ 必须下回合放掉）
  ⇒ 而**适应度是按即时伤害/密度给钱的**（同一股梯度就是 v1.5.133 里让"只枪"致命的那股）⇒ 中间那一回合被罚。
  模仿窗只管 25% 的局面 × 62 代 × 退火 ⇒ **压不过那股梯度**（93 粒：蓄了 5 次、5 颗全过期 ⇒ 它"敢蓄不敢放"）。
- 结论（比千问的版本更具体、也更可证伪）：**瓶颈是"时序信用分配 + 与密度目标的冲突"，不是可观测性。**

## 5. 下一步提案（我给的最便宜那根，未跑）

**状态分布塑形（state-distribution shaping）**：让训练局的一部分**开局就带一颗电珠（并给足 ep）**
⇒ 电磁炮在**第一个决策**就可负担 ⇒ (a) 示范标签当场可用（不用等两回合）、(b) 放炮的回报**当场**计入适应度 ⇒
把"两回合信用分配"降成"一回合"。实现面：`evo.js` **不在** `FINGERPRINT_FILES` 里（改它零重记成本），
与它已经做过的"宿主注入评分器"同一类改动。
**预注册判据**：① 花珠率 > 0 **且** 电磁炮出手 > 0（本仓从未达成）；② 五道门 + G4 两模式 ≤60% + G5 ≤25%（n≥120）；
③ 代价记账（G/座位/场A 不回退）。**通过** ⇒ 珠经济可训，且瓶颈定位得到证实；**否证** ⇒ 连"当场可放"都学不会，
那时才需要动**奖励形状**（给 `charge→fire` 序列单独计价，而不是给"出贵卡"这个动作）。

## 8. **更正（用户指正）**：我那条"大雷是亏本卡"是**用不会为传导选目标的脚本量出来的** ⇒ 结论撤回

**用户的原话（要点）**：大雷传导是**可预测**的 —— ① 有人攒了 2~3 个 ep，他很可能准备攻击 ⇒ 这时对他用大雷能拿到传导；
② 有人血厚、别的 AI 倾向集火他 ⇒ 对他用大雷也能拿到高传导。**不传导是亏的，传一次小赚，传两次"赚麻了"**
（"可能直接劈死好几个人"）。

### 8.1 你实盘记录里的量化（`results/*.txt`，只读；我数的是 `真正的落雷` 与其 `连带` 事件）

| 施放 | 传导人数 | 合计伤害 |
|---|---|---|
| 1（`31epirus-battle-23回合.txt` 第 10 回合） | **1** | 4 点 |
| 2（`epirus-battle-101回合.txt` 第 10 回合） | 0 | 2 点 |
| 3（`epirus-battle-15回合 (1).txt` 第 11 回合） | **2** | **6 点** |
| 4（`epirus-battle-28回合.txt` 第 11 回合） | 0 | 2 点 |

⇒ **4 次施放 · 2 次有传导 · 均值 3.50 点/次**（不传导基线 2.00）⇒ **用户口径成立** ✓。
"赚麻"那一例在日志里长这样：`🚫 玩家4 被无效化` + `🚫 玩家3 被无效化（连带）` + `⚡ 大雷连带：玩家3 受 2 点电伤`
+ 主目标与连带各 2 点 ⇒ **5 ep 换掉对手两个回合 + 6 点伤害**。

### 8.2 我错在哪（并把它量化）

我 §1.2 的价值表用的是 `pickHeavyFire` / 纯攒大雷这类脚本 ⇒ 它们**不会为传导选目标**。这版把它们**测出来**：

| 线 | 胜 | 大雷施放 | 有传导的局 | **传导/施放** | 大雷系伤害/局 |
|---|---|---|---|---|---|
| 只枪线（对照） | 15% | 0 | 0/60 | — | 0.00 |
| 纯攒大雷 | **8%** | 101 | 28/60 | **0.43** | 1.43 |
| **攒大雷 + 平时枪** | 15% | **0** | 0/60 | — | 0.00 |
| 教师 `pickHeavyFire` | 15% | **0** | 0/60 | — | 0.00 |
| 教师 `pickDeepSaver` | 15% | 54 | 19/60 | 0.44 | 0.80 |

- ⇒ **传导确实发生**（~0.43 次/施放），但那两条线是**盲放**：5 ep/次、不预测"谁会打谁" ⇒ 仍然是亏的（8%/15% ≤ 基线 18%）。
  也就是说：**我原来的结论是"盲放的大雷亏"，被我错写成了"大雷亏"** ⇒ **撤回那条结论**；
  §1.2 的其它行（只枪 7% · 珠爆发 30%）不含大雷语义，**不受影响**。
- ⇒ 顺带一条**新机制**：**"攒大雷 + 平时枪"一次都没放出来**（`枪` 每回合 1 ep 就把预算吃干 ⇒ 结构性排斥大雷）
  —— 这正是千问说的"ep 预算被 ジ+枪密度锁死"，现在在大雷这条线上也被实测到 ✓。
- ⇒ 也解释了它 `v7t2` 为什么空示范：`pickHeavyFire` 大雷施放 **0 次**（最大 ep=2，与 §3 一致）✓。

### 8.3 这张卡的正解（按用户口径写成可实现规格，供任何人接手）

大雷的目标选择 = **预测"本回合谁会攻击谁"**（N22 的传导集合 = 目标 T + T 的转移对象 + 本回合攻击 T 的人）：
- **信号 A（用户口径 ①）**：对手的 ep 存量（攒到 2~3 的人更可能出手）；
- **信号 B（用户口径 ②）**：上一回合的**集火对象**（血厚者被集火 ⇒ 预测本回合仍有 ≥1 席打他）；
- **收益口径**：传导的收益是**被封住的行动数**（`🚫 技能被无效化` 每人一次），不只是伤害 —— 5 ep 换对手 2 个回合是硬 tempo。
⇒ **提议（未做，等点头）**：写成一条会传导的大雷线（暂名 `bigtFocus`）进池/进教师通道，再重跑价值表；
**预注册判据**：传导/施放 明显 > 0.43（对标你实盘的 0.75）**且** 胜率 > 基线 18%。
若届时仍不划算，才可以说"这张卡在 5 席 AI 对局里不值 5 ep"——**那才是公平的否证**。


