# Qoder · 09-26 夜班探索（DS 09-25 交接之后，用户 09-26 授权："剩下的工作你来评估哪些你这边可以继续接手"）

> 规矩（照旧）：**只读 + 只改 `tools/` 与文档**；引擎/两槽/`results/*` 一字不动；指纹必须保持 `ebdbff36`；
> 判据/换槽属用户裁定。**今天起在分支 `qoder-explore-0926` 上干活**（用户 09-26：「ds今天白天还会继续推进，你两别冲突了」）
> ⇒ 不直接推 `main`；周期性 `git fetch` + rebase，最后由用户决定合并时机。
> 时间戳一律 `date` 实测；本节 DONE/DOING/NEXT 是防失忆用的，见 METHODOLOGY「夜班防失忆日志」。

## NEXT（按顺序 · 每条都写了"为什么不是 DS 的路子"）
1. **E23 假设：这棵树里"疯狂防御/维持很久"的根，是**结算层让防御几乎免费**（guard/reflect = 0 ep）。
   DS 在**训练侧**加奖励（序列奖励六次 A/B 逐字相同），没人从**规则侧**做一次反事实。
   ⇒ 内存补丁（不改仓库、指纹不变）：`L1` 同席连防第 2 次起收 1ep / `L2` 每次摆防一律收 1ep，量它对
   设防率·连防·局长·强度（五道门/净兑现/破防场）各的影响。**只量，不改规则。**
2. **E24 归因：防御到底是"学出来的"还是"边缘胜出"？** —— 在决策点记录 防御 vs 次优 的**分数差**与特征块贡献。
   若差值贴零 ⇒ 行为是刀锋（换 ε/换种子就翻，解释 40 个版本反复）；若差值大 ⇒ 深吸引子（只能改地形）。
3. **E25 用户裁定的两条测量前置**（不是判据改动，只是把线画得住）：
   ① 新 G 的 60% 线**覆盖 long**（用户 09-26 已批）⇒ 先量 long 口径下的份额分布（n≥60 粒）与现役分位；
   ② Δ(钱) 那栏的两轴判定 ⇒ 补齐"好包侧"分布，看 `Δ × 水平` 两轴能画出什么形状。
4. **E26 我自己量具的口径账**（DS §2.6 那件事的延长线）：`probe-ideal-champion` 的【6】、`probe-breadth-flip`、
   `screen-cheap-predict` 三处**在搬过口径的沙箱里跑的"单一真源"读数**，逐一处标口径或改回未搬沙箱。
5. **E27 帮 DS 的（纯测量，零文件冲突）**：seed 80 vs 现役 大 n 复核（他 §3.5）。
6. 早报一次（09-26 09:00 前后）：每条带"实测/推断"标注与复现命令。

## DONE
- （开工前）接完 DS 的 09-25 交接：`v1.5.236`（§3.2 定案 = 口径搬运 3.00×、7 个探针接参数守卫、门 D160、np 206/206）已在 `main`。
- **01:41 快照**（防失忆 · 全部已落盘并写进本日志）：**E23 / E23b / E24 / E25 / E28a / E27 已量完**；**E28b 已建台并跑通冒烟**（旋钮 + 两漏斗 + `train-3p` 接线 + 门 D162）；新门 **D161**（量具自检）；拆掉 **D149 的行号地雷**（指针改为源码现算，`probe-layer-caliber` 新导出 `srcLine`/`hardwiredLine`）并顺手把 D150 的 `import { build }` 精确形状判定改成语义判定；METHODOLOGY 加 **69/70/71**。
- **E27 结论（core 池 · 8 种子 × 1400 局/粒）**：`sel80` **−9.23pt**（t≈−11.6，DS 那个 +2.8pt 反了号）· `sel81` **+0.10pt**（t≈0.1，它与现役在胜率上不可分，被挡的是"墙前零穿透"）· `ft0`/`ft2` **权重 sha1 相同 ⇒ 是同一粒包**（别读成两次复现）。`--pool=all` 那一段跑了 35 分钟没出一行，已中止（core 那一段已经够判）。

## DOING（**16:47 快照** · 前面几版已过时，以这段为准）
- **今天到这里的账**：v1.5.244 / 245 / 246 三个版本已提交并推到分支 `qoder-explore-0926`；门禁 `np-test 209/209`（16:34 复跑）· `spec 52/52` · `smoke OK`；**引擎规则/两槽/判据阻断逻辑一字未动**，指纹 `ebdbff36`。
- **三条结案（都实测、都带样本量）**：① 换"训练/选择口径" ⇒ 五道门零效应（n=14 对，§E35，作废我自己早报那条 +1.13）；② 把克制原型"放上训练桌" ⇒ 主终点 G4 零效应偏负（n=12 对，§E39）；③ 按"G4 最克最低"预挑 5 粒 ⇒ 抗克与宽度互斥（§E41）⇒ **"再训一轮就能出货"这条路今天量到了头**，剩下唯一没试的形状是目标函数里加 min-over-counters（**属判据改动，等你裁**）。
- **给你的合并裁定件（第 8 条，一条而不是四条）**：门禁缺**"与现役相对比较"**这一维，四处同形 —— 反弹墙（`pierceLand===0` 二值闸）· G4 最克（绝对线 60%）· `G(long)`（只记录，阻断只看 multi）· 场B 清场（只记录）。**建议改成"不劣于现役 + 容差"**。
- **唯一留档的臂产物**：`docs/artifacts/E39-ctl-911.bak`（五维不劣于现役 + 全量 dry 零阻断，但场B 0.03 / G(long) 2.86 退化 ⇒ **我倾向不换**）。
- **一次性的量具落点已清**：`docs/artifacts/e40-out/`（跑完即删）；E35/E39 的臂产物仍在 `%TEMP%/kr3/`（会过期，复现命令在各节里）。
- **臂产物**在 `%TEMP%/kr3/e35/`（**会过期**）⇒ 复现命令抄在 §E35；仓库里只留了两份 `.bak`（`E35-prod-807/814`，gitignore 内）。
- **等用户裁的新裁定件（第 8 条）**：**反弹墙那一栏是二值闸**（`pierceLand === 0` 才拦）⇒ 一粒"墙伤 21.00→1.00/局、穿透命中 364→20"的候选可以**零阻断**通过 `promote --dry`（本轮实测 `E35-prod-807`）。
  形状建议（我不动判据）：下限 + 参照比（如"低于现役的 1/4 ⇒ 阻断"）。
- **等 DS/用户处理的仓 bug（我只报未修）**：`js/bundled-champion-3p.js` 的 META 里 `fingerprintRefresh:` 键**没有引号**（v1.5.174 手写的）⇒ 不是合法 JSON
  ⇒ `promote --dry` 对**现役包本身**当场 `SyntaxError` 崩（其余 4 个读者都 `catch` 成 null，所以 209 道门禁全绿也照不出来）。
  两条修法都属裁定面：① 补引号 = **动槽文件**；② 让 promote 从沙箱读 `sb.window.EPIRUS_CHAMPION_3P_META` = 不碰槽（我倾向这条）。
- 原 v1.5.237 那段（保留作历史）：门 `np-test` 全量复跑中（本版动了 `js/train/evo.js` ⇒ DS 那两道 CLI 基线门是关键看点）。绿了就提交 v1.5.237，然后按 §E28b 的预注册跑孪生臂：
  对照 = 出厂选择口径，处理 = 产品口径（`EPIRUS_TRAIN_EPS=0.2 EPIRUS_TRAIN_TEMP=0.15 EPIRUS_TRAIN_EPS_MODE=soft`），同种子同配方同预算，串行跑（两臂共用 `train-3p-out.js` 这个默认产物路径，并行会互相覆盖）。
  ⚠️ 两臂的 `trainFit` **不可互相比**（那正是被搬动的那个量）—— 只能各自跟自己口径内的历史比。
  ⇒ **这条"只能串行"已被 v1.5.244 的 `EPIRUS_T3P_OUT` 解掉**（§E35 就是 14 路并发跑出来的）。

## E23 00:22 · 建台：「防御免费」是不是这棵树的根？——**建台过程中踩到两个假读数陷阱**，都记成规矩
动机：用户报的两条病都在**训练侧**打过很多刀（击杀奖励结构上碰不到龟、序列奖励六次 A/B 冠军逐字相同、COUNTER_OPPS 上桌、SEL_LAND）。
但 `js/core/rules.js` 里 **防御族（guard/reflect/八卦阵…）费用 = 0 ep** —— 没人做过"给它定价"的反事实。DS 今天在**奖励面**加码，我从**地形面**问一次。
新工具（只读 + 内存补丁，**引擎一字不动、指纹不变**）：`tools/guard-cost-lib.mjs` + `tools/probe-guard-cost.mjs`
- 三档：`off` 对照 / `L1` 只罚**同席连续第 2 次起**的防御（对准"维持很久"，第一次挡枪仍免费）/ `L2` 每次防御都扣
- 形状：**事前**把"付不起"的防御卡从菜单摘掉（仓里已有三道同形菜单闸：`CHARGE_MIN_EP` v1.5.152 前、`hasPurgeable` v1.5.199、天火闸 v1.5.139），真选了再扣 ep
- 两栏一起印，缺一栏不合格：**【病】**攒钱者装配的设防率/`ep≥10`桶/连防/≥3连/局长/平局/攒钱者夺冠 ‖ **【强度】**门同源 `feasibilityOf` 五道 + 净兑现 + 镜平局/镜局长
- 自证：`guards/billed/epTaken/dropped` 全印；`dropped=0 且 epTaken=0` ⇒ 明写"空枪，读数作废"

**两个陷阱（都不是"结果"，是差点把没打到当成结论）**：
1. **事后扣 = 空枪**：第一版选完再扣，实测 `billed=21` 但 `epTaken=1` —— 防御恰恰是在**手里没钱**时选的（它本来免费），从 0 里扣 1 什么都不发生 ⇒ 三档读数逐字相同。差点被我写成"防御定价不影响行为"。
2. **route ② 打不进内部调用点**：强度侧我原来只在属性层包 `sb.EpirusTrainer.policyChooserN`，而 `selfPlay` 只是 `mirrorHealth` 的薄壳、**内部调闭包里的 `policyChooserN`** ⇒ 强度三档又"逐字相同"。改成 route ①（改 evo 源码 + `mutated===1` 自检，没生效就 **exit 9**）之后，差异立刻出来：
   现役 `L2` 从 **5/5 掉到 4/5**（`G(long) 3.47 → 2.93 < 3`）、`band2` 的 `G(long) 2.49 → 1.74`。
   ⇒ 这正是 METHODOLOGY 54 早就写过的"内部闭包调用不受属性层包装影响"，**我自己第二次栽在同一件事上**（第一次是口径搬运，这次是计费包装）。
3. 顺带一条**好消息**：`probe-layer-caliber.build()` 加了通用的 `mutate/inject` 钩子（ additive，不传时逐字不变）⇒ 以后任何"内存反事实"都走这**一条**搬运路，不必各自再写一份装载器（D150 的单一来源规矩延伸到"改源码"这一层）。

**第一次冒烟读数（`games=30/feas=8`，只有方向）**：`band2` 设防率 42.0% → **26.1%**（L2）、**连防 30 → 1**、≥3连 0；但**局长 44 → 45 不动**、平局 3% → 10%。
⇒ 初见结论的形状：**"维持很久"那一半确实是免费防御撑的（一收费就断链）；"把局拖长"那一半不是** —— 拖长另有来源（可能是防御被摘之后改用别的耗法，或 4 席都在苟）。这一条要用大样本 + 分桶确认。

## E23 读数 00:37 · 剂量曲线（6 粒关键包 × cost 1/2/3 ep × off/L1/L2）：**"维持很久"那一半确实是免费防御撑的——1ep 就把连防打断；但"把局拖长"要 2ep，而且第一道掉的门是现役自己的**
| 包 | 基线（设防率 / 连防 / 局长 / 门） | L2@1ep | L2@2ep | L2@3ep |
|---|---|---|---|---|
| **现役 `v7cmin4-31`** | 5.1% / 8 / 20 / **5/5** | 0.2% / 1 / 20 / **4/5** | 0% / 1 / 20 / 4/5 | 0% / 0 / 20 / 4/5 |
| `cbs1s2-band2`（用户抱怨那粒） | 42.8% / 33 / **44** / 3/5 | 27.0% / **1** / 45 / 3/5 | 0.8% / 1 / **18** / 4/5 | 0% / 0 / 18 / 3/5 |
| `v7seat24-31`（v1.5.104~143 线上） | 57.5% / 42 / **46** / 1/5 | 18.7% / 3 / **25** / 2/5 | 6.0% / 1 / 18 / 2/5 | 2.3% / 2 / 17 / 2/5 |
| `v7cmin4-82` | 4.6% / 3 / 22 / 5/5 | 0% / 0 / 22 / 5/5 | 同 | 同 |
| `v7divK-31` | 6.8% / 3 / 16 / 5/5 | 0% / 1 / 15 / 5/5 | 同 | 同 |
| `co1s7-band6` | 6.1% / 3 / 17 / 2/5 | 0% / 0 / 18 / 2/5 | 同 | 同 |
**四条结论**：
1. **连防对价格极其敏感，1ep 就断**：六粒里 `L2@1ep` 全部把连防压到 ≤3（现役 8→1、band2 33→1、seat24 42→3）⇒ 用户那句"**并且维持很久**"在机制上就是"防御不要预算"造成的。这一条与之前所有训练侧干预（上桌/奖励/SEL_LAND）都不同：它们都没动这个地形。
2. **`L1`（只罚同席连续第 2 次起，第一次挡枪仍免费）就够断链，且更轻**：band2 连防 33→**1**、设防率只从 42.8 降到 32.8；`seat24` 更明显 —— 局长 **46→25**、设防率 57.5→25.1，门仍 1/5（它本来就不过门）。⇒ 也就是说**存在一种"只惩罚龟缩维持、不惩罚合理挡枪"的形状**（这正是用户 09-23 裁过的"囤一点 ep 有好处 / 特定情况该防"的口径）。
3. **"拖成无聊长局"这一半不完全是免费的锅**：`band2` 的局长要到 **2ep** 才从 45 掉到 18（L1 全程不动，1/2/3ep 都 45）⇒ 它的长局另有来源（设防**水平**仍然 32%，加上四席都变龟后的预算/收场结构）。而 `seat24` 的长局 46→25 在 L1@1ep 就解决了 ⇒ **同一句"疯狂防御"底下至少两种病，别当一种治**。
4. **代价要说准：第一个掉的门是现役自己的 `G(long)`**。任何一档价格（含最轻的 L1@1ep 对现役：`G(long) 3.47 → 2.93 < 3`）都会让**现役包自己**掉一门（5/5→4/5）。这与我 00:33 报的那条是同一件事的两面：**现役的 long 口径广度本来就骑在 60%/3.0 那两条线上**（份额 59.3~63.9%、G(long) 3.47）⇒ 不是"定价把现役打坏"，是"现役在 long 口径上一直是压线过"。⇒ 若将来真考虑规则换代，这条必须一起裁，否则会被"现役过不了新线"的错误叙事带偏。
   另一侧的好消息：`cmin4-82`/`divK`/`co1s7` 三粒健康包在 1~3ep 下**门数一格没掉**（5/5、5/5、2/5 前后一致）⇒ 价格并不普遍伤强度，伤的是"靠压线过门"的那一类。
**顺带一条读数纪律**：`band2` 的"攒钱者夺冠率"在 L1@1ep 下从 15% 涨到 **32%** ⇒ 又被我自己上一版的家族版结论打了一次脸（§H-23 那条"换包会奖励攒钱者"）——这一格是**包相关**的，跨包不许当规律引用（METHODOLOGY 60/65 同族）。

## E23b 00:39 · 宽筛（57 粒 × `off/L1/L2`，cost=1ep）：**1ep 定价稳定断掉"连防"，几乎不花强度，但救不了"长局"**
- **连防**：基线连防 ≥10 的有 **15 粒**，`L1`（只罚同席连续第 2 次起）把其中 **9 粒压到 ≤2**、`L2` 同样 9 粒 ⇒ 断链靠的是"第 2 次要钱"这一条，**不需要惩罚第一次合理挡枪**。
- **局长**：`L1` 下 Δ 中位只有 **−2 回合**，变短 ≥8 回合的只有 2 粒（还有 1 粒变长）⇒ **"把局拖长"这一半不是免费防御单独造成的**（`band2` 44→45 不动，而 `seat24` 46→25 动了 ⇒ 同一症状两种因）——与 §E23 剂量表一致，长局要 2ep 才普遍松。
- **代价（这是我最担心的一项，结论是"接近免费"但不能吹）**：57 粒里 `L1` **掉门 5 粒 / 涨门 2 粒 / 不变 50 粒**；`L2` 掉 6 / 涨 4 / 不变 47。掉门的五粒是 `champion-3p-div475`(3→2) · `mix-35`(3→1) · `v7b1s81-band6`(3→2) · `v7c1s81-band1`(4→3) · `v7cur-31`(3→2) ⇒ **四粒本来就只剩 2~3 道门**（是被别的门卡住的包），唯一"从 4 掉 3"的是 `v7c1s81-band1`。
- **净兑现**：Δ 中位 **+0.00**（p10 −0.32 / p90 +0.08）⇒ 定价没有普遍把打法压窄；`按ジ%` 中位只 +0.2pt（p90 +24pt 是个别包转向攒钱）。
- ⚠️ 三条不许越界的说法：① 强度侧用的是 `feasibilityOf` 的 10 局/格小样本 ⇒ **单粒门数变化含噪声**，只有"5 掉 / 2 涨 / 50 不变"这种整体数可引用，别拿某一粒说事；② 这是**内存反事实**，真要上产品=改 `rules.js`=规则换代（新指纹、全部历史基线作废），不在我的权限，也只等用户裁；③ `L1` 与 `L2` 在"断链"上同为 9/15，但 `L2` 多花的钱换来的只是设防率再降 5pt（中位 −6.9 vs −1.7）⇒ **形状比剂量重要**，这条正好对用户 09-23 那句"特定情况反而要出（看到对手攒到 5 ジ 防一下大雷）"。
- 下一步（E24）：既然 1ep 就断链，那剩下的问题是**这只包的防御到底是"学出来的深吸引子"还是"分数贴零的边缘胜出"** —— `EpirusPolicy.value/forward` 与 `setFeatMask('bead,target,effects,rel')` 都是现成对外面，可以做两件事：① 每个决策点量"防御 vs 最优非防御"的分差分布；② 逐块掩掉特征，看设防率/连防塌不塌（塌=那块特征在撑）。这决定的是**修法该往哪一层找**：地形（价格）/ 特征（看得见什么）/ 奖励（学什么）。

## E24 00:43 · 防御到底是"深吸引子"还是"盲化后的安全默认"？——**两个都对，但主角是后者：把这只包看瞎，它就龟**（新量具 `tools/probe-defense-margin.mjs`）
两件事一起量的：① 每个决策点算 `max(防御卡 value) − max(非防御卡 value)`（同一批合法候选、与 ε 抽样无关的"偏好"量）；② 用仓里现成的 `EpirusPolicy.setFeatMask('bead,target,effects,rel')` **只改特征取值不改形状**，逐块遮掉/只剩一块重跑同一装配（内存实验，仓库一字未动）。

**① 偏好量（60 局 · 产品口径）**
| 包 | 设防率 | 分差中位 | 实际选防御时"本来就是 argmax"的占比 | 防御占优的决策比例随对手 ep |
|---|---|---|---|---|
| 现役 `v7cmin4-31` | 5.1% | **−52.1** | **4.0%**（96% 是靠探索才选上的） | 0% / 0% / 0% / 0% |
| `cbs1s2-band2` | 42.8% | −1.86 | **78.1%** | 33% → 36% → 39% → **42%** |
| `v7seat24-31` | 57.5% | — | — | — |
⇒ 现役的防御是**噪声产物**（价值函数从不偏好防御，全靠 ε 抽样漏进来的 4~5%，且一扣 ep 就归零 —— 与 E23 一致）；
⇒ `band2` 的防御是**真学到的**：四分之三的防御手本来就是 argmax，而且**"对手越有钱、防御占优的决策比例越高"（33%→42%）** ⇒ 这就是 §H-47 那条因果在**价值函数层面**的落点，不再只是行为统计。
⚠️ 分差的**绝对值不可跨包比**（每只包权重尺度不同：现役 ±50~90、band2 ±10）⇒ 只能比同一只包内部的分位数与占比。

**② 遮哪块特征（60 局，两粒种子各跑一遍）**
`band2`：全开 42.8% / 41.9%（种子 1/2）→
- **遮掉 `rel`（T 关系块"上一手谁对谁"）⇒ 79.4% / 77.7%**（+36pt，两粒种子一致 ⇒ 不是噪声）
- **只剩 `rel` ⇒ 31.2%**（−11.6pt）——只看得见"谁指向我"时反而**敢打**
- 只剩任意**单块**（bead/target/effects）⇒ **75~80% 设防、连防 45、局长 47（顶到上限）**
- 遮掉 bead（电珠/大雷两维）⇒ 50.0%（+7pt）；遮掉 effects ⇒ 44.4%（几乎不动）
`v7seat24-31`：遮掉 `rel` ⇒ **46.3%（−11.2pt）**、只剩 `rel` ⇒ 67.5%（+10pt）——**与 band2 方向相反**
现役：八种遮法全在 4.4~7.3% 之间 ⇒ **它的"不龟"不依赖任何单块特征**（这就是"干净"的样子）

**三条结论（也都是下一轮裁定的材料）**：
1. **"看不见就龟"是普遍机制**：信息越少，防御越是安全默认（band2 只剩单块一律 75~80%）⇒ 病不只在"奖励"，也在**它拿到多少可用于进攻的关系信息**。这给出第三条修法：**特征侧**（让 T 关系块更可信/更全），与地形（价格）、奖励（DS 那条路）并列。
2. **但"哪块特征"不可通用**：同一块 `rel` 对 `band2` 是压龟的（遮了更龟），对 `seat24` 是放龟的（遮了反而不龟）⇒ **不能建"看这块就多防"这种跨包判据**（METHODOLOGY 60"个例 vs 体质"的新变体：连归因方向都是包相关的）。
3. **两条修法在 `band2` 上互相咬合**：E23 的"连防第 2 次收 1ep"能把它的连防 33→1；而 E24 说它防御本来就常是 argmax ⇒ 所以价格改变的是"维持"，改变不了"进入"（设防率只从 42.8 掉到 27.0）。⇒ 想同时治"进入"，得动价值面（奖励/特征），不是地形。

## E25 01:15 · 用户裁"新 G 可以覆盖 long"之后先量的账：**60% 这条线要是真覆盖 long，第一个被砍的是现役自己**（量具 `tools/probe-share-modes.mjs`）
方法（按仓规先量判别力再谈立门）：`docs/artifacts/*.bak` **1453 个文件 → 按权重哈希去重成 1006 个等价类**（METHODOLOGY 62；平均每类只有 1.4 个逐位相同的文件 ⇒ 这仓不是"一族克隆绑架分布"，但仍必须按类抽），
按 4 步长在等价类上抽 **198 类**（占类 20% / 占文件 26%），每类在 `mirrorHealth(120 局 × 5 席 · ε=0)` 下**两个模式各量一次**，份额一律走 `audit-lib.landShareOf`（单一来源）。

**① 归档分布（不是"少数坏包"，是全体的形状）**
| 模式 | p10 | 中位 | p90 | 最大 | >60% 的类 |
|---|---|---|---|---|---|
| multi | 46.8% | **78.9%** | 99.7% | 100% | **135 / 198 = 68.2%** |
| long | 48.9% | **77.8%** | 99.6% | 100% | **139 / 198 = 70.2%** |
`long − multi` 差：中位 **+0.4pt**（p10 −7.5 / p90 +9.2）⇒ **两个模式的份额分布几乎同一把尺**，"覆盖 long"不是在 multi 之上加一点点严格度，而是同一判据多判一遍（多砍 13 类 = 6.6%）。

**② 但现役包在两个模式上的位置完全不同 —— 这条是给用户裁定的核心**
| 模式 | 最大单卡 | 份额 | 在 198 类里的分位 | 60% 线 |
|---|---|---|---|---|
| multi | `gun` | **49.1%** | 第 29（14%） | ✓ 距线 **+10.9pt** |
| long | `gun` | **63.9%** | 第 68（34%） | ⛔ **超线 3.9pt** |

**③ 份额对样本量的敏感性（`--gscan=1` 实测，同一粒现役包）**
| G | 20 | 40 | 80 | 120 | 200 | 300 |
|---|---|---|---|---|---|---|
| multi | 47.0 | 49.9 | 49.0 | 49.1 | 49.8 | — |
| long | **60.2** | **59.3** | 62.5 | 63.9 | 62.6 | 62.5 |
⇒ multi 稳（±1.5pt）；**long 在 n=40 读到 59.3%（线内）、n≥80 收敛到 62.5~63.9%（线外）**。
⇒ 所以"现役在 long 口径下在不在 60% 线内"**目前是由采样量决定的，不是由包决定的** —— 而 `train-3p.mjs:766` 那条塌缩闸用的正是 `SEL_LAND_GAMES`（小 n）⇒ 现在这条线**即使覆盖 long 也咬不住任何东西**（换个 n 就翻）。

**④ 线位扫描（把线放哪儿 = 砍掉多少类 + 现役还活不活）**
| 线 | multi 过线 | long 过线 | 两模式都判 | 现役 |
|---|---|---|---|---|
| 55% | 73.2% (145) | 78.3% (155) | 82.3% (163) | ✓multi / ⛔long |
| **60%（现线）** | 68.2% (135) | 70.2% (139) | **74.7% (148)** | ✓multi / **⛔long** |
| 65% | 63.1% (125) | 64.1% (127) | 69.2% (137) | ✓multi / ✓long |
| 70% | 56.1% (111) | 57.1% (113) | 60.6% (120) | ✓ / ✓ |
**三条给裁定的话（都不是我替他做的决定）**：
1. **"覆盖 long"≠"再严一点点"**：现役（唯一合格者）会当场变成不合规。要覆盖 long，就必须同时接受"要么把线抬到 65%（那就砍不到现役，但对归档仍砍 69%）、要么把现役当'已知例外'、要么重炼一粒 long 份额更散的包"。
2. **60% 这条线本身的位置很"挤"**：归档 68~70% 的类都在它上面 ⇒ 它不是"抓特例"的线，是"把整族塌缩包都算不合格"的线。这与 §N28 那句"塌缩是常态不是意外"一致 ⇒ 判据方向没错，但要清楚它是**少数派准入**（现役 multi 49% 才真的是少数派）。
3. **覆盖 long 之前必须先钉 n**：小 n 下这条线在现役身上能读出 59 也能读出 64 ⇒ 先把 `SEL_LAND_GAMES`/promote 的 n 定到收敛值（实测 n≥80 才稳），否则"过线/不过线"是采样参数说了算。

## E28a 01:19 · 开训练臂之前先量"口径会不会换答案"：**会，而且随剂量单调**（量具 `tools/probe-caliber-rank.mjs`）
问题（DS 的路子之外）：适应度与五道门全在 ε=0（METHODOLOGY 49 裁的是"门禁**继续**用 ε=0 以便复现"，并没有说"包在 ε=0 的排序 = 在 ε=0.2 的排序"），
而 5 人产品是 `ε=0.2 k=5 soft` ⇒ **我们用 A 口径的分挑包，交给 B 口径去跑**。DS 那条路是"体检再开一栏 B 口径（只记录）"；我这里问的是：**如果适应度自己带 ε，选择会不会换目标？**
按仓规（METHODOLOGY 63）先花十分钟量方向，再决定要不要烧 5 小时算力行臂。

设计：120 个权重等价类（1006 类按 8 步长抽）× `mirrorHealth(60 局)` × ε ∈ {0, 0.05, 0.1, 0.2}（temp 0.15 / k=5 / soft，route ① 打进内部调用点）。
**噪声底不能用"换种子"**（`mirrorHealth` 的种子写死 `mulberry32(9000+g)`）⇒ 改用**剂量-反应**：若 0→0.05 就把排序打散到和 0→0.2 一样，那"挪"是扰动本身；若 ρ 随剂量单调下滑，才是口径在换答案。
免费自检一条：`on=true,eps=0`（只把两参换成五参、值仍 0）与真源 `on=false` **逐字相同**（实测 dmg 14.1333 / 局长 36.82 两边一致）⇒ 差异只来自 ε 的**值**，不是搬运层。

| 轴 | ρ(ε=0 vs 0.05) | vs 0.1 | vs 0.2 | 水平 0 → 0.2 |
|---|---|---|---|---|
| `dmg/局` | 0.788 | 0.652 | **0.480** | 14.57 → 13.88（水平几乎不动） |
| 净兑现 `gLand` | 0.738 | 0.655 | **0.554** | 1.90 → **2.74**（+44%） |
| 局长 | 0.711 | 0.554 | **0.444** | 32.5 → **21.3**（−34%） |
⇒ **三条轴全部单调下降** ⇒ 不是噪声，是口径本身在换答案；而 ε=0 榜上的第 1 名（`v7aim1-82`）在 ε=0.2 榜上掉出前十，ε=0.2 的冠军（`v7b3s31-band…`）在 ε=0 只排第 11。
⇒ 水平方向也值得记一笔：**探索让局更短（−34%）且更广（净兑现 +44%）** —— 也就是说"无聊长局"那一半有一部分是 **ε=0 口径自己的产物**，不是玩家会看到的病。
现役包在四档的 `dmg/局` 名次：113 / 115 / 113 / 116（共 120）⇒ **它本来就不是"伤害榜"上的包**，这条表只用于判"排序稳不稳"，别被读成"现役很弱"。

**下一步（E28b，正在做）**：ρ<0.6 ⇒ 按预注册的判据（表尾那句）"训练侧口径该上桌"，去开一个 **适应度带 ε 的臂**与同配方的 ε=0 孪生臂对跑。
⚠️ 三条边界先说死：① 判据轴是 `dmg/局`，**不是完整 `trainFit`** ⇒ 排序不稳是"值得试"的证据，不是"必然更好"的承诺；② 适应度带 ε 会让**同一次跑里的自评**变成 ε 口径 ⇒ 臂产物必须照 D158 把生效配方自证出来，promote 仍在 ε=0 复量；③ 引擎侧改动必须**默认关**（`EPIRUS_TRAIN_EPS`），出厂读数一字不动，且要有门钉"默认关 = 逐位不变 + 真开了有作用点"（D123 的规矩）。

## E28b 01:33 · 建 ε/口径臂之前，先把自己写的前提**推翻**一次：出厂的每代评分根本不在 ε=0，而在 **temp 0.35 · ε=0.15 · 硬档**
建台动作（读 `js/train/evo.js` 的 chooser 构造点）：
| 调用点 | 出厂参数 | 是不是"选择压力" |
|---|---|---|
| `scoreMemberN` 被评席（三处：计分局 + 两处自对局快照） | **`temp 0.35 · eps 0.15`，不传 `epsK`/`epsMode`** | ✅ 这才是每代 fit 的来源 |
| `trainChooser` 漏斗（`mirrorHealth`/style/pair 那一族） | `temp 0.15 · eps 0` | 自评 + 健康门槛 + 体检 G 列 |
| `tools/audit-lib.mjs` 九处 | `temp 0.15 · eps 0` | 五道门的输入 |
| 5 人产品 `ui.js:464` | `temp 0.15 · eps 0.2 · k5 · `**`soft`** | 玩家看到的 |
⇒ §E28a 那张表（ρ=0.480）判的是"**自评/门槛**口径 vs 产品口径"，**不能**当成"选择压力 vs 产品"的账 —— 我原来那句"我们用 A 口径的分挑包，交给 B 口径去跑"要说得更准：
**选择用的是一种连玩家都看不见的第三口径**：`temp 0.35`（产品 2.3 倍）+  ε=0.15 + **硬档**（不传 `epsMode` ⇒ 不走 v1.5.141 的"防御/环豁免"、不走 v1.5.149 的序列窗锁、防御键留在探索池里）。
而 v1.5.141 当年量过的硬档后果正是：**同一包同一装配，均匀抽 top-K 把防御出现率从贪心的 4.0% 抬到 26.4%、聚能环从 1.2% 抬到 0.0%** —— 这三件事是用户 09-21 实机抱怨"防御偏多、丢了集火和滚环"之后才在**产品侧**修掉的。
⇒ 也就是说：**训练侧挑包挑了 20 多个版本，挑的是"在一种会虚增防御、打断滚环的噪声下表现好"的包；而产品侧早已把那种噪声关掉。**（这是"疯狂防御"这条病状的一个新候选来源，且与 E23 的"防御免费"是**两条独立**的来路：一条在地形、一条在口径。）
已做（默认关，指纹 `ebdbff36` 未动）：
- `evo.js` 加 `TRAIN_EPS/K/MODE/TEMP` + **两个漏斗**：`trainChooser()`（自评/门槛，关档走原两参直调）与 `fitChooser()`（每代评分被评席，关档走原 `0.35/0.15` 三参直调）；四处散点收成漏斗 ⇒ route ① 的搬运点从 4 处变 1 处。
- `tools/train-3p.mjs` 按示范族同一范式接线：**没有 setter ⇒ exit 7**、下达后**读回消费点**、退出前印**开火计数**（`fit` 与"自评"分开记），`eps>0 而两个计数都为 0 ⇒ exitCode=8`（空枪）。
- 门 **D162** 判四件（全部真调用，不钉措辞）：`eps=0` 不被 `||` 吞 / 越界必抛 · 开档必须改变 `mirrorHealth` **和** `scoreMemberN` 的 fit · 关档后两个读数**逐字回到原值**（可逆 ⇒ 没有隐性状态） · `train-3p` 不算暗键而 `train-best` 必须把它列为暗键（D143 那族：宁可 exit 6，不许静默 A/A）。
- 实测（现役权重 · 同 gen/idx ⇒ 同种子，唯一变量是口径）：`fit` **0.5487 → 0.7030 →（关档）0.5487**，`fitSeen=44`。
顺带把 DS 的一颗地雷拆了：**D149 原来钉的是探针源码里的字面行号**（`evo.js:2554`/`audit-lib.mjs:403`）⇒ 任何人在那两行上面插一行，探针就印假行号而门仍绿。现在探针与门都从源码现算（`probe-layer-caliber` 新导出 `srcLine`/`hardwiredLine` 做单一来源），并把【6】的指针改指真正的 ε 决定点（漏斗）。

## E26 01:53 · 我自己量具的口径账（DS §2.6 那条线收口）：**全仓 9 个用 `build({on:true})` 的地方逐个核过**
判据（METHODOLOGY 53/66）：一个读数如果在**搬过口径**的沙箱里跑，就必须把口径印在旁边；否则两个数不可互比。逐个看：
| 量具 | 装载档 | 判定 |
|---|---|---|
| `probe-ideal-champion`【5】【6】 | 两档并排印（ε=0 列 + 产品列） | ✅ 已合规（且本版把它的**行号指针**改成现算，见 §E28b） |
| `probe-breadth-flip` | `A=on:false` + `B=on:true` 两档并排 | ✅ 干净 |
| `screen-cheap-predict` | 只有 `on:false` | ✅ 跑的本来就是真源口径，头注也没有"产品"字样 |
| `probe-bead-loop` | 两本账并排（A 搬过 / B 未搬） | ✅ v1.5.236 已修 |
| `probe-guard-cost` | 【病】栏 `on:true`（产品）‖【强度】栏 route ① | ✅ 两栏都印口径 |
| `probe-defense-margin` / `probe-caliber-rank` / `probe-layer-caliber` | 显式产品档 | ✅ 头注与汇总行都印 |
| **DS 的 `probe-g-collapse`** | **`on:true`（产品档）算份额** | ⚠️ 它头注印了"产品口径 ε=0.2/top5/soft" ⇒ **没有撒谎**，但要说清：**它那张表的份额与门禁/promote 的份额不是同一把尺**（后者 ε=0）。本版 §E25 用的是 `on:false` ⇒ 与门禁同源。两批数不许直接对表。
⇒ 结论：没有需要改代码的地方；唯一新账是"`probe-g-collapse` 与 `promote`/§E25 是两把尺"这一条，已在这里点名（不改 DS 的工具，交他裁）。

## E29 01:53 · 新量具 `tools/probe-wasted-play.mjs`：**空挡率 / 打空率 / 空净化 / 废手**（仓里没有这一把尺）
动机：门禁量强度、`chargeProfile` 量珠的浪费、`stanceProfile` 量"摆架势占比" ——**没有人量"这一手到底有没有产生效果"**。而 E23 的"防御免费"只解释"出得起"，不解释"出了没用还一直出"。
形状：每手一个**效果证据**（回合结算窗 = 本回合最后一条决策之后 → 下一回合第一条决策之前；决策时记 `events.length`）：
`空挡` = 防了但窗内没有任何一次 `blocked`/`reflect` 落在我头上；`打空` = 攻击手没有 `damage.source===我` 也没有 `blocked.via===这张卡`；`空净化` = `purify` 事件 `curses===0`；`废手` = `action.outcome!=='ok'`。
**三条自我约束**（都是量具自己的坑，先堵）：
1. **只判 `target:'self'` 的防御族**。藤甲/转移/地雷/符咒这些是**贴到对手身上、以后才兑现**的铺垫手 ⇒ 同窗里没有它的证据，判它"空手"是量具错（卡表里印"不判"）。
2. **自带 placebo/基线率**：'防的回合被招呼到' vs '没防的回合被招呼到'。若倍差 ≈1，"空挡率高"只是"本来没人打我"，**不许**读成"防得没道理"。
3. 两种装配分开印：`banker`（4 席同包 ⇒ 决策相关，倍差只当线索）与 **`hotseat`**（3 席脚本攻击手 + 1 席被评包 ⇒ 攻击与我无关，才是干净读数）。

**冒烟读数（30 局/格 · 产品口径 · hotseat）**：
| 包 | 防御手 | **空挡率** | 攻击手 | 打空率 | 倍差（防回合被招呼 vs 没防） |
|---|---|---|---|---|---|
| 现役 `v7cmin4-31` | 27 | **92.6%** | — | — | — |
| `cbs1s2-band2` | 65 | **93.8%** | 94 | 12.8% | **0.71×** |
| `v7seat24-31` | 73 | 84.9% | 80 | 25.0% | **1.09×** |
| `v7cmin4-82` | 21 | 90.5% | 110 | 23.6% | **0.47×** |
| `v7divK-31` | 28 | 92.9% | 122 | 16.4% | 0.94× |
⇒ 初见形状：**即便对手是看不见我策略的脚本攻击手，防御也几乎什么都没挡下（85~94%），而"我防的回合"并不比"我没防的回合"更常被招呼**（倍差 0.47~1.09，只有 `seat24` 略 >1）
⇒ 也就是说 E24 那句"看不见就龟"在这里现出一个更硬的说法：**防御在这些包里既不是"预判到攻击"也不是"挡住过什么"，它是一个占手不占钱的默认位**。
⚠️ 三条不许越界：① n 很小（30 局里防御手只有 21~73 次）⇒ 这是**冒烟**，归档分布正在跑（60 粒 × 40 局）；② "被招呼到"是从事件流反推的（漏打/被闪开的攻击不会留 `to=我` 的事件）⇒ 基线率是**低估**，倍差要往大了修正，所以"≈1"这个结论偏保守而不是偏激进；③ 空净化这栏在两种装配里都是 **0 手**（v1.5.199 的 `hasPurgeable` 闸把它挡在菜单外了）⇒ 这一维要量得换"有符咒可清"的装配，本表读成"没出手"而不是"出手都空"。

**归档分布（01:56 · 60 个等价类 × 40 局 · hotseat）**：
- **空挡率**：p10 79.4% · **中位 86.0%** · p90 92.9%；**打空率**中位 15.2%（p90 28.2%）；**废手 0.00%**（六十几粒全零 ⇒ 引擎的 `legal/affordable` 面没有洞，这条可以划掉了）
- **倍差（防的回合被招呼 vs 没防）**：p10 **0.68** · 中位 **1.17** · p90 **2.02** ⇒ 整体上"防御几乎不带条件"（1.17 意思是防了的回合只比没防的多 17% 被招呼到）
  - **>1.3 的 20/60 粒**（确实会挑时候防，例如 `v7press3-81` 1.73×、`v7b-31` 1.39×、`v7k0s91-band1` 1.49×，防御手 n=223~263 ⇒ 不是小样本）
  - **<0.8 的 10/60 粒**（**反着来**：防的回合反而更少被招呼 —— `band2` 0.71×、`v7cmin4-82` 0.47× 都在这一组）
⇒ 这条比"空挡率高"更有裁定价值：**"防御"在多数包里既不是预判也不是拦截，而是一个占手不占钱的默认位；而有一小半包真的会挑时候**。同一格判据（"设防率高就判病"）在这两组上的含义是相反的 ⇒ 若将来要给设防立判据，**该立的是倍差（条件性），不是占比**。
⚠️ 两处修正口径的要紧事：① `净化` 这一维在两种装配里几乎不出手（v1.5.199 的 `hasPurgeable` 闸把它挡在菜单外），归档里只见过 1 手 ⇒ 这一栏**读不了**"空净化率"，只能读"它现在结构性出不来"；② "被招呼到"从事件流反推 ⇒ 漏打/被闪开的攻击不留 `to=我` 的事件 ⇒ 基线率被低估 ⇒ 倍差要**往大**修正 ⇒ 上面"整体接近 1"的结论偏保守。

## E30 02:10 · "现有读数能不能代理防御条件性？"——**方向测下来：不能**（60 粒 · Spearman）
按仓规（METHODOLOGY 63：**想立新栏先测旧栏的方向**），给 `probe-wasted-play.mjs` 加 `--correlate=1`：同一粒包在**未搬档（ε=0 = 门禁口径）**下把现有几栏一起量出来，再与 E29 的两条新轴求 Spearman ρ（n=60 个等价类）。
| 现有读数（ε=0 门禁口径） | 对「倍差」（防御条件性） | 对「空挡率」 |
|---|---|---|
| 出手 G（`effSkills`） | **−0.02** | −0.07 |
| 净兑现 G（`effSkillsLand`） | **−0.02** | −0.01 |
| 落地种数 | +0.24 | −0.26 |
| 局长 | −0.20 | +0.21 |
| 按ジ% | +0.03 | −0.12 |
| 座位极差 | +0.02 | +0.11 |
| 平局率 | `NaN`（ε=0 镜像 60 局里几乎全是 0 ⇒ 无方差，不可算） | 同 |
⇒ **最强的也只有 |ρ|≈0.24**，而广度/净兑现这两栏（门禁真正在判的）对"防得有没有道理"**预测力约等于 0**。
⇒ 结论形状与 §E13/E16 对"钱驱动体质"的那次一致，但**这是另一条轴**（条件性 ≠ 占比）：**门禁那一层看不到它，代理也不免费 ⇒ 真要立"防御质量"栏，必须付它自己的测量成本**（本表：`hotseat` 装配 40 局 ≈ 每粒 4~6 秒）。
⚠️ 三条限制写清楚：① ρ 是在 `hotseat` 装配上量的（3 席脚本攻击手），换成 4 席同包装配倍差会因决策相关而变形；② 倍差的分母"被招呼到"由事件流反推 ⇒ 系统性低估基线 ⇒ 真实倍差更大，但这不影响"相关性接近 0"这个结论（单调变换不改 ρ）；③ n=60 是抽的等价类，外推到全档案要按 METHODOLOGY 62 报覆盖率（本轮 60/1006 类 ≈ 6%）。
- 复现：`node tools/probe-wasted-play.mjs --every=16 --limit=60 --games=40 --quiet=1 --assembly=hotseat --correlate=1`

## 臂时间表与判据（02:22 记，防失忆）
四批串成一队（**串行是因为两臂共用默认产物路径 `docs/artifacts/train-3p-out.js`，并行会互相覆盖**；跑完一律放回快照）：
| 批 | 内容 | 种子 | gens | 预计结束 |
|---|---|---|---|---|
| 1 | PROD − CTL（产品口径 vs 出厂硬档） | 301~306 | 300 | ~02:20 |
| 2 | 同上，加大预算 | 401~414 | 400 | ~03:35 |
| 3 | **拆旋钮**：SOFT-only（只把 epsMode 硬档→soft）/ TEMP-only（只把 temp 0.35→0.15）各对 CTL | 501~506 | 300 | ~04:15 |
| 4 | **热启动版** PROD vs CTL（两臂都 `EPIRUS_HOTSTART=1 EPIRUS_SEEDPACK=js/bundled-champion-3p.js`）⇒ 问"改成产品口径重选，会不会弄坏现役血统" | 601~608 | 300 | ~04:50 |
- 第 4 批的动机（实测，不是猜）：第 1 批前 3 对的 CTL 只有 `G 2.10~2.51 / 门 2/5` ⇒ **从零 300 代根本不到可上线质量**，那批只能判方向、不能判"会不会弄坏"。热启动批才能回答后者。
- **预注册判据（现在写好，免得事后挑读数）**：终评用第三把尺（ε=0 五道门 + ε=0 镜 G/净兑现 + `hotseat` 设防率/连防 + 产品口径 G/局长），**两臂的 `trainFit` 绝不互比**。主判据 = 「门数」与「净兑现 G」的配对差 95% 带是否离开 0；次判据 = 「设防率/最长连防」的配对差（这是用户报的那条病）。若两者都跨 0 ⇒ 结论是**"这一夜不足以判，需要更大预算"**，而不是"没差别"。
- 复跑核验（同一夜内）：E23b 宽筛换 seed 991 重跑、E29 归档换 seed 7777 重跑，两个输出在 `%TEMP%/kr3/`。

## E29b 02:28 · 给我的新量具做一次**重测信度**检查 —— 结果先把 §E29/§E30 两条结论各砍掉一截
同一批 60 个等价类、同一个装配（`hotseat` 40 局），只换种子（5200 → 7777）重跑一遍，比"同一粒包两次读数的一致性"（Spearman，n=60）：
| 我新加的轴 | 重测信度 r_tt | 判读 |
|---|---|---|
| **空挡率** | **0.07** | 在 n=40 下**几乎不是包属性**，是噪声 ⇒ 逐粒那栏不许当"这粒包防得空"引用 |
| **倍差**（防御条件性） | **0.25** | 弱。聚合分布仍稳（中位 1.17 vs 1.18），但**逐粒分档不可靠** |
| 打空率 | 0.41 | 中等（可用的下限，仍不够立栏） |
逐粒 |Δ空挡| 中位 4.7pt / p90 10.7pt；|Δ倍差| 中位 0.32 / p90 1.14。三档分组（>1.3 / 中 / <0.8）**只有 22/60 粒两档一致**，其中"两档都判高"的只有 1 粒 ⇒ **我上一节那句"20 粒会挑时候、10 粒反着来"作为逐粒判定是不成立的**，只能作为"分布形状"引用。

**这直接改写 §E30 的结论**： ρ 会被测量噪声**衰减**（观测 ρ ≈ 真 ρ × √r_tt）。倍差的 √r_tt = **0.50** ⇒ 去衰减后：
`落地种 0.24→0.48 · 局长 −0.20→−0.40 · 出手G −0.02→−0.04 · 净兑现 −0.02→−0.04 · 按ジ 0.03→0.06 · 座位极差 0.02→0.04`
⇒ 正确表述是：**"广度/局长这两栏与防御条件性有中等相关（去衰减后 ≈0.4~0.5），但门禁真正当判据的 `出手G/净兑现G` 仍然 ≈0"** —— 而不是我上面写的"全都 ≈0、新栏不免费"。
- 好消息是这条更正**方向上仍支持原判断**（判据用的那两栏读不到它），坏消息是我原先那句"代理不免费 ⇒ 必须付测量成本"**低估**了成本：以现在这个 n，付了也买不到逐粒可用的读数。
- 已在跑的高 n 核验（§E29c）：16 粒 × **240 局** × 两种子，专测 `r_tt` 随 n 的爬升（`hotseat` 装配下防御手次数中位只有 44 ⇒ 分母太小是主因）。**在 r_tt 上来之前，不许把"倍差"写进任何判据草案。**
- 三条方法论落点（写进 METHODOLOGY 72）：① **新量具必须先测自己的重测信度再谈相关性** —— 否则 ρ 低可能是"没关联"也可能是"我没测准"，两者对裁定的含义完全相反；② 报告 ρ 时若已知 r_tt 就要给**去衰减**估计；③ 聚合分布稳 ≠ 逐粒可用（本例中位稳到 0.01，逐粒分档却只有 37% 一致）。

## E23c 02:30 · 给 §E23b 的"1ep 断链"换一个种子重跑（57→60 粒 · `--seed=991`）：**方向复现，量级不复现**
| | 种子 4100（§E23b） | 种子 991（本表） |
|---|---|---|
| 基线连防 ≥10 的粒数 | 15 | **12** |
| 其中 `L1@1ep` 压到 ≤2 | 9 粒（60%） | **12 粒（100%）** |
| 局长 Δ 中位 | −2 回合 | **0.0 回合**（变短 ≥8 回合的仅个位数） |
⇒ "1ep 定价断连防"这条**方向是稳的**（换种子不但复现、还更强），但**具体几粒、掉多少**是种子相关的 ⇒ 引用时必须写成"在有连防的包里普遍断链"，**不许**写成"9/15 粒"这种像总体估计的比例。
⇒ "长局不是免费防御单独造成的"这一条也复现了（Δ 中位 0.0~−2）。

## E29c 02:20 · 把 §E29b 的信度检查推到 n=240（16 粒 × 240 局 × 两种子，防御手次数中位 312）
| 轴 | r_tt @40 局 | r_tt @240 局 | 判读 |
|---|---|---|---|
| **打空率** | 0.41 | **0.81** | ✅ 随 n 正常爬升 ⇒ **这一条是包属性，够格进候选判据的讨论** |
| **倍差**（防御条件性） | 0.25 | **0.54** | 中等；能用于"分组/分布"，逐粒立线仍勉强 |
| **空挡率** | 0.07 | **0.26** | ⛔ **6 倍样本也没救回来** |
⇒ 空挡率那一栏的真相：**它不是包的性质，是这套规则的性质** —— 两种子中位都稳在 **85.0% / 88.1%**（p10~p90 只有 78.8~91.2），**包间真实散布太小、盖不住测量噪声** ⇒ 信度天然上不去。
⇒ 结论改口：**"防御几乎从不正好挡住东西"是全档案共同现象（约 85~90%），因此它不能当判据（无判别力）**；要区分包，得用**倍差**（条件性，r_tt 0.54）或**打空率**（r_tt 0.81）。这正好把 §E29 那句"该立条件性不立占比"从"猜"变成"有信度依据的判断"。

## E31 02:25 · 抽查**上线槽自己那份 meta** 能不能被今天的引擎逐字复现（结论：能，两块各自对上一个 N）
动机：v1.5.236 那条"两把珠经济尺差 7 倍 = 口径"让我怀疑槽文件里记的数是不是也是某把说不清的尺 —— 于是拿 `js/bundled-champion-3p.js` 的 `EPIRUS_CHAMPION_3P_META` 逐字段回量。
| meta 记的 | 今天的复现 | 用的量具/样本 |
|---|---|---|
| `density.jiShare 0.79` + `dmgPerRound 0.378` | 按ジ **79.0%** · dmg/回合 **0.378** | `densityProfile(long, N=20)` = `FEAS_N.density` ✓ |
| `beadGained 273` · `beadSpentRate 0.835` · `beadExpiredPerGame 0.93` | 得珠 **273** · 花珠率 **0.835** · 过期/局 **0.93** | `chargeProfile(long, N=40)` = `FEAS_N.charge` ✓ |
| `examScoreAtBuild 0.544`（`eval-5p` · 40 局 · seed 77000） | 我这轮 E27 同协议该种子读到 **54.4%** | ✓ 逐位对上 |
⇒ 三条都**逐字复现**，两块数字各自对应 `feasPlan` 的不同 N（不是自相矛盾，是单一来源的按栏样本量）。⇒ **上线槽的自证链是完整的**（D158 这套文化在真实数据上有效），这条可以放心报给 DS/用户。
顺带一条对 §E28b 漏斗的复验：`probe-layer-caliber` 现在印"`evo.js` 写死处实测 **1** 处 → 内存替换 1 处 · `audit-lib` 9 处经包装 4800 次" ⇒ 收成漏斗之后 route ① 覆盖面没缩（产品口径那列仍在翻结论：现役"座位栏 ε=0 过 / 产品挡"）。

## E32 02:35 · 变异测试：**证明我今早新加的两道门"绿得有资格"**（把 bug 装回去，看门会不会红）
新建的门如果不会在坏掉时变红，它就是装饰。所以刻意把两处修好的 bug 装回去，各跑一遍全量门禁：
| 装的 bug | 期望 | 实测（`node tools/np-test.mjs`） |
|---|---|---|
| A：`guard-cost-lib` 的 `cost` 读回 `Number(o.cost \|\| 1)`（`--cost=0` 被吞成 1ep） | D161 红 | **207/208 · 唯一红的就是 D161**，报话 "`cost:0` 必须真的是 0（`\|\| 1` 会把零价对照档变成 1ep 档）got=1 want=0" |
| B：`evo.js` 的 `TRAIN_EPS` 出厂值从 0 改成 0.2（= 默认不再关） | D162 红 | **205/208 · D162 红，而且 DS 的 D123/D127 也同时红** ⇒ "默认关"这件事被**三道**独立门守着，不止我自己那道 |
两处都当场还原，还原后再跑一遍全量：**208/208 绿**（`np-final.txt`）。
⇒ 记一条纪律（写进 METHODOLOGY 73）：**新门落地当天就做一次性变异测试** —— 判"门能不能抓住它声称要抓的那个坏"，而不是只看它绿。变异测试要**当场还原 + 还原后复跑**，且不许用 `git checkout` 之类会连坐他人未提交改动的动作（本仓是共享工作树）。

## 02:45 快照（等第二批时记）
- 提交已到 `ff6bc72`（v1.5.241）；工作区干净；`np-test 208/208 · spec 52/52 · smoke OK · battle OK · 指纹 ebdbff36`（变异测试还原后复跑过）。
- 臂：第 1 批（6 对 @300 代）已完，第 2~4 批排队中（预计 05:20 全完），`e28b-wait-eval.sh` 会在结束时自动跑终评（`e28b-eval.mjs` 门/行为那半；官方考卷那半用 `e28b-eval5.mjs` 手动跑）。
- **早读（n=6 对，不作结论）**：`最长连防 Δ −1.17±0.48SE`（5/6 对为负）· `门数 Δ +0.33±0.21` · `净兑现 G Δ −0.62±0.27` · **官方考卷 1st Δ +2.48±5.95SE，逐对从 −18.9 到 +23.2**
  ⇒ 形状：**换选择口径不是在"整体变好/变坏"，是在换"落到哪一粒包"**（同种子两臂可差 20pt）。若后面 20 对仍是这个形状，那正确的结论就不是"该换口径"，而是"**这个预算下选择噪声 >> 口径效应**，任何口径讨论都要先把每臂预算提上去"。
- NEXT（早上）：终评两半 → 合并成对表 → 早报（薄）+ 待裁清单不变（现 7 条 + 今夜新增：新 G 覆盖 long 怎么裁、"防御质量"栏立不立、立的话用倍差还是打空率）。

## E28b 终评 07:55 · 四批孪生臂跑完了（01:43→07:45，5h58m）：**换选择口径买到的是"门禁质量 + 连防"，买不到"考卷分"**
协议：同种子、同 pop/代预算，**唯一变量 = 每代评分被评席的执行口径**。终评**不用臂自己的 `trainFit`**（那正是被搬动的量），用第三把尺：
`selfPlay/mirrorHealth @120 局`（ε=0 未搬档）+ `feasibilityOf` 五道门 + `hotseat` 体质栏 + `eval-5p` 官方考卷（2 种子 × 8 局/组合）。

### ① 热启动批（最有裁定价值 · 8 对 · 300 代 · 两臂都从现役冠军起）
| 指标（HSPROD − HSCTL） | Δ | 逐对 |
|---|---|---|
| **五道门门数** | **+1.13 ± 0.35SE（t≈3.2）** | +2 +2 +1 +1 +2 +1 +1 **−1**（7/8 为正） |
| **净兑现 G** | **+0.57 ± 0.28（t≈2.0）** | +2.2 −0.2 +0.7 +1.1 +0.4 +0.1 +0.5 −0.4 |
| `G(long)` | +0.97 ± 0.36 | +2.8 +1.0 +1.9 +0.2 +0.9 +0.1 +1.2 −0.4 |
| 出手 G | +0.73 ± 0.44 | +3.0 +1.4 +0.2 +1.3 +0.6 −0.6 +1.0 −0.9 |
| 最长连防 | −0.63 ± 0.92（噪声） | −1 +3 −1 −1 −6 +1 −1 +1 |
| 局长 | −4.35 ± 3.75（噪声） | −19.1 −14.8 −13.7 −4.8 +7.7 +0.4 +9.3 +0.1 |
⇒ 对照臂（**沿出厂硬档继续训现役血统**）平均只落到 **门 2.9/5、净兑现 2.21**；产品口径臂 **门 4.0/5、净兑现 2.78**。
⇒ **这直接给 DS 的 `ft0` 那次"热启动 60 代后塌到净兑现 2.86 < 3"提供了一个新解释**：塌不塌**依赖选择口径**，不是"热启动必然塌"。
  （他的臂是 120 代 / pop 8 / games 8 / n=3；我这里 300 代 / pop 12 / games 12 / n=5 ⇒ 不可直接对数，但现象同形：出厂硬档臂过不了门。）

### ② 从零批（20 对：种子 301~306 @300 代 + 401~414 @400 代）
| 指标（PROD − CTL） | Δ | 判读 |
|---|---|---|
| 最长连防 | **−0.90 ± 0.30（t≈−3.0）** | 14/20 对为负、6 对持平、只有 1 对 +2 ⇒ **连防确实被"选择口径"压住** |
| 门数 | +0.15 ± 0.30 | 无差 |
| 出手G / 净兑现 / 落地种 | +0.20 / −0.08 / +0.00 | **无代价也无收益**（与热启动批相反方向 ⇒ 预算/起点决定谁受益） |
| 设防率（水平） | +0.53 ± 0.51 | 无差 ⇒ 压的是"维持"，不是"占比"（与 §E23 完全同形） |
| **官方考卷 1st** | **−4.55 ± 2.78pt（t≈−1.6，n=20）** | **不升反略降** ⇒ 换口径买不到考卷分 |
### ③ 拆旋钮批（6 对，样本小，只给方向）
- 只把 `epsMode` 硬档→**soft**：落地种 **+0.83±0.40**、产品口径 G **+1.55±0.60** ⇒ 广度受益；但设防率/连防逐对在 **+31 / −42** 之间摆 ⇒ 6 对完全不够判。
- 只把 **temp 0.35→0.15**：门数 **−0.67±0.42**、出手 G **−0.43±0.28** ⇒ 略坏（方向：出厂那个"偏烫"的温度对门禁读数反而有利）。
⇒ 合起来：**PROD 那一揽子里真正在起作用的更像 `epsMode`，不是温度**；但要把这个拆开单独定论，至少要 20 对 × 600 代。

### ① 与 ② 的矛盾怎么读（这条最要紧，别糊过去）
同一件事（把选择口径挪到产品口径）在"热启动"下**+1.1 道门**、在"从零"下 **+0.15 道门**、在**官方考卷**上 **−4.6pt**。
⇒ 我的判读（**推断，非实测**）：出厂那格硬档噪声相当于给搜索加了一层"防糊"正则 —— **起点已经很好时**它会把解往糊的方向拖（热启动批掉门），**从零起**它反而没那么致命，而考卷分（脚本对手池）本来就不吃这一维。
⇒ 所以这条不能报成"应该换口径"，只能报成：**"从现役继续训练出货"这件事值不值得做，取决于用哪格口径选包 —— 这属于裁定，不属于我这侧。**

复现：`env EPIRUS_ARM=hsprod$S EPIRUS_SEED=$S EPIRUS_HOTSTART=1 EPIRUS_SEEDPACK=js/bundled-champion-3p.js EPIRUS_TRAIN_EPS=0.2 EPIRUS_TRAIN_TEMP=0.15 EPIRUS_TRAIN_EPS_MODE=soft node tools/train-3p.mjs 300 5 12 12`（对照去掉那三个 `EPIRUS_TRAIN_*` 即出厂格）。
产物与逐对表在 `%TEMP%/kr3/{artifacts, e28b-eval-out.txt, e28b-eval-hs.txt, e28b-eval5-out.txt}`（**临时目录，会过期** ⇒ 数字已全部抄进本节；要复现请照上面的命令重跑）。

## E33 11:22 · 用户裁定落地：防御质量三档（白防 / 被穿透=半 / 有效）——**这把尺比我的"空挡率"好测一个数量级**
原话：「设一下防御打空率的质量判断，也就是出防御的时候完全没人打他就算白防御了（被穿透算防御半有效）」⇒ 质量分 = `(有效 + 0.5×被穿透) / 防御手数`，单一来源 `tools/defense-quality.mjs`（探针 / promote / 门 D163 共用），promote 那栏**只记录不阻断**（`EPIRUS_NO_DEFQ=1` 可关）。
四处实现时必须选的边界（原话没说，全部写进注释并被 D163 用**合成事件窗**逐条钉）：非卡伤害（梦魇/连环/违约/血债）**不算**"打我"；一回合里既挡下又被穿透记**有效**；自损 / 打别人 / 反击都不算"朝我来"。
**首批读数（攒钱者装配 · 产品口径 · 60 局）**
| 包 | 质量分 | 白防率 | 防御手数 |
|---|---|---|---|
| 现役 `v7cmin4-31` | **7.6%** | 89.2% | 204 |
| `v7cmin4-82`（健康对照） | 7.6% | 89.8% | 197 |
| `cbs1s2-band2`（用户报的那粒） | **2.5%** | 96.8% | 2545 |
| `v7seat24-31`（v1.5.104~143 线上包） | **0.8%** | 99.0% | 4751 |
| `v7b-31` | 0.3% | 99.6% | 8390 |
| 归档 60 个等价类 | p10 3.1 · 中位 **8.4** · p90 18.5 | | 手数中位 304 |
**信度对照（同一批 60 粒，换种子重跑）**：`质量分 r_tt = 0.848`（防御手 ≥500 的那 12 粒 **0.979**）‖ 我原来那把"空挡率" `r_tt = 0.07@40局 / 0.26@240局`。
⇒ 用户的定义之所以更好测：它数的是**有没有被瞄准**（分母大、事件明确），而我那个"没挡住"要去反推"没防会怎样"。⇒ **将来若立这条栏，用质量分，不用空挡率；阈值仍等你裁。**

## E34 12:45 · 第 5 轮（800 代热启动孪生臂）：**用户叫停，我先把结论按实说：口径效应没跑过预算噪声，昨晚那条要降级**
用户指令：「跑得太慢了，看有没有成果不然就停了；训练不是有多线程吗，以后不要跑单核的」⇒ 已停（3 对完成，第 4 对未开），并记下：**`tools/train-3p.mjs` 是单进程的，多核在 `train-fast/train-best` + `server/paralleltrain.mjs`（`EPIRUS_WORKERS`，上限 16）**。以后臂一律走 worker 池（已写进记忆）。
**3 对（seed 701/702/703 · 都从现役热启动 · 800 代 · pop 12 · games 12 · 唯一变量=选择口径）配对差（PROD − CTL）**：
| 指标 | Δ | 逐对 |
|---|---|---|
| 门数 | **−0.33 ± 0.88** | −2 +1 0 |
| 净兑现 G | −0.26 ± 0.45 | −0.4 +0.6 −1.0 |
| 最长连防 | −0.33 ± 0.67 | +1 −1 −1 |
| 设防率 | −0.06 ± 1.41 | +2.3 0 −2.5 |
| 官方考卷 1st（单种子 ×10 局/组合） | 701 **60.6 vs 52.0** ‖ 702 41.7 vs 54.0 ‖ 703 52.6 vs 46.9 | 方向都不一致 |
⇒ **昨晚 §E28b 报的"产品口径 +1.13 道门 / 连防 −0.90"在 800 代预算下全部缩进噪声里** ⇒ 那两条**降级为"低预算下的现象，未排除是预算交互"**；不许再当结论引用，要定论必须在**同一大预算 + worker 池 + ≥6 对**下重跑。
⇒ 这正好是 METHODOLOGY 65 的正面案例：300/400 代时两臂都远未收敛（出厂臂只有 2.9/5 门），"口径差异"里混着"谁先跑到哪儿"。
**这一轮真正的成果（与口径无关，可引用）**：
- **四臂产物没有一个能上槽**：门数 4/5、4/5、3/5（出厂）与 2/5、5/5、3/5（产品），而现役是 5/5 ⇒ **从现役热启动训 800 代是"往下漂移"而不是"往上"**（与 DS 的 `ft0` 同形，也与"挑不出池子里没有的东西"一致）。
- **新质量栏在这批上确实分出层次**：臂产物 防御质量 **9.3 / 11.2 / 10.2 / 11.3 / …%**，现役 **7.6%**，`band2` **2.5%**，`seat24` **0.8%** ⇒ 即便门数漂移，"防得准不准"这一维能把它们排开 —— 这条栏值得留着（阈值仍等你裁）。

## E35 14:55 · 把第 5 轮补到 **n=14 对**（**并发跑的，28 粒 1h46m**）：口径效应**确认不存在于这个预算**，我昨晚那条正式作废
用户 12:5x 说「那你继续吧」⇒ 按 §E34 的自我要求补功效：**同协议、种子 801~814、800 代、热启动自现役、唯一变量 = 每代评分的被评席执行口径**（出厂 `temp0.35·ε0.15·硬档` ⇄ 产品 `temp0.15·ε0.2·k5·soft`）。
**做法换了**（这是本轮的工程成果）：不再串行 ⇒ `EPIRUS_T3P_OUT`（新，见本版）把产物改道 + 14 路并发 ⇒ 28 臂 **13:07→14:53 = 1h46m**（串行要 5 小时以上）。默认 CLI 基线文件哈希 `97d2f54c5ef1` 跑前跑后逐字节相同（门禁拿它当基线，不许被臂污染）。

**配对差（PROD − CTL，n=14）**：
| 指标 | Δ ± SE | 逐对符号 | 判 |
|---|---|---|---|
| 五道门数 | **+0.29 ± 0.46** | 6 正 / 5 负 / 3 平 | t≈0.63，**与零不可分** |
| 净落地 G | −0.14 ± 0.39 | — | 零 |
| 设防率 | **+1.31 ± 0.75** | 11 正 / 3 负 | t≈1.75，**方向还反着** |
| 最长连防 | +0.64 ± 0.49 | — | 同向反着 |
| 局长 | +0.45 ± 1.61 | — | 纯噪声 |
| 产品口径 G | +0.57 ± 0.40 | — | 噪声内 |

⇒ **正式作废 §E28b 的两条结论**：「热启动批 门数 +1.13±0.35（t≈3.2、7/8 正）」与「从零批 连防 −0.90±0.30」都**没能在 n=14 / 800 代下复现**；前者换到大预算后是 +0.29±0.46，后者符号在本批甚至翻正。
**为什么它们当初看着像**：8 对 × 1500 代那批的两臂**未收敛程度不同**（METHODOLOGY 65 的形状）⇒ 我按"配对 t 大"立论，没先问"两臂是不是都跑到同一阶段"。这是**我用错了统计量**（SE 小不等于可信，功效来自独立重复数 × 预算）。
⇒ 口径这条线**结案**：在 800~1500 代预算下，"换选择口径"对五道门是**零效应**。要它成为判据，得先在**别的**目标上证明有增量，而不是再滑一档预算。

## E36 15:10 · 「那这批到底有没有一粒能上线」——**我把批内最优点送去走全量 `promote --dry`，它零阻断；但把现役拉进同一把尺之后，这个"能"字塌了**
被评对象：`E35-prod-807`（本批最优点：考卷 58.0%、五道门 5/5、产品口径 G 3.71/净 2.82、体质 Δ +0.4pt 干净、防御质量 7.0%）。
**先说一个拦路的仓里 bug（不是我造成的，本轮实测）**：`node tools/promote-champion.mjs js/bundled-champion-3p.js --dry` **当场崩**——
`promote-champion.mjs:68` 的 `JSON.parse(metaM[2])` 抛 `SyntaxError`，因为**线上槽的 META 里有未加引号的键 `fingerprintRefresh:`**（v1.5.174 手写进去的那段证据说明）。
⇒ 连带后果（全部已实测）：**凡是 `JSON.parse` 这段 META 的工具，读现役包时都拿 null**：
`skill-report.mjs:66`（吞）、`style-exam.mjs:76`（吞）、`np-test.mjs:3145`（吞 ⇒ D67 读"线上包 G4 例外"实际读成"没有例外记录"）、`server/train-server.mjs:828`（吞）；
只有 `promote-champion.mjs` 不吞 ⇒ **它是唯一会响的那个，而它响在"想复核现役"这条路上**。
**我没有修**：两条路都属裁定面 —— ① 改 `js/bundled-champion-3p.js`（加引号，权重字节不动，但那是**动槽文件**）；② 改 promote 让它从沙箱里读 `sb.window.EPIRUS_CHAMPION_3P_META`（不碰槽、我倾向这条）。本轮为拿到对照表，做了个**副本** `docs/artifacts/incumbent-forpromote.bak`（只补引号，未动槽）。

**同一条命令、同一把尺的对照表**（`promote --dry`，默认样本量）：

| 栏 | 现役 `v7cmin4-31` | `E35-prod-807` |
|---|---|---|
| A 考卷 1st（40 局） | 54.4% | **58.0%** |
| 自对局 有效技能数 | 4.44 | 3.64 |
| 反弹墙（4 席 reflectspam·长程·20 局） | **主动伤害 21.00/局 · 穿透命中 364** | **1.00/局 · 命中 20** |
| 场A 被集火还手率 | 35% | 32% |
| 座位极差 | 13pt ✓ | 12pt ✓ |
| 可行性五道 | ✅ 全过 | ✗ 场B 清场 0.28 < 0.3（**记录不阻断**；⚠️ 这一格 **§E37 已撤回**：n=40 的抽样产物，n≥80 时它读 0.40~0.44 与现役同带） |
| G4/G5 行为门 | （参照行） | ✅ **候选自己全过** |
| 设防体质 Δ(钱) | +1.1pt（非钱驱动） | +0.4pt（非钱驱动） |
| 防御质量（用户新尺） | **6.3%**（n=112 手） | **7.0%**（n=100 手） |
| 珠经济 | 得珠 273 · **花珠率 83.5% 闭环** | 得珠 835 · **花珠率 14.3% 未闭环** · 过期 17.5/局 |
| `promote --dry` 退出码 | （崩溃，见上） | **0 = 零阻断** |

⇒ **结论：这批没有值得上线的一粒**，但**理由不是"门没过"** —— 807 是**零阻断**通过的。真正的理由是它比现役**差在一栏看得见的地方**：
⚠️ **口径说清楚**：我只把**批内最优点 807 这一粒**送去走了全量 `promote --dry` ⇒ "**至少**有一粒零阻断"是实测，"**只有**一粒零阻断"我**没量**（另外三粒门数 5/5 的是 `E35-prod-805/808/814`，已留档待测）。
  按 §E36 下面那条"墙那栏只在 `===0` 才拦"的形状，我**推断**这批里过 dry 的不止一粒 —— 属推断，不是读数。
**反弹墙 21.00 → 1.00 伤害/局（穿透命中 364 → 20，掉到 1/18）**，而那一栏的**阻断条件是 `pierceLand === 0`**（`promote-champion.mjs:155`）⇒ **1/18 是"不为零"，所以不拦**。
这正是老档案里 tgt-44 家族"墙瘫"的形状，只是这次瘫到 1.00 而不是 0.00 就放行了。
⇒ 给 DS/你的**判据缺口**（我不动判据）：墙那一栏是**二值闸**，而实测分布是连续量（现役 21、807 1、`v7f3` 系 0）⇒ 若改成"下限 + 参照比"（例如 `< 现役的 1/4` 才阻断），807 会被当场拦下。**属裁定，等你点头再动。**
**防御质量这栏的横向读数（本轮 28 粒 + 现役同尺）**：臂产物质分 p10 3.3 / **中位 8.9** / p90 14.1，最好 `E35-ctl-813` **16.0%**（白防 79.3%）；现役 6.3%、`band2` 2.5%、`seat24` 0.8%。
⇒ **臂作为一个家族在质量轴上略优于现役**（中位 8.9 vs 6.3），但**"分不开病例带"**：807（7.0%）与现役（6.3%）的差在 n≈100~200 手下远小于噪声（§E33 r_tt=0.85 ⇒ 单粒 SE 约 ±3pt）。⇒ **这栏现在能当"排除明显坏的"，还不能当"选出更好的"** —— 阈值与用途仍等你裁。
**训练预算的账（给下一次）**：28 臂/1h46m 是在**并发 + 800 代 + pop 12** 下；如果按你之前说的"要定论得同预算 ≥6 对"，现在**一对的成本 ≈ 4 分钟**（并发摊薄），一晚上能跑 200+ 对 ⇒ **功效不再是瓶颈，判据形状才是**。

## E37 15:23 · 把 §E36 那格 `✗ 场B 清场 0.28` 撤回：**它是 n=40 的抽样产物，不是这粒包的弱点**（同一把尺扫五个样本量）
`node tools/probe-*` 式的临时扫描（`audit-lib` 真源函数 `aggressionProfile`，只改 `games`，其余一字不变）：

| 包 | n=20 | n=40（**promote/门禁用的就是这个档**） | n=80 | n=160 | n=320 |
|---|---|---|---|---|---|
| 现役 `v7cmin4-31` | 0.45 ✓ | **0.33 ✓** | 0.40 ✓ | 0.49 ✓ | 0.49 ✓ |
| `E35-prod-807` | 0.35 ✓ | **0.28 ⛔** | 0.40 ✓ | 0.44 ✓ | 0.41 ✓ |
| `E35-prod-814` | 4.00 ✓ | 4.00 ✓ | 4.00 ✓ | 4.00 ✓ | 4.00 ✓ |

- **807 的那格 ✗ 只在 n=40 出现**：n≥80 它读 0.40~0.44，与现役同带 ⇒ 我 §E36 表里"807 比现役多一条 ✗"**作废**（已在原处标注）。
  现役自己也在同一格上从 0.45（n=20）掉到 0.33（n=40）再回到 0.49 ⇒ **这一栏在 shipped 样本量下的散布就有 ±0.12**，而线画在 0.30。
  ⇒ 这不是"某粒包不行"，是**这条线在 n=40 上没有分辨率**（与 §v1.5.243 那条"G4/G5 默认 60 局、阈值与 SE 同量级"是同一件事，这次是第五道）。
- **`E35-prod-814` 在 n=20~320 恒等于 4.00**（每局把四个软靶全清）⇒ 与"场B 高分是软靶刷的"这条老账**完全同形**（v1.5.145 那批 `band2` 场B 4.00/局 但 G5 防席夺冠 34%>25%）。
  ⇒ 已把 814 送去走**全量 `promote --dry`（含 G4/G5）**，判它"会不会打硬防守"而不是"会不会清软靶"。
- **一条可复用的量具规矩**（值得进 METHODOLOGY，等 DS 认账再写）：**任何"按局计数"的门栏，报数时必须同报它在 2~4 倍样本下的读数**；
  若散布盖过阈值差（这里 0.28 vs 0.33 vs 线 0.30），那这条线**当前不具备判据资格**，只能当记录。

## E38 15:26 · 把批内"最强的一粒"送去走同一把尺 ⇒ **门这次做对了：它被 G4 拦下**，而这恰好把下一条实验指出来
`E35-prod-814` 是 §E37 里那粒"场B 恒 4.00/局"的包，也是本批**除口径外各维最好**的一粒（`promote --dry` 同尺读数）：

| 栏 | 现役 | `E35-prod-807` | `E35-prod-814` |
|---|---|---|---|
| 有效技能数 multi / long | 4.44 / 3.43 | 3.64 / 3.34 | **4.89 / 5.00** |
| 覆盖类别 | 3/4 | 3/4 | **4/4** |
| 净兑现（打上血的种数） | 4.44→2.66（3 种） | 3.64→2.83（3 种） | **4.89→3.05（4 种）** |
| 珠经济 | 花珠率 83.5% 闭环 | 14.3% **未闭环** | **92.4% 闭环**（得珠 157 · 过期 0.2/局） |
| 防御质量（用户新尺） | 6.3% | 7.0% | **11.3%** |
| 设防体质 Δ(钱) | +1.1pt | +0.4pt | **−1.0pt**（干净） |
| 场A 还手率 | 35% | 32% | 26% |
| 反弹墙 | **21.00/局 · 穿透 364** | 1.00/局 · 20 | 2.00/局 · 40 |
| **`promote --dry`** | （META 那个 bug 让它崩，见 §E36） | **exit 0 零阻断** | **exit 6 · 4 条阻断** |

**拦下它的正是 G4**：`行为门未过：G4[E35-prod-814/long|multi] … 最克「只枪(1ジ压制·打最肥)」87% / 89%（基线 21% / 17% · 300 局）`
⇒ 一粒"广度 5.00、四类全覆盖、珠经济闭环、体质干净、防得也准"的包，**被最便宜的那张 1ジ 枪以接近 9 成的概率打死**。
**这条读数把 09-23 夜的那个假设从"我猜"变成"有对照"**：那晚写的是"所有宽包死在同一族 1ジ 枪压制，缺的是目标函数里的 min-over-counters 项"，
今天**从一个全新方向（800 代并发孪生臂）又长出一粒宽包，它仍然死在同一行脚本上** ⇒ 不是训练量、不是口径（§E35 已证零效应）、不是奖励形状里的广度权重。
**⇒ 下一条实验（15:28 已开跑，预注册就写在这，跑完不许改终点）**：变量 = **`EPIRUS_COUNTER_OPPS`**（v1.5.172 就有的开关，把"只防御不还手/克制原型"**放上训练桌**，**规则一字不改**）——
 12 对孪生臂（901~912 · 800 代 · 热启动自现役 · 并发 8 路以免跟 DS 抢满 CPU），**主终点 = G4 那行「最克 %」**（现役基线 21%/17%，线 60%），
 次终点 = 五道门 / 净兑现 / 防御质量 / 体质 Δ / 考卷。
 ⇒ 若处理臂的「最克」从 ~87% 压到 ≤30% 而广度不掉，那就是**第一个能写进判据的增量**（仍属裁定，我只量不改）。

## E39 16:35 · 结果：**「把克制原型放上训练桌」在这条主终点上是零效应，甚至偏负** ⇒ 这条线结案（24 臂 53 分钟跑完，零失败）
先记管道：12 对 × 800 代 × 热启动自现役，变量 `EPIRUS_COUNTER_OPPS` 0/1，**8 路并发 13:07→…16:21 = 53 分钟、24 臂全 exit 0**。
主终点用**真源** `tools/gate-drafts.mjs`（`N4=300` 默认）批量量，24 粒分 4 批并行；解析脚本 `%TEMP%/kr3/e39-g4-parse.mjs`。

| 读数（n=12 对） | CTL（不上桌） | TRT（上桌） | 配对差 |
|---|---|---|---|
| G4 最克 %（long） | 均值 **60.3** | **64.1** | **+3.83 ± 5.99SE**（t=0.64；降 4 / 升 7 / 平 1） |
| G4 最克 %（multi） | 均值 46.3 | **58.7** | **+12.33 ± 7.58SE**（t=1.63；降 4 / 升 8） |
| G4 两模式都 PASS 的粒数 | 5/12 | 5/12 | **零增量** |
| 臂内自评 1st（28 对×20 局，粒度 3.6pt） | ≈28% | ≈32% | 略升 ⇒ **它确实改了训练里的东西，但改不到 G4** |

- **裁定级结论（三个独立面都指向同一处）**：
  ① **上桌不转移**：最克% 平均**不降反升**（multi +12pt，方向对我不利，且不显著）；PASS 率 5/12 → 5/12 一字不差。
  ② **弱点没有消失，只是集中了**：TRT 侧 **10/12 粒的最克脚本都是「只枪(1ジ压制·打最肥)」**（CTL 侧 8/12），
     即"对面用一张 1ジ 枪我就被打成 60~100%"这件事**不是稀有事件，是这条流水线（热启动 800 代）的默认产物**。
  ③ 复现并加强了 09-23 夜 `xn26a` 那单（G5 85%→42% 但 G4[long] 82%→90%）：**"弱点挪地方"，不是"弱点被补上"**。
  ⇒ **「把克制原型放上训练桌」这条线结案：它不能作为增量写进判据。**（训练侧我不再为它花钱；剩下的唯一没试过的形状是**目标函数里的 min-over-counters 项**，那属判据改动 ⇒ 等你裁。）
- **顺带一条今天最值钱的对照（同一进程、同一把尺的参照行）**：**现役 `v7cmin4-31` 今天读 最克 long 38% / multi 35%**，
  而我这两轮一共 **36 粒臂产物的均值是 long 60 / multi 46~59** ⇒ **从现役热启动接力训 800 代，五道门里看不出退化（§E35 已证），但 G4 这一维会实打实地退化。**
  ⇒ 这才是"臂产物从来没上过槽"的机制解释，也是一条**可操作的账**：现役在 G4 上的 38% 是**资产**，任何"接着训"都可能把它训掉。
- **今日两条判据形状缺口现在合成同一条**（都不阻断，只报给你/DS）：**门禁缺的是"与现役相对比较"这一维** ——
  ① 反弹墙：阻断条件 `pierceLand === 0` ⇒ 现役 21.00/局 vs 候选 1.00/局 照样放行（§E36）；
  ② G4：阈值是绝对线 60% ⇒ 候选"最克 59%"能过，而现役同一格是 38%（§E39）。
  ⇒ 形状建议（我不动判据）：这两栏都改成 **"不劣于现役 + 容差"** 的相对线，容差按各自样本量的 SE 给（墙 n=20、G4 n=300 ⇒ SE 已在档案里量过）。

## E41 16:37 · 按"G4 最克最低"预先挑 5 粒送去走全量 `promote --dry` ⇒ **反直觉的主结果：它们被别的门拦下，拦它们的全是广度**
**挑选规则预先写死**（不许看完结果再挑）：E39 表里 G4 **两模式都 PASS** 的 10 粒中，按「最克两格之和」升序取前 5 粒
= `ctl-908(50)` `ctl-911(64)` `trt-912(74)` `trt-911(80)` `ctl-909(84)`；量具 = 全量 `promote --dry`（含 G4/G5 + 体质栏 + 防御质量栏），5 路并行，16:34→16:37。

| 粒 | `--dry` | 考卷 | 反弹墙 /局（穿透命中） | G(multi) | G(long) | 净兑现 | 场B 清场 | 体质 Δ(钱) | 防御质量 | 被什么拦住 |
|---|---|---|---|---|---|---|---|---|---|---|
| **现役**（补引号副本） | 0 | 54.4 | **21.00（364）** | 4.44 | 3.43 | 2.66（3 种） | **0.33** | +1.1 | 6.3 | — |
| `ctl-911` | **0 零阻断** | 54.8 | **19.85（363）** | 3.38 | **2.86 ✗** | 2.39（3 种） | **0.03 ✗** | +2.2 | 9.2 | — |
| `trt-912` | **0 零阻断** | 52.7 | 1.90（38） | 3.59 | 2.48 ✗ | 2.50（3 种） | 0.03 ✗ | −0.2 | 8.0 | — |
| `ctl-908` | 6 | 47.4 | 3.10（58） | **1.82 ✗** | 1.73 | — | 0.00 | −1.3 | 10.7 | `G 1.82 < 3` |
| `ctl-909` | 6 | 57.4 | 3.35（67） | **2.87 ✗** | 2.25 | — | 0.00 | −0.6 | 8.4 | `G<3` + **G5 防席夺冠 38%/31% > 25%**（龟场收不掉） |
| `trt-911` | 6 | 55.0 | 4.40（82） | **2.93 ✗** | 3.01 | — | — | +0.1 | 6.9 | `G 2.93 < 3` |

- **主结果（这是今天第三条结案）**：**"抗克制"与"打法宽度"在这棵树里目前互斥** —— 挑出来的 5 粒里最抗克的 3 粒全部塌到两三张卡（G 1.82 / 2.87 / 2.93），另一粒（`ctl-909`）虽然考卷 57.4% 最高，却被 G5 拦下（"只防御不还手"那席夺冠 38%/31% > 线 25%）。
  ⇒ 这是"只能从池子里挑，挑不出池子里没有的"**第五次复发**，但这次带的是**反向权衡曲线**：**池子里 G4 最好的那一端，正是广度最差的那一端** ⇒ 光加"min-over-counters"这一项**不保证**能拿到又宽又抗克的包（它得先存在）。
- **两粒零阻断，其中 `E39-ctl-911` 是今天第一粒"五维不劣于现役"的包**：墙 19.85 vs 21.00、G4 最克 38/26 vs 38/35、考卷 54.8 vs 54.4、体质 +2.2 vs +1.1（都在 ≤12pt 的干净侧）、防御质量 9.2 vs 6.3。
  **但它两处明显退化**：**场B 清场 0.03/局（现役 0.33）** 与 **G(long) 2.86（现役 3.43，线 3）** ⇒ **我不建议换**：这两条都是产品可感的（无压局不收尾、长程打法变窄）。**已把这粒留档 `docs/artifacts/E39-ctl-911.bak`**，要不要换归你裁。
- ⚠️ **顺手把今天的缺口收口成一条**：`ctl-911` 能零阻断，是因为**那两条 ✗ 都不在 promote 的阻断集里** —— `G` 只判 **selfPlay multi** 那一格（它 multi 3.38 ✓），**long 2.86 只记录**；场B 只记录。
  ⇒ 与 §E36（墙 `===0` 二值闸）、§E39（G4 绝对线 60%）合起来是**同一条**：**门禁既缺"与现役相对比较"，也缺"long 模式参与阻断"**。
  ⇒ 给 DS/用户的合并裁定件（第 8 条，一条而不是三条）：**把"不劣于现役 + 容差"的相对线**同时装到 ①反弹墙伤害/局 ②G4 最克% ③G(long) 有效技能数 ④场B 清场 —— 四处都是"现役明显更好、但门不响"的同一形状。




