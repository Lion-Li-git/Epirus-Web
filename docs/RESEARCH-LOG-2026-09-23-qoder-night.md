# 研究日志 · 2026-09-23 夜（qoder 自主探索 · 分支 `qoder-explore-0922night`）

> 用户 09-22 22:5x 指令：**开新分支自己探索训练**；① 先修已知 bug（长程模式"出现过一次 hp=1 之后血回上去了摄魂仍可用"）；
> ② 继续研究**怎么训出真实技能多样性与适应能力**，理想 = 一个 5 血长程包通吃其他模式；
> ③ 现在的 `v7cmin4-31` 虽然 `G_eff=4.44` 但**实际只用 6 个技能**，怀疑"为刷 eff 专挑最容易被测到的卡"（大雷一被防就倾向不用）；
> ④ skill report 里还有很多技能是 **Δ 结构性为负 / 测不出真实价值** ⇒ 尽量让更多技能可测量。
> **时限：做到早上 09:00**，"尽量多探索，不要干一个小时就结束"。

## 状态灯（每次改动都来更新这行）

| 时刻 | 门 | 分支/HEAD | 指纹 | 槽位 | 在跑 |
|---|---|---|---|---|---|
| 22:5x | np **173/173** · spec 52/52 · smoke OK | `qoder-explore-0922night` | **71b5927f → 0f931cb7**（v1.5.166 换代） | 3P=`v7cmin4-31` · 2P=`v7xfer44c13-2p`（**未动**） | — |
| 01:2x | np 173/174（唯一红 = D82 记账，本版 CHANGELOG 补齐） | 同上 | `0f931cb7` | **未动**（两槽逐字节） | xn17b / xn15f（第二种子复现对） |
| 02:1x | np **跑完再记**（§N28 已把三处结论改写成"复现后的版本"） | 同上 | `0f931cb7` | **未动** | §N29 广度准入线原型（默认关） |

## DONE

### D-1 22:59–23:5x · 修 R48 条件绕过（v1.5.166）+ 门 D126 + 指纹换代重记
**结论：用户报的"时间闩锁"没能复现；追到并修的是一处更根本的规则实现错误（同一条 bug 的三种形态，且都短在"回魂复活回合"）。**

1. **先复现**（这条决定后面所有话的可信度）：
   - 直接判据 `computeCost`：hp 1→4 立刻判非法 ✓；执行层 `attemptAction` 在 hp=4 返回 `invalid` ✓；`legalActions` 不含摄魂 ✓；2P/3P 两模式都如此。
   - **配对扫描**：260 局长程、5.2 万次摄魂合法性查询 ⇒ **越界 0 次**。
   - `infiniteEnergy` 生命周期：`startTurn` 置、回合末清并且把复活者打死（R48）⇒ 不持久；而复活时 hp=1，**本来就落在窗口内** ⇒ 解释不了"血回上去还能用"。
   - 连边 = 铁索连环（伤害共享，一次性），与可用性无关。
   - 页面日志会把被拒的一手标成"（无效）/（ジ不足·未发动）"（`ui.js:295,482`）⇒ UI 也没骗人。
2. **真正找到的错**（用户描述的"满血也能用摄魂"在这个分支上是可实现的）：`infiniteEnergy` 把**条件**连同**花费**一起免掉，三处短路：
   `state.js:computeCost` 开头 `return {ok:true}`、`state.js:attemptAction` 干脆不走 `computeCost`、`play.js:legalActions` 直接 `push(affordable:true)`。
   规则原文（`RULES-2P.md:336`）只说"**一切花费为 0**（含蓄能产物无限）"—— 摄魂的 `HP≤1/≤3`、电磁炮的"需 1 枚电珠"、激光眼首次"需 1 枚爆珠"、过载炮第 2 次"需 ≥1ジ"/第 3 次"自损 1 血"都是**条件**或**花费**，前者不该免、后者该免。
   ⇒ 改法：`rawCost` 照原判据查条件，`computeCost` 只在条件通过后把 `ep/hp/beads` 归零并打 `free:true`；`attemptAction`/`legalActions` 取消短路。**恰好修掉 5 个被一起绕过的判据。**
3. **变化范围（实测，不是估计）**：新旧引擎同种子配对复现 **6480 局**（standard/multi/long × 9 种出招风格）⇒ 只有 **13 局触发过回魂**、**4 局读数不同**、**胜者全部相同**，差异全在复活回合内部（谁多打 1 点、谁少扣 1 血）。
4. **指纹换代**（`state.js`/`play.js` 都在五件套里）：`71b5927f → 0f931cb7`。两槽全部读数**逐位不变**：2P 考卷 `99.00%/最差 82.50%`；3P 考卷 `54.4%`、五道 `座位12.5pt / G(multi)4.44 / G(long)3.43 / 墙21 / 场A35% / 场B0.33` ⇒ 按 v1.5.157 先例把新指纹 + 这段话写进两个 bundle 的 `meta.rulesFingerprint/fingerprintRefresh`（**权重逐字节未动**，D16 已绿）。
5. **门 D126**（`np-test`）：满血免费回合三个条件门都不许绕过 + 合法表不含它 + 窗口内免费且不扣资源 + 过载炮两步 + 普通回合回归 + **反抄静态钉**（三处短路的字面形态都不许复活）。np 172→**173/173**。
6. **我改过头又被退回的一处**：把 `p.lastTarget/lastTarget2` 改成"只在成功一手写" ⇒ **D26 判红**（那条 pin 要它无条件留在玩家身上，v1.5.19 的理由是"真人局手势指向谁大家都看得见"）。既然这是**判据级取舍**而不是规则错误，已回退。
   **待裁（Q-1）**：作废/被拒的一手要不要进入 v7 的关系特征 T 块？现状 = 要。若要改，需连带重训与重记两槽。

### D-2 23:5x · 我一度把指纹算错（记录以免明天对不上）
`2a3179fb` 是**回退 lastTarget 之前**的指纹；我拿它去重记了一次 meta，随后 D16 仍红 ⇒ 真值是 `0f931cb7`（回退之后）。所有读数/A-B 都在回退之后跑，**证据仍然成立**，只是号记错过一次。

### D-3 00:0x · §N22/§N23 两件新量具（`probe-skill-marginal` + `probe-cast-vs-land`）— 记在 CHANGELOG 的 `research(0923)` 条
**§N22 为什么要第三套技能口径**：`skill-report` 的 mono-spam Δ 对辅助/防御卡是**结构性负**（反弹 −69 / 八卦 −79 / 贴贴 −82 / 天火 −82 ⇒ 那是口径的价，不是卡的价）；
对**要铺垫的卡**直接失效（电磁炮强制命中 4%、摄魂 6%、激光眼 0%）—— "每回合都打它" = 永远不许先备珠，**强迫自己反而把它逼没了**。
改成量"愿不愿意用"：ε 概率、仅在该卡合法且买得起时替换冠军一手，其余照打，同种子同对手配对，SE 取逐局胜负差；再加 `--rich=card`（**定向垫钱**，两臂同垫）。
**long 可读卡片 1~2 张（mono-spam）/ 3 张（原生 ε）→ 14 张**（multi 12 · 2P 槽 14）。
两个发现：① **长程是经济瓶颈不是偏好瓶颈** —— `光垫钱` 列在 long 下一律 **+41~46pt**（multi 只有 +17.5~+36）；② 我自己的两个错：补贴挂在 chooser 里（合法表在 chooser **之前**算好 ⇒ 补了珠也进不了 `legal`，靠"合法率 90% 却机会 0.27/局"这组自相矛盾才暴露）⇒ 改挂回合开始钩子；`--rich=6` 无差别补贴把基线打到 **1st=100%**（天花板饱和 ⇒ 所有 Δ 挤成 0）⇒ 才改成定向垫钱 + 光垫钱列自证。

**§N23 用户的怀疑成立且可量化**：`G_eff = exp(出手次数的熵)` 只看"发起了几种"⇒ 一张常出手常被防住的卡与一张少见但每次掉血的卡**在同一把尺上等价**。
现役 `v7cmin4-31`：`G=4.44`，但 multi 镜像 **出手:落地 = 530:237**（**55% 的出手没变成伤害**）、long 镜像 995:494。

### D-4 00:2x · §N24 第二把尺 `effSkillsLand` 落地（v1.5.167）+ 当选面排序键 `EPIRUS_SEL_LAND`（默认关）+ 门 D127
`mirrorHealth` 本来就有 `landByKey`（按 `damage.via` 归因）⇒ 用同一套熵数它，新增 `effSkillsLand/landedKeys/landedTotal/landedFiltered`，`promote` 并排印 `兑现 G 出手→落地`。
**为什么不把"落地"写进奖励**：仓里已证"奖励贵技能落地"会选出**乱挥双枪**的冠军（−27pt，`evo.js:evalSubsidyProbe` 头注）⇒ 只许当**同分带内的排序键**。
门 D127 = 纯函数九向 + 默认关基线哈希钉 `aa743488cc` + 开了必须印两把尺 + 必须三分归因。np 173→**174**（含本版新行）。

### D-5 00:4x · §N25 **两臂负面结果 + 一个发布前自查抓到的口径 bug**（本条最重要，别跳）
**（a）L1 臂 `v7xn15a`（`EPIRUS_SEL_LAND=1`）：排序键咬了，咬上来一包病包。** 带内 `by=land` 改判 → 冠军 = `band5`，体检实测
`考卷 35.1% · C 盾/局 11.9 · D 长程反弹墙 0% · 反弹墙伤害/局 3.20 · E 无威胁摆架势 49% · G(出手) 4.74`。
⇒ v1.5.168 补两件事：① `bandPickByLand` 先剔 `gateOk===false`（阈值 `HOLO_GIFT_MAX` 从 `promote` 的字面量 6 抽进 `audit-lib` = 单源）；② 打印**三分归因**。
**（b）L1b 臂 `v7xn15b`（+预筛）**：带 `2/6`、`by=score`（排序键没换人）、冠军 `band4` = `考卷 39% · C 盾 0.1 · D 墙 100% · 墙伤害 13.90 · 座位极差 9pt`。
但当时打印只说"未改判" ⇒ **预筛换掉了池**（6 剔 4）而产物其实换了包 —— 这就是必须三分归因的直接原因（我自己的打印骗了我一次）。
**（c）发布前自查发现的 bug（v1.5.167 未提交前修掉，故不占版本号）**：`landByKey` 按 `damage.via` 统计，而 `via` 里有一批取值**不是一张卡**——
`deliverDamage` 在 `via` 缺省时回落到**中文 reason**（`resolve.js:305`）⇒ `终局收缩`（规则白送的全员 −1）入账；另有 `headshot`（爆头 = 狙击的结果修饰，`resolve.js:1207`）、`原型制御·转移`（`resolve.js:270` 只给 reason）。
**污染量实测**：同一臂 6 粒候选，非卡名占"落地" **10%~41%**（`band4`：293 次落地里 104 次是终局收缩 = 41%）。
**它造成的后果不是"数字偏大"，而是方向错**：不滤就**奖励"拖到收缩阶段还活着"的包**、惩罚主动进攻的包。
同一批候选在污染口径 / 净口径下的 `G(落地)` 排名：

| 候选 | 出手 G | 落地 G（污染，旧） | 落地 G（净，新） | 非卡名占落地 | 盾/局 |
|---|---|---|---|---|---|
| band1 | 3.20 | 2.29 | **1.61（3 种）** | 10% | 6.3 |
| band2 | 4.32 | 2.61 | 1.48（3 种） | 19% | 10.3 |
| band3 | 1.38 | 2.14 | 1.24（3 种） | 19% | 0.0 |
| band4 | 1.17 | 2.38 | **1.04（2 种）** | **41%** | 0.1 |
| **band5（L1 当选）** | **4.74** | **2.62（全场最大 ⇒ 被选）** | **1.51（2 种 ⇒ 排第 5）** | 18% | **11.9** |
| band6 | 3.48 | 2.43 | 1.52（2 种） | 15% | 13.2 |

（两列 `落地 G` 都是 `mirrorHealth(20 局, 5 席, multi)` 实测，只是键集过没过 `R.byKey`；污染列里 band5 与 band2 只差 0.01 ⇒ **一个纯噪声级的差决定了当选者换人**。）

⇒ **L1 那句"兑现熵比出手熵还高（1.17→2.38），所以它是另一根轴"是假的**，纯粹是收缩血被算成卡。净口径下现役包 `G 4.44 → 兑现 2.66（3 种真卡）`，
而 `band5` 的形态恰恰是用户怀疑的那个极端：**出手最多（938 次/20 局，现役 537）、兑现最少（274，兑现率 29%）**。
⇒ §N25 的结论改为：**"兑现广度当排序键"这条线目前仍未被证明有用**（净口径 + 预筛后在 xn15c/d 配方下 `带=1/6`、排序键**根本没得咬**，见 d-2），
但**"净兑现 G 当读数"已经被证明有用**（它一把就把 band5 的"广"拆穿了）。
`landedFiltered` 字段 + D127 里那段"独立复算熵 + 必须真出现非卡名 via（否则本断言是空枪）"就是钉这个 bug 不复发的。

### D-6 00:5x · §N26/§N27 新量具 `tools/probe-cross-mode.mjs`（**通吃矩阵**）
"5 血长程通吃其他模式"这句目标在仓里**从来没有一件工具能一次回答**（`mirrorHealth` 只测一种模式 5 席、2P 考卷只测 2 人、`champ-audit` 一行一个装配）⇒ 通吃一直是形容词。
现在是一张表：**列 = 5 个格**（`2P`standard · `3P/4P/5P`multi · `长程`long），**三个对手环境**（`镜像` / `脚本池` / `对现役`=其余席全用现役包），
所有包在每格用**同一批种子**（配对可比），受评席 `g % n` 轮转并印**胜率极差**，每格并排印 `G(出手)→G(落地)` 与兑现率。
两处自我修正记录在文件注释里：① 事件归属不能按 `pid` 一刀切（`damage` 带的是 `source` ⇒ 第一版落地恒 0）；② **平局率必须与 1st 并排印**（镜像装配 5P 的"1st=0%"其实是全员互灭=平局，只印 1st 会把读表的人骗到完全相反的方向）。

**首批读数（40 局/格，`脚本池` 环境，`1st/平局/极差`）**：

| 包 | 2P | 3P | 4P | 5P | 长程 | **最弱格** |
|---|---|---|---|---|---|---|
| `v7xn14a-band2`（§N14 被当选面挤掉的那粒） | **97.5%** | 45.0% | 45.0% | 22.5% | 32.5% | **5P 22.5%** |
| `v7xn13a-31`（收割席臂当选） | 65.0% | 37.5% | 42.5% | 15.0% | 22.5% | 5P 15.0% |
| **现役 `v7cmin4-31`** | 32.5%/平35 | 27.5% | 32.5% | 17.5% | **12.5%** | 长程 12.5% |
| 2P 槽 `v7xfer44c13-2p` | 100.0% | 27.5% | 35.0% | 25.0% | 10.0% | 长程 10.0% |
| `v7xn14a-2p`（xn14a 当选） | 97.5% | 32.5% | 37.5% | 12.5% | 7.5% | 长程 7.5% |

**"对现役"环境（其余席 = 现役包）—— 这才是"换包上去会不会更差"的那一问**：

| 包 | 2P | 3P | 4P | 5P | 长程 | 最弱格 |
|---|---|---|---|---|---|---|
| `v7xn14a-band2` | 95.0% | **77.5%** | **50.0%** | 22.5% | **55.0%** | 5P 22.5% |
| `v7xn15a-band1` | 100.0% | 55.0% | 15.0% | 22.5% | 37.5% | 4P 15.0% |
| `v7xn13a-31` | 77.5% | 20.0% | 27.5% | 10.0% | 17.5% | 5P 10.0% |
| 2P 槽包 | 100.0% | 62.5% | 10.0%/平28 | 7.5% | 7.5% | 5P 7.5% |
| `v7xn14a-2p` | 92.5% | 32.5% | 25.0% | 7.5% | 12.5% | 5P 7.5% |
| 现役包（自己对自己 ⇒ 只当标尺） | 0%/平100 | 20.0% | 22.5% | 17.5% | 22.5% | — |

**读数**：
1. **两个内置槽各缺一半**：2P 槽包在多人/长程几乎不会打（5P 7.5%、长程 7.5%），现役 3P 包在 2P 只有 32.5%（且平局 35%）⇒ "通吃"目前**没有任何一包及格**。
2. **`v7xn14a-band2` 是全场唯一"最弱格 22.5%"的包**，且在长程格里 55% 打赢 4 席现役 —— 这粒正是 §N14 里**被当选面挤掉**的候选（当时它的 3P 双栏都比当选者强，但 `score` 被 hill05 压住）。§N14/§N15 那条"当选面缺第二栏"的病，现在有了第二件工具（通吃矩阵）独立指回同一粒包。
3. **短板格普遍在 5P**（人人都 7.5%~22.5%）⇒ 5 人场的胜负被"座位多、互相消耗"摊平了，1st 天然低 ⇒ **绝对线不能横切**（这条要先记住，任何"通吃分"阈值得按格定标）。
4. `G(出手)→G(落地)` 在矩阵里**同一包跨格会动**（现役：2P `1.20→1.76`（兑现 10%）vs 5P `2.57→2.27`（75%））⇒ 广度不是包的固有属性，是"包 × 模式"的读数 ⇒ 之前所有单格 G 读数都该被理解成"multi 装配下的那一格"。

### D-7 01:0x · §N28 接上"训练模式"这根线（v1.5.169）：**长程臂 v7xn17a 在跑**
用户那句"炼一个 5 血长程能通吃其他模式"里，**"训长程"这一半仓里从来没跑过**：`evo.js` v1.4.0 就有 `setTrainMode`（门 D10 还钉着"`setTrainMode(long)` ⇒ 建局 5 血"），但 CLI 没有键 ⇒ `TRAIN_MODE` 恒 `'multi'`，`evo.js:28` 的注释自己就写着"5 血冠军从来没被训过"。
新增 `EPIRUS_TRAIN_MODE`（走 `server/train-env.mjs` 单一来源）+ 三条纪律：模式名必须被 `R.MODES` 认，不认 ⇒ **exit 7**（不许静默退回 multi）；下达后**读回消费点**；生效值写进 `meta.recipe.trainMode/trainModeEffective`（同版顺带把 `arm/seed/gens/xn2w/selLand/kill 开火计数` 一起进 `recipe` —— §N11 的教训是"读日志才知道这臂开了什么"，而日志会滚走、`.bak` 会留下来）。

### D-8 01:5x · §N28 长程臂两对种子：**判据判负，而我的一句因果结论被复现打回**
`v7xn17a/b` = 与 `v7xn15c/f` **逐字同配方**，唯一变量 = `EPIRUS_TRAIN_MODE=long`；种子 31 与 32 各一对。

| 读数 | seed31 `multi` `xn15c-band5` | seed31 `long` `xn17a-band3` | seed32 `multi` `xn15f-band1` | seed32 `long` `xn17b-band6` |
|---|---|---|---|---|
| 期末考卷（本臂 36 对 × 20 局） | `1st 40.1%` | `1st 44.4%` | `1st 43.3%` | **`1st 46.0%`** |
| **主判**：矩阵·脚本池 最弱格 | **4P 12.5%** | 长程 **7.5%** | 长程 **7.5%** | 5P **5.0%** |
| 各格 `G(出手)` 范围 | 2.46~5.21 | 1.00~2.41 | 1.87~3.17 | **5.52~6.74（今晚最宽）** |
| 各格净 `G(落地)` | 1.14~1.58 | **1.00（1 种）** | **1.00（1 种）** | 1.19~2.05（2~3 种） |
| `mirrorHealth(long,20,5)` 平均回合 | 17.1 | **49.9** | 19.0 | 26.6 |
| long 镜落地构成 | 枪 465 · 坦克 11 · 激光剑 2 · 狙击 1 | **枪 439（+ 终局收缩 22）** | **枪 467（一种）** | 枪 391 · **激光剑 66** · 激光眼 1 · 狙击 1 |

**（1）预注册主判判负（走向 ③）**：两对种子里长程臂的"脚本池最弱格"都**不高于**它的 multi 孪生（7.5 vs 12.5、5.0 vs 7.5）
⇒ **"训 long"这条路按现配方不成立**。且四臂全都 ≤ 现役包的 12.5% ⇒ 这条配方（100 代 × pop8 × 每代 8 局 · XN2W=1 · KILL_FIELD=0.125）**在跨模式鲁棒性上没有产出任何增益** —— 这是比"long 不行"更该记住的一条。

**（2）我撤回一句自己写下的错话（本条存在的意义）**：只看 seed 31 时我读到"长程臂各格 `G(出手)` 只有 1.00~2.41、净兑现 1 种、平均回合拖到 49.9"，
就在 CHANGELOG 里写了"**长程模式的最优解就是便宜卡反复蹭 + 拖到收缩**"。**seed 32 直接否掉**：同一模式那臂是今晚**最宽**的一粒（`G(出手) 5.52~6.74`、净兑现 2~3 种、激光剑真打上血 66 次），
而**它的 multi 孪生臂才塌成"只有枪打上血"**（净兑现 `1.00（1 种）`）。⇒ **"广度塌缩"是臂级（种子）方差，不是模式属性**。
CHANGELOG v1.5.169 已就地改成撤回 + 更正的写法（不留错话在仓里，也不删痕迹）。
顺带把另一句也降级：期末考卷与广度**不同向、但也不是系统性反向**（seed 31 长程臂考卷更高更窄；seed 32 长程臂考卷更高也更宽）⇒ "考卷偏爱窄包"记成**未定**。
**教训（今晚第二次，第一次是 §N25 的污染口径）：一臂一线索不叫发现，叫噪声的一个样本。**

**（3）问题因此换掉了**：不是"long 还是 multi"，而是"**为什么同一配方会有一半的臂塌成一张卡**"。
两对四种子里 4 粒冠军里有 **3 粒**净兑现只剩 1 种（`xn17a`/`xn15f` 明确，`xn15c` 也只有 4 种且枪占 465/479）⇒ **塌缩是常态，宽的是意外**。
⇒ §N29 提案（下一步做）：把广度当**准入线**（净 `G(落地)` 与兑现率的地板）而不是当排序键 —— 排序键已经在 §N25 被证明既咬不动也咬不对。

**（4）好消息**：`meta.recipe` 第一次自己跑通 —— `v7xn17a-31.bak` 里直接读得出
`"trainMode":"long","trainModeEffective":"long","kill":{"req":0.125,"fired":516,"seats":["0","1","2","3","4"]}`；长程臂注入覆盖了 5 个受评座位（multi 那两臂只有 4 个）。

### D-9 02:2x · §N29 广度准入线（`EPIRUS_BREADTH_FLOOR` + `rejectNarrowWinners` + 门 D128/D129）第一臂就给出**走向②**
线画在"净 `G(落地) ≥ 1.5` **且** `landedKeys ≥ 2`"（标定见函数头：现役 `2.66（3 种）` · 2P 槽 `2.98` · 塌缩臂 `1.00（1 种）` · 今晚最宽那粒 `1.75`）。
臂 `v7xn19a`（= `xn15c` 同配方只开这条线）读数：
```
[广度线] 各粒：1.24(3种,兑现52%) 1.21(2种,兑现24%) 1.21(3种,兑现26%) 1.00(1种,兑现21%) 1.00(1种,兑现30%) 1.00(1种,兑现20%)
[广度线] 剔除 6/6 粒塌缩候选
⛔ [广度线] 名人堂**全部塌缩** …（走向②：该回去改奖励面）
```
⇒ **本配方（100 代 × pop8 × 每代 8 局 · XN2W=1 · KILL_FIELD=0.125 · 冷启动）根本产不出过线的包** —— 这不是"线画高了"，是"整池都在 1.0~1.24"。
机制侧同时确认：全不合格时**没有静默退回**（产物照写、`meta.breadthFloorAllNarrow=true`、stderr 响亮），这正是 D128 里"判产物不判打印"那一格要吃的情形。

### D-10 02:4x · §N30 冷/热 × 有/无收割席 四臂：**塌缩来自配方规模，不来自模式、也不来自收割席**
| 臂 | 变量 | 冠军权重哈希 | 期末考卷（臂内打印） | 同尺横量考卷 | 净 `G(落地)` |
|---|---|---|---|---|---|
| `xn15c` | 冷 · kill 开 · multi | `a8acc0f1ab`（新） | `1st 40.1%` | 40.1% | 1.24（3 种） |
| `xn19a` | 同上 + 广度线 | `a8acc0f1ab`（同 ⇒ 线没换人，只是报全塌） | `1st 40.1%` | 40.1% | 1.24（3 种） |
| `xn20a` | **热启动** · kill 开 · s31 | **`cb42add78f` = 现役本体** | `1st 47.8%` | 47.8% | 2.66（3 种） |
| `xn20b` | 热启动 · kill 开 · s32 | `c31e30790b`（新） | `1st 38.1%` | 37.4% | 1.23（2 种） |
| `xn21a` | 热启动 · **kill 关** · s31 | `3363d6a75b`（新） | `1st 44.7%` | 44.7% | 2.29（3 种） |
（最后两列都由 `C:\Users\14765\_tmp_scan.mjs` 那把**同一 rng 起点**横量，20 个包同条件 ⇒ 可比；臂内打印列另算。）

**三条结论（每条都是一个变量、不是一句话）**：
1. **`xn20a` 是"§N8 病"的现行犯**：热启动 + 100 代 ⇒ 冠军**逐字节就是种子包**（权重哈希相同）。这臂什么都没学到，但表面上"考卷 47.8% 最高"——
   ⇒ 以后凡是热启动臂，**必须先比权重哈希**再谈读数（我已把这条写进 D-10 的做法；产物 `meta.recipe` 现在自带 arm/seed/模式，就差一个"与种子是否同权重"的比对，见 NEXT）。
2. **关掉收割席注入（`xn21a`）后，广度回来了**：净 `G(落地) 1.24 → 2.29（3 种）`，考卷还高 4.6pt ⇒ 这是 **Q-6 的正面证据**：
   `EPIRUS_KILL_FIELD`（§N13 为买场B 加的对手池压力）**在冷启动小配方里是压广度的主要来源之一**。但它单独不足以到过线（`xn19a` 冷+kill=1.24，`xn21a` 热+无kill=2.29，**两个变量同时动**）⇒ 只能说"方向对上"，不能说"就是它"。**要一次只动一个变量的复现（`冷 × kill 关`）才算结案**，已排进 NEXT。
3. **模式与塌缩无关**（§N28 的更正）：长程/multi 两对种子里塌缩都出现 ⇒ 前面那句"广度塌缩是臂级方差"仍然成立，但**"臂级"这一层现在能拆**：它跟"冷启动 + 小配方 + 收割席压力"绑定，不是掷骰子。

### D-11 02:5x · §N31 历史 20 包横量：**考卷与广度是弱正相关（r=+0.234），我之前那句"考卷偏爱窄包"没有依据**
一次性把 9 粒历史产线包 + 2P 槽 + 今晚 9 粒臂冠军放进**同一把尺**（`evalN` 36 对 × 20 局 × n=5，rng 起点固定）横量：

- **产线包全都是宽的**：`v7cmin4-82 净落地G 3.80（4 种）`、2P 槽 `2.98`、现役 `2.66`、`v7cmin3-31 2.39（4 种）`、`v7densE-31 2.39（4 种）`、`v7divK-31 2.15`
  ⇒ **"这条管线产不出宽包"是错的**；塌缩集中在**今晚这种 100 代小配方**（`xn15*`/`xn17*`/`xn19a` 全在 1.00~1.24）与一粒历史例外 `v7densE-82 1.32`。
- **Pearson r(考卷, 净兑现G) = +0.234（n=20）** ⇒ 考卷与广度**不同向也不反向**，就是**弱**。
  所以我上一条（§N28 里"期末考卷抓不到广度塌"）**要再降一级**：正确说法是"考卷对广度几乎没有判别力（|r|≈0.23），所以既不能用它证明广度、也不能用它的偏好来立论"。
  仓里那条老话仍然对：**要判广度必须由看得见的量具出数**（`probe-cast-vs-land` / 净 `G(落地)` / 通吃矩阵），只是不要把"考卷系统性偏爱窄包"当成实测结论——它不是。
- **意外收获（给用户的一条候选）**：`v7cmin4-82` 净兑现 `3.80（4 种）` 比现役 `2.66` **高 43%**，同尺考卷只低 6.1pt（41.7% vs 47.8%）；
  `v7cmin3-31` 同尺考卷 **50.4%（比现役高）**且净兑现 2.39（4 种）。⇒ "想要更宽"不一定要重训，**先把历史池子拉回来量**（这条正是通吃矩阵该干的活）。
  ⚠️ 我没量这两粒的五道可行性/退化闸，**不构成"可以换包"**，只是"值得量"。

### D-12 03:2x · §N32 **付了钱的广度买到了什么**：`xn23a`（线关）vs `xn23b`（线 1.5）逐字同配方单变量
同一池子（冷启动 · 无收割席 · seed 31 · 100 代）：

| 臂 | 当选 | 净 `G(落地)` | 考卷 1st | 矩阵·脚本池 最弱格 | 各格 1st（2P/3P/4P/5P/长程） |
|---|---|---|---|---|---|
| `xn23a`（线关） | `band3` `e75c28f046` | **1.00（1 种）** | **40.6%** | 4P **10.0%** | 80.0 / 12.5 / 10.0 / 12.5 / 20.0 |
| `xn23b`（线 1.5） | `band6` `9f45dacf8e` | **2.39（3 种）** | 28.6% | 长程 **0.0%** | 47.5(平33) / 10.0 / 17.5 / **7.5** / **0.0** |
| 现役（对照） | — | 2.66（3 种） | 47.8%（同尺） | 长程 12.5% | 32.5(平35) / 27.5 / 32.5 / 17.5 / 12.5 |

线在 `xn23b` 里报的是 `剔除 4/6 粒塌缩候选 ⇒ 池内冠军 换成 2.39(3种)` —— 机器完全按预注册在动（产物 `meta.recipe.breadthFloor` 自带 `dropped:4/of:6/winnerLandG:2.39/winnerKeys:3`，D128 判的就是这格）。

**结论（这条链现在闭合了）**：**付 12pt 考卷分确实买到了广度，但没买到鲁棒性** —— `xn23b` 的冠军在 5P/长程上比 `xn23a` 更差（7.5% / 0.0%），
而两臂**全都明显低于现役包**（最弱格 10.0/0.0 vs 12.5，考卷 40.6/28.6 vs 47.8）。
⇒ **当选面（无论排序键还是准入线）只能从池子里挑，挑不出池子里没有的东西**。今晚 4 个配方尺度的池子里最好的宽候选也只有 `2.76（3 种）/ 考卷 27.9%`，
而现役包是 `2.66 / 47.8%` ⇒ **瓶颈在"池子怎么长出来"（奖励面 + 预算），不在"当选怎么挑"**。
这条否证很重要，因为它同时砍掉了我自己前面两条路（§N24/§N25 排序键、§N29 准入线）在**小配方**上的全部收益预期。
剩下唯一没测的杠杆是**预算/血统**（`xn22a`：400 代 × pop16 × 每代 12 局 + 热启动 + 无收割席），它跑完就能判："现役这个层级，重训能不能追上或超过"。
⇒ 广度线的定位随之改写：**留着当"别让池子里出现单卡包"的报警器（只记录）**，不当换冠军的凭据。要不要进 `promote` 变成判据 = **Q-7，等用户裁**。

## 预注册区（跑臂前写死，之后不许改判据）

### §N25-L1c（已结案，见 D-5(c)）
净口径重测同一批候选 ⇒ 原 L1 的"改判"是污染口径的产物。**不重跑臂**，因为池子排名已经用同一批 `.bak` 直接重算过了（比重跑更省、且逐位可比）。

### §N25-L1d 臂 `v7xn15c` / `v7xn15d`（single variable = `EPIRUS_SEL_LAND`）
- 配方：`train-3p 100 5 8 8`（GENS=100 · N=5 · GAMES=8 · POP=8）· `EPIRUS_SEED=31` · `EPIRUS_XN2W=1` · `EPIRUS_KILL_FIELD=0.125` · **净口径** · 预筛恒开。
- 唯一变量：`c`=SEL_LAND 关（基线孪生）/ `d`=SEL_LAND 开。
- 判据（读表用，不设阈值）：① `d` 的 `[兑现广度]` 行有没有"改判"；② 两臂冠军的通吃矩阵最弱格；③ 两臂冠军的 `G(出手→落地)`。
- **读数（已跑完）**：`c` 冠军 `band5`（trainFit 0.828 → 重验 `1st=40.1% top2=61.9%`），`d` 报 `同分带 1/6 · 未改判（池与排序键都没换人）`、冠军**同一粒**、开火计数两臂都是 `注入 512 局 · 覆盖受评座位 4 个 [0,1,2,4]`。
  ⇒ **净口径下这个开关在本配方上根本没得咬**（带里只有 1 粒：本臂 6 粒的胜负分差距 > tol=0.03）。这是"排序键默认关且咬不动"的第三种结局，比 L1 的"咬错"更中性 ⇒ 保持默认关，**下次别急着再加排序键**，先问"带里到底有几粒"。
  另记：本配方（收割席注入开）训出来的 6 粒**普遍很窄**：出手 G 1.10~2.73、兑现 G 1.00~1.24 ⇒ 收割席压力在买"敢下手"的同时**压掉了广度**，这条与 §N13 的"场B 0.33→0.47"是同一枚硬币的两面，**待裁（Q-6）**。

### §N28-L2 臂 `v7xn17a`（**第一条长程臂**）
- 配方：与 `xn15c` **逐字相同**（seed 31 · 100 代 · n=5 · games 8 · pop 8 · XN2W=1 · KILL_FIELD=0.125 · SEL_LAND 关），唯一变量 = `EPIRUS_TRAIN_MODE=long`。
- 判据（跑之前写死）：主判 = **通吃矩阵 `脚本池` 环境的最弱格 1st**（对照 `xn15c` 同表同种子）；副判 = 长程格 1st、`G(出手→落地)`、`promote` 五道（只读，不换槽）。
- 走向：① 最弱格 ≥ 孪生臂 且 长程格更好 ⇒ "训长程"是有价值的方向，向用户提案"把 long 折进适应度/或双模式适应度"（**判据级，要用户裁定**）；
  ② 长程更好但最弱格更差 ⇒ 报"通吃不成立"，提双模式适应度；③ 两头都更差 ⇒ 负结果入档，并检查是不是 `HEALTH`/`economyStock` 在 long 下换了尺（那是另一条线）。

### §N29-L3 广度当**准入线**（`EPIRUS_BREADTH_FLOOR`，默认关）—— 由 §N28 的"塌缩是常态"逼出来的
- **问题**（§N28 实测，不是猜）：两对种子 4 粒冠军里有 **3 粒**净兑现只剩 1 种卡 ⇒ **塌成一张卡是常态，宽的才是意外**。
  §N25 已经否掉"广度当排序键"（同分带里只有 1 粒 ⇒ 咬不动；咬动了也是坏包）。剩下的唯一姿势是**当线**：不达标 = 不合格，达标者之间仍按胜负分排。
- **配方**：与 `xn15c` 同（seed 31 · 100 代 · n5 · 每代 8 局 · pop 8 · XN2W=1 · KILL_FIELD=0.125 · 训 multi），唯一变量 = `EPIRUS_BREADTH_FLOOR`。
- **线（跑前写死，且必须**说清是草案待用户裁**）**：`mirrorHealth(20,5,'multi').effSkillsLand ≥ 2.0` **且** `landedTotal/nonJi ≥ 0.35`（现役包 = `2.66 / 0.52`，塌缩臂 = `1.00~1.24 / 0.13~0.44`）。
  现役自己必须过线（否则这条线是在砍现役，不是砍塌缩）。
- **判据**：主判 = 矩阵·脚本池 最弱格（对照 `xn15c` 的 12.5%）；次判 = 冠军净 `G(落地)` 与兑现率、期末考卷（**考卷不许变差超过 3pt**，防止"拿胜负换广度"这条 §N25 已否的路走回来）。
- **走向**：① 过线的包同时守住考卷 ⇒ 有资格向用户提案"把广度线并进 `promote` 的准入（只记录 or 阻断要用户点）"；
  ② 全池过不了线 ⇒ 报"这条配方无论怎么选都塌"，那就得回去改**奖励面**（`EPIRUS_*` 里贵卡/多样性那几条），不是继续加排序键；
  ③ 过线但考卷掉 >3pt ⇒ 与 §N25 同型（广度与胜负不能白手换），负结果入档。

## 待裁（不擅自动）
- **Q-1**：作废/被拒的一手要不要进 v7 关系特征 T 块（现状=要，D26 钉着）。
- **Q-2**：经济杠杆 —— `光垫钱` 在 long 下 +41~46pt ⇒ "广"被钱卡住。要不要动 `ep` 收入/贵卡费用？这是**规则/经济级**改动，超出训练侧。
- **Q-3**：`7′`/`v7xn14a-band2` 作为**第三个可导入包**（通吃矩阵两栏都指回它）；换槽/加槽都要用户点。
- **Q-4**：ε 预算重设计（前台 soft 探索的三条规则见 D111/D115）。
- **Q-5**：把 long 的 G 折进 `train-server` 体检的**主判**（现在只是第四道并排读数）。
- **Q-6**（新）：收割席注入（`KILL_FIELD=0.125`）在 xn15c/d 上普遍**压窄广度**（出手 G ≤2.73）——要不要把它从"常开配方"降级成"专门买场B 的那一臂专用"？
  §N30 补了一条同向证据：`xn19a`（冷 + kill 开 + 开线）= **全池 6/6 塌缩**，而 `xn23a`（冷 + kill 关）池子里第一次出现 `2.76（3 种）` 的宽候选 ⇒ 方向对上，但那一臂同时动了启动方式，**尚未结案**。
- **Q-7**（新，来自 §N29/§N32）：广度准入线 `净 G(落地) ≥ 1.5 且 landedKeys ≥ 2` 要不要进 `promote` 当判据？
  **我的建议是"只当报警器、先只记录"**：§N32 实测付 12pt 考卷分换广度**没有换到鲁棒性**（`xn23b` 最弱格 0.0% < `xn23a` 10.0% < 现役 12.5%）
  ⇒ 在小配方上把它当硬线，只会把冠军从"窄而能赢"换成"宽而谁都不赢"。真要推广度，得先有**池子里存在又宽又能赢的包**（等 `xn22a` 判预算这条杠杆）。

## NEXT（早 9 点前）
1. `xn17a` 读数 + 三行矩阵对照（`xn15c` multi 孪生 / `xn17a` long / 现役）⇒ §N28 结案。
2. 若长程臂方向可用：加 `EPIRUS_TRAIN_MODE=multi+long` 的**双模式配对适应度**提案（时间盒：只实现 + smoke，不判据）。
3. 把 `probe-cross-mode` 接进 `promote-champion` 的**只记录栏**（像 v1.5.152 的产品代理栏那样：先只印不阻断）——等用户裁阈值。
4. 早 9 点前：CHANGELOG v1.5.167~169 + README/index 版本三处一致（D8）+ np/spec/smoke 全绿 + 提交推分支 + 早报。
