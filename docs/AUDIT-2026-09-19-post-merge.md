# 复核：v1.5.123 换包后的现状（2026-09-19 下午，第三方复核者）

> 对象：ds 已把 `qoder` 快进合并进 `main`（我那 83 个提交全部在内），随后做到 **v1.5.123** 并**换上了新冠军 `v7press3-91`**。
> 我这一轮做的是：**用夜里建好的量具在同一引擎下复测新旧两包**，并把**用户下午实盘的两局**量化。
> 我这一夜的红线（不换包、不动 `js/bundled-champion-3p.js`、不 bump 版本号）**仍然有效，但只对"我"有效**——换包是 ds/用户做的决定，不是我做的；我没有回退或改动任何已上线的东西。

## 0. 现状核对（实测）
| 项 | 值 |
|---|---|
| HEAD | `2c96a0b` = **v1.5.123**（工作区在 `main`） |
| 线上包 sha1 | **`1fe3a09fe247`**（夜里是 `268461f7c9f0` ⇒ 确已换包） |
| 规则指纹 | **`61a7711c`**（夜里 `1391c094`；中间 `1d5d28a0`）⇒ 由 v1.5.116/118 的规则与特征修复造成，合法 |
| `np-test` | **147 / 148**，唯一红的是 **D67**（要求"线上包 G4 至少一个模式 PASS"）—— 因为这次是 `--force` 越过 G4 上线的；ds **如实记红、没放宽判据** ✔ |
| 我这批工具 | 全部还在（`crowding` / `probe-v2-anatomy` / `l2-e1-check` / `l2-epdepth-axis` / E4 的 `EPIRUS_BLOCK_W` 保留且默认关） |

## 1. 新包 vs 夜里的现役包：**我夜里那条诊断被这次换包正面修正了**
> ⚠ **本节结论已在 §7 被我自己收窄**：这张表全部是"1 席被测"的装配，而产品是"满桌同包"。先看 §7 再引用本节。

同一引擎、同一量具、`80` 局轮座、对手恒为 `pickBalanced`（= 我命名 V2 的那根"打整局"轴）：

| 读数 | 新冠军 `v7press3-91` | 旧冠军 `v7new5_005-31` | 读法 |
|---|---|---|---|
| V1 打乱局（vs 4 `random`） | 83.8（**4.19×** 公平份额） | 57.5（3.65×） | 两包都强，新包更甚 |
| **V2 打整局（vs 4 `balanced`）** | **36.3（1.84×）** | **20.0（1.04×）** | **旧包≈随机席位；新包第一次真正打过整局** |
| V4 成群打整局 | 17.8 | 17.8 | 无差别 |
| **击杀归属**（公平 20%） | **19%** | **11%** | 新包的胜果更"自己动手"，旧包靠对手互杀捡人头 |
| 伤害归属 | 21% | 16% | 同向 |
| ジ（基础动作）占比 | 62% | 61% | **完全没改善**（下面第 3 节真人局再验一次） |
| 防御族占比 | **2%** | 20% | 新包几乎不防守 ⇒ 与"被 G4 只枪线克制"同向，值得盯 |
| 该装配内出手种类 exp(熵) | 3.30 | 3.94 | ⚠ 与发布说明的 `G 8.01` **不矛盾**（那是自对局装配）——但说明"宽"是**装配相关**的 |
| 平均回合 | 33.5 | 33.6 | 没靠拖哨声赢 |

⇒ **夜里 §16/§28 那句话（"现役包在打整局这一维 = 0.88~1.04 倍公平份额，把它换成随机席位那个场反而更好打"）现在被换包解决了**：1.04× → **1.84×**。
⇒ 这条也是那套新量具（`crowding.mjs` 的 V2 + `probe-v2-anatomy.mjs` 的击杀归属）**第一次被证明有预测力**，不只是描述力。

## 2. 我夜里数字的**失效面**（诚实声明，别照抄）
> ✅ 这一节下面列的三条**都已经在 §5/§6/§8/§9 重跑完了**，保留原文是为了记"哪些当时是悬着的"。

所有夜间数字都是在 `1391c094` 引擎上测的；之后规则/特征变了两次（`1d5d28a0` → `61a7711c`）。
- 直接后果的实例：旧包在 V2 上从夜里的 **17.5** 变成今天的 **20.0**（换了引擎，包没换）。
- 尤其需要重跑的一条：**§12「AI 的 ep 条件性几乎为零」是在 v1.5.116 修好死特征之前测的**——
  那次修的是"目标带架势"那一维恒为 `null`（AI 看不见谁会挡它）。它不改 ep 编码，所以 §12 的主结论大概率仍成立，
  但**没重跑过就别引用**。同理 §27 那张 ep 深度×轴的相关表也是在旧引擎上算的。

## 3. 用户实盘两局（`results/`，下午 13:45/13:48）——第一次有**真人打法**样本
新工具 `tools/parse-battle-log.mjs`（只读 `results/`，不改任何文件）：

| | 局 1（23 回合，**你赢**） | 局 2（34 回合，你第 32 回合阵亡，AI2 胜） |
|---|---|---|
| 攻击声明归属 | 真人→AI 6 · AI→真人 6 · **AI→AI 15** | 真人→AI 9 · AI→真人 11 · **AI→AI 20** |
| 承伤合计 | 真人 6 / AI 各 5（**很平均**） | 真人 8 / AI 4~5 |
| ≥3 席集火 | 1 次：**第 5 回合 4 个 AI 全部狙击正在攒 ep 的你** | 1 次：3 个 AI 集火 **AI4**（也在攒 ep 的那席） |
| AI 相抵轮 | 2 次 | **5 次**（AI2×AI3 在 15/20/23/26 回合反复互狙互相抵消） |
| ジ占比 | 真人 52% · AI 52~65% | 真人 38% · AI 53~62% |
| 出手种类 | 真人 5 · AI 4~7 | 真人 **10** · AI 5~8 |

**三条从这两局里读出来的事实：**
1. **"AI 没在跟我打"是真的，而且可量化**：两局里 AI 投向**自己人**的攻击（15、20 次）是投向你的（6、11 次）的 **2~2.5 倍**。
   加上局 2 有 5 个回合是两条 AI 线**互相抵消**（谁也没打到，纯空转）——这是最直接的"手感"损耗。
2. **"突然集火"这次不是 bug 而是新能力**：两局各自的唯一一次集火，目标都是**当时在攒 ep 的那一席**（局 1 是你，局 2 是 AI4）。
   ⇒ 说明 v1.5.116 修的那个"AI 看不见谁会挡它/谁在攒钱"的死特征**确实生效了**，行为从"乱狙"变成了"打断攒钱的人"。
   ⚠ 但它只在你/它**明显攒钱**时才触发（第 4~5 回合），之后仍然退回互相消耗。
3. **"AI 只会拿基础技能磨"这条没变**：新包 62% 的出手是 ジ，与旧包 61% 一样；而真人局里你用了 10 种招、它用 5~8 种。
   ⇒ 所以发布说明里的 `G 8.01` 是**自对局装配**的数字，真人坐在桌边感知的宽度比它低。**这是装配差异不是造假**，但验收应该拿"混真人装配"也测一遍。

## 4. ~~建议加的两栏门~~（**用户已裁定撤回，2026-09-19 下午**）
我原本据两局真人局建议加两栏门：`AI→AI 攻击占比 ≤50%` 与 `相抵空转率`。**用户否掉了这两条**，理由是
"AI 应该把其他对手都当成同样的情况，相抵空转也是很正常的，**只要不导致什么很差的后果**"。
⇒ 这个裁定我接受，并且它把问题从"**数动作投向谁**"挪到了"**量它导致没导致坏后果**"——后者才是判据该有的形状：
一条不该把"AI 打 AI"本身当病灶的门，而应该问"同族互杀有没有把席位优势抹平"。
**这条我照办了，结果在 §7**（用已有的拥挤指数与 V4 装配量的，不用新装配、不用新门）。
保留的一条（与上面无关，且已被实测支持）：**别再走"加防御奖励"**——v1.5.122 已实测否证
（`BLOCK_W=0.2` 反而更差：G4 90%/70%，且被"只防御"线打 96%），机制正是我夜里 §5/§23 警告的"推向龟壳 ⇒ 掉进被只枪/只防御吃的吸引子"。**这条已经死透。**

## 5. ★★ 用户要求的"表重跑一遍"——两张夜里的关键表在新引擎（`61a7711c`）上的结果
命令：`bash tools/rerun-postmerge.sh`（90 个包 = 夜里 53 个 `v7l2*` + ds 的 36 个 `v7press/v7fix1/v7noteach` + 旧线上包 `.bak` + 新冠军；
五步：`probe-leftover` / `crowding V1` / `crowding V2·V4` / `champ-audit` / 考卷 @120，全部 80 局、同 seed）。
两份矩阵都已入库：`docs/l2-eval-matrix-engine-1391c094.tsv`（夜里那份）与 `docs/l2-eval-matrix-engine-61a7711c.tsv`（这份）。

### 5a. §27「ep 深度只买 V1 轴」——**我撤回这条收窄**（今夜"结论对、口径错"的第 7 次）
夜里 n=48（旧引擎）算出 `峰 ep × V1 = +0.35`，我据此把"ep 深度与强度解耦"**收窄成**"只与打乱局那根轴有关"。
n=90（新引擎）重跑：

| ep 深度指标 | V1 | V2 | V4 | A 卷 | TN | ‖ 同指标（夜里 n=48，旧引擎） | V1 | V2 | V4 | A 卷 | TN |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 峰 ep | +0.06 | −0.02 | −0.11 | −0.03 | −0.08 | ‖ | **+0.35** | −0.07 | −0.12 | +0.16 | −0.15 |
| ep≥3 占比 | +0.12 | −0.14 | −0.01 | −0.13 | −0.23 | ‖ | +0.35 | −0.08 | −0.07 | +0.12 | −0.09 |
| 环可负担 | +0.13 | −0.13 | −0.01 | −0.12 | −0.23 | ‖ | +0.35 | −0.08 | −0.06 | +0.11 | −0.09 |
| 兑现率 | +0.01 | −0.01 | −0.07 | +0.08 | +0.06 | ‖ | −0.21 | +0.02 | −0.10 | −0.04 | +0.05 |
| 终局余款 | +0.03 | −0.07 | −0.03 | −0.07 | −0.12 | ‖ | +0.30 | −0.09 | +0.02 | +0.07 | −0.18 |

分组（峰 ep 二分）：夜里"浅 33 包 / 深 11 包"差 **V1 +22.4pt**；现在"浅 51 / 深 32"差 **V1 +10.4pt**，
其余四列 −4.2 / +0.2 / −5.1 / −7.1。
⇒ **那条 +0.35 撑不住**：包数从 48 涨到 90（而且新人群里补进了 ds 那 36 个包）之后掉到 +0.06，
深-浅组差也从 +22.4 缩到 +10.4。**收回原话：ep 深度与强度在五把尺子上都没有载重（全表 |r|≤0.14）。**
⇒ 教训的形状和前六次**一模一样**：拿一次样本上的相关去下一个普遍结论。夜里那个 +0.35 只有 11 个"深包"撑着，
   而我当时把 11 个包的分组差当成了规律。

### 5b. §12「AI 的 ep 条件性几乎为零」——**对旧包逐位复现，对新冠军已经不成立**
`node tools/probe-ep-saturation.mjs EP_PROBE_BAK=docs/artifacts/v7new5_005-31.bak`（同一引擎、同一量具、并排两包）：

| 读数（12 个真局快照，只改 ep） | 旧包 `v7new5_005-31` | **新冠军 `v7press3-91`** |
|---|---|---|
| p(ジ) @ep=2 → ep=8 → ep≥13 | 0.000 → 0.000 → 0.000 | 0.417 → **0.000** → 0.000 |
| argmax @ep=2 | `{snipe:12}` | `{ji:5, guard:2, snipe:4, sword:1}` |
| argmax @ep=8 | `{snipe:12}` | `{guard:7, sword:2, snipe:1, cannon:1, **ring:1**}` |
| argmax @ep=16/20/30/40 | `{snipe:12}` | `{shift:6, sword:3, guard:1, rod:1, bigT:1}` **四档一字不变** |
| 2→40 全程 p 变化幅度 | **0.003** | 0.417 → 0（**结构性反应**） |

三条：
1. **§12 对旧包是逐位正确的**（旧包那行现在仍复现 `p(ジ)=0.000 全档`、`argmax 12/12 狙击`）
   ⇒ 夜里的读数没被 v1.5.116 的死特征修复推翻，**但它是"旧包的性格"，不是"表征层的天花板"**。
2. **新冠军第一次真的条件于 ep**。⚠ 我把混淆排掉了再下这句：ジ 与防御族 **成本都是 0**（`GUARD_FAMILY` 免费），
   所以"ep 高了就不打ジ、改摆架势"**不能用"候选集变宽/买得起的东西变了"解释**——那是字面的 ep 条件性。
3. **但 ep≥13 的编码夹住完全没变**（16/20/30/40 四档逐位相同）⇒ L1 那条"抬编码上限"的价值主张要改写：
   夜里我写"现役包换上新编码也没有东西可解锁"——对旧包成立；对新包，它**已经有了会被夹住的条件性**（它的反应全在 ep≤8 这一段）。

顺带（同一个量具 `probe-leftover` 30 局，multi）：新冠军 **峰 ep 8 · 环可负担 32.47% · 打环 346 次（346 次全部实际执行，不是被换成ジ的原始输出）**；
旧包 峰 ep 2 · 环可负担 **0.00%** · 打环 **0**。夜里全部 53 个包都是 0。
⇒ **所以"环这条线"的前提变了**：我 §27 写的"投入产出对不上，因为 AI 跨不过 3"这句话**事实前提已经不存在了**。
   它到底值不值，见 §6 那一枪（比相关强得多的干预）。

## 6. ★★ 新实验：**环消融**（第一次能做减法，因为终于有个包自己打环）
`node tools/probe-ring-ablate.mjs 120`——同一 seed 序列、同一轮座、同一对手装配，只把 chooser 输出的**环换成 ジ**，逐局配对；
判据用符号翻转精确检验（`tools/probe-ring-ablate.mjs` 里用 DP 枚举，不受 2^n 溢出限制）。

| 装配 | 基线 | 禁环后 | 差 | 非零配对 | p(双尾) | 该装配里环出手 |
|---|---|---|---|---|---|---|
| V1（1 席 vs 4 random） | 82.5% | 82.5% | **0.0pt** | 0/120 | 1.00 | 86 次 |
| V2（1 席 vs 4 balanced） | 34.2% | 35.8% | **+1.7pt** | 6/120 | 0.69 | 105 次 |
| V4（4 席同族 vs 1 balanced） | 17.3%/席 | 18.3%/席 | **+1.0pt** | 23/120 | 0.41 | 186 次 |

**阳性对照**（同一工具、同一局数，把狙击换成 ジ；防"量具死了"）：

| 装配 | 基线 | 禁狙击后 | 差 | 非零配对 | p(双尾) |
|---|---|---|---|---|---|
| V1 | 82.5% | **53.3%** | **−29.2pt** | 45/120 | **7.9e-8** |
| V2 | 34.2% | **40.0%** | **+5.8pt** | 7/120 | **0.016** |
| V4 | 17.3%/席 | 16.0%/席 | −1.3pt | 42/120 | 0.44 |

⇒ 四条，按可信度排：
1. **量具是活的**：禁狙击在 V1 上 −29.2pt（p=8e-8）。所以上面"禁环 0.0/+1.7/+1.0pt"不是探针没生效。
2. **环对这个包换不来分**：三根轴差值 0.0 / +1.7 / +1.0，全部 p>0.4，且两个非零方向都偏"禁了更好"。
   ⇒ 夜里"环这条线不值得再投"的**结论没变**，但**理由换成了更强的一条**：
   以前是"AI 学不会攒钱，所以这条轴用不上"（可以是训练问题）；
   现在是"**已经有一个包自愿攒到 8 点、每局打 11.5 次环，把环全拿掉它的胜率一分不掉**"（这是环境问题）。
3. **它现在打环是"中性习惯"**，不是强度来源 ⇒ 别把 `打环 346` 当成"资源线长回来了"的成绩单去报（我在 §5b 特意先自问自答了这一条）。
4. **意外的新读数**：禁狙击在 **V2 上是 +5.8pt（p=0.016）**——它在"打整局"场里用狙击（235 次/120 局）**净亏**。
   与夜里 §13（A 卷与 targeter 反号、r=−0.57，5/5 臂同号）同一形状，但这次是**干预**不是相关。
   ⇒ 给 ds 的一个具体线索：不是"ep 深度"而是"**狙击在对整局对手时的过度使用**"可能还有 5pt 量级的空间。⚠ n=6 非零对，方向可信、量级别当真。

## 7. ★★★★★ 对我自己 §1 的更正：**"这次换包正面修正了那条诊断"这句说大了**（第 8 次同类）
§1 我写了 `V2 1.04× → 1.84×` 是"第一次真正打过整局"。数字没错，**但那是"1 席冠军 + 4 席异族"装配的读数，而产品不是这个装配**：
线下 4~7 人局里**所有 AI 席共用同一个包**。用已在库的拥挤度数据把两种装配并排（新引擎、80 局）：

| 包 | V2 = 1 席 vs 4 balanced | **V4 = 4 席同族 vs 1 balanced（每席）** | 拥挤指数(k=4÷k=1) | V1 = 1 席 vs 4 random |
|---|---|---|---|---|
| 新冠军 `v7press3-91` | **36.3**（1.84× 公平） | **17.8** | **0.49** | 83.8（4.19×） |
| 旧冠军 `v7new5_005-31` | 20.0（1.04×） | **17.8** | 0.89 | 57.5（3.65×） |

⇒ **满桌同包时两个包席位成绩一模一样（17.8 vs 17.8，都低于公平 19.8%）**；换包的全部收益都在"它是桌上唯一一席冠军"时兑现。
⇒ 机制也量到了：新包的**拥挤指数 0.49**（4 份自己把自己每席的价值砍掉一半），旧包 0.89。
   也就是说它更依赖"桌上只有一个我"——它学到的强度有一部分花在**跟自己的复制体互杀**上。
   （真人局佐证：§3 两局里 AI 投向自己人的攻击是你收到火力的 2~2.5 倍。）
⇒ 头对头别拿来当"新 vs 旧谁强"的排序判据（我跑了，100 局，`docs/artifacts/h2h-new-vs-old.log`）：
   旧包单席进新包场 27.0%（multi）/ 24.0%（long），新包单席进旧包场 66%（multi）/ 69%（long）——
   **这两个数不对称的原因是上面那一列拥挤指数**（4 份新包互杀更狠 ⇒ 反向把新包抬高、正向把旧包抬高），不是谁强 3 倍。
   ⇒ 所以 §1 那句"正面修正了夜里那条诊断"改成：**它修正了"冠军作为少数派时打不过整局对手"这一维；
      在"满桌都是它"这个真实装配里，席位成绩没有变化（17.8 → 17.8），而这一维才是用户坐在桌边感知到的。**

## 8. §28a「宽度不收费」重跑：pooled 全正，但**批内一分为二**——我 §13a 那条纪律这次用在了自己新表上
n=91（新引擎）。⚠ 新加的 `WIDTH_FILTER` 就是 §13a 那两步检查里的"臂内分层"（夜里 53 个 `v7l2*` 与 ds 的 36 个包不同代，
混在一起算 r 可能只是"世代效应"）：

| r(G, ·) | **全体 n=91** | 夜里批 `^v7l2` n=53 | ds 批 `v7press\|v7fix1\|v7noteach` n=36 | ‖ 夜里（n=42，旧引擎） |
|---|---|---|---|---|
| V1 打乱局 | +0.09 | **+0.25** | **−0.17** | +0.26 |
| V2 打整局 | +0.21 | −0.06 | **+0.42** | +0.04 |
| V4 成群 | +0.31 | +0.19 | **+0.42** | +0.12 |
| A 卷 | +0.18 | +0.21 | +0.12 | +0.17 |
| targeter | +0.02 | −0.11 | +0.24 | −0.01 |

⇒ **V1 那一列是批级假象**：两批里符号相反（+0.25 / −0.17），pooled 的 +0.09 什么都不说。以后别再引用它。
⇒ **V2/V4 那一列是真的**：ds 批内 +0.42/+0.42（n=36），夜里批 −0.06/+0.19 ⇒ 至少没有一批是**负**的。
⇒ 分组均值（全体）：宽 G≥6（n=8）V1 72.0 / V2 28.6 / V4 18.4 / A 卷 38.1；最窄 G<3（n=36）67.9 / 15.8 / 11.3 / 31.2。
⇒ **所以用户那句"略微降低强度换宽度"里的"降低强度"这半边仍然没有证据**（找不到"宽度要拿强度换"的负号），
   而 §28a 的处方不变：**问题不是愿不愿意付强度，是目标函数里根本没有宽度这根梯度**（`DIV_W=0.06` 在胜率项前无梯度；
   `G≥3` 是事后砍窄包，不是让种群变宽）。
⚠ 一条与发布说明有关的实测：**同引擎下旧包的 G=8.05（10 种）比新冠军的 G=8.01（14 种）还略高**。
   ⇒ "G 8.01"不能当"史上更宽"的证据（它只说明**跨过了 3 这个门槛**）；真正变的是**用到的种类数 10→14**。
   这不是造假，但引用时要把 G 和种数分开说，否则下一轮会拿一个不升反降的量去证明"变宽了"。

## 9. 这次重跑**没有**覆盖什么（别照抄）
夜里以下几条仍测于 `1391c094`，我没有重跑（重跑要重新训练那些臂，不是跑个量具的事）：
§14/§15/§16a 的**头对头两向**（`h2h-sweep.log`，01:35 生成）、§16 的七尺子相关矩阵里的 H/HR 列、§17 的臂对照。
**重跑了并且活下来的**：§25 的配对干预（`node tools/l2-e1-check.mjs f c` + 新矩阵 ⇒ V2 **+7.3pt p(双)=0.027**、V4 **+5.4pt p=0.015**、V1 **−28.3pt p=0.010**、A 卷 −5.9pt）
——**这是今夜唯一在换引擎后逐位复现的干预结论**，也是我认为最该传给 ds 的一条。
顺带修了量具三个问题：`l2-e1-check` 的 p 值夜里印成 0.9948 是因为它**只算单尾**（V1 是显著为负，不是不显著）——
现在同印 `p(单尾)/p(双尾)`；`probe-ep-saturation` 的包名是**硬编码**的 `v7new5_005-31`，换包后会指着错的包读，已改成 payload sha8；
`probe-leftover` 的"打环"以前只数 chooser 原始输出，现在同印"实际执行"次数（这次两包都是 raw==执行，所以夜里的 0 不是口径造成的）。

## 10. 一句话给早上之后的我/ds
换包**确实动了我夜里指出的那块短板**（V2 1.04× → 1.84×、击杀归属 11% → 19%、ep 条件性从"没有"变成"看得见"），
但 §7 那条更正请一起读：**在"满桌都是它"这个真实装配里席位成绩 17.8 → 17.8 没动**，
而 §6 那一枪（自愿打环、禁环后一分不掉，同一工具禁狙击掉 29pt）说明**环这条线该停的不是"没教会"，是"教会了也不值钱"**。
