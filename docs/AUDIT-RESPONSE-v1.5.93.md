# 第三方复核 · 第九轮：`v1.5.86–v1.5.93` 夜间自主探索的结果评估
对象：`D:\code\Epirus-Web` @ **v1.5.93**（HEAD `e0d3c18`，2026-09-17 00:25；线上包 sha1 `e4b4f780dda2` **未变**）
承接：`AUDIT-RESPONSE-v1.5.85.md`（第八轮）· 被复核材料：`CHANGELOG.md` v1.5.86–93 + 附录 D–H + `HANDOFF-2026-09-16.md` §5/§6/§9
标注：`[实测]` 我或我的只读 agent 跑出来的数 · `[代码事实]` 逐行读到 · `[推断]` 解释

---

## 0. 结论（一句话各一条）

1. **工程结果（真）**：第八轮那条"env 到不了 worker"他不但修了，还修得比我要求的严 —— 单一真源 + worker 回执 + 门禁 D77，并**重跑了作废的臂**。`[实测]`
2. **科学结果（干净的否定）**：开关真生效之后，多样性臂**全部收窄**（G 1.00~2.98）而 A 升到 46~53% ⇒ **他 §5.6"窄而深是环境最优"第一次拿到真 A/B 支持**；他自己在 9 seed 后把"角色广度 +51% 真赢"撤回成均值假象。`[实测]`
3. **产品结果（负，且恶化）**：整晚 65 个新包**没有一个**降低"对最便宜一张卡的易损性"，最后留在盘上的那批反而是最脆的（只枪格 **97~100%**，在位包 65%）。`[实测]`
4. **新发现的失败模式**：`v7w3c-93` 在 64.5% 的决策点上 ep≥3、**环明明可负担**，却 **0.00% 出手环** ⇒ "环问题"与"原型制御问题"是**同一个病**（价值估计，不是曝光度/枚举/经济门槛）。`[实测]`
5. **今晚没闭环的一处**：最后 20 个 `.bak`（`roleC2`/`role12`/`w3c`/`w3r1`，00:13–00:42）在 CHANGELOG **查无字** ⇒ 本文 §5 直接给出我这边的读数，供他分析时对表。

---

## 1. 状态核实（agent 跑，我采信）`[实测]`

| 项 | 结果 |
|---|---|
| `np-test` | **129 / 129**（末三条 = D81/D70/D71 ✔） |
| `spec-run` / 指纹 | 36/36 · `8dda2677`（与第七、八轮同 ⇒ 核心规则一字未动） |
| `smoke` / `ui-probe` | SMOKE OK · **UI 探针 10/10** |
| 线上包 | sha1 `e4b4f780dda2` **未变**；`.training-live.sha1` 00:34 自检一致（`mtime 00:42` 是 runner 覆写后还原，`git status` 仍干净） |
| 版本号 | `index.html` 唯一串 = **v1.5.93** |
| 一晚上的提交 | v1.5.86→93，含 **4 次纯 docs 自我更正**（`c266cf2`→`9adc22c`→`e0d3c18` 三次改 v1.5.93 同一条目） |

---

## 2. 第八轮四条建议的落地核对（**其中两处他做得比我的建议更对**）`[代码事实]`

| 我建议 | 他的实现 | 我的判定 |
|---|---|---|
| 修开关接线 + 同源守门 | **v1.5.89** `server/econ-env.mjs`（server `:21/:344`、worker `:17/:85` 共用）+ `[econ] worker 生效值` 回执 + **D77** | ✅ 并**优于**我的方案：我让他"在 worker 里补一份读取"，他拒绝并指出"那正是它第二次犯错的方式" ⇒ 改成单一真源。另处理了我没提的边界：空串 ≠ `0`（`:34`）、新旧名优先序（`:47`） |
| 第 6 道门：只枪压制场做 feasibility | **v1.5.90** `densityProfile` + 对手 `gunspam` 进池 + **只记录不阻断**（`audit-lib.mjs:401 DENSITY_BLOCK=false`）+ **D78**"没有已知好一侧就不许上闸" | ✅ **他对我错**：按我原话上闸会在当晚就把在位包判死而无替代。这条纪律该记进 METHODOLOGY |
| 宽度只当门槛不当奖励 | **v1.5.91** S 权威定义（`目标=H+T·S`，按声明 cat 分层、不按伤害加权、只测不奖 **D79**）；**v1.5.92/93** 类间权重 `DIV_CAT_W`→`DIV_ROLE_W`（8 功能角色，`roleOf` 只从声明字段推导，**D81**），默认 0 = 旧口径逐位不变 | ✅ 采纳；但 92/93 仍是"在混合公式里加项"（`evo.js:1077/1081`），我 §4 说的那个"解析 softmax 熵"落点仍未做 |
| 靶向率当能力线 | G6 采纳为**只记录**并说明"四代包含线上全部 3.5~22.5% ⇒ 能力未长出而非某包退化" | ✅ 判读正确 |
| （附带）G4 基线格守卫 | `gate-drafts` 现在多了第三态 **UNRUN**（基线异常时该格"不得当作通过"） | ✅ 我只有"不可判"的措辞，他把它变成了显式状态 —— 本轮 `roleC2-31` 就靠这条避免了假绿 |

---

## 3. 科学结果：真 A/B 之后，"广度不被付钱"从假说变成观察 `[实测]`

`champ-audit --games=40 --exam-games=40`（A 列）+ sse 日志 `healthReject` 原文：

| 臂（seed 31） | A考卷1st | G 有效技能数 | 座位极差 | 健康门裁定 |
|---|---|---|---|---|
| `v7ctrlE-31`（对照·开关真生效） | 46.8% | **1.00（1 种）** | 0pt | `healthReject:["G 有效技能数 1.00 < 3","自对局回合 47.0 > 40"]` |
| `v7catE1-31` | 40.5% | 2.06（4 种） | 34pt | `G < 3` |
| `v7densE-31` | 49.6% | 2.98（6 种） | 30pt | `G < 3` |
| `v7role12-31` | 49.3% | 2.30（6 种） | 28pt | `G < 3` |
| **`v7role1w-31`** | **53.4%** | 2.07（4 种） | **7pt** | `healthReject:["G 有效技能数 2.08 < 3"]` |
| `v7divK2-81`（作废臂的真重跑） | 43.0% | **5.11（11 种）** | 28pt | **无 reject（整晚唯一过健康门）** |

**这张表里最该看的一列是最后两列的矛盾**：`G≥3` 这道健康门，现在**正好在删除 A 最高的包**（53.4% 的 `role1w` 被"G 2.08<3"删掉），而唯一过门的 `divK2-81`（G 5.11）A 只有 43%、且**破防红**（§4）。
⇒ 第八轮我说的"同一个量既当奖励又当门槛 ⇒ 目标不自洽"，**今晚有了实测形状**：对拉发生在门槛与分数之间，不是在种子之间。
⇒ 回答他 HANDOFF **Q2**（G 与破墙反相关是结构还是 seed 噪声）：**是结构性的目标对拉**，因为过门的那一个恰好是破防最弱的一个（`divK2-81` 防席夺冠 55%/50%）。
⇒ 他自己的裁定我认可：`v1.5.93 §6` 把"+51%"降级为"6 seed 的均值假象（4 胜 5 负），只有 A +3.1pt 站得住"，并确认 `G<3` 在 n=5..80 下是真塌缩。

---

## 4. 产品结果：整晚在"最便宜一张卡"上恶化 `[实测]`（`gate-drafts` G4，n=60/格）

| 包 | 只枪(1ジ·打最肥) 长程 | 同 多人 | 最坏格 | G5 破防（防席夺冠） | G6 靶向率 |
|---|---|---|---|---|---|
| **线上 52（在位）** | 65% | 40% | 65% | 0% / 0% ✓ | 24.2% ✗ |
| `v7role1w-31`（A 53.4%） | **90%** | 65% | 90% | 3% / 18% ✓ | ✗ |
| `v7role12-31` | 55% | **27% ✓** | 55% | 17% / 15% ✓ | ✗ |
| `v7roleC2-31` | 67%（**UNRUN**：基线 0%） | 40% | — | 3% / 10% ✓ | ✗ |
| `v7w3c-31` | **100%** | **100%** | 100% | **27%** ✗ / 13% | ✗ |
| **`v7w3r1-31`（最新）** | **100%** | **97%** | 100% | **75% ✗ / 38% ✗** | ✗ |
| `v7divK2-81`（唯一过健康门） | 90% | 72% | 90% | **55% ✗ / 50% ✗** | ✗ |
| 上轮的 `v7f3-94`（参照） | **5%** | 5% | 67%（瞄威胁者） | 0% ✓ | ✗ |

三点：
1. **A 与稳健性反向**：A 最高的 `role1w-31`（53.4%）被一行枪打死 **90%**；在位包 65%。⇒ **"A 不衡量强度"这条第八轮的结论，今晚又涨了 13pt 的反例**。
2. **他留在盘上的最后两个臂（`w3c`/`w3r1`）是整晚最脆的**：`w3r1-31` 同时红 G4(100%/97%) + G5(75%/38%) + 座位 30pt + G 1.34。⇒ **醒来看见 `w3r1` 数字漂亮也别换**（它 A 有 48.3%，正是最会骗人的那种）。
3. **经济出口整晚没动**（我的探针，n=40 与 n=25 两批）：`ep≥3 占比` 普遍 0.0~2.1%、**环出手 0.00%**（全部样本）、`ジ占比 51~69%`、长程攻/回合 0.11~0.32。

---

## 5. 【给他今晚分析用】未记账的 20 个包，我这边的读数 `[实测]`

**范围**：`v7roleC2-{31,94,95,96}`、`v7role12-{31,94,95,96}`、`v7w3c-{31,81,82,91,92,93}`、`v7w3r1-{31,81,82,91,92,93}`（00:13–00:42，共 20 个 `.bak`；`w3c`= `DIV_W=0.3`+角色层关，`w3r1`= `DIV_W=0.3`+`DIV_ROLE_W=1`）。CHANGELOG 对这四条臂 **0 次提及**；`roleC2/role12` 8 个在 HEAD 前、`w3c/w3r1` 12 个在 HEAD 后（未进任何提交）。

已知配置差异（他若记录会更省事）：`v7w3c/v7w3r1` 的 `.training-out-3p.js` 与 `ring2-*w3r1.log` 同刻生成 ⇒ 落盘时训练锁仍在，**bundle 行不可信**（这是他自己 D16/训练锁那条规矩的适用场景）。

我这边的抽样读数（经济出口，n=25/格；"vs只枪"= 1 席被测 + 4 席只枪打最肥）：

| 包 | 自对局 ep峰 | ep≥3 占比 | **环可负担** | 环出手 | 攻/回合 | 夺冠 | 回合 |
|---|---|---|---|---|---|---|---|
| `roleC2-31` | 2 | 0.0% | 0.0% | 0.00% | **0.00** | **0%** | **104** |
| `roleC2-94` | 2 | 0.0% | 0.0% | 0.00% | 0.21 | 20% | 73 |
| `role12-94` | 3 | 2.1% | 2.1% | 0.00% | 0.17 | 20% | 90 |
| `role12-95` | 2 | 0.0% | 0.0% | 0.00% | 0.19 | 20% | 99 |
| `w3c-91` | 3 | 0.1% | 0.1% | 0.00% | 0.21 | 36% | 47 |
| **`w3c-93`** | **81** | **50.0%** | **50.0%** | **0.00%** | 0.15 | 12% | 76 |
| `w3r1-91` | 5 | 1.4% | 1.4% | 0.00% | 0.26 | 16% | 53 |
| `w3r1-93` | 2 | 0.0% | 0.0% | 0.00% | 0.20 | 8% | 72 |

### 5-1 两个新失败模式（建议他写进 CHANGELOG，因为它们会重复出现）
- **退化包**：`roleC2-31` 与 `ctrlE-31` 自对局**一次攻击都不出**（0.00 攻/回合、104 回合、0 胜、G=1.00），却 A 考卷 46.8~47.1%。
  ⇒ **A 考卷对"完全不出手的包"照样给高分** —— 这是比"窄"更严重的口径漏洞；它的入口是并列判胜（`1st` 含并列）。建议：把 A 严胜（已有列）与"自对局 0 出手率"绑成一条 reject（`零伤害率` 现在是 0%，因为它量的是每局伤害，不量出手次数）。
- **囤积包**：`w3c-93` ep 峰值 **81**、50% 的决策点上 ep≥3 且**环可负担**，但 **环出手 0.00%**、`ジ占比 76%`。
  ⇒ **这条推翻了"环是因为攒不到 3 ジ 才没人用"的读法**：他 §5.3 的样本是"12 局里 ep 从没到 3"（收入饥饿），而今晚出现了"常年 ≥3 却永远不上"（**转化拒绝**）。两种形态并存 ⇒ 必须分开治。

### 5-2 由此得到的一个明确结论（回答他 §5.3/§9-Q3 与用户的"ep<3 主导"）
**环和原型制御现在是同一个病，不是曝光度病、不是枚举病、不是经济门槛病。**
证据链：`proto` 可负担 51.4% / 候选表 100% / 采样选中 **0.0%**（他的 `probe-beadloop`）；`ring` 在 `w3c-93` 上可负担 50.0% / 选中 **0.00%**（我的探针）。
⇒ `[推断]` 缺的是**"这类动作值多少"的价值估计**，而不是"能不能出这一手"。可行的两条路：① 教师示范（他已有 `setImitUntil`/`EPIRUS_IMIT_TEACHER=antiring` 的接线，方向对）；② 把 `H+T·S` 里 S 的分量做成"角色覆盖"而不是"卡种覆盖"（`roleOf` 已经备好），但**前提是先把 §4 那三个红关掉**，否则广度只会来自退化。

---

## 6. 一条可以现在就上闸的门（回答"D78 什么时候能阻断"）`[实测]`

D78 卡在"没有已知好一侧"。**已知好一侧已经有了**：上一轮的 `v7f3-94` 只枪格 = **5% / 5%**（n=60，`gate-drafts` 原样输出），而它在位时被测过破防 0%、座位 12~13pt。
⇒ 建议：以 `v7f3-94` 为"密度门"的**标定包**（不是上线包），把 `只枪(打最肥)` 那一格的阈值定在 **≤45%**；
现状：在位 65% ✗ / 今晚全部 55~100% ✗ / 94 5% ✓ ⇒ **有已知好一侧 ⇒ 可以阻断**。
⚠️ 别用 `94` 当全能参照：它自己被"瞄威胁者"打 67%。**每个格子各用自己的已知好一侧**，不要用一包定全部阈值。

---

## 7. 建议顺序（今晚这批分析完之后）

1. **给"自对局 0 出手"补一条 reject**（§5-1 退化包），否则 A 会继续奖励沉默；顺手把 `零伤害率` 与 `零出手率` 分成两列。
2. **把环与原型制御并成一个"转化"课题**：验收指标不要用使用率（会被 0 出手包骗），用他已有的形状 —— "**可负担时的选中率**"（`w3c-93` = 0.00%/50.0%）。教师示范优先于奖励系数。
3. **D78 上闸**（§6），同时把 G4 的"红的理由"打进 `promote-champion` 输出（他已经在 gate-drafts 里做了，接过去即可）。
4. **A 分数降级为诊断量**：连续两轮它都与稳健性反相关（`role1w-31` 53.4% ↔ 只枪格 90%；`densE-31` 49.6% ↔ 78%，他 `v1.5.90` 自己那三个数也是 78/100/82）。若只留一个换包判据，留"**最坏格 minimax**"。
5. **臂记账纪律**：跑完的臂必须落一条 CHANGELOG（哪怕结论是"作废"），否则盘上 20 个 `.bak` 无人知道它们是什么 —— 本轮就有 12 个是 HEAD 之后落盘、零记录，其中一个座位极差 **99pt**。

---

## 8. 我的口径与过错 `[声明]`

1. 本文 §4/§5 的格值来自 `tools/gate-drafts.mjs`（**他已改过的版本**：阈值、UNRUN 三态、"红的理由"文案都是他的实现）⇒ 与他自己的运行应当可复现到同一批数；**局数不一致时以 n=60 为准**。
2. 经济出口探针我用 n=40（§4 表）与 n=25（§5 表）两批，**同一列不可跨批比较**；`vs只枪` 那一列是"1 席被测 + 4 席脚本"，与 G4 的"1 席脚本 + 4 席被测"**方向相反，别混读**。
3. `roleC2-31` 的 G4[long] 我标 UNRUN（基线 0%）⇒ 该格**既不算过也不算红**；它的 67% 只能当线索。
4. 我**没有**跑 `battle-test`、没有跑训练（只读包与脚本），因此 §3 的"臂间因果"我只在他已落盘的产物上复测，不能替臂配置背书。
5. 上一轮我错的那条（克制表漏"只枪"）他已收进门并在文件头留了注释；本轮该格的在位包真值 **65%（长程）** 以此为准。

---

### 附：复跑入口
```bash
node tools/np-test.mjs && node tools/spec-run.mjs && node tools/smoke.mjs && node tools/ui-probe.mjs   # 129/129 · 36/36 · OK · 10/10
node tools/rules-fingerprint.mjs                                    # 8dda2677
GATE4_GAMES=60 node tools/gate-drafts.mjs docs/artifacts/v7w3r1-31.bak docs/artifacts/v7role1w-31.bak
node tools/champ-audit.mjs docs/artifacts/v7divK2-81.bak --games=40 --exam-games=40
# 我这轮的临时探针（未入库，需要就说）：epirus-ep2.mjs（ep/环可负担/环出手/出手密度，支持批量 .bak）
```
