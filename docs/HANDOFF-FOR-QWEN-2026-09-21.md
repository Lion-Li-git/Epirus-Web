# 交给千问：训练侧清单（DS 2026-09-21 14:00 交）

> 前台侧（浏览器运行时）我已经改完并落了版本 **v1.5.142**（三条探索状态规则 + 门 D111），细节见 `CHANGELOG.md` 顶条
> 与 `docs/RESEARCH-LOG-2026-09-21-ds.md` §10。**下面全是训练侧/研究侧的事，按优先级排。**

## 0. 先说结论（别重做）

- **大雷不是废卡**（我的旧结论已撤回）：用户实盘 4 次施放传导 0/0/1/2 人 ⇒ 均值 **3.50 点/次**（不传导基线 2.00）；
  而 `pickHeavyFire`/纯攒大雷这类脚本实测**传导/施放只有 0.43~0.44**（盲放）⇒ 价值表里那两行 15%/8% 是"盲放的大雷"，不是这张卡。
  正解规格见 `docs/RESEARCH-LOG-2026-09-21-ds.md` §8.3（线名 `BIGT_FOCUS`）。
- **珠经济可训**：`BEAD_W` 0.05 → **0.5** 就能买到（`v7bead3-82` 花珠率 **85.7%**、`--dry` 判"闭环 ✓"、电磁炮出手 4 次/20 局，
  现役 0 次）⇒ "是结构不是钱"那条推论**作废**；正确表述 = "价格高于 0.05 量级、低于 0.5 量级"。
- **教师示范通道**：千问 `v7t2` 用的 `pickHeavyFire` 实测**贵卡出手 0、最大 ep=2**（结构上买不起 cost≥3）⇒ 那段示范是空的；
  只有 `pickBeadBurst` 真在走那条链（贵卡出手 19.9%、`charge:69 railgun:67`）。

## 1. P1 · 验证我加的三条前台规则**真的生效**（只读，最便宜）

`tools/behavior-profile.mjs`（千问 #26 建的量具）跑现役包 × 前台口径（`pickChampion(..., 0.15, 0.2, 5, 'soft')`）：
- 期望：**贴贴 从 2.70/局 掉到 ≈0**（ep≥3 才允许）、**天火 从 0.00/局 变成 >0**（有符咒时并入探索集）、
  电磁炮/激光眼出现率上升（持珠时并入）；
- 若天火仍 0 ⇒ 说明"有存活符咒"的判定没覆盖到实际情形（我用的信号：`state.p[*].stickers` 里 `owner===pid`，或 `lastSkill===CURSE`）⇒ 报实测。
- 成本：一条只读剖面（分钟级）。

## 2. P2 · `BEAD_W` 中间档扫描 **并把口径定在长程**（这是用户真正在意的模式）

- 现状：`v7bead3-82` 在**长程 5 血**里 蓄能 0.10~0.17 次/局、**电磁炮 0.05~0.10 次/局**（= 5~10 局才一次）
  ⇒ 用户打 1~3 局看不到炮是**正常**的；`--dry` 那条"珠经济闭环 85.7%"是**转化率**（花掉/得珠）不是频率 —— 别混读。
- 目标：把**长程口径的电磁炮频率**抬上去（判据建议：≥1 次/局 才算"用户能看见"），同时守住 `--dry` 的
  座位/场A/G（`v7bead3-82` 这三格相对现役是略退的：座位 12.1 vs 8.2 · G 6.72 vs 6.84 · 场A 22% vs 31%）。
- 配方：`v7bead2/3` 逐字 + `EPIRUS_BEAD_W=0.15 / 0.25`（两臂 ≈25 分钟）· 判据用 `tools/gate-drafts.mjs`（**n≥120**）
  + `promote-champion --dry`（含长程 `chargeProfile`）。
- ⚠️ 标度不是免费的：`v7bead3` 已把目标分在 2/3 seed 上压低（93: first 0.481→0.399）。

## 2b. P1.5 · **门禁口径已改（用户裁定，v1.5.145）：广度两个模式都判** ⇒ 训练口径要对齐，否则这条会成为"死线"

- 改动：`promote-champion.mjs` 现在把 **`selfPlay(..., 'long')` 的 G 也传进 `feasibilityOf`**（`G2/G2name`），
  **同一条线 3**；现役 `v7cmin4-31` 实测 **G(multi) 4.44 · G(long) 3.43 ⇒ 两模式都过** ✓；门 **D112** 守着。
- **但训练侧（fit/形状项/池子）仍在 `multi` 上优化** ⇒ 我们**用一个模式训练、用两个模式验收**。
  现状能过是因为 long 恰好 3.43；一旦某代把 long 压到 3 以下，会出现"训练目标提升、门反而红"的假象。
- **要做的**（二选一，建议 ①）：
  ① 把训练侧的 fit/形状项也按 **long** 计的份额加一档（或至少在选种时把 long 的 G 作为**并列**指标，
     像 v7cmin4 那样在收尾时按 long 复核）；② 明确写下"本门只用于上线把关、训练不追它"，并接受它会周期性红。
- ⚠️ **同模式两量具分歧**（留待用户裁定，别自己改）：同一 long 下 `selfPlay` 3.43 vs `breadthProfile` G_eff **2.79**
  ⇒ 恰好骑线。若要"取更严的那个"，那是**用户裁定级**的判据改动。

## 3. P3 · `BIGT_FOCUS`：会传导的大雷线（**新脚本**，进池/教师通道）

规格（用户口径写死）：目标选择 = 预测"本回合谁攻击谁"——
- 信号 A：对手 ep 存量（攒到 2~3 的更可能出手）；
- 信号 B：**上一回合的集火对象**（血厚者被集火 ⇒ 预测本回合仍有人打他）；
- 收益口径 = **被封住的行动数**（`🚫 技能被无效化`），不只是伤害。
**预注册判据**：传导/施放 **> 0.43**（对标用户实盘 0.75）**且** 胜率 **> 基线 18%**（口径同 G4：1 席脚本 vs 4 席现役冠军 · multi · n=60 · seed0=90210）。

## 4. P4 · 贴贴线：把用户的"贴三张 + 引爆两次"做成可检验的东西（**训练侧才有的问题**）

- 事实：**贪心策略完全不使用贴贴/天火**（ε=0 时两者皆 0.00/局；我改的只是**探索**）⇒ 想让策略自己长出来，只能走
  教师/形状/奖励，而**现在没有任何教师示范这条链**（`pickHeavyFire` 的 order 里没有 FIRESTORM）。
- 建议做法（顺序）：
  1. 先写一条脚本线 `pickCurseCombo`（贴贴×3 → 天火×2；符咒引爆后**仍存**，所以窗口内可再引爆）当**对手/教师**；
  2. 用 §8.1 那套价值表先量它**值不值钱**（1 席脚本 vs 4 席现役冠军）——若 ≤ 基线就不再投入（避免第四次"空示范"）；
  3. 若 > 基线 ⇒ 进教师计划（`EPIRUS_IMIT_PLAN` 的一段）或进池，按本仓预注册流程跑。
- ⚠️ 别重踩：`v7t2` 的教训是"教师触发条件必须与那件事真的相关"（本仓 v1.5.37 老教训第三次复发）。

## 5. 规矩（沿用）

预注册判据跑前写死 · 产物 `.bak` 点名进 CHANGELOG（D82）· 不 promote 不切槽除非用户点头 ·
指纹文件（`rules/state/resolve/play/policy`）改动要单独立项 + 全链重测重记 · `results/*` 永不 add ·
上线级判据 **n≥120**（n=60 在 60% 线附近是抽样噪声：`v7s8-82` 的 G4-multi n=60=58% 过 / n=120=61% 红）。
