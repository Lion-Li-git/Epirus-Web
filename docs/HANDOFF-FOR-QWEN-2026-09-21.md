# 交给千问 · **晚间版**（DS 2026-09-21 夜 · 只留还没做/今晚新出的）

> 上一版（14:00 版）的 P1/P2/P3 与"状态分布塑形"**你都已经做掉了**，本版把它们删掉，只留下面这些。
> 已完成项的结论在 `CHANGELOG.md` 与两份 `RESEARCH-LOG-2026-09-21-*.md` 里，**不用再翻这里**。
>
> 已交付索引（一句话，防止重复提议）：P1 前台三规则复跑 → 你 `36b4de0`；P2 `BEAD_W` 中间档 → 你 `78c66fe`/§14.4
> 收口（0.15 档三粒长程电磁炮 0.00/局 ⇒ **这条旋钮不是单调的**）；P3 `BIGT_FOCUS` → 你 `36b4de0` 已实现
> （`js/train/bots.js` + `tools/script-value.mjs`）+ §20 复核；我 §5 的状态分布塑形 → 你 `8e4eb81`（`EPIRUS_BEAD_SEED`）。

---

## 1【今晚新 · 最高优先】门禁口径改了：**广度两个模式都判**（v1.5.145，用户裁定）⇒ **训练口径必须对齐，否则这条线会变成假红**

- 改动：`tools/promote-champion.mjs` 现在把 `selfPlay(W, params, 'long', G)` 也传进
  `feasibilityOf({ G: sp, G2: spL, G2name: 'long' })`，**与 multi 同一条线 3**；门 **D112** 钉住。
  现役 `v7cmin4-31` 实测 **G(multi) 4.44 · G(long) 3.43 ⇒ 两模式都过** ✓（这条新门它站得住）。
- **问题**：训练侧的 fit / 形状项 / 池子仍按 **`multi`** 计量 ⇒ 我们**用 multi 训练、用两个模式验收**。
  现在的 long 恰好 3.43，一旦某代把 long 压到 3 以下，就会看到"**训练目标在涨、门反而变红**"的假象 ——
  那不是回归，是口径错配。
- **要做的**（二选一，DS 建议 ①；这是**训练侧**的活，我不越界）：
  1. 把 long 的 G 也纳入选种/收尾复核（像 `v7cmin4` 收尾那样），或给 fit 里的广度项加一档 long 计权；
  2. 明确写下"此门只做上线把关、训练不追它"，并接受它会周期性红。
- ⚠️ **同模式两量具分歧**（**用户裁定级，别自己改**）：同一 long 下 `selfPlay` G=3.43（门用）vs
  `breadthProfile` G_eff=**2.79**（打印）⇒ 恰好骑线。体检打印里已显式告警，等用户定"要不要取更严的那个"。

## 2【今晚新】验收标准加第三栏：**真机栏**（ε=0 栏 / 浏览器模拟栏 / **真机栏**）

- 事实（DS 用用户实机 `results/31` 五局量的，现役 `v7cmin4-31` · 长程 5 血）：
  | 能力项 | ε=0（训练/门禁/skill-report） | ε.2 soft（浏览器模拟） | **真机** |
  |---|---|---|---|
  | 电磁炮/局 | 1.60（你测 3.87） | 0.55（你测 0.93） | **0.20** |
  | 防御类占比 | 0.2% | 6.3%（你测 4.4%） | **23.0%** |
  | 蓄能/局 | 2.05 | 1.15 | **0.40** |
- ⇒ **模拟替代不了产品**：真机（1 真人 + 3 AI）是第三套分布。这同时**钉死了你 §23 的悬念**（你测 4.4%、
  怀疑用户是 localStorage 旧包）：**不是旧包，真机就是 23%**。
- 工具已建：**`tools/log-behavior.mjs`**（只读）：`node tools/log-behavior.mjs results/<目录>` 直接出真机栏。
  ⇒ 你的 DOING-1（给 champ-audit / skill-report 加浏览器口径列）做完后，建议**再加这一栏**：
  **三栏都达标**才允许把"某能力项"写进上线理由。

## 3【仍开着】P4 · 贴贴线（训练侧才有的问题；我今晚只改了**前台探索**）

- 事实：**贪心策略完全不使用贴贴/天火**（ε=0 时两者皆 0.00/局）。我 v1.5.142 加的三条规则只作用于
  `epsMode==='soft'`（浏览器运行时）⇒ **策略本身仍不会走这条链**。
- **现在没有任何教师示范它**（`pickHeavyFire` 的 order 里没有 FIRESTORM）⇒ 只能走教师/形状/奖励。
- 建议顺序（**先量价值再投入**，别第四次踩"空示范"）：
  1. 写一条脚本线 `pickCurseCombo`（贴贴×3 → 天火×2；符咒引爆后**仍存** ⇒ 窗口内可再引爆；
     用户口径"贴三张然后引爆两次"性价比最高）当**对手/教师**；
  2. 用价值表（1 席脚本 vs 4 席现役冠军 · n=60 · seed0=90210）先量它值不值钱 —— **≤ 基线就停**；
  3. 若 > 基线 ⇒ 进 `EPIRUS_IMIT_PLAN` 一段或进池，按预注册流程跑。
- ⚠️ 教训：`v7t2` 那次失败是因为教师的触发条件与要教的事**无关**（`pickHeavyFire` 实测贵卡出手 **0**、最大 ep=2）
  —— 本仓 v1.5.37 老教训的第三次复发。

## 4 规矩（沿用，已压缩）

预注册判据**跑前写死** · 产物 `.bak` 点名进 CHANGELOG（**D82**）· 不 promote 不切槽除非用户点头 ·
指纹文件（`rules/state/resolve/play/policy`）改动单独立项 + 全链重测重记 · `results/` **永不 add**（v1.5.145 已整目录退出跟踪）·
上线级判据 **n≥120**（n=60 在 60% 线附近是抽样噪声：`v7s8-82` 的 G4-multi n=60=58% 过 / n=120=61% 红）。
