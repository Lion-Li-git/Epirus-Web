# 通宵研究日志（千问 · 2026-09-19 深夜 → 09-20 晨）— 分支 `qoder-research-0920`

> 用户授权：代替 DS 做一晚训练研究，无特别限制，09-20 09:00 前停工；本分支可推送。
> **规矩（自守）**：不 promote、不碰 `js/bundled-champion*.js` 与 `index.html`（ring2-run 会自动还原，仍不去赌）；
> `results/*` 是用户私档，永不 add；产物 `.bak/.log` 被 .gitignore 忽略，只在本文点名（D82 家族口径）；
> 每个臂的判据**跑前写死**；防失忆：每次状态变化都更新本文（本文件是唯一进度真源）。

## 状态灯（最近一次更新在标题行时间戳）

- [00:5x] **DONE** = Q1 臂 6/6 产物（线上包未被改动，sha1 与臂首快照一致）。**快筛（只枪聚焦线 · n=40 · G4 口径 seed0）**：

  | 包 | 只枪 long | multi | 打在枪手/局 | 判读 |
  |---|---|---|---|---|
  | 线上包 | 78% | 58% | 3.23 | 对照（≈已记录的 75/62） |
  | **v7aim1-82** | **5%** | **8%** | **5.00** | ★ **攻击解**：场B 清场 **0.80** ✓ · 场A 24% ✓ · 枪手余血 0.00 |
  | v7aim1-81 | 53% | 50% | 3.65 | ✗ **被动解**（场B 0.00 · 98% 平局 · 场A 9%）——同 v7gf1-92 的形状 |
  | v7aim1-91 | 65% | 58% | 3.17 | ✗ 未过 |
  | v7aim1-92 | 90% | 48% | 2.48 | ✗ long 恶化（probes：forceAttack 后 32% ⇒ 它输在**瞄准**不在花钱） |
  | v7aim1-31/93 | 100/85% | 100/68% | 0.88/2.10 | ✗ |

  ⇒ **预注册判据落在"介于"上**：通过要 ≥2/6 的真攻击解，实测 **1/6**（82）。机制**没有死**——模仿教师能买到
  "把伤害引到压迫者身上"这个 *攻击性* 行为（v1.5.134 的池子杠杆从未产出过这种包：它只产出被动解）——
  但命中率低。**结论草稿**：杠杆有效但需**接力**：以 82 为热启动点（用本版新加的 `RING2_HOT`）再长一代，
  看攻击解能否固化/扩散（Q1b，§5c）。
- [00:5x] **DOING** = 82 的完整只读体检（五道门 + G4 全格）在跑 → `q1-82-dry.log`
- [pending] Q3 臂：旋钮已接好（`EPIRUS_RING_W` 往返验证 ✓），等体检完成即开 3 seed 探臂
- [pending] 小修三件的 np-test 验证（含"故意改一格 chooser ⇒ D67 应红"的消融自证）


## 5. 新发现（训练进行中顺手查的）——**"热启动 = 最新冠军"在 NP 臂路线上其实是假**

事实链（全部可复核）：
1. `ring2-run.mjs:85` **无条件覆写** `process.env.EPIRUS_BUNDLE_IN = staging`，随后每次 seed 前 `copyFileSync(BASE, STAGE_IN)`，
   `BASE = docs/artifacts/champion-5p-v1.3.58.bak` ⇒ 外部传的 `EPIRUS_BUNDLE_IN=js/bundled-champion-3p.js`（HANDOFF §1.2/§3 的启动命令）**整条被吞**。
2. 用 `train-server.weightsId` 同款算法实测：`v1.3.58.bak → d13d3c856c6cff62`、线上包 → `03b35fca…`；
   而 `v7ws1-91 / v7big1-92 / v7gf1-92 / 线上包` 的 meta `hotstartFrom` **全是 d13d…** ⇒ 全部臂与线上包**同谱系**（都从 v1.3.58 旧种子长出来）。
3. ⇒ HANDOFF §1.2"热启动起点改用最新冠军"与 §2.2"热启动真的生效的硬凭据（五道门读数逐项相同）"**归因错了**：
   读数逐项相同恰恰是因为**起点根本没换**；"种群 G 中位 3.55 vs 2.4"的差若要成立，变量得另找（池子/CLEAR_W/recipe）。
   ⚠️ 我没有重跑 §2.2 的实验，这条只是**谱系记账与代码事实**，不是对"G 变高"的否定。
4. A/B 可比性**没坏**（所有近期臂同谱系）——坏的是那句话说的是另一件事。
5. 修法（本版已做）：`RING2_HOT=<路径>` 显式开关（不设 ⇒ 默认行为逐字不变），产物 meta 的 `hotstartFrom` 变成可核对的"所选起点的 weightsId"。
   **留给早上的裁定**：要不要真做一次"从线上冠军热启动"的臂（第一次让 `RING2_HOT=js/bundled-champion-3p.js` 成为实验而不是口号）；
   以及 2P 侧 `train-best.mjs:61` 的"3P 侧早就是热启动（= 最新冠军）"同样要按本节口径改写。

## 5a. G4IMPL 阳性对照（已过）

复制 gate-drafts → 只改「只枪」格 chooser 的一处实现（加 `target: null`）⇒ **G4POOL 恒 `960dfe0b` 而 G4IMPL `caecc92f`→`1b22c3f1`**
—— 正是 v1.5.133 §5 那条缝"焊上"的形状；变异体下 D67 的冻结豁免当场作废。临时文件已删。
（np-test 全量回归留到臂全部跑完，避免与训练抢 CPU 且口径中途漂移。）

## 5d. Q1b 结果（00:4x）：接力**保住了行为**，但"修一格冒一格"

| 包 | 只枪 long/multi | 打枪手 | 场B 清场 | 场A | 全表体检 |
|---|---|---|---|---|---|
| v7aim2-31 | 40% / 3% | 5.00 | 0.80 ✓ | 22% ✓ | （未跑，预期同类） |
| v7aim2-82 | **0% / 3%** | 5.00 | 0.80 ✓ | 21% ✓ | ✗ G2.00<3 · ✗ **新最克格「瞄威胁者(1ジ枪)」75%(multi)** · 靶向 52.6% |
| v7aim2-93 | 55% / 20% | 5.00 | 0.80 ✓ | 13% ✗ | （未跑） |

- **预注册判据的字面裁定：未达**（要"五道门全过+G4 两模式 ≤60"，82 挂在 G 与新格）。
- 但两条实质结论是硬的：① **瞄准/攻击解可以热启动接力保存活**（3/3 seed 保持"打枪手 5.00、枪手 0 余血、场B 稳 0.8"，无一退回被动）——`RING2_HOT` 第一次真热启动即验证；② **专才的代价显形**：只枪格修绿后，克制表露出新的最克格（瞄威胁者 75%）+ 广度继续坍（G 2.76→2.00）⇒ "过一格"≠"过全表"，**换包判据必须以全表最坏格 + G 为准**（这正是 promote 体检在做的事，包没被我说服上线是对的）。
- 给 DS/用户的读数：**模仿通道能买到"瞄准"这个基因**（今晚最大的机制收获），但当前配方（IMIT 0.5 + 单教师）会把它**过拟合成教条**——下一根杠杆要么降教师占比（0.25）+ 保热启动，要么把"全表最坏格"直接进适应度（=Q2 形状法，代价最高）。今晚先跑 Q3，Q2 只交设计稿。

## 5c. Q1 终裁 + Q1b 接力臂（预注册）

**Q1 终裁（体检补全 82）**：G4/G5 行为门 ✅ 全过（8 格 × 两模式）· 墙 18.05 ✓ · 场A 24% ✓ · 场B 0.80 ✓ ·
狙击场靶向率 48.1%（线上 17.7%）—— **但**座位极差 35pt（0号座 0%）✗ · G 2.76<3 ✗ ⇒ 专才窄体，**不可换包**。
预注册"≥2/6 真攻击解"未达（1/6）：机制成立、命中率低。诚实读法：
`v7aim1-82` 是全仓**第一个"不靠变被动过 G4"的包**（v7gf1-92 是被动解，82 把枪手打到 0 血）。

**Q1b 接力臂（预注册，跑前写死）**：假设 = "瞄准基因可以从 82 热启动接力、在自然选择压下长回宽度与座位公平"。
配方 = Q1 **逐字相同**（教师 aimdefender · frac 0.5 · OVERRIDE · CLEAR_W 0.1 · DIV_W 0.3 · 24 局 · 同池），
**唯一变量 = `RING2_HOT=docs/artifacts/v7aim1-82.bak`**（第一次真热启动 —— 起点是"会瞄的包"而不是旧谱系）。
seeds 31,82,93。
- **通过**：≥1/3 seed 五道门全过 **且** G4 两模式 ≤60%。
- **否证**：3 seed 全部要么退回被动解（场B 0.00）要么 G4 恶化（>60%）⇒ 热启动接力保不住行为，
  下一步只剩 Q2（把"打脚本外部席的伤害"做进适应度形状）——这条否证本身就是 §5"谱系≠行为"的正面证据。
- 产物点名：`v7aim2-{31,82,93}.bak`。



## 0. 起点事实（HEAD = `c7076ac` · v1.5.134 · 指纹 `00e732a7`）

- 门禁基线：spec 45/45 · np-test 154/154（D67 走"已记录例外"：meta.gate4Pool=`960dfe0b` + 两行留痕）。
- DS 已证：珠爆发线 2P 重训后 100%→0%（我也用自己 6 条线复验 0 胜）；`forceAttack/aimGunner` 规则覆盖能两全
  （只枪 13%/0% + 场B 清场 1.00）⇒ **互斥是旋钮的性质不是游戏的**，差距在学习信号/形状。
- v1.5.134 §3：**瞄准是主因**（打枪手身上 3.27→5.00/局就 100% 拿下，不额外花钱；r=−0.82 跨 13 包）。
- 教师通道事实：`EPIRUS_IMIT_TEACHER=<名字>` 在 `evo.js` 的 `BOT_PICKS` 查表；`ring2-run.mjs` 是通用臂自跑器。
- 环奖励现状：`evo.js:1955 RING_W=0.10`（setRingReward，未接 econ-env ⇒ 臂间不可调）；贵卡 `EPIRUS_BIGCARD_W` 已接。

## 1. Q1 臂 ——「防守型瞄准教师」（预注册判据，跑前写死）

**假设**：模仿通道能直接买到"把伤害集中到压迫席身上 + 买得起就打"的行为（v1.5.134 §5 的形状假设的便宜替代）。
**实现**：`bots.js` 新 `pickAimDefender` —— 行为 = 击杀优先 → 否则打"本局对我方输出最高者"；出手 = 买得起枪就枪、
买得起狙且目标是攒珠型则狙；**不摆防御**（教师示范的是瞄准+花钱，不是龟）。evo `BOT_PICKS` 注册名 `aimdefender`。
**臂配方**：与 `v7gf1` 六臂**逐字相同**（池子/教师占比/热启动/CLEAR_W=0.1/DIV_W=0.3/24 席局），
**唯一变量 = `EPIRUS_IMIT_TEACHER=aimdefender`**（替掉 pickDeepSaver）⇒ 严格配对。seeds 31,81,82,91,92,93。
**通过（机制成立）**：≥2/6 seed 同时满足 ① G4[long] 只枪 ≤60% ② **场B 清场 ≥0.3**（不许用被动换绿）③ anatomy 基线行"打在枪手身上" ≥4.0/局。
**否证**：全 6 seed G4>60%，或凡 G4 绿者场B=0.00 ⇒ "模仿也只买得到躲"，此杠杆死刑、只剩形状法（Q2）。
**产物点名**：`v7aim1-{31,81,82,91,92,93}.bak`（预期；未 promote）。

## 2. Q3 臂 ——「既打环又会花贵卡」→ 重做环消融（预注册）

**假设**（handoff §4-2 原话）：要回答"环值不值钱"，需要**既打环、又会花贵卡**的包。
**实现**：给 `RING_W` 接 econ-env 单一来源（新键 `EPIRUS_RING_W`，D77 运行时往返自动盯）；臂 = v7gf1 配方 + `BIGCARD_W=0.1` + `RING_W` 抬到 0.25（奖惩不对称是环学不出来的阻力这条假设的正面检验）。
**合格判据（跑前定死）**：30 局自对局里 ring 出手 ≥100 次（线上包 346）且贵卡（大雷/地雷/净化/落雷/电磁炮）合计 ≥1%/回合、≥2 种非零。
**消融**：对**合格包**跑 `probe-ring-ablate`（V1/V2/V4 · n≥300 · 配对符号检验），如实报 pt 与 p——**只回答"环值不值钱"，不构成换包判据**。
**产物点名**：`v7rb1-{31,82,92}.bak`（先 3 seed 探路，长出合格包再补）。

## 3. 小修两件（不碰指纹文件优先）

1. `meta.g6` 恒 `{}`：`promote-champion` 的 G6 正则匹配标题行、值在 detail 行（v1.5.129 §7-3 自记）。
2. **G4POOL 实现缝**：把"实现身份"并入例外绑定的方式是设计题——直接改 `G4POOL_ID` 的哈希内容会让
   **在位包的 `--force` 例外瞬间失效**（D67 红）。方案：保留 `G4POOL_ID` 不动，**另发** `G4IMPL_ID`
   （对 COUNTERS 各格 chooser 源码 sha1），写进 meta；D67 判据改为"pool 与 impl 双绑，缺一即例外失效"。
   对已发货包做**一次性兼容**：无 `gate4Impl` 字段的旧 meta 只在**当前** id 组合下豁免（写死在门里，改一格实现即破豁免）。

## 4. 风险与回退

- 臂跑在 ring2-run 内（自带备份/还原 bundle 与 index.html）；我只额外保证：**不手改这两个文件**。
- np-test 若被我的改动弄红，红要在我自己的 commit 里闭环；闭环不了就回退该 commit。
- 明早 09:00 前：最终全门禁跑一遍 + 本文"状态灯"收尾盘点 + push。

## 5e. Q3 探针结果（01:0x）与 Q3b 预注册

**Q3（BIGCARD 0.2 + RING_W 0.25 · BASE 旧谱系 · 3 seed）——未产合格包，但地图变清楚了**：

| 包 | ring/30局 | 贵卡(mine/purify/railgun/bigT) | 天火出手 | 回合/局 | 判读 |
|---|---|---|---|---|---|
| v7rb1-31 | **0** | 0 | **155** | 95 | 第三吸引子：贴贴→天火刷屏（环 0） |
| v7rb1-82 | 10 | 13（地雷3/净化1/电磁炮9 · 3 种非零） | 0 | 79 | "两者都做"但环太稀（阈 100） |
| v7rb1-92 | 0 | 0 | **184** | 102 | 同 31 |
| 线上包 | 59 | 0 | 0 | 43 | 只环不花钱（已知半洞） |

⇒ 新事实：**贵卡奖励长出来的是"贴贴-天火"低成本轨迹**（天火 cost2 无珠、按"贵卡=cost≥3 或 energyNeeds"的声明口径**不算贵卡**——
要么它吃了别的 shaping（贴贴=curse cost1 也不贵），要么 `countBigCards` 有口径缝：**明早要查**）；
而环在新压力下直接归零 ⇒ "同抬两旋钮"在旧谱系上买不到共存。
**Q3b（HANDOFF §4-2 候选②的正面执行，预注册跑前写死）**：`RING2_HOT=js/bundled-champion-3p.js`（用环血统包做起点）
+ BIGCARD 0.2 + RING 0.25，其余同 Q3，seeds 31,82,92。
- **通过**：≥1/3 seed 满足 ring ≥100/30局 **且** 贵卡 ≥2 种非零、合计 ≥30 ⇒ 在它身上跑 `ABLATE_KEY=ring` n=600
  （V1/V2/V4 配对符号检验），回答"环值不值钱"；**只记录不换包**。
- **否证**：3 seed 环仍 <100 ⇒ "环+贵卡共存"在现奖励形状下不可达，§4-2 结案为**带证据的否证**而不是继续悬挂。
- 产物点名：`v7rb2-{31,82,92}.bak`。

### 5e-补（01:2x）"贴贴天火流"的账查清了：**贵卡口径没缝，是 CLEAR_W 的第二被动解**

- `isBigCard`（evo.js:1703-1708）严格按 cost≥3 或 energyNeeds ⇒ 天火/贴贴**不记分**（我 §5e 里怀疑的口径缝不存在，撤回）。
- 真机制：`countClears` 把**终局收缩打死的人也计为清场**（收缩开始前最后一个伤害归属者）⇒ "活得最久"就是清场引擎。
  31/92 的自对局 **95~102 回合/局**（对照线上包 43）⇒ 它们不是"贵卡流"，是**长寿刷收缩流**——
  `CLEAR_W=0.1` 的第二种躺赢解（第一种是 v7gf1-92 的纯龟）。RB1 双旋钮只是把"花钱压力"绕开了（不花贵卡也能吃满 clear+存活）。
- **可立门的读数（给 DS/用户的草案，今晚不动 `V4_MIN` 那类用户裁定件）**："自对局均回合 ≥70（> 收缩点+25）且伤害/回合 <0.35"
  即判"刷收缩"，场B 清场计数应打折或加"收缩前死亡数"口径。**记账不动手**：这是判据设计，属裁定件。
- 这也反向加强 Q3b 的意义：起点=线上包（43 回合快节奏）+ 双旋钮，看共存是否可达。

## 5f. Q3/Q3b 终裁（01:4x）：§4-2「既打环又会花贵卡」= **带证据的否证**

- Q3b（线上包热启动 · 环血统 59/30局 起点 · BIGCARD 0.2 + RING 0.25）：31 → ring 10/贵卡 0；82 → ring **0**/大雷 126（真贵卡、单卡教条）；92 → ring 0/天火 38。
- **两代合计 0/6 满足共存判据**（ring≥100 且贵卡≥2种且≥30）。方向性读数：贵卡一旦长出（rb2-82 大雷 126），**环直接死**——
  两者抢同一笔 ep 预算，RING_W 0.25 抬不住大雷的回报密度。
- ⇒ **建议 §4-2 这样结案**："环值不值钱"这个问法在当前奖励几何下**不可检验**（要共存包 = 要"同一资源同时买两种不同时间的回报"，现形状只奖励其一）。
  要么给环**跨回合复利**的形状（段长奖励已有、但被贵卡的即时回报压过——需要的是"环×贵卡"交互项，不是更大的 RING_W），
  要么承认环题冻结。**今晚不再烧臂**（第三次重复同一否证的信息增量已经薄了）。
- 产物点名（D82）：`v7rb2-31.bak` / `v7rb2-82.bak` / `v7rb2-92.bak`（未 promote）；另有 Q1 系 `v7aim1-{31,81,82,91,92,93}.bak`、Q1b 系 `v7aim2-{31,82,93}.bak`、Q3 系 `v7rb1-{31,82,92}.bak`。

## 6. 早报告（给 DS + 用户 · 一晚的全部delta）

**代码 delta（分支 `qoder-research-0920`，main 未动）**：
1. `pickAimDefender` 教师 + `EPIRUS_RING_W` 接 econ-env（默认关 ⇒ 出厂逐字不变）+ `RING2_HOT` 真热启动开关；
2. G6 读回跨行修复；**G4IMPL 实现身份双绑**（阳性对照过：改一格实现 ⇒ POOL 不变/IMPL 变 ⇒ 冻结豁免作废）；
3. **查清一条账**："NP 臂热启动=最新冠军"是假的（runner 覆写 env+恒拷 BASE；全部近期臂与线上包同谱系 d13d…）。
   HANDOFF §1.2/§2.2 与 `train-best.mjs:61` 的注释需要按 §5 改写。A/B 可比性没坏。

**实验 delta（13 个包，全部 .bak 未 promote，线上包 sha 三度核对未变）**：
- **Q1 模仿教师 = 今晚的正面结果**：产出全仓第一个**攻击解**过 G4 的包（v7aim1-82 只枪 5%/8% · 打枪手 5.00 · 枪手 0 余血 · 场B 0.80 · 靶向 48%），且 Q1b 接力 **3/3 保住行为**（0%/3%）——"瞄准"这个基因**买得到、传得下去**。代价：G 2.0-2.8（窄）、新最克格"瞄威胁者"75%。
- **Q3 = 否证结案**：环×贵卡共存两代 0/6，贵卡长出之日即环死之时（抢同一 ep 预算）。要共存需要**交互项**，不是更大的系数。
- **新吸引子入账**：CLEAR_W 的"长寿刷收缩"被动解（95-102 回合/局刷天火）——判据草案在 §5e-补，属裁定件未动手。

**待裁定/待办（明早的球在谁手上）**：
1. 读 §5（热启动账）并改写 HANDOFF/`train-best.mjs` 注释；
2. 要不要把 v7aim2-82 当**种子**继续第三接力（教师占比降到 0.25 换广度？）——攻击解已可接力，问题只剩"过全表"；
3. "收缩刷分"判据要不要立（草案 §5e-补）；
4. §4-2 按"带证据的否证"结案（措辞可用 §5f）；
5. createState N<3 过滤 + Q2 形状适应度：设计在 REVIEW-QODER §4，今晚没做（都动指纹/大改，留人有）。

## 5g. Q1c（01:5x · 预注册跑前写死）：降教师占比 0.5→0.25 + 热启动 82，赌"广度回血、行为保住"

唯一变量 = `EPIRUS_IMIT_FRAC=0.25`（对 Q1b）。seeds 31,82,93。产物 `v7aim3-{31,82,93}.bak`。
- **通过**：≥1/3 seed 的**只读体检全绿**（五道门 G≥3/座位<30/墙/场A/场B + G4 全表两模式 ≤60% + G5）。
  ⇒ 若出：它就是**换包候选**，但**我不 promote**——写进早报告由用户/DS 裁定。
- **否证**：3 个都过不了 ⇒ 结论=模仿通道只产专才（教条化），通才必须走 Q2 形状适应度（设计稿见 REVIEW-QODER §4-5/§5d）。
- 中间态（G4 保住但 G 仍 <3 或座位红）：如实报"哪一项挡的"，这本身是"教条 vs 广度"的剂量响应数据。

## 5h. Q1c 终裁 + 全晚机制总账（02:0x）

**Q1c（教师 0.25 + 热启动 82）**：31 只枪 long 回潮 75%；82 滑向**经济刷屏吸引子**（energy 90.7%、95 回合、场B 0.07）；
93 = 全晚最完整的一粒：只枪 10%/8% ✓ · **G 3.27 ✓** · 场A 28% ✓ · 场B 清场 1.32 ✓ · 墙 10.4 ✓ —— 但挂 座位 33pt ✗ + **新最克格「瞄威胁者(1ジ枪)」68%/78%** ✗。
**判据裁定：未过（无一粒全表绿）；但"教师占比↓ ⇒ 广度↑"的剂量响应成立**（G：2.00→2.76→3.27 三代接力 / 同一起点）。

**全晚总账（15 包 · 6 臂/探针 · 3 度线上包 sha 核对未动）**：
1. 「瞄准」基因可买、可接力、可加宽度（教师剂量减半三代 G 单调回升）——**模仿通道是有效训练面**；
2. 但 G4 八格是**跷跷板**：修好"只枪"必冒"瞄威胁者"（aim2-82 75% / aim3-93 68/78% 同族复现），
   单教师单格解法到不了"过全表"；
3. **下一步性价比最高的一根（未跑，交接）**：用**已存在的分段教师课程**（D85 机制在 evo.js）——
   段1=`pickDeepSaver`（攒钱/贵卡面）→ 段2=`pickAimDefender`（瞄准面），配方其余照 Q1c。
   它正面检验"跷跷板是单教师教条化造成的"这个解释；一晚一臂（≈12 分钟）就够出结论。
4. 环×贵卡共存两代 0/6（§5f）；CLEAR_W 有第二种被动解=长寿刷收缩（§5e-补，判据草案待裁）。
5. 账目更正（§5 热启动）+ 两处小修（G6/G4IMPL）+ 新旋钮 `EPIRUS_RING_W`（默认关）+ `RING2_HOT` 全部入分支。

**分支收尾**：`qoder-research-0920` @ 本 commit；np-test **154/154** · spec 45/45 · smoke OK · 指纹 00e732a7（未动引擎）。
main 一字未动，线上包/2P 包未 promote。早上顺序建议：先读 §5（账）→ §5h-3（分段课程臂）→ 裁 §5e-补（刷收缩判据）。

## 7. 白班 1h 补充（09:5x 起 · 读 RESEARCH-QUEUE 后开工）

- **P1 已开跑**（v7cur · 分段计划 `pickDeepSaver:0.5,pickAimDefender:0.5` · frac 0.25 · HOT=v7aim2-82 · seeds 31,82,93）。
  通道核实：`[imit] 主线程生效值 ... plan=... 段数=2`；worker 走 `msg.imitPlan` 回执（v1.5.97 设计），无 A/A 半开。
- **P4 同步在跑**（`scripts/p4-seat-phase.mjs`：3 盐配置 × 3 seed base × 120 局自对局，82 与线上包对照）。判读规矩：
  **换 base 后最差席位稳定同一格 ⇒ 相位；乱跳 ⇒ 噪声**（"极差 35pt"是 n≈91 的标量，先证伪它是不是抽样）。
- **P2 设计稿（不动手，等 P1 否证再花训练成本）**：项 = `S4_W · min(1, 打在脚本席伤害/局 ÷ 4.0)`，装配复用 V4 形状
  （4 席家族 vs 1 席 `pickGunFocus`）；**判据用伤害列不用胜负标量**（§0-5 与 v7gf1-92 的教训）。
  单一来源的正解是把装配**下沉到 `js/train/evo.js`**、让 `tools/v2v4-lib.mjs` 反向取用（deps 已有 T/S/Play/R 通道），
  否则就是 D107 禁止的第二份拷贝。默认 `S4_W=0` ⇒ 出厂逐字不变，走 econ-env 单一来源 + D77 往返。

## 8. P1/P4 终裁（10:3x · 白班一小时收工）

**P1 课程臂：预注册判据未过（0/3 全绿），但把"跷跷板"图完整画出来了**
| 包 | 只枪 L/M | 瞄威胁者 L/M | 五道门挂点 |
|---|---|---|---|
| v7cur-93 | **20/10 ✓** | **33/10 ✓** | **行为门 G4/G5 全表首次两模式绿** + 场A 34 ✓ 场B 0.8 ✓ 座位 22 ✓ —— 挂 墙 0.00 + G 2.88 |
| v7cur-82 | 15/10 ✓ | 23/15 ✓ | G 6.44 ✓ 场A 26 ✓ —— 挂 座位 44pt（**0 号座 54%**，方向反了）+ 墙 0 + 场B 0.00 |
| v7cur-31 | 100/100 ✗ | 0/3 | 退化（教师接力断链） |
- ⇒ "跷跷板=单教师教条化"这条解释**不成立**（课程并没有让两格同时绿的代价消失，只是把失败点挪走）；
  **但 93 是全晚最接近出货的一粒**：它证明了"G4 全表两模式绿 + 场A/B + 座位"可以**同时**成立，只差墙与广度两项，
  而这两项是**已有明确处方**的（破墙=剑/坦/电磁炮面；`wallFilter` 旋钮现成；广度=DIV_W/形状法）。
- **三粒齐挂"反弹墙 0.00"不是巧合**：课程把梯度全押在"瞄准+枪"轴，穿透卡（剑/坦）归零 ⇒ 墙轴要进课程第三段
  （或 P2 判据里把"打在墙席上的穿透伤害"单列）。**建议下一臂（不今晚）**：三段课程 deepsaver→aimdefender→(剑/坦破墙线)，
  起点换 v7cur-93（它其余项全好）。

**P4 座位相位诊断（`tools/probe-seat-phase.mjs`，只读已入库）：不是相位，是结构性座位泄漏现行**
- `v7aim1-82` 长程自对局 3 盐配置（gate 口径 / 每局随机盐 / 盐钉死 0）× 3 独立 seed base × 120 局 = **360 局，0 号座 0 胜**，
  其余四席 13~30% —— **换盐不变相** ⇒ 排除 slotSalt 相位；线上包对照 9~27% 无此悬崖。
- 且 cur-82 的偏座**方向相反**（0 号座 54%）⇒ 方向随训练随机、悬崖随瞄准训练而来 ⇒
  最可信机制：**策略网络对"绝对座位"有可用通道**（候选枚举顺序/特征里的座位项在镜像局里被网络当身份用），
  属 D50/D58"座位身份通道"家族的**新现行**（那次是对手槽位并列按 pid 升序，这次疑似在 featuresV7/候选槽位）。
- **可修性判据（给 DS 的下一步，分钟级）**：把 82 的 featuresV7 输出在"座位置换后的同一局面"上做等变性检查
  （若特征随绝对座位改变而网络行为系统性改变 ⇒ 定位泄漏通道）；修掉它可能同时解掉 Q 系/课程系的"偏座"阻断项。
- 影响面：这直接威胁**所有**以 82 系热启动的臂的座位读数 —— "极差 35pt"不是噪声，是真的。

**白班收工账**：v7cur-{31,82,93} 未 promote · `probe-seat-phase.mjs` 入库 · D82 记账 · np-test **154/154** · 线上包 sha 未动 · 已推 origin。

## 9. ★★ P4 追到底：抓到**座位身份泄漏的现行犯**（`policy.js:397` 的 falsy bug）并修复

**取证链**（全部只读探针，脚本在工作区 `scripts/p4*-*.mjs`）：
1. `featuresV7` 等性价检查：对称局面五座状态特征**逐位相等**（状态块干净）；
2. 行为取证（`probe-seat-phase` 口径）：v7aim1-82 镜像自对局 **120 局 × 3 盐配置，首死 120/120 全是 0 号座**；
   单局 trace：R3 四席同时狙 0 号（一发"爆头"还是无来源伤害记在 0 头上），0 号一回合吃 5 伤阵亡；
3. **等性价对打进候选层**：`(座1→0)` 与 `(座0→1)` 的 `actionFeatures` 在 dim16/dim21 不等；
4. 读码定案：`const tid = cand.target…; if (!tid || !MASK.target) 推 6 个 0` —— **target=0 号座被 `!tid` 判成"无目标"**，
   所有座位看"打 0 号"的候选都拿到"hp0/ep0 软目标"全零块 ⇒ 座位身份经由候选特征进网络；
   瞄准型策略（目标分差全靠目标块）受害最深，线上包轻度（此前 10~18% 的 0 号座读数其实是这条通道+噪声）。

**修复**：`!tid` → `tid == null`（一处）。守门 **D108**（镜像等性价对 + **不对称局面**的对照组；
我第一版对照组用对称局面被自己的门打了红——对称局面上不同目标特征**本应全等**，那条断言把真对称当假修法）。
**修复后**：82 镜像首死悬崖消失、席位胜率极差回到 4~7/60（噪声档）；`v7cur-93` 座位 22pt → **10pt ✓**（G4 全表仍绿）。

**语义变更的连锁（按 R61/v1.5.129 同款流程走完）**：指纹 `00e732a7 → a78c28cb`；
两包 meta 重记（权重逐字节未变：bundle 各 1 行 diff = meta）；2P `promote-champion2p` ✓ · 3P `--force` 重记（exam 0.563→0.569）；
np-test **155/155（+D108）· spec 45/45 · smoke OK · battle-test OK**。
⚠️ **在旧语义下训出来的全部 v7 包现在都是"带伤运行"**：行为会小幅漂移（cur-93 场B 0.8→0.03 就是一例——它的"瞄准"部分是靠零块捷径学出来的）。
⇒ **给 DS/用户的裁定件**：① 是否把 a78c28cb 合回 main（= 承认一次语义修 + 后续臂全部在新语义下跑）；
② 新臂（三段课程/形状法）一律修复后重训，旧 Q 系产物的"攻击解"结论要在新语义下用**同一配方复跑一遍**才算数（成本 12 分钟）。

## 10. 下午班第二段（14:2x-）：新语义复验 + 三段课程 + P3 回扫

**Q1f 复验（Q1 配方逐字重跑于 `a78c28cb`）——昨晚结论不仅成立、还更强**：
| 包 | 只枪 L/M | 瞄威胁 L/M | 打枪手 | 判读 |
|---|---|---|---|---|
| v7aim1f-31 | **15/28 ✓** | 33/5 ✓ | 4.30 | 双格绿（旧语义同配方是 100%/退化） |
| v7aim1f-82 | 100/98 ✗ | 33/18 | 0.50 | 退化（seed 面方差，与旧语义下 82 出解形成对照——修复改变了吸引子版图） |
| v7aim1f-93 | **38/28 ✓** | 0/0 ✓ | 4.35 | 双格绿 |
⇒ "瞄准基因可买"不是零块捷径的产物；同时 **seed 方差比旧语义更大**（旧语义下零块是所有人的公共捷径 ⇒ 行为更一致地坏）。
新语义下座位公平对照读数：线上包镜像极差 3~5/60（修前 8~19），v7aim1f-31 6~10/60。

**P1' 三段课程（deepsaver→aimdefender→breakDef · HOT=v7aim1f-31）**：
| 包 | 只枪 | 瞄威胁 | 墙 | G | 座位 | 场A | 场B清场 |
|---|---|---|---|---|---|---|---|
| **v7cur2-31** | **15/8 ✓** | 33/3 ✓ | **2.15 ✓（剑43）** | **6.4 ✓** | **17pt ✓** | 26% ✓ | **0.00 ✗** |
| v7cur2-82 | 100/48 ✗ | 18/18 | — | — | — | — | — |
| v7cur2-93 | 83/48 ✗ | 43/15 | — | — | — | — | — |
⇒ 三段课程**把五道门里的四道一次做绿**（史上第一次），唯一红=场B 清场 —— 而它的自对局是 73.1 回合/局 + 0.27 伤/回合，
**正好落在 §5e-补"长寿刷收缩"草案判据的怀里**（≥70 且 <0.35）：它不是"会清场的不打"，是"靠收缩赢"。
⇒ 与 P3 判据互为表里：**如果立"刷收缩"折扣，v7cur2-31 会连场B 的 0 都遮不掉；如果不立，它就是最接近全绿的候选**。
   这条裁定件现在有了具体的、可读的名字。
- 产物点名（D82）：`v7aim1f-{31,82,93}.bak`、`v7cur2-{31,82,93}.bak`（全部未 promote）。
- P3 只读回扫（全 .bak × 长程自对局 20 局的 回合/伤每回合）在 `scripts/p3-shrink-scan.mjs` 跑，结果见 §10-附（跑完补）。

## 10-附. P3「刷收缩」只读回扫结果（14:5x · 677 个 .bak · 长程自对局 20 局/包）

**草案判据（≥70 回合 且 <0.35 伤/回合）命中 148/677 = 22%。** 关键切片：
- **阳性对照成立**：`long-33.bak`（v1.5.7 那次"20/20 零伤害全平"的病源包、反摆烂设计的起源）命中（104 回合 / 0.00）——
  草案抓得住**已知最坏样本**。
- **现役与历届上线包不命中**：`v7press3-91`（现役）· `v7f5t-31` · `v7wall1-93` 均 0 命中 ⇒ 立此门**不会追溯推翻任何已发货冠军**。
- **但它会判掉我今天的明星候选**：`v7cur2-31`（82 回合 / 0.330）命中 ⇒ "四门全绿的候选"与"刷收缩判据"**互相锁死**——
  立折扣，cur2-31 归入刷分包；不立，它就是史上最全绿候选。**这条裁定没有中间地带，用户拍板即可。**
- 命中面的大头是**历史臂产物**（ms2/ring2/v7big1/v7aim/v7ws 各系）——它们从未上线，追溯改判只影响"当年结论的读法"，
  不影响产品。⚠️ 但注意：v7big1-31/82 命中说明**贵卡奖励臂也在往长寿方向滑**（它们的大雷出手 3.5% 是在 97+ 回合的局里放出来的）。
- 脚本 `scripts/p3-shrink-scan.mjs`（工作区）；原始输出 `docs/artifacts/p3-scan.out`（gitignored，148 行名单在里面）。
- 方法论注记：回扫是在**修复后语义**（a78c28cb）下跑的——旧语义下的零块捷径会让部分包的"打 0 号"行为失真，
  所以这 148 个名单是"如果今天重新体检"的口径，不是历史读数的复算。

## 11. Q4 四段课程臂（15:2x · 预注册跑前写死）

配方 = cur2 **逐字同**，唯一变化 = 计划加第四段：`pickDeepSaver→pickAimDefender→pickBreakDef→pickBeadBurst`（各 0.25），
`RING2_HOT=docs/artifacts/v7cur2-31.bak`（从"四门绿、两票红"的候选接力）。seeds 31,82,93。产物 `v7cur3-{31,82,93}.bak`。
- **通过**：≥1/3 seed 的只读体检**零阻断**（五道门 + G4/G5 全表两模式 ≤60%）⇒ 它就是"史上第一个全绿候选"，交用户裁定是否上线（我不 promote）。
- **部分通过（记录用）**：G4 最克格从"珠爆发"挪走 **且** 花珠率 >0 ⇒ 第四段起作用了；场B 清场 ≥0.3 ⇒ 经济出口长出。
- **否证**：三粒的珠爆发格仍 >60% 或花珠率仍 0 ⇒ "示范花珠"买不动这个面，经济出口问题升格为**形状问题**（只剩 P2）。

## 11-附. Q4 终裁（15:4x）：预注册"零阻断"未达（0/3），但画出了完整的"三角跷跷板"

| 包 | 五道门 | G4/G5 | 珠爆发格 | 花珠率 | 一句话 |
|---|---|---|---|---|---|
| **v7cur3-82** | **✅ 五道全过**（座位 13/G 4.0/墙 1.1/场A 31/**场B 清场 4.0/局=全仓历史最高**） | ✗ 只枪 90% · G5 防席 30% | **3/0 ✓** | **0.0%** | 第一个五道全绿的包，红在行为门 |
| v7cur3-31 | ✗（只枪回潮） | ✗ | 33/20 ✓ | 未查 | 同方向 |
| v7cur3-93 | ✗ |  | 58/8 ✓ | 未查 | 瞄威胁 60/58 边缘 |

**三个可注册的读数**：
1. **第四段治好了珠爆发格（3/3 seed ≤58），但花珠率仍 0.0%** ⇒ 包学会的是"**躲珠线**"不是"**用珠**"——示范覆盖能改"挨打面"，改不动"花钱面"（与 §5f 环贵卡互斥、第6道"未闭环"连号红同一条病）。
   ⇒ 预注册"部分通过"按**花珠率>0**的口径算**未达**。
2. **课程段是覆盖式的**：第四段把第一/二段的"打枪手"密度洗掉了（cur2-31 只枪 15% → cur3 三粒 80~93%）。
   分段计划 = 连续窗口 + 各自退火 ⇒ 后段重画、前段褪色。
3. **三角跷跷板成型**：{主动清场（场B 4.0）} ↔ {按住只枪（打枪手 4.5+/局）} ↔ {防珠线} 三点，
   九臂以来每个包只能同时占两条边（cur2-31：枪+墙+场B红；cur3-82：场B+墙+只枪红；aim 系：枪+珠红墙红）。
   **教师课程穷举排列也跳不出这个三角**——它缺的不是示范顺序，是**同时给三条边算分的适应度**。
   ⇒ 队列结论升级：P2（形状/多目标适应度）从"下一根杠杆"变成"**唯一剩下的杠杆**"。
   具体处方：把 `duelAssembly` 的三列（打在脚本席伤害 / 场B 清场 / 珠线承伤）做进 fit（各配 env 权重、默认 0），
   而不是再加第五段教师。
- 产物点名（D82）：`v7cur3-31.bak` `v7cur3-82.bak` `v7cur3-93.bak`（未 promote）。
- 今晚臂数已满（6 臂 + 3 探针 + 1 回扫），线上包 sha 五度核对未动。P2 是代码+训练双改，建议交 DS 掌刀或明晨我接。

## 12. P2 形状适应度已实现（16:3x）+ Q5 臂预注册

**实现**（全部默认关 ⇒ 出厂逐字不变；不触指纹，无需重记）：
- `EPIRUS_S4_W` 走 econ-env 单一来源（D77 往返自动覆盖）；evo `scoreMemberN` 收尾加 `S4_W × 评分器(0..1)`；
- 评分器 = `server/shape-scorer.mjs`（宿主注入 worker 与 server 两处沙箱的 `__shapeScorer`）——
  复用 `tools/v2v4-lib.duelAssembly`（D107 单一来源不破）；口径 = V4 形状下对 `pickGunFocus` 与 `pickBeadBurst`
  两条外部线的 `dmgToScriptedPerGame/4.0`（封顶 1）取平均 —— **奖励"把火力压到威胁身上"这个后果，不奖励赢**；
- 断线必抛（S4_W>0 而评分器缺线 ⇒ 抛错，不留 A/A 静默路）；
- **顺带抓出一个既存泄漏**：D77 的 `wallFilter=true` 哨兵从不被 reset 抹掉 ⇒ 后续所有 scoreMemberN 都活在
  "破墙硬过滤开着"的假世界（D109 的"断线必抛"第一次跑红才把它照出来）——已收进 reset 块。
- 新门 **D109**（默认 0 · 断线必抛 · 真评分器 0..1）；np-test **156/156** · spec 45/45 · 指纹 a78c28cb 不变。

**Q5 臂（预注册跑前写死）**：配方 = cur2 逐字（三段计划 + frac 0.25 + HOT=v7cur2-31），**唯一新增 = `EPIRUS_S4_W=0.15`**（+EPIRUS_S4_GAMES=6 控成本）。seeds 31,82,93。产物 `v7s4-{31,82,93}.bak`。
- **通过**：≥1/3 seed 只读体检**零阻断**（五道门 + G4/G5 全表）⇒ 史上第一个全绿包，交用户裁定上线（我不 promote）。
- **部分通过**：打在枪手身上 ≥4.0/局 **且** 场B 清场 ≥0.3（三角的两条边第一次同时站上）——即便整体仍红，这也是 P2 生效的证据。
- **否证**：三粒都 ≤2 条边 ⇒ 形状项也压不动三角 ⇒ 结论升级为"当前网络/奖励几何下全绿不可达"，交 DS 重新设计。
