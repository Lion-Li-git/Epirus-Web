# 状态灯（09-24 夜 · Qoder · 按 DS 交接 `docs/HANDOFF-FOR-QWEN-2026-09-24.md` 跑一晚）

> 自包含防失忆日志。**每完成一格就更新 DONE/DOING/NEXT + 时间戳**。
>
> ⚠️ **时间戳更正（09-25 00:23 实测钟点）**：H-1…H-8 标题里的"00:05 / 00:20 / 00:55 / 01:00 / 01:55 / 02:00 / 03:00"**是我估的，不是读表读来的** ——
> 真实墙钟由 commit 时间戳可反推：交接文档 `f36db63` = 09-24 23:21，v1.5.205 主体 = 09-25 00:10，§E = 00:18。
> ⇒ 那批时间戳最多跑到真实钟点前面 ~2.5 小时，**先后次序可信、绝对时刻不可信**。从现在起每条都先 `date` 再落标题（这就是 §H-9 记的那条工具账）。
>
> 分支：`qoder-handoff-0924`（基线 = `origin/main` = `f36db63` · v1.5.204 + 交接）。**不动 main、不动两槽线上包、不提交 `results/*`。**
> 本地自证过的基线：`np-test 194/194` · 指纹 `ebdbff36`。
> 结束时刻：**09-25 09:00**。中途不向用户汇报，最后一次性汇报（交接 §5-11）。

## 今晚要回答的问题（DS 交接 §0 的两句）
现役包不是"训练没到位"，而是"**名次适应度 + 当前经济**"的最优解 ⇒ 理想冠军只有两条来路：**改价** 或 **改判据**。
今晚的任务 = 把这两条各自的**可达性**量出来，而不是再抽一次种子。

## 硬约束（照旧 + 交接 §8）
- 不跑 `tools/train-best.mjs`（它默认写线上 2P 槽并改 `index.html` 缓存戳）。
- 不 `promote`（`--dry` 除外）；不换槽；不提交 `results/*`；**永不 `git add -A`**。
- 规则改动只允许在**内存补丁**里 ⇒ 指纹必须仍是 `ebdbff36`（每次提交前 `node tools/rules-fingerprint.mjs` 自证）。
- 新落的 `.bak` 必须在 CHANGELOG 里**逐名**点名（D82 现在双向查：在场未点名 / 点名却不在场）。
- 版本三处一致（D8）：CHANGELOG 首条 `## v` = README「当前版本」= index.html「程序 v」。
- **报数三件套**：点估计 + **n** + **±1.96SE**；阈值不写 n 就不写。

## 预注册（跑之前写死，不许事后挪 · 每条都写"若失败我学到什么"）

### E1 ep 门槛可达性（DS 最高优先 · 不训练）
DS 已量到：决策时刻 ep 均值 0.49~0.76、`ep≥5` 占 **0.0%**、`ep=3~4` 2.4%/0.0%。我要补的是**剩下两半**：
- ①每个决策点"**买得起但没买**"的卡（尤其大雷/电磁炮）②"**买不起但只差 ≤2ep**"的卡；
- ③把大雷单价在内存里改成 **5 / 3 / 2** 三档，量"改价后 `ep≥cost` 的决策占比"。
- **预测（我自己押的）**：`cost=3` 时 `ep≥3` 仍 **<5%**，而"**买得起但没买**"的占比会**远大于**"差 ≤2ep"的占比
  ⇒ 结论将是"**不是够不着，是不想去**"（激励层）而非"钱不够"（价格层）。
- **若失败学到**：若 `ep≥3` 真跳到 >15% ⇒ "改价"是有效杠杆，下一晚该做的是价格扫描而不是判据改动。
- 空枪检测：决策点计数为 0 ⇒ 工具**非零退出**（交接 §5-4「静默跳过 = 关掉测量」）。

### E2 ep 收支结构（不训练）
按卡统计每局 **ep 支出**与**收入来源**（含 ジ 的机会成本）。
- **预测**：支出集中在低费卡高频 + 防御类，**大雷/电磁炮 ≈0**；收入侧 ジ 占比 >50%。
- **若失败学到**：若支出其实集中在贵卡 ⇒ 问题回到"单笔门槛"（E1 的 cost 扫描），不是习惯问题。

### E3 破防场立门（**前置已由我 09-24 上午量完**）
已知好/已知坏已分开：现役 0.00 点/局·0% 存活；`v7aim3-93` 9.59 点·90% 存活·净兑现 3.26（5 种）；`v7divK-31` 8.04·97%；
`v7cmin4-82` 1st 也 100% 但只有 2.48 点·14% 存活 ⇒ **"只看 1st 会放行蹭一发"是实测事实，不是修辞**。
- 今晚做的是：**把门草案连同判别数据一起交出去**，但**不安装**（装了现役包自己就红 ⇒ 属用户裁定）。
- **预测**：线画在"归因伤害 ≥4/局 且 终场存活 ≥50%"时，`docs/artifacts` 的当选带里通过率为**两位数百分比**，且现役必红。
- **若失败学到**：若通过率 >50% ⇒ 这条判据没有区分力（多半是我的幅值口径被"局短"糊了），得先修量具再谈门。

### E4 G4「1ジ枪压制」这一族为什么不修（**我自己加的，不在 DS 清单里**）
动机（实测，非猜）：两粒"既广又会清场"的现成包 `v7aim3-93` / `v7divK-31` 都**只死在 G4[只枪]**（65~74%，n=300），
G5 反而全绿；而我昨晚"上桌"的臂（池子里含 `gunSpam`）也死在同一格（77~84%）⇒ **加对手上桌治的是 G5 那一族，治不了 G4 那一族**。
- 做：跑 `tools/probe-g4-anatomy.mjs` 的 §C 反事实（`forceAttack` = 它选 ジ 而枪买得起时改判成枪；`forceTurtle` = 完全不还手），
  对**现役 + 两粒宽包 + 我一粒上桌臂**各跑一遍。
- **预测**：若"只枪靠吃龟"成立 ⇒ `forceAttack` 让这一格从 65~84% **明显掉**（≥15pt），`forceTurtle` 升到 ~100%；
  且**掉不到 60% 以下** ⇒ 说明这格不是"holding 一个习惯"能修的，需要的是**反制样本进适应度**（min-over-counters）。
- **若失败学到**：若 `forceAttack` 就能把它压到 60% 以下 ⇒ 出路是一个**菜单级"买得起攻击卡时不许无限 ジ"的次优动作闸**（与我昨晚给净化加的那道同族），
  成本极低，值得单独一晚。

### E5 ε 扫描（不训练）
同一只包扫 `ε=0.05/0.10/0.15/0.25`，量 序列率 / 电磁炮率 / 出手 G / 1st / 平局率。
- **预测**：悬崖在 ε≤0.1 就跌满（老账 4.30→0.90）；**且 1st 与 ε 的关系非单调** ⇒ "探索度"不是可白拿的免费午餐。
- **若失败学到**：若悬崖不存在 ⇒ 序列能力缺的是经济/判据（回到 E1/E3）。

### E6 广度当选键复测（**v1.5.199 之后必须重测**）
事实：我给 `cbs1s2-band2` 报的读数（净兑现 3.25 / 破防 4.95 点 / A 考卷 38.9）是"**会空放净化**"那只包的数 ——
净化闸门上线后它 112 次空净化被挡掉、出手 7517→5153 ⇒ 行为已变，旧读数作废。
- 做：六粒 `cbs1s*` 臂 + `co1s8-band1` 在**修好的引擎**上重跑四栏（考卷 / 破防幅值 / 净兑现 / G4+G5@n=300）。
- **预测**：净兑现仍 ≥ 现役（那是权重决定的主趋势），**破防幅值会掉**（净化占的那 3ep 原本在空转，挡掉后钱流向别处）；A 考卷变化 <5pt。
- **若失败学到**：若破防幅值反而升 ⇒ 说明"空净化"当时在**吃掉防御预算**，那 v1.5.199 那道闸不只是省钱，是行为修正 ⇒ 要回头补记 CHANGELOG。

### E7 口径统一（收尾 10 分钟）
座位极差一律按 **n=400** 报（交接 §3.2：同一只包 n=60 读 43.2pt、n=400 读 26.6pt）；顺手量训练侧示范覆盖（残余 60/23/17）。

## DONE（⚠️ 下面 23:40~02:10 这批钟点属**旧估序列**，见文件头更正：次序可信、绝对时刻不可信）
- 23:40 读完交接 + 本地复跑基线（`np-test 194/194`）；确认我的量具与 D135–D142 全在，`probe-beadloop`/`skill-diag` 被 DS 删掉（无代码边）。
- 23:40 确认 E3 的前置（已知好/已知坏）上午已量完，并把 E4 这条**DS 清单外的新实验**写进预注册。
- 00:05 **H-1** E1+E2 出数（新量具 `tools/probe-ep-reach.mjs`）：大雷"可达时成交率 0.0%"vs 电磁炮 32.0% ⇒ 改写交接 §3.1。
- 00:20 **H-2** E4：`v7divK-31` 被 forceAttack 救回（80%→15%）⇒ 我的预注册被推翻；现役同招更差；§D 抓到"池线≠门线"。
- 00:55 **H-3/H-6** E5：先踩"温度≠探索率"的坑（METHODOLOGY 50），改成真杆后量到 **评测口径 vs 产品口径** 的巨大分歧（破防场 100% 平 vs 12.5% 平）⇒ **改写交接 §2 第 3 条**（METHODOLOGY 49）。
- 01:00 **H-5(草)** E3 门草案成文（G7 破龟群，**未安装**）。
- 02:00 **H-4/H-7** E6 复测：`cbs1s2-band2` 从"零阻断"变成被 `G<3` 挡下 ⇒ 抓到"广度读数把空净化算一张卡"这个洞（METHODOLOGY 51）。
- 02:10 修 `probe-g4-anatomy` 的过期期望值（自我否定的自检）+ 门钉升级（METHODOLOGY 52）；CHANGELOG **v1.5.205** + 三处版本一致。

## DOING
- 02:12 **E9 四格配方臂**并行跑中（`sp77a/b/c/d` · seed 77 · 300 代 · band 目录全在 `mktemp` ⇒ 仓库不新增 `.bak`）；跑完用 `probe-defense-cause --saver=hold` 量倍差。

## NEXT（按顺序）
1. ~~拆 §51 那半没量到的东西~~ ⇒ **做完并被推翻**（H-8）。
2. ~~E7 口径统一~~ ⇒ **做完**（H-10 座位 / H-11 示范覆盖），并长出新账：评测层 13 处 ε=0 写死（H-9）。
3. ~~把配对法搬到别的栏~~ ⇒ **做完并超出**：`probe-layer-caliber`（H-12）+ `probe-breadth-flip`（H-13/H-16）+ G7 草案按口径重读（H-14）。
4. ~~"攒钱→防御"那条病~~ ⇒ **查到凶手 + 定住因果 + 落到玩家代价 + 换种子复现**（H-15~H-24）；体质占 13/41 权重（32%）。
5. **E9 归因**（H-25 预注册）：四格跑完量倍差 ⇒ 判"上桌"这根旋钮是不是在养这体质；结果无论朝哪边都要落 §H-26。
6. 早报（一次，最后）：**第一条就是那只候选的病**，然后是口径三账 + 广度线 32% 被口径挡 + 门草案 + 明早该裁定的清单。

## H-1 00:05 · E1+E2 出数了，而且**改写了交接 §3.1 的结论**（新量具 `tools/probe-ep-reach.mjs`，200 局/场）
现役 `v7cmin4-31` · multi · 5 人 · 受评席决策点 n=1736(pool)/9400(guardwall)/4871(mirror)：

| 场 | 决策时刻 ep 均值 | `ep≥5` 占比 | 大雷**真放出去** | 电磁炮：可达时成交率 | ジ 占比（=holding） | ep 支出结构 |
|---|---|---|---|---|---|---|
| 脚本池 | 0.81 | **0.1%** | 0.0% | 100%（n=8，太小） | 64.0%（其中 35.0% 枪买得起） | 狙击 58.3% · 枪 37.0% |
| 破防场 | 0.49 | **0.0%** | 0.0% | 卡根本不在合法表 | 51.1%（**枪买得起 0.0%**） | **枪 100.0%** |
| 镜像场 | **8.23** | **44.8%** | **0.0%** | **32.0%**（n=299） | 78.2%（其中 81.2% 枪买得起） | 狙击 40.2% · 枪 23.2% · 蓄能 22.1% · 电磁炮 13.2% |

- **交接 §3.1 说"99% 的决策时刻够不着门槛"—— 只在脚本池/破防场成立**：镜像场里 `ep≥5` 占 **44.8%**、`ep≥3` 占 60.4%、改价到 2ep 则 72.2%，**而大雷真放出去始终是 0.0%**。
  ⇒ **决定性读数是"可达时成交率"：大雷 0.0%，同一批决策里电磁炮 32.0%** ⇒ 不是"够不着"，是"**这档钱宁可攒着也不放大雷**"；同场同钱的另一张卡它会用。
  ⇒ **改价买不到大雷**（cost 5→3→2 三档，成交率一直 0.0%）——这正是我 §E1 预注册押的方向，且比 DS 的表述更强：**门槛可达性随场地从 0.1% 跳到 44.8%，所以"够不够得着"根本不是一个数**。
- **破防场给出了一个机制（不是意愿）**：收入 4800ep、支出 4600ep 里 **100% 花在枪上**，`ep` 永远停在 0~1（100.0% 的决策 `ep<2`），
  而"选 ジ 时枪买得起"= **0.0%** ⇒ 它在破防场跑的是一个 **`ep=0→ジ、ep=1→枪(必被挡)` 的极限环**，**结构上永远攒不到穿防卡的 2~3ep**。
  ⇒ 交接 §4 的出口 (a)"改收入"由此从算术推断升级为**有机制支撑**；而"改大雷单价"被证伪（0.0% 成交率与单价无关）。
- 镜像场则是反过来：它**真的在攒**（ep 均值 8.23、78.2% 的决策按ジ），攒到 5ep 也不放大雷 ⇒ "愿不愿意攒"和"攒来干什么"是两个独立的问题。

## H-2 00:20 · E4（G4「1ジ枪压制」解剖，DS 清单外我加的）—— 两件事：**一条格可以单点修**，以及**"上桌≠过门"的机制被抓到了**
`tools/probe-g4-anatomy.mjs` §C 反事实（同 seed 同轮座，只改一个决策规则），60 局/格：

| 包 | 只枪线胜它 long/multi | forceAttack 后 | Δ | forceTurtle 后 |
|---|---|---|---|---|
| 现役 `v7cmin4-31` | 38% / 35% | 45% / 35% | **+7 / 0（改不动，甚至更差）** | 100% / 100% |
| `v7aim3-93`（既广又会清场） | **7% / 12%** | 13% / 13% | +6 / +1 | 100% / 100% |
| **`v7divK-31`** | **80% / 62%** | **15% / 23%** | **−65 / −39** | 100% / 100% |
| `cbs1s2-band2`（净化闸后） | 20% / 2% | 12% / — | −8 | 100% |

- **预注册被推翻（好事）**：我押"forceAttack 最多掉一点、掉不到 60% 以下 ⇒ 需要 min-over-counters"。实测 **`v7divK-31` 从 80%→15%**（long，n=60，直接掉到线下 45pt）
  ⇒ **"它被只枪吃"确实就是"它买得起枪还在攒着"这一个习惯**，单点可修；但**同一招对现役是 +7pt（更差）** ⇒ 这不是"该装的规则"，而是**每只包的弱点各不相同**。
- **G4 八格里"1ジ枪族"其实不是一条线**：`v7aim3-93` 的「只枪」只有 7%，可它被「瞄威胁者(1ジ枪)」打 73%；`v7divK-31` 反过来。
  ⇒ **修一格不等于修一族**，min-over-counters 仍然是正解，但今晚多了两个具体数字（谁死在哪格、单点干预能给多少 pt）。
- **抓到"上桌≠过门"的机制**（§D 段）：训练池里的 `Bots.pickGunSpam` 与门禁 G4 的「只枪(打最肥·击杀优先)」**同一条包上差 −37pt / −24pt / +17pt / −7pt（方向随包变）**
  ⇒ 我昨晚"加了 `EPIRUS_COUNTER_OPPS` 仍然死在只枪 77~84%"现在有了解释：**池子那条线和门禁那条线不是同一条线**。
- ⚠️ **给 DS 的一个工具 bug（真）**：`probe-g4-anatomy.mjs` 的 §A 自检把期望值**硬编码成 75%/62%**，而现役实测 **38%/35%** ⇒
  对线上包永远打印「⛔ 复现失败 ⇒ 下面的读数先别读」，等于**每次跑都先自我否定**（读数值本身是对的，与 gate-drafts n=300 一致）。
  这就是 §N39 那条"引用注释里的历史数字要当场复跑"的**同一颗雷长在工具里**。我来修（tools 面，不动判据）。

## H-3 00:55 · E5 先踩到一个**杠杆陷阱**（值得单独记，因为它会让整晚白跑）
`probe-ideal-champion.mjs` 里那个写死的 `0.15` 是 **`policyChooserN(params, temp, …)` 的第 2 个参数 = softmax 温度**，
而交接 §4-E5 要扫的"ε 悬崖"是**第 3 个参数 eps**（`evo.js:496` `state.rng.next() < eps` ⇒ 在候选里均匀抽，`epsK` 默认 top-5，只有浏览器传 eps>0 才生效）。
⇒ 我第一版把 `--eps` 接到了温度上，扫出来 **几乎不动**：镜像场电磁炮 0.47/0.43/0.43/0.44、序列率 0.47/0.43/0.43/0.44（temp=0.05/0.10/0.15/0.25）。
   这**不是"悬崖不存在"**，是**拉错了杆**。已把两个旋钮拆开（`--temp` 与 `--eps`，默认 `temp=0.15, eps=0` ⇒ 历史读数逐字不变），重扫见 H-5。
   ⚠️ 给 DS：这个陷阱长在工具里（只有一个 0.15、名字读起来像 ε），谁按交接做 E5 都会掉进去 ⇒ 建议把参数名写成 `--temp`。

## H-4 01:00 · E6 复测（净化闸门之后 · 评测种子 880001 · 240 局/格）
| 包 | 破防 1st / 归因伤害 / 终场存活 | 头对头 vs 现役 | 净兑现 |
|---|---|---|---|
| 现役 | 0.0% / 0.00 / 0% | 18.8% | 2.66（3 种） |
| `cbs1s2-band2` | 100% / **4.89 / 83%** | 15.8% | **2.88（4 种）** |
| `cbs1s5-band1` | 100% / **9.80 / 94%** | 27.5% | **3.04（4 种）** |
| `co1s8-band1` | 100% / 8.72 / 80% | **34.2%** | 2.03（4 种） |
| `cbs1s3-band3` | 99.6% / 2.17 / 2% | 32.9% | 2.66（3 种） |
| `cbs1s4-band3` | 0% / 0.00 / 0% | **44.6%** | 2.01（4 种） |
- **预注册"破防幅值会掉"被推翻（好）**：`cbs1s2-band2` 4.95→**4.89**（不同评测种子，配对证据是事件签名那次：出手 7517→5153、空净化 112→0）
  ⇒ 说明那 112 次空净化**没有在吃它的防御预算**；被挡下来的 3ep 流向了别的卡，净兑现 3.25→2.88（仍 > 现役 2.66）。
  ⇒ v1.5.199 那道闸对这只包是"省钱 + 略降广度"，不改它的核心能力。
- **`cbs1s5-band1` 现在是四栏最均衡的一粒**：破防 9.80 点·94% 活着 + 净兑现 3.04（4 种）> 现役 + 头对头 27.5% > 现役 18.8%。
  但它的 G4[只枪] 需要重测（昨晚同族 `cbig1s5` 是 77%/84% FAIL）⇒ 正在跑。

## H-5(草) E3 的交付物 = **一条门草案，但不安装**（安装属判据改动 ⇒ 等用户点头）
**G7「破龟群」（提案）**：装配 = 1 席被测 + 4 席 `Bots.pickGuardSpam` · multi · 5 人 · **240 局**；
判据 = **受评席累计有归因伤害 ≥ 4.0 点/局 且 终场存活 ≥ 50%**；现役按 v1.5.18 惯例进**参照行**（它 0.00/0%，必然红，但不阻断候选）。
- 判别力（实测，不是设计推测）：124 粒当选带里 **22 粒（18%）过线**；已知好 `v7aim3-93` 9.59/90%、`v7divK-31` 8.04/97%、`cbs1s5-band1` 9.80/94%、`co1s8-band1` 8.72/80%。
- **为什么不能只看 1st**：`v7cmin4-82` 在同场读 **1st 100%**，但只有 **2.48 点/局、终场存活 14%** ⇒ 它是"蹭一发然后跟四只龟一起被终局收缩清场、按伤害判胜"。
  另一粒极端反例 `cbig1s1-band4`：1st 100% / 1.00 点 / 存活 0%，而 **G5 里一只龟能赢它 86~89%** ⇒ 三条读数（1st / 幅值 / G5）合起来才立得住。
- 与"上桌"的关系（这是它可执行的关键）：普通训练桌上没有不还手的防御者 ⇒ 这条门**装上去 82% 的现有臂会红**；
  但 `EPIRUS_COUNTER_OPPS=1` 的臂是 **7/9 过线** ⇒ **"立门 + 上桌"是配套的**，只立门不装桌 = 要求一个池子里普遍不存在的能力。

## H-6 01:55 · E5（真·探索率，含 `epsK`/`epsMode`）——**今晚最要紧的一条是它把我自己和 DS 的口径都纠正了**
现役 `v7cmin4-31` · 5 人多人的**破防场**（1 席被测 + 4 席只防御不还手）· 120 局/格：

| 口径 | 平局率 | 受评席**绝对**胜率 | 终场存活 | 镜像场 电磁炮/序列 次/局 | 镜像 蓄能/局 |
|---|---|---|---|---|---|
| `eps=0`（**门禁/评测口径**，我昨晚与 DS 交接 §2 第 3 条用的都是它） | **100.0%** | 0.0% | 0.0% | 0.43 / 0.43 | 1.54 |
| `eps=0.2, k=5, soft`（**产品口径**：`ui.js:464` 就是这一行） | **12.5%** | **87.5%** | **40.8%** | 0.18 / 0.18 | 0.34 |
| `eps=0.2, uniform`（v1.5.139 被否的那代） | 44.2% | 55.8% | 9.2% | 0.26 | 0.76 |
| `eps=0.4, k=5, soft` | 3.3% | 96.7% | 48.3% | 0.08 / 0.08 | 0.12 |

- **纠正**：「现役在破防场 0% 胜 / 100% 平」**是 ε=0 的评测读数**，不是玩家会看到的局面 —— 产品在 ε=0.2 soft 下同一场 **87.5% 胜 / 12.5% 平 / 40.8% 局终还活着**。
  ⇒ 交接 §2 第 3 条「会清场 ✗」在**产品口径下已达标**（阈值 ≥50% 胜）；我昨晚那一整条"破防叙事"（K-4~K-11）的**绝对值**同样要按这个口径打折，**相对排序不变**（所有包都在 ε=0 上量）。
- **悬崖是真的**，而且量级与老账一致：序列/贵卡侧 `0.43 → 0.18 → 0.08`（ε=0/0.2/0.4 soft）；而**反拖平侧是同一条杠杆的收益**：平局 `100% → 12.5% → 3.3%`。
  ⇒ 交接 §0 的"两条来路（改价/改判据）"**漏了第三条：运行时探索的形状与大小**。
- **形状比大小更值钱**：同为 ε=0.2，`soft` 把平局压到 12.5% 而 `uniform` 只到 44.2%、终场存活 40.8% vs 9.2%
  ⇒ v1.5.141 用户那次"降 ε + 加 `epsMode='soft'`"的裁定，**今晚第一次在破防场上量出它的价值**（当时只看了防御率/集火率/胜率）。
- **代价如实记**：soft 换来了反拖平，但序列率从 0.43（ε=0）掉到 0.18 —— **不是免费的**；要"既会序列又不拖平"仍需目标函数层面的东西（E1/E2 的结论不变）。

## H-7 02:00 · E6 的收尾：净化闸门上线后，`cbs1s2-band2` 从"零阻断"变成**被广度门挡下**
`promote --dry`（在修好的引擎上重跑，n=300 默认）：
- `✅ 候选自己全过`（G4[long] 最克 瞄威胁者 33%；G5 1%/0%）+ 2 格 **UNRUN:弱**（G4[multi] 基线只有 5% ⇒ DS 的 v1.5.203 处置法判为"强包形状"，只记录）
- **但阻断 1 项：`G 有效技能数 2.95 < 3`** —— 修闸之前它是 multi 5.16 / long 3.83（过）；净兑现 `3.25(5 种) → 2.88(4 种)`。
- **顺带暴露一个量具洞（要报给 DS/用户）**：`G`/`净兑现` 统计的是**非ジ出手的种类与熵**，而"**空净化**"这种**恒亏动作也计入** ⇒ 一只包可以靠空放净化把广度刷上去。
  这只包的净化原本 ~2.8 次/局，挡掉之后广度直接掉到门下 ⇒ 广读数里**含多少空转**没有被扣除。（同族老账：用户说"G_eff 像刷分"、§N24 的命中加权广度。）
  ⚠️ 我只量到"挡掉空净化 ⇒ G 3.83→2.95"这个**总效应**，**没有**把它拆成"计数上少一张卡"与"行为真的变窄"两半 —— 下一晚可做。
- 结论：今晚**没有**一粒既过全部门又广又破防的候选 ⇒ 两槽照旧不动。

## H-8 03:00 · 把 §51 那半没量到的东西量完了，结果是**我自己那条猜测被推翻**
`probe-ep-reach.mjs` 加 §E（镜像 40 局 · 5 席 · 空转一律按**事件**判定：`purify.curses===0` / 该回合无 `reason==='天火'` 伤害 / `beadExpire`）：
五粒包里 **空净化与空天火全是 0**（两道菜单闸在真实包上确实挡干净了），空转的大头是**蓄能珠过期**（现役 270 颗 / `v7cmin4-82` 744 颗）。
扣掉空转重算 `G`：现役 4.15→4.14、`cbs1s2` 2.99→2.92、`co1s8` 2.63→2.58（**差 ≤2.1%，不是主因**）；
而 `v7cmin4-82` **4.83→5.79（扣了反而升 19.9%）** ⇒ `G` 对"去掉重复动作"**不单调**（熵被高频重复压低，去掉就回升）
⇒ 所以"扣空转后再套 `G≥3` 门线"这个改法**不成立**，会把方向带偏；真要治空转得另立一列（空转占比）或用 §N24 的命中加权口径。
⇒ `cbs1s2-band2` 的 `G 3.83→2.95` 是**行为真变窄**（总出手同批 7517→5153），不是"少一张卡"。已在 METHODOLOGY **51.1** 把 51 条降级为猜测并写清代之以实测。

## H-9 00:23 · 第四条工具账：给 `probe-ideal-champion` 的【5】【6】加口径标注 ——**顺带查出一件我以为是新发现、其实是 DS 已裁的事**
起因是给 `probe-ideal-champion` 的【5】【6】加口径标注（ε 扫描时这两行逐字不变）。定位写死处时数出来的东西比预想大：
- `grep -c 'policyChooserN(params, 0.15)'` ⇒ **`tools/audit-lib.mjs` 9 处**（`reflectWall:148` · `ringWallProbe:185` · `fieldRate:222` · `sniperField:305` · `seatSymmetry:403` · `densityProfile:603` · `chargeProfile:647` · `aggressionProfile:713` · `breadthProfile:834`）+ **`js/train/evo.js` 4 处**（`1658` · `1729` · `2523` · `mirrorHealth` 内 `2554`）。
  ⇒ 也就是说 **G1~G5 每一道门的读数、`promote --dry` 的每一栏、`gate-drafts` 的每一列**都是 ε=0 的"纯策略"读数。
- 而产品侧**自己就有两个口径**（这条我以前说漏了）：`ui.js:464` 的 5 人冠军 = `pickChampion(..., 0.15, 0.2, 5, 'soft')`；
  `ui.js:630` 的 2 人困难 = `pickChampion(state, 1, legal, c, 0.15)` —— **ε=0**，而且那里有一句刻意的注释「播放口径：与训练口径一致（temp0.15，纯策略）」。
  ⇒ 正确表述不是"评测口径 ≠ 产品口径"，而是"**评测口径 = 2 人口径 ≠ 5 人口径**"。本次量的包是 `bundled-champion-3p.js`（走 5 人那条），所以对它的产品口径确实是 ε=0.2 soft。
- 我做了什么：`probe-ideal-champion` 的【5】【6】两行**在输出里自报口径**（源码里有、输出里没有 = 读表的人看不见），门 **D149** 钉住"必须自报 + 行号要实测"。
  钉法本身抓到我一处错：我第一版按旧笔记写 `evo.js:403`，实际 `mirrorHealth` 的写死处在 `evo.js:2554` ⇒ 门红了，改成实测值。
- ⚠️ **自我更正（我在这条上冲太快了）**：写完上面两句我就去查 `v1.5.152` —— **DS 早就知道并有意为之**：
  METHODOLOGY 第 49 条自己写着「门禁继续用 ε=0 是**有意的**（可复现、评的是包本身，v1.5.152 因此另设"产品代理栏"）」，
  而且 `promote-champion.mjs:228-244` 真的有一栏 `fieldProfile(params, 0.2, 'soft', …)`，门 **D118** 钉着它"只记录不阻断"。
  ⇒ 所以"13 处写死"**不是 bug、也不是新发现**，是一条已有裁定的设计；我不该把它写成"给 DS 的账"。
- **真正剩下的、有实测支撑的那半**：产品代理栏**只覆盖"真桌出招份额"这一族**（电磁炮/蓄能/防御/集火/最大ep/局长），
  而 **座位极差、广度 `G`、头对头 G5、G4 反制矩阵都还没有产品口径的第二栏**。
  E7（H-10）正好在这一族里量出了后果：`cbs1s2-band2` 的座位判定**两口径相反**，而它在 ε=0 的判胜率只有 26~30% ⇒ 极差算在不到 1/3 的局上。
  ⇒ 要提的不是"改口径"，而是"**给座位这一栏也补一条只记录的产品代理**"（与 D118 同性质、同处置），这是判据侧的事，**列进早报请用户裁**，我不动。

## H-10 00:34 · E7 口径统一：**n 的噪声压得住，口径压不住**（新量具 `tools/probe-seat-caliber.mjs`，配对设计 + 自检）
先说工具：`seatSymmetry` 不接受 Chooser 注入 ⇒ 我按它逐行复刻了一份可注入口径的版本（同 seed 序列、同 `slotSalt` 公式，只换 ε），
并**用 `seatSymmetry` 自己校验复刻**（n=100/seed0=12000 下 5 席回胜利逐字相同 `10/18/16/16/20`；不一致就 exit 8 作废整张表）。
两栏是**配对**的 ⇒ 差值只来自口径，不来自抽样。5 粒包 × n∈{60,100,200,400} × 3 个 seed 基址取中位数：

| 包 | n=60 评测 / 产品 | n=400 评测 / 产品 | n=400 判胜率（评测/产品） |
|---|---|---|---|
| 现役 3p | 17.0 / 21.7 | **6.2 / 5.5** | 81% / 100% |
| **`cbs1s2-band2`** | **30.8 ⛔ / 12.1 ✓** | 8.8 / 3.1 | **26% / 98%** |
| `cbs1s5-band1` | 17.6 / 8.5 | 5.4 / 5.3 | 90% / 99% |
| `co1s8-band1` | 13.8 / 10.0 | 4.6 / 3.8 | 96% / 100% |
| `v7aim3-93` | 12.5 / 11.7 | 4.7 / 4.0 | 88% / 100% |

- **交接 §4-E7 的"一律 n=400"是对的，而且是必要的**：n=60 那一列的 seed 间带宽最大到 **[10.9~27.3]**（`cbs1s5-band1`）—— 同一粒包换个 seed 基址就能翻 2.5 倍；
  v1.5.202 的"该 n 的零分布 p99"线（26.7 / 21.0 / 14.5 / 10.3pt）把 20 格里 18 格都判住了 ⇒ **D145 那条标定在工作**。
- **但 n 压不住口径**：两栏在**同一条线上判定相反**的格子恰好 2 个，全都在 `cbs1s2-band2`（n=60 评测 30.8 ⛔ vs 产品 12.1 ✓；n=100 评测 21.4 ⛔ vs 产品 12.2 ✓）。
  ⇒ 这只候选（今晚四栏最均衡的那粒）在**门户口径下会被读成"座位偏置"，在产品口径下不会**。
- **机制就在最后一列**：`cbs1s2-band2` 在 ε=0 的判胜率只有 **26~30%** —— 正好压在 `audit-lib.mjs:415` 的 `decisiveRate<0.3 ⇒ verdict='unjudgeable'` 线上**来回抖**；
  极差因此是在不到 1/3 的局上算出来的。而 ε=0.2 soft 下同一批种子判胜 98%。
  ⇒ 与 E5 同源的那句话再次成立：**龟型包在 ε=0 下大面积拖平，凡是"平局多"的包，评测层的每道门都在小分母上读数**。
- ⚠️ 这条不是"建议换口径"，而是"**读数要带 n + 带判胜率**"：D145 已经给线随 n 标定，但极差这一列现在不印判胜率（`unjudgeable` 只在 `notes` 里，见 `audit-lib.mjs:514`）。

## H-11 00:34 · E7 的第二半：示范覆盖复量 ——**形状比历史上更均，但密度一点没涨**
小臂复量（`train-3p.mjs 40 3 12 16` · seed 31 · `IMIT_TEACHER=pickBigTFocus` · `IMIT_ONLY=bigT` · `FRAC=0.5` · 与门 D134 同一套 env，只是把 40 代/8 局换成 40 代/12 局×pop16 好对上历史账）：
- **覆盖席 `{0:265, 1:110, 2:122}` = 53/22/25**（fired=497）。历史两档：v1.5.186 修后 **59/26/15**、09-23 夜 **60/23/17**。
  ⇒ 0 号席的支配度 60%→53%，且 **1/2 两席从"44:33"变成"110:122"（反过来略微均势）** ⇒ v1.5.186 那批修的是真东西，没有回潮；D134 的钉（≥3 席、单席 ≤80%）远未触线。
- **但密度天花板原封不动**：`tries=3910 / fired=497` ⇒ **12.7%**，与 09-23 记的"~12%、每 8 局才有一局被教师接管一次"是同一个数；
  未开火原因里 `被 only 过滤 3413`（87%）、`教师无动作 0`、`买不起 0` ⇒ 卡脖子的确实只是 `IMIT_ONLY`+`IMIT_BETA` 这两块天花板，**Q-14 ① 仍然待裁**。
- **顺带又一条 E1 的独立确认（在训练桌上）**：`bigT` 出手归因 `{imit:497, native:728}`，而 native 里 **`nativeEcon` 只有 2 次**（`nativeSub` 726）
  ⇒ 大雷出手中"原生经济"占 **2/1225 = 0.16%** —— 与 E1 在评测桌上的"可达时成交率 0.0%"是两条独立的量具、同一个结论。
- 产物卫生：本臂的 band 文件全部写进 `mktemp` 目录（`EPIRUS_BAND_DIR`），仓库里只多了一个已被 `.gitignore:10 docs/artifacts/*-out.js` 覆盖的 `train-3p-out.js` ⇒ **没有新 `.bak` 进仓**（D82 无需点名）。

## H-12 00:52 · 超出交接的一条（我叫它 E8）：把**整个评测层**搬到产品口径，五道门有 2/4 粒结论相反 —— 而用户实机报的那条病**只有产品口径看得见**
新量具 `tools/probe-layer-caliber.mjs`（只读、不落盘、**仓库一字不改**）：口径靠两条内存手法搬 ——
① `evo.js` 源码读进来后在内存里把 4 处 `policyChooserN(params, 0.15)` 换成带 `temp/eps/epsK/epsMode` 的版本再丢进 vm；
② `audit-lib.mjs` 是 ESM 不进气泡，但它调 `W.EpirusTrainer.policyChooserN` ⇒ **装载后把沙箱里那个属性包一层**。
两条路都**必须自证生效**：打印"替换 4 处 + 经包装调用 N 次"，不成立就 `exit 9`（否则"两栏相同"会被读成"口径无关"—— 这就是 METHODOLOGY 47.4 那族）。实测 N=2400~2880。

**五道门（`feasibilityOf` 的同一份纯函数，两个口径各算一遍）**：

| 包 | ε=0（=现在门禁读的） | 产品口径 | 差异来自哪一栏 |
|---|---|---|---|
| 现役 3p | ✅ 全过 | ✗ 座位偏座 20.8pt ≥ 线 19.2 | ⚠️ **单次抽签**：换 `probe-seat-caliber` 配 5 个 seed 基址在 n=120 复量 ⇒ 产品中位 **11.7pt [9.2~20.8]**、n=200 时产品口径 **✅ 全过** ⇒ 判"这条是 n=120 那一格分母不够"，**不判它真偏座** |
| `cbs1s2-band2` | ✗ `G 2.92`、`G(long) 2.49`、场B 0.00 | ✗ 只剩场B 0.01 | 两条广度 fail **消失** |
| `cbs1s5-band1` | ✅ 全过 | ✅ 全过 | — |
| **`co1s8-band1`** | **✗ `G 2.89 < 3`**、场B 0.01 | **✅ 全过**（G 4.52 · 场B 0.37） | **广度门 + 场B 都翻** |

- **广度族在 ε=0 系统性偏低**：4/4 粒 `ΔG(multi)` 全为 **正**（+1.45 / +1.63 / +2.06 / **+4.40**），净兑现同向（+0.37~+1.01）。
  机制不神秘：`G` 数的是"非ジ出手的种类/熵"，探索本身就会把更多种类带进出手 ⇒ **同一只包，越探索越"广"**。
  ⇒ `G < 3` 这条线（v1.5.145 用户裁定"两个模式都判"）在评测口径下对**窄型候选偏严**，`co1s8-band1` 就是这么被挡的。
- **但方向不是单向的 ⇒ 换口径不能只换 ε，必须按列重标定**：现役 反弹墙伤害 **12.17 → 5.05**（−59%）、蓄能 **4.99 → 2.09 次/局** 而珠浪费率 **19% → 31%**、
  `G(long)` **3.30 → 5.39**（与 multi 同向）、输出密度 0.441 → 0.889（+102%）、场B 清场 0.45 → 1.19。**同一只包同一批种子，十列里两列降、八列升**（降的是"打墙"与"蓄能"这两族）。
  ⇒ 复现性顺手记一句：本量具两次独立运行（换 `--packs` 顺序）现役那一格**逐字相同** ⇒ 装配内没有额外随机源，读数可比。
- **用户 09-24 实机报的那条病，量到了，而且门的盲区被抓得很干净**（`fieldRate('active')` = 1 席"只会攒钱+出攻击卡"的替身 vs 4 席被评包 · 200 局）：

| 包 | 设防率 ε=0 → 产品 | **最长连设防（回合）** | 无威胁时设防率 | 局长 |
|---|---|---|---|---|
| 现役 3p | 0% → **6%** | 1 → **4** | 0% → 7% | 18.5 → 13.8 |
| `cbs1s2-band2` | 2% → **8%** | 1 → **4** | 2% → 8% | 29.4 → 15.6 |

  ⇒ "**碰到攒钱的人就防御、而且维持很久**"这件事在**评测口径下几乎不存在**（设防率 0~2%、连设防只有 1 回合），在产品口径下变成 **4 回合连防**；
  ⚠️ **这一栏五道门至今不看**（D118 的产品代理栏测的是出招份额，不测"设防持续性"）⇒ 这是"门的盲区"目前最实在的一个例子，
  也是**能直接对到用户观察**的一条 —— 早报里我会把它放在第一条。
  （口径来源核对：本表所有数来自 `probe-layer-caliber` 一次运行；`probe-field-baseline` 那类"跨场设防率对比"本文**没有**引用，因为我没跑过它。）
- **口径 × 装配是 2×2，缺一格都会被骗**：同一只现役包 `G(long)` 从 **3.30 → 5.39**（只换口径、装配仍是镜像自对战），而把 4 席里塞进一个"会攒钱的人"（`fieldRate('active')`）之后读到的设防持续性才是玩家看到的样子。
  ⇒ 我此前所有"广度"结论都在**镜像装配 + ε=0** 这一格里；换任何一格结论都会动。
- **没装门**（本量具自带 `exit 9` 自检）；只加了一条 **D150** 钉"写死处数量必须与 `evo.js` 实际数量一致"⇒ 谁改了单一真源，这工具必须跟着改，不许静默少覆盖。

## H-13 01:12 · 档案批量复量：**`G<3` 这条广度门线挡掉的包，53% 是口径挡的**（`probe-breadth-flip.mjs` · 160 粒 · 搬运复用 `probe-layer-caliber` 的 `build`）
160 粒档案包（`docs/artifacts/*.bak` 共 1451 粒，每 9 取 1）· `mirrorHealth` 60 局 × 5 席 · 确定性 ⇒ 每格一次：

| 判据 | 评测口径 ε=0 | 产品口径 ε=0.2 soft |
|---|---|---|
| `G(multi) < 3` 被挡 | **84 粒（53%）** | **8 粒（5%）** |
| `G(long) < 3` 被挡 | 93 粒 | 4 粒 |
| 净兑现 `< 3` | 148 粒 | 117 粒 |
| ΔG(multi) | 中位 **+2.51** · 正 **157 粒** / 负 3 粒 / 零 0 | — |
| 跨界方向 | **"ε=0 挡 → 产品过" 76 粒 : 反向 0 粒** | |

- **预注册 ① 成立，而且不是"偏严"这么轻**：广度这条线在 ε=0 下**挡掉一半以上的档案池**，搬到玩家口径几乎全放行，**方向完全单侧（76:0）**。
- ⚠️ **但结论不能写成"所以该换口径"** —— 反过来看这张表：产品口径下只有 8/160 被挡 ⇒ **那条线在那一侧基本失去判别力**。
  机制也清楚：`G` 数的是"非ジ出手的种类/熵"，**探索本身就在制造种类** ⇒ 同一条线在两口径下量的不是同一个东西（一个量策略、一个量"策略+噪声"）。
  ⇒ 真正的选项有三个，且都属判据侧（等用户裁）：
  **(a)** 留在 ε=0 但**把线抬高**（现在 3 太松；实测产品口径的分布才像"该有的一半"）；
  **(b)** 换成**与探索无关的广度定义** —— 净兑现那一栏就是这类（148→117，两口径都严，判别力不随口径蒸）；
  **(c)** 双口径并报、只按"两栏都过"算（D118 那种"只记录不阻断"的加强版）。
  我的倾向是 **(b)**：同一夜的数据里只有它是**两口径都不放过大多数包**的那把尺。
- 顺手把同一个坑在**我自己的**另一件量具上补了：`probe-kill-reward.mjs` 的 Chooser 也写死 `policyChooserN(params, 0.15)` ⇒ 加了 `--temp/--eps/--epsk/--epsmode`（默认 `0.15/0` ⇒ 历史读数逐字不变，实测加前后 `bundled-champion-3p` 那行 0.0%/0.00/0% 一字不差）。

## H-14 01:13 · 用新口径开关重读 **E3 的 G7 草案**：挡现役的其实只有"终场存活"那一栏（同种子配对 · 120 局 · 破防场）
| 包 | ε=0：1st / 归因伤害 / 终场存活 | 产品：1st / 归因伤害 / 终场存活 | 草案两栏（伤害≥4 且 存活≥50%） |
|---|---|---|---|
| **现役 3p** | 0.0% / **0.00** / **0%** | 90.0% / **4.63** / **43%** | ε=0 两栏都红；**产品口径下"伤害"这栏它过了**，只剩存活差 7pt |
| `cbs1s2-band2` | 100% / 4.87 / 90% | 100% / 5.78 / 89% | 两口径都过 |
| `cbs1s5-band1` | 100% / 9.88 / 93% | 98.3% / 10.28 / 79% | 两口径都过 |
| `co1s8-band1` | 100% / 8.63 / 73% | 100% / 8.09 / 74% | 两口径都过 |

- ⇒ 草案里"现役必红、所以放进参照行"这句话在两个口径下**都成立**（这条草案不会被口径翻掉），但**红的原因只剩一个**：
  **`归因伤害≥4/局` 不是分辨栏，`终场存活≥50%` 才是**。⇒ 若将来真要立这条门，**只立存活那一栏**（两栏并列会把"能不能收尾"读成"蹭到多少血"，而后者在产品口径下现役也有 4.63）。
- 另一条同源的：`1st` 那一栏从 0.0% 跳到 **90.0%**（现役），与候选的 98~100% 只差 8~10pt ⇒ **"破防 1st"作为下限判据的分辨力大部分是 ε=0 给的**（E5 那条打折意见在这里再确认一次）。

## H-15 01:26 · 把用户那句"碰到攒钱的就疯狂防御"拆成两问，**两问的答案都不是"是"**（新量具 `tools/probe-defense-cause.mjs` · 1 席攒钱替身 + 4 席被评包 · 200 局）

> ⚠️ **本节的读数已被 §H-19 取代**（ep 是从事件流前缀和重建的 ⇒ 花了不扣；且单档装配里 ep 轴与回合轴重合）。正确结论：**线上包没有"对手有钱→就防"的响应，但 `cbs1s2-band2` 有（6.6 倍、连防 41 回合）**。

先钉量具的立足点：**分桶用的是"该回合开始时对手累计 ep"**，判据来自事件序实测（同一回合内 5 条 `action` 全部先于结算类 `ep/damage` 事件 ⇒ 前缀和 = 回合开始时）；`ep≥10` 那一桶 n 只有 20 时**打印"分母不足"而不是 0%**。

| 包（产品口径 ε=0.2 soft） | 对手 ep0~1 | ep2~4 | ep5~9 | ep≥10 | 最长同席连续设防 | ≥3 连的局 |
|---|---|---|---|---|---|---|
| **现役 3p** | **9.3%**（n=2431） | 4.6%（4772） | 3.1%（944） | 20.0%（n=20，不算数） | **4 回合** | 4.5% |
| `cbs1s2-band2` | 6.9%（2685） | 8.3%（5894） | 7.4%（620） | 8.9%（56） | 4 回合 | 6.0% |
| 现役 · **ε=0 对照** | 0.8%（715） | **0.0%**（2058） | 0.0%（382） | — | **1 回合** | 0.0% |

- **第一问："是因为对方在攒钱吗？"⇒ 方向不支持。** 现役的设防率随对手 ep **单调下降**（9.3% → 3.1%），`band2` 则是平的（6.9/8.3/7.4/8.9%）。
  ⇒ 在这只包的策略里，"摆架势"**不是冲着有钱的人去的**；用户看到的"他攒钱我就防"更可能是**同一局里两件同时发生的事**（对手在攒 ⇔ 我方刚打完一轮、自己 ep 也低），而不是因果。
  ⚠️ 这条**只否证了"按对手 ep 分桶"这一种威胁定义**，没说用户的观感是错的 —— 换一个触发定义（上一回合我被谁打过 / 我被瞄着）还没量。
- **第二问："维持很久吗？"⇒ 只在产品口径下成立，而且是 3~4 回合、约 5% 的局。** ε=0 那边最长连设防恒为 **1**、≥3 连的局 **0.0%** ⇒
  与 H-12 那条并起来：**"疯狂防御"与"维持很久"都是运行时探索的产物，门禁那一侧结构性看不见。**
- ⚠️ 上面这张表是**修方法之前**的原始四桶（`ep≥10` 只有 n=20 却印成了 20.0%）⇒ 两处缺陷与修法见 **H-17**；H-17 的结论方向与这张表一致。
- 机制上为什么 ε=0 完全没有防御：`evo.js:530-536` 的 soft 探索**不会把非防御出手换成防御**（探索集 `pool` 过滤掉 `CAT.DEFENSE`），
  而贪心（ε=0）在这条场上从不选防御 ⇒ 产品口径下那 6~9% 的设防**只能来自"贪心本来就选了防御的那些回合"**，
  也就是**探索先把局面带进了"该防"的状态**，而不是探索直接抽出了防御。（这一句是从代码路径 + 上表 ε=0 恒 0 一起推的，**不是逐手归因**；逐手归因需要能区分探索/贪心两条分支的打点，本工具没做。）

## H-16 01:26 · 自我更正：**我在 v1.5.208 里"倾向 (b) 换成净兑现"这句话，被同一批数据的锚点表否掉了**
`probe-breadth-flip` 加了"两端锚点 + 分位数"之后（165 粒）：

| 线 | ε=0 挡住 | 产品挡住 | 档案分布 |
|---|---|---|---|
| `G ≥ 3` | 85 粒（52%） | 8 粒（5%） | ε=0 p50 **2.90**（线正好压在中位数上）· 产品 p10 3.66 / p50 5.92 |
| 净兑现 `≥ 3` | **150 粒（91%）** | 119 粒（72%） | ε=0 p50 1.75 · **p90 2.92（线在 p90 之上）** · 产品 p50 2.56 |

两端锚点（显式进表）：已知好 `v7aim3-93` G 3.81→6.44 / 净兑现 **3.50→3.86**、`cbs1s5-band1` 8.01→9.18 / **3.05→3.69**、`v7cmin4-82` 4.75→7.49 / **3.74→4.12**、
`v7divK-31`（H-2 里"会破龟群"的那粒）3.06→5.11 / 净兑现 **2.23→2.60**；已知退化 `cbig1s1-band4`（破防 1st 100% 但只有 1.00 点 / 0% 存活）G 2.12→4.14 / 净兑现 **1.77→2.14**。

- ⇒ **净兑现挂在 3 上更糟**：它在 ε=0 挡掉 **91%** 的档案池（线在 p90 之上），而且会**挡掉已知好的 `v7divK-31`（2.23）**、只放过 `cbig1s1-band4` 与它 0.47 之差 ⇒ **"分开已知好与已知坏"这条资格它没过**（附录 B2-6 / C-4 同规）。
- 真正被数据支持的结论是第 55 条那句的加强版：**这两列在任何单一口径上都分不开两端 —— 差别只在"挡多少"**。
  所以可选的正解不是换列，而是：**先固定口径，再把线改成"该口径下档案分布的分位数"**（例如 ε=0 上 p50=2.90 ⇒ "不低于池中位"天然会分开 `cbig1s1-band4`(2.12) 与 `v7divK-31`(3.06)/`aim3`(3.81)）。
  这条属判据改动 ⇒ **只写进早报请用户裁，我没动任何门。**

## H-17 01:29 · 把 H-15 那两个**方法缺陷**修掉了（修完强度降级，但结论更站得住）

> ⚠️ 这一节只修了"分母"与"印平均回合"，**没修到根**（真因是重建 ep 与单档装配）⇒ 见 §H-19。

1. **高桶只有 n=20 却被算成一个数**（现役 `ep≥10` 那一桶 20.0%）——看着像发现，其实是 4 次里中了 1 次。
   ⇒ 加 `--bucket-min`（默认 40）+ **从最高桶往下自适应合并**，并在行尾印 `⚠️ 该桶分母 < 40`。合并后现役是 `ep 5~∞ = 3.4%（n=964）`，与 `ep0~1 = 9.3%` 同向 ⇒ 结论没变，但**不再靠一个 n=20 的格子说话**。
2. **`ep 桶` 与 `回合数` 是同一条轴**（这个装配里攒钱替身的 ep 单调涨）⇒ 我原来那句"防御不是冲着有钱的人去"其实混着"晚期不打架势"。
   ⇒ 每桶现在并排印**该桶平均回合**（200 局那一次）：现役 `ep0~1 ≈ 第 2.1 回合（n=2431）/ ep2~4 ≈ 8.1（4772）/ ep5~∞ ≈ 12.4（964）`。
   ⇒ 读数强度按实降级为：**"这条分桶没看到正相关"，不是"看到了负相关"**；要真分开"对方有钱"与"局已晚期"，需要一个 **ep 会振荡的对手**（会花掉的攒钱者）——那是下一次的量具活。
   两问的**方向性结论不变**：设防只在产品口径出现（ε=0 恒 0~0.8%、最长连 1 回合），且**不与对手 ep 同向**。

## H-19 01:40 · **抓到真凶了，而且不是线上包**：`cbs1s2-band2` 有"对手有钱→我就防"的反应（51.8% vs 低桶 7.9%、同席连防最长 **41 回合**），线上包同场只有 1.6% / 9 回合
先量具自身的两个修正（都在读数旁边写明，不偷偷重跑）：**① ep 不许从事件流重建**（前缀和只加不减 ⇒ 量的是"对手一辈子挣过多少"，不是"它手里现在有多少"；改用决策时实读 `state.p[0].ep`）；
**② 一档装配答不了因果**（纯攒钱替身里 ep 与回合号是同一条轴，第 r 回合 ep≈r−1）⇒ 加 `--saver=cycle`（堆到 4 就全花掉 ⇒ ep 被压住），**同一回合号跨两档配对**才是因果判据。

**修正后重测（装配：1 席纯攒钱 + 4 席被评包 · 200 局 · 产品口径）**：

| 包 | 对手 ep0~1 → 5~9 → **≥10** 的设防率 | 同席连防最长 | ≥3 连的局 | 单跑档的"回合轴"差距 |
|---|---|---|---|---|
| 现役 3p | 7.9% → 5.4% → **1.6%**（**下降**） | 9 回合 | 29.0% | 19.1 回合 |
| **`cbs1s2-band2`** | 7.9% → 33.0% → **51.8%**（**上升 6.6 倍**） | **41 回合** | **90.5%** | 24.3 回合 |
| `cbs1s5-band1` | → 18.7% → **20.9%**（上升 2.7 倍） | 8 回合 | 77.0% | — |
| `co1s8-band1` | → 4.8% → 1.5%（下降） | 5 回合 | 25.5% | — |
| `v7aim3-93` | → 2.9% → 1.3%（下降） | 4 回合 | 16.5% | — |
| `v7cmin4-82` | → 4.4% → 2.1%（下降） | 4 回合 | 26.0% | — |

- **因果那一问用"同一回合号跨两档"判**（把 ep 与晚期分开）：`cbs1s2-band2` 在第 6/8/10/12/14 回合，`hold` 档（对手 ep 5~12）设防 **29.8 / 38.1 / 38.1 / 42.8 / 44.8%**，
  而 `cycle` 档（同回合对手只有 ep 3~4）是 **14.8 / 17.0 / 12.6 / 16.4 / 19.0%** ⇒ **同一回合号下，对手钱多一倍，它设防率高 2~3 倍**，且两档在第 4~5 回合（ep 还相同时）曲线**重合**（15.3%/19.8% 两档逐字相同）。
  ⇒ 这不是"晚期所以被动"，是**真在响应"对方攒钱"**。现役同法测：第 6~14 回合两档设防率 **4~7% vs 3~8%**，无系统性差异 ⇒ 线上包**没有**这个响应。
- **对到用户的实机记录**（只读用户自己的 `results/*`）：设防占比 `results/band2` **22.5%**（单席最多 52 次）· `results/31` 19.0%（33）· `results/93` 0.8%（3）
  ⇒ 用户报病的那局正是在测 **band2** 那只候选（目录名就是它），而这只包**恰是我今晚评为"四栏最均衡"的那一粒**。
  ⚠️ 实机记录不能当判据（人类对手、局数少、口径混杂），但它与合成场的方向**一致**，所以这条不再是"我只在模拟里看到"。
- **机制（自增强回路，写清楚以便复现）**：4 席都是同一只"会响应对方有钱就防"的包 + 1 席只攒不打 ⇒ 没人能打穿 ⇒ 局长飙到 25+ 回合 ⇒ 攒钱席 ep 一路涨 ⇒ 4 席设防率涨到 51.8% ⇒ 更打不穿。
  ⇒ 这就是用户那句"碰到一个人攒钱就会开始疯狂防御并且维持很久"，**而且比他说得更严重**（连防能到 41 回合 = 基本整局）。
- **为什么 `promote --dry` 全绿挡不住它**：破防场/G5 那两栏判的是"1 被测 + 4 龟"或"4 被测 + 1 龟"，而**"被测包自己凑成一桌龟"这件事在 ε=0 的镜像格里读出来是 0 设防**（H-12 的 0%）。
  ⇒ 补一条具体建议（属判据，等裁）：**镜像格加"对手只攒不打"这一装配的设防持续性读数**（`fieldRate('active')` 的连防最长 + ≥3 连占比），只记录不阻断起步。
- **今晚的候选结论因此翻转一半**：`cbs1s2-band2` 我原记"四栏最均衡、可考虑复测后换槽"⇒ **不加换槽建议**，它带着用户实机刚报的那个病；`cbs1s5-band1` 同族同反应（2.7 倍、77% 的局有 ≥3 连）⇒ 也不推。
  两槽照旧不动。

## H-20 01:49 · 把"是不是因为被打过"这条混淆也钉住了，结果是**安慰剂式**的：两档在 ep 分岔之前逐字重合，分岔之后才分开
`cycle` 档的对手每几回合会真打出一发 ⇒ 光看"同回合两档差 2~3 倍"还分不清是"看见对方有钱"还是"自己刚被打过"。
修法：再加一条曲线，**只统计"自上次自己决策以来一点血都没掉"的那些决策**（决策时直接读 `state.p[pid].hp` 的净变化）。

`cbs1s2-band2` · 没掉血子样本 · 按回合号（括号内是该回合对手平均 ep）：

| 回合 | `hold`（对手钱一路涨） | `cycle`（对手 ep 钉在 ≤4） | 倍差 |
|---|---|---|---|
| r4（ep 3.0 / 3.0） | 12.1%（n=619） | 12.1%（n=619） | **1.00（重合）** |
| r5（4.0 / 4.0） | 16.6%（650） | 16.6%（650） | **1.00（重合）** |
| r6（5.0 / 3.0） | 29.7%（619） | 15.8%（417） | 1.9× |
| r8（6.9 / 3.0） | 37.2%（634） | 14.8%（466） | 2.5× |
| r10（8.8 / 3.1） | 38.4%（643） | 11.3%（371） | 3.4× |
| r12（10.6 / 3.2） | 43.3%（616） | 14.9%（282） | 2.9× |
| r14（12.5 / 3.3） | 45.2%（604） | 14.9%（202→n 见下） | 3.0× |

- **r4/r5 两档数值逐字相同**（那两回合两档的对手 ep 本来就一样）⇒ 这是一次天然的**安慰剂对照**：装配的其他部分全同，只有 ep 从第 6 回合起分岔，而设防率也恰好从第 6 回合起分岔。
- 桶那一侧同样成立：`ep5~9` 没掉血 32.7%（n=3525）vs 全样本 33.0%；`ep≥10` 没掉血 **52.3%**（n=14959）vs 全样本 51.8%
  ⇒ **掉血与否几乎不改变读数**，"被打了所以防"解释不了这条上升。
- **线上包用同一套测：什么都不成立**（没掉血子样本 · hold vs cycle）：`r6 5.8 vs 7.5`、`r8 7.9 vs 5.7`、`r10 5.5 vs 3.5`、`r12 6.2 vs 4.8` —— 两档互有高低、无系统方向；
  桶那一侧 7.9% → 6.2% → 5.3% → **1.6%** 单调下降。
- ⚠️ 诚实记两条边界：① `cycle` 档打到 r12+ 的局本来就少（n 从 619 掉到 282）⇒ 后段倍差的分母比前段薄，方向仍一致但绝对值别太当真；
  ② 这条只在"**1 席攒钱替身 + 4 席同一只包**"这一种装配里成立。真实 5 人局里 4 席是不同包/人类，回路强度未必一样 ⇒ 早报里我按"这只候选在产品口径下有这个响应、且门看不见"报，不按"玩家一定会遇到 41 回合连防"报。

## H-21 01:57 · 跨包筛：这个响应**不是 band2 的孤例**，而是今晚这批臂里 **13/41（32%）粒**的共同体质
给 `probe-defense-cause.mjs` 加 `--quiet=1`（一粒一行 + 末尾按"有钱→转防"倍差排序汇总表），扫最近 44 粒臂包（60 局/粒 · `saver=hold`）：

| 倍差 | 包（`ep0~1 → ep5~9 → ep≥10` 设防率 / 连防最长） |
|---|---|
| ≥7× | `cb1s4-band6` 9.4→98.1→**99.2%** / 47 · `cb1s6-band6` 9.2→37.6→68.7% / 41 · `cbs1s6-band4`(=`cbig1s6-band4`) 9.2→47.9→67.7% / 45 |
| 4~5× | **`cbs1s2-band2` 9.2→37.9→51.0% / 34** · `cbig1s9-band4` 20.8→97.5→99.7% / 47 · `co1s9-band4` 9.8→32.7→45.9% / 11 · `cb1s3-band4` 13.5→11.2→63.0% / 26 |
| 2~4× | `co1s6-band1` 3.7× / 18 · `cr1s7-band5` 2.9× / 17 · `co1s3-band4` 2.5× / 5 · **`cbs1s5-band1`(=`cbig1s5-band1`) 2.2× / 5** · `cr1s3-band6` 2.1× / 7 · `kr1s6-band6` 2.1× / 4 |
| ≤1（无响应） | 现役 0.16× / 4 · `v7aim3-93` 0.12× / 3 · `co1s8-band1` 0.15× / 5 · `cb1s1-band1`、`kr1s5-band1`、`co1s7-band6` 等 |

- ⇒ **倍差≥2 且高桶≥20% 的一共 15 行，去掉重复权重后是 13 粒 / 41 粒唯一权重（32%）**。⇒ 这不是"某一粒候选运气不好"，而是**当前训练配方会稳定养出这种体质**（4 席同包 + 对手只攒 ⇒ 自己跟自己打成龟群，而门禁五道栏读不到）。
- **顺带一条记账发现**：`cbs1s3-band3`=`cbig1s3-band3`、`cbs1s5-band1`=`cbig1s5-band1`、`cbs1s6-band4`=`cbig1s6-band4` **三对权重逐字节相同**（剥壳 sha1 `43e904e3` / `9c97bcb6` / `b2f0776e`）
  ⇒ 44 粒里只有 41 个不同权重。这与 09-24 记的 `EPIRUS_SEL_LAND` 咬合率 3/6 同源：**带内换人的排序键在同分带里换不出新东西时，两臂产物是同一粒包**。
  ⚠️ 也就是说"我扫了 44 粒"要说成"41 粒不同权重"，否则倍差表里那两行重复会把 32% 虚报成 34%。
- ⚠️ 这张表**不是判据**，也不改变今晚"两槽不动"的结论；它的作用是：一旦用户愿意加"设防持续性"那一栏（只记录起步），
  **已知会有 1/3 的现有臂包红**，线该画在哪（倍差 2？连防 5？）有这张分布可以照着定，而不是拍脑袋。

## H-22 02:04 · 想再往前一步问"是**哪配方**养出这体质"时，撞到一个**结构性缺口**：band-save 的 META 不记 env ⇒ 跨包归因做不到
把 §H-21 那 44 行按臂家族聚合（中位数，倍差 = `ep≥10 / ep0~1` 设防率）：

| 家族 | 粒数 | 倍差中位 | 连防最长中位 | ≥3 连的局中位 | 倍差≥2 且高桶≥20% |
|---|---|---|---|---|---|
| `cb1s*` | 6 | **4.66** | **33** | 86% | 3/6 |
| `cbs1s*` | 6 | **3.86** | 24 | 94% | 2/6 |
| `co1s*` | 8 | 1.64 | 5 | 50% | 3/8 |
| `cr1s*` | 9 | 1.55 | 6 | 68% | 2/9 |
| `cbig1s*` | 9 | 1.56 | 5 | 72% | 2/9 |
| `kr*s*` | 6 | **0.63** | 4 | 42% | **0/6** |

- 表面上 `cb1s*`/`cbs1s*` 最重、`kr*s*` 完全干净 —— 但**这句话不能当结论**：`docs/artifacts/*.bak` 的 `EPIRUS_CHAMPION_3P_META` 里只有
  `arm / xn2w / xn2g / gens / seed / selected`，**没有一个字节记录那次运行开了哪些 `EPIRUS_*` 旋钮**（`EPIRUS_COUNTER_OPPS` / `EPIRUS_SEL_LAND` / `EPIRUS_KILL_REWARD` / `EPIRUS_REGEN_SLICE`…）。
  ⇒ 家族名是我自己起的助记符，**配方只活在我当时的会话里**（今晚已因压缩丢过一次），所以"哪配方养出来的"这问**结构上无法回答**。
- ⇒ 一条具体、便宜的工具账（**没动，等用户/DS 点头**）：`train-3p.mjs` 的 band-save 处把 `server/knob-guard.mjs` 已经认得的"本次实际读到的 env 键值"回写进 META
  （单一来源已经在：`detectDarkKnobs`/`readKeysOf` 就在那里），以后"跨包归因"才有分母可用。这也顺手治 §H-21 那种"两粒包其实是同一权重"的重复计数问题（能直接比 env+权重）。
- 所以 §H-21 的正确强度停在这里：**"这体质在今晚这批臂里占 13/41（32%），且家族间差异大到值得查配方"**，不再往前。

## H-23 02:06 · 把代价落到"玩家会遇到什么"上：换 band2 不会让你更难赢，**会让你赢得更无聊**（120 局 · `saver=hold`）
| 包 | 局长中位（p90 · 上限 47） | 平局率 | **那个只攒钱的人夺冠率** | 同席连防最长 | ≥3 连的局 |
|---|---|---|---|---|---|
| 现役 3p | **20**（38） | 3% | **0.0%** | 9 | 30.8% |
| `cbs1s2-band2` | **41**（46） | 3% | **13.3%** | 35 | 91.7% |

- 这条把"病"从抽象变成可感：**攒钱不打人这件事，在线上包里收益是 0%（它照样把你打死），换成 band2 收益变成 13.3%，而代价是局长中位从 20 拖到 41 回合（p90 已到 46，几乎撞 47 上限）。**
  ⇒ 用户看到的不是"AI 变强/变弱"，是**它开始奖励龟缩**——与 METHODOLOGY 第 48 条"空爆/空防"那一族同源，但这次是**对手侧**的龟缩奖励。
- ⚠️ 三条边界：① 这是"1 席纯攒 + 4 席同一只包"的**极端装配**，真实局里 4 席不同包、人也会打；
  ② 平局率两档都只有 3% ⇒ 拖长不等于平局（最后是"终局收缩"收的），别把"局长 41"讲成"永不结束"；
  ③ 攒钱者夺冠率对**现役**是 0.0% 这个事实，本身就是"这装配的题出得偏难"的证据，所以两档之间的**差**才是可用的读数，绝对值不是。

## H-24 02:15 · 换一套完全不同的种子基址复跑：这条体质的**排序逐位稳住**（不是这批种子挑出来的）
`--seed=987654`（与前面用的 4100 系全不同）· 120 局/粒 · `saver=hold`：

| 包 | 本次 `ep0~1 → ep5~9 → ep≥10` / 倍差 / 连防 | 本次前一次（seed 4100 系）倍差 |
|---|---|---|
| `cb1s4-band6` | 10.2% → 91.1% → 99.4% / **9.74** / 47 | 10.58 |
| `cbs1s2-band2` | 9.9% → 34.4% → 56.6% / **5.75** / 39 | 5.56 |
| `cbig1s9-band4` | 20.6% → 96.5% → 99.6% / 4.83 / 47 | 4.79 |
| `cbs1s5-band1` | 9.8% → 17.8% → 20.0% / 2.05 / 5 | 2.17 |
| `co1s8-band1` | 9.9% → 4.6% → 2.2% / **0.22** / 4 | 0.15 |
| 现役 3p | 9.9% → 4.4% → 1.6% / **0.16** / 4 | 0.16 |

- ⇒ 六粒的**排序完全一致**，倍差差在 ±0.3~0.6 之内；"有响应/无响应"的分界线（≥2 vs ≤0.25）隔着 8 倍，离种子噪声很远。
- 附带一条：低桶设防率两批都是 9.9~10.6% ⇒ 这一档的"基线防御率"本身也是稳的，倍差不是被分母抖出来的。

## H-25 02:16 · 前瞻实验（E9，我自己加的）：**"哪一配方养出这体质" retrospective 问不出来，那就 prospective 跑一遍**
动机：§H-22 的缺口 —— band-save 的 META 不记 `EPIRUS_*`，所以 44 粒存量包**无法**归因到配方（只能按我起的家族名分组，那是助记符不是证据）。
办法：同一个新种子、同一份预算，四格配方各跑一臂，跑完直接量体质。
- **预注册（跑前写死）**：`A 基线 / B 只上桌(`EPIRUS_COUNTER_OPPS=1`) / C 只兑现当选键(`EPIRUS_SEL_LAND=1`) / D 两者都开`；
  装配 `train-3p 300 3 12 16`、**seed 77（今晚没用过）**、band 目录指向 `mktemp`（⇒ 仓库不新增 `.bak`、不欠 D82）。
  读数 = `probe-defense-cause --saver=hold` 的倍差（`ep≥10 / ep0~1`）+ 同席连防最长。
  **押**：B 与 D 的倍差 ≥ 2×A 与 C ⇒ "把龟放上桌"这个动作本身在养"看见钱就防"的体质（若如此，DS 交接推荐的 `COUNTER_OPPS` 需要配一条反作用读数）；
  **若四格都在 1 附近** ⇒ 体质与这两根旋钮无关，是**适应度形状**（名次适应度 + 无"对手会攒钱"这一格）的产物，只能靠判据/装配那一层治。
  **失败也学到**：若某格根本跑不出当选带（空枪），那格的"无响应"不能当证据，必须重跑或标 UNRUN。

## H-27 02:34 · 新量具 `probe-bead-loop.mjs`：**"珠浪费率高"这一个数里藏着两种相反的病**，而现役的是"用得起却没射"
用户 09-24 报的第二条病是"空蓄能"。已有的读数只有 `chargeProfile` 的"得珠/花掉/过期"（按**珠子**计），它告诉你浪费了多少，但不告诉你**为什么** ⇒ 分不出这两种病就不知道该动哪一层。
新量具在**被评席下一次决策的那一刻**看它的合法表（不重建），把"蓄能后的下一次决策"分成三桶（60 局 · 5 席镜像 · ε=0）：

| 包 | 蓄能后能配到下一次决策的样本 | ① 电磁炮**买不起**（经济） | ② 买得起**却没射**（选择） | ③ 买得起也射了 | 真源 `chargeProfile` 浪费率 |
|---|---|---|---|---|---|
| 现役 3p | n=385 | **1.0%** | **68.1%** | 30.9% | 48.2%（得珠 382 / 过期 184） |
| `v7cmin4-82` | n=1617 | **83.3%** | 11.7% | 5.0% | 88.2%（得珠 1811 / 过期 1597 · 蓄能 30.2 次/局） |

- ⇒ **两粒包的"浪费率"都很难看（48% / 88%），但病根完全相反**：
  **`v7cmin4-82` 是经济问题**（83% 的下一次决策根本买不起电磁炮 ⇒ 它按 30 次/局的速率在攒一种自己花不掉的珠子）；
  **现役是选择问题**（68% 是"买得起、珠子还活着、却没射"）。
- **② 里它到底在干什么**（量具直接印出来，不留想象空间）：现役 262 次里 **256 次在按 ジ**、6 次又蓄能；`v7cmin4-82` 189 次里 ジ 143 / 蓄能 35 / 聚能环 11。
  ⇒ 连到 §H-1 那条：**同一个"攒"的习惯在两处漏钱** —— 手上有珠、电磁炮买得起，它按 ジ 继续攒 ep；而攒到的 ep 也从不变成大雷（E1：镜像场 `ep≥5` 占 44.8%，大雷真放出去 0.0%）。
  所以"空蓄能"和"空攒钱"不是两条病，是**一条**：**它把"攒"当成了动作本身**。
- ⚠️ 口径写死：本表**按决策计**，真源**按珠子计**，两者不该相等（一颗珠可能被别的卡吃掉；一次蓄能也可能与手里已有的珠混在一起）⇒ 所以表里并排印 `chargeProfile` 那一行，谁也不许拿其中一个去"校验"另一个。
- ⚠️ 也如实记我的第一版错：最初我用 `state.p[0].ep` 的前缀和重建"对手 ep"（花了不扣），并用"蓄能后的下一次决策"当窗口却没判**珠子还活不活**；
  现在加了 `beadAlive`（隔到两回合以后的那批单列 `beadGone`，不混进①②的分母）。实测这批包 `beadGone` 恒 0（镜像场每回合都有决策），但**这条不写就会出现"8 次假买不起"那种误判**（第一版就是这么错的）。

## H-26 02:26 · E9 四格配方臂跑完了：**我的预注册押错方向，而且我差点把一组不存在的数字当成结果写下来**（这是本夜最该记的一条）
先记那条**近失错误**：中间一次我想读四格结果时，路径拼装炸了（Windows 临时目录 → node 解析成 `D:\tmp\...`），两次运行其实**根本没跑出汇总表**（`grep -c 跨包筛 = 0`），
而我当时在思考里记下"C/D 倍差 3.95 / 3.73 ⇒ 预注册反了、`SEL_LAND` 才是凶手"。**那两个数在工具输出里不存在**（3.73 是 `co1s6-band1` 在 §H-21 表里的倍差，被我串了台）。
⇒ 规矩再加一条硬版本：**"读数只能来自带表头的那一次运行输出文件"**；跨文件串数字 = 造数。（这次是靠"跑完去 grep 表格行数"发现的，不是靠我记得。）

**修好路径后的唯一权威读数**（`--games=150 --quiet=1`，产品口径）：

| 臂 | 配方 | 倍差 | `ep≥10` 设防率 | 连防最长 | ≥3 连的局 |
|---|---|---|---|---|---|
| `sp77a` ≡ `sp77c` | 基线 / +`SEL_LAND=1` | —（该桶分母不足） | — | 5 | 34% |
| `sp77b` ≡ `sp77d` | +`COUNTER_OPPS=1` / 两者都开 | **0.54** | 4.2% | 4 | 35% |
| 现役 3p | （对照） | 0.25 | 1.9% | 9 | 31% |

- **预注册不成立（好事，如实记）**：我押"上桌 ⇒ 体质"。实测**上桌两臂倍差 0.54、判"无响应"**，与不上桌的基线同档 ⇒ **`EPIRUS_COUNTER_OPPS` 不是这体质的来源**，
  交接 §4 推荐的那根旋钮**不需要**因此加反作用读数（至少在这格预算/这格种子下）。
- **顺带又抓到一条 `SEL_LAND` 的实证**：`sp77a` 与 `sp77c`（唯一差别是 `EPIRUS_SEL_LAND=1`）**权重逐字节相同**（剥壳 sha1 `103fe2c6`），`sp77b` 与 `sp77d` 也是（`7c5d47ea`）
  ⇒ seed 77 上这根键**一次人也没换**；四格实际只有**两格**。这与 09-24 记的"咬合率 3/6"同源，也是 §H-21 里 `cbs1s*`/`cbig1s*` 三对同权重的同一个机制。
  ⇒ 所以"某键没用"这类评价，**先看同分带里有没有第二粒**；这次是"确实没换人"，不是"没接线"（键本身在 `pick-best` 里被门 D124/D136 钉着）。
- **四格都无响应 ⇒ §H-21 那 32% 的体质不在我能自由摆的这两根键上**。它可能来自：1500 代预算、热启动、`cb1s*` 那批自己的配方差异……
  **但今天仍然无法归因**，理由就是 §H-22 那条：**band-save 的 META 不记 env**。⇒ 这条实验的价值是**把"该补 META"从建议变成阻塞项**。
- ⚠️ 边界：① 单种子单预算（300 代 vs 被筛那批的 600/1500 代）⇒ 只能说"这格预算里没长出来"，不能说"上桌无害"；
  ② `sp77a/c` 的 `ep≥10` 桶分母不足（那些包把局打得快，攒钱席到不了 10ep）⇒ 它们那格是"没量到"，不是"量为零"；
  ③ 复跑在做（seed 78/79 的 A/C 两臂，见 H-26b）。

## H-28 02:31 · E9 复跑（seed 78/79）把结论从"哪根旋钮"改写成**"这是一台抽奖机"**：一粒**什么都没开的基线臂**（`sp779a`）倍差 **11.45**、高桶设防 88.7%、同席连防 46 回合
| 臂 | 配方 | 倍差 | `ep≥10` 设防率 | 连防最长 | ≥3 连的局 |
|---|---|---|---|---|---|
| `sp779a`（**基线，零旋钮**） | — | **11.45** | **88.7%** | **46** | 95% |
| `sp778a`（基线） | — | 0.68 | 43.5% | 39 | 100%（低桶本来就防 64%） |
| `sp77a`/`sp77c`（基线 ±兑现键） | — | —（分母不足） | — | 5 | 34% |

- ⇒ **三粒基线里两粒掉进"龟缩吸引子"**（79 号是"看见钱才防"、78 号是"不分场合一直防"），一粒干净。⇒ 体质**不是某根旋钮养出来的**，
  是**当前目标函数（5 人名次适应度）里一个会自发掉进去的吸引子**，概率量级 **1/3~2/3**；与档案侧那 13/41（32%）同一量级 ⇒ 两边互相撑住了。
- 机制上讲得通（**这条仍标"推断"**，没做干预实验）：4 席龟 + 1 席只攒 ⇒ 谁都不出手就能靠"终局收缩按累计伤害判胜"活着 ⇒ 摆架势是局部最优。
  这与 §H-12（破防场 ε=0 全平）、METHODOLOGY 47"下限判据"是同一块地板。
- **因此预注册正式判负**：我押 `COUNTER_OPPS` 是来源 —— 实测**上桌两臂 0.54（无响应）**，而**零旋钮臂能到 11.45** ⇒ 方向不对，押错。
- **另一条独立收获：`EPIRUS_SEL_LAND` 在这三个种子上一次人也没换**：`sp77a≡sp77c`（`103fe2c6`）、`sp778a≡sp778c`（`7378dea6`）、`sp779a≡sp779c`（`4bb43940`）—— **0/3 咬合**。
  比 09-24 记的 3/6 更冷。⇒ 那根键在 300 代/16 种群这一档基本是"摆设"（键本身在 `pick-best` 里被门钉着，是**咬合不到**，不是**没接线**）；
  想让它有用，得先让同分带里真的有多粒（种群/预算问题），**这根线索归 DS 的门面还是我的训练面都行，先记账**。
- **可执行的那一条**（不越权，等裁）：既然体质是自发吸引子，**筛候选时必须有一栏"对手攒钱时的设防持续性"**，
  否则每次重训都是重新抽奖，抽到算运气好；现成的读法就是 `probe-defense-cause --quiet=1`（倍差 + 连防 + 玩家侧代价三样都印）。

## H-29 02:32 · 预注册 E10（预算这一根轴）：**同三个种子各跑一臂 1500 代基线**，与已有的 300 代基线配对
动机（档案侧的家族差）：`cbs1s*`（1500 代）倍差中位 3.86 vs `co1s*`（300 代）1.64 —— 但两族不只差预算，所以**retrospective 判不了**（§H-22）。
§H-28 给了一个干净的可比对照：`sp77a/sp778a/sp779a` 都是**零旋钮基线**、300 代，其中 1/3 掉进"看见钱才防"、1/3 掉进"无差别一直防"。
⇒ 现在只改一个变量：**同一批种子（77/78/79）、同样 3 人 × 12 局 × 种群 16，只把 300 代换成 1500 代**。
**押**：1500 代组掉进吸引子的比例 ≥ 300 代组（档案侧家族差的方向一致）⇒ 那么"多跑几代"不是解药而是助长，早报要写进"别用预算换体质"；
**若相反**（1500 代更干净）⇒ 档案侧的家族差是配方差不是预算差，得回去查 `cbs1s*` 那批还开了什么（而 META 不记 ⇒ 仍是 §H-22 那条阻塞）。
**失败也学到**：若两组的当选权重逐字节相同（`EPIRUS_HOTSTART`/收敛太快那类事故），这条实验**没做成**，必须报"空枪"而不是"无差异"。

## H-30 02:32 · 加大样本（45 粒 × 120 局）后，**我自己的分档规则被证明是错的**：还有第二类病被我标成了"无响应"
新加的 `--quiet=1` 表带上了"玩家侧代价"两栏（局长 / 攒钱者夺冠率）之后，同一批包分出**三类**：

| 类别 | 判据 | 粒数 | 局长中位 | 连防中位 | 攒钱者夺冠率中位 |
|---|---|---|---|---|---|
| **响应型** | 倍差 ≥2 且 `ep≥10` ≥20% | 13（29%） | **30** | **31** | 0% |
| **恒定龟缩型** | 对手 `ep0~1` 时就已经设防 ≥20% | **14（31%）** | 22 | 11 | 0% |
| 两类都不是 | — | 18（40%） | 18 | 5 | 0% |
| （对照）现役 3p | — | 1 | 20 | 9 | 0%（`ep0~1` 8.1% → `ep≥10` 2.1%，倍差 0.26） |

- **错在哪**：§H-21 那张表里我给 `cbs1s3-band3`（对手 0~1ep 时设防 **97.4%**）、`kr2s6-band5`（83.6%）、`cbs1s4-band3`（56.5%）等打的都是"**无响应**"——因为它们"倍差≈1"。
  **倍差这把尺只对"响应型"有效**：一只**不分场合一直防**的包倍差永远是 1 附近，被我的规则读成了"健康"。⇒ 实际更糟：它对玩家体验的破坏不依赖对手有没有钱。
  ⇒ 规则改成两把尺并联：**倍差≥2 且高桶≥20%** 或 **低桶（对手没钱时）就≥20%**。合并后 27/45 = **60% 的档案包至少沾一类**（我原先只报出 32%）。
- **代价那一栏没有跟着一起涨 ⇒ 必须降级一条我昨天写得太顺的结论**：H-23 说"换 band2 会让只攒钱的人夺冠率 0% → 13.3%"。
  45 粒合并看，**三类的攒钱者夺冠率中位都是 0%** ⇒ 那个 13.3% 是 **band2 这一粒的特性**（它 `ep≥10` 设防 50.6% 且局长 41），**不是"这体质会奖励龟缩"家族规律**。
  ⇒ 早报里只按"局长被拖长（中位 18 → 30）+ 连防从 5 涨到 31 回合"报代价，**不再引用攒钱者胜率**。
- ⚠️ 口径也记一笔：这一轮 120 局/粒 ⇒ 短局包的 `ep≥10` 桶仍然会缺（表里"那格是'没量到'，不是'量为零'"）；`sweep44`（60 局）那份的 32% 与这里的 29% 属同一档，差在样本与局长，不必纠结谁准。

### H-30 补（同一次运行的四分位，免得"中位数"被当成没有散布）
| 类别 | n | 局长中位 [q1~q3] | 连防中位（max） |
|---|---|---|---|
| 响应型 | 13 | **30 [22~39]** | 31（47） |
| 恒定龟缩 | 14 | 21 [20~30] | 11（47） |
| 干净 | 18 | 18 [17~20] | **5（9）** |
⇒ 分布几乎不重叠：**干净组的 q3（局长 20）还低于响应型的 q1（22）**，连防那一栏更干净——干净组**最大只有 9 回合**，而响应型中位就 31。
⇒ 也就是说"要不要立这一栏"其实不需要复杂阈值：**连防 ≥15 回合**这一条就能把 13/13 响应型与 18/18 干净组全分开（中间没有样本）。
   现役是局长 20 / 连防 9 / 倍差 0.26 ⇒ 落在干净组内。⚠️ 这只是**描述能力**的证据，不代表这条判据该不该上线（那是用户裁定）。

## H-31 02:40 · E10 结果：**预算不是那根轴**——1500 代没消除吸引子，只是让它**换了形态**（而"换形态"这件事恰好第二次应验了 H-30 那条方法论）
同一批种子（77/78/79）、同样 3 人 × 12 局 × 种群 16、零旋钮，只把 300 代换成 1500 代；两把尺并联（H-30 的修正版）来判：

| seed | 300 代基线 | 1500 代基线 |
|---|---|---|
| 77 | 干净（倍差 —、连防 5） | 干净（倍差 0.67、连防 7、局长 22） |
| 78 | **恒定龟缩**（对手 0~1ep 时就防 64%） | 干净（倍差 1.72、低桶 8.7%、连防 6） |
| 79 | **响应型**（倍差 **11.45**、`ep≥10` 88.7%、连防 46） | **恒定龟缩**（低桶 **100%**、倍差 0.68、连防 18、局长 47=上限） |

- **按预注册的原判据（倍差）看，1500 代"更干净"**（沾病 1/3 vs 2/3）⇒ 好像"多跑几代有益"；
  **但按 H-30 补的第二把尺看，seed 79 只是从"看见钱才防"变成"不分场合一直防"（低桶设防 100%、局长直接顶到 47 回合上限）**⇒ 同一粒种子换了病，不是好了。
- ⇒ **E10 落点：预算不是这体质的成因，也不是解药**；n=3 个种子也撑不起任何方向的结论（Fisher 检验 p≈1.0）。
  与 §H-28 合起来：**"看见钱才防 / 一直防"是当前目标函数（5 人名次适应度）上同一个吸引子的两种表现，靠换种子/换预算都是在抽奖。**
- **方法论上值回票价的一条**：H-30 刚立的规矩（比值尺对"分子分母一起涨"失明）**在 E10 自己的数据上第二次抓到误判**——
  如果我只按预注册的倍差判，会写下"1500 代更好"这种话。⇒ 规矩：预注册里的判据要允许被**同一夜的新方法论**追补，但追补必须写明"这是改判据、不是改结果"（本节两条都印）。
- 交接 §4 的 E1~E7 至此全部跑完（E1/E2/E3/E4/E5/E6/E7 见 H-1~H-16），E8~E10 是我自己加的三件，都已各自有结论。

## H-32 02:43 · 收工状态与**明早该裁定的清单**（判据侧我一格都没动，全部列在这里）
本夜落地的都是**工具/量具/文档**：`np-test 199/199` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36` · 引擎与两槽与 `origin/main` **逐字节一致** · `results/*` 只读未提交 · 分支 `qoder-handoff-0924` 已到 **v1.5.214**（15+ 次提交）。

**要用户点头的（按我判断的轻重排）**：
1. **候选池里 60% 沾"龟缩体质"（响应型 32% + 恒定龟缩型 31%）⇒ 换候选不解决问题**。要不要给体检/promote 加一栏"**对手攒钱时的设防持续性**"（只记录起步，装配与读法都现成：`probe-defense-cause --quiet=1`；四分位显示"连防 ≥15 回合"这一条就能把 13 粒响应型与 18 粒干净组**零重叠**分开）。
2. **`G<3` 这条广度线有 53% 的阻挡是口径造成的**（档案 160 粒双口径：84 粒 → 8 粒、跨界 76:0）；**但搬到产品口径它会剩 5% 阻挡率 = 没判别力**。三选一：(a) 留 ε=0 抬线；(b) 换成与探索无关的定义；(c) 双口径并报都要过。⚠️ 我一度倾向 (b)，被自己的锚点表否掉（净兑现挂 3 会挡掉已知好的 `v7divK-31`）。
3. **E3 的 G7「破龟群」草案**（未安装）：按口径重读后**只剩"终场存活≥50%"这一栏在分辨**（现役 ε=0 是 0%、产品口径 43%，而"伤害≥4/局"那一栏现役在产品口径已经有 4.63）⇒ 若要立，**只立存活那一栏**。
4. **`train-3p` 的 band-save META 不记 `EPIRUS_*` 配方** ⇒ 今晚"哪根旋钮养出体质"这问结构上无法回答（E9/E10 就是为此而跑的前瞻实验）。修法很便宜：把 `server/knob-guard.mjs` 已经认得的实际 env 键值回写进 META。
5. **`EPIRUS_SEL_LAND`（我 09-24 推荐的当选键）实测 0/3 种子咬合**（三对当选权重逐字节相同）⇒ 它现在基本是摆设；要么先解决"同分带里得有第二粒"（种群/预算），要么摘掉别留迷惑项。
6. **击杀奖励线结案确认**（§H-1 之前）与 **`EPIRUS_BREADTH_FLOOR` 的 N=3 口径**：两条老账仍待点头/摇头。

**我自己认错的部分**（都写在原位，不删）：把 32% 当家族比例（漏了恒定型）、"奖励龟缩 13.3%"升格成家族规律、一次把别处的数字串成"本次结果"（`3.73`）、五条日志钟点是估的、以及 v1.5.208 倾向的 (b) 被自己的锚点表否掉。

## H-33 02:48 · 把"① 经济 / ② 选择"这个拆分推到候选池上，结论是**它按包分裂，不是家族体质**（而 DS 早就把 `BEAD_W` 那根旋钮跑尽了）
六粒包同一装配（60 局 · 5 席镜像 · ε=0 · `probe-bead-loop`）：

| 包 | 蓄能后配到下一次决策的 n | ① 买不起 | ② 买得起却没射 | ③ 射了 |
|---|---|---|---|---|
| 现役 3p | **385** | 1.0% | **68.1%**（其中 256/262 在按 ジ） | 30.9% |
| `v7cmin4-82` | **1617** | **83.3%** | 11.7% | 5.0% |
| `cbs1s2-band2` | **5** | 100% | 0 | 0 |
| `cbs1s5-band1` | **6** | 100% | 0 | 0 |
| `co1s8-band1` | **1** | 100% | 0 | 0 |
| `v7aim3-93` | **0** | — | — | — |

- ⇒ **② 那一档（"用得起却不射，转头按 ジ"）目前是**现役这一粒的特性**，不是池子的共同体质**：其他候选压根不蓄能（n=0~6），
  所以既不能说它们"更会射"，也不能说"不会蓄"——**它们是没量到，不是量为零**（这条表自己会印"样本 <60 ⇒ 只当方向"）。
- 与 §H-1 接起来更清楚：**现役 = "会蓄、会攒、两样都不花"**（蓄能 385 次里 68% 用得起没射；大雷 `ep≥5` 占 44.8% 时成交率 0.0%）；
  `v7cmin4-82` = "疯狂蓄（1617 次）但真的买不起下游"（83%）；`cbs1s*`/`co1s8`/`aim3` = **几乎不蓄**（这三类在珠这条线上是"未测"）。
  ⇒ 想治"空蓄能"，得**先决定要治哪一粒**：现役要治的是"用得起不射"的打分，`cmin4-82` 要治的是经济，别的包根本没有这条病。
- ⚠️ **这条旋钮不用再试**：`js/train/evo.js:385` 记着 DS 已实测 `BEAD_W` **0.15/0.25/0.5/1.0 四档 × 三 seed 都买不动**（我本来想跑 E11 试它，查到这条就停了 ⇒ 省 20 分钟与一粒空枪臂）。

## H-34 02:49 · 收工前把本夜**每一个数字**机器对了一遍输出文件：**0 个无出处**（这个自查法可复用，专治 §H-26 那类"串号"）
做法：把 `/tmp/kr3/*`（本夜所有量具输出，约 108 万字符）拼成一个语料，再从 §H-19 之后的正文里正则抽出所有 `\d+\.\d%` 与所有 `\d+\.\d`，逐个回查是否出现在语料中。
- 结果：**小数 55 个全部命中、百分数 84 个全部命中**；CHANGELOG 的 v1.5.210~214 头部 17 个百分数也全部命中。
- 顺手扫了 v1.5.211 往下的**全部历史条目**，只有 7 个数找不到（`41.9/48.9/50.5/55.1/67.6/85.3/85.5`），逐一定位后它们分属 **v1.5.131~180** 这些**几周前的旧条目**（旧临时输出早就不在了）⇒ 不是本夜的账，也不去改历史。
- ⇒ 建议固化成一个可复用的小工具（**我没做，避免又添一件没人维护的脚本**）：`CHANGELOG/日志数字 ⊆ 当日量具输出`。§H-26 那次串号如果有这道检查，当时就会红。

## H-35 03:00 · 待裁件 (iii) 的数据答复：**G4/G5 该印噪声带**——同一格 n=70 实测偏 +2pt、n=240 偏 +1pt，但**"这次偏得小"不等于"带子窄"**
拿 `probe-g4-anatomy` 的 §A 自检（它比对的是"只枪线 vs 线上包"这一格，冻结期望值 long 38% / multi 35%）：

| 样本 | long 读到（偏） | multi 读到（偏） |
|---|---|---|
| n=70 | 40%（**+2pt**） | 36%（+1pt） |
| n=240 | 39%（+1pt） | 35%（0） |

- 二项算一下就知道带子有多宽：一格 ~38% 的胜率在 n=70 时 SE=√(.38·.62/70)=**5.7pt** ⇒ **95% 带 ±11pt**；n=240 收到 ±5.5pt；n=300 是 ±5.4pt。
  ⇒ 上面两次"偏 +1~2pt"只是**一次抽到的位置**，不改变带子宽度——**这正是危险所在**：读表的人会以为"n=70 也挺准"。
- 而判据恰好骑在边界上时就会翻结论：本仓真实旧账 `co1s8-band1` 的 G5[multi] 在 **n=60 读 28%（FAIL，线 25%）→ n=300 读 22%（PASS）**，6pt 的差**完全在 n=60 的 ±11pt 带内**。
  ⇒ 所以答复是：**要印**（`±1.96SE` 或直接印"带子跨过线"的标记）；`25%/60%` 这两条线在不印带的情况下，对边界格就是掷硬币。
- 顺带把这条与 D145（座位极差的线随 n 标定）对齐：**同一族病，同一味药** —— 线要按该 n 的零分布/带子来判，不能固定一个数。

## H-36 03:02 · 产物归置：E9/E10 的 11 粒当选包留在 **`docs/artifacts/e9-out/`**（被 `.gitignore:27` 的 `docs/artifacts/*-out*/` 覆盖 ⇒ 不入库、`git status` 干净）
理由：它们的配方今晚无法从 META 反查（§H-22），所以**包本身是唯一可复算的证据**；`mktemp` 目录一重启就没了，而 D82 扫的是 `docs/artifacts` 顶层（`readdirSync` 不递归），放子目录既不欠点名也不脏仓库。
⇒ 明天任何人要复算"零旋钮臂也会掉进吸引子"（§H-28）：`node tools/probe-defense-cause.mjs --quiet=1 --packs=docs/artifacts/e9-out/sp779a-band6.bak,...`。

## H-37 03:18 · 预注册 E12（大样本体质筛，正在跑）：**要把"线画在哪"这件事变成一次可证否的读数**，而不是我口头推荐
问题（待裁件 #1 的实际卡点）：如果真要加"设防持续性"这一栏，阈值取多少？我上一夜口头说过"连防≥15 回合看着能把两端分开"，
但那是**在 45 粒 × 120 局这一份样本上看极值**说的 —— 极值处最容易过拟合。⇒ 现在扩到 **200 粒档案包 × 150 局**（`--quiet=1`，装配同 §H-21）重做。
**跑之前就写死的读数与判据**（不许看完再改）：
- 报三个比例：响应型（倍差≥2 且 `ep≥10`≥20%）、恒定龟缩型（`ep0~1` 就≥20%）、两者并集 —— 各带 **±1.96SE 的宽区间**（n≈200 ⇒ 60% 那条的 SE ≈3.5pt）。
- 再报**候选线的 ROC 式读数**：`连防 ≥15 回合` 单条 ⇒ 覆盖多少比例的响应型（TP）、误伤多少干净组（FP）。
  **通过标准：TP ≥90% 且 FP ≤10%** ⇒ 这条线可以进早报当候选；否则**明写"单条不够，必须两栏并联"**（连防 + 水平设防率）。
- ⚠️ 同时报"分母不足（`ep≥10` 那格 n<40）"的粒数 —— 短局包会大量落在这里，**它们是"没量到"，不能算"无响应"**（这条决定响应型比例的置信区间是不是可信）。

## H-38 03:25 · E12 第一次跑崩在第 159 粒上，原因值得单独记：**`docs/artifacts/*.bak` 里混着两个 index.html 的备份**
`index-html-before-ab.bak` / `index-html-before-ab2.bak` 是 HTML 文件（`<!DOCTYPE html>…`），不是冠军包 ⇒ `vm.runInNewContext` 当场 `SyntaxError: Unexpected token '<'`，
**整场 200 粒的筛作废**（我已经跑了 158 粒，全丢）。
- 数了一下：`docs/artifacts/` 下 1451 个 `.bak`，其中 **2 个不是包**。⇒ 之前 §H-13/H-21 那两轮（排序后每 9/12 取 1）抽样**恰好没抽到**它们，读数不受影响（已按同一规则复算样本成员确认）。
- 修法（已做）：两个扫池工具都改成**先认货再装载**（`window.EPIRUS_CHAMPION` 不在前 4000 字节里就跳过），并把跳过项**计数点名**印在汇总里。
  ⇒ 这是 §47.4"静默跳过 = 关掉测量"的反面教训的**新亚种**：跳过必须**可见**（印名字与数量），否则"n=198"会被读成"扫了 200 粒"。
- **给 DS 的一条小账（不代改）**：D82 按 24 小时时窗点名 `.bak`，而目录里存在**非包**的 `.bak` ⇒ 今天没撞上（那两个是旧文件），
  但下一次谁备份别的文件到那个目录、或谁再写一个"扫 `*.bak` 当包"的工具，就会再撞。便宜的处理：非包备份改用 `.bak-html` 之类后缀，或扫池工具统一走"认货"过滤（本夜已做）。

## H-39 03:29 · E12 跑完（198 粒 × 150 局）：**我口头推荐的那条线没过自己写的标准**
预注册标准（§H-37，跑前写死）：候选线 `连防 ≥15 回合` 要 **TP ≥90% 且 FP ≤10%**。实测：

| 阈值 | TP（覆盖多少响应型） | FP（误伤多少干净组） | 判定 |
|---|---|---|---|
| 连防≥10 | 43/49 = **88%** | 6/92 = 7% | ✗（TP 不足） |
| 连防≥15 | 41/49 = **84%** | 2/92 = 2% | ✗（TP 不足） |
| 连防≥20 | 40/49 = 82% | 1/92 = 1% | ✗ |
| 连防≥30 | 32/49 = 65% | 1/92 = 1% | ✗ |

- **三个比例（各带 95% 区间）**：响应型 **24.7% [18.7~30.8]** · 恒定龟缩型 **28.8% [22.5~35.1]** · 并集 **53.5% [46.6~60.5]**（198 粒，另有 **17 粒高桶没量到** ⇒ 它们的"响应型"判定不可用，8.6%）。
  ⇒ 与 §H-30 那份 45 粒（29%/31%/60%）在区间内一致；也说明**样本从 45 涨到 198，区间宽度从约 ±14pt 收到约 ±7pt**。
- **落点**：**单靠"连防长度"这一条判据不成立**——它漏掉 15% 的响应型（响应型里有 8 粒连防 <15 回合，即"会转防但转得不长"）。
  ⇒ 要真立这一栏，得是**两栏并联**（连防长度 **或** 高桶设防率/倍差本身），而我原先给用户的"连防≥15 就能零重叠分开两端"这句话**只在 45 粒的极值上成立，扩样后不成立** ⇒ 撤回。
  ⚠️ 这条是**判据**，仍然只报不装。
- **方法论收口（本夜第 5 条自我更正）**：极值型阈值最容易在小样本上"看起来干净"——45 粒时干净组最大连防只有 9，所以线画 15 显得宽松安全；198 粒里干净组仍只有 2 粒超线（FP 2%），但**响应型的下尾**（8 粒 <15）被小样本藏住了。
  ⇒ 定线要报的是 **TP 与 FP 两条**，不是"两组极值不相交"。

## H-40 03:36 · 把"线该画在哪"算成一张 ROC 菜单（198 粒），并如实标注**哪些 TP 是自证式的**
拿 §H-39 那份表（响应型/恒定型/干净三组，n=49/57/92）把几种候选判据都过一遍：

| 判据 | 覆盖"会龟"两类 | 误伤干净组 | 备注 |
|---|---|---|---|
| 连防 ≥12 | 响应型 43/49 = 87.8% | **2/92 = 2.2%** | 单条最好的一档，但 TP 仍 <90% ⇒ 预注册标准不过 |
| 连防 ≥8 | 45/49 = 91.8% | 12/92 = 13.0% | TP 过线，FP 超 10% |
| 局长 ≥25 | 39/49 = 79.6% | 28/92 = **30.4%** | ⇒ **别拿局长当判据**：干净组里也有一大把长局 |
| 高桶设防 ≥20% | 49/49 = 100% | 7/92 = 7.6% | ⚠️ **自证式**：响应型的定义就含这一条 ⇒ TP=100% 没有信息量 |
| 低桶设防 ≥20% | 恒定型 57/57 = 100% | 0/92 = 0.0% | ⚠️ 同样自证式（恒定型定义即此） |
| **低桶≥20% 或 连防≥12**（两栏并联） | **100/106 = 94%** | **2/92 = 2.2%** | 达到预注册标准（TP≥90 且 FP≤10）—— 但其中"恒定型全覆盖"那半是自证式的 |

- ⇒ 真正**非循环**的那个数是：**响应型 49 粒里有 6 粒（12%）连防 <12 且低桶 <20%，任何这套判据都抓不到它们**（例：`cr1s2-band6`、`co1s3-band4`、`cbs1s5-band1`）——它们"会转防但转得不长、平时也不摆"。
  ⇒ 所以就算装了这栏，**它对"响应型但短促"的那部分仍是瞎的**；要覆盖那部分只能直接看**倍差本身**（而倍差需要"对手攒钱"这一格装配，成本更高）。
- **早报里给用户的建议因此改成两句**：① 若要低成本加栏，用"低桶≥20% 或 连防≥12"，**明说它漏 12% 的响应型**；② 若要真覆盖，就得把"同一回合号跨 hold/cycle 配对的倍差"做成常规装配（成本约 2× ，因为它要跑两档替身）。
- ⚠️ 记一条方法学：**用自己定义标签的变量去评 TP 是循环论证**（上表两行 100% 即此）。评判据时要报的是"干净组误伤率"+"标签之外的漏网名单"，不是 TP=100%。

## H-41 10:07 · 两口径的"能不能看见"对表 + **两个假说各自被一条事实直接打死**（省下六条臂）
**① 体质在 ε=0 几乎不可见（同一批 198 粒、同一装配，只换口径）**：

| 口径 | 响应型 | 恒定龟缩型 | 并集 |
|---|---|---|---|
| 产品 ε=0.2 soft | **49（24.7%）** | 61 | **106（53.5%）** |
| 评测 ε=0 | **14（7.1%）** | 56 | **68（34.3%）** |

- **响应型 49 粒里有 37 粒（76%）在 ε=0 完全抓不到**；两口径都抓到的只有 67 粒；反向（只有 ε=0 抓到）只有 1 粒（`cr1s6-band1`）⇒ 不是对称噪声，是**单向盲区**。
- ⇒ 这条直接改变裁定 ① 的形状：**若把"设防持续性"挂现有 ε=0 门禁机器上，等于装了个只能看见 1/4 的栏**；要有效就必须单独装配并跑产品口径（成本约 2×，因为要 hold/cycle 两档）。
- ⇒ 同时这也是"门禁 ε=0 是有意设计"（v1.5.152/D118）那条裁定的**具体代价**第一次被量化成"漏 76%"。这不构成"该改口径"的结论——是"该在哪一侧装栏"的事实。

**② 我原本想做的解药实验被一条代码事实否掉了**：假设是"训练桌上没有会攒钱的人 ⇒ 学不会破攒钱"。
  但 `tools/train-3p.mjs` 的默认 `OPPS` **早就包含** `breakdef`（`bots.js:948`：86% 出ジ 攒钱等落雷）与 `farmer`（囤 ep 型），共 9 个对手里 2 个就是攒钱者。
  ⇒ 桌上有攒钱者，体质仍有 53.5% ⇒ **假说不成立，六条臂（基线 vs 加攒钱席 × 3 seed）不用跑**。
  ⇒ 剩下的解释方向只有一个更朴素的：**镜像自对战（5 席同包）这一格本身**——桌上有攒钱者不等于"你**队友**也全是同一只包"，而龟缩回路的成立条件恰恰是 4 席同包互不相犯（§H-15 的 `ep=0→ジ / ep=1→枪` 极限环是同一件事的另一面）。这条**还没量**（要做就得改适应度的装配，属训练侧但会动 fitness 形状，明天问一句再动）。

**③ 新工具 `tools/screen-cheap-predict.mjs`（正在跑，198 粒 × 100 局）**：问"**能不能用门已经算出来的 ε=0 镜像读数（按ジ占比/每回合伤害/局长/蓄能/珠浪费）预测体质？**"
  ⇒ 能 ⇒ 这栏近乎零成本；不能 ⇒ 明说必须单独装配。跑完在 §H-42 给对表与分离度（工具本身不重复实现标签，避免第二份真源）。

## H-42 10:12 · 一条自省的规矩（本夜我连踩三次同一类错，全是"改完工具没立刻跑"）
1. 给扫池工具加"先认货"时**漏 import `readFileSync`** ⇒ 宽 catch 把 ReferenceError 洗成"200 粒全读不动"，**退出码还是 0**（差点把崩溃当成结论）；
2. `probe-breadth-flip` 同样漏 import ⇒ 它没有宽 catch，所以**大声崩**（崩比静默好）；
3. 用 python 字符串替换改 JS 时把 `'\n'` 写成**真换行**塞进单引号字符串 ⇒ `SyntaxError`，而且这次是 `node --check` 直接挡下（前两次 check 也挡不住 #1/#3 那类）。
⇒ 固化：**改完工具必须立刻跑最小用例**（`--games=4 --packs=<一粒>`），`node --check` 只能挡语法，挡不了 ReferenceError 与"静默全跳过"。
   本夜之后新加的兜网：D150 里"`probe-breadth-flip` 必须跑得通 + 必须点名跳过"、catch 只认 `e.code`、"一粒没量到也非零退出"。

## H-43 10:14 · 预注册 E13（等 §H-41③ 那张表跑完就做）：**目标函数到底罚不罚龟缩？**——用已经存在的 `trainFit` 与体质标签对表
动机：§H-41 把"桌上缺攒钱者"这个解药否掉之后，剩下唯一还站得住的结构解释是**"自对战里龟缩没有代价"**（5 席同包互不犯 ⇒ 名次对所有人都是 ~20% ⇒ 没有梯度）。
这句话现在是**推断**，而它如果成立就直接决定裁定 ①/② 的形状（"要动适应度"vs"只要动验收栏"）。**不用重跑任何臂**就能量：`docs/artifacts/*.bak` 的 META 里存着当选时的 `trainFit`。
**读数（跑前写死）**：同一批 ~198 粒，按体质三组（响应/恒定/干净）分别取 `trainFit` 的中位与四分位；再算 `trainFit` 与"镜像 ε=0 低桶设防率"的 Spearman。
**判据**：
- 若**响应型/恒定型的中位 `trainFit` ≥ 干净组** ⇒ "适应度对龟缩无惩罚"从推断升级为**实测**，且裁定①应升级为"这是目标函数的事，加栏只能挡住出货、挡不住长出来"；
- 若**明显更低**（例如差 >0.05 且分组不重叠多数）⇒ 梯度是有的，体质能过关是**当选带/带宽**那一层的事（与 §N24/当选键那族同源），修法就不一样；
- 中间情形如实报"分不开"。
**必须声明的三个混淆**：① `trainFit` 跨臂不可比（不同预算/配方，见 §N14 那条"A 考卷差 25pt 其实是预算"）⇒ 所以**同一家族内**（`cbs1s*`、`co1s*`、`cr1s*`… 各自内部）也要各算一遍再合并；
② `trainFit` 是**当选那一粒**的值，不是全带 ⇒ 带宽内差异被压掉；③ 干净组里含老包（预算/特征代不同），所以要另报"仅 09-24/25 新臂"的子样本。

## H-44 10:18 · E13 交作业：**跨臂版"分不开"，配对版也只到"无信号"**；但顺手挖出一条关于"当选键"的事实
按 §H-43 预注册先跑跨臂版（三组中位 `trainFit`：响应 1.209 / 恒定 1.175 / 干净 1.183）⇒ **三组挤在 0.03 里**，且 198 粒里只有 8 粒所在臂有 ≥4 带 ⇒ 跨臂版按预注册的第二/三档只能报"分不开"。
改跑**同臂配对版**（这是 §H-43 混淆①的正解：同臂 = 同预算同配方同种子，唯一变量是哪一带当选）：筛表 194 粒 ∩ META = 105 粒 ⇒ **≥2 带且恰有 1 粒当选的臂只有 8 条**（61 条臂有完整 6 带，但 198 抽样是"每 N 取 1"，把一臂拆散了 ⇒ 这是抽样设计的问题，不是数据的）。
**读数（8 臂，选中粒 − 同臂兄弟均值）**：连防最长 Δ 中位 **+4.80**（4 臂更低 / 4 臂更高，平均秩 2.50 = 零假设值）；局长 Δ 中位 −1.80（5 低 / 3 高，秩 1.75）；二值（有无龟缩体质）选中更差 4 臂 / 更好 3 臂 / 平 1 臂。
⇒ 按 §H-43 判据落"**分不开**"：当选那一粒**并不**系统比兄弟干净，也**并不**系统更脏。8 臂的符号检验要 7:1 才到 p<0.05，所以这条**只是"没测到"，不是"测到没有"**——要坐实得跑"整臂全带"而不是抽样（见 §H-46）。
背景板（不限同臂，⚠ 混臂 ⇒ 只能当背景）：当选粒 n=65 龟缩体质 50.8% / 连防中位 8 / 局长中位 22，未选中 n=40 是 55.0% / 15 / 30。方向偏"当选更干净"，但差距来自哪 one-hot 不出来。

**顺带挖到的事实（比 E13 主判更有用）**：自检"当选 == 臂内 `trainFit` 最高"在 **8 条臂里只成立 1 条**。例：`chainbase` 选 band3（fit 0.979）而非 band1（1.245）；`chainw15` 选 band4（1.434）而非 band1（1.712）。
读了 `tools/train-3p.mjs:627/687-768` 才知道为什么：带内最高 `trainFit` 之后还有**三道改判**——名人堂用 `evalN(ALL_PAIRS,20局,seed 987654)` 的 `firstRate+0.5·top2Rate` 重排、§N9 退化闸（`densityProfile.zeroAtkRate≥0.9` 判"纯ジ龟包"）、`EPIRUS_SEL_LAND` 兑现广度带内换人。
⇒ 含义：**"目标函数罚不罚龟缩"这一问，在当选层已经被三道闸各拦了一次，而体质照样漏过来**（`cbs1s2-band2` 就是过了这三道闸出货的）。这把 §H-43 的"要么罚要么不罚"二选一，改成了"罚的机关存在、但三条键（胜负分/零出手率/兑现广度）都不看'对手有钱时你会不会转防'，所以拦不住"。裁定①的形状因此偏向**加验收栏**而不是**改适应度**——但这条要靠 §H-46 的全档案筛坐实。

## H-45 10:23 · 裁定①的"能不能免费"这一问，答案是**不能**：六栏 ε=0 镜像读数对体质的 ROC 全在对角线上
动机（§H-41 留的）：加"设防持续性"栏到底贵不贵，取决于**门已经算的那几个 ε=0 数**能不能预测体质。跑 198 粒 × 100 局镜像（`tools/screen-cheap-predict.mjs`）后与筛表对表，194 粒同进两张表。
标签（产品口径筛表定义）：响应 46 / 恒定龟 39 / 其余 109 ⇒ 需拦并集 85 粒（**43.8%**）。
**读数（每栏取"TPR − 1.5·FP"最优点）**：
| ε=0 栏 | 需拦组中位 | 干净组中位 | 最优 TPR | 同点 FP |
|---|---|---|---|---|
| 按ジ% | 49.3 | 58.8 | 48.2% | 11.9% |
| 伤害/回合 | 37.0 | 39.0 | 12.9% | 4.6% |
| 出手伤害% | 22.2 | 31.5 | 60.0% | 23.9% |
| 局长 | 32.0 | 32.2 | 0.0% | 1.8% |
| 蓄能/局 | 0.1 | 0.1 | 3.5% | 4.6% |
| 珠浪费% | 79.0 | 88.0 | 36.5% | 36.7% |
两栏组合也试过（`按ジ≥70 或 局长≥45` ⇒ TPR 21.2% / FP 15.6%），仍在对角线附近。
**三条要点**：
1. **方向是反的**：六栏里凡是有点分离度的都判"越小越可疑"——被产品口径标为龟的粒，在 ε=0 镜像里设防份额反而**更低**（49.3 vs 58.8）。机制也说得通：ε=0 五席同包互不犯 ⇒ 大家都攒着，设防率高是**常态**而不是体质；体质只在"桌上真有一个攒钱的人"时才显形。
2. `局长(ε=0)` 两栏中位 **32.0 vs 32.2** ⇒ 完全零信号；镜像里的局长上限被 47 回合顶死，与"遇到攒钱者才拖长的局"不是同一件事。
3. **锚点直接证伪"用现有栏当判据"**（`anchors-prod.txt` × `anchors-cheap.txt`，7 粒 × 150 局产品口径 + 100 局 ε=0）：
   | 包 | 倍差 | 连防 | 局长(产品) | ‖ 按ジ%(ε0) | 局长(ε0) |
   |---|---|---|---|---|---|
   | `v7seat24-31` | **9.07** | 44 | 46 | 76.6 | **47.0** |
   | `cbs1s2-band2` | 6.43 | 37 | 39 | 60.8 | **17.4** |
   | `v7cmin4-31`（现役） | 0.25 | 9 | 20 | **79.3** | 31.9 |
   | `v7cmin4-82` | 0.26 | 4 | 22 | 60.0 | 39.6 |
   | `v7new4-31` | 0.79 | 7 | 21 | 68.5 | 21.9 |
   | `v7divK-31` | 0.60 | 4 | 16 | 60.0 | 18.1 |
   ⇒ 现役好包的 `按ジ%` 是全场**最高**（79.3），病包 `band2` 只有 60.8；任何以 `按ジ%` 为键的判据都会**优先误伤现役**。而 `band2` 在 ε=0 镜像里局长只有 17.4 回合（看着最健康），它在玩家桌上是 39 回合。
**结论给裁定①**：这栏**不可能零成本**——必须单独装配"1 席攒钱替身 + 4 席被评包"，而且必须跑**产品口径**（ε=0 连看见都看不见）。成本实测：约 20 秒/粒@150 局（比想象中便宜一个量级，见 §H-46 的计时）。

## H-46 10:25 · **一条覆盖度作废通知**：09-25 那份"198 粒体质筛"只覆盖了全档案 **16% 的等价类**，而我以为它是档案普查
触发：给锚点补测时顺手把 `v7cmin4-31 / v7divK-31 / v7seat24-31 / v7xn1-31` 拉进筛表跑，结果两件事叠在一起：
1. **`v7xn1-31` 与 `v7cmin4-31` 每一栏读数逐字相同**（出手事件 10178、四桶 7.8/7.2/5.4/1.9、局长 20、连防 9）⇒ 同权重不同名。于是按权重向量给 `.bak` 去重：**1449 粒可解参数 → 1004 个不同权重**（175 个等价类含 ≥2 粒）。
2. 拿 `same198.txt` 去比对这 1004 类 ⇒ **只覆盖 162 类（16.1%）**，未测 **842 类**，而且漏掉的是**整棵 `v7*` 树（784 类里的绝大多数）**——09-25 那次"每 2 取 1"的抽样恰好按文件名排序把它们错开了。
**最要命的是漏掉的树里就藏着目前测到的最重病例**：`v7seat24-31` 倍差 **9.07×**、`ep≥10` 设防率 **73.3%**、同席连防 **44 回合**、局长中位 **46**（p90 47 = 顶到上限）、平局率 **27%** ⇒ 比引发这次排查的 `cbs1s2-band2`（6.43×/49.8%/37/39/2%）**更严重**。
⇒ 已写下的"32%（或并集 53.5%）的档案有这体质""kr 家族 0/6 干净"这些比例，**适用范围只是 band/消融家族**，不能当"档案普查"引用。这条更正同时削弱两件事：① "再抽一粒骰子还有 1/3 概率同体质"里的"1/3"待重估；② "`kr*s*` 家族免疫"这条反例可能只是抽样假象。
**E14 预注册（正在跑，`repsweep.txt`）**：对 **1004 个等价类各跑 1 粒 × 150 局 × `saver=hold` × 产品口径**，一次性给出：
- **判据（跑前写死）**：报三档数——(a) 响应（倍差≥2 且 `ep≥10`≥20%）占 1004 的比例；(b) 并集（再加"低桶≥20% 且连防≥12"的恒定龟）；(c) **按家族分层**的中位倍差/连防（`v7*` 家族要单独成行，因为它从没被测过）。
- **不许做**：拿这张表推"换哪粒"——它是**比例表**不是候选榜单，换槽仍走用户裁定 + `promote --dry`。
- **成本实测**：8 粒 × 20 局 ≈ 1.0 秒 ⇒ 1004 × 150 局约 25~35 分钟（先前我以为要几小时，§H-45 那句"比想象中便宜一个量级"由此来）。
- **混淆声明**：① 去重按"权重向量逐字相同"，同权重不同槽位口径（2P/5P 维数不同）会落进不同类 ⇒ 类的粒度偏保守（重复没被完全合并）；② 60/150 局的分桶分母 smaller ⇒ 响应判定对"高桶样本不足"的粒会漏（`—` 桶），这类粒在 198 表里有 18 粒，全档案里单列并报数，不并入任何一组。

## H-47 10:26 · 同回合配对的**正式读数**（两粒病例 × `hold`/`cycle`）：**"钱"这一因子的净效应 = band2 2.46×、seat24 1.49×**，而两档在 ep 分岔前**5/5 回合逐字重合**
之前 §H-32/§H-36 只做过 band2 一粒的两档对照，且靠人眼比曲线。这次把锚点跑齐后**逐回合对齐算差**（同种子 4100、150 局、产品口径、`saver=hold` vs `saver=cycle`）：
| 回合 | `cbs1s2-band2` hold→cycle | 倍差 | `v7seat24-31` hold→cycle | 倍差 |
|---|---|---|---|---|
| r1~r5（两档 ep 还没分岔） | 13.3/2.2/2.3/15.1/19.9 —— **两档一字不差** | 1.00× | 13.3/2.2/6.5/40.2/38.7 —— **两档一字不差** | 1.00× |
| r6~r14 均值 | **37.2% vs 15.1%** | **2.46×** | **51.9% vs 34.8%** | **1.49×** |
玩家侧代价同表对照：局长中位 `band2` 39 → **15**，`seat24` 46 → **20**；`band2` 连防 37 → 6、≥5 连 54% → 2.7%；`seat24` 连防 44 → 13、≥5 连 68.7% → 45.3%；`band2` 那个攒钱的人夺冠率 hold 13.3% → **cycle 27.3%**（钱被压住之后，攒钱者反而赢得更多）。
**三条判读（这次能说的比"倍差"多）**：
1. **因果坐实**：placebo 窗（ep 未分岔处两档逐字相同，5/5）排除了"两档只是换了批随机数"，而分岔恰好从 ep 分岔那一回合开始 ⇒ 同回合号上的 2.46×/1.49× 只能是"对手手里的钱"造成的。这条把用户原话从"看起来像"升级到"实测"。
2. **两粒病例的病因不同**：`seat24` 的 cycle 档自己就有 ~35% 的后期设防平台 ⇒ 它主要是**"越晚越龟"**（回合日程），钱只是再乘 1.49×；`band2` 的 cycle 档掉回 15%（≈现役水平）⇒ 它几乎是**纯"钱驱动"**。⇒ 修法不该一样：`band2` 这型要在"对手 ep"这一维上加约束/加训练对手；`seat24` 这型光加那一维**治不到**它的晚局龟缩。
3. **"局长"这条玩家体验轴是替身档决定的**：同一只包，`hold` 46 回合、`cycle` 20 回合 ⇒ **线上真人是不是"一路攒钱不花"决定了遇不遇到这个病**。这条应该进裁定⑥/①的证据里（如果真人打法更像 cycle，代价会小得多；§H-22 的实机记录是判据来源）。
**顺手抓到一条工具账**：曲线行印的话是"（`--pair` 就是比这条）"，但 `probe-defense-cause.mjs` **没有 `--pair` 这个档**（`grep` 只有一处字符串）⇒ 这是一句指向不存在功能的文案。上面这张表是我用临时脚本对着两份输出手工对齐的；E15 要把 `--pair=1` 真做出来（跑完 E14 再动这个文件，避免改到一半被后台的分块筛读到新代码）。
