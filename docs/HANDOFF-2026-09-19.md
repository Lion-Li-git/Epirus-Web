# 交接（2026-09-19 晚间）—— 给复核者（千问）审 + 给新会话起步

> 这份是**自包含**的：一个新会话读它就能接手；复核者读它能直接对着**可核对的 id**审。
> 更早的过程文档（夜里/下午）：`docs/RESEARCH-LOG-2026-09-18-overnight.md` · `docs/AUDIT-RESPONSE-2026-09-19-overnight.md`
> · `docs/FOLLOWUP-for-ds-2026-09-19.md` · `docs/AUDIT-2026-09-19-post-merge.md`。
> 本会话另出一份门禁审计：`docs/GATE-AUDIT-2026-09-19.md`。

---

## 0. 一页状态（**当前事实**，每条都可核对）

| 项 | 值 |
|---|---|
| 分支 | `main`（版本 **v1.5.131**；`qoder` 与 `qoder-audit-0919` 都已合并进 main，分支都保留未删）|
| **线上 3P 包** | 源 `docs/artifacts/v7press3-91.bak` · **sha1 `1fe3a09fe247`** · `examScoreAtBuild` **0.563**（v1.5.129 因 R61 改引擎而重记 meta，**权重逐字节未变**）|
| **线上 2P 包** | `js/bundled-champion.js` · `examScoreAtBuild` **0.9831** · `examMinBaseline` **0.775** · **`examGateOk: true`**（v1.5.130 重训：珠爆发洞已堵，见 §0c）|
| 规则指纹 | **`00e732a7`**（v1.5.129 起；**2P 与 3P 两个包的 meta 同值** ✓）—— 旧值 `61a7711c` 已作废 |
| 门禁 | `spec-run` **45/45** · `np-test` **151/151（exit 0，全绿）** · `smoke` OK · `battle-test` **BATTLE OK** |
| **门禁红** | **没有**。D67 已按用户裁定②改成"**已记录的例外**"（与克制表口径 id 绑定，见 §0b）；当前它在 n=60 下**走分支②** |
| ~~产品级已知洞~~ **已堵（v1.5.130）** | **2P 冠军的「珠爆发线」洞已关闭**：该线进 2P 训练池 ⇒ 重训后 20 基准**全 >50%**，探针 `probe-beadburst` 该线 **100.0% → 0.0%**（见 §0c）。⚠️ **遗留**：**5 席侧**同一格仍只有 **30%(long)/15%(multi)**，本版**未动**（要动就得改 NP 池 ⇒ 见 §4-1）|
| **新发现（v1.5.131）** | ① **狙击在 V2（1 席 vs 4 balanced）净亏 +15.2pt**（n=600 · p=3.0e-22 · 非零对 103/600；记录里是 +5.8pt/7 对 ⇒ **不是噪声、量级更大**，见 §4-4）。② **R61 让 V2 掉了 24.7pt**：`AUDIT-2026-09-19-post-merge.md` 的 V2 列（**34.2%**）是旧指纹 `61a7711c` 的读数 —— **已用受控 A/B 查清**（独立 worktree @ `2ccd558`，同权重只换引擎）：V2 **34.7% → 10.0%**、V1 85.5% → 85.3%、V4 17.2 → 16.1%/席，且 V2 里狙击出手 **1132 → 716 次** ⇒ **线上 3P 包的权重是"旧语义下的最优解"**（2P 包没有这个问题）。⇒ **V2/V4 没有任何门**，这 24.7pt 静默丢了两个版本；查清过程见 **§4-11（已结案 = R61）**，由此产生的臂见 **§4-12（已跑：重训追不回来 ⇒ 修法是给 V2/V4 立门 = §4-3）** |
| 线上包画像（v1.5.129 重测） | 五道门全过：座位 **11pt** · `G` 8.01 · 墙 17.95 · 场A 30% · 场B 清场 1.0 ｜ 覆盖 4/4 类 · 角色 6/8 · 伤害 **0.637/回合** |
| 已知弱点（G4） | `G4[long] 只枪 **68%**（n=120）/ 75%（n=60）` · `G4[multi] **62%**（n=60）`⇒ 多人的那一格**正好坐在 60% 阈值上**（n=120 时它是过的）⇒ 它的 PASS/FAIL 有一部分是**抽样噪声** |
| 用户实盘记录 | `results/epirus-battle-23回合.txt`（用户赢）与 `epirus-battle-34回合 (2).txt`（用户第 32 回合阵亡）—— **用户的私人记录，不要提交** |

---

## 0b. 本轮（v1.5.129）交付了什么、读数是什么（**下一个会话从这里起步**）

> 用户三条裁定：① 主攻"堵 `docs/REVIEW-QODER-2026-09-19.md` §3 的珠爆发线 + 顺修两处小账"；
> ② **D67 采方案②**（认得 meta 里已留痕的 `--force`）；③ `resolve.js` 的 `voided` 漏口**按 R10 同族直接修**。
> 完整账（含三处自我更正）见 `CHANGELOG.md` 的 **v1.5.129** 段。

1. **R61**（新裁定）：被无效化的一手**不写 `lastSkill`**（`js/core/resolve.js:1179` 加 `!a.voided`）
   ⇒ 打断方不再白送 **R32 激光眼"连用价"**（2 ジ且免爆珠）。守门 **D102**（**带对照组**）+ `tests/spec.js` 的 R61 用例；
   口径写进 `docs/RULES-2P.md` 的 R61 条。
2. **「珠爆发」线入册两处**：`js/train/bots.js` 新增对手脚本 `pickBeadBurst` ——
   ① 2P 考卷（`tools/p2-baselines.mjs` **表尾**第 20 个基准，只追加以保住现有种子）；
   ② G4 克制表（`tools/gate-drafts.mjs` 第 8 格，**直接复用同一个函数对象**，不抄第二份）。
   守门 **D103**（含**端到端**断言）。独立探针：`node tools/probe-beadburst.mjs`。
3. **D67 三分支 + 口径绑定**：`gate-drafts` 打印 `G4POOL <id>`（= 克制表键序 + 阈值 的 sha1 前 8 位，本轮 **`960dfe0b`**），
   `promote-champion` 写 `meta.gate4Pool` / `meta.gate4Forced`（含被放过的具体行）；
   **D67 只在口径 id 相等时才认这条例外** ⇒ 改了池子/阈值，旧记录**自动失效、必须重记**。
4. **两个包重记 meta**（R61 改引擎 ⇒ 指纹必变）：**权重逐字节未变**
   （3P 包 `git diff --numstat` = `1 1`；2P 包工具自检"权重逐字节未变"）。2P 考卷 **平均 93.44% → 89.00%**、最差 **0.00%**。
5. **顺修**：README 的计数漂移（`36/36`、`80/80 D1–D31`、过期的"本版 v1.5.20"块）；
   `docs/METHODOLOGY.md` 新增 **§E.36–39**。

**本轮最该记住的三条读数（都反直觉）**：

- **G4 看不见 1v1 的洞**：「珠爆发」格在 **5 席** harness 里只有 **30%(long)/15%(multi)**（**不是**最克格），
  而同一条线在 **1v1** 里是 **100% 胜 / 均 9.2 回合** ⇒ **判据的座位数必须匹配洞的座位数**（METHODOLOGY §E.38）。
- **单点行为全对 ≠ 整条链转得起来**：我第一版脚本 200 局里 `railgun` 出手 **0** 次
  （珠只活一回合 + 蓄能花 1 ジ ⇒ 死循环）⇒ METHODOLOGY §E.39，D103 的 ①b 就是为它立的端到端断言。
- **"状态空间被包含" ≠ "策略被包含"**（`tools/probe-2p-mismatch.mjs`，n=200/格）：
  3P 冠军打 2P 只有 **16.5%**（6.3 回合），而**同一 matchup 换成 `multi` 逐字节相同** ⇒ 与模式无关；
  判别点是"会不会防最便宜的密度线"——一行**只枪**打 3P 冠军 **99.5%**、打 2P 冠军 **0.0%（0.00 伤/局）**。
  反向也成立：2P 冠军丢进 5 席 ⇒ **0% 胜 / 100% 平**。⇒ METHODOLOGY §E.40；
  也是下面 §4-1 的重要约束：**一个包不可能同时服务两套经济学**。

---

## 0c. v1.5.130 交付了什么（**堵住 2P 的「珠爆发」洞**；下一个会话从这里起步）

> 用户指令：**"那把 2P 冠军修一下吧"** + 提问 **"我在想如果我们有一个很好的 2p 冠军，能不能将其能力蒸馏到 np 冠军里面"**。
> 完整账见 `CHANGELOG.md` 的 **v1.5.130** 段；方法论教训写进 `docs/METHODOLOGY.md` **§F.41**。

1. **洞堵上了**：`js/train/evo.js` 的 `BOT_PICKS` / `buildOpps` 把 `beadburst` 加进 **2P 默认训练池**
   （w=2.0 ⇒ 池子 **19→20 席**、占 **6.25%** 对局）⇒ 重训 `js/bundled-champion.js`：
   `examScoreAtBuild` **0.89 → 0.9831**、`examMinBaseline` **0.00 → 0.775**、**`examGateOk` false → true**；
   探针里该线 **胜 100.0% → 0.0%**（冠军 6.0 回合、`ji:600 gun:600` **抢节奏** —— **不是**靠原型制御/避雷针挡的）。
   ⚠️ **代价**：池子变了 ⇒ 之后所有 2P 训练臂读数与本版之前**不可直接比**；而 `evo.js` **不在** `FINGERPRINT_FILES` 里
   ⇒ 这条代价**没有机械门**会记住（指纹不变、D16 不红），只能靠 CHANGELOG。
2. **热启动**（`train-best.mjs` 原先让 16 个个体**全随机重开**，而 3P 侧早就不是）：实测**同样的池子改动**，
   **全随机重开 = "修一个洞、开两个洞"**（珠爆发 0%→**93%**，但 `defend`/`reflectspam` 100%→**0%**，avg 89%→85%）；
   **热启动 = "0 回归 + 1 修复"**（6 个候选**全部**如此：珠爆发 →100%、其余全保持、avg →98.3%）。
   `EPIRUS_HOTSTART=0` 退回旧口径；接线与 3P 侧同口径（`server/train-server.mjs:272` 的 `T.seedChampion`）。
3. **择优工具的一个假承诺被修**（同一 bug 的另一面）：`train-best` 注释写着"绝不回归"、实现里**没人看它**，
   且闸门罚项**在有洞时退化成常数**（`gateMin*0.4-1`，gateMin=0 ⇒ `sc ≡ -1`）⇒ 它把上面那个"1 洞变 2 洞"的包
   **当成最优写进了线上**。现在择优是纯函数单一来源 **`tools/pick-best.mjs`**（**不回归层** + 容差带 + 发散度），
   产物 meta 记 `prevScoreAtBuild` / `fixesVsPrev` / `regressionsVsPrev` / `hotStart`；新门 **D104**
   （喂合成候选表验 9 条行为）。**门数 150 → 151**。
4. **留档**：`docs/artifacts/champ2p-burst93.bak` = 上面那个"全随机重开"臂的包（**没上线**；它是"这条线**可学**"的证据）。
5. **用户那个蒸馏问题的结论**（细节见 CHANGELOG §5）：**能力转移的载体是"任务/对手线"，不是权重快照**；
   权重的角色是**起点**（它决定的是"学到新东西时会不会忘掉旧的"）。只搬权重 ⇒ 搬来的是**别人桌上的均衡**
   （2P 包丢进 5 席 = **0% 胜 / 100% 平**，§E.40）。⇒ 要在 NP 侧复现本版的成功，动作是
   **把珠爆发线加进 `server/opp-pool.mjs`**（+ 保持既有热启动），而**不是**把 2P 包塞进 NP 槽或用它做教师。
   **代价同第 1 条（之后 NP 臂读数不可比）⇒ 属用户的判据/预算决定，本版未执行。**

---

## 1. 本会话交付清单

### 1.1 规则（全部是**用户裁定**，每条都有可执行存档）

| 编号 | 内容 | 守门 |
|---|---|---|
| **R61**（v1.5.129）| **被无效化的一手不写 `lastSkill`**（`js/core/resolve.js:1179` 加 `!a.voided`）⇒ 打断方不再白送 R32 激光眼"连用价"；与 R10 同族，见 `docs/RULES-2P.md` 的 R61 条 | D102 + spec |
| **R24** | 原型制御"伤害总数 ≥3 各自转移给作用者"按用户口径重做为**决算式**：`deliverDamage` 照旧挡 + `protoNote` 记账，伤害阶段末尾 `protoReflectAll` 裁决；**有来源的各自反给施法者、无来源的（天火）只计入不反**；地雷/转移/**铁索传导**不计入（传导在 `rawDamage` 里直接改血 ⇒ 天然不被挡）| D99 + spec |
| **R56** | 同层内**资源型技能先结算**（过载炮的清除须含目标本回合收入）| D92 |
| **R57** | 天火**不需要目标**（卡面声明 + 引擎全场 + 文档三条一致；伤害**无来源** ⇒ 不触发大雷传导）| D93 |
| **R58** | 藤甲火弱**覆盖当回合 + 下回合**（贴上即生效，第三回合起失效）| D94 |
| **R59** | 地雷 = **当前回合 + 其后两回合**的自身 buff（取代 R38「持续直到被触发」）| D95 |
| **R60** | 净化清除**自身全部持续状态**（含增益：藤甲/地雷/避雷针/符咒/大雷禁用/梦魇）| D96 |
| **R10**（更早）| 被无效化的聚能环**不得续计**（出招即计次 + 复位漏 `voided` ⇒ 打断者反而送 +3）| D91 |

**用户实盘报的两个 bug 都已修**：
① 原型制御 ≥3 从未实现（原版 `README.md:257` 的后半句）⇒ 已实现（R24 口径）；
② **"无根据的突然集火"的根因是一条死特征** —— `policy.js` 的"目标带架势"那一维读 `X.guardOf(state, tid)`，
而**决策时刻 `state.actions` 全是 null**（`startTurn` 清空 + `autoGameN` 先收 picks 再施加）⇒ 实测**恒 null**
⇒ 改用决策时刻真有的信号 `(t.guardNext || t.baguaExtra || t.copiedGuard)`（守门 D99）。

### 1.2 训练侧（全部默认关 ⇒ 出厂行为一字不变）

| 旋钮 | 作用 | 守门 |
|---|---|---|
| `EPIRUS_BLOCK_W` → `blockW`（E4）| 给"**真的挡下**伤害"计分（`blocked`/`reflect` 且 `to===seat`；摆架势不算）| D100 |
| `EPIRUS_WIDTH_W` → `widthW` | 广度收益项（按**绝对**"用过的招数种类数"，`≤3 不给钱 · ≥7 给满`）| D100 的窗口子检查 + D80 |
| `EPIRUS_BIGCARD_W` → `bigcardW` | **贵卡出手**奖励（贵卡 = `cost≥3` **或** `energyNeeds` 非空 —— 从声明字段推导，不写卡名清单）| D101 |
| 热启动 | 训练起点改用 **`EPIRUS_BUNDLE_IN=js/bundled-champion-3p.js`**（= 最新冠军），不再从沿用已久的旧种子长 | 实测见 §2.2 |

### 1.3 量具与守门（新增/修正）

- **"终局收缩"哨兵**从 `damage.source == null` 改为 `reason === '终局收缩'`（旧口径被地雷/天火的无来源伤害误触发 ⇒
  场B 清场被系统性少读；实测同批 60 局里 **40 局读错**：1.33/局 vs 真值 4.00/局）⇒ `audit-lib.mjs` + `evo.js:countClears` 同步，守门 **D98**。
- 新增守门：**D97**（环的段长口径）· **D98** · **D99** · **D100**（E4）· **D101**（贵卡）。
- 复核者的量具三修已随分支合并进 main（`l2-e1-check` 的 p 只算单尾 · `probe-ep-saturation` 硬编码包名 · `probe-leftover` 没区分原始输出与实际执行）。
- 合并了他两个分支（`qoder` 83 提交 · `qoder-audit-0919` 3 提交），并采纳他一条更正（见 §6）。

---

## 2. 关键实测（本会话产生的，供复核者审）

### 2.1 五次杠杆的总账（同 6 seed · 开 `ALLOW_HEALTH_FAIL=1`）

| 杠杆 | 过门率 | 结论 |
|---|---|---|
| 加压池 40%（门禁对手线加权）| 0/6（但出了**G4/G5 全过**的 `v7press1-82`）| 广度 `G` 9.31、场B 清场 **0.00** |
| 加压池 60% | **1/6** | 五道全过的 `v7press2-93` 反而 **G4 88%/81%** + G5 39% + 自对局 97 回合 ⇒ **加压过头更脆** |
| 特征修正（死特征修好）| 2/6 | `v7fix1-82` **珠经济闭环 ✓**（花珠率 0.914）+ 伤害 0.839/回合，但 **G4 80%** |
| 无教师对照 | **0/6** | 干净单变量 ⇒ **真示范（pickDeepSaver）有用**（"强包出自无教师臂"**未复现**）|
| `CLEAR_W` 0.05 → 0.1 | 0/6 → 1/6 | **场B 清场 ↔ 抗只枪是单调互斥**（0.05：G4 ✓ / 场B 0.00；0.1：场B 1.0 ✓ / G4 76/67% ✗）|

### 2.2 两个否证 + 一个成功（本会话新增）

- ⛔ **E4（奖励挡下）否证**：`BLOCK_W=0.2` ⇒ **G4 90%/70%、G5 77%/36%**（比无 E4 的对照更差），
  `-31` 的 long 格被「只防御」打 **96%** ⇒ 机制：**奖励防御把包推向"龟"，而只枪/只防御正是吃龟的**
  （与复核者 §5/§23 的警告一致）。E4 实现**保留默认关**。
- ⛔ **广度收益项（按绝对种类数）否证**：`WIDTH_W=0.2` ⇒ 种群 `G` 中位 **3.55 → 2.60**（目标 ≥4）、
  座位崩（92/77/80/99pt）、A 卷 −3.6pt；卡面检查显示"最宽"那个 `v7ws2-93` 其实是 **97% ジ、种类 0**
  ⇒ 不是"凑卡面"而是**退化成只打ジ** ⇒ 形状不安全（复核者引过的 Q3："光加熵会推向乱打"）。
- ✅ **热启动换新冠军成功**：`v7ws1-91` 的五道门读数与 `v7press3-91` **逐项相同**（座位 11pt · `G` 8.01 ·
  墙 17.95 · 场A 30% · 场B 1.0）⇒ "热启动真的生效"的硬凭据；且**种群 `G` 中位 3.55 > 旧管线 2.4**，
  `G≥3` 从「12 个 seed 里 2 个」变成「**6 个里 4 个**」⇒ 建议保留为默认。
- ✅ **贵卡出手奖励（`v7big1`）机制成立但稀疏**：`v7big1-92` **从 0.0% 长出了 真正的落雷 3.5% / 地雷 1.5% / 净化 1.6%**
  （同时**珠经济闭环 ✓（花珠率 1.0）**· `G` 7.96 · 伤害 **0.874/回合**，本会话最高）；但**只有 1/6 seed 长出**、
  电磁炮/摄魂/激光眼 仍 0.0%，且代价明确（五道门 0/6 · A 卷 51.2%→43.2% · 2 seed 座位偏 70~73pt）。
- ⚠️ **环消融的"合格实验"仍未满足**：在 `v7big1-92` 上禁环 ⇒ V1 **0.0pt**（p=1.000，ring 只出手 **9 次/120 局**）、
  V2 0.0pt（2 次）、V4 0.0pt（**0 次**，工具自标"空枪：无信息"）。⇒ 这个包**有了花钱的地方，但没有"攒环"**
  （对照线上包 **346 次/30 局**）⇒ 要回答"环值不值钱"，需要一个**既打环、又会花贵卡**的包
  （候选：`BIGCARD_W` + `RING_W` 同开 / 从打环多的包热启动再叠贵卡奖励 / 用 `v7big1-92` 当热启动点只加环奖励）。

### 2.3 用户洞察的量化（复核者 §6 那个环消融的**适用面更正**）

同引擎各 20 局自对局：线上包 **真正的落雷 0.0% · 地雷 0.0% · 净化 0.0%** · 过载炮 0.6%
⇒ 它的**花钱面基本只有狙击/枪** ⇒ 复核者 §6 的消融测的是"**一个只花在狙击上的策略需不需要环**"，
**不是**"环在这个游戏里值不值钱"。（原始更正见 CHANGELOG v1.5.124 §3；数字见 v1.5.125 §2b）

---

## 3. 复现命令（都只读或可控）

```bash
node tools/np-test.mjs            # 149 条门（已知唯一红 = D67）
node tools/spec-run.mjs           # 44 条引擎 spec
node tools/smoke.mjs && node tools/battle-test.mjs   # 页面 + 真浏览器对战
node tools/rules-fingerprint.mjs  # 应打印 61a7711c
node tools/promote-champion.mjs js/bundled-champion-3p.js --dry --exam-first=0   # 现包完整体检（只读）
node tools/parse-battle-log.mjs   # 只读解析 results/（用户的实盘记录）
node tools/probe-v2-anatomy.mjs 80 js/bundled-champion-3p.js <别的包>   # 可玩性三栏（击杀归属/种类/ジ占比）
node tools/probe-ring-ablate.mjs 120   # 环消融（配对 + DP 精确符号翻转）
bash tools/rerun-postmerge.sh     # 复核者那 90 包矩阵重跑（较重）
# 训练一臂（6 seed ≈ 11 分钟）：
RING2_ARM=v7x1 RING2_TAG=x1 RING2_SEEDS=31,81,82,91,92,93 \
EPIRUS_BUNDLE_IN=js/bundled-champion-3p.js EPIRUS_ALLOW_HEALTH_FAIL=1 \
EPIRUS_DIV_W=0.3 EPIRUS_CLEAR_W=0.1 EPIRUS_TRAIN_SEAT_GAMES=24 \
EPIRUS_IMIT_FRAC=0.5 EPIRUS_IMIT_TEACHER=pickDeepSaver EPIRUS_IMIT_OVERRIDE=1 \
RING2_POOL='random,balanced,aggro,defend,wall,antidef,breakdef,mix,farmer,tankline,heavyfire,deepsaver,ringspam,reflectspam,guardspam,targeter,gunspam,snipespam,gunspam,guardspam' \
node tools/ring2-run.mjs
```

---

## 4. 待决定的事（**9 条**，3 条已结案；每条都写明代价）

1. ~~【最高优先】要不要真堵住 2P 的「珠爆发」洞？~~ **✅ 已执行（v1.5.130，2P 侧）**：
   用户指令"把那把 2P 冠军修一下" ⇒ 把 `pickBeadBurst` 加进 `js/train/evo.js` 的 `BOT_PICKS` / `buildOpps`
   并重训（6 候选 × 600 代，**热启动**）：**洞已关闭**（`examGateOk` true、20 基准全 >50%、探针该线 0.0%）。
   细节与代价见 **§0c**。
   **"5 席侧那半条" ✅ 已核实并结案（v1.5.131）—— NP 侧不是洞**：G4 的 `duel()`（`gate-drafts.mjs:256-268`）
   量的是「1 席脚本 vs 4 席冠军」，而**基线格**（冠军对冠军 5 席）实测 **18%(long) / 10%(multi)**，
   「珠爆发」格 **30% / 15%** = 基线的 **1.5~1.7 倍**、**离 60% 阈值（用户裁定 v1.5.104）还差 30~45pt**
   ⇒ 它有牙，但**不是杀手**。同一张表里真红的是「**只枪**」**75% / 62%** —— 那是**已知的 `--force` 例外**（见 §0b-3）。
   ⇒ 因此**不跑 NP 臂、也不动 `server/opp-pool.mjs`**：那是给不存在的问题花 11 分钟，还让此后 NP 臂读数不可比。
   ⚠️ 这段推理其实**早就在** `tools/gate-drafts.mjs:242-248` 的注释里 —— 我上一条评估把"5 席侧仍 30/15%"
   当成"遗留洞"是**一处评估错误**（它是**覆盖度**陈述，不是缺陷陈述）。
   （另：`opp-pool.mjs` 的 `OPP_DEFAULT` 只取 `OPP_SPECS` 的**前 9 个** ⇒ 将来若只为**覆盖度**要加这条线，
   代价也只是显式臂，**默认臂一字不动**。）
   由 §0b 第 3 条 / METHODOLOGY §E.40 可知 2 席与 5 席是**两套经济学** ⇒ 两边**各练各的包**，别指望互相顶替；
   而**能力转移的正解是"对手线进池"**（v1.5.130 已用 2P 侧证明：池子给梯度 + 热启动防遗忘），**不是搬权重**。
2. **环消融要不要重做**（v1.5.127 收盘时的状态，**本轮未动**）：`v7big1-92` 证明"贵卡奖励能长出花钱面"
   （落雷 3.5%/地雷 1.5%/净化 1.6%），但它**不打环**（9 次/120 局）⇒ 还差"**既打环、又会花贵卡**"的包
   （三个候选见 v1.5.127 §2.2 末条）。只有那种包身上的禁环消融，才能回答"环在这个游戏里值不值钱"。
3. **"满桌同包"这一维仍然没有任何门禁量它**（复核者 §7：V4 每席 17.8 → 17.8、拥挤指数 0.49 vs 0.89）。
   若要立门，形状应是**拥挤指数 / V4 每席成绩**（量"后果"），**不是**"数动作投向谁"（用户已否掉后者）。
4. ~~狙击在 V2 净亏 +5.8pt（p=0.016，仅 7 个非零对）⇒ 需大 n 复跑~~ **✅ 已复跑（v1.5.131，n=120 → 600）**：
   同一工具（`tools/probe-ring-ablate.mjs`，`ABLATE_KEY=snipe`）、同 seed、同装配、同包 ⇒
   **V2 +5.8pt → +15.2pt**（非零对 **7/120 → 103/600**，p **0.016 → 3.0e-22**）；V1 **−24.0pt**（p=7.3e-26，量具仍是活的）；
   V4 −0.3pt（p=0.698）。⇒ **不是小 n 噪声，方向确认、量级更大**（**狙击在"打整局"场里净亏**）。
   **因此"要不要针对狙击做一次干预臂"是一个新的、有据可依的候选**（属用户的判据/预算决定）。
   ⚠️ 同时查出**那张审计表已过期**（V2 基线 34.2% 是旧指纹 `61a7711c`；今天同口径读到 **10.0%**）⇒ 见 §4-11。
5. **门禁审计 —— ✅ 已执行（v1.5.128）**：删 **D69**（自指：检查 `np-test.mjs` 自己的行序）·
   **L3** 诚实化（改名 + 删掉 `npt.indexOf('REPRO2')` 这条近乎恒真的自指断言）· **D77 的 1200 字符窗口换成运行时往返**
   （每键喂哨兵值 ⇒ 读回断言"被接受且生效"；配套 `economyReward()` 补 `wallGames` 回读字段）·
   **D80** 的格式钉 → 运行时喂值读回 · **D100** 的窗口子检查删除（同一规则从 3 处降到 1 处）·
   **D101** 的三条文本钉 → 运行时行为验（大雷/地雷/净化/电磁炮记分 · 枪/狙击/ジ不记 · `voided` 不记）。
   门数 **149 → 148**，`np-test 147/148`（唯一红 D67）。
   ⚠️ 两处**没做**（并更正了我自己的审计）：**D90 不删**（逐字读后 8 条断言只有 1 条与 D98 重叠，
   其余含**消息下发** = A/A 家族的真实覆盖）；**跳过** `D13∩D15` 与"对称性家族 6→1"的合并（各自还有独有断言）。
   细节见 `docs/GATE-AUDIT-2026-09-19.md` §4。
6. **D67 的分支②依赖 meta 留痕**：若哪天**忘了 `--force` 就重记**，或手改权重却没重记，D67 会（**正确地**）红
   —— 读判词即可（它会写"口径 id 不一致 / 没有可用的越线留痕"），**别当成新 bug**。
7. **`meta.gateDrafts.g6` 是空的**（既有现象：新旧包都空）：`promote-champion` 的 `G6[...] 靶向率` 正则
   匹配的是**标题行**，而实测值在下一行的 detail 里 ⇒ 想恢复这条记录就得改正则（小活，本轮未做）。
8. ~~D67 怎么处理~~ **✅ 已按用户裁定②执行（v1.5.129）**：三分支 + 口径 id 绑定，见 §0b-3。
9. **【大工程】复核 §4 的"删 `standard`、改成多人里可选 2 人"** —— 提案本身是"**先立后删**"，本轮**未动**。
   本轮给它补了两条**实测依据**（原来只有复核的分析）：
   - **AI 侧**：`multi@N=2` 里 `holo/dualGun/mirror` 这三张多人专用卡**一次也没被冠军选中**
     （A/B 两行逐字节相同的直接原因）；所以"合并会影响 AI 行为"的担心**实测不成立**。
   - **但人类侧仍必须过滤**：`Play.legalActions` 在 N=2 的 multi 里**照样把这三张卡列出来**
     （== 玩家能选中全息屏障 = 给自己唯一对手套盾的陷阱卡 == 正是 v1.5.7 把它踢出 2P 的原案）
     ⇒ 过滤的理由是**UI/规则一致性**，不是 AI 行为。
   - 且由 §0b 第 3 条：**2 席与 5 席是两套经济学** ⇒ 合并后**必须保留 2 席专属包**，
     不能把 3P 包直接顶上去（复核 §4-3 的结论，现已有量化支撑）。
10. ~~【产品 bug·小】"用内置冠军"按钮清不掉 2P 的本机冠军~~ **✅ 已修（v1.5.131）**：
    两个槽位一起清（`js/ui/ui.js:340` 的 `useBuiltinChampion()`），并让 2P 侧**如实标注"我在打哪个包"**
    （`aiInfo()` 与 `renderChampState()` 都改按 `trainer.js` 新回传的 `store.lastSource` 说话）。
    守门 = `tools/battle-test.mjs` 新增段（种两个键 → 点按钮 → **两个都变 `null`**）；
    **反证跑过**：把那行 `removeItem('epirus.champion.v3')` 删掉 ⇒ 3 条断言立刻红（`BATTLE FAILED`）。
    —— 以下是**修前的诊断**（留作记录）：**代码事实**（逐行核对）：
    - `js/train/trainer.js:11-32`：`store.load()` **本地优先** —— `localStorage['epirus.champion.v3']` 里只要有一个
      **合法**包就 `return P.unpack(j)`（`:19`），**内置包永不被查询**；只有"缺失/不合法"才回退（`:29-32`）。
      不合法的包会**自愈删除**，**合法的一直遮蔽内置包**。
    - `js/ui/ui.js:340-352`：`useBuiltinChampion()`（`index.html:49` 的按钮）**只清 `epirus.champion3p`**（3P 键），
      **完全不碰 2P 键**；而那个分隔点正是"2P 老用户永远打自己旧冠军"。
    - `js/ui/ui.js:378`：**"当前用本机还是内置"的指示器只有多人侧**（`multiChampFrom`）⇒ 2P 侧用户**看不出**自己面对的是本机旧冠军。
    - `index.html:49` 的 tooltip 写着"清除本机自训/导入的冠军，改回内置冠军"——**对 2P 槽位而言这是假承诺**。
    ⇒ **代价/风险**：v1.5.130 刚把线上 2P 冠军从"被珠爆发线 100% 打穿"修到 `examGateOk:true`，
    但**这一整类用户拿不到这个包**（他们仍在打/面对旧包）。修法小（清两个键 + 提示文案 + tooltip），
    真正的成本在**守门**：`js/ui/ui.js` 是浏览器侧文件，`np-test` 载不进来 ⇒ 这条只能由
    **`tools/battle-test.mjs` / `smoke.mjs` 的 CDP 用例**守（种两个键 → 点按钮 → 断言两个键都没了）。

11. ~~【v1.5.131 查出】V2 的 24pt 差异~~ **✅ 已查清：就是 R61**（独立 worktree @ `2ccd558` 跑同一命令，
    引擎是唯一变量；不动主工作区、不碰 `results/`）：

    | 装配 | `61a7711c`（R61 之前） | `00e732a7`（今天） | 差 |
    |---|---|---|---|
    | V1（打乱局） | 85.5% | 85.3% | −0.2pt |
    | **V2（打整局）** | **34.7%** | **10.0%** | **−24.7pt** |
    | V4（成群） | 17.2%/席 | 16.1%/席 | −1.1pt |

    ⇒ 审计那张表**在它自己的引擎上是对的**（34.7% ≈ 记录的 34.2%）；差异 **100% 来自 `e7fc9fd`（v1.5.129 的 R61）**。
    且 V2 里的**狙击出手数 1132 → 716 次**（−37%）⇒ 不只是数值漂移，**行为真的变了**
    （与 §4-4"狙击净亏从 +5.8pt 变 +15.2pt"同源）。
    机制**假设**（未证）：R61 同时改了 ① R32 激光眼"连用价"的经济、② **T 块特征**（`policy.js:209-212/259-260` 读 `lastSkill`）
    ⇒ 在"上一手被无效化"的状态里，冠军看到的是**训练时从未见过的输入**（权重是在旧语义下训的）。
    方法论见 `docs/METHODOLOGY.md` **§F.42**。
12. ~~【由 §4-11 推出】要不要在当前引擎下重训线上 3P 包？~~ **✅ 已跑臂并否证"重训能追回"（v1.5.131）**：
    臂 = §3 的标准命令（池子与线上包 meta 的 `opps` **逐字相同**）⇒ 唯一变量是引擎；6 seed 全部"有提升"，
    **线上包未被改动、已还原**（`git status` 无改动 ✓）。**同一次运行内**对照（`probe-ring-ablate.mjs 600`）：

    | 包 | V1（打乱局） | V2（打整局） | V4（满桌同包） |
    |---|---|---|---|
    | **现包** | **85.3%** | **10.0%** | **16.1%/席（七个里最高）** |
    | r61fix-31/81/82/91/92/93 | 49.0 ~ 85.5% | **0.0 ~ 20.0%** | **1.8 ~ 15.0%/席** |

    ⇒ ① **V2 上"重训"不是解**（6 个 seed 0~20%，中位数 ~2% ⇒ 由 **seed/协议方差**主导，不是"权重过时"单一原因）；
    ② **V4 上新包全部 ≤ 现包** ⇒ **无一候选值得 promote**。
    ⇒ **结论：那 24.7pt 追不回来，除非让 V2/V4 成为判据** —— 即**与 §4-3 合并**：
    **先给 V2/V4 立门（含阈值/基线），再谈换包**；在那之前"换包"这个动作无法被任何量具验收。
    ⚠️ 附带：`r61fix-93` 在 V1 里 ring 出手 **10389 次**（现包 386）、**禁环反而 +33.5pt**（p=2.1e-54）
    ⇒ 退化包能通过现有选择标准（又一个"这条轴没有门"的例子；§4-2 的环消融现在多了个**反面**样本）。

---

## 5. 继承给下一个人的坑（**别重踩**）

- ~~**D77 的 1200 字符窗口**~~ **✅ 已于 v1.5.128 改成运行时往返**（每键喂哨兵值 ⇒ 读回断言"被接受且生效"，
  见 §4-5）⇒ 原先那三条限制（新键的 `if` 必须写在函数最前面 / 模式串会在 1200 字符处被截断 / 建议改掉它）
  **全部不再适用**，留作历史。**剩下的通用教训**：任何"切片窗口 + 文本钉"式门都会把**排版**当成契约。
- **env 只能有一个读取点**：`server/econ-env.mjs`（`ECON_ENV_KEYS`/`ECON_REWARD_KEYS`/`readEconEnv`）。
  历史上"env 只落在服务进程、16 个 worker 没拿到"导致**两臂实际是 A/A**（附录 D 臂 K）。
- **跑动中的训练臂会回写 `index.html` 的版本号**（从它起跑时的快照恢复）⇒ 提交前重新对齐 README/index/CHANGELOG 三处（D8 盯这个）。
- **pwsh 里 `Select-Object -First N` 会提前杀掉上游进程** ⇒ `$LASTEXITCODE` 变成假 0（我曾因此误判"体检通过"）；
  要看退出码就用 `-Last N`。
- **本仓是 CRLF**：任何"源码文本钉"式门都可能因换行/排版变红。
- **6 seed 臂 ≈ 11 分钟**（不是 40 分钟）；`.training.lock` 存在时 `promote-champion` **拒跑**（保护，不是判决）。
- **`results/*` 是用户的私人实盘记录 —— 不要提交、不要 add**（用户明确说过）。
- 换包后 **`localStorage` 里已缓存冠军的用户不会自动更新**（REVIEW §11.1）—— **v1.5.131 起两个槽位都能清了**
  （「用内置冠军」同时清 `epirus.champion3p` 与 `epirus.champion.v3`）。但**默认仍然优先用本机那个**，
  所以"这次换的包到底有没有生效"永远要**先点一下那个按钮**再判断（`battle-test` 现在两个槽位都守着）。
- **`results/*` 当前的未提交状态是人家的私人账**：本会话工作区里它们显示为 **`D`（被删）** + 两条新的 `??`
  （`epirus-battle-23回合.txt`、`epirus-battle-34回合 (2).txt`）—— 那是**用户自己**的增删；
  **永远不要 add/commit 它们**（METHODOLOGY #26：`git add -A` 会把这些"工作区里早就没了"的文件提交成删除）。
- **"本仓是 CRLF"这句不准确**（v1.5.129 实测）：实际是**按文件混合**、但**每档内部统一、无混行**。
  LF：`js/core/{rules,state,play}.js`、`js/train/{policy,trainer}.js`、`server/train-server.mjs`、
  `tools/{smoke,audit-lib,np-test}.mjs`、两个 `js/bundled-champion*.js`、本文件；
  CRLF：`js/train/evo.js`、`tools/spec-run.mjs`、`css/style.css`、`CHANGELOG.md`、`docs/RULES-2P.md`、`tests/spec.js`。
  ⇒ 写"源码文本钉"式门时**别按整仓 CRLF（或整仓 LF）假设**，逐档确认。

---

## 6. 本会话的两次**自我更正**（诚实记录，免得下一个人替我圆场）

1. 我曾写"两个座位探针读数不一致（promote 26.9pt vs np-test 43pt）"⇒ 复核者用 **n=400 收敛曲线**
   （240→27.7 · 320→27.0 · **400→26.6pt**）证明那是**抽样噪声**、真值 ≈27pt ⇒ **我的疑虑被关掉**，不是两个量。
2. 我曾在发布说明里用 `G` 8.01 暗指"更宽"⇒ 复核者同引擎并排量 **旧包 8.05（10 种）> 新包 8.01（14 种）**
   ⇒ **`G` 不能当"更宽"的证据，要与"用到的种类数"分开说**（已采纳，CHANGELOG v1.5.123 §1b）。
