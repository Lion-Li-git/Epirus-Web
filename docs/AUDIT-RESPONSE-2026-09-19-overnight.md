# 通宵自主推进（2026-09-18 → 09-19）· 第三方复核者 → 给 ds 的交接

> 我这一夜是在**你不在场**的情况下动手的（用户授权："做一晚上都行，9 点前停工"）。
> 所以本文件优先交代**我改了哪些代码、凭据是什么、哪些结论推翻了昨天我自己写下的话**。
> **未换包**（线上包仍 `268461f7c9f0`）、**未动 `index.html`/`README` 版本号**、**未推 `main`**（全部在 `qoder` 分支）。
> 每步的实测数字都给了复跑命令；凡我没跑的都标「推断」。

## 0. 一句话结论（**02:45 重写；文件开头的这段才是最终版，下面各节是过程**）

1. **"恢复聚能环"这条线正式建议作废** —— 不是没教会，是它在健康包身上**主动亏**（§4：同一动作在退化包上 +43pt、在健康包上 −52.5pt），
   而且**ep 深度与强度在这批数据里解耦**（§10b/§12：七个包峰 ep 全在 1~3、攒钱奖励给 3 倍也跨不过 3；`f-81` 峰 ep=1 反而是对抗场最好的之一）。
2. **真正的结构发现：这个游戏的"谁赢"有两把互相反号的尺子**（§16，17 个包 × 7 把尺子的相关矩阵）。
   `打乱局（惩罚不犯错的对手）` 与 `打整局（对手会配合、会集火）` 呈 **r≈−0.5**；而**现役包在"打整局"这一维只有 0.88 倍公平份额**
   —— 即把它换成一个 random 席位，那个场反而更好打（§18）。25 个被测产物里 15 个在这一维上 ≥1.0×。
   ⚠ 但 §5 给的那个**因果**说法（"因为它只有 12.6% 的出手给防御 ⇒ 所以被反制"）**没有被证明，也不要把两件事混成一件事**：
   `champ-audit` 的 D 列（防御族出手占比 12.6% 对 random 70.8%）是**事实**；
   而 §13a 打掉的是**另一个量**（cost≥2 出手占比，含攻击卡）与成绩的跨包相关（−0.66 → 去杠杆点后 −0.09）。
   目前的状态是：**"提高防御预算 ⇒ 提高 C 轴"这条链条缺证据**，
   而且已有的一个反例很不乐观：`v7l2o` 臂（把兑现比率只认攻击 ep）在 `champ-audit` 的 **F 被集火还手**上从现役包的 14% 提到 **26~36%**，
   但 **C 盾/局 反而从 2.5 掉到 0.0~0.2**（它并没有更"防守"），而 **targeter 场三个 seed 全负**（−23.4/−15.9/−20.0）
   ⇒ "结构指标改善"与"胜率改善"不兑换，这件事今夜已经实测过一次了。
3. **L2′ 三条旋钮的判决**（§10/§11/**§25 是最终版**）：① 余款罚 = 无对象（触发率 0.00%）；③ 2C→4C = 等价于没改；
   **只有 ② `CONV_RATIO` 有真效应** —— 16 对同 seed 配对：**V2（1 席打 4 只 balanced）+7.4pt，符号翻转 p=0.012**、
   V4（成群）+5.4pt p=0.0075，代价 **V1（打乱局）−28.3pt** 与 A 卷 −5.4pt。
   ⇒ 但它是**"轴切换器"不是"增强器"**：换到"1 席打 4 席现役包"这个最接近真实对手的装配，**f 与对照差 −0.4pt（12 对）= 没有区别**；
   而且 `f` 臂产物**全部 G<4**（现役包 8.05）⇒ 按用户"多样性可换一点胜率"的价值观，**② 不该上**。
   （§17 当时基于 5 个包说"值得花一次训练预算"，§25 用 16 对把它收口了。）
3b. **门禁的最终答案（§26）**：`gate-landscape` 五道门 **0/4 全过**，`promote-champion --dry` 把 §20 短名单两只都拦下
   （`c-103` 被"只防御不还手"一行脚本以 68% 克制 + 清场 0.05；`s-91` 偏座 40pt 硬阻断），
   同时门禁自己标出**在位包 FAIL G6 靶向率**（参照行不阻断）⇒ **"该换"但"没有合格候选"，保持现状正确**；
   下一步该做的是 E4（给"挡下伤害"计分）与被门禁点名的清场/反克制能力，而不是再加经济旋钮。
4. **本文件里有 5 处"我推翻我自己"**（§4/§5 旧建议、§10→§11 的未配对均值、§14 的两方向解释、§14b 对 `c-82` 的下调、§18 的 20% 断言）。
   每一处都标了原因和量具 —— **不要按被划掉的版本做决策**，也不要重做那些实验。
5. 红线全部守住：**未换包**（线上包 `268461f7c9f0`，训练前后各比对一次）、未动版本号、未推 `main`、未 `git add results/`。

## 1. 我改了什么代码（全部 env 开关、默认关闭）

`evo.js` + `server/econ-env.mjs` 新增 5 个旋钮（走单一来源 ⇒ 16 个 worker 结构上不可能分叉）：

| env | 干什么 | 出厂值 |
|---|---|---|
| `EPIRUS_HOARD_LEFTOVER=1` | 囤积惩罚的自变量从 `maxEp` 换成**终局余款**（攒钱奖励那一支仍挂 maxEp，不反向激励） | 关 |
| `EPIRUS_CONV_RATIO=1` | `conv` 从"2 次大件封顶"换成比率 **已花/已获得** | 关 |
| `EPIRUS_CONV_OFFENSE=1` | 上面分子**只算花在会造成伤害的卡上的 ep**（治 §5 的龟壳 captured） | 关 |
| `EPIRUS_HOARD_CAP_MULT=n` | 饱和点 `C → C×(n−1)`（默认 2 ⇒ 逐位等于旧式） | 2 |
| `EPIRUS_STOCK_BONUS=b` | 攒钱奖励上限（0.05 → 可抬，检验"奖惩不对称"这句话） | 0.05 |

凭据（都是实跑，不是"应该没问题"）：
```bash
node tools/probe-l2-shaping.mjs      # 12/12 绿。G1：(5 组人数×模式)×(4 组门槛)×(mEp 0~60 步长 0.5)
                                     #     = 2420 个点上证明"默认关闭与旧式逐位相同"（不同 0 个）
node tools/np-test.mjs               # 145/146（唯一红的是 D82 记账门，见 §7）
node tools/spec-run.mjs              # 42/42
node tools/rules-fingerprint.mjs     # 1391c094 未变（没动引擎与特征）
node tools/smoke.mjs && node tools/ui-probe.mjs   # SMOKE OK · UI 10/10 · JS errors (none) ⇒ 你明天还能正常玩
```

## 2. L1（ep 特征冻结）—— 表征层实测，**不需要训练就能定案**

```bash
node tools/probe-ep-encoding.mjs 12
```
- **M1 注入性**：现状三种 ep 编码（`min(ep,12)/12`、`(ep−c)/12` 夹[-1,1] 的 c=1 与 c=4 两种）
  在 **ep≥16 的 6 档（16/20/24/30/40/60）上全部只产出 1 个不同值**；三个提案（log1p / √ / 两段线性+压缩）都是 6/6。
  ⇒ 夹住是**编码本身**的性质，与权重无关。
- **M2**：真实中局面 × 16 档 ep ⇒ arm A 192 输入只产生 146 个不同状态向量（76%）、线上包 109（57%）。
- **M3 精确改点**：状态侧只有下标 **[2, 18, 54]** 随自己席位 ep 变化（动作侧 `(ep−cost)/12` 在 `FEAT_A` 里，另有 `policy.js:379`）。
- ⇒ 结论不变：**抬上限 = 换代**（`FEAT_S` 语义变 ⇒ 旧包不兼容 ⇒ 必须升 `PACK_VERSION` 并全量重训），
  不是改一行。**要不要付这个代价由你/用户定**，我把证据摆齐了。

### ✅ 03:14 更新：这一条的**字面实验已经做完了**，见 **§24**（`tools/probe-ep-unfrozen.mjs`，内存里替换、磁盘零改动）
下面这段当时写的"不能真的改"仍然成立 —— 但当时我把"不能落盘改"错当成"这个实验做不了"，其实可以只改沙盒。
结论：抬上限确实解除逐位冻结（1.75/6 → 5.83~6.00/6），**但网络输出几乎不动**（argmax 94~100% 一致）⇒ 换代只对重训有意义。

### ⚠ 关于「抬 ep 编码上限」这一条：当时以为今夜**不能真的改**，原因不是工作量，是指纹
`tools/rules-fingerprint.mjs` 覆盖的文件里包含 **`js/train/policy.js`**（现值 `1391c094`，与线上包成绩账绑定）。在 `policy.js` 里哪怕加一个**默认关闭**的新编码分支，指纹就会变 ⇒ 门禁 D16 会判「两个线上冠军包的成绩已过期」而全体变红，而这恰恰是我这一夜的红线（不能把在位包的成绩账搞脏）。
⇒ 所以 ③ 的**字面做法**（改了再跑一遍探针）留给你/用户决定要不要付换代代价时再做；我改成做**不需要改码就能定案**的等价测量：
  · M1 在标量层面直接比三种现状编码与三种提案的**注入性**（现状在 ep≥16 的 6 档上只产出 1 个值、提案 6/6）；
  · M2 用真实中局面证明塌缩确实发生（192 输入 → 146/109 个不同向量）；
  · M3 给出**精确改点**（状态侧只有下标 [2,18,54]；动作侧 `(ep−cost)/12` 在 `FEAT_A` 里，见 `policy.js:379`）。
⇒ 这三条合起来回答的就是「抬上限有没有意义」这个问题本身，而**不需要**先付一次换代代价。
⇒ 若你决定做：改动点是 `policy.js:202 / :238 / :258 / :379 / :405`，代价 = `FEAT_S` 语义变 ⇒ 旧包不兼容 ⇒ 升 `PACK_VERSION` + 全量重训 + 修所有钉死特征下标的断言（含 `RING_SELF_IDX` 探针）。

## 3. 教师归因（这条是"顺手捡的"，但可能比 L2′ 本身有用）

`v7ringA1-*`（昨夜 arm A）与今夜对照 `v7l2c-*` 的配置**只差 `EPIRUS_IMIT_*` 一组 env**
（同池 = E+gunspam 17 名 · 同 `CLEAR_W=0.05` · 同 `TRAIN_SEAT_GAMES=24` · 同 250 代 · 同 seed 号），
于是两者之差就是**教师的效应量**：

| | `v7ringA1-82`（deepSaver 真示范 0.5） | `v7l2c-91/93`（无教师） |
|---|---|---|
| 终局兑现率（已花/已获得） | **23%** | **98% / 97%** |
| 人均终局余 ep | **23.2** | 0.2 / 0.5 |
| ep≥4 决策点 · 峰 ep | 68.0% · **43** | 0% · **2** |

⇒ **"ep 溢出但一毛不拔"那个标本是教师造出来的**，不是池子、不是门槛、也不是 shaping 逼出来的。
⇒ 推论（对你有用）：`pickDeepSaver` 当教师时，它自己 86% 出手在攒钱 ⇒ **示范会把"囤积"直接注入种群**，
   而现有的 `economyStock` 拦不住它（罚分挂在 `maxEp` 且在 2C 处夹住）。用攒钱型教师时要配套 `CONV_*` 两键。

## 4. ★ 环的价值是"条件反号"的 —— 并且这解释了过去六次失败

高 ep 安静窗口（`ep≥16 · hp 健康 · 近 2 回合没挨打 · 环可负担`）配对反事实（`tools/probe-ring-highep.mjs`，按局 bootstrap 95%CI）：

| 被测包 | 对照胜率 | 环×4 | 环×8 | 白送+12 | 白送+18 |
|---|---|---|---|---|---|
| `v7ringA1-82`（会连打 40 手 ジ 的）long | 12.0% | **+31.3**[11.5,51.0] | **+43.2**[19.8,64.6] | +34.9[7.3,58.9] | +13.0 跨0 |
| 同上 · multi | 9.4% | +10.4[0.0,21.4] | −3.6 跨0 | **+11.5**[3.1,19.3] | **+24.0**[13.0,34.9] |
| `v7l2c-82`（会花钱的）long | **86.3%** | +2.5 跨0 | **−52.5**[−80.0,−25.0] | **+3.8**[1.3,6.3] | +3.8[1.3,6.3] |

⇒ **同一个动作，在两种包上符号相反，而且只在 5 血长程里相反。**
  对"本来就没别的事可做"的包，按 8 手环是把闲置回合变现（+43pt，量级≈白送 12 点）；
  对"会花钱"的包，交出 8 个回合就是 −52.5pt，而同窗口白送钱仍 +3.8pt ⇒ 它输的是**机会成本**，不是收入。
⇒ **一个只在退化状态下才划算的动作，健康种群永远不会选它。** 这就是"环"上六次训练失败（本轮 2 臂 + 之前 4 次）的统一解释。
⇒ 想让它成立只能改规则（降低"交出几回合"的成本），而用户裁定过规则不改 ⇒ **建议正式作废这个目标**。
⚠ multi 那两行有样本不足的成分（3 血下同时满足安静+高 ep 的快照很少），**不许读成"无效应"**。

## 5. ★★ 真正的弱点轴：在位包在 `targeter` 场里**低于随机**（并推翻我昨天的一条建议）

```bash
node tools/l2-eval.mjs 120        # 三考卷矩阵，每场自带 pickRandom 基线
node tools/probe-targeter-why.mjs 60
```
`--mode=long --field=targeter`（4 席读招反制）实测（120 局 · seed 88100）：**线上包 1st=9.2%，而 pickRandom=24.2%**。
机制追踪（50 局/包）给出的原因，**和我昨天的猜测都不一样**：

| 包 | 防御族出手占比 | 该场胜率 | A 卷 |
|---|---|---|---|
| 线上包 | **12.6%** | 6.7% | 45.1% |
| `v7l2c-91`（对照） | 17.5% | 0.0% | 48.4% |
| `v7l2h-93`（只开余款惩罚） | 27.6% | 36.0% | 48.3% |
| pickRandom | 70.8% | 26.7% | 8.0% |
| `v7l2f-81`（全开） | **82.3%** | **56.0%** | **36.4%** ↓10pt |

- 被否证①："它见人就狙所以被点名" —— **狙击只占它 5.0% 出手**。（`probe-ep-saturation` 里 argmax 恒为狙击，
  是因为 `SNIPE` 费用=**2**（`rules.js`）而它的峰 ep 正好=2 ⇒ 一攒到 2 就立刻被狙击花掉，永远跨不到环要的 3。  ⚠ 我初稿在此写的是"狙击 4 费 ⇒ argmax 长期落在一张买不起的卡上"，**成本看错了一位**，已改正（现版本才与"兑现率 94%、峰 ep=2"自洽）。
- 被否证②："它被 3~4 席集火" —— 挨打回合平均只被 **1.51** 席打（随机 1.74），而它**还手率 26.7% 高于随机的 18.8%**。
- 成立的原因：**防守预算**。随机把 70.8% 的出手给防御族 ⇒ 死亡率 78%；在位包只给 12.6% ⇒ 死亡率 **95%**。
- **方法论警告（我认为这条最值钱）**：**由"不施压的脚本"组成的专用场，量的是"谁最会龟"，不是"谁会打"**。
  `ringwall`（4 席只攒不打）与 `targeter` 都有这个毛病，所以它们的随机基线特别高不是巧合：
  `ringwall` 在 n=200 下**线上包 45.0% 输给随机 51.5%**。
  ⇒ **历史上所有引用 `ringwall` 读数排的产物好坏都要降级**，包括我昨天在
  `docs/OPTIMIZATION-ep-cliff.md` §5-L3 写的"资源挪到反环能力，那条有读数 53.3% vs 42.5%" ——
  那两个数正出自 `ringwall`，**建议的依据已撤销**（原文里已加自我更正块）。正确的靶场是 `targeter`，
  而且**必须同时报随机基线**（`l2-eval.mjs` 已把这条做进工具里）。
- **★ 我上一条对"龟是怎么来的"的解释是错的，已用实测否证**（写在这里免得你按错的机理去改）：
  我原以为 `conv`（花没花）与"余款惩罚"（剩没剩）被"随时买得起的防御卡"captured。
  实测 `v7l2f-81`（82.3% 防御那只）：

  | 读数 | 值 | 说明 |
  |---|---|---|
  | 兑现率（已花/已获得） | **97% / 98%** | 它把钱花得一点不剩 ⇒ **余款罚分 = 0**（余 ep/人 0.2） |
  | cost≥2 出手占比 | **0.0%** | `conv` 的 `heavy` 根本不计它 ⇒ **conv 项 = 0 分** |
  | 峰 ep | **1** | 防御卡成本是 0~1（`GUARD/REFLECT/BAGUA=0`、其余 1）⇒ 它**攒不到能被罚的量** |

  ⇒ 两项新旋钮在这个包上**几乎不出力**（既不给分也不罚分），所以它的龟**不是 shaping 喂出来的，
  是裸胜率项推出来的**：在低压迫训练场里"活得久"就等于赢。
  ⇒ **所以 `EPIRUS_CONV_OFFENSE` 不是这个病的解药**（它仍然值得留着，但它治的是另一个病：
  把"乱花小钱"也算成兑现）。真正的对策是两条：
  ① **判据侧**把"防守族出手占比 ≤30%"做成**门**（不是奖励）——见 §6 第 4 条；
  ② **环境侧**给训练场加压迫（在场里放会持续施压的对手），让"只龟不打"在胜率上就不成立。
  ⚠ 这条更正同样适用于日志里 00:41 那一节。

- **而且"怕反制"不是个别 seed 的运气，是系统性的**（50 局快筛，targeter 场，随机基线 28.0%）：
  11 个产物里 **9 个低于基线** —— 对照臂 `v7l2c` 的 101/102/103/82/91/93 = 20 / 4 / 20 / 2 / **0** / 4%，
  处理臂 `v7l2h` 的 81/91/93 = **0** / **0** / **36**%，`v7l2f` 的 81/82 = **56** / 4%。
  而且这些包的防御族占比几乎都是 **0.0%**（ジ 57~71% + 狙击 13~28%）
  ⇒ **"只会攒和狙、一手指防御都不摆"是整个种群的通病，不是某个 seed 的意外**。
  ⇒ 顺带一句给设计的话：这批包的出手构成里 `聚能环` 出现过 1 次（`v7l2c-102` 2%）——
  仍然是六个产物里唯一一次，与 §4 的结论一致。

## 5b. ★★★ 头对头：那只"龟"**真的比线上包强**，不是专用场骗人（`tools/head2head.mjs`，60 局轮座·同 seed 同盐·正反两向）

| 被测包 | multi：被测 1 席 vs 线上包 4 席 | long：同 | 反向：线上包 1 席 vs 被测 4 席（multi / long） |
|---|---|---|---|
| **`v7l2f-81`（全开 · 82.3% 防御）** | **28.3%**（基线 20%） | **51.7%** | **10.0% / 8.3%**（基线 20%） |
| `v7l2h-93`（只开余款惩罚） | 6.7% | 18.3% | 25.0% / 25.0% |
| `v7l2c-93`（对照） | 16.7% | 25.0% | 25.0% / 23.3% |
| `v7ringA1-82`（arm A） | 8.3% | 13.3% | **70.0% / 68.3%**（线上包吊打它） |

⇒ **两个方向一致**：`v7l2f-81` 一席打四席线上包拿 28%（multi）/52%（long），
  而线上包一席打四席它只剩 **10%/8.3%**（都远低于 20% 基线）⇒ **它不是"在反制场里活得久"，是整体更强**。
⇒ 但**我不换包**（那是 ds 的发布动作，且用户没让我换）。这条是**给用户/ds 的决策材料**，代价也摆在台面上：
  · 好处 = 强度（头对头）与"被反制不崩"（targeter 56% vs 线上包 9.2%）；
  · **代价 = 它 82.3% 的出手在防御族、cost≥2 的卡一张不出（0.0%）、峰 ep 1**
    ⇒ 与用户明确要过的"技能多样性"正面冲突，而且**线下 4~7 人局里没人想跟一个一直在摁盾的对手玩**；
  · A 卷还掉 10pt（36.4% vs 45.1%）。
**★ 但这条最要紧的推论是：A 卷与头对头又打架了** —— `v7l2f-81` 的 A 卷只有 36.4%（比在位包低 10pt），
  头对头却两向一致地吊打在位包。⇒ 这与本项目历史上反复出现的"**A 卷分数不追踪强度**"是同一个形状
  （第五轮、第九轮、`roleC2-31` 都撞过）。⇒ **判据里必须以头对头为主、A 卷为辅**，
  而且**"A 卷没掉"不能再当充分条件**用（我 §6 第 1 条已按此改写）。

⚠ **我那句"龟是被低压迫训练场奖励出来的"，前提只成立一半 —— 已实测并降级**
（`node tools/probe-pool-pressure.mjs 24 E17`；构造 = 0~3 席某脚本 + 4 席 pickRandom，量"它给随机留多少活路"）：
  · 池 E17 里"造成伤害/回合 < 1.0"的只有 **3/17**：`farmer`(0.00，随机席夺冠 **79.2%**)、
    `guardspam`(0.00)、`wall`(0.42) ⇒ **"池子大半不打人"是错的**，我的原话要收回；
  · 但 `ringspam` 是个例外形状：它造成伤害 12.58/回合（很高）**却给随机席留 54.2% 的夺冠率**
    ⇒ "伤害高"与"压迫强"不是一回事（它的伤害大多打在同类身上）；
  · 按 5 席抽样，至少含一个"留活路型"对手（farmer/guardspam/wall/ringspam）的概率约 **73%**（1−(13/17)⁵）
    —— 这是**已测的部分**；而"**因此**种群会学会龟"这半句**没有被证明**，属推断。
⇒ 所以"加压迫"从"结论"降为"**待验的假设**"：批次 4 用高压池（`HIGH` 那一档已写进脚本）跑一臂，
  看它的防御族占比与头对头是否真的按预期变化；不灵就删掉这条建议。

⇒ 我的建议（不是决定）：**先别换包**；把它当"强度轴与多样性轴确实互相拉扯"的**证据**，
  若要往这个方向走，应该由用户在"强度 / 多样性 / 好玩"之间定权重，而不是让 GA 自己撞到一个龟壳上。
**★ 但 `champ-audit` 一摆开，"要不要它"就不再是"强度 vs 多样性"的抽象取舍了**
（`node tools/champ-audit.mjs <文件>`，自对局 20 局 + 考卷 20 局 ⇒ **n 小，方向可信、单格数字别较真**）：

| 列 | 线上包 `bundled-champion-3p.js` | 候选 `v7l2f-81` | 谁好 |
|---|---|---|---|
| A 考卷 1st / 严胜 | **47.7% / 47.7%** | 40.7% / 38.4% | 线上包 |
| B 伤害/局 | 14.6 | 14.1 | ≈ |
| C 盾/局（互套盾风险） | 2.5 | **0.1** | 候选 |
| **D 长程反弹墙** | **80%** | **0%** | **线上包（它完全不会破墙）** |
| E 无威胁摆架势 | 7% | 14% | 线上包 |
| **F 被集火还手** | **14%** | **44%** | **候选**（正是 `v1.5.114` 换包时 `--force` 越过的那条门） |
| F 挨打到反击的回合 | 20.6 | 35.2 | 候选 |
| **G 有效技能数** | **8.05（10 种）** | **3.03（6 种）** | **线上包（多样性差 2.7 倍）** |
| 座位极差 | 27pt | 24pt | ≈ |
| 反弹墙伤害/局 | 0.35 | 0.00 | 线上包 |
| **H 混合场**（4 种风格同场） | **60%** | **0%** | **线上包（差距悬殊）** |
| I 对被动 | 100% | 100% | ≈ |

⇒ 它拿到的是 **F（被集火还手）这一格**，赔进去的是 **D 破墙、H 混合场、G 多样性、A 卷**。
⇒ 所以结论比我上一条更硬：**不是"强度 vs 好玩"的价值观取舍，而是它只修了一格、把另外三格修坏了。**
  **不建议换包**（我也没换）；`F` 那一格值得单独要 —— 见下面"能只要 F 吗"。
⇒ 顺带修正我自己：`targeter` 场是**有用的探针**（它能一眼看出"只会攒和狙"这种偏科），
  但它不能当**总判据** —— 一个 0% 破墙、0% 混合场的包照样能在它上面刷高分。⇒ §5 那句"正确的靶场是 targeter"
  要读成"**它是当前最没被利用的一个探针**"，不是"以后就按它排好坏"。

### 01:22 全产物体检表（`tools/champ-audit.mjs` 逐文件跑，20+20 局 ⇒ n 小，看方向）
完整表在 `docs/artifacts/audit-table.log`。挑出能回答决策的几列（A=A考卷1st · 墙=反弹墙伤害/局 ·
D=长程反弹场 · F=被集火还手 · G=有效技能数(种类)）：

| 包 | A | D | F | G(种) | 墙伤害/局 | 备注 |
|---|---|---|---|---|---|---|
| **线上包 `v7new5_005-31`** | 47.7 | 80% | **14%** | **8.05 (10)** | 0.35 | 多样性最高，F 最弱 |
| `v7seat24-31`（上一版线上包） | 36.7 | 90% | 25% | **8.21 (20)** | 8.00 | 更练但更弱 |
| `v7l2c-103` | 45.9 | **100%** | **27%** | 5.61 (10) | **15.95** | 综合最平衡的一个 |
| `v7l2c-82` | **51.4** | 100% | **34%** | 4.11 (7) | 17.90 | B重击 3.6/局（全场唯一非零） |
| `v7l2c-93` | **53.4** | 0% | 33% | 3.24 (7) | 0.20 | A 最高但**不破墙** |
| `v7l2c-91` = `v7l2h-91` | 49.6 | 95% | 25% | 5.47 (12) | 1.45 | 两行一字不差 ⇒ 又是同一个冠军（开关在此 seed 空转） |
| `v7l2h-93` | 50.7 | 100% | 24% | 5.14 (11) | 11.20 | 但 **E 摆架势 52%**（坏） |
| `v7l2f-102` | **55.7** | 100% | 29% | 3.24 (8) | 18.05 | A 最高 + F/墙 都好，代价是 G 掉一半 |
| `v7l2f-81`（那只龟） | 40.7 | **0%** | **44%** | 3.03 (6) | 0.00 | 只赢 F |
| `v7ringA1-82`（arm A） | 43.4 | 100% | 27% | **1.99 (6)** | 17.60 | G 全场最低 |

⇒ **三件事同时清楚了**：
1. **"能不能只要 F"——能，但代价是多样性**：多个包把 F 从 14% 抬到 24~34%、把破墙伤害从 0.35 抬到 12~18/局，
   而 **G 有效技能数从 8.05 掉到 3.2~5.6**。⇒ 真正的取舍是 **"F+破墙" vs "技能多样性"**，
   不是"强度 vs 好玩"（这正是用户点名要的那一维，所以决定权在用户）。
2. **今夜最值得递上去的候选不是那只龟，而是 `v7l2c-103` 与 `v7l2f-102`**：
   `v7l2c-103` 用 **对照配置（没开任何新开关）**就同时拿到 A 45.9 / D 100% / F 27% / G 5.61(10 种) / 墙伤 15.95；
   `v7l2f-102` 拿到 A 55.7 / D 100% / F 29% / 墙伤 18.05，代价是 G 3.24。
   ⇒ 注意 (1)：**这两个都不是"我改了 shaping"的功劳** —— `v7l2c` 是开关全关的对照臂
   ⇒ 提升主要来自**去掉 imit 教师 + 换 seed**，不是来自 L2′。**我没有据此换包的权限，也不建议按 A 卷换**（见 §5b：A 卷与头对头会打架）。
3. `v7l2c-91` 与 `v7l2h-91` **整行一字不差** ⇒ 第三次独立确认"余款惩罚在这套配置上对部分 seed 完全空转"。

⚠ 一条方法论：`skill-report.mjs` 此刻的"使用="列对**线上包也全是 0.0%**（与其它列一致地退化）
  ⇒ 本轮的出手构成一律以我自己的 `probe-targeter-why` / `probe-leftover` 为准，
  别拿 `skill-report` 的 0% 当"它不用这张卡"的证据（那是量具退化，不是包的行为）。

## 5c. A 卷到底追踪不追踪强度？今夜第一次给出**量化**答案（但样本还不够，见最后的诚实说明）
把 `h2h-sweep.log`（头对头正向，60 局 × 两模式）与 `audit-table.log`（`champ-audit` 的 A 考卷 1st，20 局）
拼起来，对最近 24h 落盘的 **17 个产物**算相关：

```
Pearson r(A卷, 头对头) = −0.03      Spearman（秩相关）= +0.04      n = 17
A≥45% 却头对头 ≈ 基线或以下：v7l2c-101(A45.6/16%) v7l2c-91(A49.6/16%)
                              v7l2h-91(A49.6/16%) v7l2h-93(A50.7/12%)
A<45% 却头对头 ≥2× 基线：      v7l2f-81(A40.7/40%) v7l2f-101(A32.0/38%)
```
⇒ 表面上看：**A 卷与"能不能打赢别的包"毫无关系**（r≈0），而且四个反例横跨 12~16pt 的头对头差。
⚠ **但这个 r 本身不可信，我必须自己先否定它**：`champ-audit` 的 A 只跑 **20 局** ⇒ 单点 SE ≈ 11pt，
   而 17 个产物的 A 值**总散布才 sd≈5pt** ⇒ 噪声大于信号，这种情况下 r 被衰减到 0 是**必然**的，
   不能当成"A 卷无用"的证明，只能当成"A 卷在这个分辨率下没有可测的信息"。
⇒ **正确的做法我已经排上**：`tools/l2-eval.mjs 120` 会给同一批产物跑 **120 局**的 A 卷（还自带随机基线），
   届时重算一次 r 才算数。在此之前，请按"弱结论"读：
   **不要用 A 卷的 ±2pt 差别做任何排序决策**（今夜 4 个反例的差距是 12~16pt，那一档差别是真的）。

## 6. 判据（我建议从现在起固定成四条一起看，缺一不批）

1. **头对头**（`tools/head2head.mjs`，**以正向为准**：被测 1 席 vs 在位包 4 席；公平基线 ≈ **(100−和棋率)/5 ≈ 15%**，不是 20%）——**这一条排第一**：
   A 卷在本项目里已经至少四次证明它不追踪强度（§5b 又是一例），所以"掉 A 卷"不能单独否掉一个包，
   "不掉 A 卷"也不能单独支持一个包；
2. **A 卷**（`eval-5p` 默认场）只当辅助读数；
3. **`targeter` 场必须显著高于该场自己的随机基线**（低于基线的产物，"胜率再高"也要写清它赢在哪）；
3b. 反过来也成立：**专用场的随机基线本身要定期重测**（`ringwall` 那条就是这么废掉的）；
4. **资源线**：`node tools/probe-leftover.mjs` 的 兑现率 / 人均余 ep / 出手每回合；
5. **防守预算**：防御族出手占比要落在 **12%~30%** 带内，**≥50% 一律不批**（否则 1~3 条能被龟壳同时满足，且没人想跟一个 82% 时间在摁盾的对手玩）。

## 6b. ⚠ 选择偏差（我自己给判据时也得受这条约束）：这套无教师配置的**健康门槛拦下率约六到七成**
`ring2-run.mjs` 的落盘前体检在 `v7l2c` 拦下 12 次尝试里的 7 次、`v7l2h` 9 次、`v7l2f` 8 次
（拦下理由两类：`G 有效技能数 < 3` 与 `自对局回合 > 40`）。⇒ 我上面所有"处理 vs 对照"的比较
**都是在"活下来的产物"上做的**，不是在全分布上做的：
· 如果某个开关主要改变的是"过门率"（而不是过门产物的性质），我的矩阵**看不见它**；
· 反过来，`v7l2h-81` 这类"对照被拦、处理落盘"的例子**不能**读成"处理更好"——
  它只说明处理臂那次跑出来的包**碰巧**过了体检（也可能是 seed 运气）。
⇒ 建议：真要评 L2′，把**过门率本身当一等指标**（每臂跑 12 个 seed，报"落盘几/12"），
   我现在报的读数都只是"幸存者内部"的比较。这条我写进了 §8 的待办（批次 3 起就按这个记）。
⚠ 另外我为了看数字，重复用同一份 `ring2-status-<tag>.log`（append），上面那些"被拦次数"是
   批次 1 + 批次 2 合计（每批 6 seed，含 runner 固定前置的 seed 31）⇒ 分母不是 6。

## 7. 记账与遗留（给你，不用重做我的实验）

- **D82 现在是红的**：这一夜新落的 `v7l2c/h/f/o/a-<seed>.bak` 必须在 `CHANGELOG.md` 里**逐字点名**（缩写不算）。
  我会在停工前补一条本分支的记账段；你要是换包，请照 `v1.5.115` 那条的格式把字面名列全。
- 训练锁存在时 `gate-landscape` 会**整行输出 `—` 并判 `✗ (其它)`（连线上包那行也是）** ⇒ 那是保护行为不是判决，
  等臂跑完再看门地形。
- 我没动但值得你看的一处：`probe-convert` 的【收入饥饿】判据已改成读 `computeCost` 的实际 ep
  （环声明的 `cost` 是 `null` ⇒ 之前恒漏报）。现在线上包会正确报 `聚能环 0.0%`。
- `autoGameN` **不能从中间状态续跑**（会重复一次 `startTurn` ⇒ 回合 +1、梦魇扣血、行动槽清空都重复；
  我的忠实性自检 0/6 → 用 `tools/probe-ring-value.mjs` 里的 `resumeN(state, chs, skipStart)` 后 8/8）。
  凡想做分支/反事实/搜索类评测或训练改进，都得走这个驱动。
- 我今夜**新增的量具**（都在 `tools/`，不带守门，可直接搬）：
  `probe-l2-shaping`（开关接线自检）· `probe-leftover`（资源线判据，**02:56 起新增"终局余款 >C 触发率"栏**）
  · `probe-ep-encoding`（表征注入性）· `probe-ep-saturation`（整条 ep 阶梯上的 argmax 稳定性）
  · `probe-ring-highep`（高 ep 窗口配对反事实）· `probe-targeter-why`（弱点轴机制追踪）
  · `probe-ring-afford` / `probe-cashout` / `probe-wealth-placebo` / `probe-pool-pressure`（可负担性 / 兑现 / 白送钱上界 / 池压力）
  · **`crowding.mjs`（新轴 V1/V2/V4 + `CROWD_SELFCHECK=1` 对称性自检）** ⇒ §16/§17/§21 三条结论全出自它
  · `l2-eval`（三考卷矩阵 + 强制随机基线）· `head2head`（正反两向）· **`l2-h2h-join` / `l2-axis-join`（h2h × 考卷 × 新轴联表算相关）**
  · `l2-arms` / `l2-schedule` / `l2-batch5-wait.sh` / `l2-batch6-wait.sh`（臂批跑与**可追踪**排程）· `l2-bookkeep`（D82 名单生成）。
- ⚠ 两处修的是**会毁数据**的行为，请注意别改回去：
  1. `l2-eval.mjs` 以前每次运行**截断重写** `l2-eval.log` —— 那张 60+ 行矩阵是 §11/§13/§16 的唯一数据源，而 `.log` 在 gitignore 里。
     现在默认**追加**（要重建传 `L2_EVAL_FRESH=1`），并且矩阵已固化成 tracked 文件 **`docs/l2-eval-matrix-120.tsv`**（每次补跑后重新 `cp` 一次再提交）。
  2. `probe-convert.mjs` 的环成本以前读 `def.cost`（环声明的是 `null` ⇒ 恒漏报"聚能环 0.0%"），改成走 `computeCost` 的实际 ep。

## 9. 给早上的你：一张决策表（**不换包**，这只是材料）

头对口正向（被测 1 席 vs 线上包 4 席 · 公平份额 20% · 60 局轮座 · `h2h-sweep.log` 仍在补全）：

（公平基线 ≈ **15%**，不是在位包自己那行以外的 20%；见下面的校准框）

| 包 | 正向 multi | 正向 long | A 卷 | F 被集火还手 | D 破墙 | 墙伤/局 | G 有效技能数 |
|---|---|---|---|---|---|---|---|
| **线上包自镜像（=基线）** | **15.0%** | **13.3%** | 47.7 | 14% | 80% | 0.35 | 8.05 (10) |
| `v7l2c-82`（开关全关） | **45.0%** | **46.7%** | 51.4 | 34% | 100% | 17.90 | 4.11 (7) |
| `v7l2c-103`（开关全关） | **43.3%** | **41.7%** | 45.9 | 27% | 100% | 15.95 | 5.61 (10) |
| `v7l2f-106`（全开） | 40.0% | **50.0%** | 待补 | 待补 | 待补 | 待补 | 待补 |
| `v7l2f-81`（全开 · 龟） | 28.3% | 51.7% | 40.7 | **44%** | **0%** | 0.00 | 3.03 (6) |

**★ 顺带校准出一个我原本用错的基线（这一条会让上面所有读数更好读）**
`v7new5_005-31` 就是**在位包自己**，它打 4 席在位包（镜像）拿 **multi 15.0%（和 23%）/ long 13.3%（和 12%）**，
而它的**反向一行与正向一字不差**（15.0/13.3）⇒ 这既是我这套 `head2head` 的**内部一致性检查**（镜像两向必须相等 ✓），
也说明**公平基线不是 20%，而是 (100 − 和棋率)/5 ≈ 15%**（多人局和棋率 12~23%，被拿掉的那份不会变成某人的胜）。
⇒ 按 15% 重读：`v7l2c-82` 45.0/46.7 = **约 3 倍公平份额**；`v7l2c-103` 43.3/41.7 同量级；
   而 `v7l2c-101/91`、`v7seat24-31`(上一版线上包) 这些 15~17% 的都是**与在位包打平**，不是"更弱"。
⇒ 另有一处旧数可以顺手纠正：`v7seat24-31` 反向 26.7%（= 在位包 1 席打 4 席旧包拿 26.7% > 它的自镜像 15%）
   ⇒ 与 `v1.5.104` 那次"新包优于旧包"的判断方向一致 ✓。

⇒ 材料想说的三句话：
1. **确有几个产物在"打 4 席在位包"时拿到 2 倍公平份额**（43~47%），而 A 卷也不差（45.9~51.4%）
   ⇒ 这不是指标骗人，是**这一夜真的产出了比在位包强的包**。
2. **它们几乎全部出自开关全关的对照臂** ⇒ 变化来自"去掉 deepSaver 教师 + 换 seed"，
   **不是 L2′ 的功劳**；L2′ 目前唯一被证实的作用是在个别 seed 上把包推向龟（且 A 卷付 10pt）。
3. **在位包唯一无法被替代的是多样性**（G 8.05/10 种）——所有挑战者都在 3.0~5.6 之间。
   ⇒ 用户点名要的正是这一维。所以**换包与否的实质问题是：愿不愿意用一半的多样性去换强度+F 那一格**。

⚠ 三条限制，别把这些数读过头：
· 头对口的**反向**（在位包 1 席 vs 4 席候选）我**不当判据**——4 份同策略会互相挤压（镜像拥挤），
  但它是个好**诊断**：`f-102` 正向 33.3/36.7 而反向在位包拿 43.3/63.3 ⇒ 那四份同族在互杀，别读成"它弱"；
  那一列只用来诊断"是不是同族互杀"（例：`f-102` 正向 36.7% 但反向在位包拿 63.3% ⇒ 典型互杀信号）；
· 健康门槛拦下约六到七成 seed ⇒ 以上都是**幸存者**（见 §6b）；
· A 卷 / F / D / G 取自 `champ-audit` 的 20+20 局，**n 小**；头对头是 60 局。

## 8. 批次逐项判决（**已全部填完**；每条都标了当时的 n 与量具，被后文更正处就地标注，不要按第一版读）

- [x] `v7l2h`/`v7l2f` 与对照的 targeter 分布比较 ⇒ **做到 20 个包 × 3 考卷 @120，结论是"读不出臂间差异"**（§11a 同 seed 配对表）：
      `h−c` 配对均值 +12.9pt 但 **SD 18.2（n=2）**；`f−c` +11.7pt 但 **SD 14.6（n=3）**；未配对时 `f` 臂 A 卷还"掉 6.5pt"（§11c 已撤回）。
      ⇒ 三条 L2′ 臂在**考卷口径**上全部够不到判定；只有换到 **V2（打整局）口径 + 按 G 分层**才看得见效应（§17/§21）。
- [x] `v7l2o`（兑现只认进攻卡）⇒ **按设计判负**（批次 3，seed 91/92/93）：
      ① targeter 高出随机 **−23.4 / −15.9 / −20.0pt（三 seed 全负，均值 −19.8；对照 −11.7）** ⇒ 它存在的唯一理由没成立；
      ② A 卷 46.6/46.0/41.8（高出随机 +38.4/+37.8/+33.6）⇒ 与对照持平；
      ③ "防守占比压回带内" **当时是用 cost≥2 占比当量具测的，那是替代量具** ⇒ `champ-audit` 直接读数：`C 盾/局 0.0~0.2`（现役包 2.5）
        ⇒ **它根本没有更防守**，只是"被集火时还手率"从 14% 升到 26~36%。⇒ 正确状态是 **③ 未达**（见 §19 末的更正）。
- [x] `v7l2a`（**arm A 原配置 + 三个开关**）首产物 `v7l2a-81` ⇒ **L2′ 第一次交出对靶的正结果，但它治过头了**：

      | 读数 | `v7ringA1-82`（同教师、开关全关） | `v7l2a-81`（同教师 + 三开关） |
      |---|---|---|
      | 兑现率 multi / long | **23% / 42%** | **91% / 96%** |
      | 人均终局余 ep | 23.2 | **0.9** |
      | 峰 ep | 43 | **4** |
      | 环可负担决策点 | 70.9% | **2.6%** |
      | 打环 | 0 | 0 |

      ⇒ 教师注入的囤积**被治好了**（兑现率 23%→91%、余 ep 23.2→0.9）⇒ `HOARD_LEFTOVER` + `CONV_RATIO` 确实有把手，方向如设计 —— 这是今夜 L2′ 唯一一次**按设计意图**起效的证据。
      ⇒ **但 overshoot**：峰 ep 从 43 掉到 **4** ⇒ 环反而更够不着（可负担点 70.9% → **2.6%**）。也就是说这套塑形能把「不花钱」治好，治到的终点仍然是「**在 ep≤2 就花光**」（＝在位包的画像），不是「攒到 3 再花」。
      ⇒ 与 §5/§5c 的总线一致：**这套规则下的最优解就不是攒钱** ⇒ 任何方向的塑形都会把种群推回「即时小额消费」，推不出多回合资源线。
      ⚠ n=1 产物（该臂其余 seed 还在跑）：效应量 4~40 倍 ⇒ 方向可信；但「**峰 ep 掉到 4**」这条要等其余 seed 确认，别现在当定论。
- [x] `v7l2s`（只抬 `STOCK_BONUS` 0.05 → **0.15**）⇒ **否证，这条假设死了**（seed 82/91，各 40 局自对局）：
      两个 seed 的 **峰 ep 仍然 = 2**、`环可负担 = 0.00%`、`打环 = 0`、兑现率 **96~98%**、人均余 ep 0.4。
      ⇒ "**攒钱奖励给得太小**"不是原因 —— 把它翻三倍连门槛都碰不到。
      ⇒ 换个说法讲清楚：这不是"权重不够大"的**量**的问题，是**结构**问题 ——
        攒钱要付的是"少打一回合"的**胜率代价**（尺度 ~1.0），而 shaping 项都在 0.0X 尺度上，
        **任何 0.0X 的常数都买不到一个多回合计划**；而真正能看到多回合后果的只有胜率项本身，
        它对环的梯度又恰好是零（§4/§5b：健康包交出 8 个回合 = −52.5pt）。
      ⇒ 这条否证同时**加强** §5 的结论：想动资源线，得动"场"或动"规则"，动 shaping 权重是徒劳的。
      ⚠ 负结果的样本说明：峰 ep=2 是**上界读数**（不是均值），两个 seed 都顶在同一个天花板 ⇒ 对"能否跨过 3"
        这个二值问题，n=2 已足够否证；但它**不能**用来证明"任何更大的权重都不行"（那已由尺度论证覆盖）。

## 10. ★★★ 02:02 增补：L2′ 三条各自的裁定 + **"ep 深度是强度前提"这个假设被数据否证**

### 10a. 三条旋钮的判决（每条都有"接线凭据 + 效应凭据"两栏，缺一不算生效）
| 旋钮 | 接线凭据 | 效应凭据 | 裁定 |
|---|---|---|---|
| ① `HOARD_LEFTOVER`（罚分自变量 → 终局余款） | ✅ 产物 meta `ecoOverride.hoardOnLeftover=true`、`ecoEffective` 同步为真、worker 日志 16 行生效值 | ⚠ **只有分数被扰动、决策没被扰动**：`v7l2c-91` 与 `v7l2h-91` 的 **champion payload 逐位相同**（`9bfc20200925`），而代际 `best` 从 gen 0 就不同 ⇒ 250 代里**一次选择都没变** | **保留代码、默认关闭、不要再为它训臂**。病灶不存在：7 个包 × 2 模式的**终局余款 >C 触发率一律 0.00%**（人均 0.2~0.9，最大 1~3，门槛 10/20）|
| ② `CONV_RATIO`（兑现从"次数封顶"→"已花/已获得"比率） | ✅ 同上 | ✅ **12/12 个 seed 的代际曲线全部偏离对照**，产物结构随之改变（`v7l2f-81`：兑现率 97%、cost≥2 出手 **0%**、峰 ep **1**） | **有效，但方向是"更彻底地花光"**：f 臂两个特殊场最好（targeter +4.0 / ringwall +2.4 均值高于随机），~~A 卷均值 33.2 对 对照 39.7（−6.5pt）~~ **这句已在 §11c 撤回**（未配对均值；配对后是 −2.0±11.8pt，与 0 不可分）。要不要它 = 要不要拿标准卷换对抗卷 ⇒ **产品决策，交用户**，我不替你定 |
| ③ `HOARD_CAP_MULT` 2C→4C | ✅ | ✗ 只改**饱和点**、门槛没动 ⇒ 在触发率 0% 的前提下**等价于没改** | **不要把 ② 的成绩记到 ③ 头上。** 若将来要它有意义，必须先把门槛降到观测右尾（个位数）|

⚠ 我 00:17 在研究日志里写的"L2′-① 是空操作"**只对一半**：它对**决策**是空操作，对**分数**不是。
区分这两件事的量具是"同 seed 跨臂比 champion payload"（不是文件 sha1 —— meta 带时间戳；也不是比分数字 —— 分数字段必然不同）。
⇒ **给 ds 的可复用判据：任何新 shaping 上线前，先跑"同 seed 开/关两臂 ⇒ payload 是否改变"**。
改不了 payload 的 shaping，训多少个 seed 都是白训（我这轮 6 个 seed × 1 臂 = 就是这么抓到的）。

### 10b. "要变强先要有 ep 深度 / 会打环" —— 这个前提贯穿了前六轮，今夜**没有支持证据**
- 全部 7 个被测包（含线上包、含把攒钱奖励抬到 3× 的 `s` 臂）在 5 席自对局里 **峰 ep 恒 2~3、ep≥3 占比 ≤0.1%、环可负担点 ≤0.07%**
  ⇒ 攒钱奖励给到 3 倍也**跨不过 3 ep**（与 `probe-l2-shaping` 沙盒一致）。
  这不是"不愿攒"，是**跨期**问题：按实测收入 ≈0.6 ep/次决策，首次 tap 环要连 5 次决策不花钱，
  而每一次不花钱都在胜率项上直接掉分 —— 要把这个买回来就得把 shaping 给到胜率量级 ⇒ **与"奖惩不用给太多"的裁定正面冲突 ⇒ 应判死，不是再试一档。**
- 反例最干净的是 `v7l2f-81`：**峰 ep=1、cost≥2 出手 0%**（这批里最"浅"的包），
  却在 targeter 场 +27.5pt、ringwall 场 +40.0pt（同批最好的两栏之一）。
  ⚠ 别读反：它 A 卷 −6.5pt，且 `ringwall` 场判别力差（随机 47.5% > 线上包 36.7%，见 §5）⇒ 只能说"浅包也能在对抗场活"，不能说"浅就是强"。
- 合起来：**"资源线（环/ep 深度）"与"强度"这两条轴在这批读数里是解耦的，甚至略反相关。**
  §4 讲的"环只在退化状态划算"与此一致：会花钱的包按环 ×8 是 −52.5pt。

## 11. ★★★★ 02:05 今夜最重要的方法论结论：**这套评测读不出 L2′ 这个量级的效应** —— 并据此**撤回我在 §10 写的一句**

### 11a. 60 行三考卷矩阵跑完后，改成**同 seed 配对**再看（挑战臂 − 对照 `c`，单位 pt，"高出随机"之差）
| 对比 | n(配对 seed) | A卷 | TN(targeter 场) | RW(ringwall 场) |
|---|---|---|---|---|
| `h − c`（只开余款罚） | 2 | −3.0 ± 4.2 | +12.9 ± 18.2 | +0.5 ± 0.6 |
| `f − c`（比率化兑现） | 3 | **−2.0 ± 11.8** | +11.7 ± 14.6 | +4.2 ± 14.4 |
| `s − c`（攒钱奖励 3×） | 3 | −0.7 ± 5.0 | +16.7 ± 17.4 | **+28.6 ± 53.2** |

⇒ 三行的均值全部**小于其标准差**，最小的也要 2~10 倍才够 2SE。**没有任何一臂任何一个考卷达到可判水平。**
（顺带一个漂亮的自洽检查：`h − c` 在 seed 91 那一对是 **0.0 / 0.0 / 0.0** —— 与"两个包 payload 逐位相同"完全吻合，
说明配对差这套量具没有接线错误。）

### 11b. 方差被拆成两笔，只有其中一笔能靠"多打局"消掉
- **对局抽样噪声**：120 局下 1st 率的二项 SE ≈ `sqrt(0.25/120)` ≈ **4.1pt** ⇒ 抬到 480 局才降到 2pt。
- **训练结局异质**：同臂不同 seed 的 SD 实测 **4~53pt**（上表），是前者的 1~13 倍 ⇒ **只能靠加 seed 消**。
  配对设计（同 seed 两臂）已经把它砍掉一大半（未配对时 `f` 臂 A 卷 SD=10.0、配对后 −2.0±11.8 的 SD 来自**差**本身），
  但剩下的仍然主导。
- ⇒ **分辨率账**：要判出一个 10pt 的臂效应（2SE 门槛），
  `TN` 那类（SD≈15）要 **~9 个配对 seed**；`RW` 那类（SD≈53）要 **~110 个配对 seed** ⇒ 后者实际不可行，
  只能改判据（换成方差更小的读数，例如"防御预算占比""出手/回合"这类**结构量**而不是胜率）。
  这正是我今夜把 `champ-audit` 的结构列和 `probe-leftover` 当主证据、把考卷胜率当旁证的原因。

### 11c. ⚠ 撤回 §10 里的一句（我自己五分钟前写的）
§10 写了"**f 臂 A 卷均值 33.2 对 39.7（−6.5pt）** ⇒ 用标准卷换对抗卷"。**这句是拿未配对均值做的比较，作废**：
配对后是 **−2.0 ± 11.8pt**，与 0 无法区分。f 臂真正的、可复现的效应是**结构性的**（峰 ep 1、cost≥2 出手 0%、兑现 97%），
不是"换卷"。⇒ **不要按 §10 那句话去做取舍决策。**
（同样的道理：`s` 臂在未配对表里"三栏全绿"看起来最像赢家，配对后是 A −0.7 / TN +16.7 / RW +28.6，**都够不上判定**。
   我今夜两次被"未配对均值"骗（00:41 的龟、02:00 的 f），所以把这条写死在这里。）

### 11d. 给 ds 的固定口径建议（不新增规则、不改代码，只是别再自欺）
1. **任何"臂 X 比臂 Y 强"的结论，必须给同 seed 配对差的均值 ± SD 与 n**；只给每臂均值表的，按未证处理。
2. 考卷胜率类读数**至少 8 个配对 seed** 才有判定力；不够 8 个就明说是"筛选"不是"判定"。
3. 结构量（`probe-leftover` 的 出手/回合 · cost≥2 占比 · ep 直方 · 终局余款触发率；`champ-audit` 的 D/F/G/H 列）
   **可以 n=1~3 报**，因为它们方差小一个量级 —— 但要说清它们不回答"谁赢"。
4. `champ-audit` 是**确定性**的（固定 seed ⇒ 同一包连跑三次逐字节相同）⇒ 拿它"复跑取平均"是无效操作，
   要置信度必须**换种子重跑**，不是重复跑。

## 12. ★★ 02:10 增补：ep 条件性在**整条 2→40 阶梯**上都是平的（比 §2 的"16 以上冻结"更强，也更不乐观）

`node tools/probe-ep-saturation.mjs`（真局快照 × 只改 ep × 其余不动；12 快照 / 11 档）复跑一遍，把两张表读全：

| 被测包 | p(ジ) 随 ep | p(最好的攻击) 随 ep | argmax 在 ep=2 与 ep=40 |
|---|---|---|---|
| `v7ringA1-82`（arm A） | 0.663→0.833→0.582（8~12 平）→0.555（13）→**0.453（16/20/30/40 全平）** | 0.165→…→**0.378（16~40 全平）** | `{ji:7,snipe:5}` → `{ji:6,snipe:5,tank:1}`，16 之后**四档一字不变** |
| **线上包** `v7new5_005-31` | **0.000（全档）** | 0.519→0.521→0.522（**2 到 40 只动 0.003**） | **`{snipe:12}` —— ep=2 与 ep=40 完全同一分布，12/12 快照都是狙击** |

⇒ 三条给 ds 的硬结论：
1. §2 说的"ep≥16 冻结"**低估了**。现役包的问题不是"高 ep 看不见"，是**从头到尾几乎不条件于 ep**：
   把 ep 从 2 改成 40，**可负担候选集扩到包含 cost 4~6 的重卡**，argmax 仍然 12/12 是狙击。
   ⇒ "它不是'攒一波再爆发'的智能体"这件事，是**权重层面的事实**，不是特征夹住造成的假象。
2. 因此 **L1（抬编码上限）的收益只能来自"让下一代训练看得见"，不可能来自"让现役包看得见"** ——
   现役包换上新编码也没有东西可解锁（上面那 0.003 就是全部）。
   ⇒ 如果只为救现役包，**这次换代不值得付**；如果是为了让重训有希望，那要连带重训预算一起评估（`FEAT_S` 语义变 ⇒ `PACK_VERSION` ⇒ 全量重训 + 修所有钉死特征下标的断言）。
3. 与 §10b 的"峰 ep 恒 2~3、`f` 臂峰 ep=1 仍在对抗场最好"完全一致：**ep 深度这条轴对现役种群的强度基本不载重。**

⚠ 诚实说明（③ 的字面做法没做完，原因不是工作量）：
我本想按 objective ③ 的原话"临时抬 `policy.js` 的 clamp 再跑一次探针"，做法是把 `js/` 拷到 `/tmp` 沙盒里改（**不动仓库文件**）。
该动作被会话的权限层拦下（判定为"改模型特征工程/结构性改动"）。**我没有绕道**，因为拦下的东西与我自己的红线同源
（`policy.js` 在规则指纹覆盖范围内，指纹一变 D16 就把在位包的成绩账判过期）。
⇒ 于是改成跑**不需要改任何代码**的上面这条更强读数。若明早你/用户愿意付换代代价，字面实验仍可做，
   精确改点与代价清单在 §2 末尾（`policy.js:202/238/258/379/405`）。

## 13. ★★★★★ 02:17 今夜最可用于决策的一条：**A 卷与"对抗场存活"是反相关的**（并且这次做对了去混淆）
数据 = 23 个包 × 三考卷 @120 局（`docs/l2-eval-matrix-120.tsv`，含批次 3 的 `o` 臂）联 19~20 个包的结构量表。

### 13a. 先给一个**反面教材**（我差点把它当结论报出去）
把"cost≥2 出手占比"与 targeter 场成绩做全体的相关：`r(heavy, TN) = −0.66`、`r(heavy, A卷) = +0.72`（n=19）——
看起来像"找到了那个结构性权衡的定律"。**去掉 `heavy=0%` 的两个 `f` 臂包之后，r(TN) 掉到 −0.09。**
⇒ 那条"强相关"完全是 **臂级效应**（`f` 臂既被代码设定成几乎不出重卡、又恰好是 TN 最好的两个包），
   不是包级规律。臂内分层看还互相打架：`c` 臂 r(TN)=**+0.60**（反号）、`h` −0.48、`s` −0.58、`f` −0.96（n=4~6，符号都不稳）。
⇒ **写给 ds 的判据：跨包相关必须做"臂内分层 + 去掉极端杠杆点"两步，任一步让它翻号/塌到 0 就当没这回事。**
   我这夜已经栽在未配对均值两次（00:41、02:00）、这次是第三次（跨包相关）—— 三个坑同一个根：**把"臂/包"这一层的不独立当成独立样本。**

### 13b. 而这一条**经受住了**同样的两道检查 —— 所以它值得进决策
**r(A卷高出随机, targeter场高出随机)：**
- 全体 **−0.57（n=23）**；
- **臂内分层：5 个可比臂的 r 全为负 —— c −0.45 / f −0.87 / h −0.10 / o −0.06 / s −0.33，均值 −0.36，符号一致 5/5**
  （若真无关联，5 个臂全同号的单侧概率 ≈ 3%）；
- 它不是靠某个杠杆点撑起来的：去掉最好的两包（`f-101`、`f-81`）后全体仍 −0.42。
⇒ **在标准 35 组合考卷上分数越高的包，在"会瞄人"的对抗场里越差。** 两张卷子不是"同一件事的两种测法"，
   而是**在往相反方向挑包**。
⇒ 直接后果（这是我给判据排序的唯一依据）：**只用 A 卷当晋升门槛，会系统性地选出"针对不了人但打得过bot场"的包。**
   现役包 A 卷 +37.6（这批里偏上）而 TN −15.0（低于随机），正好落在这条负相关的线上 ——
   **它不是"练得不够"的意外，是 A 卷这个目标函数的必然产物。**

### 13c. 与之配套的两条弱关系（供参考，别过度读）
- `r(TN, RW) = +0.21`、`r(A卷, RW) = −0.19` ⇒ `ringwall` 场与另外两张卷子都近乎无关（再证 §5：它判别力差，别拿它当判据）。
- `casts`（出手/回合）与任何考卷的相关都 ≤ |0.3| ⇒ "手速"不是这批包成绩差异的来源。

### 13d. 落地建议（不改规则、不改代码，只改验收）
晋升判据从"A 卷不掉 + 专项好"改成：
1. **头对头**（`tools/head2head.mjs`，同 seed 同盐正反两向）为第一判据；
2. **对抗场（targeter）高出随机**为第二判据，且**必须与 A 卷同时报**（因为二者反相关，只报一个就是在自选方向）；
3. A 卷只当"**别退步太大**"的下限（比如 −5pt 内），不当收益上限的理由；
4. 结构量（`probe-leftover` 的 cost≥2 占比、ep 直方、终局余款触发率）只用于**解释**，不用于**排名**
   （§11b + 本节的教训：结构量的改善不兑换成胜率，见 `v7l2o` 三 seed 结构达标而 TN 全负）。

## 14. ★★★★ 02:19 **"头对头"不是一个量，是两个**（r(正向,反向) 只有 −0.25）⇒ 据此**下调 §9 的"最强挑战者"**
新工具 `tools/l2-h2h-join.mjs` 把三份已有读数联表（17 个包：`h2h-sweep.log` × `l2-eval-matrix-120.tsv`），没重跑任何实验。

### 14a. 同一个头对头测试的两个方向，几乎各说各话
| | 含义 | 与正向的相关 |
|---|---|---|
| **正向** | 被测 **1 席** 打 线上包 **4 席**（以少打多，基线 20%） | —— |
| **反向** | 线上包 **1 席** 打 被测 **4 席**（被测有"人数"就赢得多 ⇒ 被测弱） | **r = −0.25**；臂内分层 **c −0.25 / f −0.03 / h −0.47** |

⇒ 两个方向本应是同一个"强度"的镜像，实测**只共享约 6% 的方差**（17/19 的包同号判定，**6 个包直接互相打脸**）。
   机制也讲得通：5 人混战里"4 个自己"是**另一种游戏**（自己人互相挡、互相抢输出目标），不是"1 个自己"的对称放大。
⇒ **建议**：从此**分开设名**（`正向=以少打多能力` / `反向=群聚压制能力`），不要再用"头对头"这个单一名词当判据。

### 14b. ⚠ 自我修正：§9 那句"最强挑战者 = `v7l2c-82`"**读过头了**
`v7l2c-82` 实测：正向 **45.0%**（这批最高）· 反向 **35.0%**（线上包 1 席打 4 席它还能赢 35% ⇒ **高于 20% 基线 = 判定它弱**）。
**两个方向判定相反**，而我在 §9 只按正向就封了"最强挑战者"。⇒ 正确的说法是：**"正向（以少打多）最强的是 `c-82`"**。

### 14c. 修好之后的答案：**两个方向都强**的候选（正向 >25% 且 反向 <20%）
| 包 | 正向 | 反向 | A卷 | TN |
|---|---|---|---|---|
| `v7l2c-103` | 43.3 | **13.3** | +37.2 | 0.0 |
| `v7l2f-106` | 40.0 | **5.0** | +28.3 | −4.2 |
| `v7l2s-82` | 35.0 | 15.0 | +42.6 | **+13.3** |
| `v7l2f-101` | 33.3 | **0.0** | +22.8 | **+30.8** |
| `v7l2h-81` | 31.7 | 10.0 | +39.4 | −24.2 |
| `v7l2f-81` | 28.3 | 10.0 | +27.4 | **+27.5** |
⇒ 只有 **6/17** 个包双方向都过；其中**三栏（正向、反向、TN）都不输现役**的只有 **`v7l2s-82` 与 `v7l2f-101`** 两个。
   （`s-82` = 只把攒钱奖励抬到 0.15；`f-101` = 全开 + 比率化兑现，A 卷最差的那个 seed —— 又是一次"标准卷与真实强度脱钩"。）

### 14d. 考卷能不能预测头对头？—— **不能**（这条与 §5c 的 r=−0.03 一致，现在补上另外两张卷子）
| 考卷（高出随机） | r(与正向) | r(与 −反向) | 臂内 r(与正向) |
|---|---|---|---|
| A 卷 | **−0.03** | −0.57 | c +0.25 / f −0.43 / h +0.27（**符号不稳**）|
| targeter | +0.06 | **+0.60** | c −0.06 / f +0.29 / h −0.68（**符号不稳**）|
| ringwall | +0.44 | +0.40 | n 不足 |
⇒ 三张考卷与"正向头对头"的相关**全在噪声里**（|r|≤0.44，n=17，且臂内一分层就翻号）。
⇒ **今夜到此为止的最硬结论：现有评测体系里，标准卷 / 对抗场 / 头对头是两个近似正交的方向，
   没有任何一个能替代另一个，也没有任何一个的分辨率够判 ±10pt（§11b）。**
   所以**晋升判据必须是"多头一致"**（§14c 那种筛法：每个方向都别垫底），而不是"某个总分最高"。

### 14e. ⛔ 修正上面 §14b 的**过度修正**（我写 14b 时忘了 §9 自己已经标过的那条限制）
§9 的警示框里明明写着：**反向那一列"不当判据"—— 4 份同策略会互相挤压（镜像拥挤）**，
并且给了实例（`f-102` 正向 33.3/36.7 而反向在位包拿 43.3/63.3 ⇒ 那是同族互杀，不是"它弱"）。
所以拿 `c-82` 的反向 35% 去"下调最强挑战者"，**是我用自己的老笔记打自己** —— 那句下调**作废**。

但这条修正本身也要设一个界，否则它就变成"凡反向不利于我就说那是互杀"的**免责话术**。可判别的形式是：
- **镜像自洽检查**：线上包自镜像时正向 = 反向 = **15.0%**（§9 校准框）⇒ 拥挤效应对"策略广"的包（G 8.05/10 种）**不成立**；
  `c-82` 是 **G 4.11/7 种**（窄得多）⇒ 对它**可能**成立。**"窄包更容易互杀"是有机制根据的，不是临时找台阶。**
- **能一步分离两个解释的实验**（≈5 分钟，给 ds）：把反向场从"4 席被测"改成"**3 席被测 + 1 席 random**"。
  · 若线上包的胜率从 35% **掉回 ≈15%** ⇒ 那 20pt 是"同族互杀"送来的假优势 ⇒ `c-82` 的正向最强成立；
  · 若仍 **≥30%** ⇒ 它带 4 席确实压不住人 ⇒ `c-82` 的正向 45% 要打上问号（它可能只是"以少打多擅长、成规模就不行"）。
  现有 `tools/head2head.mjs` 只支持 1v4 / 4v1 两种装配，**这一个对照我今夜没做** ⇒ `c-82` 的"最强挑战者"标签
  目前状态是：**正向成立、反向存疑、缺一个 3+1 装配的分离实验**。换包决策前请把这个补上（就一个命令行开关的事）。

## 15. ★★★★★ 02:24 **把 §14 的两方向矛盾彻底解决了**：反向场**没有判别力**（新量具 `tools/crowding.mjs`，两个 seed 各 80 局）
做法：把对手**恒定为 random**（先剥离"对手强弱"），只改同一个包占的份数 `k=1/2/4`，看**每席位**胜率怎么掉。
（这正是 §14e 说要补的那个分离实验，只是换了个更干净的装配：不需要 3+1，因为随机对手让"拥挤"成为唯一变量。）

| 包 | k=1 每席 | k=2 每席 | k=4 每席 | 拥挤指数 4÷1 |  | k=1 | k=4 | 指数 |
|---|---|---|---|---|---|---|---|---|
| （seed 7777）↓ / （seed 4242）→ | | | | | |（第二份独立复跑）| | |
| 线上包 | 57.5 | 30.6 | 19.7 | 0.34 | | 53.8 | 20.3 | 0.38 |
| `v7l2c-82` | **91.3** | 34.4 | 20.0 | 0.22 | | **81.3** | 20.6 | 0.25 |
| `v7l2c-103` | 78.8 | 28.1 | 16.9 | 0.21 | | 73.8 | 15.3 | 0.21 |
| `v7l2h-81` | 80.0 | 38.8 | 20.6 | 0.26 | | 80.0 | 20.3 | 0.25 |
| `v7l2s-82` | 65.0 | 33.8 | 20.3 | 0.31 | | 70.0 | 21.6 | 0.31 |
| `v7l2f-106` | 45.0 | 26.9 | 20.9 | 0.47 | | 51.3 | 19.7 | 0.38 |
| `v7l2f-101` | 30.0 | 23.1 | 16.6 | 0.55 | | 27.5 | 20.0 | 0.73 |

### 三条结论（两个 seed 都成立 ⇒ 可以当事实用）
1. **`k=4` 时所有包的每席位胜率都掉到 15~22%**（= 随机水平的 20% 附近），**与包的强弱无关**。
   ⇒ **"4 席同一个包"这个装配把强弱差异全部抹平 ⇒ `head2head` 的反向列本质上没有判别力**（它测的是拥挤，不是强度）。
   ⇒ §14a 那个 `r(正向,反向) = −0.25` 不是"强度有两个维度"，而是**反向那一列是噪声**。我今夜为它写了整节 §14 的解释，**作废**。
2. **`k=1`（1 席被测打 4 席 random）是这批里区分度最大的一栏**：线上包 54~58%，而 `c-82` **81~91%**、`c-103` 74~79%、`h-81` 80%。
   ⇒ 与正向头对头（`c-82` 45% vs 公平基线 15%）、A 卷（+44.3 vs 线上包 +37.6）**同向**
   ⇒ **`v7l2c-82` 强于线上包**这件事现在有**三个互相独立的装配**支持；§14b 我对它的下调**是错的，正式撤回**（§14e 已提出质疑，这里用数据关掉）。
   它在**唯一**输给出线包的一栏是 targeter 对抗场（−23.4 vs 线上包 −15.0）—— 也就是"会瞄人的场"，与 §5/§13 那条弱点轴同源。
3. **拥挤指数本身是个有用的新读数**，而且它和"强弱"是**两回事**：
   `f-101` 最弱（k=1 只有 27~30%）却**最不怕同族**（指数 0.55/0.73）；`c-103` 很强但指数最低（0.21）。
   ⇒ 它衡量的是"这个策略能不能和另一个自己共存"。若将来要做**多 AI 同桌**（用户说过离线是 4~7 人），
      这一栏比反向头对头有用得多 —— **该用 `crowding.mjs` 而不是 `head2head` 的反向列**。

### 判据更新（覆盖 §6/§13d/§14）
晋升证据应当是这四条独立装配**同向**，且**不再使用 `head2head` 反向列**：
① `head2head` 正向（1 席被测 vs 4 席现役，公平基线 15%）
② `crowding.mjs` 的 **k=1**（1 席被测 vs 4 席 random）
③ targeter 对抗场高出随机（§5 的弱点轴）
④ A 卷作为"别退步"的下限检查（§13b 证明它与 ①③ 反相关，不能当收益理由）
外加 `champ-audit` 的 G（有效技能种类）当**代价**栏 —— 目前所有挑战者都在这里付账（`c-82` 4.11/7 对线上包 8.05/10）。

## 16. ★★★★★ 02:35 今夜最后也是最大的一条：**"谁赢"有两把互相反号的尺子**，而现有验收只装了其中一把
（顺带 **修正 §15 的一处过度声明** —— 那次控制实验是我自己要求做的，结果它同时打掉了我的结论和抬出了新轴。）

### 16a. 先撤回 §15 的结论 ①
§15 说"`k=4` 时所有包每席位都掉到 15~22% ⇒ 反向装配没有判别力"。**这句被自己的控制实验否证**：
那一栏的平坦大半是**天花板假象** —— 4 席最多合计赢 ~80%（剩下那位总得赢点），每席位自然贴着 20%。
把对手从 `random` 换成 `balanced`（同一个量具 `CROWD_OPP=balanced`，80 局 × 两 seed），
`k=4` 每席位立刻铺开成 **3.8% ~ 24.7%** ⇒ **这个装配是能分辨的**，我据此把 §15 的结论 ① 降级为
"在随机对手面前分辨不出"（那本身只是一条关于该装配的信息，不是关于反向头对头的判决）。
⚠ 但 §15 的结论 ②（`c-82` 强于线上包由多个独立装配支持）**没有被推翻**，只是不再依赖"反向无判别力"这个论据。

### 16b. 相关矩阵（17 个包；V1=1席打4 random · V2=1席打4 balanced · V4=4席打1 balanced 每席位 · H/HR=头对头正/反向 · A=A卷 · T=targeter 场）
```
        V1    V2    V4     H    HR     A     T
V1   1.00 -0.49 -0.57 -0.30  0.40  0.71 -0.63
V2  -0.49  1.00  0.68  0.44 -0.44 -0.42  0.18
V4  -0.57  0.68  1.00  0.21 -0.66 -0.78  0.61
H   -0.30  0.44  0.21  1.00 -0.25 -0.03  0.06
HR   0.40 -0.44 -0.66 -0.25  1.00  0.57 -0.60
A    0.71 -0.42 -0.78 -0.03  0.57  1.00 -0.64
T   -0.63  0.18  0.61  0.06 -0.60 -0.64  1.00
```
⇒ 七把尺子塌成**两个方向相反的因素**（不是我的解读，是矩阵自己分成的两组）：
- **因子 P（打乱局 / 惩罚不犯错的人）**：`V1 +0.71` `A +1.00(就是它)` `HR +0.57`，而对 `V2 −0.42`、`V4 −0.78`、`T −0.64` 全负。
- **因子 C（打整局 / 对手会配合、会集火时还能赢）**：`V2`、`V4`、`T`、`H(+0.44 与 V2)` 互相为正，对 P 组全负。
⇒ **一个包在 P 上越高，它在 C 上就越差，几乎是镜像关系**（`r(V1,V2) = −0.49`、`r(A,V4) = −0.78`）。
   实例就在明细里：`v7l2h-93` 是 P 轴榜首（V1 **92.5%**），C 轴垫底（V2 **13.8%**，比线上包的 17.5 还差）；
   `v7l2f-101` 正相反（V1 30.0% 全场最低档，V2 33.8% 并列第二，TN **+30.8** 第一）。

### 16c. 三条直接可用的推论
1. **现役包坐在 P 轴上，而且坐得很浅**：`V2 = 17.5%`（公平份额 20% ⇒ **低于随机水平**），
   与 §5 那条"在会瞄人的场里低于随机"是**同一件事的两种测法**。今晚 17 个被测包里 **11 个 V2 高于现役包**，
   最高的 `f-106/f-82` 拿到 36.3%（≈1.8 倍公平份额）。
   ⇒ 换句话说：**用户线下 4~7 人局里遇到的"会配合的对手"这一维，现役包是明确吃亏的，而且这个吃亏在 A 卷上完全看不见（r(A,V2)=−0.42，还反号）。**
2. **② `CONV_RATIO` 第一次出现"正向且方向一致"的证据**（此前 §10/§11 只说它"有效但代价不明"）：
   `f` 臂 5 个 seed 的 V2 全部 ≥23.8%（均值 **32.5**），其余所有臂+现役包均值 **22.0**；
   同 seed 配对 `f − c` 在三个共同 seed 上为 **+16.3 / +5.0 / +3.8（3/3 同号，均值 +8.4pt）**。
   ⇒ 仍然**够不上判定**（80 局每席位 SE≈4.4pt、配对 n=3），但这是今夜唯一一条"三个 seed 同向 + 独立新轴"的正向证据，
      批次 6（12 个新 seed × c,f 配对）就是为把它推到可判水平而排的。
3. **验收套件缺一个轴，而补上它只要十几秒**：`node tools/crowding.mjs 80 <包>` 的 **V2 列**
   （1 席被测 vs 4 席 `pickBalanced`，80 局轮座）就是 C 轴最便宜的代理读数。
   ⇒ 建议把它加进晋升判据（§15 那张四条判据的清单里，第 ② 条现在应当换成 V2 而不是"打 4 random"，
      因为后者已被证明与 A 卷同侧、不提供新信息：`r(V1,A)=+0.71`）。

### 16d. 一句话给早上的用户
**"要不要换包"这个问题，今夜第一次有了不靠单一考卷的答案：现役包在"对手会配合"这一维上低于随机水平，
 而今晚有 11 个产物在这一维上超过它 —— 但它们全都为此付 A 卷的分数（因为这两维在这批包里天然反号）。
 真正要决定的不是"哪个分高"，而是"你要你的电脑对手更会打乱局，还是更会打整局"。**

## 17. ★★★★ 02:39 把 §16c 那句"② 第一次有正证据"做成**带对照的检验**（而不是 5 个包对 17 个包的目测）
数据 = 已有 24 个产物在 `crowding.mjs` 上的读数（80 局轮座；V1 对手 random / V2、V4 对手 balanced），
对照组**只用同配置的 `c` 臂**（开关全关、同池、同代数量、同健康门槛），不是"其它所有臂"。
置换检验 5 万次，单侧。

| 轴 | `f` 臂 (n=5) | `c` 臂 (n=8) | 差 | 置换 p | 解读 |
|---|---|---|---|---|---|
| **V2** 1 席打 4 balanced（打整局） | **32.5** | 24.2 | **+8.3pt** | **0.024** | ② 在这一维上**确实受益** |
| **V4** 4 席打 1 balanced 每席（成群打整局） | 18.6 | 14.0 | +6.9pt | 0.038 | 同向、量级小 |
| **V1** 1 席打 4 random（打乱局） | 53.6 | 77.4 | **−23.8pt** | 0.974（反方向显著） | 代价：**乱局惩罚能力明显变差** |

⇒ 这三行合起来是今夜**第一条方向自洽、带同配置对照、且过了置换检验**的结果：
  **`EPIRUS_CONV_RATIO=1` 把包从"乱局机器"往"整局选手"推**：整局 +8.3pt（p=0.024），乱局 −23.8pt（p≈0.97 反向）。
  与 §10/§11 的旧说法（"f 臂有效但代价不明"）相比，代价现在**有数了**，而且是**另一根轴**上的代价，不是同一根轴上的噪声。
⇒ 为什么此前 60 行考卷矩阵看不见它：`r(V2, A卷) = −0.42` ⇒ **A 卷不但测不到这个收益，还会把它读成损失**。
  这就是 §16 那两把尺子的现实后果，也是我今夜从"没差异"改口到"有差异但在另一根轴上"的原因。

### ⚠ 三条限制（照例先给自己泼冷水）
1. **V2 是事后建的轴**（我先看到 random 对手那栏数字反常，才换 `balanced` 做控制）。事后建的轴上的 p 值不能当预注册结果读；
   它的正确定位是**"值得为它花一次训练预算的假设"**，而不是"已证"。
   ⇒ 批次 6（12 个新 seed × c/f 配对）就是为它准备的确认实验：**看 f 在同 seed 配对下 V2 是否仍高于 c**。
2. **幸存者差异**：`c` 臂过健康门槛 8/13，`f` 臂 5/12。两臂被拦下的不是同一批 seed ⇒ 比较的是"幸存者分布"，
   若健康门槛与 V2 相关（它管的是"技能种类数/回合数"，与"打整局"未必无关），差值会被抬高。
   ⇒ 干净的修法：把两臂都改成 `EPIRUS_ALLOW_HEALTH_FAIL=1` 重训一批（会牺牲产物质量换可比性），或者只比"两臂都过的 seed"（批次 6 在做的事）。
3. V2 只有 80 局 ⇒ 每席 SE≈4~5pt。臂均值差 8.3pt 在包级置换检验里显著，但**单个包的 V2 读数不足以定名次**（别拿它去排"谁第一"）。

### 现在的推荐做法（给用户/早上的 ds）
- 想推进 ②：**别再看 A 卷**，用 `node tools/crowding.mjs 80 <新产物>` 的 V2 列 + 批次 6 的配对结果来判断；
- 不推 ②：那现役包在 C 轴上 17.5%（低于公平份额）这个事实要接受，它是 A 卷目标的直接产物（§13b/§16c）。

## 18. ★ 02:45 新量具的自检结果（它抓到的是**我自己的断言**错，不是装配错）+ 用和棋修正后的锚点
`tools/crowding.mjs` 加了 `CROWD_SELFCHECK=1`：在**对称装配**（5 席全 random / 5 席全线上包镜像）下跑 k=1/2/4。

| 自检 | k=1 | k=2 | k=4 |
|---|---|---|---|
| 全场 random | 13.8% | 12.5% | 14.4% |
| 全场线上包（镜像） | 15.0% | 15.0% | 15.6% |
| 期望值 `(100−和棋)/5` | 13.0%（和棋 35%）/ 15.3%（镜像和棋 24%） | 同 | 同 |

⇒ 第一版我把恒等式写成"应当 20%"，自检直接 ⛔ 报红 —— **红的是断言，不是装配**：
  多人局有 24~35% 的和棋，那部分不会变成任何人的胜，所以公平份额是 **(100−和棋率)/5 ≈ 13~15%，不是 20%**。
  这正是 §9 校准框已经写过的那条，**我今夜第二次忘了它** ⇒ 现在它写进了工具（`fair1` 列 + 自检断言），不会再有第三次。
⇒ 修正后对称性检验 **6/6 通过（比值 0.96~1.11）** ⇒ 座位轮转没有漏进身份通道，`crowding.mjs` 的读数可用。

### 用"倍数（每席位 ÷ 该装配公平份额）"重锚 §16/§17 的关键读数（对手 = 4 席 balanced）
| 包 | V2 倍数 | | 包 | V2 倍数 |
|---|---|---|---|---|
| `v7l2f-106` | **2.27×** | | `v7l2c-82` | 1.07× |
| `v7l2f-82` | **1.93×** | | `v7l2c-102` | 1.01× |
| `v7l2f-101` | 1.78× | | `v7l2s-82` | 1.00× |
| `v7l2c-103`／`v7l2c-114` | 1.69× | | **线上包** | **0.88×** |
| `v7l2s-91` | 1.67× | | `v7l2h-93`／`v7l2s-111` | 0.71× |
| `v7l2f-81` | 1.63× | | `v7l2o-93` | **0.44×** |
| `v7l2o-91` | 1.62× | | | |
⇒ **现役包在"对手会配合"这一维上 = 0.88 倍公平份额**（即：把它换成一个 random 席位，这个场反而更好打），
   而 25 个被测包里有 **15 个 ≥1.0×**。§16c 那句"11/17 高于现役"在这个更准的锚点下变成 15/24，方向不变。
⇒ §17 的臂结论**不受影响**（它比的是两臂同一锚点下的每席位胜率，`f` 32.5 vs `c` 24.2、p=0.024），
   只是把"公平份额 20%"这个标注改成"≈19~20%（对手 balanced 时和棋很少，所以这里恰好接近 20%）"——
   而 `k=1 打 4 random` 那根轴的公平份额只有 **13~16%**（和棋 21~35%），
   所以 V1 那些 80~92% 的数**除以公平份额是 5~7 倍**，比我原本的读法更夸张；反向说，现役包 V1=57.5% 也有 3.6 倍。

## 19. ★★★★ 02:48 把 C 轴领先的包送上 `champ-audit` 的完整体检台（**结果不体面**）
新跑 `--games=20 --exam-games=20`（表里标 ★ 的行是本节新测；其余取 §9 与批次 3 的读数）：

| 包 | V2 倍数 | h2h 正向 | A卷高出随机 | TN 高出随机 | D 破反弹墙 | F 被集火还手 | G 有效技能 | H 混合场 | 座位极差 | 珠浪费 |
|---|---|---|---|---|---|---|---|---|---|---|
| **线上包** | **0.88×** | （自镜像 15.0） | +37.6 | −15.0 | 80% | 14% | **8.05 (10)** | 60% | 27pt | 0% |
| ★`v7l2f-106` | **2.27×** | 40.0 | +28.3 | −4.2 | **0%** ⚠ | 42% | 3.60 (7) | **5%** ⚠ | 29pt | **100%** ⚠ |
| ★`v7l2f-82` | 1.93× | 11.7 | +41.6 | −17.5 | 100% | 32% | 3.47 (4) | 55% | 15pt | 100% ⚠ |
| ★`v7l2f-101` | 1.78× | 33.3 | +22.8 | **+30.8** | **0%** ⚠ | 46% | 3.24 (4) | **0%** ⚠ | **8pt** | 0% |
| `v7l2c-103` | 1.69× | 43.3 | +37.2 | 0.0 | 100% | 27% | 5.61 (10) | — | 6pt | — |
| ★`v7l2c-114` | 1.69× | 未测 | +44.0 | −17.5 | 100% | 38% | **3.05 (4)** ⚠ | 70% | 35pt | 0% |
| ★`v7l2s-91` | 1.67× | 43.3 | +44.3 | −15.9 | 100% | 29% | **5.95 (7)** | 70% | 40pt ⚠ | 0% |
| ★`v7l2o-91` | 1.62× | 未测 | +38.4 | **−23.4** | 100% | 36% | 3.09 (8) | 55% | 23pt | 0% |
| `v7l2c-82` | 1.07× | **45.0** | +44.3 | −23.4 | 100% | 34% | 4.11 (7) | — | — | — |

### 这张表说的三件事
1. **C 轴的两个榜首（`f-106`、`f-101`）在其他列上是残缺的**：破反弹墙 **0%**、混合场 **0~5%**、`f-106` 还有**珠浪费 100%**。
   ⇒ 它们不是"更强的包"，是**把资源全押在一种处境的包**。§16 把它们抬到 V2 顶端是真的，但**不能拿 V2 单一列做换包决定**。
2. **没有任何一列上"全线不垫底"的包**：最接近的是 **`v7l2s-91`**（V2 1.67× · h2h 43.3 · A 卷 +44.3 · D 100% · G 5.95/7 · H 70%，
   只有 TN −15.9 与座位极差 40pt 两栏难看）和 **`v7l2c-103`**（六栏都好，缺 H/珠浪费两栏未测，TN 0.0）。
   ⇒ 但 **`s` 臂是"攒钱奖励 0.15"那一臂** —— 而 §10b 刚说过它**没能把峰 ep 抬过 3**。
      也就是说 `s-91` 若真是最好的挑战者，它的强**不是**通过"学会攒钱"实现的 ⇒ 别把它的分数记在旋钮 ③/攒钱机制头上（这是今夜第三次同一个错误的形状：**给一个相关量编了一个机制故事**）。
3. **现役包唯一无人能敌的列还是多样性**（G 8.05/10 种；所有挑战者 3.0~6.0）。
   这一列不是装饰品 —— `promote-champion.mjs` 拿 E/F/G 做**阻断条件**，而 G<3 更是健康门槛直接拒发产物。
   ⇒ 换包的实质问题从 §9 起没变过：**愿不愿意用一半的多样性去换"对手会配合时也能赢"。**

### 修正一处我自己的话（02:47）
批次 3 裁定里我写 `v7l2o` 的"结构判据过了（cost≥2 出手占比 14~16%）"—— **那是替代量具，不是它的设计判据**。
`champ-audit` 的直接读数：`o` 臂 **C 盾/局 = 0.0~0.2（现役包 2.5）** ⇒ **它根本没有更防守**，
只是"被集火时还手率"从 14% 提到 26~36%。⇒ 批次 3 那条判据的正确状态是 **③ 未测/未达**，不是 ✔。
（同一个混淆我在 §0 也写错过一次，已就地改掉。教训：**"cost≥2 占比"≠"防御族占比"，两个别混用。**）

## 20. ★★★★ 02:49 补齐头对头之后，**"全线不垫底"的那个包换了**：是 `v7l2s-91`，不是 `c-82`
`node tools/head2head.mjs 60 <新包>`（60 局轮座 · 同 seed 同盐 · 公平份额 ≈15%，`h2h-sweep2.log`）：

| 包 | 正向 multi | 正向 long | 反向（线上包 1 席打 4 席它） |
|---|---|---|---|
| **`v7l2s-91`** | **43.3%** | **46.7%** | 31.7% / 33.3% ⚠ 反向偏弱 |
| `v7l2s-111` | 35.0% | 31.7% | 20.0% / **8.3%**（反向最好的一档）|
| `v7l2c-114` | 33.3% | 31.7% | 36.7% / 21.7% |
| `v7l2o-91` | 36.7% | 35.0% | 28.3% / 36.7% |
| `v7l2o-93` | 26.7% | **41.7%** | 11.7% / 15.0%（反向也强）|
| `v7l2o-92` | 11.7% | 23.3% | 25.0% / 25.0% ⇒ 这一臂最差的一例 |
| 参照：`v7l2c-82` | 45.0% | 46.7% | 35.0%（反向判弱，见 §14a/§16a）|
| 参照：**线上包自镜像** | 15.0% | 13.3% | 15.0% |

### 把 §19 与 §20 合起来读（10 列全的包只有这几个）
| 包 | V2 | h2h 正向(多/长) | A卷 | TN | D 破墙 | F 还手 | G | H 混合 | 座位极差 | 珠浪费 | 几栏不合格 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **`v7l2s-91`** | 1.67× | **43.3 / 46.7** | +44.3 | −15.9 ✗ | 100% | 29% | **5.95 (7)** | 70% | 40pt ✗ | 0% | **2 / 10** |
| `v7l2c-103` | 1.69× | 43.3 / 41.7 | +37.2 | 0.0 | 100% | 27% | 5.61 (10) | 未测 | 6pt ✔ | 未测 | 0~1 / 10 |
| `v7l2c-82` | 1.07× | 45.0 / 46.7 | +44.3 | −23.4 ✗ | 100% | 34% | 4.11 (7) | 未测 | 未测 | 未测 | 1~2 / 10 |
| `v7l2o-93` | 1.4× 级 | 26.7 / **41.7** | +33.6 | −20.0 ✗ | 0% ✗ | — | 4.65 (9) | — | 20pt | 0% | 2 / 8（列不全）|
| **线上包** | **0.88×** ✗ | 15.0（自镜像） | +37.6 | −15.0 ✗ | 80% | 14% ✗ | **8.05 (10)** ✔ | 60% | 27pt | 0% | 3 / 10 |
⇒ **今夜到早上为止的结论应当这样写**：这批产物里 **`v7l2c-103` 与 `v7l2s-91` 是"没有任何一栏明显塌掉"的两个**，
   而它们在 C 轴（打整局）上是现役包的 **1.7 倍**、头对头正向是 **2.9~3.1 倍公平份额**。
   ⇒ 但请注意三个不要：
   · **不要**把它当成"已判定更强"——每栏 n=20~60 局，SE 4~9pt，而 §11b 说臂级判定要 8 个配对 seed；这是**筛选级**证据。
   · **不要**因为 `s` 臂赢了就宣布"攒钱旋钮有效"——§10b 实测它**峰 ep 仍是 2~3**，赢的机制不在它的设计意图上（机制未定）。
   · **不要**换包（我没换，也不建议早上就换）。**下一步应当是 `gate-landscape` + `promote-champion --dry` 过一遍门禁**，
     看这两只包在**成套阻断条件**下说什么 —— 那才是换不换的守门人，我给的只是材料。

## 21. ★★★ 02:55 用健康门槛的那个协变量（G）给 §17 的结果做分层检验 ⇒ **活下来了，但缩水，且暴露了它的代价**
先把 15 个还没体检的产物补跑 `champ-audit`（`docs/artifacts/audit-battery2.log`），再按 `G 有效技能数` 分层重做置换检验：

| 比较 | f 臂 | 对照 | 差 | 置换 p（5 万次）|
|---|---|---|---|---|
| 未分层（V2） | 31.6 (n=4) | 其余 20.8 (n=15) | +10.8 | **0.0056** |
| **只在 G<4 层内** | 31.6 (n=4) | 非 f 22.9 (n=6) | **+8.7** | **0.025** |
| **只在 G<4 层内 · f vs c 同配置对照** | 31.6 | c 22.1 (n=3) | +9.5 | **0.058** |
| G≥4 层内 | **f 臂 0 个包** | — | 不可比 | —— |

⇒ §17 那条"② 在 C 轴上受益"**在按多样性分层之后仍然成立**（+8.7 / p=0.025；换成严格同配置的 `c` 臂对照则 p=0.058，属"提示级"）。
   效应比未分层缩掉约 20% ⇒ **幸存者效应确实占了一部分，但不是全部。**
⇒ **新的、也是更该记住的一条**：`f` 臂 4 个有 G 读数的产物**全部 G<4**（3.03/3.24/3.24/3.47），
   而 `c/h/s/a` 臂都有 ≥4.6 的产物、现役包是 **8.05 (10 种)**。
   ⇒ **`CONV_RATIO` 这个旋钮的真实代价不是"A 卷掉几分"（那是未配对均值的旧读法），而是"它长出来的包一律窄"**，
      而健康门槛（G≥3）只是刚好没让它们全军覆没 ⇒ 也解释了 §12 那个"批次 5 里 f 臂 0/5 全被拦"。
   ⇒ **用户点名要的是多样性** ⇒ 按这条，`f` 臂**不该**被端上桌；要看的是"能不能在保住 G≥5 的前提下拿到 C 轴收益"（今夜没有这样的包）。
### 顺手的一条交叉验证
`champ-audit` 对 `v7l2c-91` 与 `v7l2h-91` 输出的**整行逐字相同**（A 49.6% · 伤害 13.9 · 盾 0.3 · 回合 28.3 · D 95% · E 12% · F 25% · G 5.47/12 种 · 座位极差 31pt …）。
⇒ 两个**互相独立实现**的量具（champion payload 哈希 与 体检表）给出同一个结论：这两次训练产出的是同一个包。
   也就是 §10a 那条"L2′-① 对决策是空操作"的证据，现在多一条独立佐证。

## 22. ★★★ 03:00 给下一步的三个具体实验（每个都标了**预估成本**和**什么结果算证伪**）
### E1（最便宜，先做）：把 V2 正式当判据，并注意到**它的分辨率比考卷高一个量级**
今夜三个共同 seed 的配对 V2 差：`f−c` = **+16.3（seed82） / +5.0（101） / +3.8（102）** ⇒ 均值 +8.4、**SD 6.9**。
对比考卷侧同 seed 配对 SD 是 **14~53pt**（§11a）。
⇒ 判一个 8pt 的效应所需配对数 `n ≈ (2×6.9/8)² ≈ 3` —— **不是 §11b 的 9 个 seed**。
   所以批次 6 只要产出 **≥4 对**（`c` 与 `f` 同一 seed 都过了健康门槛）就够给 §21 一个**判定级**答案。
   ⚠ 这条本身也提醒我：判"要不要更多 seed"之前，先算**这个读数自己的 SD**，别把考卷的分辨率套到所有量具上。
**证伪条件**：批次 6 的配对差均值若 <0 或 SD >20 ⇒ §17/§21 的正向读写作废，`CONV_RATIO` 回到"无效应"。
### E2（15 分钟）：`CONV_RATIO` × 多样性下限 —— 今夜**从没试过的组合**
`f` 臂的代价已被量化：**4/4 个幸存者 G 全 <4**（3.03~3.47）对现役包 8.05 ⇒ "会打整局"与"打法宽"目前不可兼得。
但没有任何一臂试过 **`CONV_RATIO=1` + 显式多样性压力**（`EPIRUS_DIV_FORCE_GENS` / `EPIRUS_K_ROLE` / `EPIRUS_DIV_W`，旋钮都已存在且默认旧值）。
另外 `l2-arms` 里定义了但**从未跑过**的 `v7l2b`（攒与花同时给斜率）也该在这一批里带上。
**判据**：`V2 > 28%` **且** `G ≥ 5` **且** `H 混合场 ≥ 50%` 三者同时成立 ⇒ 这才是"没有牺牲用户点名的那一维"的改进；任一不满足就照 §19 的结论办（不上桌）。
### E3（10 分钟）：`f` 臂的**珠浪费 100%** 与 **破墙 0%** 要单独追一次
`f-106`/`f-82` 的 `珠浪费=100%`（`champ-audit` 列）+ `D 长程反弹墙 = 0%` 一起看，指向一件事：
**它们把"该留珠/该破墙"的分支整个丢掉了**，不是权衡而是缺失。⇒ 用 `tools/probe-convert.mjs`（已修好环成本漏报）
和 `tools/probe-targeter-why.mjs` 各跑一遍这两个包，看是不是**特征可见但策略不选**（那就是训练问题），
还是**候选表里根本没出现这些动作**（那是 `econBase`/门槛问题）。两者对应的修法完全不同，别混。
⚠ 我今夜只做到"发现这两个数难看"，**没有**追到机制 —— 这一条留给早上的你，是我自己没做完的部分。

## 22b. 以及一条我认为**不该再花算力**的方向（写清楚免得你我去重踩）
**任何"让包学会攒 ep / 学会打环"的经济 shaping 加档**：今夜三条独立证据都指向同一面墙 ——
① 终局余款 >C 触发率 **0.00%**（7 包 × 2 模式，§10a）⇒ 没有对象；
② 攒钱奖励 3× 后峰 ep 仍是 **2~3**（`s` 臂，§10b）⇒ 买不动跨期计划；
③ 峰 ep=1 的 `f-81` 在对抗场最好之一（§12）⇒ **强度不需要 ep 深度**。
要突破它只能把 shaping 给到胜率量级（用户明令禁止）或改规则（用户明令不改）。⇒ **建议正式停止这条线**，
   把预算挪到 E1/E2/E3 与"多 AI 同桌"（`crowding.mjs` 的 V4/拥挤指数那一维，今夜第一次量，信息量不小）。

## 23. ★★★ 03:03 §22 的 E3 我现在就收掉了：**防御不是"看不到"，是"目标函数不为它付钱"**
新量具 `tools/probe-defense-visible.mjs`（30 局 5 席自对局 · 逐决策看 seat 0 的候选表与前向概率）。
先记一条先验（省得走弯路）：**防御族卡费用是 0 ep**（`rules.js:54~58` 的 `mk(SK.GUARD,…,0,3,…)` 等）
⇒ "攒不到钱 ⇒ 出不了防御"这个解释**结构上不成立**。

| 包 | seat0 决策数 | **候选表含防御卡%** | 含防御时 p(防御)均值 | argmax=防御% |
|---|---|---|---|---|
| 线上包 | 440 | **100.0%** | 0.125 | **33.2%** |
| `v7l2s-91` | 750 | 100.0% | 0.056 | 16.4% |
| `v7l2o-93` | 448 | 100.0% | 0.024 | 7.1% |
| `v7l2c-103` | 429 | 100.0% | 0.007 | 2.1% |
| `v7l2f-106` | **60** ⚠ | 100.0% | 0.000 | 0.0% |

⇒ **成因是 (B) 策略，不是 (A) 结构**：五个包**每一次**决策的候选表里都有防御卡（100.0%），
   差别**全在网络给不给概率**。⇒ `D 长程反弹墙 = 0%` 这类读数不是"候选表漏了它"，
   **是适应度里没有一项为"挡住一次伤害"付钱**（防御的回报是"少输"，而胜率项是结果量 ⇒ 中间步骤没有梯度）。
   ⇒ 修法只有两类：**(i)** 给"挡下的伤害"加一个独立奖励项（像 `dealW` 那样，但要小心 §10b 那类"给不到量级就没用"）；
      **(ii)** 承认这是结果导向训练的固有盲区，改从**评测/筛选**端解决（用 V2/对抗场筛，不指望 shaping 教会）。
⇒ **顺带一个反直觉的数**：在自对局面前，**现役包反而是这批里最肯出防御的（33.2%）**，
   而 §5 说的"它只有 12.6% 的出手给防御"是在**被针对的场**里的读数。
   ⇒ 两件事都对，但合起来说明：**现役包的防御倾向是"看对手给的"，不是自己稳定具备的** —— 这比"它就是不防守"更准确，
      也更值得作为改进目标（把它变成"不被针对也留防御预算"）。
⚠ `f-106` 那行 **seat0 决策数只有 60**（其余包 429~750）⇒ 它自对局的场次/长度结构异常（每局 seat0 只出 2 手），
   所以它那几个 0 我**不当结论用**，只当"这一行需要单独查"的标记。⇒ 记进 §7 的遗留。

## 24. ★★★ 03:14 objective ③ 的**字面实验**现在做完了（不落盘改任何代码）
之前我在 §2 结尾写"字面做法留给你/用户决定"，那是**我把代价算错了**：真正危险的只是"改动落在磁盘上"，
而"改动只活在探针自己的 vm 沙盒里"完全不碰指纹、不碰 np-test、不碰线上包。⇒ 新工具 **`tools/probe-ep-unfrozen.mjs`**：
读 `policy.js` 的**源码字符串**，在内存里替换 §2 列出的那 6 个 ep 编码点（任一处找不到就抛错，不许静默漏改），
喂给一个新建的 vm 沙盒执行。实测（线上包 · 12 个真实中局面 × ep 12/16/20/30/40/60）：

| 变体 | 不同**状态向量** / 6 档 | 不同**概率向量** / 6 档 | argmax 与 ep=16 一致 |
|---|---|---|---|
| `original`（对照） | **1.75** ⇐ 16/20/30/40/60 全塌成同一个 | 1.08 | 100% |
| `linear40`（把 12 换成 40） | **5.83** | **1.92** | **94%** |
| `log40`（提案 A） | **6.00** | **1.75** | **100%** |

⇒ 两句话，一句回答 ③、一句给出代价：
1. **"16/20/30/40 是否不再逐位相同" —— 是。** 表征层的冻结确实纯粹由那 6 处 `/12` 夹住造成（抬到 40 或换 log1p ⇒ 6 档全部可区分）。
   （顺带这也**验证了 §2 的"精确改点"清单是完整且仍然存在的**：6 处逐字匹配成功。）
2. **但解冻之后网络的输出几乎不动**：状态向量从 1.75/6 抬到 5.83~6.00/6，概率向量却只从 1.08 抬到 **1.75~1.92/6**，
   且 argmax 与 ep=16 时**94~100% 相同** ⇒ **现役权重里没有"ep 条件性"这个东西可解锁**。
   ⇒ 与 §12 独立同向（那里是"整条 2→40 阶梯 argmax 恒为狙击 12/12"）。
   ⇒ 所以"L1 抬编码上限"**只有配重训才有意义**，这不是推论而是量出来的：
      输入解冻了、输出没动 ⇒ 想让它动就得付 `FEAT_S` 换代 + 全量重训的代价（§2 末尾的清单）。
✅ 事后核对（03:14）：`git status js/` 为空、`rules-fingerprint` 仍 **1391c094**、线上包仍 **`268461f7c9f0`**。

### 22c. ⛔ 修正 E1 里我自己算错的一处：**"~4 对就够判"是错的**，配对符号翻转检验有**离散下限**
把上面那条算式做成了工具 `tools/l2-e1-check.mjs`（只读不算，输入是 `crowd-*.log` + `audit-*.log` + 考卷矩阵），
拿现有的 **3 对**（seed 82/101/102，批次 2 的幸存者）跑一遍，输出：
```
V2 打整局：均值 +8.4pt · SD 6.9 · SE 4.0 · 同号 3/3 · 符号翻转检验 p=0.1333   ← 不显著
V4 成群  ：均值 +3.7pt · SD 4.5 · 同号 2/3 · p=0.2628
V1 打乱局：均值 −22.9pt · SD 29.9 · 同号 0/3 · p=1.0000（代价确实存在）
考卷 A卷 ：−2.0pt · SD 11.8      考卷 TN：+11.7pt · SD 14.6   ← SD 比 V2 大一倍到两倍
```
⇒ 两件事：
1. 均值/SE 那套**正态近似**（`n ≈ (2×6.9/8)² ≈ 3`）在 n=3 时不能配符号翻转检验一起用：
   **翻转检验的最小可达 p 是 `2^-n`** ⇒ n=3 时下限就是 **0.125，永远不可能 <0.05**，n=5 才有 0.031 的空间（且要 5/5 同号）。
   ⇒ 所以 E1 的真实门槛是 **≥6 对**，不是 4 对；批次 7（`EPIRUS_ALLOW_HEALTH_FAIL=1` 保证 12 对）**正好跨过这个下限**。
   ⇒ 教训：**报 p 之前先看检验本身在这个 n 上能不能产出那么小的 p**（离散检验的分辨率由 n 决定，不由效应决定）。
2. 好消息是**三根轴方向一致且互相区分**：V2/V4 为正、V1 显著为负（−22.9pt，3/3），
   而考卷口径在同一批配对上 SD 是 11.8~14.6（V2 是 6.9）⇒ **V2 确实是更锐利的量具**，这正是 §16/§17 想说的话。

## 25. ★★★★★ 04:46 **E1 出结果了：② 的收益是真的，但它比 §17 说的更窄——而且是"装配特定"的**
批次 7 用 `EPIRUS_ALLOW_HEALTH_FAIL=1` 拿到 **12 对同 seed 产物**（加批次 2 的 3 对 + seed 31 ⇒ 16 对）。
`node tools/l2-e1-check.mjs f c`（配对差 `f − c`，符号翻转检验 20 万次）：

| 读数 | 配对 n | 均值差 | SD | 同号 | 单侧 p |
|---|---|---|---|---|---|
| **V2** 1 席打 4 `balanced` | 16 | **+7.4pt** | 11.7 | 11/16 | **0.0120** ✔ |
| **V4** 4 席打 1 `balanced` | 16 | **+5.4pt** | 7.2 | 13/16 | **0.0075** ✔ |
| **V1** 1 席打 4 `random` | 16 | **−28.3pt** | 35.2 | 3/16 | 0.995（即反向显著）⇒ **代价是真的** |
| **头对头正向** 1 席打 4 席**线上包**（multi） | 12 | **−0.4pt** | 13.4 | 7/12 | ⇒ **完全没有差别** |
| 头对头正向（long） | 12 | −2.5pt | 19.8 | 6/12 | ⇒ 同样没有 |
| 考卷 A 卷 | 15 | **−5.4pt** | 10.5 | — | ⇒ 见下面的自我修正 |
| 考卷 targeter | 15 | +7.7pt | 24.8 | — | 方向为正但不显著 |

**分层（把 G 当协变量，§21 的做法）**：`G<3` 层内 6 对 **+12.7pt**（5/6 同号）、`G∈[3,4)` 2 对 +4.4pt（2/2）、
`G≥4` **0 对** ⇒ 效应不靠多样性解释，但 `f` 臂在这个 seed 段**一个宽包都没长出来**。

### 25a. 结论（一句）
**② `CONV_RATIO` 是一个"轴切换器"，不是"增强器"**：它把包从"惩罚犯错的人"（V1 −28pt）切向
"和会配合的对手过招"（V2 +7.4pt / V4 +5.4pt，两者 p<0.012），代价还包括 A 卷约 5pt 与产物一律偏窄（G<4）。
⇒ **但它给的"更强"只在『对手是脚本 bot』那种整局里成立**：
   换成"1 席打 4 席现役包"这个最接近真实对手的装配，**f 与 c 无差别（−0.4pt，12 对）**。
   ⇒ 所以 §17 那句"② 在 C 轴上受益"**成立但要点到为止**：C 轴也不是一根轴，它按"整局的对手是谁"再分裂
   （`balanced` 脚本 +7.4 ✔ ／ 4 席现役 −0.4 ✘ ／ targeter 脚本 +7.7 不显著）。**别说成"② 让包更强"。**

### 25b. ⛔ 自我修正第四次：§11c 里我"撤回"的那条，现在**部分回来了**
§11c 说 `f` 臂 A 卷"未配对 −6.5pt 是假的，配对后 −2.0±11.8pt 与 0 不可分"。
⇒ 那句**撤回**当时是基于 **n=3 对**；现在 n=15 对是 **−5.4±2.7pt**（SE 2.7 ⇒ t≈2.0）。
⇒ 正确的说法是：**方向和未配对时的估计（−6.5）其实差不多，只是 n=3 时测不出来**。
   也就是说我 02:05 那次"撤回"撤得太早了 —— 它否证的是一个我当时样本不足以判断的命题，
   而 fuller data 站回了原来的方向。**教训：撤回也要标 n；小样本上的"撤回"和大样本上的"结论"一样危险。**

### 25c. 顺带一条对"要不要用 ②"的实际判断（给用户的语言）
- 用户说过线下是 **4~7 人**、且"为多样性牺牲一点胜率可以接受"。
- ② 恰好是**反着来的**：牺牲的是"乱局压制力"和 **G 多样性（产物全部 <4 种）**，换来的胜率收益在最接近真实对手数的装配里 ≈ 0。
⇒ **按用户自己给的价值观，② 不该上。** 但它作为"研究工具"有价值：它证明了
   **适应度里『把花光 ep 当目标』这一项会把种群推向'只会打脚本'而不是'只会打弱者'** —— 这是机制级的发现，不是调参。
⇒ 若还要继续挖，正确的下一步不是调 `CONV_RATIO` 的权重，而是 §22 E4：**给"挡下一次伤害"独立计分**
   （§23 已证防御是"看得见但不选"，且防御卡 0 ep ⇒ 这是唯一还可能有效的 shaping 方向）。

## 26. ★★★★ 04:51 `gate-landscape` + `promote-champion --dry` 的实测结果：**今夜没有一个产物能过门禁，包括在位包自己**
（训练全部结束后跑的，锁已释放 ⇒ 门是**真判**不是"训练中"占位。**两个 dry 都没有换包**：跑完线上包仍是 `268461f7c9f0`。）

### `gate-landscape`（五道门）
| 包 | 结果 | 阻塞项 | 单卡% | G_eff | G | 清场 | 墙伤 | 座位极差 |
|---|---|---|---|---|---|---|---|---|
| **线上包（参照行）** | **✗ 墙** | 反弹墙 | 31.8% | 6.65 | **8.05** | 4.0 | **0.35** | 26.9 |
| `v7l2s-91` | ✗ 座位 | 偏座 | — | 5.90 | 5.95 | 2.02 | 17.9 | **39.6** |
| `v7l2c-103` | ✗ 场B | 清场不足 | 28.8% | 6.25 | 5.61 | **0.05** | 15.95 | 6.1 ✔ |
| `v7l2f-106` | ✗ 场B | 清场=0 | **62.6%** | 3.03 | 3.6 | 0 | 17.0 | 29.3 |
⇒ **五道全过 = 0 / 4**，且在位包自己也过不了（只枪格）。工具自己的话抄在这里：
   *「第一行是在位包（参照行）：它自己过不了 —— 别把'参照不行'读成'候选不行'。」*

### `promote-champion --dry`（真门禁，只读）
- `v7l2c-103` ⇒ **⛔ 6 项阻断**：
  · **G4**：有一行脚本能以 **68%** 击败它（「只防御不还手」）；
  · **G5**：面对"只防御不还手"**清场失败**（防席夺冠 65%(long)/70%(multi)，阈值 ≤25%）；
  · 珠经济：**花珠率 2.9% · 浪费率 97%**（未闭环）。
  · 同时它标出**参照行不阻断的两条 FAIL：`G6[线上包] 靶向率 ≥40%` 没过**，还有 2 格 UNRUN ⇒ 按门禁自己的定义，这是"**该换包**"的信号。
- `v7l2s-91` ⇒ **⛔ 5 项阻断**：**座位极差 40pt**（51/11/11/15/12%，判胜 91 局）+ **G4**（「只枪 1ジ压制」72%/63% 能克它）。

### 这一节对 §20 的更正（重要）
§20 说 `s-91` 与 `c-103` 是"没有任何一栏明显塌掉"的两个 ⇒ **在门禁口径下不成立**：
它们各自塌掉的正是**硬阻断格**（`s-91` 偏座 40 ≥ 30 阈值；`c-103` 清场 0.05 且被"只防御"行以 68% 击败）。
⇒ 也就是说我 §20 那张表**用的列不是门禁的列**（我列了 A 卷/TN/D/F/G/H/座位，但没列"能否被一行脚本克制"与"清场阈值"）。
   **补一句可复用的判据：候选表必须直接用 `promote-champion` 的阻断条件当列**，否则"全栏不垫底"是可以一边垫底一边好看的。
⇒ 今夜真正的排序结论因此更保守：**没有任何一个产物达到"可上架"标准**；
   在位包被门禁自己判为"该换"（G6 靶向率 FAIL + 反弹墙），但**没有合格候选可换** ⇒ 保持现状是对的，
   下一步该做的是 §22 的 E4（给"挡下伤害"计分）或"清场/克制脚本行"这两条**被门禁点名**的能力，
   而不是继续在经济 shaping 上加旋钮。

## 27. ★★★ 05:05 用户一句话问出来的漏洞：**"ep 深度与强度解耦"这句口径是错的**，得改成"只与 P 轴正相关、与 C 轴无关"
问的是：§10b/§12 说"ep 深度不载重"，那是**在哪根轴上测的**？—— 答案是"A 卷 + targeter + 自对局反事实"，
而 §16 已经证明 `r(A卷, V1) = +0.71` ⇒ **A 卷基本就是 P 轴的代理**。也就是说我当时**只在 P 轴上测过，却写了"与强度无关"**。
补测（新工具 `tools/l2-epdepth-axis.mjs`，n=**48** 个包；单位是包、同臂 seed 不独立 ⇒ 只读方向不读显著性）：

| ep 深度指标 | V1 打乱局 | **V2 打整局** | V4 成群打整局 | A 卷 | TN |
|---|---|---|---|---|---|
| 峰 ep | **+0.35** | **−0.07** | −0.12 | +0.16 | −0.15 |
| ep≥3 决策占比 | +0.35 | −0.08 | −0.07 | +0.12 | −0.09 |
| 环可负担点占比 | +0.35 | −0.08 | −0.06 | +0.11 | −0.09 |
| 兑现率（反向指标） | −0.21 | +0.02 | −0.10 | −0.04 | +0.05 |
| 终局余款 | +0.30 | −0.09 | +0.02 | +0.07 | −0.18 |

按"峰 ep"二分（浅 ≤2 共 33 包 / 深 ≥4 共 11 包）的均值差：**V1 +22.4 · V2 −2.5 · V4 −0.1 · A 卷 +3.4 · TN −2.6**。

### 三条改口
1. **ep 深度不是"与强度无关"，是"只与打乱局那根轴有关"**：它给的是 **+22pt 的乱局压制力**（这也是为什么现役包在 bot 场里分数一直好看）。
   在 C 轴上是 **−0.07 / −2.5pt**，即**完全不平、甚至略负**。⇒ 原话要收窄成这句。
2. **但"环线该作废"的结论不变，而且更清楚了**：当初做环的动机写的是"让 AI 对**会玩的人**更强"＝C 轴；
   而 ep 深度在 C 轴上是平的 ⇒ 投入产出对不上（这是**新写的这条更直接的理由**，比原来"A 卷读不出"更贴动机本身）。
   因果版证据仍是 §4 的 −52.5pt（健康包交出 8 个回合打整局）。
3. **反向证据也自洽**：`兑现率` 与 V1 是 **−0.21**（越"立刻花光"越不压制乱局）、与 V2 是 +0.02 ——
   和 §25 的干预结果同一形状（② 把峰 ep 压到 1 ⇒ V2 +7.4 / V1 −28.3）。
### 给自己的注脚
这是今夜**第六次**"结论对、口径错"（前面是未配对均值、跨包伪相关、两方向解释、c-82 下调、20% 公平份额）。
共同形状都是：**用一个装配测出的相关性，去说一个所有装配都成立的话。**
⇒ 凡是"X 与强度无关"这类句子，以后必须写成"X 与**哪个读数**无关"，读数名写在同一行里。
