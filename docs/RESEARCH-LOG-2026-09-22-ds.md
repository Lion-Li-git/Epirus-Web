# RESEARCH LOG · DS 2026-09-22（跨 N 混适应度第一臂 + "起点保护"否证）

> 接 `RESEARCH-LOG-2026-09-22-qoder-night.md`（千问夜班）与 `RESEARCH-LOG-2026-09-21-ds.md`（我 09-21）。
> 用户 09-22 指令："那你试一下训练吧"。

## 0. 先记一条**否证**（我在跑训练之前做的、按规矩回滚）

**假说**：ε 悬崖的成因是"链的**起点**（蓄能）仍被探索改判"（v1.5.149 的序列窗锁只锁了链的中后段：持珠/上手蓄能/有我方符咒）。
**做法**：把铺垫卡（`CHARGE`/`CURSE`）从 soft 探索集里**整个拿掉**（`HOLD_MIN_EP = Infinity`，同一机制、只把门槛抬到无穷）。
**实测**（`behavior-profile --field=self --gamemode=long --epsmode=soft --games=20`）：

| ε | 电磁炮/局（修前） | 电磁炮/局（修后） | 蓄能/局（前→后） |
|---|---|---|---|
| 0.1 | 1.10 | **1.10** | 2.00 → 2.65 |
| 0.2 | 1.00 | **0.90** | 1.60 → 1.20 |

⇒ **没修好**（噪声级）⇒ **假说否证、改动已 `git checkout` 回滚**（无实测收益的改动不留）。
⇒ **真正的机理（两次否证后剩下来的解释）**：ε 的代价不是"每回合篡改序列步"，而是**整局的状态分布漂移** ——
  任何 ε>0 都把长程局长从 **46.1 → 23~29 回合**、把防御从 0.0% 抬到 2.5~5.1% ⇒ 经济与集火结构整体换了一套
  ⇒ 蓄能/电磁炮的**贪心分量**跟着掉（ε=0.1 与 0.2 几乎一样：1.10 vs 0.90 ⇒ **悬崖在 ε>0 就已饱和**）。
  这与我在三栏对照里发现的"真机是第三套分布"是同一类现象：**行为是分布的函数，不是每回合规则的函数**。
⇒ **产品含义（要用户裁）**：ε 买到的是"镜像局 100% 破局"（ε=0 时 0%），代价是"贵卡经济塌"。
  而**这两件事的模式不同**：镜像死锁只在 **N=2 且双方同策略**时出现（ε=0 的 2 席长程镜像 104 回合 0% 决胜）；
  我 09-22 的 ε=0 **5 席长程自对局**局长 46.1、**决胜 100%**（没有死锁）⇒ 建议 **ε 按人数/N 分档**（N=2 保 ε，N≥3 收到很小或 0）。

## 1. N6 跨 N 混适应度 · 第一臂（**预注册 · 跑前写死**）

**要回答的问题**：把 2P 切片写进 3P 的目标函数（千问 §N6 的实现，`EPIRUS_XN2W`），
能不能让一个 **3P 包在不牺牲 3P 的前提下、在 2P 里不再是 0-120 被屠**（现役 3P 包在 2P = **0-120**，夜测 E1-E3）？
这是"2P 移植靠撞大运"（§N5：23 粒里 ~17%）的**系统性出路**。

**配方**：
```
EPIRUS_HOTSTART=1 EPIRUS_SEEDPACK=docs/artifacts/v7cmin4-31.bak \
EPIRUS_SEED=31 EPIRUS_XN2W=1 \
node tools/train-3p.mjs 100 5 8 8
```
（热启动 = 现役 3P 冠军 `v7cmin4-31` ✓；N=5 · 每代 8 局 · 种群 8 · 100 代 ⇒ 单进程量级的一臂，**探索性**，非 promote 级。）
产物：`docs/artifacts/v7xn1-31.bak`（跑完立刻改名 + 点名；`train-3p` 直写 `train-3p-out.js`，**不碰槽**）。

**判据**：
- ① **主判据（2P 迁移）**：产物在 2P 上对现役 2P 冠军（`v7xfer44c13-2p`）的 H2H —— 现役 3P 包是 **0-120**；
  预注册门槛：**≥ 20% 胜率**（= "有明显裂口"，不要求赢）。工具用千问夜班的 `tools/night-probe-2p-transfer.mjs`（口径同 §N5）。
- ② **代价判据（3P 不许崩）**：`promote-champion --dry` 记录五道门 / G4(multi，n=60 够用) / G5 / 珠经济 / 座位 / G；
  探索性臂不要求全过，但**必须记录**；若连"球经济闭环"或 G 都塌到现役以下 ⇒ 记"混适应度把 3P 侧拖坏"。
- ③ 目标分（`firstRate/top2Rate`）与现役对照。
**否证分支**：若 ① 仍 ≈0 ⇒ 记"**这一形式的跨 N 混合在这个预算下不迁移**"（而不是"跨 N 思路死"），
并列出两个更重的下一步：把 W 调大 / 把 2P 局数提高 / 或者改成分阶段课程（先 2P 后 3P）。
**风险登记**：单进程、单 seed、pop 8 ⇒ 结论**只对"这一预算"负责**；不许拿它当"跨 N 不可行"的证据（那是更长臂的事）。

## 2. N6 第一臂的结果：**跑通了，但是一记空枪**（结论：`EPIRUS_XN2W` 的 2P 切片没有梯度）

**跑法**（预注册原样）：`EPIRUS_HOTSTART=1 EPIRUS_SEEDPACK=docs/artifacts/v7cmin4-31.bak EPIRUS_SEED=31 EPIRUS_XN2W=1 node tools/train-3p.mjs 100 5 8 8`
⇒ 70.7 秒跑完 ✓（单进程比预期快得多）；非空枪自检过 ✓（banner 打出 `XN混适应度 W=1 2P局=4/个体`）。
产物 `docs/artifacts/v7xn1-31.bak`（= `train-3p-out.js` 改名）。

**读数**：
- ① 主判据（2P 迁移）：**0/120 = 0%**（现役 = 0-60 ✓）⇒ 按预注册记**否证**；
- ② 3P 侧：体检读数与现役**逐字相同**（G(multi) 4.44 · G(long) 3.43 · 墙 21 · 场A 35% · 场B 0.33 · 花珠率 0.835）。

**但"否证"之前先查空枪 —— 结果它真的是空枪**：
- **产物与现役逐字节相同**（108714 字符、5693 个数值项 **0 处不同**）⇒ `train-3p` 在"提升不足"时**保留现有冠军**，
  所以这一臂**没有产出任何新候选**；① 的 0% 是**热启动点自己的读数**，不是混适应度的效果。
- **机理（三个观测一次说通）**：N6 的 2P 切片是 `T.scoreMemberN(params, OPPS, XN2G, 2, gen, i, 0)`
  ⇒ 对手是**多人池**；而我实测 **现役包对 `pickBalanced`/`pickGunSpam`/`pickAggro` 在 2P 里全是 0% 胜率**
  ⇒ **人人 ≈0 分 ⇒ 这一项是常数** ⇒ `fit' = (fit_main + W·fit_2)/(1+W)` 加常数**不改变排序**
  ⇒ 选择完全由 3P 侧驱动 ⇒ **必然回到热启动点**。

**⇒ 结论（不是"跨 N 不可行"，而是"这一实现不可用"）**：`EPIRUS_XN2W` 当前**测不到任何东西**（结构上无梯度）。
**修法（两条，都很小）**：
1. **2P 切片换对手**：从"多人池"改成 **2P 强参照**（现役 2P 冠军 `v7xfer44c13-2p`、`v7t2p-2p` + 四脚本 ——
   正是他们夜班 §N3 用的那套），这样切片才有梯度（能分出"谁在 2P 里撑得久"）；
2. **保留带内候选**：把 v1.5.147 给 `train-best` 做的"带内全落盘"移植到 `train-3p`，否则"提升不足 ⇒ 保留现有冠军"
   会把整臂的工作丢掉（本臂就是这样，连候选都没留下）。
3. 另外：`W=1` 时若切片是常数，它还会把 3P 的 fit **稀释一半**（排序不变、但 `bestFit` 的门槛变了）⇒ 修好 1 之后要重新标定 W。

**因此本臂在预注册里的"否证分支"应当按这条修正记**：不是"跨 N 混合不迁移"，而是**"这一版本的切片没有梯度、臂为空枪"**，
按仓规**不许拿它当任何结论的证据**。下一次臂：先修 1+2，再预注册（判据不变：2P H2H ≥20%）。
（新增可复用探针：`tools/probe-2p-h2h.mjs` —— 参照组复现了夜班 0-120 的同型读数 0-60、局长 7.0。）

## 3. 修好之后重跑：**梯度回来了，但"训什么对手就得到什么专家"**（v1.5.150）

**改动**（`tools/train-3p.mjs` · 门 **D116** 钉住）：
1. 2P 切片的对手 = `XN2_OPPS`（`EPIRUS_XN2REF` 逗号分隔的包路径，**默认 = 现役 2P 冠军**）—— 不再是多人池；
   `EPIRUS_XN2SCRIPTS=1` 可把多人池并回来（默认不并）；**参照读不出 ⇒ `process.exit(2)`**（拒绝静默退化）；
2. **带内候选落盘**（移植自 `train-best` 的 band-save）：hall 里每粒写 `<ARM>-band<k>.bak`，meta 自证
   `xn2w/xn2g/xn2refs`；目录可用 `EPIRUS_BAND_DIR` 指向临时目录（门 D116 因此能行为式测它、又不欠 D82 的账）。

**臂 2（单变量：只换切片对手 · `v7xn2-31` · W=1 · 100 代 · pop8 · seed31 · 70 秒）**：

| 包 | 2P 头对头 vs 现役 2P 冠军 | 2P 考卷（20 基准） | 3P 五道门 |
|---|---|---|---|
| 现役 3P 包（对照） | **0%** | — | 全过 |
| `v7xn2-31`（当选） | **95%** | **59.4%（最差基准 0%）✗** | ✗ 墙 0/局 · 珠经济未闭环 · 蓄能 0/局 |
| `v7xn2-31-band1` | **100%** | **55.5%（最差 0%）✗** | — |
| band2 / band3 / band4 | 95% / 63% / 87% | — | — |

⇒ 预注册判据（≥20%）**5/5 命中** ✓，**梯度确实回来了**（修复有效的铁证）；
**但**"头对头赢冠军 95~100%"**不等于**"是好的 2P 冠军" ✗ —— 考卷只有 55~59%、且有基准 0% 全败
⇒ 它们是**专克现役冠军的特化手**，不是广谱冠军（**考卷才是产品判据**）。

**臂 3（单变量：参照 + 多人池 · `EPIRUS_XN2SCRIPTS=1` · `v7xn3-31`）**：头对头 **0%** ✗ · 考卷 **49.75%** ✗
（3P 侧：场B 0.20 ✗ 但墙 4.35/局 ✓）⇒ **把弱对手混进来 = 把梯度稀释掉**（退回常数切片的病）。

**⇒ 结论（这轮最有用的一句话）**：**切片用哪个对手集，就训出那个对手集的专家** ✓
- 只对**单个 2P 冠军** ⇒ 特化手（H2H 漂亮、考卷挂）；
- 对**冠军 + 多人池** ⇒ 两头都不像（H2H 0%、考卷更差）；
- ⇒ 想要**广谱 2P 冠军**，切片就必须对着**产品判据自己那套基准**打（`promote-champion2p` 的 20 个基准），
  或对着"一串 2P 冠军 + 它们的克星"打 ⇒ **这是下一臂的单变量**（并把 W 从 1 往下调：3P 侧塌得太狠）。

**顺带两条方法论收获**：① **2P 槽换代必须看考卷** —— H2H 120-0 只说明"克这一个对手"（夜班 §N7 的食物链同理：
它证明的是相对次序，不是广谱性）；② **"带内候选落盘"是臂可评估的前提** —— 臂 1 之所以什么都说明不了，
就是因为候选全被丢了（当选判定只留一粒，而它恰好是热启动点）。

## 4. 臂 4：**对着考卷那 20 个基准打** —— 也不迁移 ✗（但把机理钉死了）

**动机**：既然"训什么对手就得到什么专家"，那就**直接对着产品判据（2P 考卷的 20 个基准）打**
（新增 `EPIRUS_XN2REF=exam`，单一来源 `tools/p2-baselines.mjs`，与 `promote-champion2p`/`evalChamp` 同表）。
**配方**：`EPIRUS_HOTSTART=1 EPIRUS_SEEDPACK=docs/artifacts/v7cmin4-31.bak EPIRUS_SEED=31 EPIRUS_XN2W=1 EPIRUS_XN2REF=exam
EPIRUS_ARM=v7xn4-31 node tools/train-3p.mjs 100 5 8 8`（banner 确认列出 `exam:random+…+exam:beadburst` 20 个 ✓）。

**读数**：

| 包 | 2P 头对头 vs 现役 2P 冠军 | 2P 考卷 | 3P 五道门 |
|---|---|---|---|
| `v7xn4-31`（当选） | **0%** | 40.25% ✗ | ✗ G(long) 2.88 <3（墙 19.4/局 ✓ · 场A 35% ✓ · G(multi) 3.75 ✓） |
| `v7xn4-31-band1` | **0%** | 53.75% ✗ | — |

**⇒ 四臂合起来，机理钉死了**：

| 臂 | 2P 切片的对手 | 2P 迁移 | 2P 考卷 | 结论 |
|---|---|---|---|---|
| 1 | 多人池（原实现） | 0%（**空枪**：产物=热启动逐字节） | — | 无梯度 |
| 2 | **1 个 2P 冠军** | **95~100%** ✓ | 55.5~59.4% ✗ | **有梯度，但训出特化手** |
| 3 | 冠军 + 9 个脚本 | 0% ✗ | 49.75% ✗ | 信号被平均稀释 |
| 4 | **考卷 20 基准** | 0% ✗ | 40.25~53.75% ✗ | 多样"惩罚者"给不出梯度 |

**机理（本轮最有价值的结论）**：
- **梯度要求参照"可被打裂"，而不是"多样"**：3P 血统包对**每一个**脚本基准都是 **100% 惨败**（局长 7.0 回合），
  突变体也一样惨败 ⇒ 平均值**长期贴在 0**，选择看不到差异 ✗；而对**学出来的 2P 冠军**，存在"跨过某个门槛就开始赢"的
  可攀爬路径 ✓（臂 2 就是爬上去的）。
- ⇒ **"一包吃两场"不能靠"把验收标准塞进 fit"实现** ✗ —— 验收（考卷）是**多样惩罚者的平均**，而平均化的适应度
  在这个种群规模（pop 8）/预算（100 代）下给不出可用梯度。
- ⇒ **下一步的正解形状**（预注册待写）：
  ① **两阶段课程**：阶段 1 用**单个 2P 冠军**把 2P 能力"撬"出来（70 秒就够 ✓），阶段 2 **在 2P 冠军血统上**
     再用考卷基准扩展广度（这时起点已不是 0，平均值有区分度 ✓）；
  ② 或**秩聚合**而不是均值（`fit₂ = 击败的对手**个数**占比` vs 现在的平均分 ✗ 两者在 2P 里其实等价，
     所以更可能是 ③）；③ **加大种群/预算**（pop 8 → 32+）让"多样"也能积累梯度；
  ④ 验收口径不改（考卷是产品判据 ✓），改的是**训练路径**。

**顺带**：臂 4 的 `G(long) 2.88 < 3` 是 **v1.5.145 那条"广度两个模式都判"** 首次在真臂上抓到的红 ✓ —— 说明那条新门不是摆设。

## 5. 臂 5 = **阶段 2**（预注册 · 跑前写死）：起点不再贴 0 之后，20 基准切片会不会变成有区分度的梯度？

**假说**（§4 结论的直接推论）：臂 4 的 20 基准切片之所以没梯度，是因为**起点对每个基准都是 100% 惨败**（均值贴 0）。
若起点已经"会打 2P"（臂 2 的产物 `v7xn2-31`：对现役 2P 冠军 95%），则**至少有一部分基准能被逐个攻下** ⇒ 均值有区分度 ⇒ 课程制成立。

**配方**（**单变量**：只把热启动点从 3P 冠军换成臂 2 的产物；其余与臂 4 逐字相同）：
```
EPIRUS_HOTSTART=1 EPIRUS_SEEDPACK=docs/artifacts/v7xn2-31.bak EPIRUS_SEED=31 \
EPIRUS_XN2W=1 EPIRUS_XN2REF=exam EPIRUS_ARM=v7xn5-31 node tools/train-3p.mjs 100 5 8 8
```
产物：`docs/artifacts/v7xn5-31.bak` + `-band*.bak`（band-save）。

**判据**：
- ① **主判据（课程制成立与否）**：当选者或任一 band 的 **2P 考卷「过门=是」**（= 没有基准 ≤50%）⇒
  "阶段 1 撬开 + 阶段 2 扩展"这条路可行 ✓；若最好的一粒仍 **<60% 或最差基准 0%** ⇒ 课程制**也不成立** ✗。
- ② 与臂 2/臂 4 对照：2P 头对头 vs 现役 2P 冠军（臂 2 = 95%、臂 4 = 0%）。
- ③ 3P 侧只记录（v1.5.145 两模式门会说话）。
**否证分支**：① 不达 ⇒ 记"**在一个'过拟合单参照'的起点上扩广度同样不行**" ⇒ 那时候的正解只剩
"换训练器"（用 `train-best` 那套 fit = 考卷本身 × 2P 专用训练场）或"加大种群/预算"，并明确交用户裁。
**风险登记**：起点是**特化手**（考卷 59%），可能"局部最优卡死" ⇒ 若 ① 不达，先看 band 谱系是否整体优于起点（有系统性上移就算"部分成立"）。

## 6. 臂 5（阶段 2）的结果：**几乎没动** ✗ ⇒ 课程制在这个预算下不成立

**读数**（73.1 秒 · 6 个带内候选 ✓ · 起步分就比臂 4 高：gen0 bestFit 0.815 vs 臂 4 的 0.744 ✓ ⇒ 起点确实"有分"了 ✓）：

| 包 | 2P 考卷 | 2P 头对头 vs 现役冠军 | 3P 侧 |
|---|---|---|---|
| **阶段 1 产物** `v7xn2-31`（= 本臂的热启动点） | **59.38% / 最差 0%** ✗ | 95% | 墙 0/局 ✗ |
| `v7xn5-31`（当选） | **59.38% / 最差 0%** ✗ | 95% | 墙 0/局 ✗（G(long) 4.19 ✓） |
| `v7xn5-31-band1` | **59.38% / 最差 0%** ✗ | 95% | — |
| `v7xn5-31-band2` | 56.19% / 0% ✗ | — | — |

⇒ **预注册的否证分支成立**：在"过拟合单参照"的起点上扩广度**同样不行** ✗ —— 考卷分与头对头**逐位不动**，
band 谱系也没有系统性上移（59.38 → 56.19，反而更低）⇒ 不是"局部最优卡一点"，是**这套组合（3P 训练器 + 2P 切片 + pop 8/100 代）根本推不动考卷**。

**⇒ 五臂合起来的最终结论（交给用户裁）**：
1. **便宜的路只治"能不能打 2P"，治不了"广不广"**：70 秒能让一个 3P 血统包对现役 2P 冠军拿 95~100%（臂 2 ✓），
   但它的考卷永远停在 55~59%、必有一个基准 0% 全败 ⇒ **当不了 2P 槽冠军** ✗。
2. **把验收塞进 fit 的两条做法都失败**（臂 4 直接塞 ✗、臂 5 课程制塞 ✗）。
3. **夜班已经证明过"正确的做法"**：`tools/train-best.mjs`（2P 专用训练器，它的 fit **本身就是考卷** `evalChamp`）
   从 `tgt-44` 热启动 ⇒ 产出 **c13（考卷 99% / 最差 82.5%）** ✓✓ —— 那才是广谱 2P 冠军的产地。
4. ⇒ **我能给的最省时路线（用现有工具，不改判据）**：
   **阶段 1（便宜、70 秒）**：用 3P 训练器 + 2P 强参照切片，把 3P 血统"撬"成会打 2P 的血统（`v7xn2-31`）；
   **阶段 2（正确的训练器）**：把这条血统当 **`train-best` 的热启动种子**（而不是拿 3P 训练器继续硬推），
   让 fit = 考卷本身去扩广度 —— 与夜班造 c13 的唯一差别就是**种子**更好、起点更高（臂 2 的 95% vs tgt-44 的 0%）。
   ⇒ 这一臂值得跑（`train-best` 一臂 ≈ 8 分钟，3 个 seed 半小时级），**要用户点头**（它是"再花算力换一个 2P 槽候选"的决定）。

## 7. 口径的第四层：**装配**（用户追问"ε=0 下防御 0% 也不是很对吧"查出来的，v1.5.151）

**问**（用户）：ε=0 的"防御类 0.0%"不对吧？4.8% 倒还行。
**查**：分两步 —— ① 先怀疑量具；② 再怀疑装配。**两个都中了**。

**① 量具确实错**：`tools/log-behavior.mjs`（真机栏）的防御类用的是**手写正则**
`/防御|反弹|八卦阵|原型制御|金钟|镜面/` ⇒ 匹配不上真卡名「**金刚盾**」、漏了「无极变速/藤甲/全息屏障」
（同为 `CAT.DEFENSE`）、还列了不存在的「镜面」⇒ 真机栏长期**漏数**。
改成从 `js/core/rules.js` 取表（单一来源）。**修后真机 = 防御类 27.20/局 = 占 AI 出手 24.8%**（原 23.0%）。

**② 装配比 ε 更大**：同一包 `v7cmin4-31`、同一 **ε=0**、长程 20 局 ——

| 装配 | ε | 防御类 | **电磁炮/局** | 蓄能/局 | 最大ep | 局长 |
|---|---|---|---|---|---|---|
| 镜像（5 席同包 · **门禁/体检用的就是它**） | 0 | 0.0% | **4.30** | 5.05 | 76 | 46.1 |
| 真桌（1 冠 + 4 脚本） | 0 | 0.0% | **0.10** | 0.20 | 4 | 27.3 |
| 镜像 | 0.2 | 4.8% | 1.00 | 1.60 | 8 | 23.1 |
| 真桌 | 0.2 | 6.1% | **0.20** | 0.30 | 3 | 29.5 |
| **真机**（用户实机 5 局 AI 席） | — | **24.8%** | **0.20** | 0.40 | — | 27.4 |

⇒ **装配单独值 43 倍**（4.30 → 0.10），**ε 单独只值 2 倍**（0.10 → 0.20）。
⇒ 千问 §23 的"同一包两套口径差 4 倍"**把 ε 与装配混在一起算了** ✗ —— 更正为：**装配是第一因素**。
⇒ **"真桌 + ε=0.2"与真机几乎重合**（电磁炮 0.20 vs 0.20、蓄能 0.30 vs 0.40）⇒ 最接近产品的模拟
   **不是门禁那套镜像，而是真桌装配 + 浏览器 ε** ✓。
⇒ 用户的两个直觉都对：ε=0 的 0% 是**镜像/真桌都成立的"ε=0 事实"**，但它**不是产品事实** ✓；
   而真机 **24.8%** 才是玩家看到的（比浏览器镜像的 4.8% 高 5 倍）。

**③ 撤回"ε 按人数分档"提案**（我自己 09-22 提的，如实入账）：
它依据的是**镜像装配**里量的"ε 代价"（电磁炮 4.30→1.00）✗；在真桌装配里 ε=0.2 反而**提高**电磁炮（0.10→0.20）
⇒ 降 ε 会让产品侧贵卡**更少** ⇒ **不做** ✓。真机防御高的最可能原因是"**真人席在场改变了威胁结构**"
（被打就防 ⇒ 与用户原话"看到对手攒到 5 ji 就防一下"一致），不是 ε。

**工具/门**：`behavior-profile.mjs` 新增 `--field=self,pool`（一次并报多装配；默认口径不变）；门 **D117** 钉住
（防御类必须从规则表取、取不到必须响、必须能并报多装配、默认口径不许改）。
**下一步的建议**：把"**真桌 ε=0.2**"当作**产品代理栏**加进验收（它已被证明与真机重合），
而门禁继续用镜像 ε=0（可比性）；两者**都报**，不再混读。


## 8. 臂 6（阶段 2 · `train-best` 收口）—— 预注册（跑前写死）

**假说**：§6 证明"3P 训练器 + 切片"只能训出**特化手**（考卷 59%）；而 `train-best` 的 fit **本身就是 2P 考卷**
（`evalChamp`）⇒ 若把种子从夜班的 `tgt-44`（2P H2H **0%**）换成臂 2 的产物 `v7xn2-31`（2P H2H **95%**），
它应该能收敛到**过门**（没有任何基准 ≤50%）的 2P 冠军 —— 这也是"两阶段课程"的正式检验。

**配方**（换槽法：`train-best` 从磁盘槽读种子；跑完无条件还原 + sha256 校验）：
```
种子 = docs/artifacts/v7xn2-31.bak（臂 2 产物 · 对现役 2P 冠军 95%）
node tools/train-best.mjs 3 500        # 与夜班造 c13 同规模（3 候选 × 500 代）
```
产物：`docs/artifacts/v7xn6-2p.bak`（从槽里取走后再还原槽）。

**判据**：
- ① **主判据（产品口径）**：2P 考卷 **过门 = 是**（= 20 个基准无一 ≤50%）⇒ 课程制/好种子成立 ✓；
- ② 与夜班 c13 对照：平均分（c13 = 99.00% / 最差 82.50%）· 与 c13 的 2P 头对头（用 `tools/probe-2p-h2h.mjs`）；
- ③ 2P 镜像"种类"（c13 = 6）—— 判它是不是又强又宽。
**否证分支**：① 不达（仍有基准 ≤50%）⇒ 记"**好种子不能越过那道结构性的洞**" ⇒ 2P 槽路线的下一步要换维度
（例如：把考卷基准做成**课程**而不是一次性平均，或直接对那道必败基准做专项训练）。
**风险登记**：换槽期间（约 8 分钟）磁盘上的 2P 槽是种子包 ⇒ 老用户此时刷新页面会拿到它（localStorage 优先，概率低）；
跑完以 sha256 校验还原。

### §8 结果（**预注册判据全中**）：两阶段课程成立 ✓

换槽法跑 `node tools/train-best.mjs 3 500`（种子 = `v7xn2-31`）。跑完**槽按 sha256 逐字节还原**（`39ee2b754bef77b5` ✓）。
训练期读数（28 对 × 20 局）：

| 候选 | 代数/耗时 | avg wr | wall | defend | 其它 |
|---|---|---|---|---|---|
| 0 = **种子**（不训练） | — | **59%** | **20%** | **50%** | — |
| **1（当选）** | 500 代 / 113.8s | **98%** | **100%** | **100%** | hill05 3.71 · 种类 4 |
| 2 | 500 代 / 160.9s | 89% | 100% | 100% | hill05 1.72 · 种类 3 |
| 3 | 500 代 / 113.9s | 94% | 100% | 100% | **退化闸剔除**（种类=0 · v1.5.94 同族）|

**主判据（2P 考卷）**：`平均 97.75% · 最差基准 70.00% · 过门 = 是` ✓✓
（种子同口径：`59.38% / 0.00% / 否` ✗ ⇒ **那个 0% 的结构性洞被补上了**）
**② 与现役 2P 冠军 c13 头对头（120 镜像局）**：**120-0 = 100%** ✓（局长 6.7）
**③ 与 c13 考卷对照**：97.75/70.00 vs 99.00/82.50 ⇒ 对**脚本基准**略弱 ✗，但对**学出来的对手**全胜 ✓
（口径差异使然：考卷只考脚本，H2H 考的是"和 2P 冠军打"。）

**结论**：
- **"两阶段课程"成立** ✓：阶段 1（3P 训练器 + 2P 强参照切片 · 70 秒）负责"**撬开能不能打 2P**"；
  阶段 2（`train-best` · fit=考卷本身）负责"**补广度**"。单靠任一步都到不了 `过门 = 是`（臂 2 = 59%/0% ✗、
  臂 4/5 直接对着 20 基准打 = 40~53% ✗）。
- 产物：`docs/artifacts/v7xn6-2p.bak`（另：`docs/artifacts/tb1-band1.bak` = train-best 的带内留档）。
- **未 promote**（按仓规：切 2P 槽要用户点头）⇒ 现役 2P 槽仍是 c13。**候选比 c13 强在哪**：H2H 120-0；
  **弱在哪**：考卷平均 97.75% < 99.00%、最差基准 70% < 82.5% ⇒ 换不换是**用户裁定**（这是"平均略降、
  头对头全胜"的典型权衡，与 v1.5.148 换 c13 那次的权衡同类）。
- 顺带证实：`vetoDegenerate`（夜班补的退化闸）**当场拦住一个 500 代的退化候选** ✓；
  `train-best` 的带内落盘（v1.5.147）让我们**能看到没当选的候选**（否则这次只有一个数）。

## 9. 臂 7（跨 N 适应度 · 起点换成"过门的 2P 包"）—— 预注册（跑前写死）

**为什么现在才问得动这个问题**：臂 3/4/5 的 2P 切片都从"2P 全灭"起步（0% 胜率）⇒ 切片是常数/无梯度 ✗。
现在有了**过 2P 考卷门**的 `v7xn6-2p`（平均 97.75% / 最差基准 70% / H2H 对 c13 120-0）⇒ 切片终于"有话可说" ✓。

**问题**：把 2P 切片（考卷 20 基准）加进 3P（N=5）的 fit，能不能既**留住 2P**（考卷仍过门）又**找回 3P**（五道门）？

**配方**（两臂，单变量 = W；其余逐字相同）：
```
EPIRUS_HOTSTART=1 EPIRUS_SEEDPACK=docs/artifacts/v7xn6-2p.bak EPIRUS_SEED=31 \
EPIRUS_XN2W=<1 | 0.25> EPIRUS_XN2REF=exam EPIRUS_ARM=v7xn7a-31|v7xn7b-31 \
node tools/train-3p.mjs 100 5 8 8
```
产物：`docs/artifacts/v7xn7{a,b}-31.bak` + 各自 `-band*.bak`。**不 promote。**

**判据**：
- ① **跨 N 成立**（主判据）：当选者或 band 里存在一粒 **同时**满足 —— 2P 考卷 `过门 = 是` **且** 3P 五道门全过
  （或至少"只挂一道且明显优于起点"）。这是本仓第一次可能出现"一包吃两场"的候选。
- ② 两边各自不许塌：2P 平均 ≥ 90%（种子 97.75%）；3P 侧记录 G(multi)/G(long)/墙/场A/场B。
- ③ 与两个现役冠军对照：H2H vs c13（2P 槽）· `--dry` vs `v7cmin4-31`（3P 槽）的记录值。
**预测（如实写）**：起点 `v7xn6-2p` 是**2P 专家**（它出自 3P 冠军 → 臂 2 → `train-best`，3P 侧在臂 2 就已塌：墙 0/局）
⇒ 本臂实际在问"**一个 2P 专家能不能在 N=5 的压力下把 3P 侧练回来、同时不丢 2P**"。
**否证分支**：① 不成立 ⇒ 记"**跨 N 混适应度在 pop8/100 代下仍只是单向的**"（要么丢 2P、要么丢 3P），
并给出下一步：加预算（pop 32+）或做**交替课程**（3P 一臂 ↔ 2P 一臂，交替热启动 —— 本质上就是 v7xn6 的两阶段反过来再来一轮）。

### §9 结果：**跨 N 是"两败"** ✗ —— 并暴露出真机制：**每阶段都在忘掉上一场**

两臂各 100 代 / N=5 / pop8 / seed31，起点 = **过 2P 考卷门**的 `v7xn6-2p`（97.75% / 最差 70% ✓），
2P 切片 = 考卷 20 基准（`EPIRUS_XN2REF=exam`），单变量 = W：

| 候选 | 2P 考卷 平均/最差 | 3P 五道门 | 3P 代理栏（电磁炮/蓄能） | 训练期 3P 1st |
|---|---|---|---|---|
| **种子 `v7xn6-2p`** | **97.75% / 70.00% ✓ 过门** | ✗（3P 侧 gen0 `1st=0%`）| — | **0%** |
| `v7xn7a-31`（W=1） | 63.88% / 0.00% ✗ | ✗ **G 1.23 · G(long) 1.29 < 3**（座位 8.6pt ✓ · 墙 17.95 ✓ · 场A 29% ✓ · 场B 3/局 ✓）| **0.00 / 0.10** ✗ | 45.4% |
| `v7xn7b-31`（W=0.25） | 68.12% / 0.00% ✗ | ✗ G 2.97 · G(long) 2.32 < 3（座位 4.2pt ✓ · 墙 14 ✓ · 场A 35% ✓）| **0.00 / 0.00** ✗ | 38.5% |

（band1 同向：a-band1 71.75%/10%、b-band1 68.12%/0% —— 都不合格。）

**⇒ 判据 ①（跨 N 成立）不达**：没有任何一粒同时满足"2P 过门 + 3P 五道门"；而且比"单向"更糟 —— **两边都退**。

**⇒ 真机制（本轮最有价值的一条）：这不是"混合失败"，是"遗忘"** ——
把三个阶段连起来看：
1. 阶段 1（3P 训练器 + 2P 切片：臂 2）**练会 2P，忘掉 3P**（墙 0/局、gen0 3P 1st=0%）；
2. 阶段 2（`train-best`：臂 6）在 2P 上收口成功（97.75%/70% ✓），但**没有把 3P 找回来**（3P 侧仍全灭）；
3. 阶段 3（本臂：3P 的压力 + 2P 切片）**部分找回 3P（1st 0%→45%）却把 2P 忘掉**（97.75%→64~68%）
   而且**广度塌了**（G 1.23~2.97 < 3 —— v1.5.145 那条"两个模式都判"的门第二次在真臂上抓红 ✓）。
⇒ **每一阶段都在牺牲上一场的能力**，直到"哪一场都不像"。

**下一步的正解形状（按代价排序）**：
1. **排练（rehearsal）而不是轮替**：同一臂里同时保留**足够权重的**两场（本臂 W=1 时 2P 只占 fit 的一半、
   且每代只有 4 局 2P vs 8 局 5P ⇒ 5P 梯度仍主导搜索）⇒ 提高 2P 局数（`EPIRUS_XN2G=8~16`）比调 W 更对症；
2. **加大预算**：pop 8 → 32+（100 代在双目标下显然不够）；
3. **冻结/分区**：只让"决策头"动、护住已学会的部分（改动最大，但这是治"遗忘"的正规药 ✗ 本臂没有这个机制）。

**给产品线的近期结论**：**别指望"一包吃两场"** —— 现阶段两条产线要分开：
2P 槽用 `train-best`（考卷=fit）喂 2P 种子（`v7xn6-2p` 已过门 ✓，可作为第三包/下一个种子）；
3P 槽继续用 `v7cmin4-31`（五道全过 ✓）—— 两者**不混训**。

**记账**：产物 `v7xn7a-31.bak` / `v7xn7b-31.bak` + 各自 `-band*.bak`（全部 gitignored + D82 点名）；**未 promote**。
⚠️ 测量陷阱（如实记）：我第一版评估脚本在**同一进程里连换 5 个候选**读考卷 ⇒ `promote-champion2p` 的
"槽字节变了"自检误判、且 2P 外壳的包（`v7xn6-2p` 是 `window.EPIRUS_CHAMPION`）被只认 `_3P` 的正则漏掉 ⇒
读出一条**假的 99.00%（其实是还原后的 c13）**。改为"每候选一次独立调用 + 每轮还原"后重测得到上表 ✗✓。
