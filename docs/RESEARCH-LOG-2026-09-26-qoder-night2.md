# Qoder · 09-26 夜 → 09-27 早（夜班二 · 目标：**能不能训出"有策略性"的冠军**）

> 用户 20:5x 授权原文摘要：「那你就直接试一下吧，现在也很晚了。反正你今天也没有什么变动，就继续在这条分支上做探索好了，
> **看一下能不能做出有策略性的冠军**，不用开新分支。**做到明天早上九点结束**。你可以完全使用性能跑长训练」
> +「也不用局限于这几个。必要的时候也可以做一些新的修改和探索改动」
> 规矩（照旧）：只在本分支 `qoder-explore-0926` 上干活；**引擎规则/两槽/`results/*` 一字不动**；判据/换槽归用户裁；
> 探针只读；改 `evo.js`（适应度侧）**不改指纹**（`FINGERPRINT_FILES = rules/resolve/state/play + policy.js`），
> 而改 `policy.js` = 换代 ⇒ 本夜班**刻意不动 policy.js**。时间戳一律 `date` 实测。

## 先定北极星（否则一整晚就是在优化一个没定义的词）
"有策略性" = **同一只包在不同环境下的表现落差小，且最差环境不塌**，具体三个可复跑的量：
1. **脆弱性极差** `spread = max_r 胜率(r) − min_r 胜率(r)`，r 跑 19 个环境块（10 个训练池内 + 9 个**池外 held-out**）；
2. **最差环境胜率** `worst-regime win%`（比极差更抗刷：它只看地板）；
3. **G4 最克 %**（门禁真源，已经有绝对数）+ **珠经济闭环**（花珠率）——后者是"愿意为贵卡攒钱"这条策略性的物证。
⚠️ 刻意**不把 A 考卷当北极星**：档案里它连续两轮与稳健性反相关（最高的粒往往最脆）。

## 预注册的判停规则（写死，免得明天早上我给结论挪门）
- **P1 值不值得追条件性**：oracle 上界 `headroom = mean_r max_p fit − max_p mean_r fit`。
  **headroom < 0.05**（fit 量纲约 0~1.8，噪声底见 Q1 表的种子 sd）⇒ 条件性在这个地形里不值钱 ⇒ **今晚转去做地形（防御成本/穿防收益），不再碰聚合**。
- **P2 改聚合会不会换人**：`τ(mean-agg, min-agg)`（Kendall）。
  **τ ≥ 0.9** ⇒ 换聚合等于没换 ⇒ 放弃 L2，转 L3 或地形。
  **τ < 0.7 且 min-agg 的首选在"池外 held-out 最差环境"上真的更好** ⇒ 值得把 `EPIRUS_FIT_AGG=min` 做成可开关注入训练。
- **P3 可逆性门票**：任何适应度改动默认关，且**同种子同预算 A/A 必须权重逐位相同**（D162 形状）；做不到就不配上臂。
- **P4 上线判据不变**：产物一律走 ε=0 五道门 + G4/G5 + 体质栏 + 防御质量栏 + `eval-5p`；**我不换槽、不跑非 `--dry` 的 promote**。

## 今晚的动作序列
> 【01:54 回看】序列本身是对的，但有两处**实际走向不同**，按实读改在这里（不改上面的原文，避免留下"我当时怎么想的"的痕迹被覆盖）：
> ① E49 的 (a)/(b) 分叉**两支都做了**：先按 (a) 建成开关并跑双臂（§E50/§E51/§E53），又按 (b) 跑了地形反事实（§E54/§E54b）；
> ② 成本假设翻案（矩阵 79 秒而不是几小时）⇒ 档案级扫描从"明天再说"变成今晚第一件事（§E49）。
> 另外 P2 里预注册的 `min` 聚合被数据否掉、换成 `worst-half/ES`（§E48 结论 4），这条是**按预注册自己砍掉的**。
1. **E48（在做）**`tools/probe-regime-fitness.mjs`：真源 `scoreMemberN` 逐环境量 fit/胜率 ⇒ 一次给出 Q1 脆弱性表、Q2 换聚合的 τ、Q3 oracle 上界。
   成本：8 包 × 19 环境 × 5 种子 × 12 局 ≈ 单次几分钟；**只读**。
2. **E49** 按 P1/P2 的结果二选一：
   (a) 值得 ⇒ 在 `evo.js` 加 `EPIRUS_FIT_AGG`（`mean` 默认 / `min` / `mix:mean+λ·min`）+ 分层局数（每个环境块 ≥3 局，配对种子 CRN），过 P3 门票；
   (b) 不值得 ⇒ 转去做**地形反事实**（E23 那条：防御/反伤收费的内存补丁版），量"条件性收益是否被免费防御压平"。
3. **E50** 长训练双臂（mean vs min，必要时 +L3 乘子三臂）：≥12 对 × 800~1500 代，全速并发（`EPIRUS_T3P_OUT` + `EPIRUS_BAND_DIR`，带内候选这次必留）。
4. **E51** 终评：三个北极星量 + 五道门 + 考卷 + 体质 + 防御质量；并且把**带内池**也量一遍（今天证明过：当选层看不出东西时，落选层才有权）。
5. 每个结论都写进本文件，DONE/DOING/NEXT 保持最新；全量门禁绿了才提交。

## DONE

### §E48 23:43~23:44 · regime 适应度矩阵（真源 `scoreMemberN` · 8 包 × 19 环境 × 5 种子 × 12 局 = 4560 局 / **实测 79 秒**）
量具：`tools/probe-regime-fitness.mjs`（只读；每个"环境块"= 对手池里只放一种脚本，其余与训练同参）。
输出 `C:\Users\14765\AppData\Local\Temp\kr3\e48\regime.txt`（14KB，8 包全量 + Q1/Q2/Q3）。

1. **没有一粒是全才**：面板 8 粒（含现役）跨环境胜率极差 **92~98pt**。现役 `bundled-champion-3p`：平均胜率 **36.8%**、最好环境 `farmer` **100%**、最差环境 `gunspam` **2%**。
   ⇒ "没有策略性"从形容词变成一个数。
2. **地板是同一种原型**：8 粒里 **7 粒**的最差环境都是 `gunspam`（2~8%），`promis`/`aimdef`/`focus`/`adaptive`/`strong` 也压在 3~27%；反过来 `mine`/`bigt`/`farmer` 一律 77~100%。
   ⇒ 与今天下午 §E39 **独立对上**：那边 10/12 粒臂产物的"最克脚本"集中在「只枪(1ジ压制)」。**两次测量、同一个洞** ⇒ 档案的通病不是"不会切换"，而是"有一条共同的地板"。
3. **换聚合会换人**（不是空操作）：`τ(mean-agg, min-agg) = 0.500`（仅池内 `0.429`）；`τ(池内均值, 池外最差) = −0.071`
   ⇒ **训练现在看见的东西，与"没见过地形上的地板"几乎不相关**。P2 的"τ≥0.9 ⇒ 放弃"这一支**没触发**。
4. **但 `min` 这个统计量本身是错的 ⇒ 我按预注册否掉自己提的那版**。只用池内 10 格选人、判在池外 9 格（= 训练能看见的 ⇒ 泛化）：
   `mean` / **`min`** / `median` 三选一选到**同一粒**（`E39-ctl-911`），regret 同为 **0.135 fit / 11.8pt 胜率**；
   换成 **`p25` / `worst-half-mean` / `mean−1·sd`** 才换到 `v7t1-31`，regret 降到 **0.113 / 9.2pt**（V1 全 19 折同向：`median` 0.089 最好，`min` 0.124 最差）。
   ⇒ **方向（稳健统计量）活着，我预注册的那个具体实现（min）死了**：min over 噪声格 = 把噪声当信号。若明天要上 `EPIRUS_FIT_AGG`，配的是 **worst-half / p25**，不是 min。
5. **条件性的价值今天没证出来**：oracle 上界 `headroom = 0.077`，而每格 fit 的种子间 sd ≈ **0.076** ⇒ **headroom = 一个噪声单位**。P1 名义上过了 0.05 线，实质是"分不出是否为 0"。
6. **成本翻案**：我原来按"这个矩阵要跑几小时"排的今晚计划 ⇒ 实测 **79 秒** ⇒ 全档案等价类级扫描是今晚负担得起的（见 NEXT）。

**自我修正两条（都当场做过）**：① 第一版 LORO 脚本把"池外折"的谓词写反了（实际跑的是 10 个**池内**折却标成"9 个池外"，`pickHist` 出现 10/9 这种不可能的数才发现）⇒ 已修正，上面引的是修正后的数；② 探针把 **fit 单位的种子 sd 乘 100 当成 pt 印**（`种子 sd 中位 7.6` 其实是 fit-sd 0.076）⇒ 量具里已改，引用时别再当百分点读。

### §E49 00:15~00:24 · 档案级 regime 扫描 Stage A（863 等价类 × 19 环境 × 4 种子 × 12 局 = 78.4 万局 ÷ 10 路并行 = **实测 12 分钟**）
量具：`tools/probe-regime-fitness.mjs --panel=archive --parts=10 --shard=i` + `tools/analyze-regime-scan.mjs`（判词）+ `tools/archive-classes.mjs`（等价类单一来源，从 `probe-pool-frontier` 里提出来共用，改前后类数一字未变：文件 1457 → 863）。

**先记我自己一处定义错误（它改写了结论，不是脚注）**：第一版把"池内 10 类"**手抄**成一份名单，里面
`promis` 这个名字**训练池里根本没有**（池里那条叫 `protomine`，两者都指向 `pickProtoMine`），而
`reflectmix / defreflectgun / guardgun` 三条**不在池里**却被我标成池内。更要紧的是
**`gunspam` 本来就在训练池里** —— 我读 `server/opp-pool.mjs` 只读到第 45 行就下了"gunspam 不在池里"的判断，
那是**推断当实测**。现在"在不在池里"改成**运行时按函数引用判**（`regime-panel.poolFromSpecs`），不再抄名字。
OPP_SPECS 的文件头自己写着"加名字漏一处踩过三次"，我还是踩了第四次。⇒ 候选 METHODOLOGY 79：**共享判据必须运行时从真源算，抄名字表迟早错位**。
后果：Stage A 的池内/池外两栏错位 ⇒ τ 只读方向，**Stage B（33 环境 + 正确判定）才是账**。

1. **"到底打不打得动"有答案了：打得动。** `gunspam` 上档案中位胜率 **8%**，但**最高 52%**、≥35% 有 **7 粒**；
   `protomine` 最高 56%、`aimdef` 最高 **81%**、`reflectmix` 最高 48%、`strong` 最高 46%。
   ⇒ **政策类装得下"抗枪/抗雷"的打法，是选择没挑它** —— 不是 L1 容量天花板，也不是地形不可越。
   今晚这条线的性质因此从"能不能做出有策略性的冠军"变成"**为什么每轮都没选到它**"。
2. **换聚合真会换人**（n=863，这次有功效）：`τ(池内mean, 池外地板) = 0.380`；
   `τ(bottom-3全环境, 池外地板) = 0.749` · `τ(mean−1sd, 池外地板) = 0.675` · `τ(p25, 池外地板) = 0.650`。
   ⇒ **稳健统计量显著更会预测"没见过的地形上的地板"**。§E48 那版给出 −0.071，方向和量级都被 n=8 毁了。
3. **条件性值钱（P1 翻案）**：`headroom = 0.201 = 噪声底(0.080) 的 2.5 倍`（仅池外 0.176 = 2.2 倍）。
   8 粒面板那版是 0.8 倍"分不出是否为 0" ⇒ 当时是**样本不够**，不是没价值。
4. **短名单（按池外地板 top 8）**：`v7t2-93`（地板 0.488 · 池外平均胜率 **48%** · 池外最差 19%）一骑绝尘，
   其后 `v9-32 / v7fix1-93 / v7new6-98 / v7big1-92 / v7f35t-93 / v7s9-93 / v7t2-31` 都在 0.44~0.47；按池外 mean 的前八名与之重合 4 粒。
   ⚠ **winner's curse 说在前面**（推断，不是实测）：863 个候选 × 每格噪声 0.080 ⇒ 纯噪声下 bottom-3 均值的最大值期望 ≈ μ+3.1σ ≈ **0.44**
   ⇒ **这份 top 名单跟"噪声能造出的最高分"几乎分不开**。所以起了 **Stage C**：12 粒短名单 + `v7l2f-106` + 现役，
   用**没碰过的种子流**（`--seed-offset=700`，12 种子 × 12 局）复量，复量还站得住的才算候选。
5. 顺手把"探针只读"这条边界写清了：`--json` 只写调用者给的路径（本轮全在 `%TEMP%/kr3/e49/`），仓库一个字节不写。

**在跑**：Stage B = 全档案 × **33 环境**（池内 23 条全部由 OPP_SPECS 函数引用算出 + 池外 10 条真·没见过）× 4 种子，ETA ~00:50；
Stage C = 上面那 14 粒的独立种子复量。两件事都跑完才谈 E50（上不上 `EPIRUS_FIT_AGG`）。

### §E50 00:22~00:29 · 训练侧开关落地 + **P3 门票实测通过**（`fit` 从"逐局求平均"改成"(1−w)·mean + w·ES(最差 q)"）
接线三处（全部按房规，**没有新增第二处读取点**）：
- `js/train/evo.js`：`FIT_TAIL_W`（出厂 0）/ `FIT_TAIL_Q`（出厂 0.25）+ 走 `setEconomyReward` + `economyReward()` 读回 + **reset 分支一起抹掉**（D77 往返喂 0.5 哨兵，漏抹就泄漏给后面的门）。
  逐局记账只在开关开着时建数组 ⇒ 关着时热路径逐字不变。`evo.js` **不在 `FINGERPRINT_FILES`** ⇒ 这根改动**不动引擎指纹**（承诺过：今晚指纹仍是 `ebdbff36`）。
- `server/econ-env.mjs`：`EPIRUS_FIT_TAIL_W` / `EPIRUS_FIT_TAIL_Q` 进 `ECON_ENV_KEYS` + `ECON_REWARD_KEYS` + `readEconEnv`，**各自只映射一个奖励键**（`train-3p` 的"env ⇒ 唯一键"派生判定才不把它当暗键；实测 `W⇒["fitTailW"]`、`Q⇒["fitTailQ"]`）。
- `tools/train-3p.mjs`：`CLI_ECON_REWARD_KEYS` 加两键 + 下达后**读回消费点**，非数值/越界/被 clamp 一律 `exit 7`（与 `REGEN_SLICE`/大雷连带同一范式）。`train-3p` 里没有 `Worker` ⇒ 是单进程，setter 就在真跑 fitness 的那份模块上（这一步不确认就会重演附录 D 臂 K 的 A/A）。

**P3 门票（预注册写的那条：做不到逐位相同就不配上臂）—— 两级都实测过了**：
- 沙箱行为级 7 条全绿：关着 ⇒ `fit`/`fitMean` 逐位相同；显式 `W=0` ⇒ 同样逐位相同（`0` 不被 `|| 默认` 吞）；
  开着 `W=0.6,q=0.25` ⇒ `fit` 从 **0.8774 降到 0.6356**（Δ=−0.242）而 **`fitMean` 一字未动**、`fitGames` 仍是 8 ⇒ **只换聚合，不多花一局**；`q=1` ⇒ 回到平均（判的是形状不只是"数值动了"）；`reset` 后无残留。
- 臂级真跑（2 代 × 3 粒，同种子）：不设 vs 显式 `0` ⇒ **产物权重逐位相同** ✅；`0.4` ⇒ 权重不同 ✅；`abc` 与 `5` ⇒ `exit 7` ✅。
- ⚠ **一次自我修正（假通过，写进 D165 的注释里）**：我第一版比较函数写成 `p.p || p.weights || p.w1 || 形状签名`，
  而产物里权重数组的真字段叫 `a` ⇒ 它退化成"数组有几个"的签名，于是对 `W=0.4` 那臂也报了"权重逐位相同"的 **PASS**。
  真信号是从"数组字段差异: vs.4:a"那行看出来 ⇒ 比较必须落到**数组本体**。**推断当实测的第 N 次，这次是我自己的判据脚本。**
- 新增门禁 **D165**（`tools/np-test.mjs`：沙箱行为级 17 条 + 臂上真跑 11 条 = 28 条断言，总数待全量门禁实测确认），与全量门禁一起在跑。

### §E49b 00:20~00:31 · winner's curse 复量（**没碰过的种子流**：`--seed-offset=700` · 12 种子 × 12 局 × 33 环境）
> ⚠ **口径标注更正（01:54 自查）**：本节表里的"池外平均胜率/地板"两列用的是 **Stage A 那 12 类"额外原型"列**
> （其中 `gunspam`、`protomine` 其实**就在训练池里** —— 见上面那条名字事故），严格叫法应是**"额外 12 类尺"**。
> 但候选与现役是**同一把尺、同一条种子流** ⇒ `44% vs 27.2%`、`19% vs 3%` 这两个对比本身不受影响。
> 权威的"池内 23 / 池外 10"（按函数引用判）用于 §E51-final/§E52/§E53，那里的"池外地板"才配这个名字。
筛出来的 12 粒 + 池内 mean 的首选 `v7l2f-106` + 现役，换一条独立种子流量回来。地板 = 池外最差 3 格均值：

| 粒 | 筛出时(4 种子) | 复量(12 新种子) | Δ | 池外平均胜率 |
|---|---|---|---|---|
| `v7t2-93` | 0.455 | **0.473** | +0.018 | **44%** |
| `v7t2-31` | 0.440 | 0.475 | +0.034 | 42% |
| `v9-32` | 0.409 | 0.445 | +0.036 | 35% |
| `v7f35t-93` | 0.415 | 0.418 | +0.003 | 36% |
| `v7s9-93` | 0.430 | 0.429 | −0.001 | 36% |
| `v7fix1-93` | 0.460 | 0.434 | −0.026 | 38% |
| `v7new6-98` | 0.434 | 0.404 | −0.030 | 35% |
| `v7big1-92` | 0.470 | 0.404 | **−0.066** | 28% |
| `v7l2f-106`（池内 mean 的首选） | 0.387 | 0.397 | +0.010 | 30% |

1. **名单不是抽奖抽出来的**：13 个复量值对 13 个筛出值的 Δ 落在 −0.066~+0.047（中位 ≈ +0.003，噪声底 0.080 ⇒ 全在 1σ 内），
   **排序基本保持**；只有 `v7big1-92` 掉了一格（它筛出时是第 2，复量后第 8）⇒ 那一粒的分数确实带了噪声。**头部两粒反而更高**。
2. **与现役同尺对照（同一批新种子、同一把 33 环境尺）**：现役 `bundled-champion-3p` 的池外逐环境胜率是
   `strong 3% · aimdef 4% · protomine 4% · adaptive 6% · gunspam 6% · beadburst 7% · focus 14% · reflectmix 13% · guardgun 19% · defreflectgun 57% · mine 94% · bigt 99%`
   ⇒ **池外平均 27.2%、地板 3%**；而 `v7t2-93` 是 **池外平均 44%、最差环境 19%**。
   也就是说：**"换地形就不会打"这件事，档案里已经有解到一半的答案，而且不是靠 G4 那把尺看到的**（今天下午三合一 = 0 粒是按 G4 反制脚本量的）。
3. ⚠ 还**不能**说"找到了可上槽的冠军"：这批候选的 **宽 G / 花珠率闭环 / G4 最克 / 五道门阻断数** 正在过（`§E49c` 交叉表），
   而且"池外地板高"只说明**在没见过的脚本上不那么脆**，不等于 5P 产品口径下也好。

### §E49c 00:35 · 那 13 粒候选过**门禁那把尺**（`probe-pool-frontier --stage=2`，G4 真源 · N4=300）
参照（同一次运行里现量）：现役 `G 4.44/3.43 · 净兑现 2.66/2.48 · 得珠 273 · 花珠率 0.835 · 最克 38/35`。

```
宽 2 · 闭环 1 · 宽∩闭环 1 · 三合一(宽∩闭环∩抗克) 0
唯一进"宽∩闭环"的 v7blk1-91：最克 99/85，杀手 =「只枪(1ジ压制·打最肥)」
候选 13 粒里 9 粒（69%）花珠率恰好为 0
```

1. **三合一仍然是 0，而且是在第三条尺上**（G4 反制脚本 → 池子前沿 → 池外 33 环境）。
   按 METHODOLOGY 77 的规矩我已经换了三层筛的对象（当选者 → 落选池 → 反向起点 → 现在是"换一把没见过的地形尺"），
   ⇒ **"档案里存在可上槽的稳健冠军"这条到此关闭**。
2. **两把尺互相印证，不是各说各话**：`v7blk1-91` 在 regime 尺上是"池外平均 40%"的那一端，
   而它对 `gunspam` 的胜率只有 8%；G4 尺给它的最克 99/85、杀手写的就是「只枪」。
   ⇒ **"池外地板"和"G4 最克"量的是同一个洞的两面**，今天下午的 `10/12 粒臂产物最克脚本=只枪` 与今晚的"7/8 粒池外最差环境=gunspam"是同一条证据的两次独立出现。
3. **稳健与"会花珠"在档案里是互斥的**：13 粒候选里 9 粒花珠率 = 0（现役 0.835）
   ⇒ 打得出没见过的地形的，多半是**不攒钱、直接压制**那一类打法；而门禁的闭环判据要的是反面。
   这条是本夜对"为什么做不出有策略性的冠军"最接近机制的回答：**目标函数要的经济闭环，和扛住压制型对手所需的即时输出，在同一个体上没有共存过。**
4. 所以 §E51 的因果检验（把 `fit` 从逐局平均换成"混尾部"）是**剩下唯一没试过的训练侧杠杆**——
   它不改判据、不换槽、不动规则，只改"选择看得见什么"。

## 中期读数（00:58~01:01，n=4 对，**方向与假设相反**）
先把这条写下来免得明早只看得到 n=14 的最终数：主终点 `池外地板(最差3格fit)` 的配对 Δ
**t4−ctl = −0.111（4 对里对照赢 4 / 0，t=−10.6）**、`t8−ctl = −0.070`；
`池内mean-fit`（中立尺，探针不开那根开关）也 **−0.132 / −0.107**；`跨环境胜率极差` **+0.023**（更散）。
⇒ 处理组的产物**在训练自己看得见的轴上就更差**，不是"看不见地板"的那种差。

### 机制（这条比数字更重要，属**推断**，但有两处可查的证据）
臂日志的最后 `bestFit`：`ctl 1.204 → t4 0.781 → t8 0.658`（seed 701，seed 702 同形：1.073 → 0.665）。
按 §E50 实测过的 `mean − ES ≈ 0.40`（现役、8 局），w=0.4 理论上只该移 ~0.16，**实际移了 0.42**
⇒ 训练中的个体 `mean − ES(最差2局)` 远大于 0.4：**8 局里"最差 2 局"基本就是运气**。
所以这两剂药不是"给目标函数加了一分尾部敏感度"，而是**把选择压力的主要成分换成了逐局噪声**，
顺带把 `fit` 的绝对尺度压掉 0.42~0.55（`fit` 夹在 −0.3~1.8，别的 15 个加项都是 0.0x~0.1 尺度）
⇒ 与 `wallReject=-5` / 同分带 / 各种**绝对阈值**那类逻辑的相对关系也一起变了。
⇒ **所以本夜能立的结论是"这个剂量形状在 8 局预算下不成立"，不是"尾部敏感度没用"**。
干净的检验需要**每人 ≥24~32 局**（尾部才有 6~8 个样本）⇒ 代价是适应度 3~4 倍 ⇒ 见 §E53（下面 NEXT 新增）。
这条也算给 L2 那页诊断补了一句：**"改聚合"不是免费的，它的真实价格 = 局数，而局数 = 代数**。

## §E51b 01:27~01:31 · 两条自检：是"当选者挑坏了"还是"整群变差"？以及"退到哪里"？
> ⚠ **本节的第 1、3 条读法在 §E51-final 里被降级**（那两组对比各自建立在 3~4 支臂上，扩到 14 对后主终点只差 −0.022）。
> 本节仍然成立的只有：带内候选这一轮**真的全部留住了**（252 粒），以及"接力训练会掉最克"这条独立复现。
同一把中立尺（offset **9000**，6 新种子 × 12 局 × 33 环境），把**热启动源（现役）**、三臂的当选者、和三臂各自的**带内落选池**（每臂 6 粒）放在一起：

| 组 | 池内 mean-fit | 池外地板（最差3格fit） | 池外平均胜率 |
|---|---|---|---|
| **现役（接力起点）** | 0.728 | 0.361 | 33% |
| ctl 当选者 ×3 | **0.791 / 0.807 / 0.801** | **0.429 / 0.398 / 0.486** | 36~40% |
| t4（W=0.4）当选者 ×3 | 0.684 / 0.585 / 0.738 | 0.348 / 0.273 / 0.346 | 21~31% |
| t8（W=0.8）当选者 ×3 | 0.797 / 0.536 / 0.690 | 0.370 / 0.306 / 0.399 | 26~33% |

带内落选池（9 臂 × 6 粒 = 54 个读数）的分组摘要：

| 组 | 池内最好粒 | 池外地板 组内最好粒 | 池外地板 池中位粒 |
|---|---|---|---|
| ctl | 0.826 | 0.499 | 0.392 |
| t4 | 0.713 | 0.363 | 0.276 |
| t8 | 0.749 | 0.384 | 0.344 |

三条读法（都有数，不是评论）：
1. **不是"当选者挑坏了"的管道问题**：处理臂**整批带内池**都低一截（组内最好粒 0.499 → 0.363）⇒ 是搜索本身去了更差的地方。
   （这条正是 METHODOLOGY 77 那句"落选层才有权"反过来用：先看群体，再怪选择。）
2. **对照组是真有进步的**：三支 ctl 臂的池内 fit 与池外地板**全部高于热启动源**（0.79~0.81 vs 0.728；0.40~0.49 vs 0.361）
   ⇒ "接力 800 代白跑"这个常见怀疑在这里被排除，因此处理组的落后不是"臂本来就不动"。
3. **处理组退到起点之下**：t4 有 2/3 臂的池内 fit 低于现役（0.684 / 0.585 < 0.728），池外胜率 21~31% 全线低于现役 33%
   ⇒ 在 8 局/个体的预算下，`ES(最差2局)` 不是"给目标加一分尾部敏感度"，而是**把主要选择压力换成逐局运气**（与 00:58 中期读数、
   与 `bestFit 1.204→0.781→0.658` 的尺度漂移两处独立对上）。
⇒ 于是 **§E53（同预算 32 局/个体 ⇒ 尾部有 8 个样本）才是对"尾部敏感度"这个想法本身的判决**；§E51 判掉的是"在 8 局预算上直接换聚合"这个做法。

## §E53 01:16~01:41 · **给尾部统计量足够样本量**再判一次（同预算 32 局/个体 ⇒ ES 取最差 8 局）
设计：6 对 × 2 臂（ctl / `W=0.5`），`node tools/train-3p.mjs 500 3 32 12`，全部热启动自现役；
终评尺 = **又一条新种子流**（offset 6000，10 种子 × 12 局 × 33 环境）+ G4 真源（`--g4-scope=all`）。12 臂全 `exit=0`。

| 配对 Δ（t5 − ctl，n=6 对） | 均值 ± SE | t | 符号 |
|---|---|---|---|
| **池外地板（最差3格fit）= 主终点** | **−0.043 ± 0.024** | −1.80 | 处理赢 2 / 对照赢 4 |
| 池外平均胜率 | −0.052 ± 0.013 | −4.01 | **0 / 6** |
| 池内 mean-fit（训练自己看得见的） | −0.045 ± 0.016 | −2.87 | **0 / 6** |
| 跨环境胜率极差 | +0.015 ± 0.011 | +1.41 | 4 / 1（更散） |
| G multi（宽度） | −1.947 ± 0.984 | −1.98 | 1 / 5 |
| 最克 long（资产账） | **+8.5 ± 10.7** | 0.80 | 3 / 3（无信号） |
| 花珠率 | −0.370 ± 0.238 | −1.55 | 2 / 4 |

1. **把 §E51 的损伤主要来自"8 局的运气"这一半证实了**：同样换聚合，预算从 8 局抬到 32 局后，主终点的伤害从
   −0.111 缩到 **−0.043**（约 1/2.6），`池内` 从 −0.132 缩到 −0.045 ⇒ 前一轮里**大半是统计量没样本**。
2. **但方向没有翻**：`池外平均胜率` 与 `池内 mean-fit` 都是 **6 对全输（0/6）**，宽度 G multi 也 5/6 输
   ⇒ 尾部统计量**即使有样本，也不产生收益**；它换来的是**臂间散布爆炸**：
   处理组地板 0.280~0.480 / 最克 49~100，对照组地板 0.375~0.443 / 最克 56~87。
   最好那一粒（`E53-t5-801` 地板 0.480、最克 49/22）与最差三粒（804/805/806：地板 0.28~0.31、最克 92~100）同在一组 ⇒
   **这不叫"更稳健的选择压力"，叫"更多一粒走极端的机会"**（推断，但两组各 6 粒的极差是实测）。
3. **按预注册的判读规则**：主终点 t=−1.80 未过线 ⇒ 属规则里的 ③/负方向一侧 ——
   效应点估计为负且 6/6 对在同向，**不许写成"没测出来"**；写成"**这根杠杆在两个预算档上都没有正效应，且伴随退化风险**"。
4. 顺手第二次独立复现了那笔**资产账**：连**对照臂**的最克均值都到 `70.7 / 55.5`（现役 38/35）
   ⇒ "接力训练会把抗克资产训掉"在 500 代 × 32 局这一轮又成立一次（n=6，非单点）。
   ⇒ 任何"拿臂产物换槽"的提案，先要过这一关。

## §E51-final 01:46 · **更正我自己 00:58 的中期读数**（同一终点，n=4 → n=14 差 5 倍）
43 粒（42 臂 + 现役）全量过完中立尺（offset 3000，8 新种子 × 12 局 × 33 环境），**14 对全齐**：

| 配对 Δ | t4−ctl（n=4 中期） | **t4−ctl（n=14 最终）** | t8−ctl（n=14） |
|---|---|---|---|
| 池外地板（主终点） | −0.111 ± 0.011（t=−10.6） | **−0.022 ± 0.026（t=−0.83）** | −0.014 ± 0.026（t=−0.54） |
| 池外 mean-fit | −0.109 ± 0.045 | −0.031 ± 0.028（t=−1.09） | −0.010 ± 0.029 |
| 池外平均胜率 | −0.074 ± 0.041 | **−0.041 ± 0.020（t=−2.06）** | −0.026 ± 0.019（t=−1.38） |
| 池内 mean-fit | −0.132 ± 0.034 | −0.025 ± 0.035（t=−0.71） | −0.026 ± 0.038（t=−0.69） |
| 跨环境胜率极差 | +0.023 ± 0.007 | +0.007 ± 0.009（t=0.76） | −0.001 ± 0.007 |

1. **中期那条"处理组在训练自己看得见的轴上就更差（−0.13）"不成立**：扩到 14 对后同一数字是 −0.025 ± 0.035，
   而我在 00:58 把它写成了机制结论。**效应量差 5 倍，SE 反而被中期低估 2.4 倍**（0.011 vs 0.026）——
   因为头 4 个种子恰好同向，配对 SE 只反映"这 4 对内部一致"，不反映总体。
   ⇒ 这是 METHODOLOGY 75 的**现场二次复现**，而且这次是**负方向**的过度声明（比正方向更危险，因为它容易写进结案）。
2. §E51b 那组"3 支臂 × 带内池"的对比（ctl 最好粒 0.499 vs t4 0.363）**同样不能当效应读**：它建立在 n=3 臂上，
   而 n=14 的同一终点只差 −0.022。那一段留作"落选池有没有被丢掉"的**管道自检**（答案：留住了，252 个带内候选），
   不作为处理效应的证据。**我在 §E51b 写的三条读法里，第 1、3 条按此降级。**
3. 按预注册的三分支，最终落点是 **③"n=14 判不出正效应"**，而不是我 00:58 写的"反向"：
   主终点 95%CI ≈ [−0.078, +0.034] ⇒ **排除了"正效应 > +0.034"（约 0.7 倍单臂 SE）这一类希望，但不能宣称有害**。
   唯一勉强过线的是 `池外平均胜率`（t4 −0.041，t=−2.06，n=14）—— 这是次终点，只作描述。
4. **和 §E53（32 局预算，6 对）合起来看**：两档预算、两条独立种子流、两套配对，
   主终点分别是 −0.022（t=−0.83）与 −0.043（t=−1.80），`池外平均胜率` 分别是 −0.041（t=−2.06）与 −0.052（0/6 全输）
   ⇒ 合起来的读法只有一句：**尾部聚合没有带来任何正收益，方向持续偏负**；
   要不要把它写成"关闭"，交用户裁（我这一侧不再为它开臂）。

## §E54 01:47~01:52 · 地形反事实（P1 的 (b) 支路）：**给防御定价，地板是抬起来还是塌下去？**
新量具 `tools/probe-floor-pricing.mjs`（只读 + **内存补丁**：E23 的 `guard-cost-lib` 走 route ① 源码改写 ⇒ 内部调用点也生效；`rules.js` 一字未动 ⇒ **规则指纹不变**）。
三条自检都过了：**① `off` 档与不补丁沙箱逐字相同**（fit `0.18041868498411473` 两边一致）；
**② 不是空枪**（`L1` 摘掉菜单项 740,196 个 / 实扣 107,631 次 92,654 ep；`L2` 摘 1,145,526 个 / 扣 101,176 次 101,176 ep）；
**③ 环境块函数全部解得到**（`promis` 与池里的 `protomine` 是同一个 `pickProtoMine` ⇒ 显式对齐，不再重演 §E49 那个名字事故）。

| 包（地板组 = 5 类压制原型均值） | off | L1（第 2 次起扣 1ep） | L2（每手扣 1ep） | L2@3ep（Δ vs off） |
|---|---|---|---|---|
| **现役** | 5%（最差 3%） | 5% | 4% | −0.6pt |
| **`v7t2-93`（档案最稳健那粒）** | **34%（最差 17%）** | 28% | **19%（最差 10%）** | **−26.1pt** |
| `v7t2-31` | 26% | 25% | 18% | −21.4pt |
| `v9-32` | 26% | 19% | 18% | −11.4pt |
| `v7blk1-91` | 19% | 16% | 16% | −7.8pt |

1. **方向是反的，而且是单调的**：扣 1ep 时最稳健那粒的地板 **−15.2pt**，扣 3ep 时 **−26.1pt**；
   同时"对照组"（三类本来就会打的）几乎不掉（−1.4 ~ +1.9pt）⇒ **不是"把强度整体压下去了"，是专门压掉了它们对付压制型原型的能力**。
2. ⇒ **"只枪把所有包踩在 2~8%"这条地板，不是防御免费的产物**。至少在这一个地形杠杆上（防御的事前价格），免费防御不是原因；
   反而说明**这些"会看地形打"的粒，一部分能力正是靠近乎免费的防御买来的**。
3. ⚠️ **这条与用户报的病构成一个真实的张力**（值得进裁定件）：给防御定价是治"有钱→转防"最直接的刀，
   但它会**牺牲池外稳健性** —— 实测同方向、两个价位。想同时要"不龟"和"换地形还会打"，得另找地形量（
   本探针只试了防御的**价格**，没试**穿防收益 / 克制环 / 防御的时机价值**）。
4. 边界（不许读成"改了规则"）：补丁是"**事前把买不起的防御卡从菜单摘掉、真选了才扣**"（E23 那版的教训：事后扣是空枪），
   它衡量的是"防御占用预算"的强度信号，**不等于**规则换代后的新均衡。
   所以本节的结论止于：**"给防御定价"不能解决地板问题，且会削弱当前最好的稳健候选**。

### §E54b 01:52 · 同一条链的下一环**没接上**（用 E29 的"无效出手"尺复查我的解释）
§E54 之后最容易写的一句话是"它们稳健 = 靠便宜防御"。拿 `tools/probe-wasted-play.mjs`（产品口径 · 60 局 · 攒钱者装配）
量这五粒的防御族与防御质量，**这句话被自己的数据否掉一半**：

```
空挡率（摆防御但那段时间没人招呼它）：v7blk1-91 95.0% · 现役 95.6% · v7t2-93 96.4% · v7t2-31 96.4% · v9-32 99.7%
防御质量分（用户裁定的三档口径）：      p10 0.4 · 中位 6.1 · p90 7.6   ⇒ 五粒全在同一个带里
```

1. **"防得好"不是稳健的原因**：稳健候选与现役在防御质量/空挡率上**看不出差别**（都防得很空）。
2. **"防得多"也不是**：§E54 里被定价伤得最狠的是 `v7t2-93`（−26.1pt @3ep），而它防御回合数（n=2,726）比 `v9-32`（n=6,270）少一半。
3. ⇒ 所以 §E54 的正确读法只是一条**排除**：`地板 = 防御免费的产物` 这个解释**不成立**；
   "为什么给防御定价偏偏最伤最稳健的那几粒"——**机制还没定位到**（推断：更可能走 ep 预算的挤占路径，即防御手把买进攻卡的窗口吃掉了，
   但这一条**没有量过**，不许写进结论）。
4. 明早如果要继续追地形，需要的不是"再定价一次"，而是**能分辨那三条候选机制的量**（防御的时机价值 / 进攻卡的可负担性被挤占 / 反伤收益）。

## §E52 01:14~02:09 · 42 臂产物逐粒过**门禁那把尺**（G4 真源 · `--g4-scope=all` · 44 类 × 7 批）
参照同一次运行现量：**现役 G 4.44/3.43 · 净兑现 2.66/2.48 · 得珠 273 · 花珠率 0.835 · 最克 38/35**。

1. **三合一 = 0（第五次独立确认，这次是"训练侧"的 42 粒）**：42 臂里 **宽 3 · 闭环 6 · 宽∩闭环 0 · 三合一 0**。
   §E53 那一轮 12 臂同样 宽 3 · 闭环 4 · 宽∩闭环 1 · 三合一 0。
2. **但"抗克"这一维在臂上的命中率远高于档案**：**5/42 臂 = 12%** 做到"两模式最克都 ≤ 现役"，
   而下午档案全池是 **7/863 ≈ 0.8%** ⇒ 命中率差约 **15 倍**。
   （⚠ 这只说明"训练能长出抗克"，**不**说明它能同时长出经济 —— 见第 1 条。）
3. 臂 vs 现役逐轴（同一把中立尺 3000 · 8 新种子）：`池外地板` 更高 **22/42**、`池外平均胜率` 更高 **15/42**、
   `五轴全过`（地板↑ 胜率↑ 抗克 宽 闭环）**0 粒**。
4. ⭐ **今晚最有决定意义的一条：出现了两粒"各只差一条子判据"的近失**（以前每次都是差一整维）：

   | 臂 | G(m/l) | 净兑现(m/l) | 得珠/花珠率 | 最克(l/m) | 宽 | 闭环 | 抗克 |
   |---|---|---|---|---|---|---|---|
   | **`E51-t8-713`** | **7.58/4.24** | **3.50/3.18** | 99 / 0.828 | **32/19** | **✔** | **✗** | **✔** |
   | **`E51-t8-701`** | 4.76/4.78 | 2.44/3.36 | **189 / 0.958** | 30/**37** | ✗ | ✔ | ✗ |
   | 现役 | 4.44/3.43 | 2.66/2.48 | 273 / 0.835 | 38/35 | ✔ | ✔ | — |

   * `E51-t8-713`：**宽 ✔ + 抗克 ✔**，只栽在"闭环"的 `得珠 ≥100` 这一条上 —— 实测 **99**（花珠率 0.828 与现役 0.835 同档，
     即它**花得很干净，只是挣得少**：99 vs 现役 273 = 36%）。所以"差 1 个珠"是**贴着线**，不是"经济已经追平"。
   * `E51-t8-701`：**闭环 ✔（189 珠 / 0.958）+ G 两模式都高于现役**，只栽在 `净兑现 multi 2.44 < 2.66`（宽 ✗）与 `最克 multi 37 > 35`。
   * ⚠ **两条"近失"都在各自子判据的样本散布之内**（`selfPlay` 每模式 20 局、`chargeProfile` 40 局）⇒
     0.22 的净兑现差与 1 个得珠的差**都不能当"确实只差一点"**；要把近失变成结论，必须**加局数复量这两粒**（明天第一件事，成本低）。
5. 那笔**资产账在臂上第三次复现**：三组的最克均值 `ctl 66.3 / t4 61.5 / t8 60.4`（现役 38）⇒ **接力 800 代平均把抗克性训掉约 25pt**；
   处理组比对照低 5~6pt，但配对 t 只有 −1.04 / −0.73（CI ±14pt）⇒ **方向一致、不足以定论**（这是唯一一轴处理组看起来"有利"的量，
   要判它必须专门加对数）。

## §E55 02:11~02:14 · 两粒近失**翻四倍样本复量**，顺带量出一条门禁的软肋
`--stage=2 --g4-scope=all --games=80`（`selfPlay` 每模式 20→80 局；`chargeProfile` 的得珠口径固定 40 局，未变）。

| | G(m/l) | 净兑现(m/l) | 得珠/花珠率 | 最克(l/m) | 宽 | 闭环 | 抗克 |
|---|---|---|---|---|---|---|---|
| **`E51-t8-713` @20 局** | 7.58/4.24 | 3.50/3.18 | 99 / 0.828 | 32/19 | ✔ | ✗ | ✔ |
| **`E51-t8-713` @80 局** | **7.66/4.16** | **3.53/3.18** | 99 / 0.828 | 32/19 | **✔ 稳** | ✗ 仍差得珠 | **✔ 稳** |
| `E51-t8-701` @20 局 | 4.76/4.78 | 2.44/3.36 | 189 / 0.958 | 30/37 | ✗ | ✔ | ✗ |
| `E51-t8-701` @80 局 | 5.03/4.66 | **2.57**/3.21 | 189 / 0.950 | — | **✗ 仍不过**（线 2.66） | ✔ | ✗ |
| **现役 @20 局** | 4.44/3.43 | 2.66/2.48 | 273 / 0.835 | 38/35 | ✔（定义即取此值） | ✔ | — |
| **现役 @80 局** | 4.17/3.36 | **2.63**/2.42 | 273 / 0.835 | 38/35 | **✗ 自己掉到自己定的线以下** | ✔ | — |

1. **`E51-t8-713` 的近失是真的，但只"近"在一条线上**：4× 样本后 `宽` 与 `抗克` 两维**稳稳通过**（净兑现 3.53/3.18 明显高于现役同 n 的 2.63/2.42），
   唯一不过的是"闭环"里的 `得珠 ≥100`，实测 **99**。⚠ 但它的得珠绝对量只有现役的 **36%**（99 vs 273），花珠率倒是同级（0.828 vs 0.835）
   ⇒ 正确说法是"**花得很干净、挣得太少**"，不是"离现役只差一个珠"。
2. **`E51-t8-701` 的近失被复量否掉了**：净兑现 multi 从 2.44 升到 2.57，仍低于 2.66 ⇒ `宽` 稳定不过（`抗克` multi 37>35 也不过）。
3. ⭐ **顺带量出门禁的一处软肋（实测，不是评论）**：`宽` 的净兑现线 **2.66 是现役在 20 局下的读数**，
   而**同一包在 80 局下是 2.63** ⇒ 这条绝对线**自带样本量依赖**：把候选从 20 局改测 80 局，现役自己就从"过线"变成"不过线"。
   这正是裁定件第 8 条（"门禁缺'与现役相对比较'这一维"）的一个新实例：**判"不劣于现役"必须两包同 n 并排量**，
   否则换样本量就等于换判据。
4. 42 臂的两两交集统计：`宽∩抗克 = 1`（就是 `E51-t8-713`）、`闭环∩抗克 = 0`（那条 1 计是现役自己）、`三合一 = 0`。

## §E57 02:28~02:33 · `E51-t8-713` 的完整档案（同尺并排 · 只 `--dry`，什么都没写）
`promote-champion --dry` 两边都跑：**两粒都"✅ 五道全过"**，但并排读数是一笔真实的交换：

| 读数（同一工具、同 n） | `E51-t8-713` | 现役 |
|---|---|---|
| **反弹墙·主动伤害/局** | **1.00**（穿透卡命中 20） | **21.00**（穿透卡命中 364） |
| 广度 G（multi/long） | **7.58 / 4.24** | 4.44 / 3.43 |
| 最克（long/multi，越低越好） | **32 / 19** | 38 / 35 |
| 座位极差 | **9pt** | 13pt |
| 防御质量 | **8.0%** | 6.3% |
| 珠经济（40 局） | 得珠 99 · 花掉 82 · 花珠率 82.8% · 蓄能 2.48/局 | 得珠 273 · 花掉 228 · 83.5% · 蓄能 6.83/局 |
| A 考卷 1st（只诊断） | 50.5% | 54.4% |
| 中立尺（3000·8 新种子）池外地板 / 最差环境 / 平均胜率 | **0.398 / 10% / 29%** | 0.364 / **2%** / **33%** |
| 五道门 | ✅ 全过 | ✅ 全过（参照） |

1. ⭐ **它就是裁定件第 8 条的活体案例**：一粒**五道门全过**的候选，反弹墙里的主动伤害是现役的 **1/21**（1.00 vs 21.00 每局）
   —— 因为那道闸是 `pierceLand === 0` 的**二值判**，只要不是零就不拦。
   ⇒ 今天下午那四处同形问题（反弹墙二值闸 / G4 绝对线 60% / `G(long)` 只记录 / 场B 无分辨率）在今晚多了一个**具体到包名**的例子；
   加上 §E55 的"2.66 这条线在 80 局下连现役自己都过不了"，"改成**同 n 并排的'不劣于现役 + 容差'**"这一条建议现在有两条实测撑着。
2. **但它不是"更差"**：广度、抗克、座位对称、防御质量、池外地板五项都优于现役；换来的是**穿防输出几乎归零 + 经济规模只有现役 36%**；
   池外**平均**胜率还略低于现役（29% vs 33%）⇒ 形状是"**地板抬高、天花板略降、经济变薄**"。
   要不要收这种形状 = **纯裁定**，我不能替用户判（本文不给建议换槽的结论）。
3. 复跑口径（明早要复核只需三条）：
   `node tools/promote-champion.mjs --dry docs/artifacts/e50-out/E51-t8-713.bak` ·
   `node tools/probe-pool-frontier.mjs --packs=docs/artifacts/e50-out/E51-t8-713.bak --stage=2 --g4-scope=all --games=80 --rows` ·
   `node tools/probe-regime-fitness.mjs --packs=docs/artifacts/e50-out/E51-t8-713.js,js/bundled-champion-3p.js --seeds=8 --seed-offset=3000`。
   ⚠ 产物在 `docs/artifacts/e50-out/`（被 .gitignore 忽略、非 D82 扫描范围）⇒ **重启机器或清 %TEMP% 不会掉，但清仓会掉**；
   若明早要长期保留这粒，需要先经用户裁"要不要把它挪进 `docs/artifacts/` 顶层并记账（D82 点名）"。

### §E57b · 那条"最克到底被不被保护"要多少对数才判得出（预计算，免得明天拍脑袋）
E51 的配对 SE：`最克long` t4−ctl = 7.05（n=14）、t8−ctl = 8.05（n=14），点估计 −7.3 / −5.9。
要在 α≈0.05 下把"|Δ|≈6pt"判成显著，需要 `SE ≤ 3` ⇒ `n ≥ 14×(7.05/3)² ≈ 77 对`（≈154 支臂 ≈ 1.8h 算力 + 约 100min 的 G4 逐粒量）。
⇒ **这不是"再跑一晚"能顺手关掉的事**；今晚不动它，留给用户裁要不要投这笔算力（我这边不擅自开 154 支臂）。

## 收工快照（02:15 · 本轮全部结论都在这 12 行内，其余都有 §编号可查）
- **做了什么**：把"环境"变成第五把可复跑的尺（`probe-regime-fitness` + `analyze-regime-scan` + 等价类/环境块两处单一来源）；
  建了一根默认关的训练侧杠杆（`EPIRUS_FIT_TAIL_W/Q` + 门 D165）；跑了 **54 支臂**（E51 42 + E53 12）与三批终评。
- **① "没有策略性"现在是有数的事实**：档案 863 类在 33 个原型上的胜率极差普遍 92~100pt；
  地板集中在 `gunspam`(只枪)——**而它本来就在训练池里**（见过也没学会）。
- **② 天花板不在政策类**：档案里 `gunspam` 最高 52%、7 粒 ≥35% ⇒ **装得下，是选择没挑**。
- **③ 换聚合的方向被两档预算、20 对配对共同否掉**：主终点 Δ −0.022（t=−0.83，n=14）与 −0.043（t=−1.80，n=6），
  池外平均胜率两档都稳定偏负 ⇒ **不再为它开臂**。
- **④ 我自己把一次中期读数写重了**：n=4 读到 −0.111/t=−10.6，n=14 真值 −0.022/t=−0.83 ⇒ 已当场更正并写成 METHODOLOGY 81。
- **⑤ 地形这一刀砍反了**：给防御定价（内存补丁）**降低**地板，且最伤最稳健那粒 —— `v7t2-93` 地板 34%→19%（每手 1ep），扣到 3ep 再降 26.1pt；
  而"防得好 / 防得多"两条解释都被 E29 的尺否掉 ⇒ 只排除了一种解释，机制未定位（§E54/§E54b）。
- **⑥ 三合一 = 0 第五次复现**（这次是训练侧 42 臂：宽 3 · 闭环 6 · 宽∩闭环 0）；
  **但 `宽∩抗克` 第一次出粒 = `E51-t8-713`**（G 7.66/4.16 · 净兑现 3.53/3.18 · 最克 32/19 · 差在得珠 99<100，绝对量仅现役 36%）。
  ⭐ 补测（§E57）：**这粒"五道门全过"，而它在反弹墙里的主动伤害只有现役的 1/21（1.00 vs 21.00/局）**
  ⇒ 那条闸是 `pierceLand === 0` 的二值判；裁定件第 8 条从此有了一粒具名的活体案例。
- **⑦ 资产账第三次复现**：接力 800 代把最克从 38/35 训到 ctl 均值 66.3（处理组 61.5/60.4，方向有利但 t=−1.04 不足以定论）。
- **⑧ 门禁的一条绝对线被证明自带样本量依赖**：现役自己在 80 局下 `净兑现 2.63 < 线 2.66` ⇒ "与现役相对比较必须同 n 并排"。
- 状态：分支 `qoder-explore-0926` 到 **v1.5.252**（`a2e5e75` 主体 + `a38c4d5` §E57 账本 + `e96ae97` 候选请进档案并 D82 点名，三次都已 push）。
  门禁 **最后一次全量 np-test 211/211（02:40:16 实测，含 D82/D8）· spec 52/52 · smoke OK（02:24:52）· 指纹 `ebdbff36` 不变**；
  **引擎规则 / 两槽 / `results/*` 一字未动，没换槽，只跑过 `--dry`**。

## 判读规则（**在看到 §E51 数据之前写死**，免得明早我给结论挪门）
- **主终点只有一个**：`池外地板（最差 3 格 fit）` 的**配对** Δ（t4−ctl 与 t8−ctl 各一次）。
  噪声底 0.080/格 ⇒ 单臂 bottom-3 的 SE ≈ 0.046；14 对取配对差的 SE 才是分母。
- 次终点（**只做描述，不拿来宣布成立**）：`最克long`（那笔资产账）、`池外平均胜率`、`跨环境极差`、`池内mean-fit`。
  明说多重性：6 个量 × 2 个剂量 = 12 个读数 ⇒ 只要主终点不过线，任何"某个次终点看起来好"都不算发现。
- **三种结局的写法先定好**：
  ① 主终点 `|t| ≥ 2` 且方向为正 ⇒ 写成"有迹象"，**不写成结论**，且必须用**新的种子流 + 更多对**复量一次才算立住；
  ② 主终点 `|t| < 2` 但 95% CI 把 "> +0.10（1.25 噪声底）" 排除掉 ⇒ 可以写"**这根杠杆的效应小于一个噪声底**"（这才是有效的零结果）；
  ③ CI 宽到既排不掉正效应也排不掉零 ⇒ **老老实实写"n=14 判不出"**，不许把"没测出来"写成"没有效应"（METHODOLOGY 75 的原文教训）。
- 硬约束：齐臂对数 <12 ⇒ 不做判读；任何一臂缺 G4 读数 ⇒ 该指标按缺失处理并印出来，不许当 0（METHODOLOGY 78）。
- 若落在 ②/③ ⇒ 训练侧"改聚合"这条路**带因果证据关闭**，剩下的方向是地形/特征（防御 0 ep 定价、靶向特征），
  那属规则改动 ⇒ 我只写裁定件，不动手。

## NEXT（明早 09:00 之后，按优先级；每条都注明"要不要我动手"）
1. **把 `E51-t8-713` 报给用户裁**（今晚唯一"新形状"的产物）：`宽 ✔ + 抗克 ✔`（G 7.66/4.16、净兑现 3.53/3.18、最克 32/19）
   只栽在闭环的 `得珠 99 < 100`（绝对量 = 现役的 36%）。要补的是**它的 `promote --dry` 五道门阻断数 + 5P 产品口径 + 体质栏/防御质量栏**，
   然后交裁："宽+抗克、经济不足"这种形状要不要收。⚠ 不许写成"离现役只差一个珠"（§E55 第 1 条已把这句限死）。
2. **把"绝对线自带样本量依赖"并进裁定件第 8 条**（§E55 第 3 条实测）：现役自己在 80 局下 `净兑现 2.63 < 线 2.66`。
   建议动作：`宽` 的净兑现判据改成**同 n 并排"不劣于现役 + 容差"**，与反弹墙 / G4 / `G(long)` / 场B 那四处同形问题一起处置。
3. **最克这一轴值不值得再花对数**：处理组 61.5/60.4 vs 对照 66.3，方向一致但 t=−1.04、CI ±14pt ⇒
   要判"尾部聚合是否保护抗克资产"，需**以 `最克long` 为主终点**另加 ≥20 对（今晚没做：它是次终点，预注册里就不许拿它宣布）。
4. **训练侧还剩什么**：只剩"改地形 / 改特征"两条（`policy.js` 换代 = 用户裁；防御定价已被 §E54 否掉一半）。
   若继续投算力，建议投 1 的补测与 3 的定向配对，**不要再开第三种聚合变体**。
5. 交接口径：本文 §E48~§E55 + 白班那本 `RESEARCH-LOG-2026-09-26-qoder-night.md` §E35~E47；
   方法论新增 **79**（成员资格比身份不比名字）/ **80**（比较器不许兜底 + 必配"必须不同"的对照）/ **81**（中期读数不得用于效应判读）。
（旧 NEXT 已并入上面的 §E49 / §E49b 与下一条 NEXT 1-2：E49 的两问都已答 —— (a) **有**，档案里存在池外 ≥35% 的粒，
`gunspam` 最高 52%、7 粒 ≥35% ⇒ 政策类装得下，是选择没看见；(b) 交叉表正在过。）
