# 交接给千问 · 2026-09-25（DS 班次）

> 读法建议：§1 是**你现在就能接手的状态**，§2 是**今天真正测出来的东西**（有证据、可直接引用），§3 是**排队中的活**（按我建议的顺序），
> §4 是**我今天犯的错**（请别重犯，尤其 §4.1），§5 是红线与仓规。
> 所有数字都附了复现命令；凡我**收回**过的结论都明确标注"【已收回】"，不要引用它们。

---

## 1. 仓库状态（可直接接手）

| 项 | 值 |
|---|---|
| 分支 | `main` · 工作树**干净** · 已推送 `8eeebca` |
| 今天推的版本 | **v1.5.227 → v1.5.235**（9 个 commit，全部已 push） |
| 规则指纹 | **`ebdbff36`**（**全程未动**；覆盖 `js/core/rules.js|resolve.js|state.js|play.js` + `js/train/policy.js`） |
| 两槽 / results | `git diff --exit-code -- js/bundled-champion.js js/bundled-champion-3p.js results` ⇒ **未动**（未训练 publish、未 promote） |
| 门禁 | **205 门**。最后一次全绿是 `82e0c4e`（205/205）。⚠️ **v1.5.235（`8eeebca`）未再跑门禁** ⇒ **你开工第一件事：`NP_TIME=1 node tools/np-test.mjs`** |
| 门禁耗时 | 单趟 ~266 秒（改了 `tools/` 后缓存按内容失效）；**不要冷热各跑一趟**（用户明确要求过） |
| 评估工作目录 | `docs/artifacts/eval-out/`（被 `.gitignore` 的 `docs/artifacts/*-out*/` 忽略 ⇒ 不欠 D82 的账、不进 git） |

**今天新增/改动的文件**（都在 `tools/`，除文档）：
`tools/probe-g-collapse.mjs`（新）· `tools/seq-reward-lib.mjs`（新）· `tools/audit-lib.mjs`（+`landShareOf` · +`rejectUnknownFlags`）·
`tools/pick-best.mjs`（+`COLLAPSE_LINE` · 份额子句）· `tools/train-3p.mjs`（+`EPIRUS_SEQ_W` · 结账分母）·
`tools/probe-ep-reach.mjs`（+类别构成/`--noji`/`--regen`/参数守卫）· `tools/probe-bead-loop.mjs`（+`--mode`/参数守卫）·
`tools/np-test.mjs`（+D159 · D152 语义化）· `docs/METHODOLOGY.md`（**第 66/67/68 条**）· `CHANGELOG.md` · 本文件。

---

## 2. 今天测出来的东西（可直接引用，都带复现命令）

### 2.1 G（广度判据）已换定义：**最大单卡落地份额 ≤ 60%**

- 旧判据 `landedKeys ≥ 2`（口语"≥4 种"）**从来没触发过** —— 池里每粒包都是 4~12 种（中位 5）；
  而且它**会误导**：`v7expo-31` 那种「**10 种卡打上血、76.4% 的伤害来自 `sword`**」照样通过。
- 新判据：`max(真卡名落地)/landedTotal > 0.60` ⇒ 塌缩。**加严不是放宽**（只剩一种卡 ⇒ 份额 100% ⇒ 必砍）。
- **单一来源**：`tools/pick-best.mjs` 的 `COLLAPSE_LINE` + `tools/audit-lib.mjs` 的 `landShareOf(W, mh)`。
- 判别力实测（20 粒 · 产品口径 · **按内容去重** · 覆盖率 1.9%）：**34.6% → 100.0%、中位 58.4%、9/20 超线**；现役 **45.9%** 在安全侧。
  复现：`node tools/probe-g-collapse.mjs --sample=19 --games=120 --reach-games=60`
- ⚠️ 算份额时**分子分母必须同一套过滤**：`landByKey` 含**非卡键**（"终局收缩"…），`landedTotal` 是过滤后的 ⇒
  我第一版直接除算出 **44900%**（`v7teach-32`）。已抽成 `landShareOf`，D159 **专门禁止退回手写**。

### 2.2 经济杠杆：**门都能打开，但政策不进门**（"大招开不出来"是意愿问题）

现役包 · 80 局 pool · 同一批决策点（`node tools/probe-ep-reach.mjs --games=80 --fields=pool --pack=js/bundled-champion-3p.js`）：

| 杠杆 | 动作 | **可达率** | **成交率** |
|---|---|---|---|
| 降价 | 大雷 5 → 3 → 2 ep | 0.4% → 4.1% → **22.9%** | **0.0% / 0.0% / 0.0%** |
| 加水源 | `--regen=1`（`state.js:48` 的 `opts.regen`） | 3.7% → **49.1%** | 0.0% → **0.2%** |
| 加水源 | `--regen=2` | 41.4% | 0.3% |

⇒ **两把供给钥匙把门开到 20%~49%，政策一步没迈进去** ⇒ 这是**奖励/意愿**问题，不是经济问题。
⇒ 对用户最初那个"击杀回 2ep"的念头的回执：**它会把可达率抬上去，但按现有证据不会让大招被用起来**；
   而 v1.5.141 的前例警告：把"贵卡落地"当奖励换上来的是**乱挥双枪 −27pt** 的包。

### 2.3 水床是**单向**的（这条对设计很重要）

禁ジ反事实（`--noji`，**策略**干预、规则未动）：ジ 64.3%→0% ⇒ **攻击 33.7%→0%** · **防御 0.1%→100%** · 高价层可达 3.7%→0% · 同 80 局决策点 731→1540（局变长）。
反过来加水源：防御 0.1%→0.7%（几乎不动）· 攻击 33.7%→52.0% · 局长 16.6→**15.1（变短）**。
⇒ **收入变多不会压成防御；只有切断收入才会**。同一个变量的两个方向不可互推。

### 2.4 选优：现役**不是**局部最优，但抬得最高的那粒**过不了门**

基线（1 代不进化 = 现役在同一协议 28对×20局）= **47.9%**；4 个 seed × 120 代：

| seed | 1st | 门 |
|---|---|---|
| 78 | 48.8% | — |
| 79 | 45.7% | — |
| **80** | **50.7%** | ✅ 过（净兑现 **3.39 / 6 种** 达标；`promote --dry` 自检通过） |
| **81** | **51.4%** | ⛔ **阻断：反弹墙里穿透卡零命中**（面对 4 面反弹墙一枪未发） |
| 77 | 49.2% | — |

- seed 80 从 seed 80 继续训 60 代 ⇒ **ft0**：臂自评 **52.8%**，但 `promote` 阻断 **G 有效技能数 2.86 < 3（塌缩到两三张卡）**。
- ⚠️ **+2.8/+3.5pt 落在 ±7pt 噪声带内**（门禁实测参照行 n=60 与 n=300 摆 7pt）⇒ **单次不作数**，要**大 n 复核**才能谈 promote。
- ⇒ 结论：**想上槽的候选一个都没有**（seed 81 硬阻断、ft0 阻断），现役仍是唯一合格者。**promote 需用户同意（红线）。**

### 2.5 序列奖励（`EPIRUS_SEQ_W`）：机制做出来了，**六次 A/B 冠军逐字相同**

- 实现：`tools/seq-reward-lib.mjs` —— "蓄能[电珠] → 下一回合电磁炮"完成时给 **+W ep**，**只在内存里**（指纹不变），默认 0。
  判定不用改源码（事件流已有 `{type:'bead',pid,kind:'elec'}` 与 `{type:'action',pid,key:'railgun'}`）；
  **但要让 `evo.js` 内部的调用点也走包装，必须改源码**（`patchEvoChooser`：改名 `__policyChooserN_raw` + 同名包装层）。
- **付款是真的**：78 / 28 / 125 / 165 / 180 / 159 次，最大一次 **1590 ep ≈ 收入的 17%**。
- **但六次 A/B 的当选冠军逐字相同** ⇒ 两个解释（"注入点被绕过"、"量级太小"）**都被证伪**。
- 【已收回】v1.5.229 我写"付不出去 ⇒ 空结果是结构性的" —— **那是我 `EPIRUS_HOTSTART` 用法错误**（它是开关 `=1`，种子包走 `EPIRUS_SEEDPACK`），修对后 5 代臂就付了 92 次。
- 【已收回】另两个我讲过的解释：**"注入点被绕过"**（改成源码注入后付款翻倍，冠军仍相同）· **"量级太小"**（W 拉到上限 10，冠军仍相同）。
- ⇐ 剩下的最可能原因是**观测量**（见 §4.2）。

### 2.6 【未解决】两把珠经济尺子差 **7 倍**

同一粒包 `ft0`、同为 40 局：

| 量具 | 模式 | 得珠 | 花掉 | 过期 | 浪费率 | 蓄能/局 |
|---|---|---|---|---|---|---|
| `probe-bead-loop` | multi | 215 | 104 | 98 | 45.6% | 3.58 |
| `probe-bead-loop` | **long** | 218 | 105 | 100 | 45.9% | 3.63 |
| `promote-champion` 珠经济栏 | long | **1501** | 121 | 1364 | **91%** | **37.52** |

- 【已收回】我先说"差 7 倍 = long vs multi" ⇒ **对齐模式后数字几乎不动，假设被证伪**。
- ⇒ **"浪费率"目前不是可据以决策的数**。我先前那句"继续训练把浪费率从 74%→45.6% 成功"**已收回**（只在一把尺子上成立）。

---

## 3. 排队中的活（按我建议的顺序）

1. **先跑门禁**（v1.5.235 未验证）：`NP_TIME=1 node tools/np-test.mjs`（预期 205/205）。
2. **定案实验（10 行）**：在 `probe-bead-loop` 与 `promote-champion` 的上下文里，用**完全相同**的参数各调一次 `chargeProfile`，比对返回对象。
   - 同一个数 ⇒ 调用侧喂的东西不同（查 **N 参数**：probe 传 `max(60, GAMES)`、promote 传 `FEAS_N.charge`；以及**是否同一份 params / 同一口径**）；
   - 不同的数 ⇒ `chargeProfile` 内部有随上下文变化的隐性状态 ⇒ **真 bug，要加门**。
3. **合并两本账**（若第 2 步指向"口径不同"）：让两处都调**同一个账本**（单一来源），并让读数**必须印口径**（模式/局数/N）。
4. 然后才回答用户的原始问题：**"从 seed 80 继续训练能不能降浪费率"**。
5. **大 n 复核 seed 80 vs 现役**（300+ 局、多 seed），再谈 promote。
6. `probe-bead-loop` 的**头注仍承诺 `--fields`（代码没实现）** —— 要么补实现（分场地本来就该支持），要么改文档。**别留着"文档撒谎"**。
7. 你之前留下的待裁定项（我已完成 ④ band-save META 配方）：① 加"设防持续性"栏（只记录）② `G<3` 的三选一 ③ E3 G7 草稿 ⑤ `EPIRUS_SEL_LAND` 0/3 咬合（先查同分带有没有第二粒，否则摘掉）⑥ `EPIRUS_BREADTH_FLOOR` 的 N=3 口径。

---

## 4. 我今天犯的错（请别重犯）

### 4.1 ⛔ 禁止用"脚本拼字符串"改代码（今天我栽了 5 次）

`sed` / `node -e` 在**引号、转义、锚点**上连错 5 次，每次都**"看起来成功"**（脚本自己打印 ✔）而**文件其实没变或被改坏**：
`sed` 把 `train-3p` 一行改成语法错误 · 三处 `node -e` 锚点转义不匹配（其中一次把 D152 的修复静默丢掉）· 一次生成的代码里引号没活下来。
⇒ **一律用 `edit` 工具逐字改**；改完**必须回读核对**（`grep`/`node --check`），不信脚本的自评。
**这与我今天两次踩的"静默忽略参数"是同一族病：假成功的通道比真失败更危险。**

### 4.2 群体层面的干预，观测量也必须是群体层面的（已写进 METHODOLOGY 68）

我连续**六次**拿"**当选冠军的字节**"去判一个作用在**对局分布**上的干预 ⇒ 六次全"逐字相同"。
该看的是**群体读数**（每代蓄能率 / ep 收入 / 胜率分布）。**下一步做任何奖励实验，先定群体观测量。**

### 4.3 其它三条（同样写进文档）

- **"某件事从不发生"的结论，必须先排除"我根本没把开关打开"**（`EPIRUS_HOTSTART` 那次）；
- **别对"没验证过存在的旋钮"下结论**（`--cost` 那次，我据此写错了一版结论）；
- **单次观察不等于结论**（我从 seed 77 一次就说"现役是局部最优"，四个 seed 一跑就被推翻）。

### 4.4 门守的是**行为**，不是**空白**

今天两次红都是"门钉了实现细节的字面形态"：D159（正则没算一对括号）· D152（钉 `import { chargeProfile }` 的**字面文本**，我往同一行多 import 一个名字就红）。
⇒ 写门时问一句："**合法的重构会不会把它弄红？**" 会 ⇒ 改成语义断言。

---

## 5. 红线与仓规（照旧）

- **不许动** `js/bundled-champion.js` / `js/bundled-champion-3p.js` / `results/*`；每次提交前 `git diff --exit-code` 自证。
- **指纹必须保持 `ebdbff36`**；改规则 = 规则换代（新指纹、全部历史基线作废）⇒ 独立决策，**不许混在实验里**。
- **未经用户同意不得 promote / 上槽**。
- 新旋钮：**默认关时行为逐字不变** + **必须自证**（印"本臂实付 N 次"这类分母）+ **自动进 `meta.recipe.env`**（v1.5.226 起已自动）。
- **不认识的 `--` 参数必须响亮失败**（`audit-lib.rejectUnknownFlags`，exit 64）。
- 内存实验（击杀奖励 / 序列奖励）产出的包**不是现状规则下的冠军** ⇒ `meta.recipe.env` 会记，别拿回现状引擎直接评。
- 汇报习惯：**长程后台任务不要前台等**；一轮只做一次汇总，不要中途反复汇报。
- 不确定时**先量判别力再立门**（METHODOLOGY 65/66/68 是同族）。
