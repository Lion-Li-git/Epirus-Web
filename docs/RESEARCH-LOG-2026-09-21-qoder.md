# RESEARCH LOG · qoder 夜班 2026-09-20→21（分支 qoder-bugs-0921 · 基于 main@1055080）

## 0. 状态灯（防失忆 · 每完成一格就更新，带时间戳）

**DONE**
- [23:0x] qoder-research-0920 全部 43 提交合入 main（merge 1055080，np-test 156/156、spec 47/47 后推送）。
- [23:0x] 本日志开档；任务板：#19 指向盲审计 / #20 随机重调 / #21 贵卡臂 / #22 人数适应性（#18 合并=本条已完成）。
- [00:0x] **#19 完成（commit f42891d）**：挑衅合规必须打对施法者（tauntBy 名单·多挑主任一·挑主全死免罚）/ 过载炮反制要求指回炮手 / 小雷≥3人环全空转（游走找环）/ 贴贴可被八卦阵挡 / resolveTarget 兜底去 opp[0] 座位偏置改 saltPick。
  附带：V2 阈值门回滚成记录不阻断——**绝对阈值跨指纹代无效**（五修把整条 V2 轴平移 ~6pt：零 10.2→7.7、93 21.2→13.7），要立门只能立相对口径（现役≥零+5 类），列裁定件。spec 51/51（R23d/R38b/R39b/R54b 反证组）。指纹 72a9c1b5→**56402bbf**，两槽重测重记（3P 考卷 41.9 全门绿、2P 0.9831）。
- [00:2x] **#20 完成（commit 36a6a88）**：键级探索 ε=0.4 top5（替掉全候选 ε.25）+ 天火菜单闸（施法者无存活符咒⇒摘天火，与 ep<2 蓄能闸同族、训练共用）。2 席长程镜像 104 回合 0%→46 回合 100% 决胜；真·空爆归零（残 n=0 全为"持咒者先于 pri3 阵亡/镜面复制"的合法物理，非昏手）。vs balanced 脚本节奏反而更快（31.9 vs 46.9 回合）。evo/ui 不在指纹表 ⇒ 零重记成本。

**DOING**（白班 09-21 上午更新，详见 §8-§10）
- [11:0x] ✅ 残局 MULTI_ONLY 规则修复 + UI 事件顺序子档已提交（9b09b76）；指纹 **be6c2195**；两槽成绩重录完（3P 41.9→43.1%、2P 0.9831 恒等）；
  np-test 156/156、spec 52/52、battle-test 5p/3p 无 JS 错误；版本号转正 **v1.5.140**（用户 GO："需要的时候你可以推新版本"）。
- [11:1x] → v7u1 重训臂起跑（§9 预注册已写死；HOT=现役 v7n1-93、seeds 31/82/93、配方=v7t1 去掉 BIGCARD_W）。
- #26 随机性再调（§10）：v7u1 跑的同时量**行为基线**（防御占比/集火率/滚环率/昏手率/局长/破局率），基线没出来不改代码。

**NEXT**
1. v7u1 终裁（§9 五条判据，n≥120）→ 若 ②不中按否证记账，人数适应性回到"专训窄体包 + 用户裁定"。
2. #26 随机性：按基线挑 (a)/(b)/(c) 中最小改动，先量后改，改完复测镜像破局率不能掉回 0。
3. push（含 v1.5.140 出版）；把 §8 的裁定件并进早班报告：2P 槽换不换 v7t2p、V2 相对门、刷收缩门、贵卡交 DS 的特征面题。

**（夜班遗留的 DOING/NEXT 原文，留档）**
- #21 贵卡臂（预注册 → v7t1 起跑）；并行做 #22 的人数谱量化。

**NEXT（按用户 2026-09-20 深夜指令，09:00 前停工）**
1. #19 挑衅指向 bug 验证 → 全技能"指向盲"审计（对照 D:\code\Epirus\README.md 逐卡）→ 修复 + 反证用例 → 指纹重记。
2. #20 ε=0.25 太糙（贴贴不引爆/空爆）：试 ε↓ / top-k 限制探索 / Gumbel(σ)；指标=镜像破局率+昏手率；自己对战验证。
3. #21 高 ep 教学臂：BIGCARD_W 复跑 or 环×贵卡交互行 或教师窗口延长——预注册后跑，一臂约 15 分钟。
4. #22 人数适应性：93 在 2/3/4/5 人的量化 + "跨人数训练"配方设计（2p 打过原 2p 冠军是显式目标）。
5. 收尾：CHANGELOG/D82、push、给用户的早班报告（含各裁定件）。

## 1. 规矩（沿用上两班）
- 每个臂/每条批量修复**跑前预注册判据**；如实报否证。产物 .bak 点名进 CHANGELOG（D82）。
- 指纹文件改动 = 单独立项 + 全链重测重记（np-test/spec/battle/两槽包）。results/* 永不 add。
- 不 promote 不切冠军除非用户点头（93 在槽）。合并 main 的授权本晚已用完一次，不再自作第二次。
- 后台任务判活=标记文件+实测 PID；轮询不写进 tool call。

## 1. #21 贵卡臂（v7t1）预注册（跑前写死）

**配方** = v7n1 臂逐字（Q10 血统：HOT 接力、S4_W=0.3、S4_GAMES=6、三段计划 deepsaver:0.33/aimdefender:0.34/breakdef:0.33、
frac 0.25、DIV_W=0.3、CLEAR_W=0.1、SEAT_GAMES=24、22 席池、ALLOW_HEALTH_FAIL=1），**唯一变量 = `EPIRUS_BIGCARD_W=0.1`**，
起点 HOT=`docs/artifacts/v7n1-93.bak`（现役冠军，新引擎 56402bbf 语义下训练）。seeds 31,82,93。产物 `v7t1-{31,82,93}.bak`（不 promote）。

**动机**：用户实机两粒候选八局 + 我 Q 系读数坐实"AI 零高 ep 攻击卡"；v7big1 当年证明 BIGCARD_W 能长出贵卡但代价=环与宽度
（§4-2"贵卡长出之日即环死"）。现在有了第五行（穿透行为）+ 天火菜单闸 + 键级探索不改变训练菜单语义——**复验代价结构是否变化**，
这是 §4-2 预留的"要它可检验得加交互项"之前最低成本的正面再证。

**终裁判据**（全 n≥120）：
- ① 贵卡真出手：skill-report 富经济口径下 **电磁炮或大雷使用率 ≥5%**，且原生场 120 局里 ≥1 次大雷/电磁炮落地（两者取一即"长出"）；
- ② 不回退：五道门全过 + G4 两模式全格 ≤60% + G5 两模式 ≤25%（口径同 v7n1 终裁）；
- ③ 环不回退：聚能环使用率 >0（skill-report 原生口径）——若贵卡长出而环死，按 §4-2 复现记账（不是新知识，但要把**这一代代价结构**钉死）；
- ④ 行为面穿透判据保持：打龟穿透占比 ≥50%（第五行的账不能烂）。
**通过**=①②同中（③④作代价记账）；**否证**=①不中（0.1 在现配方下长不出贵卡）⇒ 贵卡路升级为"必须带交互项/教师示范段"的设计问题，
下一臂改教师计划（heavyfire 段进三段计划），不再滑 BIGCARD_W 标量（"滑权重不是杠杆"旧账同族）。

## 2. #22 人数谱基线（0921 夜班实测 · tools/count-spectrum.mjs（0921 白班从临时目录收进仓库））

1 冠军席 vs (N-1) 脚本席（60 局/点、轮座、温度 0.15、eps=0）：

| 包 | N=2(standard) | N=3 | N=5 |
|---|---|---|---|
| v7n1-93（现役） | **32%** | 33% | 13% |
| v7press3-91（前任3P） | 57% | 45% | 13% |
| 2P 内置冠军 | **97%** | 40% | 18% |

2P 标准正面对决（120 局/组）：**93 vs 2P冠军 0-120、93 vs 前任3P 0-120** —— 现役包在 2P 不光打不过专训包，
**比前任 3P 包还退步**（前任对脚本 57% vs 93 的 32%）。⇒ "先立后删"里"3P 包在 2 人局严重失校"的 09-19 结论
在新语义下**更严重了**（第五行+防御轴把它推向"打龟向"的构建，2P 没有龟给它打）。
计划：t1 之后跑 **v7t2 = train-best 2P 专训臂**（新语义 56402bbf 下），终裁 = 对 2P 内置冠军 120 局对决 + 2P 考卷；
若专训 2P 也赢不了现 2P 冠军则按否证记账（那 2P 槽只能沿用旧包，"删 2P"另议）。

## 3. v7t1 终裁（否证分支触发）+ v7t2 预注册

**v7t1（BIGCARD_W=0.1 唯一变量）终裁**：
- ①贵卡出手 **不中**：三粒电磁炮/大雷/雷击之枪/地雷使用率全 **0.0%**（skill-report 原生+富经济双口径）⇒ 按预注册记否证。
- ②不回退 **中**：G4 三粒全 PASS（最坏 58%）· G5 三粒 0~7% 全 PASS —— 奖励尾流没买到贵卡，也没赔上既得过（与 v7big1 当年"丢环丢宽度"不同，这次代价面干净）。
- ③环：聚能环 0.2~2.6% 存活。④穿透/防御面：G5 就是它的读数，保持。
- 诊断签名："没学会的强招：大雷 +11pt / 电磁炮 +9.4pt"——**网络知道这些卡值分、就是不出手**（ep 预算被 ジ+枪密度吃掉，
  且 BIGCARD_W 只奖励"出贵卡"这个动作、不教"先攒后放"的节奏）。⇒ 与 §4-2 合并读：标量奖励改不动节奏，示范通道才可能改。

**v7t2 预注册（教师节奏段 · 否证分支的下一步）**：v7t1 配方逐字（含 BIGCARD_W=0.1 尾流），**唯一变化 = 三段计划扩四段**：
`pickDeepSaver:0.25,pickAimDefender:0.25,pickBreakDef:0.25,pickHeavyFire:0.25`（frac 0.25 不变）。HOT=v7n1-93，seeds 31,82,93，产物 v7t2-*。
判据 = 同 t1 的①~④；**通过**：任一 seed 大雷或电磁炮使用率 ≥2%（原生口径）且 ② 不回退；**否证**：示范段也长不出 ⇒
贵卡问题定论为**形状/特征面**（网络看不见"我 ep 富余 & 目标防姿/残血"与贵卡价值的连接），转 6th-row/特征交叉设计给 DS，臂路停止。

## 4. #22 v7t2p（2P 专训臂）预注册 + 槽位安全规程

**通道**：`tools/train-best.mjs`（2P 专用训练器；热启动=现 2P 冠军 seedChampion，本仓唯一真生效热启动）。
配方：默认 3 候选 × 500 代，评测=20 基准×40 局（>0.5 门 + min 主导），新语义 56402bbf 下训练。
**槽位安全**：train-best **直写 js/bundled-champion.js** ⇒ 跑完立即：产物拷贝进 `docs/artifacts/v7t2p-2p.bak` →
`git checkout -- js/bundled-champion.js index.html` 还原槽位到已提交字节（.bak 残留删除）。**本臂只测路通不通，不动槽**。
**判据**（跑前写死）：① 对现 2P 冠军 H2H（standard，120 局，先手轮转）**胜差 >0**；② 20 基准考卷 avg ≥ 现冠军的 0.9831−2pt；
③ N=2 对脚本席胜率 ≥ 现 2P 冠军同场读数。**通过** ⇒ 早班报"2P 可换专训包"裁定件；**否证** ⇒ 2P 槽维持现冠军、
"删 2P 改多人可选 2 人"的原计划按人数适应性数据另议（3P 包 2P 失校已量化：32% vs 97%）。

## 5. v7t2 终裁（贵卡臂路就此收束）

heavyfire 教师段（四段计划、frac 0.25）+ BIGCARD_W=0.1 尾流：三粒电磁炮/大雷/小雷使用率**仍全 0.0%**（判据① 不中）；
价值分反而更悬殊（v7t2-82：大雷富经济 +18.4pt、电磁炮 +15.6pt —— **越教越"知道好"，越不出手**）。
⇒ **双重否证定论**：标量奖励（t1）与教师示范（t2）两条通道都改不动"ep 预算被 ジ+枪密度锁死"这一结构问题；
   与 §4-2（环×贵卡 0/6）合并 = **三次独立否证同一病灶**。按 §3 预注册：臂路停止，
   转给 DS 的是**特征/形状面设计题**：网络缺的不是分数，是"我 ep 富余 & 目标残血/防姿 ⇒ 贵卡价值兑现"的可学习连接
   （候选做法：动作特征加 ep 分带×卡费交叉、或形状第 6 行"富 ep 局的高费出手率"——都要重训验证，不再今晚拍腿）。
②③④：G4/G5 不回退（读数在门禁表跑完后补记，预期与 t1 同级）；环 0.7~3.0% 存活。

**t2 门禁补记（n=120）**：31/93 全绿（G4 最坏 30%/29%、G5 0~8%）；82 唯一红=激光剑连刺格 61%（超阈 1pt、v7s9-82 同族的振荡格）⇒ 与 v7n1 同级，不回退成立。
**#21 结案**：贵卡出口在现有特征面上**不可训练达成**（三重否证），移交 DS 设计题（动作特征 ep 分带×卡费交叉 / 形状第 6 行）。

## 6. #22 v7t2p 终裁：2P 专训"先立"成功（附宽度短板如实记）

train-best（热启动现 2P 冠军、新语义 56402bbf、3 候选×500 代）选中候选2（avg wr 98→99%）。产物存 `docs/artifacts/v7t2p-2p.bak`，
**线上槽已 git 还原未动**（本臂只测路通不通，按 §4 规程）。

**判据（预注册 §4）**：
- ① H2H 对现 2P 冠军（standard，120 局，先手轮转）：**120-0（平0）**；换种子(999)仍 **0-120** 方向一致；
  软化 temp 0.4→**120-0**、0.8→**119-1** —— **不是**靠吃旧包贪心确定性（那是浏览器实际会 eps 软化的东西），是真的更强。
- ② 20 基准考卷：train-best 自评 avg 99% ≥ 现冠军 98%−2pt（其口径，未用 promote-champion2p 重跑）。
- ③ N=2 对脚本席（60 局）：新包 97% = 现 2P 冠军 97%（平手，未回退）。
⇒ **三条通过**：2P 槽"有能力打过原 2P 冠军"的专训包已产出，**"先立后删"里"先立"这块补齐了**（09-19 的 2P 合并分析卡在"3P 包在 2 人局 0 胜"——现在有一条能赢的 2P 血统）。

**⚠️ 诚实短板（明早必读）**：候选2 有效技能=3、落点卡种几乎只剩 `snipe,gun`（divNorm 0.117）——**窄体专家包**。
2P 元游戏本就窄（MULTI_ONLY 三张被 N<3 过滤），但这么窄意味着它可能是"把现冠军这一条线打到极致"而非"更会的 2P 包"：
换槽前必须过 `promote-champion2p.mjs` 全套（20 基准 min>0.5 门 + 它自己的宽度/回归检查），且要防它对**非现冠军**的第三方 2P 对手过拟合。
候选3（种类=4 divNorm=0.344）更宽但 avg 98%<99% 落选——若明早想要"宽一点的 2P 包"，可从候选3 那条重跑或调择优先宽度。
**建议**：作为裁定件交用户，不自动 promote；真换 2P 槽要 promote-champion2p 全绿 + 用户 GO。

（教训入账：`Get-Process node` 会匹配到 Adobe Creative Cloud 自带的 node.exe，等待器盯错了 PID 白等到"卡住"——
后台判活要用启动时 `$!` 的真实 PID，或标记文件，别按进程名 `node` 捞。）

## 7. 夜班收工状态（约 02:00，任务板四格全结）

- 槽位：3P=v7n1-93、2P=原冠军，双双按 56402bbf 重测重记、权重未动；工作树干净，np-test 156/156、spec 51/51、battle OK。
- 明早裁定件（累积）：① v7n1-93 已上 3P 槽的体验反馈（ε.4top5 随机新配置也待你实机看）② V2/V4 草案阈值（V2 建议改相对口径）
  ③ 刷收缩门立不立 ④ 2P 槽要不要换 v7t2p（先立已成，但窄体专家，需 promote-champion2p 全绿+你 GO）⑤ 贵卡=特征面设计题交 DS
  （动作特征 ep 分带×卡费 / 形状第 6 行"富 ep 高费出手率"）。

## 8. 白班（09-21 上午）：残局 MULTI_ONLY 规则修复 → v1.5.140 出版 → v7u1 预注册

**用户实机（`results/93-2`，五局）抓到两件事 + 一条规则裁定**：
1. **规则**："多人场在进入 2p 状态时也应当和 2p 的情况一样" —— 旧实现 `canUseSkillInMode` 只看 `state.mode.skills`（开局模式），
   于是 3~5 人场**打到只剩 2 人**时仍允许 `双枪射手 / 镜面反射 / 全息屏障`。22 回合与 28 回合那两局可见冠军在残局把 1 ジ 砸在全息屏障上
   （1 对 1 时它是纯架势，且它正是那两局的最后一步）。⇒ 现按**当前存活人数**动态过滤（`alive<=2` 禁用、回魂到 3 人自动恢复）。
   指纹 56402bbf → **be6c2195**（注释改动也换指纹，这是 v1.5.7 立指纹时**故意**的敏感）。反证用例 spec **R62**（4→3→2→3 双向 + `attemptAction` 被拒）。
   *（编号避让：v1.5.129 已有一条 R61 ⇒ 新用例记 R62；CHANGELOG 顶部也补记了 135~139 那五组"不升版本"的代码内变更号。）*
2. **UI 顺序**："先输出『挡下伤害』再输出『摆出架势』"（R9/R10/R23 三局）。根因 `evDisplayRank` 防御档一律 `[0,0]` ⇒ 同档退化成写入序，
   而引擎按发射顺序记（攻击方的 `blocked` 常早于防守方的 `guardSet`）。新增 `EV_SUB_RESULT`：摆出=0、其结果类=1。门 **D38** 加断言。非指纹面。
3. **随机性反馈**："这一版随机还可以"但**防御偏多**、**丢了集火和滚环**（见 §10，先量基线再动）。

**重录与出版**：两槽权重逐字节未变、只换考卷 ⇒ 3P `v7n1-93` 多人 3 血考卷 **41.9% → 43.1%**（残局少一张废卡，方向为正），
2P 内置冠军 **0.9831 一点没动**（2 人场本就禁这三张 ⇒ 该改动对 2P 是**恒等**，天然对照组）。
np-test **156/156**、spec **52/52**、battle-test 5 人 + 3 人各真浏览器一局**无 JS 错误**（3 人那局 R14 起进入 2 人残局，全程再无 MULTI_ONLY 出手 = 规则在页面链路上也生效）。
用户 GO："现在已经是白天了。需要的时候你可以推新版本" ⇒ 本条转正为 **v1.5.140**（CHANGELOG/README/index.html 三方一致，D8）。

## 9. v7u1 预注册（跑前写死 · 用户指令"你可以基于此再训练一下原本的93"）

**配方 = v7t1 命令逐字去掉 `EPIRUS_BIGCARD_W`**（即 v7n1 配方 + 现役包当热启动）：
`RING2_ARM=v7u1 RING2_TAG=u1 RING2_SEEDS=31,82,93 RING2_HOT=docs/artifacts/v7n1-93.bak RING2_POOL=<22 席池逐字>
EPIRUS_ALLOW_HEALTH_FAIL=1 EPIRUS_DIV_W=0.3 EPIRUS_CLEAR_W=0.1 EPIRUS_TRAIN_SEAT_GAMES=24
EPIRUS_IMIT_FRAC=0.25 EPIRUS_IMIT_OVERRIDE=1 EPIRUS_IMIT_PLAN='pickDeepSaver:0.33,pickAimDefender:0.34,pickBreakDef:0.33'
EPIRUS_S4_W=0.3 EPIRUS_S4_GAMES=6`。**唯一变化 = 引擎语义 be6c2195**（残局禁用三张）。产物 `v7u1-{31,82,93}.bak`，**不 promote**。

**为什么值得重训而不是只看现役包**：现役包是在"残局还能套全息"的世界里练出来的，它的 1 ジ 预算分配里有一档是给那张废卡的；
规则一禁，那部分预算会流向别处 —— 流向哪里是**训练动力学问题**，不是我能从权重读出来的。

**判据（全 n≥120，先写后跑）**：
- ① **不回退**：五道门全过 + G4 两模式全格 ≤60% + G5 两模式 ≤25%（口径同 v7n1/v7t1 终裁）；
- ② **人数谱改善**：1 冠军席 vs (N-1) 脚本席的 **N=3 读数 ≥ 现役 33%**（目标 ≥38%），N=5 ≥ 现役 13%；
      并加测一条**残局胜率**：三席以上场里"第一次降到 2 人存活"之后冠军最终获胜的比例，同一装配下对现役包同场同种子带各测一遍（现役包在新引擎下重测 ⇒ 这才是干净对照）；
- ③ **穿透轴不回退**：打龟穿透占比 ≥50%（第五行的账不能烂）；
- ④ **零浪费机械核对**：新臂各场残局 2 人阶段 `双枪/镜面/全息` 出手 = 0（引擎已保证；若实测 >0 说明还有第二条绕过路径，**直接报**）；
- ⑤ **代价记账**（不阻断）：聚能环使用率、防御出手占比、有效技能数 G、局长 —— 与现役同表并列，防止"靠变龟/变窄"过判据。
**通过** = ①②同中；**否证** = ①中②不中 ⇒ "残局规则修复不改变人数谱"记为否证，2P/人数适应性的出路回到
"专训窄体包"（v7t2p 那条）+ 交用户裁定，不再在 3P 配方上滑标量。

## 14. §9 v7u1 终裁（已结案：**不换包**）+ P2 标度扫描（四档×三seed 全表，已结案）

### 14.1 v7u1 判据①（n=120 全部门禁）—— **三粒全绿**

`GATE4_GAMES=120 node tools/gate-drafts.mjs v7u1-{31,82,93}.bak`（日志 `logs/u1-gates.log`）：
G4 两模式 × 三粒 = 6 格**全 PASS**，最坏格 `v7u1-93/long` 的「只枪(1ジ压制)」 **52%**（线 60%）、`31-long` 47%、`82-long` 最克格是「珠爆发」；
G5 两模式全 PASS（最坏 `31-long` 防席夺冠 **10%**，线 ≤25%）。在位包同表 43%/25%（long/multi）⇒ **不回退** ✓。
（同一份输出里 `G6 靶向率 ≥40%` 四行全 FAIL —— 那是**在位包自己也 FAIL 的"只记录不阻断"格**，不是 v7u1 的回退。）

### 14.2 判据② 人数谱 + 残局胜率（`tools/count-spectrum.mjs` 重写进仓库，新增"第一次掉到 2 人后夺冠"这一格）

n=60/点 · 1 冠军席 vs (N-1) 脚本席 · seat 轮转 · ε=0：

| 包 | N2（standard） | N3（multi） | N3 残局夺冠 | N5 | N5 残局夺冠 |
|---|---|---|---|---|---|
| **现役 `v7n1-93`（新引擎下重测）** | 32% | **43%** | 45% | **23%** | 23% |
| `v7u1-31` | 33% | 37% | 36% | **32%** | 32% |
| `v7u1-82` | 47% | **52%** | **53%** | 28% | 29% |
| `v7u1-93` | **57%** | 33% | 34% | **32%** | 33% |

⇒ **两个必须分开报的结论**：
1. **我 §9 预注册里写的"现役 33%"是旧引擎的账**（§2 那张表量于 56402bbf）。**v1.5.140 的规则修复本身**就把在位包从
   N3 33%→**43%**、N5 13%→**23%**（残局不再把 1 ジ 烧在废卡上），而 **N2 32%→32% 一字没动** —— 2 人场本就禁这三张，
   对 N2 是恒等 ⇒ 这是一条自带对照的读数，可信。
2. 用**新鲜基线（43%）**判，三粒里只有 **`v7u1-82`** 真在人数谱上赢过现役（N2 47 / N3 **52** / N5 28），
   `v7u1-93` 是"**2 人席特化**"（N2 **57%** 但 N3 掉到 33%）⇒ **判据② 按"超过现役"的原始意图：82 ✓、93 ✗、31 ✗**。

### 14.2b 正面对决（#22 那一问的正式答案）

`--duel=` 加进了同一把尺子里（2 席 `standard` · 120 局 · 先手轮转）：
**`v7u1-93` vs 2P 内置冠军 = 0-120**（与昨晚现役 93 的 0-120 同形）。
⇒ **"N2 打脚本席 57%" 与 "2P 互殴" 是两件事**，前者被规则修复抬起来了，后者一点没动 ——
**3P 血统（含在新规则下重训的 v7u1）在 2P 正面对决里仍然全败**，这条路到此为止；
#22 的出口仍然是 DS/我昨晚产出的 **`v7t2p-2p`（对现 2P 冠军 120-0）**，代价是窄体专家（有效技能 3）⇒ 仍是用户裁定件。


### 14.3 判据③④⑤

④ **已经在 §13 之前先量到了**：`tools/behavior-profile.mjs` 的"残局 MULTI_ONLY 出手"在**全部四个 ε 档 × 两种场型**下都是 **0** ⇒ v1.5.140 的动态禁用没有任何绕过路径。
③⑤ 待 n=120 的 `promote-champion --dry`（穿透占比 / G / 座位 / 场A）一并出，跑在 14.2 之后。

### 14.4 P2 · BEAD_W 中间档：**0.15 档三粒的长程电磁炮 = 0.00/局**

预注册（DS 交接件 P2）："0.05 无经济、0.5 过冲 ⇒ 0.15~0.25 很可能同时保住座位与场A"。**实测把这条假设打回一半**：
`--field=self --gamemode=long --eps=0 --games=20`（`docs/artifacts/bead15-long.out`）：

| 包 | BEAD_W | **电磁炮/局** | 蓄能/局 | 观测最大 ep |
|---|---|---|---|---|
| 现役 `v7n1-93` | 0.05（默认） | 0.00 | 0.00 | 80 |
| `v7bead3-82` | **0.5** | **0.20** | 0.20 | 6 |
| `v7bead15-31` | 0.15 | 0.00 | 0.05 | 81 |
| `v7bead15-82` | 0.15 | **0.00** | 0.00 | 15 |
| `v7bead15-93` | 0.15 | 0.00 | 0.00 | 6 |

⇒ **价格→行为是单调的，没有"中间甜点"**：0.15 买不到经济（连蓄能都没有），0.5 也只买到 **0.20 次/局**
（DS 判据要 **≥1 次/局** 才算"用户看得见" ⇒ **连 0.5 档都差 5 倍**）。
⚠️ 必须区分两件事：DS 那条"花珠率 85.7%"是**转化率**（花掉/得珠），他自己也警告过别混读；**频率**这个量当前最好也只有 0.20/局。
⇒ 若真要把电磁炮做到"用户打一两局就能看见"，方向不是"找中间档"，而是**继续加价格（1.0+）或改珠经济本身**
（电磁炮 2 ジ + 1 电珠、而珠只活到下一回合末 ⇒ 这是**两回合序列**，v1.5.141 的 `EPIRUS_SUB_BEAD` 已经在补贴局里绕过一次，仍然要 0.5 量级才动）。
等 `v7bead25` 三粒落地一起判（如果 0.25 也是 0.00，那"中间档"这条假设就是**否证**，下一根应该是 1.0 而不是 0.2）。

### 14.4b 补完之后：`BEAD_W` × seed 是一张**彩票表**，不是一条曲线

长程 5 席同包 · ε=0 · 20 局/格 · **电磁炮出手/局**：

| BEAD_W \ seed | 31 | 82 | 93 |
|---|---|---|---|
| 0.05（默认 · 现役） | 0.00 | 0.00 | 0.00 |
| **0.15**（本班新跑） | 0.00 | 0.00 | 0.00 |
| **0.25**（本班新跑） | **0.50** | 0.00 | 0.00 |
| **0.50**（DS `v7bead3`） | 0.00 | **0.20** | 0.00 |

⇒ 每个非零标度**恰好只有一个 seed 出手**，且最高的那格（0.25 档 seed31 = 0.50/局）也**没到"≥1 次/局"的判据**；
⇒ **"0.15~0.25 是甜点"这条交接件假设否证**：`BEAD_W` 这个价格旋钮买到的是**中彩票的概率**，不是频率。

**1.0 档（`v7bead100`，等臂补的对照）落下来了，比"彩票"更难看**：三粒 电磁炮 **0.00/局**、**蓄能也是 0.00/局**（seed31 观测最大 ep=84 死攒、82/93 最大 ep=6 根本没攒起来）。
⇒ **这条旋钮连"单调"都不是**：0.05→0.15 无、0.25 有一粒 0.50、0.5 有一粒 0.20、**1.0 归零**。
⇒ 结论要说到这一层才诚实：**`BEAD_W` 不是在调"珠经济的音量"，它只是在挪选择压力的相对权重，而珠线真正的瓶颈是那条**两回合序列**本身**
（0.25/0.5 那两格非零也只证明"偶尔有 seed 撞出来"，不证明"价格买得到"）。
⇒ 下一根应是 DS §5 提案过**从未跑**的**状态分布塑形**（开局带珠 + 足 ep ⇒ 把两回合序列降成一回合），而不是继续滑标度。

⇒ 下一根该试的不是 0.25→1.0 的连续滑（我先补了一臂 `v7bead100`=1.0 作对照，看"价格再抬 4 倍会不会变成普遍出手"），
而是 DS §5 **提案过但从未跑**的那根：**状态分布塑形**（一部分训练局**开局就带一颗电珠 + 足量 ep**）
⇒ 把"蓄能→放炮"这个**两回合序列**当场降成一回合 ⇒ 若连"第一步就可放"都长不出频率，那珠经济就该按 DS §7(b)
定成"针对某条脚本的窄反制"，**留在考卷里**、不再逼冠军自己长。这条要动 `evo.js` 的训练侧（非指纹面），我先不擅自实现。

### 14.5 v7u1 终裁（判据①②③④⑤ 汇总 · 结论=**不换包**，但 93 有一件别的东西值得说）

| 包 | 3 血考卷 | 五道门 | G4 最坏格(n=120) | G5 最坏 | 座位 | G(有效技能) | 场A | 场B清场 | N2/N3/N5 | 2P 互殴 |
|---|---|---|---|---|---|---|---|---|---|---|
| **现役 `v7n1-93`**（新引擎） | 43.1% | ✅ | 43% / 25% | 0% | 11pt | 4.57（S 口径 4.31） | 27% | 1.10 | 32 / **43** / 23 | — |
| `v7u1-31` | — | — | 47% / 40% | 10% | — | — | — | — | 33 / 37 / 32 | — |
| `v7u1-82` | 43.0% | **✗ 场B 0.23 < 0.3** | ≤52% 全 PASS | 0% | **8pt** | **6.29**（S 6.63） | 25% | **0.23** ✗ | 47 / **52** / 28 | — |
| **`v7u1-93`** | **55.9%** | ✅ 五道全过 | 52% / 39% | 2% | 13.8pt | 6.02（S 5.76） | **37%** | 1.25 | **57** / 33 / **32** | **0-120** |

- **判据①（不回退）三粒全 ✓**：n=120 的 G4 两模式 × 6 格全 PASS（最坏 52%，线 60%）、G5 全 PASS（最坏 10%，线 25%）。
  同表里 `G6 靶向率 ≥40%` 四行 FAIL 是**在位包自己也 FAIL 的"只记录不阻断"格**，不是 v7u1 的回退。
- **判据②（人数谱超过现役）只有 `82` ✓（N3 52% vs 现役 43%）—— 但 82 挂在五门上（场B 清场 0.23 < 0.3）⇒ 不可换包**；
  `93` 的 N3=33% 比现役退 10pt ⇒ **按预注册"①②同中才算通过"，v7u1 判 不换包**。
- **判据③（穿透轴）**：`93` 反弹墙穿透命中 **360**/20 局、激光剑+狙击枪双落（现役同格 359）⇒ 第五行的账没烂 ✓；`82` 305（略低）。
- **判据④（残局零浪费）**：全部档位读数"残局 MULTI_ONLY 出手 = **0**"（§14.3）。
- **判据⑤（代价记账）**：`93` 的**广度是这批里最大的一条反差** —— 有效技能数 **6.02**（现役 4.57）、场A 被集火还手率 **37%**（现役 27%）、
  考卷 **55.9%**（现役 43.1%），代价是座位极差 13.8pt（现役 11）与 N3 退 10pt ⇒ **"2 人席特化 + 更宽的卡池"，不是"更强"**。
- ⚠️ 考卷那条按仓库规矩**只作诊断**（A 与稳健性两轮反相关），所以我不拿 55.9% 去要换包；
  但它与 `有效技能 6.02 / 场A 37%` 同向 ⇒ **建议作为"下一臂的起点包"用 `93`**（若继续 #22/人数适应性），
  而不是作为换包候选。**槽位未动、未 promote。**




## 12. P3 · `BIGT_FOCUS`（会传导的大雷线）—— 按 DS 的规格实现，跑出**一半干净、一半不干净**的否证

**做了什么**：`js/train/bots.js` 新增 `pickBigTFocus` + 打分函数 `bigtHubTarget`（DS §8.3 的两个信号逐字实现：
①第三方 ep≥2 且上一手指向 T 计 2 票、②只指向 T 计 1 票、③T 自己指着别人 +1、④hp≥3 半票），
外加一条**对照组** `pickBigTRandom`（同样攒到 5 ジ、同样"能一发带走先带走"，唯一区别 = 目标随机）——
没有对照组的 0.66 无从判读。取证工具进仓库：`tools/script-value.mjs`（1 席脚本 vs 4 席现役冠军 · G4 装配 · n=60 · seed0=90210，
直接数 `bigTChain` / 连带 `voided` / 除以该席真正放出去的大雷次数 ⇒ **传导/施放** 可反驳）。

**先把 DS 的表独立复现一遍**（同一装配、我自己写的工具）：`pickHeavyFire` 大雷施放 **0.00/局、最大 ep=2** ✓、
`pickDeepSaver` 传导/施放 **0.42**（他 0.43）✓、`pickBeadBurst` 胜率 **30%**（他 30%）✓ ⇒ 交接件 §0 三条"别重做"我都验过了，不是照抄。

**读数（n=60 · multi · 4 席现役冠军）**：

| 线 | 胜率 | 大雷施放/局 | **传导/施放** | 连带封行动/局 | 大雷系伤害/局 | 最大 ep |
|---|---|---|---|---|---|---|
| `pickBigTFocus`（新） | **15%** | 1.48 | **0.66** | 0.98 | 4.93 | 5 |
| `pickBigTRandom`（对照） | 15% | 1.48 | **0.66** | 0.98 | 4.93 | 5 |
| `pickBreakDef`（憋大招） | 15% | 1.48 | 0.63 | 0.93 | 4.83 | 5 |
| `pickDeepSaver` | 20% | 0.88 | 0.42 | 0.37 | 2.50 | 5 |
| `pickHeavyFire` | 20% | **0.00** | — | 0.00 | 0.00 | **2** |
| `pickBeadBurst` | **30%** | 0.00 | — | 0.00 | 0.00 | 3 |

**预注册判据（DS 交接件写死：传导/施放 > 0.43 **且** 胜率 > 基线 18%）→ 半中**：
- ① 传导 **0.66 > 0.43 ✓**（而且已经接近用户实盘的 0.75）；
- ② 胜率 **15% < 18% ✗** ⇒ 按判据**不达标**，"会传导的大雷"作为一线**没能打过基线**。

**但这条否证只有一半是干净的，必须分开说**：
- **干净的一半**：`pickBigTFocus` 与 `pickBigTRandom` 六个读数**逐字相同** ⇒ 在这具场里**传导不是选出来的，是场面自己撞出来的**
  （4 席现役冠军彼此集火，随便指一个人都有别人在打他）。⇒ "用户实盘 0.75"不需要"会预测"也能达到，
  这一条支持"大雷的价值不来自目标技巧"。
- **不干净的一半**：想检验"目标信号有没有用"这件事**在决策时刻根本没有输入可读**。我 dump 了 6 个大雷决策点的盘面：
  此时第三方普遍 `ep=1~2`、`lastSkill` 是 `ji`/`guard`/`bagua`（这些的 `lastTarget` 就是自己 ⇒ 票恒为 0），
  28 个决策点里**最大票数几乎只有那半票 hp 加分**（样例 `0.5/2/4` = 最高票 0.5、并列 2 人、候选 4 人）。
  ⇒ **①②两个信号实测近死**，所以"选点没用"这个结论我**不敢下**；能下的只有"**在这具场里花 5 ジ 放大雷不划算**"。
  机制也好解释：**大雷可负担的那一刻，正好是全桌最没钱、最没人刚打过人的那一刻**（攒 5 ジ 要 5 回合，
  而对手的攻击波是 1~2 回合的事）——信号是**过期的**，不是不存在。
- ⇒ **下一步（如果还要投入）**：预测不能只看"上一手"，得看**当回合正在结算的指向**（`state.actions` 在 ①段就有全场指向，
  而脚本在 chooser 时刻看不到 ⇒ 需要引擎侧给一个"本回合已宣言的指向"公共视图），或者把大雷的攒钱纪律改成
  "别人攒到 2~3 ep 时就放"（不等自己满 5）。**这两条都要动引擎可见信息或改卡的经济节奏，不是脚本层能补的** ⇒ 交用户裁定要不要立项。


## 13. 读 DS 的交接件 + P1 独立复跑（14:10，分支 `qoder-train-0921`）

**先记账：DS §3 抓到我 v7t2 的结论不成立，我认。** 他的取证是 `pickHeavyFire` 观测最大 ep=2 ⇒ **结构上买不起** cost≥3 的卡
⇒ 我那臂的"贵卡示范段"**一次都没示范过**，所以"教师示范也否证"这句话**建立在空集上**（本仓 v1.5.37 老教训第三次复发，我踩的）。
同一条链上我还有第二处错：`v7t1` 的标准教师 `pickDeepSaver` 示范的**几乎只有大雷**，而大雷当时被 DS 量成亏本卡（那张表后来又被 §8 自己撤了，
用的是"不会为传导选目标的脚本"⇒ 也是空集类错误）。⇒ **我的"贵卡=特征面设计题"这条定论降级为"未证"**，改口为：
"示范通道至今没有一次带着**有效教师 + 有效价格**跑过；DS 的 `v7bead1/2/3` 是第一次，且 3 已经买到花珠率 85.7%。"

**P1（只读验证他 v1.5.142 的三条前台规则）**：把量具 `tools/behavior-profile.mjs` 扩了两处
（`--field=self` 五席同包 / `--gamemode=long` 长程 + 贴贴/天火/电磁炮/激光眼/蓄能/大雷的**每局出现率**与观测最大 ep），
并用 `git show 56a9302:js/train/evo.js` 临时取回**他改之前的 soft** 做同装配对照（跑完已还原，`git diff` 干净）：

| 装配 = 5 席同包 · long · 20 局/格 · temp .15 epsK 5 | 贴贴/局 | 天火/局 | 电磁炮 | 蓄能 | 防御 | 环 | 集火 | 最大 ep | 局长 |
|---|---|---|---|---|---|---|---|---|---|
| ε=0（训练/门禁口径） | 0.00 | 0.00 | 0.00 | 0.00 | 4.5% | 11.2% | 60.4% | 80 | 69.8 |
| ε=.2 **soft（v1.5.141，他改之前）** | **2.70** | **0.00** | 0.00 | 0.75 | 12.5% | 1.5% | 26.7% | 9 | 27.1 |
| ε=.2 **soft（v1.5.142 三条规则后，现役）** | **0.00** | **0.00** | 0.00 | 0.25 | 12.6% | 2.9% | 23.0% | 9 | 25.0 |

- ✅ **DS §10.2 那行读数独立复现一致**（2.70 / 0.00，一字不差）⇒ 他的归因不是推断，是可复跑的。
- ✅ **规则①生效**：贴贴 2.70 → **0.00/局**（ep<3 不许进探索集），"净亏 1 ep 的铺垫卡"这条当场止住。
- ✗ **规则③在真实对局里是死的**：天火仍 **0.00/局**。机制 = ③ 的触发前提是"自己有存活符咒 / 上一手是贴贴"，
  而这个前提**只能由贴贴自己造出来**，而贴贴刚被①掐掉 ⇒ 两条规则互相抵消，组合永远开不了张。
  D111 之所以绿，是因为它喂的是**构造态**（直接摆一个带存活符咒的 state）——门验的是"规则接线对"，没验"规则在实局里可达"。
  ⇒ **建议交用户裁定的修法**（都不用训练）：把①改成"**贴贴只有在同一决策里天火也可负担/可并入时才允许进探索集**"（配对探索），
  或走 DS §10.3 的第 3 条（抽到铺垫卡 ⇒ 下一回合优先探索收尾卡）。**探索不该单独付组合的成本**，这条我与 DS 同判。
- ⚠️ 顺带一条他表里没有的：长程自对局下 ε=.2 的探索把**环 11.2%→2.9%、集火 60.4%→23.0%** 又打回去了
  （我在混合场 #26 治好的两条，在长程场仍被探索吃掉）⇒ `soft` 只保护"贪心已经选了防御/环"的那一手，**不保护连段本身**。


**已落盘并推送（分支 `qoder-bugs-0921`，HEAD=本次提交）**
- v1.5.140 出版 + 两槽按 `be6c2195` 重录（见 §8）；np-test 156/156、spec 52/52、battle-test 5p/3p 干净。
- **v7u1 三粒全落**（`v7u1-{31,82,93}.bak`）：`zero-promote-check` 3/3 相对起点有提升（自评 0.430/0.470/0.459 · top2 0.69/0.62/0.70）。
  **§9 的 n≥120 终裁还没做**，槽位未动、未 promote。
- **#26 随机性已按实机读数改口径并上线（非指纹面）**：`policyChooserN` 加 `epsMode='soft'`（探索不许覆盖贪心的防御/聚能环 + 探索集不放防御键），
  浏览器从 ε.4 uniform → **ε.2 soft**。同包同装配 40 局/点对照（`tools/behavior-profile.mjs`，新）：

  | 配置 | 防御 | 攻击 | 环 | 集火 | 昏手 | 胜率 | 镜像破局 |
  |---|---|---|---|---|---|---|---|
  | ε=0（纯贪心） | 4.0% | 27.5% | 1.2% | 25.5% | 3.0% | 23% | 63% |
  | ε=.4 uniform（上一版实机） | 26.4% | 23.2% | **0.0%** | 22.0% | 3.0% | 30% | 100% |
  | ε=.4 soft | 22.8% | 23.9% | 0.2% | 26.5% | 4.2% | 25% | 100% |
  | ε=.2 uniform | 16.7% | 26.4% | 1.4% | 26.7% | 3.7% | 35% | 100% |
  | **ε=.2 soft（现役）** | **9.1%** | 28.6% | **1.5%** | **34.1%** | 6.2% | 33% | 100% |

  ⇒ 用户三条读数各治到一处（防御 26.4→9.1、环 0.0→1.5、集火 22.0→34.1），镜像破局没回退。**代价如实记**：被无效化出手 3.0%→6.2%。
  ⚠️ 这张表是 **n=40/点 + 单一包（现役 93）**，换包要重测；昏手上升这一条待用户实机确认是否可接受。

**回来后的 NEXT（按序）**
1. §9 的 v7u1 终裁：`node tools/gate-drafts.mjs` / G4-G5 n=120 + `tools/count-spectrum.mjs`（已收进仓库）换 `--champion` 路径 + 新增的**残局胜率**读数。
2. 用户实机试 ε.2 soft：若"昏手/该防不防"难受，退到 ε=.2 uniform（防御 16.7%）或把 soft 的"防御"半边单独关（代码里是两半，可分开关）。
   另欠一条门：`epsMode='soft'` 现在**只有读数表撑着、没有 np-test 运行时断言**（应补"贪心=防御时 ε=1 探索也必须还防御"这条行为检查）。
3. 明早/下次见面的裁定件没变：2P 槽换不换 v7t2p、V2 相对门、刷收缩门、贵卡=特征面题交 DS。
4. CHANGELOG 顶部 v1.5.141 那半段（ε 口径）目前是"不升版本"记的；若用户实机点头，可以并进下一次出版。


## 10. 随机性再调（#26）**跑前写的预注册**（结果在 §11）

用户的三条读数都是**行为面**的：防御偏多、集火丢了、滚环丢了。现探索集是 **top-5 个不同技能键上的均匀 ε=0.4**（v1.5.139）。
结构上的嫌疑：候选里防御键（防御/八卦阵/反弹/全息/镜面/原型/避雷针/贴贴）占比高且经常挤进 top5 ⇒ 均匀抽它们 = **系统性抬防御、压攻击连段**。
候选做法（**先测基线再逐个试**，都在 `ui.js`/`evo.js` 非指纹面）：
- (a) 探索集里 GUARD 家族键**降权**（或只从"非防御 top-K"里抽）；
- (b) 探索集保留但**按分差加权**（离最优越近越可能被抽，替代"完全均匀"）；
- (c) 聚能环键保护（滚环是经济出口，探索把它换掉等于自断收入）。
指标（同一装配、n=120、温度 0.15、ε=0.4 vs ε=0）：防御出手占比 / 集火率（连续两回合打同一目标）/ 聚能环使用率 / 昏手率（严格必废出手）/ 平均局长 / 破局率（镜像 2 席）。
硬约束：**不能把镜像僵局测回去**（104 回合 0% 决胜那条是 v1.5.139 立起来的），也不能重新产生"贴贴不引爆"。

