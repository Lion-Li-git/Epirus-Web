# 拍手游戏 Epirus — 2 人 + 多人（3~5 人）对战（网页）

依据国际拍手游戏规则制定协会的规则（[原仓库 Lion-LiHaoyi/Epirus](https://github.com/Lion-LiHaoyi/Epirus)，规则文档 v2.1.0）
重新实现的一整套 **2 人可玩、可自对战训练** 程序。

> **当前版本：v1.3.39** · 2 人对战（v1.0.0 功能冻结）+ **多人 3~5 人（技能齐全 + 3 人冠军 · AI 状态特征 v5）**，见 `docs/RULES-NP.md`。
> v1.3.4：**网页训练场可直接训练 3/4/5 人 AI**（Node worker_threads 并行评估，16 核）。
> v1.3.5：**修正多人局"同优先级按座位号升序结算"造成的座位偏置**（改为快照同时结算）+ 规则文档口径对齐。
> v1.3.6：**多人 AI 学会攒钱了**（ε-探索 + 经济适应度；贵技能出手 0%→32.3%）。
> v1.3.7：**技能覆盖熵**抑制策略坍缩（有效技能数 1.88→3.22，用到种类 2→9）。
> v1.3.8：每回合回 ep 与聚能环的**实测否决**（机制就绪、默认关闭）+ 探针进程卫生修复。
> v1.3.9：**分段 ep shaping**（0~4 奖励 / 5~10 中性 / 11+ 惩罚）治好了囤积；更正"环支配"结论。
> v1.3.14：**多目标择优**（胜率容差带内取最发散）+ 头对头验收；过载炮新规；训练分析可视化报告。
> 版本历史见 [`CHANGELOG.md`](CHANGELOG.md)。

- **游戏部分零依赖、纯静态**：双击 `index.html` 即可游玩，不需要装任何东西、不需要联网。
- **训练部分可选**：另起一个 Node 服务（只用内置模块 `http` / `worker_threads` / `vm`），用于练出"冠军 AI"。

## 快速开始

1. 双击 `index.html`（推荐 Chrome / Edge）。
2. 默认「标准模式 + 中等难度」直接开打：点技能出招，双方同时行动，回合日志逐条结算。
   技能按钮按当前资源实时置灰——**ジ/珠子不足或条件不满足的技能直接不可点**（不会"点了才提示无效"）。
3. 想打"练出来的 AI"：双击 `tools/start-train-server.cmd` 启动训练服务 → 训练场点「开始训练(Node服务)」→
   页面自动连接、实时显示进度与多条种子曲线 → 训练完冠军自动写进内置文件并载入内存 →
   把对局难度切到「困难（冠军策略）」即可与冠军对战。
4. 冠军策略可「导出 JSON」分享给朋友，「导入」别人的冠军。

## 功能

### 对局

- 标准模式（3 血 · 28 个可用技能）；三档难度：**简单**（随机/简单逻辑）/ **中等**（规则启发式）/ **困难**（内置或本地练出的冠军策略）。
- HP（含 0.5 血）、ジ、电/爆能量珠、地雷/符咒/挑衅/梦魇/回魂等状态全部可见；技能按钮悬停显示手势与说明。
- 「悔一步」可退回本回合开始前（每局限用一次）；可导出对局记录。

### 训练场

- 轻量对抗自对战（类 GAN 的对抗思想，零 AI 框架）：候选策略群每代与「当前冠军 + 19 个脚本基准」对战打分。
- 基准包含：连招反制、破防、防御墙、永久架势（反弹/防御/八卦）、**坦克线 / 重火力 / 防御+枪三拍**（人类式硬门槛）、**原型制御墙**、**空放型**、**三条节奏型**（反弹→枪→坦克 / 反弹→反弹→坦克 / 防御→反弹→枪）。
- 精英保留 + 变异/交叉繁殖；适应度 = 胜负 + 换血 + 拖时长惩罚 + **出招多样性（熵）奖励**。
- **冠军按真实胜率挑选**：任一基准不高于 50% 直接失去冠军资格（纯平局 0.5 也不过门），过门后按 `0.5×平均 + 0.5×最差对局` 排序。
- 训练在 **Node 多核并行**（`worker_threads`）上跑，页面 SSE 实时进度 + 多条种子曲线（y 轴自适应，最优分可 >1）。
- 多种子 × 多轮 + 种子谱系：轮末保留前 ⌈√n⌉ 名作父代，再按"越靠前分得越多"分配 n 个新种子
  （n=3→2+1，n=4→2+2，n=5→2+2+1，n=8→3+3+2）；「从头训练」只在**第一轮**清空旧冠军。
- 导出训练 CSV 为**标准长表**：`round,global_gen,gen,seed_id,parent_seed_id,best,baseline,champAge,champChanged,sigma`，
  行序严格 `[round] → [seed_id] → [gen]`，可直接做折线图。

### 规则完整性

- **蓄能珠只供下一回合**：蓄能获得的能量珠仅可在下一回合使用，该回合结束时未使用即清空（原规则"累计一个单位…供给下一回合"；
  早期实现误判为"可无限累加"，会让 AI 囤珠失衡）。同回合重复蓄能只刷新，不叠加。

- 2 人可用的 28 个技能 + 4 个隐藏技能（铁索连环 / 吸血鬼公爵 / 回魂 / 梦魇）全部实现。
- 多人专用技能（双枪射手 / 镜面反射等）置灰，留待 3 人模式。

## 目录结构

```
Epirus-Web/
├─ index.html               ← 双击即玩
├─ css/style.css
├─ js/core/                 ← 规则引擎（与规则文档一一对应）
│   ├─ rules.js               技能 / 分类 / 模式数据
│   ├─ state.js               状态结构与出招（费用 / 禁用 / 条件；无贷款）
│   ├─ resolve.js             回合结算引擎（优先级管线 / 防御矩阵 / 地雷 / 转移 / 隐藏技能）
│   └─ play.js                对局驱动（自动对局 / 合法出招 / 空放兜底）
├─ js/train/
│   ├─ bots.js                脚本基准 AI（含人类式打法 / 永久架势 / 连招反制 / 原型墙 / 空放型）
│   ├─ policy.js              轻量 MLP 策略 + 状态特征（62 维，含双方对称参考）
│   ├─ evo.js                 对抗进化训练器（scoreMember / finishStep / 硬门槛择优）
│   └─ trainer.js             冠军存取（localStorage / 导入导出）+ 训练 CSV
├─ js/ui/ui.js              界面与交互
├─ js/bundled-champion.js   内置冠军（由训练器生成）
├─ server/
│   ├─ train-server.mjs     Node 训练服务（SSE 实时进度：/train /champion /reset）
│   ├─ paralleltrain.mjs    worker_threads 多核并行评估池
│   └─ train-worker.mjs     单 worker 评估入口
├─ tools/
│   ├─ start-train-server.cmd  双击启动训练服务
│   ├─ train-fast.mjs          命令行快速训练
│   ├─ train-best.mjs          2 人多种子择优训练（推荐）
│   ├─ train-3p.mjs            多人（3~5 人）自对战训练 → bundled-champion-3p.js
│   ├─ eval-3p.mjs             多人冠军评测（1st/top2 + 出招分布）
│   ├─ smoke.mjs               CDP 真浏览器冒烟测试（2 人）
│   ├─ spec-run.mjs            Node 桩跑 2 人引擎自测（36/36）
│   ├─ np-test.mjs             N 人引擎自测（12/12）
│   ├─ np-probe.mjs            CDP 真浏览器多人（3/5 人）探测
│   └─ diag.mjs / remote-probe.mjs  诊断脚本
├─ docs/RULES-2P.md         ★ 2 人规则裁定版（R1..R55 + 子证 R23'/R34'/R19'）
├─ docs/RULES-NP.md         ★ 多人（3~5 人）裁定版（N1..N13）
├─ results/                 导出的训练 CSV 样例
└─ tests/spec.html          引擎自测（浏览器打开即可，通过数 = 用例数）
```

## 规则裁定说明

原规则文档面向真人手势对局（及 3 人以上场景），存在若干未写死的细节（如判定概率、蓄能珠时效、
避雷针反噬归属、天火窗口等）。`docs/RULES-2P.md` 以 **R1..R55 裁决编号 + 子证（R23'/R34'/R19'）** 逐条拍板并标注，
引擎代码注释与其一一对应；`tests/spec.js` 覆盖主要裁定。**若要改规则，先改文档再改代码。**

## 自测

浏览器打开 `tests/spec.html`：**36 条规则场景用例 + 500 场随机对局无异常**，全部 ✔ 即引擎与文档一致。

命令行（无需浏览器）：

```bash
node tools/spec-run.mjs    # 2 人引擎：36/36
node tools/np-test.mjs     # 多人引擎：26/26（目标/相抵互为目标/大雷连带/死者不行动/双枪/镜面/聚光炮/反复横跳/合二为一/胜负/3P 冠军包/fuzz）
node tools/eval-3p.mjs     # 3 人冠军评测：28 对手对 × 座位轮换 → 1st/top2 + 出招分布
node tools/smoke.mjs       # 2 人页面冒烟（CDP 真 Chrome）
node tools/np-probe.mjs    # 多人页面探测（3 人 + 5 人，CDP 真 Chrome）
```

## 训练（Node 并行）

```bash
node tools/train-fast.mjs 500          # 500 代，约 60s，直接写盘
node tools/train-best.mjs 4 600        # 4 个候选择优（推荐，约 8~10 分钟）
```

- 跨候选与轮末择优**同口径**：任一基准不高于 50% 直接失去冠军资格，过门后按 `0.5×平均 + 0.5×最差对局` 排序。
- **评测口径与实战一致**：`js/core/play.js` 的 `autoGame` 中双方都基于"行动前状态"决策
  （旧实现是"先手已落子再问后手"，会让训练/评测数字与页面手感产生系统性偏差）。
- **绝不空放（贷款兜底）**：`autoGame` 对任何脚本/AI 返回的"买不起或条件不满足"的招一律兜底改按ジ——
  否则招式作废会让对手 `lastSkill=null`，值网络掉进未训练区（曾出现"对手每回合空放 → 冠军 100% 拖成 60 回合平局、出招锁死 ジ↔贴贴"）。
  同时 `policy.js` 给 `lastSkill` 补了两位显式"上一招无效"特征（原先 `idxOf(null)===0` 与"上一招是ジ"同码），
  并把 `whiff` 空放型对手放进池里让它真的见过这个状态（该对手带 `whiffOk` 标记，是唯一被兜底放行的）。
- **连招反制训练（无运行时连招防护）**：池内 `combocounter` 脚本会读对手最近 5 次出招、预测其最常用招并选克制技
  （枪→反弹、坦克→狙击、防御架势→坦克、电系→防御等），模拟"正常对局里明显连招会被对手针对"。
  因此游戏内**不再使用运行时连招防护**——困难档直接用冠军策略（`P.choose(temp 0.15)`），
  游戏口径与训练口径完全一致，AI 靠训练学会"别被看穿"。

## 已知限制

- 冠军的技能覆盖仍偏窄（约 9/28、熵 1.8 bit / 满 4.81）；地雷、挑衅、转移伤害等资源技尚未被用起来。
- 原型制御墙只能靠地雷 / 转移绕过，冠军目前是硬打过去，不是"会用解法"。

## 多人（3~5 人）— 已实现

- 对局面板选“人数（3/4/5）”即可开多人局；技能与 2 人一致，另加「双枪射手」（两个目标）。
- 裁定见 `docs/RULES-NP.md`（N1..N13）：显式目标 / 同优先级两两相抵 / 小雷逐边 /
  大雷不连带 / 防御矩阵逐对 / 铁索图不递归 / 最后存活者胜。
- 已实现：N 人引擎 + 目标 UI + **全部多人专用技能**（双枪射手 + 镜面反射 + 隐藏技能 聚光炮 / 反复横跳 / 合二为一）+ 淘汰后自动观战。
- **3 人自对战训练已落地**：`tools/train-3p.mjs`（多对手聚合特征 + 名次适应度 1.0/0.3/0 + 座位轮换 + 名人堂验证），
  产物 `js/bundled-champion-3p.js`；多人困难档加载它（缺失时回退脚本自适应）。
  AI 状态特征 **v5**（每个对手一个槽位 + 最近 3 步技能历史，FEAT_S 62 → 122）；实测 3 人 **1st 49.9% / top2 71.8%**（更强脚本 aggro 60.4%，farmer 2.4%）。
- 未做：**6 人以上**（引擎支持任意 N，未测性能/平衡）、**4/5 人专项训练**（当前 3P 冠军直接复用）。

## 免责声明

游戏规则（CC BY-NC-SA）版权归国际拍手游戏规则制定协会；本实现仅供学习交流，请遵守当地法律与游玩场合礼仪。
