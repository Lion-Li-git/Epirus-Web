node_modules/
*.log
*.bak
.DS_Store
Thumbs.db

# 训练工具的非发布输出（EPIRUS_PUBLISH 未设时的落点）。
# v1.3.54：这些是**测试残留**——不忽略的话 `git add -A` 会把它们提交进仓库，
# 与 v1.3.48 那次"测试跑覆写线下冠军"是同一类事故。
docs/artifacts/*-out.js

# v1.5.18：对战测试（tools/battle-test.mjs）的截图落点。原先默认写进 `results/`，
# 而那里的 battle-0{1,2,3,4}.png 是**被 git 跟踪**的 ⇒ 一次只读复核照抄验收单命令
# 就会弄脏工作区（第三方复核 §5-9 实测踩到）。历史截图仍在 results/，不再被覆盖。
docs/artifacts/battle-out/
# v1.5.50：训练隔离用的临时产物（ring2-run 的 EPIRUS_BUNDLE_IN/OUT 落点），不得入库
docs/artifacts/.training-*
docs/artifacts/.training.lock
docs/artifacts/ui-out/

# v1.5.145（DS 整理）：results/ 是用户的私人对局记录，整目录不再进仓库（此前有 15 个被跟踪）。
# 只动索引、不动磁盘：清缓存用 `git rm -r --cached results/`。
results/

# v1.5.145（DS 整理）：训练/探针的**运行日志**（*.out）与成批输出目录不进仓库（可重跑得到）。
*.out
docs/artifacts/*-out*/

# 夜班 09-22 教训：git add -A 曾吞掉臂记账文件（.done/.summary/.marker）。
# *.bak 早被全局忽略，但这些点文件/摘要没有 —— 补上（含既有 .training-*，规则合并）。
docs/artifacts/.*
docs/artifacts/*.summary
docs/artifacts/*.marker
