## v1.5.237 — Qoder 09-26 夜班（分支 `qoder-explore-0926`）：**给"防御免费"做地形侧反事实**（1ep 就断连防 · 几乎不花强度）· **新 G 若覆盖 long 第一个被砍的是现役自己** · **选择口径其实是客户看不见的硬档** · 新门 **D161/D162** + 拆掉 D149 的行号地雷（**引擎规则/两槽/判据一字未动**，指纹仍 `ebdbff36`）
- **⚠️ 本版动了 `js/train/evo.js`（训练侧），但**默认关 ⇒ 出厂逐字不变**，且这道"不变"不是我说的：np-test 里 DS 那两道 **CLI 臂基线**门（比对 `docs/artifacts/train-3p-out.js` 权重哈希）本版仍绿。规则四件套 `js/core/*.js` 一字未动 ⇒ 指纹 `ebdbff36` 不变（D16 绿）。
- **E23「防御免费」是不是这棵树的根？**（新量具 `tools/guard-cost-lib.mjs` + `tools/probe-guard-cost.mjs`，**内存补丁**，不改仓库）
  实测（剂量表见 §E23 · 宽筛 57 粒见 §E23b）：**同席连续第 2 次防御起收 1ep（`L1`）就把"维持很久"打断** —— 基线连防 ≥10 的 15 粒里 **9 粒压到 ≤2**，而最健康的三粒包（`cmin4-82`/`divK`/`co1s7`）**门数一格没掉**；57 粒里 `L1` 掉门 5 / 涨门 2 / 不变 50，净兑现 Δ 中位 **+0.00** ⇒ "接近免费"，但不能吹（强度侧是 10 局/格小样本，只有整体数可引用）。
  另一半要 2ep 才松：`band2` 局长 45 在 `L1` 全程不动、`L2@2ep` 才掉到 18 ⇒ **同一句"疯狂防御"底下至少两种病（进入 vs 维持），别当一种治**。
- **E24 防御是"学出来的"还是"看不见时的安全默认"？**（新量具 `tools/probe-defense-margin.mjs`）：**两个都对，主角是后者**。现役的防御 **96% 靠 ε 抽样漏进来**（价值函数从不偏好防御，分差中位 −52）；`band2` 的防御 **78% 本来就是 argmax**，且"对手越有钱、防御占优的决策比例越高"（33→42%）⇒ §H-47 那条因果第一次落到**价值函数层**。遮特征实验：**只剩任意单块 ⇒ 设防率 75~80%、连防 45、局长顶到上限**（两粒种子一致）⇒ "看不见就龟"是普遍机制；但同一块 `rel` 对 `band2` 是压龟的、对 `seat24` 是放龟的 ⇒ **块语义是包相关的，不许建跨包"看这块就多防"的判据**。
- **E25 用户裁"新 G 可以覆盖 long"之后先量的账**（新量具 `tools/probe-share-modes.mjs`，份额一律走 `audit-lib.landShareOf` 单一来源）：**60% 这条线若覆盖 long，第一个被砍的就是现役自己** —— 现役 multi 49.1%（198 个权重等价类里的 14 分位 ✓ 距线 +10.9pt）而 **long 63.9%（⛔ 超线 3.9pt）**。
  且 **long 份额对样本量敏感**：G=20/40/80/120/200/300 读 **60.2 / 59.3 / 62.5 / 63.9 / 62.6 / 62.5** ⇒ "现役在不在 long 线内"目前是**由 n 决定而不是由包决定**（`train-3p.mjs` 的塌缩闸走的正是小 n）。归档侧：>60% 的占 **68.2%(multi) / 70.2%(long)**，扩到两模式都判只多砍 13/198 类 ⇒ 这条线不是"抓特例"，是"把整族塌缩包算不合格"。三条裁定材料已写进 §E25，**没有一条是我替他做的决定**。
- **E28 建臂之前先把自己的前提推翻**：我原来写"适应度在 ε=0、产品在 ε=0.2"。量了排序稳定性（新量具 `tools/probe-caliber-rank.mjs`，120 个等价类 × ε 四档）：**按 `dmg/局` 排 ρ(0 vs 0.2)=0.480，且随剂量单调**（0.05→0.788 · 0.1→0.652）⇒ 口径确实在换答案；**但去建旋钮时读源码发现：每代评分 `scoreMemberN` 的被评席出厂就是 `temp 0.35 · ε=0.15 · 不传 epsMode`（硬档）**，ε=0 的是自评/健康门槛那一路。⇒ 真正的表述是**"选择用的是一种玩家永远看不见的第三口径"**，而 v1.5.141 量过硬档的后果正是"防御 4.0%→26.4%、聚能环 1.2%→0.0%"——**产品侧关掉的噪声，选择侧一直留着**（这是"疯狂防御"的新候选来源，与 E23 的"防御免费"是两条独立来路）。
  已做：`evo.js` 加**默认关**的执行口径旋钮（`EPIRUS_TRAIN_EPS/_EPS_K/_EPS_MODE/_TEMP`）+ 两个出口 `trainChooser()`（自评/门槛）与 `fitChooser()`（每代评分被评席）；四处散点收成漏斗 ⇒ route ① 的搬运点 4→1。`train-3p` 按示范族同一范式接线（没 setter ⇒ exit 7、下达后读回消费点、退出前印开火计数、`eps>0` 而两个计数为 0 ⇒ exitCode 8）。**服务端 / `train-best` 故意不接** ⇒ 传它会被黑键闸响亮 exit 6（D143 那族）。实测：现役权重同 gen/idx 下 `fit` **0.5487 → 0.7030 →（关档）0.5487**。
- **新门 D161**（钉量具不钉结论）：`makeGuardCost` 单元级判定（`cost:0` 不许被 `|| 1` 吞 · `off` 必须原样返回同一个函数 · 事前摘菜单 · 真选了才扣 · 摘到空必须退回原菜单）+ 两档真跑（零价档读数必须与对照**逐字相同**且被自检抓成空枪；重价档不许判成空枪、`dropped>0`、**强度栏必须变**=route ① 打进内部调用点的行为证据）。**E23 那两个假读数各值一道门**：事后扣 = 从 0 里扣（`billed=21` 而 `epTaken=1`）、route ② 打不进 `mirrorHealth` 的闭包调用 ⇒ 两次都是"补丁没反应"被读成"结论"。
- **新门 D162**：开档必须改变**被选择的那个数**（`scoreMemberN` 的 fit，不是只看 `mirrorHealth`）· 关档必须**逐字回到原值**（可逆 ⇒ 没有隐性状态，门自己也在 `finally` 里复位）· `train-3p` 不算暗键而 `train-best` 必须列为暗键 · `audit-lib` 不许出现这个 env 名（作用范围关在训练侧）。
- **拆了 DS 的一颗地雷（METHODOLOGY 69）**：D149 原来钉的是探针**源码里的字面行号**（`evo.js:2554`/`audit-lib.mjs:403`）⇒ 任何人在那两行上面插一行，探针印假行号而门仍绿。现在 `probe-layer-caliber` 导出 `srcLine`/`hardwiredLine` 做单一来源，探针与门都现算；【6】的指针改指真正的 ε 决定点（漏斗）。顺带把 D150 的 `import { build }` **精确形状**判定改成"导入名单含 build"（我合法地多加了两个 helper，它当时就红了）。
- **E27 大 n 复核 seed 80 vs 现役（DS 交接 §3.5 第 5 条，纯测量零冲突）**：`eval-out/sel80.bak` 与现役在同一协议下各跑 **8 个种子 × 35 组合 × 40 局 = 11200 局/粒** ⇒ **sel80 均值 45.86% vs 现役 55.09%，Δ = −9.23pt（均值差 SE≈0.80，t≈−11.6）** ⇒ 他那个"+2.8pt 但在 ±7pt 噪声带内"的读数**在大 n 下不但没站住，还反了号**。另：`sel81` 与现役 **Δ=+0.10pt（t≈0.1）**（它被门挡的是"反弹墙零穿透"，不是胜率）；**`ft0`/`ft2` 权重 sha1 相同（`70c94e59…`）⇒ 它们是同一粒包**，别把两行一样读成复现。⚠️ 绝对水平不可与他那张"28对×20局"表直接比（我这里是 35 组合 × 40 局），能比的是**同一协议内的配对差**。
- 产物卫生：**本版无新 `.bak` 落仓**（冒烟与臂的带内产物一律写 `%TEMP%/kr3/arms`，`EPIRUS_BAND_DIR` 那条路）；`docs/artifacts/train-3p-out.js` 是 CLI 臂默认产物的正常刷新；`results/*` 只读未提交。未做：没装/没改任何判据、没换槽、没动规则、没 promote。`np-test 208/208` · 指纹 `ebdbff36`。

## v1.5.236 — 接 DS 的 09-25 交接：他 §3.2 的"两把珠经济尺差 7 倍"**定案 = 口径搬运**（光口径就 3.00×，不是 `chargeProfile` 有隐性状态）+ 新仓规落地（7 个探针接 `rejectUnknownFlags`）+ 门 **D160**（Qoder · 工具面，**引擎/两槽/判据一字未动**）
- **版本号说明（先把账接平，不改写历史）**：台账原本停在 v1.5.230，而 DS 最后 5 个 commit 消息写着 v1.5.231~235 却**都没碰 `CHANGELOG.md`/`README.md`/`index.html`** ⇒ D8 绿只是因为没人升版。本版**直接跳到 v1.5.236**（让三件套追平"已被 commit 声称过的最高号 + 1"），231~235 的内容仍以他本人条目为准；这条差异我只在这里陈述，不去替他补写他的条目。
- **开工第一件事是他指定的**（交接 §3.1：v1.5.235 未再跑门禁）：`NP_TIME=1 node tools/np-test.mjs` → 改任何东西之前先 **205/205 全绿**；逐门榜首 D135 28.3 秒，**我自己的 D155 是 13.0 秒**（在他"要动就得逐个论证"名单里 ⇒ 不动它，也不偷偷减采样）。
- **§3.2 定案实验（他给的 10 行设计）**：同包、同 `mode=long`、同 40 局，唯一变量 = 沙箱有没有被 **route ②** 搬过口径（那层包装把两参 `policyChooserN(params, 0.15)` 升成 ε=0.2/k5/soft）：搬过 ⇒ 得珠 **91** · 浪费率 29.7% · 蓄能 2.27/局 · 局长 25.5；没搬（= `promote` 珠经济栏那把尺）⇒ 得珠 **273** · 13.6% · 6.83/局 · 53.5。
  ⇒ **光口径就差 3.00 倍**，剩下的部分是 N（本工具传 `max(60,GAMES)`、promote 传 `FEAS_N.charge=40`）⇒ **不是他担心的"隐性状态型真 bug"，不需要为此加门**。
- **但病在我这边，已修**：`tools/probe-bead-loop.mjs` 那行"对照真源 `chargeProfile`"其实一直跑在产品口径下 ⇒ 正是 METHODOLOGY 53/54/66 说的"读数不印口径就不能互比"。改法：**并排印两本账** —— 账本 A（本工具口径 · 搬过 ε）/ 账本 B（**未搬口径 ε=0** · 与 `promote --dry` 同尺），行尾直接给比值，"真源"这个词只留给 B；同一次跑（现役 · `long×60`）实测 **A 得珠 127/浪费率 25.2% ‖ B 得珠 357/12.6% ⇒ 2.81× 全属口径**。
- **§3.6"文档撒谎"清掉**：`--fields` 从头注删除并写明原因（本工具是**五席同包镜像**，没有"场地"这一维；要分场地请走 `probe-cross-mode` 或 `promote --dry`）。
- **新仓规落地（v1.5.234：不认识的 `--` 参数必须 exit 64）**：给我自己 7 个量具接上 `rejectUnknownFlags`（`probe-defense-cause`/`screen-cheap-predict`/`probe-breadth-flip`/`probe-seat-caliber`/`probe-layer-caliber`/`probe-kill-reward`/`probe-ideal-champion`），**逐个真验**：传 `--typo-flag=1` 全部 exit 64 且点名；传正常档全部 exit 0（守卫不误伤自己）。
- **门 D160（新，`np-test 206/206`）**钉四件事，全部**语义/行为**断言（DS 的 D152/D159 教训：钉字面文本会被合法重构打红）：① 接过的不许掉；② **守卫必须早于任何 `arg()` 读取**（晚跑的守卫挡不住已经算完的读数 = 假成功形态）；③ **被 `import` 的模块只能在 `main()` 里守卫**（`probe-layer-caliber` 若守卫在顶层，会把调用方的 `--packs/--pair/--seeds` 当成非法参数打死）；④ 名单漏项这种死法用双向真跑兜（非法必 64 / 合法必 0）。**剩下 4 个不是我的工具**（`probe-cross-mode`/`probe-g-collapse`/`probe-intercept-cost`/`probe-skill-marginal`）在门里**只点名不阻断**，等各自 owner，我不越权改。
- **我改红了两道门，当场都修了（记进日志，别当成"门不稳"）**：① `--json` 漏在 kill-reward 的守卫名单 ⇒ 打红 D139/D141；② 改名"真源那一行" ⇒ 打红 DS 的 D152（改法：把那串字面留在 B 行，它本来就该指未搬口径的那把尺）。这批**一次脚本拼接都没用**，全部逐字编辑 + `node --check` 回读（DS §4.1 的禁令，我今早正好栽过 5 次）。
- **给他/用户复核的两条**（见 §H-58，都不动代码）：**新 G 的 60% 线目前只求值 `multi`**（现役我量到 multi 47.0~50.4% 安全，但 **long 59.3~63.9%**，n≥120 全在线之上；且 `train-3p.mjs:764` 那条还要 `SEL_LAND`/`BREADTH_FLOOR` 打开才跑）⇒ "现役在安全侧"这句要带口径说。
- 产物卫生：**本版无新 `.bak` 落仓**（`docs/artifacts/train-3p-out-band1..4.bak` 是 09-25 **19:39** 留下的、被 `.gitignore:3` 的 `*.bak` 忽略、且已在 CHANGELOG:2661 点过名 ⇒ 非本版产物）；`results/*` 只读未提交；引擎与两槽与 `origin/main` 逐字节一致。未做：没装/没改判据、没换槽、没改规则。`np-test 206/206` · `spec 52/52` · `smoke OK` · `battle OK` · 指纹 `ebdbff36`。

## v1.5.230 — **更正 v1.5.229 的结论**：序列奖励"付不出去"是我**热启动用法错误**造成的假象；修对后它付得出去（0.51 次/局）

> **更正**：v1.5.229 我写"现役训练配置下一次都没付出去 ⇒ 空结果是结构性的"。**那是错的** ——
> `EPIRUS_HOTSTART` 是**开关 `=1`**（不是路径；种子包走 `EPIRUS_SEEDPACK`）。我写成路径 ⇒ 热启动没生效
> （日志里 `coldStart=true`）⇒ 起始种群是掷骰子级的、不蓄能 ⇒ 奖励当然付不出去。**修对之后：5 代臂发放 92 次 / 184 ep（0.51 次/局）**。
>
> **教训**（与 METHODOLOGY 66 自查那条同族）：**"某件事从不发生"的结论，必须先排除"我根本没把开关打开"。**
>
> **修正后的 A/B（同 seed=31 · 12 代 · pop 6 · 正确热启动）**：W=0 与 W=2 的当选冠军行为读数**逐字相同**
> （得珠 140 · 花掉 56.4% · 浪费率 36.4% · 蓄能 2.33 次/局），而 W=2 臂训练期间**实付 78 次 / 156 ep**
> ⇒ 结论修正为 **"付得出去、但 12 代剂量看不见"**，不是结构性归零。下一步是剂量/时长（W 2→5 或 40 代以上）。
>
> 门禁 **205/205** · 指纹 **`ebdbff36`** · 未 publish、未 promote。

## v1.5.229 — 序列奖励（用户批准方案 a）：**机制做出来了，但在现役训练配置下"付不出去"** ⇒ 空结果是结构性的

> **做了什么**：`tools/seq-reward-lib.mjs`（新）—— "**蓄能[电珠] → 下一回合电磁炮**"每次完成给 **+W ep**（`EPIRUS_SEQ_W`，默认 0）。
> - **与击杀奖励同族**：只在**内存里**生效（仓库文件一字不动 ⇒ **指纹仍 `ebdbff36`**），默认关时行为逐字不变。
> - **不用打源码补丁**：判定所需信息**事件流里已有**（`{type:'bead',pid,kind:'elec'}` 与 `{type:'action',pid,key:'railgun',outcome:'ok'}`）
>   ⇒ 注入点 = 包一层 `EpirusTrainer.policyChooserN` **工厂**（一处覆盖 mirrorHealth/评估/真桌所有路径）。
> - `meta.recipe.env` 自动记下 `EPIRUS_SEQ_W` ✓（v1.5.226 的机制直接派上用场）。
>
> **验证过的三件**
> 1. **默认关不变**：固定 `EPIRUS_SEED=31` 跑两次 ⇒ 冠军参数**逐字节相同** ✓（我第一次用不同 arm 名比 ⇒ 误判"不同"，那是我的检查设错了）。
> 2. **识别正确**：mirror 装配 30 局、电磁炮 86 次 ⇒ **发放 71 次（2.37 次/局）** ✓。
>    （我试过再加"恰好隔一回合"的严格校验 ⇒ 发放掉到 **0**，因为 `tick` 跑在回合结算**之前**、回合号比较恒不成立 ⇒ 那是**时机 bug**，已回退并写进注释。
>    误差方向是**漏付**（83%），不是多付 —— 因为电珠只活一回合，"能打出电磁炮"本身就蕴含"上一回合自己充过电珠"。）
> 3. **收尾结账**（本次新增）：印"本臂共发放 N 次 · M ep"，并在 N=0 时**响亮说明"梯度恒为零"** —— 否则"奖励没效果"与"奖励压根没付出去"分不清。
>
> **实测（A/B 短臂 · 同 seed=31 · 20 代 · pop 6 · 每代 6 局）**
> - 基线（W=0）与序列臂（W=2）产出**逐字相同的冠军**，且两个冠军**都是"蓄能 0.00 次/局"**。
> - 直接看结账：**本臂发放 0 次 · 0 ep**（5 代臂与"热启动"臂同为 0）。
> ⇒ **结论：不是代际太短，是奖励一次都没付出去** —— 掷骰子级的新基因组**不蓄能** ⇒ 梯度恒为零 ⇒ 效果为空是**必然**。
> ⚠️ 我的"热启动"调用**用法错了**（输出里 `coldStart=true` ⇒ 包没装上）⇒ 那条还没被检验，是**下一步的入口**：
>   让起始种群真会蓄能（现役包在 pool 场是 **2.19 次/局**）之后再量"付得出去时有没有效果"。
>
> **与方案 (b)/经济实验的关系**：上一版已证"可达率抬到 49% 也没人打"；本版进一步证明**奖励类改动要先过"付得出去"这一关**，
>   而现役训练配置**过不了**。⇒ 想动大招，顺序应当是：**先让训练场里出现这个行为（热启动/课程），再谈奖励**。
>
> **门禁**：205 门 · 指纹 **`ebdbff36`** · 引擎与两槽未动 · 未训练 publish、未 promote。

## v1.5.228 — 经济实验（用户批准）：**"大招开不出来"是意愿问题，不是经济问题** —— 降价与加水源都把门开到 20%~49%，政策一步没迈进去（成交率 ≤0.3%）

> **预注册（跑前写死，见 METHODOLOGY 67）**
> H1 主判据：水源 `epRegen` 0→1 使"高价层(≥3ep)可达率"从 3.7% 升到 **≥15%**，且"可达时成交率" **>0**（基线 0.0%）。
> H2 反判据（水床）：防御份额不许上升（基线 0.1% ⇒ ≤2%）；攻击份额不许下降（基线 33.7%）。
> H3 副作用：每局回合数不许明显变长（基线 16.6）。
> H4 分岔：只降价（不动水源）⇒ 可达率上升但**成交率仍为 0**。判负：任一反判据失败。
>
> **实测（现役包 · 80 局 pool · 同一批决策点 · 产品口径）**
>
> | 杠杆 | 动作 | **可达率**（够得着） | **成交率**（够得着去不去） | 防御 | 攻击 | 局长 |
> |---|---|---|---|---|---|---|
> | **降价** | 大雷 5 → 3 → 2 ep | 0.4% → 4.1% → **22.9%** | **0.0% / 0.0% / 0.0%** | 0.1% | 33.7% | 16.6 |
> | **加水源** | `epRegen` 0 → 1 | 3.7% → **49.1%** | 0.0% → **0.2%** | 0.7% | 52.0% | **15.1** |
> | **加水源** | `epRegen` 0 → 2 | 41.4% | 0.3% | 0.0% | 86.0% | — |
>
> **裁定**：H1 **半通过**（可达率 3.7%→49.1% ≫ 15% ✓，但成交率只有 0.2% ⇒ 实质未通过）·
> H2 **通过**（防御 0.1%→0.7% ≤2%，攻击不降反升 33.7%→52.0%）· H3 **通过**（局长变**短**）· H4 **通过**。
> ⇒ **结论：两把供给钥匙都把门开到 20%~49%，而政策一步没迈进去** ⇒ "大雷开不出来"是**奖励/意愿**问题，**不是经济问题**。
> ⇒ 直接回答用户最初那条"击杀能回 2ep 很赚"：**它会把可达率抬上去，但按现有证据不会让大招被用起来**；
>   要动的是**奖励面**（而 v1.5.141 的前例警告：把"贵卡落地"当奖励会换来乱挥双枪 −27pt 的包）。
> ⇒ 顺带证伪一条像模像样的解释：**水床是单向的** —— 收入变多不会压成防御（防御几乎不动、攻击上升、局变短），
>   只有**切断收入**才会（`--noji`：防御 0.1% → **100.0%**，攻击 33.7% → 0%）。
>
> **本版工具改动（都在 `tools/probe-ep-reach.mjs`，全部加法、默认值下输出逐字不变）**
> 1. **出招类别构成**：`ジ / 攻击 / 防御 / 能量 / 特殊` 份额（与既有读数**同一批决策点**）。
> 2. **`--noji`**：在**菜单**里禁掉ジ ⇒ "压缩ジ"的极端反事实（**策略**干预，规则未动、指纹不变）。
> 3. **`--regen=<k>`**：把一等规则参数 `opts.regen`（`js/core/state.js:48`，2P v1.0 口径 = 0 = 关）接到 `oneGameN` 上。
>
> **我自己的两处更正**
> - 第一版把降价那栏写成"`--cost=3` 与 `--cost=5` 读数逐字相同 ⇒ 降价无效"——其实**该量具根本没有 `--cost` 参数**
>   （降价档是它内部 §B 循环印的）⇒ 那两次是**同一次运行**。抓住它的是"两次连类别构成都逐字相同"这个可疑现象。
> - 表述更正：先前说"pool 局长 47.0"，那是**另一个字段**的读数；同一口径下 pool 局长是 **16.6**（本版表格用的是后者）。
>
> **门禁**：205 门 · 指纹 **`ebdbff36`** · 引擎与两槽未动 · 未训练、未 promote。

## v1.5.227 — **换 G 的定义**（用户裁定）：广度从"数种类"改成"**最大单卡落地份额 ≤ 60%**"；副产品是新量具 `probe-g-collapse.mjs` + 门 D159 + METHODOLOGY 66

> **为什么换**（实测，`tools/probe-g-collapse.mjs` · 产品口径 · **按内容去重**后抽样）：
> 旧判据 = `landedKeys ≥ 2`（口语里还有"≥4 种"）。而**池里每粒包都是 4~12 种（中位 5）** ⇒ **这条子句从来没触发过**；
> 更糟的是它**会误导**：`v7expo-31`/`v7r3-42`/`v7soft-36`（同实异名）**10 种卡打上血、却有 76.4% 的伤害来自 `sword`**
> ⇒ 在"数种类"的尺子下是模范，在"看份额"的尺子下是重病（`METHODOLOGY.md` 第 66 条）。
>
> **新定义 = 最大单卡落地份额 ≤ 60%**
> - 单一来源：`tools/pick-best.mjs` 导出 `COLLAPSE_LINE = 0.60`；份额由 `tools/audit-lib.mjs` 的 `landShareOf(W, mh)` 算；
>   训练路径（`tools/train-3p.mjs`）从**同一次** `mirrorHealth` 取份额，不另跑一遍。
> - **是加严不是放宽**：只有一种卡 ⇒ 份额 100% ⇒ 一定被砍（旧子句的 case 被蕴含）；`>` 不是 `>=`（恰好 60% 不砍）。
> - 缺份额字段的条目退回旧子句，并把缺失个数报成 `shareMissing`（不许静默降级成"看不见就当过"，也不许一律判死）。
>
> **判别力（先量再立门，20 粒 · 内容去重后抽样 · 覆盖率 1.9%）**
>
> | 轴 | 实测散布 | 处置 |
> |---|---|---|
> | **① 塌缩**（最大单卡份额） | **34.6% → 100.0%**，中位 **58.4%**，**9/20 超线**；现役 **45.9%** 在安全侧 | ✅ **采用**（取代旧判据） |
> | ② 宽度（打上血的种数） | 1~12 种、中位 5 ⇒ 旧线从不触发；且 `10 种 / 76.4% 靠一张` 照样通过 | ❌ **撤掉**（我原提案里的一轴，被自己的测试否掉） |
> | ③ 高价层（≥3ep 可达率 / 可达时成交率） | 可达率 0.0%~60.7%（**10/20 为 0**）；成交率多为 0，少数非零（`v7new4-82` **50.0%** / `v7xfer44c12-band2` 25.5% / `long-34` 19.9%） | ⚠️ **只记录不阻断**（立线会全线报红） |
>
> **改动清单**
> 1. `tools/pick-best.mjs`：`rejectNarrowWinners` 加份额子句（新增 `export const COLLAPSE_LINE`；返回加 `collapseLine`/`shareMissing`；
>    `victims` 带上 `maxLandShare`/`maxLandKey` ⇒ 日志能读出**是哪张卡塌的**）。
> 2. `tools/audit-lib.mjs`：新增 `landShareOf(W, mh)` —— **单一来源**（只数真卡名、分母用过滤后的 `landedTotal`）。
> 3. `tools/train-3p.mjs`：份额取自同一次 `mirrorHealth`；`[广度线]` 的日志改印阈值 + 每粒的 `最大X%`。
> 4. `tools/probe-ep-reach.mjs`（**加法**）：新增"**高价层 ≥3ep**"逐决策统计（不手抄名单：按 live menu + 定价现算；
>    买得起一律用菜单自带的 `affordable`）⇒ `--json` 多一个 `costly` 字段。③ 的读数就是它给的。
> 5. `tools/probe-g-collapse.mjs`（新量具）：三轴一次印全 + **判别力自评**（min/中位/max/超线数）；
>    抽样**按内容去重**并报覆盖率（服 `METHODOLOGY 62`）。
> 6. `np-test` 新门 **D159**（把判别力固化成回归测试）：`10 种但 76.4% 必须砍` / `恰好 60% 不砍` / `60.1% 砍` /
>    `只剩一种卡仍砍` / `缺字段走旧路并计数` / `净兑现低于地板仍砍` + 静态断言"两处共用单一来源"。
>
> **我自己这一版踩的坑（都由"打印出份额本身"当场抓住）**：先写成 `max(landByKey)/landedTotal` ——
> 而 `landByKey` 按 `damage.via` 统计、**含非卡键**（"终局收缩"…），`landedTotal` 却是**过滤后**的数
> ⇒ 算出 **44900%** 的份额（`v7teach-32`）。现抽成 `landShareOf` 并由 D159 钉住"只数真卡名、份额 ≤1"。
> （教训：**把量本身打印出来**，荒谬值才看得见；只打印判定结论的话，这个 bug 会静默把好包判成塌缩。）
>
> **门禁**：204 → **205**。**红线**：只改 `tools/` 与文档；引擎与两槽未动、指纹仍 **`ebdbff36`**、未训练、未 promote。
>
> **附：用户质疑「压缩ジ ⇒ 贵卡更少、防御更多」—— 我量了，他是对的**（`--noji` 反事实：只在**菜单**里禁掉ジ，
> 属**策略**干预、规则未动 ⇒ 指纹不变。现役包 · 80 局 pool · 同一批决策点）
>
> | | 正常 | `--noji`（禁ジ） |
> |---|---|---|
> | ジ份额 | 64.3% | 0.0% |
> | 攻击份额 | 33.7% | **0.0%** |
> | **防御份额** | **0.1%** | **100.0%** |
> | 高价层（≥3ep）可达率 | 3.7% | **0.0%** |
> | 同一 80 局的决策点 | 731 | **1540**（对局拉长一倍） |
>
> ⇒ **ジ是唯一水源，压它 = 压整张预算**：贵卡先消失，剩下的回合只能靠**免费防御**填。这是**水床**，不是猜的。
> ⇒ **与本版 G 的关系（要紧）**：**G 不碰ジ** —— 它只判"造成伤害的卡里最大一张占多少份额"，而ジ**不造成伤害**
>    （`landByKey` 按 `damage.via` 计数；20 粒样本的最大卡全是 `gun/sword/snipe/tank/bigT`，**从没出现过 ji**），
>    且它**只当否决、不进 fit**（§N29 的老规矩）。⇒ 新 G **不会**对ジ施加压力。
> ⇒ 用户真正要的"大招能用"由**第三条轴（高价层可达/成交，本期只记录）**盯着，而那条轴的天花板是
>    **规则经济**（击杀奖励 / 单价 / epRegen），**不是任何"伤害分布"类判据** —— 这是本期最该记住的边界。
> ⇒ 配套量具（加法）：`probe-ep-reach` 新增"出招类别构成（ジ/攻击/**防御**/能量/特殊）"与 `--noji` 开关。


## v1.5.226 — 产物自证**实际生效的 `EPIRUS_*` 配方**（用户批准的第 2 项）：主 meta 与 band `.bak` 的 `recipe.env` 现在写全，且**加了旋钮不用改代码**

> **病（这是千问 E9/E10 被迫绕道跑的根因）**：`tools/train-3p.mjs` 的产物 `meta.recipe` 一直是**手抄名单**
> （`arm/seed/gens/games/pop/xn2w/xn2g/selLand/kill/trainMode/counterOpps/bigtChainW/killReward/breadthFloor`…）——
> **新旋钮天生不在里面**（METHODOLOGY 13「同一份名单两处各写一遍必出事」的同族）。
> 后果很具体：想问"**哪根旋钮养出这粒包**"，查 `meta` 却发现**没有 env** ⇒ 只能归因到"抽奖"，
> 并用**前瞻实验绕道**才能把问题问出来。
>
> **修法（单一来源，不另抄名单）**：读集问 `server/knob-guard.mjs` 的 `readKeysOf`
> —— **与 `enforceKnobs` 用的是同一份读集**（`entry: 'tools/train-3p.mjs'` + `SELF_ENV_KEYS`），
> 再取**环境里真的设了的**那些键 ⇒ **以后加旋钮不用改这里**。
> - 记的是"**实际生效的配方**"，不是默认值：**没设的键不出现**（默认值要看代码，不在这里猜）。
> - 两处都写：主 meta 的 `recipe.env`（+ 自洽计数 `recipe.envKeys`）与 **band `.bak` 的 `recipe.env`**
>   —— 后者才是长期留存的那一份（`.bak` 会留下来，日志会滚走）。
> - 读集算不出来时写 `{"__error": "..."}`，**绝不静默成空表**（空表会被读成"这臂没配方"）。
>
> **实测（小臂 + 两个旋钮 + 临时 `EPIRUS_BAND_DIR`）**
> `train-3p.mjs 1 3 2 2` 写出的 `d158probe-band1.bak` 里：
> `recipe.env = {"EPIRUS_ARM":"d158probe","EPIRUS_BAND_DIR":"<tmp>","EPIRUS_BIGT_CHAIN_W":"0.5","EPIRUS_KILL_REWARD":"2"}` ✓
> 逐键逐值对上，且**没设的 `EPIRUS_XN2W` 没有出现**。
>
> **新门 D158**（`np-test` 204 门）：静态 —— 读集必须来自 knob-guard、必须与 `enforceKnobs` 同一 entry+extraReadKeys、
> 主 meta 与 band meta 都要带配方；行为 —— 真跑一条小臂，**读它写出来的 `.bak` 的 META**，判逐键逐值、
> 臂名在、**没设的旋钮不许凭空出现**、`envKeys` 与 `env` 自洽。
> ⚠️ 这条 spawn **故意不走缓存**：它要读子进程**产出的文件**，正落在 `tools/np-cache.mjs` 前置要求的排除面上
> （那条规矩这次直接派上了用场）。
>
> **红线**：只改 `tools/`（+ 文档/版本号）；引擎与两槽未动、指纹仍 **`ebdbff36`**、未训练、未 promote。
> 门禁 204 门（v1.5.225 的 203 + D158）；缓存机制照旧（本版新增的门不缓存，因为它读产物）。

## v1.5.225 — 门禁提速·第二步：确定性重活上**内容寻址缓存**（用户批准方案 a）= 冷跑 **263.9 秒 → 热跑 121.6 秒**（**省 142.3 秒 / −54%**），两次都 203/203

> **起因**：v1.5.224 之后门禁仍约 4 分钟，用户指出「没改动的地方重复跑浪费时间」⇒ 批准方案 (a)。
>
> **病（实测）**：D135 两条 60 代训练臂 **41.4 秒**、D134 16.1、D136 13.8 —— 它们**是确定性的**
> （固定 seed + 固定 env + 固定输入），而绝大多数提交根本没碰训练侧：改一行文档也要等它们重跑。
> 再扩到 D123/D127 的臂、D150/D137 的只读探针、D155 的 `promote --dry` 之后，可缓存的重活合计 **≈142 秒**。
>
> **判据（三条，缺一不可）—— 这就是"缓存"与"静默跳过"的分界线**
> 1. **键 = 内容**：`argv` + `EPIRUS_*` env（临时路径折成 `<TMP>`）+ **源码树内容**（`tools/` `js/` `server/` 的
>    .mjs/.js，82 文件 / 2.4 MB，实测哈希 **8 ms**）+ argv 引用的文件 + node 版本 的 sha1。
>    **任何一处内容变一个字节，键就变**（D157 行为式钉住这一条）。
> 2. **只缓存 `(status, stdout, stderr)`，绝不缓存"通过/失败"** ⇒ **断言照旧在缓存输出上跑**；
>    门没被跳过，只是那个确定性子进程没重跑。D157 另钉：模块源码里不许出现 pass/fail 字样。
> 3. **响亮 + 逃生口**：每次命中独立一行（key 前 8 位 + 省下秒数）；收尾印「命中 N 次 · 真跑 M 次 · 省下 X 秒」；
>    `NP_NOCACHE=1` 一律真跑（D157 行为式验证：设上之后命中数不许再涨）；缓存坏了/读不动 ⇒ 回退真跑。
>
> **⚠️ 前置要求（写进 `tools/np-cache.mjs` 头注，防以后乱扩）**：只许缓存「**断言只用 stdout/status**」的子进程。
> 若某条门还要读该子进程**产出的文件**（如训练臂写进 `EPIRUS_BAND_DIR` 的 band 随后被断言打开），
> 缓存命中那一次**没有那些文件** ⇒ 光缓存 stdout 会造假绿。**本版逐条核过**：
> 在该门的 spawn 之后 grep `readFileSync|readdirSync|existsSync` —— 命中处只剩"读仓库源码做静态断言"（与产物无关）。
> 覆盖面：4 条重臂 + D123/D127 的臂 + D150/D137 的只读探针 + D155 的 `promote --dry` = **16 处 spawn**。
>
> **实测（同一棵树，清缓存后冷跑 + 热跑）**
>
> | | 整轮 | 缓存 |
> |---|---|---|
> | **冷跑**（填缓存） | **263.9 秒** | 命中 0 · 真跑 16 |
> | **热跑**（不改任何文件） | **121.6 秒** | **命中 19 次 · 省 142.3 秒** |
>
> 两次都是 **203/203 exit 0**（断言一条没少）。命中明细里最大的几笔：`train-3p 60 3 8 8` 两条各 **21.1 / 18.6 秒**、
> `40 3 8 8` **16.0**、`30 3 8 6` **13.0**、两条 `probe-layer-caliber` **6.4 / 9.0**、两条 `promote --dry` **7.6 / 6.8**。
>
> **新门 D157**（`np-test` 203 门）—— 判的是行为，不是文本：
> 键的四项敏感性（稳定 / argv 敏感 / **`EPIRUS_*` 敏感** / **同一路径只改一个字节必变**）·
> **临时路径必须归一化**（`mkdtemp` 每次随机 ⇒ 不折 `<TMP>` 就永不命中）·
> 命中必须给出**与真跑逐字相同**的 stdout/status · `NP_NOCACHE=1` 时命中数不许再涨 ·
> 覆盖面 ≥12 处 · **门里不许调 `cacheReset()`**（见下）。
>
> **我自己在这一版连栽的三个坑（全部由"量"或"门"当场抓住，不是事后发现）**
> 1. **D157 引用了 `writeFileSync` 但 np-test 没 import 它** ⇒ 门自己红（`writeFileSync is not defined`）。
>    ⚠️ 我单独在另一个进程里验证过那条逻辑、所以没暴露 —— **"另起一个进程验证"不能代替"在真沙箱里跑一次"**。
> 2. **临时路径只折了前缀、没折 `mkdtemp` 的随机后缀** ⇒ 冷跑写 4 条、热跑又写 4 条新条目，**命中 0 次**。
>    抓到它的是"冷/热对比"这个动作本身（`ping` 命中数而不是 `ping` 通过数）。**现已被 D157 行为式钉住。**
> 3. **D157 里调了 `cacheReset()`** ⇒ 收尾印成"命中 2 次 · 省 0.2 秒"，与真实的"命中 19 次 · 省 142.3 秒"对不上。
>    现在改成**差值**判定，并加断言禁止门里再出现 `cacheReset()` 调用。
>
> **红线**：只改 `tools/`（+ 文档/版本号）；引擎与两槽未动、指纹仍 **`ebdbff36`**、未训练、未 promote、未落盘新 `.bak`。
> 缓存落在**系统临时目录**（`<tmpdir>/epirus-npcache`，最多 400 条，自动裁剪）—— 不进仓库、不污染 `git status`。

## v1.5.224 — 门禁提速：拿掉**我上一版自己加的那两项**（D149 探针 spawn 46.5→13.4 秒 · 删冗余 D156 = −22.8 秒）＋ 新增 **`NP_TIME=1` 按门计时**

> **起因**：用户问「门禁为什么要跑 4 分钟，不是让你清理过门禁吗」。我先量后改。
>
> **先纠正我自己一个表述失误**：v1.5.201 那次"清理"删的是 **50 个不在 np-test 执行路径上的工具** + 修 6 处写错的门
> ⇒ **它从来不是提速**，而且我当时**没量时长**。这次把量具补上了（见下面 ③）。
>
> **① 实测（`NP_TIME=1`，同一棵树）**
> 改前最慢 5 道：**D149 46.5 秒**（千问 的门，但**是我上一版把它的探针 spawn 变贵的** —— 两次 spawn ×16~23 秒）·
> **D135 41.7**（两条 60 代训练臂，与本次无关）· **D156 22.8**（**我加的、与升级后的 D149 断言完全重复**）·
> D150 17.1（千问）· D134 17.0（40 代臂）。
>
> **② 改动（三件，都在 `tools/`）**
> 1. **删 D156** —— 它的断言（`import build` / `B2.patched !== B2.hardwired` / `__viaWrapper > 0` / 两列可见 / 自证打印）
>    在升级后的 D149 里**逐条都有** ⇒ 纯冗余。门 **203 → 202**，省 **22.8 秒**。
> 2. `probe-ideal-champion.mjs` 新增 `--seat-n` / `--mirror-n`（**门禁专用小样本档**；默认值不变 ⇒ 报告照旧跑满
>    座位 n=100+400、广度 n=400）。D149 两次 spawn 都传 `--seat-n=60,120 --mirror-n=120`
>    ⇒ 单次 spawn **16 → 7 秒**、**D149 46.5 → 13.4 秒**（省 33.1 秒，同轮实测）。
>    ⚠️ 两次 spawn 必须传**同一组**档位 —— D149 有一条"两轮读数逐字相同"的对照断言（已同步改成接受 `MIRROR_N`）。
> 3. `np-test` 新增 **`NP_TIME=1`**：默认**零成本**（只在 `t()` 里 push 两个数）；打开后印
>    **整轮墙钟（`process.uptime()`）· 门内合计 · 门槛外** + **最慢 15 道门**。
>    ⇒ 下次再问"门禁为什么慢"，先跑它，不用猜（这道量具就是被这次提问逼出来的）。
>
> **③ 成绩要说准：按门报，不按总时长报**
> - **可采信的**：同一轮里逐门对比 —— **D149 46.5 → 13.4**（−33.1）· **D156 22.8 → 0**（−22.8）⇒ **合计 −55.9 秒**。
> - **不可采信的**：整轮墙钟。同一棵树背靠背两次跑出 **214.5 与 247.8 秒**（更早一次门内合计 320.7 秒）⇒ **±30% 噪声**，
>   而且不是我的残留进程造成的（查过：无 node 进程、无运行中作业）。**所以本版不宣称"总时长降了多少"**；
>   谁要判断提速，请用 `NP_TIME=1` 看**逐门**那两行。
>
> **④ 还没动的大头（要用户拍，约 72 秒）**：D135（41.4 秒，两条 60 代臂）· D134（16.1，40 代）· D136（13.8，30 代）——
> 它们是**真跑训练臂的功能门**（验补贴相位旋转 / 大雷连带归因 / 教师归因）。减代或缓存会改变"门到底验了什么"：
> **(a)** 按输入内容哈希缓存（必须**响亮打印"用了缓存"**、任何异常一律回退真跑、且默认关）；
> **(b)** 先证明 40 代够用，再把 D135 的 60 代降下来；
> **(c)** 不动。
> 另有一批 10~16 秒的门（D150/D155/D123/D127/D67/D105/D137…），各自 spawn 一次量具或跑一次小臂，同样属于"要动就得逐个论证"。
>
> **红线**：只改 `tools/`（+ 文档/版本号）；引擎与两槽未动、指纹仍 **`ebdbff36`**、未训练、未 promote、未落盘新 `.bak`。

## v1.5.223 — 把「理想冠军」第 5/6 条也接上**产品口径**（新门 D156 + D149 契约升级）= 实测这两条**翻转**：座位 n=100 从 12.5pt 变 **24.0pt（判 biased）**、广度净兑现 G 从 2.63 变 **3.01（4→7 种，未达标→达标）**

> **缘起**：交接文档（`docs/HANDOFF-FOR-QWEN-2026-09-24.md`）§2 修订后，我自己写下的"下一件该做的事" ——
> 第 5/6 条当时**没有产品口径读数**，因为 `seatSymmetry`（audit-lib.mjs:403）与 `mirrorHealth`（evo.js:2554）
> 内部把 Chooser 参数写死成 `policyChooserN(params, 0.15)`（ε=0），而产品跑的是 `ui.js:464` 的 ε=0.2/top5/soft。
>
> **做法（复用，不新写）**：千问 已经有 `probe-layer-caliber.mjs` 的 `build()`（在**内存里**替换 evo 写死处 +
> 把沙箱里的 `EpirusTrainer.policyChooserN` 包一层 ⇒ **仓库一字未动、指纹不变**），而且 **D150** 已经把
> "替换/装载逻辑只能活在 `build` 里"立成了门。
> ⇒ 我第一版另写了个 `tools/layer-caliber.mjs` 想抽成新模块，**读到 D150 后自己删掉了**（那正是它禁止的第二份实现），
> 改成 `probe-ideal-champion.mjs` 直接 `import { build } from './probe-layer-caliber.mjs'` —— 与 `probe-breadth-flip.mjs` 同一范式。
>
> **探针改动**：① 引擎装载也走 `build()`（关闭档 = 与仓库当前行为逐字相同 ⇒ 历史读数不变）；
> ② 【5】【6】固定印**两列**（ε=0 原生 + 产品 0.15/0.2/5/soft）；
> ③ 带**两半自证**：`evo.js` 替换数必须 = 源码现算数、且 `audit-lib` 那条包装层必须**真被调用过** —— 对不上就整表作废（非零退出）。
> 理由：两栏若因为"没搬成功"而相同，会被读成"口径无关"（这正是 D150 立门的动机）。
>
> **实测（现役 `v7cmin4-31` · 每场 120 局）**
>
> | 条目 | ε=0（原口径） | **产品口径** | |
> |---|---|---|---|
> | 【5】座位极差 n=100 | 12.5pt（ok） | **24.0pt（biased！线 21.0pt）** | ⚠️ **两口径结论相反** |
> | 【5】座位极差 n=400 | 6.3pt（ok） | 7.0pt（ok） | 一致 |
> | 【6】出手 G / 净兑现 G / 打上血的卡 | 4.21 / **2.63** / 4 种 ⇒ 未达标 | **6.40 / 3.01 / 7 种 ⇒ 达标** | ✅ **翻转** |
>
> ⇒ **交接文档 §2 的结论随之更新**：**口径无关**的失败只剩三条 ——
> **第 1 条（大雷原生 0.00）· 第 2 条（换线方向是"少打多充"）· 第 4 条（序列率 0.42→0.17，产品口径下更差）**；
> **第 3 条（破防场）与第 6 条（广度）两条缺口都被口径解释掉了**；第 5 条要看 n（n=400 两口径都 ok，n=100 产品口径判偏）。
>
> **⛔ v1.5.224 更正：D156 已删**（它与升级后的 D149 断言完全重复，且实测吃 22.8 秒）—— 门数 203→202，断言一条没少。
>
> **门**：新增 **D156**（探针必须复用 `build`、两半自证、【5】【6】两列都印、自证要**打印出来**）；
> **D149 契约升级**（原为"这两行不吃旋钮 ⇒ 必须自报口径" ⇒ 现为"**两口径都要印** + 搬运自证"；
> 它原有的单一真源、四参数调用、旋钮分开三条**仍钉着**）；**D150 复用方从 1 个扩到 2 个文件**。`np-test` 202 → **203**。
>
> **红线**：只改 `tools/` 与文档；引擎与两槽未动、指纹仍 **`ebdbff36`**、未训练、未 promote、未落盘新 `.bak`。

## docs(口径) 2026-09-25 · **不升版本** · 未 promote —— 交接文档 §2 被口径修正（原表是错的）+ 清掉两个"伪装成 .bak 的 HTML"

> **① 口径修正（千问 09-24 夜发现的，我已独立复核）**：`docs/HANDOFF-FOR-QWEN-2026-09-24.md` §2 第一版的 7 条表
> **全是 ε=0 的"评测口径"**，而产品跑的是 `js/ui/ui.js:464` 的 `pickChampion(..., 0.15, 0.2, 5, 'soft')`。
> 同一只包、同一个量，换口径结论会翻转 —— 我复跑确认：**破防场受评席 1st 从 0.0%（ε=0）变成 100.0%（产品口径）**，
> 那条"不会清场"的缺口**在产品口径下不存在**。另两处：镜像场电磁炮/蓄能 0.42/1.45 → **0.17/0.34**（探索一开反而用得少）；
> 序列完成率 0.42 → **0.17**（产品口径下更差）。
> **我犯了两个错，都写进该节**：㈠ 把 `policyChooserN(params, temp, eps, epsK, epsMode)` 的**第 2 个参数（温度）当成了 ε**
> —— 所以我让千问扫的 E5「ε 悬崖」**拉错了杆**（温度 0.05→0.25 时电磁炮 0.47/0.43/0.43/0.44 几乎不动）；
> ㈡ 整个探针从没接过产品口径。⇒ **本仓评测层整体是 ε=0**（`audit-lib.mjs` 9 处 + `js/train/evo.js` 4 处写死），
> 凡走它们的读数（含训练侧健康门槛、体检 G 列）都不是产品口径。
> 修订后："口径无关"的失败只剩 **第 1 条（大雷原生 0.00）** 与 **第 2 条（换线方向是"少打多充"）**，
> 而 **第 5/6/7 条目前根本没有产品口径读数** ⇒ 下一件该做的事是把它们接上产品口径，不是再跑一晚训练。
> 另加 **§10「本夜实际结果」**（千问 的分支 `qoder-handoff-0924` @ `704ec04` = v1.5.222：E1"改价买不到大雷"、
> 珠浪费率的两种相反病、用户实机病的凶手 `cbs1s2-band2` 等）与 §6 第 5 条的反向指针。
>
> **② 清理"伪装成 .bak 的 HTML"**（千问 提醒、并已在 E12 踩过一次）：`docs/artifacts/index-html-before-ab{,2}.bak`
> 是 index.html 的手工备份（HTML 不是包）⇒ 任何"扫全部 `*.bak` 当包"的工具会当场 SyntaxError 把整场筛作废，
> `D82` 也会要求 24h 内被碰过的 `.bak` 记账。处置：移到 **`docs/artifacts/html-out/*.html`**
> （被 `.gitignore:27 docs/artifacts/*-out*/` 覆盖 ⇒ 不入库；不再以 `.bak` 结尾 ⇒ 递归扫描也扫不到）。
> **验证**：顶层与递归 HTML 命中 **0**；顶层 1449 个 `*.bak` 全量认货 **1449 全部是真包**；`git status` 干净。
>
> **红线**：纯文档/文件整理，未动引擎（指纹 `ebdbff36`）、未动两槽与 `results/*`、未训练、未 promote。

## handoff(0924 · DS · 给千问的一夜实验交接 · **不升版本** · 未 promote)

> 用户 09-24：「你把现有的工作整理一下，写一个新的文档拿来给千问，我等下让他**根据你的方法尝试跑一晚**。你也可以多提一点方法。」
> 产出 **`docs/HANDOFF-FOR-QWEN-2026-09-24.md`**（自包含，接手方不需要读本次会话）：
>
> - **现状**：今晚 v1.5.200–v1.5.204 各做了什么（黑键闸单一来源 / 删 50 个死工具 / 修 6 处门 / 判据按实测定案 / UNRUN 处置语义 / 方法学落账）。
> - **理想冠军 7 条 × 现役实测**（6 条未达标；量具 `tools/probe-ideal-champion.mjs`）。
> - **已量清楚的三件（别重做）**：击杀奖励为什么"重训没效果"（实付 1.05ep/局、破防场 0.00、门槛 `ep≥5` 占 **0.0%**）·
>   座位偏置（现役与随机零分布不可分；线随 n 标定）· 两处旧误判（"两把尺子"其实是两只包；`maxDraw` 是死门不是误杀）。
> - **7 个预注册实验**（E1 ep 门槛可达性 · E2 ep 收支结构 · E3 把破防场纳入主判 · E4 激励可达性 · E5 ε 扫描 ·
>   E6 广度当选键 · E7 口径统一），每条带**预测 + 通过判据 + 代价 + "失败也学到什么"**。
> - **11 条会复发的规矩**（METHODOLOGY 第 47 条的压缩版）+ **10 条方法建议**（报数三件套 / A-A 与 A-B 双对照 /
>   顺序决定成本 / 先找已知好与已知坏 / 两把尺子先归一 / 否定也要预注册 / 前提条件可达性优先…）。
> - **命令清单**（含一条警告：`tools/train-best.mjs` **没有** `EPIRUS_PUBLISH` 门槛、默认就写线上 2P 槽）**+ 红线 + 建议顺序**。
>
> **只读整理**：未改任何引擎、判据或产物；`np-test 194/194` · `spec-run 52/52` · 指纹 `ebdbff36` · 两槽与 `results/*` 未动。

## measure(0924c · DS · 只读测量 · **不升版本** · 未 promote) —— 击杀奖励为什么「重训没效果」：**它买的那个动作，现役包在 99% 的决策时刻够不着门槛**（方法学记入 METHODOLOGY 第 48 条）

> 用户的推理是算术上成立的（「击杀回 ep ⇒ 降低后续开大招的开销」），所以「没效果」必须另有解释。
> 用**真规则实现**（`tools/kill-reward-lib.mjs` 的内存补丁）在现役 `v7cmin4-31` 上量了 200 局 × 三个档位 × 两个场地。
>
> **① 规则的实际语义 ≠「击杀回 2ep」**：规则 1 = 每次死亡回 **1ep** 给"优先级最高"的参与者；
> 规则 2 = 按**造成的有效伤害**回**等量** ep（overkill 不付）。
> 一次 1 点伤害的补刀只回 1ep ⇒ 实测两档**实付相同**：脚本池场 **1.05 ep/局**（规则 1）／**1.05**（规则 2），
> 占 ep 收入（≈5/局）的 **21~22%**。
>
> **② 在最需要它的场地里，它一分钱都不付**：破防场（4×只防御不还手）**死亡 5.0 次/局**，
> 但**有来源伤害 0.00/局** —— 5 次死亡全部来自无来源的终局收缩（`damage.source == null`，R57 语义）
> ⇒ **规则实付 0.00 ep/局**。即激励恰好在"该换穿透线去清场"的场景里**结构性缺席**。
>
> **③ 即使付了也跨不过门槛（这条才是决定性的）**：大雷 = **5ep**（电磁炮 2ep + 1 珠；ジ = 1ep/次）。
> 决策时刻的 ep 分布 —— 脚本池场均值 **0.76**（`ep≥5` **0.0%**、`ep=3~4` **2.4%**）；
> 破防场均值 **0.49**（`ep≥5` **0.0%**、`ep=3~4` **0.0%**、`ep<3` **100%**）。
> ⇒ 一次 +2ep 的回款能改变"能不能打大雷"的决策占比 = **2.4% ／ 0.0%**。
>
> **通则（METHODOLOGY 第 48 条）**：买一个动作之前先量**门槛在决策时刻的分布**（「占总收入的比例」是错的透镜）；
> 并确认奖励**能到达**那个场景（触发条件是否成立）；同族奖励形状先互相比（本例两个形状实付相同 ⇒ 别把希望押在换形状上）。
>
> **出口仍是用户指出的那两条**：**改价**（把大雷费用 / ep 收入挪到"积累"真的会发生的量级）或
> **改判据**（把破防场 / 被压制场纳入主判）。
>
> **红线**：只读 —— 不改仓库引擎（补丁只在内存）、不训练、未 promote、两槽线上包与 `results/*` 未动。

## v1.5.222 — 把**设防持续性栏**以"只记录不阻断"接进 `promote --dry`（新模块 `tools/defense-axis.mjs` + 门 **D155**）；立这条门当场抓到我自己两处错（Qoder · 工具面，**引擎/两槽/判据一字未动**）
- 背景（为什么现在做）：§H-56 把两件事分开了——**仪器已确认**（Δ 两端差 18pt、跨种子极差 1~3pt、placebo 13/13 全过），**阈值没确认**（中间挤着 7 粒 Δ +13.5~+15.6 的包，加种子也不分开 ⇒ 是价值判断）。所以这步只做"看得见、不拦人"的那半。
- 新模块 **`tools/defense-axis.mjs`**：只解析文本、**不跑仿真**（可以安全 import，不像 `probe-defense-cause` 一 import 就开跑）。`parsePairTable()` = 同回合配对表的唯一解析处；`formatRecord()` = 体检那行文案的唯一出处。
- `tools/promote-champion.mjs` 在产品代理栏之后加**设防持续性栏**：`spawn` 探针 → 交 `defense-axis` 解析（**不在体检里复制第二份仿真**，D150 同规矩），印 `Δ(钱) ±pt · hold 均% · cycle 基线（水平轴）% · 跨种子极差 pt ⇒ 判定`；默认 `40 局 × 2 种子`（实测整次 `--dry` 约 9 秒），`EPIRUS_NO_GUARD=1` 关掉。
- **门 D155（新，`np-test 202/202`）**：六条源码断言（有这栏 / 有关闭开关 / 解析单一来源 / 体检不许自己 `createState` / 解析不出时必须明说"没量到"不许回 0）+ **三段真跑**：① 固定样本回归；② 真链（小样本探针 → 同一份解析 → 必须得到有限 Δ 与判定文案）；③ **行为式铁证**——开/关这栏各跑一次 `promote --dry`，**去掉该行后全部输出（含 stderr 的阻断结论）与退出码必须逐字相同**。
- **被自己的门抓到的两条错（记下来，这是本仓"文档必须能被机器验一次"的第 N 次复发）**：
  1. 解析器第一版"按空白切开再按下标数"⇒ 把 `band2` 的 **Δ 读成了跨种子极差**（`+23.5` → `+1.4`，恰好是个看起来像现役的健康数）。⇒ D155 ① 的固定样本回归是这个坑的盖子；改法是**锚到尾部的正则**（每粒种子列有 1~3 个数，下标必串位）。
  2. "这栏不影响判定"的检查最初只拼 `stdout`，而 `promote` 的**阻断结论走 stderr** ⇒ "两次相同"可以是假的（两边都提前失败也会相同）。⇒ 现在比较 `stdout + stderr`，并额外断言"这次 dry-run 真的走到了门结论"。
- **实测对照**（`cbs1s2-band2.bak`，`--dry --skip-gate-drafts`）：开栏/关栏**同为 exit 6**、去掉那一行后输出逐字相同；该栏印 `Δ(钱) +23.5pt · hold 均 37.2% · cycle 基线 13.7% · 极差 1.4pt ⇒ 钱驱动（高，≥18pt）`，而**拦住它的仍然是老的 `G 有效技能数 2.95 < 3`** ⇒ 以后任何人 promote 候选都会被迫看见这个数，但它不替用户做决定。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交、引擎与两槽与 `origin/main` 逐字节一致。未做：**没把这栏升级成阻断项**（要升级需先解决 §H-56 灰区 + §H-55"Δ 单轴放过水平病"，形状应是 `Δ × 水平` 两轴），没改当选键、没换槽、没改规则。`np-test 202/202` · `spec 52/52` · `smoke OK` · `battle OK` · 指纹 `ebdbff36`。
## v1.5.221 — E20：把 Δ 做成**三种子池化**后，中间带没收窄而是**7 粒挤在 +13.5~+15.6 的密集带** ⇒ "线放哪"不是测量问题；仪器本身已验证（极差 1~3pt、placebo 13/13）（Qoder · 工具面，**引擎/两槽/判据一字未动**）
- `tools/probe-defense-cause.mjs` 新增 **`--seeds=N`**（只服务 `--pair=1`）：同一粒包跑 N 粒种子，配对表改成**按计数池化**（不是把率求平均 ⇒ 池化后的分母与"跑 N×150 局"等价），逐粒印**每粒种子的 Δ 与跨种子极差**；判定三档 `≥18 高 / 12~18 不判定 / ≤12 低`，并印"带宽来自实测极差"。
  - **三条向后兼容自证**：① 不开 `--seeds`/`--pair` 时默认档输出与改前**逐字节一致**（七粒锚点重跑 + `diff`）；② `--seeds>1` 却不配 `--pair` ⇒ **exit 4 报错**（不许静默按单种子跑完装作跑了两种子，METHODOLOGY 53 同族）；③ `seeds=1` 退回单种子表、不印池化说明。
- **门 D154 扩到 13 组断言**（`np-test 201/201`）：新增"SEED0 两处都必须走 SEED_BASE"（漏一处就是"换了参数却没换种子"）/"池化 + 极差都要印"/"灰区宽度必须声明是实测的"/**真跑** `--seeds=2` 与 `--seeds>1 无 pair 必须 exit 4`。立门当场就抓到一条我自己写错的断言（要求输出里出现"不判定"，但单粒样本可能整表都没有灰区行 ⇒ 改成核对**表尾图例**）。
- **E20 读数**（13 粒 × 2 档 × 3 种子 × 150 局 · 产品口径 · placebo 13/13 全过）：`cbs1s2-band2` **+23.9**（极差 3.1）· `v7seat24-31` **+18.9**（3.4）· 然后 **7 粒挤在 +13.5~+15.6**（`v7b3s31-band3` 15.6 / `eco-34` 15.5 / `fw-persist-s2` 15.2 / `v7press-36` 15.2 / `fgtB-34` 14.4 / `v7big1-81` 14.3 / `v7l2s-115` 13.5，极差 0.9~4.8）· 低端 `v7cmin4-31`（现役）**+1.0**、`co1s7-band6` −0.3、`v7aim3-93` −1.2、`v7bead3-82` **−10.2**（压住钱反而更防）。
  - **原本假设被推翻**：我猜"`eco-34` 跨线 = 测量不稳，加种子能收窄"。加到三种子后极差确实只有 1~3pt，但**中间那 7 粒彼此相差不到 2pt** ⇒ 它们不是被噪声推开，是**本来就挤在一起**；再多种子也不会把"该不该拦"变成可测事实。⇒ **阈值是价值判断**（灰区不判 + 另一轴兜水平，是 §H-55 的形状）。
  - **仪器精度是够的**：两端干净（高 ≥+18.9 / 低 ≤+1.0，中间隔 18pt）⇒ "Δ 测不测得准"与"线放哪"以前被我混在一句"这条线落在噪声里"里，现在分开：**前者已确认，后者待裁**。
  - **`v7press-36` 是一粒活样本**：v1.5.27 真上线过的冠军，池化 Δ 只 +15.2，但 `hold` 均 **99.3%**、`cycle` 均 **84.2%** ⇒ 它的病是**水平**（几乎全程在防），Δ 轴对它轻描淡写 ⇒ 只装 Δ 会放过最龟的一类（§H-55 第 1 条的具体化）。
- METHODOLOGY 加 **65**（立阈值前先用跨种子极差买门票；报阈值必须同时报阈值附近的噪声尺度；灰区要显式印；多种子要按计数池化；两端至少各一粒做跨种子对照）。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交、引擎与两槽与 `origin/main` 逐字节一致。未做：**没装任何判据栏**（含"记录不判"形状接进 `promote --dry` 这一步——那是 DS 的出厂工具，等用户点头再动）、没改当选键、没换槽、没改规则。`np-test 201/201` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。
## v1.5.220 — E19（好包侧补测）：Δ≥15pt 在 26 粒"好包"上**零误伤**，但同一粒 `eco-34` 换个种子就跨线（+14.2 → +15.9）⇒ **这条线目前落在种子噪声里，不能立**（Qoder · 只读，**引擎/两槽/判据一字未动**）
- 补测动机是 §H-54 自己挂的 caveat（"好包侧只有 5 粒锚点撑着，我不拿它当推荐"）。取仓库记录里**过门/近过门/被点名为好**的 20 粒（`v7aim3-93`·`v7xn14a-band2`·`co1s7-band6`·`cbig1s2-band1`·`cbs1s4-band3`·`v7cmin3-31/82/93`·`v7new5_005-31`·`v7new6-94/96`·`v7press3-91`·`v7fix1-82`·`long-33`·`eco-34`·`v7r2-35`·`v7divK-81/82`·`v7cur2-31`·`v7bead3-82`），每粒 `--pair=1` 两档 × 150 局。
- **读数**：20 粒 placebo 全过（r1~r5 两档逐字相同）；**Δ 包络 −9.7 ~ +14.2，其中 19/20 粒 ≤ +4.0pt**，7 粒为负（压住钱反而更防 ⇒ 它们的设防与钱无关）。病例侧仍是 `band2` +22.0 / `seat24` +17.2 ⇒ 两端确实分得开。
- **但线站不住（本版最重要的一条）**：`--seed=4100` 读 `eco-34` Δ=+14.2（C 型），`--seed=7777` 读 **+15.9（跨到 B 型）** ⇒ Δ=15 这条约定线**落在贴线中部的种子噪声里**；同批对照里两端很稳（`band2` +22.0→+24.8、现役 +1.9→+0.5）⇒ **不稳的是中间带，不是两端**。
- 三条给裁定（只报测量，不推荐阈值）：
  1. **Δ 单独不能当判据**：`eco-34` 真正的问题是**水平**（`cycle` 基线就有 66.5%、连防 47、局长 47 的"恒龟"），Δ 是它身上最小的一维 ⇒ 只装 Δ 一栏会把档案里最龟的那一类（E14 的 95 粒）放过去。这给"两轴必须并联"补上机制版理由：**一轴测钱引起的增量，一轴测不分场合的水平**。
  2. **要立就得带灰区**：+12~+18 定为不判定带；收窄只有两条路——加大 `--games`（成本线性）或**多种子取平均**（3 种子 ≈ 2 分钟/粒，仍比一轮 ε 扫描便宜）。
  3. **"好包"是我从仓库记录取的**（过门/近过门/被点名），不是玩家满意度标签 ⇒ 这 26 粒里可能本就含未发现的病例，"零误伤"不等于"零争议"。
- 工具面顺带自证：`--pair=1` 在 20 粒批量上跑通并逐粒印 placebo 结论（D154 只钉了小样本单次跑，这次的批量跑是它的实测补充）。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交、引擎与两槽与 `origin/main` 逐字节一致。未做：没装/没改判据、没换槽、没改规则。`np-test 201/201` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。
## v1.5.219 — E15 落地：**`--pair=1` 现在是一条命令**（两档同跑 + placebo 自检 + A/B/C 分型）+ 门 **D154**；顺手量出七粒锚点的 Δ 分离度：**病例 +22.0/+17.2pt vs 五粒健康锚点 ≤+1.9pt，中间零重叠**（Qoder · 工具面，**引擎/两槽/判据一字未动**）
- `tools/probe-defense-cause.mjs` 新增 `--pair=1`：同一次调用里跑 `hold`（钱一路堆）与 `cycle`（堆到 4 就花光 ⇒ ep 钉在 0~3），末尾印**同回合配对表** = placebo 窗（r1~r5 两档必须逐字相同，不同就当场宣告作废）+ 净效应窗（r6~r14，两侧分母各 ≥20）+ Δpt + 倍差 + **A 钱驱动 / B 钱+晚局 / C 回合日程为主** 三型。
  - **为什么要内置**：§H-47/§H-53 那张表原先是我拿两份输出手工对齐的；手工对齐 = 可复现性为零，而这正是"能不能立一栏"的证据底座。
  - **向后兼容已自证**：不开 `--pair` 时输出与改前**逐字节一致**（七粒锚点重跑 + `diff` 验证 ⇒ 历史读数仍可复现）；`SAVER` 从 `const` 改 `let` 是这次唯一的结构性改动。
- **D154（新门，`np-test 201/201`）**：钉六条源码事实（有 `--pair` 档 / 两档在同一次调用里 / 曲线留档在内存 / placebo 自检会宣告作废 / 输出里留着"单档倍差不能当流行率"那句更正 / 分型线要自己声明是约定的）+ **两条真跑断言**（`--games=25 --pair=1` 必须 exit 0 且印出"✓ placebo 自检通过"与三档分型；默认档不许出现配对表 ⇒ 不许偷跑两档）。
  - 立门动因记在门里：09-25 那次"文案指向一个不存在的 `--pair` 档"（§H-47 抓到）属于**工具对自己撒谎**这一族，同 D123/D150。
- **锚点正式读数（150 局 · 产品口径 · 同种子）**：`cbs1s2-band2` **+22.0pt**（37.2%→15.1%，A 型）· `v7seat24-31` **+17.2pt**（51.9%→34.8%，B 型）· 现役 `v7cmin4-31` +1.9 · `v7new4-31` +1.5 · `v7cmin4-82` +0.6 · `v7divK-31` −0.0（全 C 型）。五粒健康锚点与两粒病例之间**15pt 无重叠**，`v7xn1-31`（与现役同权重的冗余对照）与现役两行逐字相同 ⇒ 顺手做了一次**同权重方差自检**。
- **两条必须挂的 caveat（免得这版被读成"我推荐了一条判据"）**：① **Δ≥15pt 是我为了分型取的约定线，不是数据给的线**（表里也这么印）——数据给的是分布：192 粒响应型上 Δ 的 p25 −0.2 / 中位 +4.1 / p75 13.2 / p90 23.3，Δ≥15 约落在 p86。② **好包那一侧现在只有 5 粒锚点撑着**，样本太薄 ⇒ 本版**不推荐阈值、不装栏、不换槽**；要立这栏，先补测好包侧的 Δ 分布，再由用户裁定线画在哪。
- 成本如实报：这栏 = 两档 × 产品口径 × 150 局 ≈ **40 秒/粒（两档合计）**，比现有任何一道门都贵（但比一轮 ε 扫描便宜）。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交、引擎与两槽与 `origin/main` 逐字节一致。未做：没装/没改判据、没换槽、没改规则、没动 `EPIRUS_SEL_LAND`。`np-test 201/201` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。
## v1.5.218 — E14/E16/E17 三批读数收口：全档案去重后体质率是 **28.9%**（不是 53.5%）、当选确实偏爱"局长短"（32 臂符号检验 p=0.0033）**但只偏 1.8 回合**、而 **3P 槽历史上真正上线过的冠军有 30.8% 带这体质**（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）
- **E14（§H-50 · 补 §H-46 那个 16% 的洞）**：1004 个去重等价类 × 150 局 × 产品口径全部量到（跳过 0）。**响应型 195 粒 = 19.4% [17.0~21.9] · 恒定龟 95 粒 = 9.5% · 并集 28.9% [26.1~31.7] · 判不了（高桶分母不足）87 粒 = 8.7%**。
  ⇒ v1.5.216 那组"并集 53.5%"里虚增的几乎全在"恒定龟"那一档（**已被 E18 修正，见下条**）（28.8%→9.5%），因为"每 2 取 1 + 按文件名排序"把 `chain*`/`kr*`/`ms*`/`champ*` 这些消融与课程家族抽爆了；**新上线的整棵 `v7*` 树（716 类）响应率 19% 但中位数很温**（倍差 0.81 / 连防 5 / 局长 20）。⇒ 已在 §H-46 挂"覆盖度作废通知"，并写进 METHODOLOGY 62。
- **E18（§H-53 · 本版当场自我作废一半）：上面那个"响应型 19.4%"是含混了回合轴的粗筛**。对 192 粒响应型逐粒再跑 `cycle` 档（同种子同局数，只把替身的钱压回 0~3）做**同回合配对**后：placebo 窗 r1~r5 两档 **100.0%** 重合（替身自检通过），但 Δ(hold−cycle) 设防率中位只有 **+4.1pt**、81.3% 的"响应型"Δ<15pt ⇒ **它们其实是"晚局龟"而不是"有钱才龟"**（`hold` 档里"对手 ep≥10"几乎等价于"已经打到 r25+"）。
  ⇒ 三条改写：**(a)** 配对后仍站得住的"钱驱动"体质 = **A+B 36 粒 = 3.6% 全档案**（Δ≥15pt 档；Δ≥5pt 是 9.2%），19.4% 只能当上界；**(b)** 若真装这栏，**两档替身都得跑**，单档会误伤 81% 的标记——这比 v1.5.217 那条"ε=0 不能免费代理"更进一步：**连产品口径的单档都不够**；**(c)** 用户抱怨的两粒在配对后**都成立**（`band2` Δ22.0pt 纯钱驱动 / `seat24` Δ17.2pt 钱+晚局双重），所以病是真的，但不该推广到整棵树。
- **E16（§H-51 · 给 E13 补功效）**：同一套配对代码搬到全档案 ⇒ 可配对臂从 8 条涨到 **32 条 / 170 粒**。当选粒在同臂里**局长显著更短**（24 低 / 7 高，精确符号检验 **p=0.0033**，Δ 中位 −1.80 回合）、`ep≥10` 设防率也偏低（p=0.0146），但**"倍差"这一维完全读不出来（p=0.42）**。
  ⇒ §H-44 那句"8 臂只是没测到"现在有了答案：**测到了，很小**——当选流程偏爱"收得快"，不看"是否因对方有钱转防"；而 1.8 回合的臂内差对上 19~20 回合的跨臂差（现役 20 / `band2` 39 / `seat24` 46），分布大面积重叠 ⇒ **这道梯度拦不住一粒带体质的包出货**（`band2` 本身就是过了三道改判出货的）。
- **E17（§H-52 · 回溯审计，本次最硬的一条）**：从 `git log -- js/bundled-champion-3p.js` 取 72 次提交、`git show` 出**当时槽文件内容**（工作树一字未动）→ 按权重去重得 **31 个真正上线过的 3P 冠军** → 逐粒跑产品口径体质筛。
  - **第一版读数错 18 粒，已当场抓到并作废**：只有 13 粒是 `FEAT_S=213`（今天的状态特征维），其余是老维 `62/122/123`——`unpack` 不拒绝它们，但拿今天的引擎解老维权重解出来的是**另一种行为**（v1.3.0/v1.3.1 两粒被读成"95% 全桶设防"）。⇒ 主口径改成**按特征维筛样本**，老维"只列不判"；这条进 METHODOLOGY（跨版本回测："解得出来"≠"解得对"）。
  - **读数（13 粒可比子样本）**：响应型 **4 粒 = 30.8%**（09-13 `v7press-36` 4.55×/`ep≥10` 98.6% · 09-15 v1.5.66 6.31× · 09-18 `v7seat24-31` 9.07× · 09-20 `v7cur-93` 2.83×）+ 恒定龟 1 粒（v1.5.31）+ 干净 8 粒；**现役（v1.5.174 槽 = `v7cmin4-31`）干净**（0.25×、连防 9、局长 20）。
  - ⇒ 三条给裁定：**(a)** 这体质不是候选意外，是**流水线复发事件**（四个不同生产时期各一次，线上率 30.8% 还高于档案率 19.4%——能上线的本来就偏"能打"）；**(b)** **现在没有换包动机**，这栏防的是下一次；**(c)** 最重那粒 `v7seat24-31` 当初是"六条全过、广度每项不输在位"上线的 ⇒ **"门不够严"是错的诊断，"门里没这一维"才是**。
- 另：E14 的 1004 粒表里找到 **5 粒"倍差>8 却局长<28"**（`v7ws2-91` 19、`v7ring-34` 16、`v7b-44` 23…），E17 又找到同型轻症 `v7cur-93`（倍差 2.83 / 局长 22）⇒ **单轴判据必然要么漏要么误伤**，"响应幅度 × 玩家侧代价"两轴并联第三次在生产史上被重证（与 §H-39/§H-50 一致）。
- METHODOLOGY 加两条：**62**（扫池先按等价类去重 + 必须报覆盖率；按文件名等距抽样会系统漏掉整棵树）、**63**（拿现成读数当免费代理之前先测**方向**——本例方向是反的，`按ジ%` 会优先误伤现役）。
- 产物卫生：**本版无新 `.bak` 落仓**（`git show` 出来的 31 粒历史槽文件写在系统临时目录，未进仓库）、`results/*` 只读未提交、工作树两槽与引擎与 `origin/main` 逐字节一致。未做：没装/没改判据、没换槽、没改规则。`np-test 200/200` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。
## v1.5.217 — 裁定①那一问（"设防持续性"能不能免费加）**答案是不能**：六栏 ε=0 镜像读数对体质的 ROC 全贴在对角线上，最好的组合也只有 TPR 21% / FP 16%；同批收 E13 配对版"分不开" + **一条覆盖度更正：198 粒筛表其实只覆盖全档案 16.1% 的等价类**（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）
- 新量具 **`tools/screen-cheap-predict.mjs`**（只读）：对同一批包跑门**已经算过**的 ε=0 镜像量（`densityProfile` 的 设防份额/伤害每回合/出手伤害份额/局长 + `chargeProfile` 的 蓄能每局/珠浪费），198 粒 × 100 局；与产品口径筛表**按名字对表**（⇒ 必须有 `--packlist=` 才能合并，本版一并加上）。
- **对表结论（194 粒同进两张表）**：六栏每一栏取"TPR − 1.5·FP"最优点，最好也只有 **出手伤害% ≤23 ⇒ TPR 60% / FP 24%**，`局长(ε=0)` 两组成员中位 **32.0 vs 32.2**（零信号）。**方向还是反的**：被产品口径判为龟的粒，在 ε=0 镜像里设防份额反而更低（49.3 vs 58.8）——机制也自洽：ε=0 五席同包互不犯 ⇒ 高设防率是常态不是体质。
  ⇒ 给裁定①的硬事实：**这一栏不可能零成本**，必须单独装配"1 席攒钱替身 + 4 席被评包"且必须跑产品口径。（成本反而很低：实测 ≈20 秒/粒@150 局。）
- **锚点直接证伪"拿现有栏当判据"**：现役好包 `v7cmin4-31` 的 `按ジ%(ε=0)` 是七粒锚点里**最高**的 79.3%，病包 `cbs1s2-band2` 只有 60.8%；而 `band2` 在 ε=0 镜像里局长只有 17.4 回合（看着最健康），玩家桌上是 39 回合 ⇒ **任何以 `按ジ%` 为键的判据会优先误伤现役**。
- **E13 交作业（§H-43 预注册 → §H-44 兑现）**：跨臂版三组 `trainFit` 中位 响应 1.209 / 恒定 1.175 / 干净 1.183（挤在 0.03 里），同臂配对版（8 条臂）连防 Δ 中位 +4.80、4 低 4 高、平均秩 2.50 = 零假设值 ⇒ 按预注册判据落"**分不开**"，且如实标注"8 臂只是没测到，不是测到没有"。
  - 顺带挖到更有用的一条：**"当选 == 臂内 `trainFit` 最高"8 条臂只成立 1 条**——读 `train-3p.mjs:627/687-768` 才知道带内最高之后还有三道改判（名人堂 `firstRate+0.5·top2Rate` 重排 · §N9 退化闸 `zeroAtkRate≥0.9` · `EPIRUS_SEL_LAND` 兑现广度）。⇒ "目标函数罚不罚龟缩"这个二选一被换成：**罚的机关存在，但三条键没有一条看"对手有钱时会不会转防"，所以拦不住**（`band2` 正是过了这三道闸出货的）。裁定①的形状因此偏向加验收栏，而不是改适应度。
- **覆盖度更正（§H-46 · 本版的自我作废）**：给锚点补测时发现 `v7xn1-31` 与 `v7cmin4-31` 每栏读数逐字相同 ⇒ 按权重向量去重：1449 粒可解参数的 `.bak` → **1004 个不同权重**，而 09-25 那份"198 粒普查"只覆盖 **162 类 = 16.1%**，漏掉的恰好是**整棵 `v7*` 树**。而漏掉的树里就藏着重病例：**`v7seat24-31` 倍差 9.07×、`ep≥10` 设防 73.3%、连防 44 回合、局长中位 46（p90 47 = 顶到上限）、平局率 27%**，比引发这次排查的 `band2`（6.43×/49.8%/37/39/2%）更严重。
  ⇒ **v1.5.216 里那三个比例（响应 24.7% / 恒定 28.8% / 并集 53.5%）的适用范围要读作"band/消融家族"**，不能当档案普查引用；"`kr*s*` 家族 0/6 免疫"这条反例可能只是抽样假象。全档案等价类筛（E14）已预注册并在跑，读数另开一版。
- **同回合配对正式读数（§H-47）**：两粒病例各跑 `hold`/`cycle` 两档（同种子 4100 · 150 局 · 产品口径）⇒ **ep 未分岔的 r1~r5 两档 5/5 回合逐字重合**（这副替身的自检），分岔后同一回合号上 `band2` hold 37.2% vs cycle 15.1% = **钱的净效应 2.46×**、`seat24` 51.9% vs 34.8% = **1.49×**；代价轴：局长 `band2` 39→15、`seat24` 46→20，`band2` 那个攒钱的人夺冠率 hold 13.3% → cycle **27.3%**。
  - 两粒**病因不同**：`seat24` 的 cycle 档自己就有 ~35% 的后期设防平台（= 回合日程型，钱只再乘 1.49），`band2` 的 cycle 档掉回 ≈现役水平（= 纯钱驱动）⇒ **只在"对手 ep"那一维加约束治不到 `seat24` 那一型**。
  - 另一条要给裁定用：**同一只包，替身档 `hold` 46 回合 / `cycle` 20 回合** ⇒ 玩家遇不遇到这个病，取决于真人是不是"一路攒钱不花"（§H-22 的实机记录才是这条的判据来源）。
- 抓到一条工具账：曲线行的文案印着"（`--pair` 就是比这条）"，但 `probe-defense-cause.mjs` **没有 `--pair` 这个档** ⇒ 指向不存在的功能；上面的对齐表是临时脚本手工做的，E15 要把 `--pair=1` 真做出来（含"ep 未分岔处必须重合"的自检）。
- 产物卫生：**本版无新 `.bak` 落仓**（临时清单/输出全在系统临时目录）、`results/*` 只读未提交。未做：没装/没改判据、没换槽、没改规则、没动 `EPIRUS_SEL_LAND`。`np-test 200/200` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。
## v1.5.216 — E12 大样本筛（198 粒 × 150 局）：**我上一夜口头推荐的"连防≥15 回合"这条线，没过自己跑前写下的标准**（TP 84% < 90%）；并集体质 **53.5% [46.6~60.5]**（响应型 24.7%、恒定龟缩 28.8%）（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）
- 预注册（§H-37）标准是 `TP≥90% 且 FP≤10%`。实测：连防≥10 → TP 88%/FP 7%；**≥15 → TP 84%/FP 2%**；≥20 → 82%/1%；≥30 → 65%/1%。
  ⇒ **FP 一路很好、TP 始终差一截**：响应型里有 8 粒"会转防但转得不长"（连防 <15）。⇒ 真要立这一栏必须**两栏并联**（连防长度 **或** 倍差/高桶设防率），单条不成立；同时**撤回**我上一版对用户的"连防≥15 能把两端零重叠分开"那句话（那是 45 粒极值给的错觉）。
- 三个比例的 95% 区间也一并给出（198 粒）：响应型 **24.7% [18.7~30.8]**、恒定龟缩 **28.8% [22.5~35.1]**、并集 **53.5% [46.6~60.5]**；另有 **17 粒（8.6%）高桶分母不足** ⇒ 它们的响应型判定不可用，不许算"无响应"。样本 45→198 后区间从约 ±14pt 收到约 ±7pt。
- 顺带**修了自己两个刚写出来的 bug 并各加一条跑通断言**（本夜第 6、7 处自我更正）：给扫池工具加"先认货再装载"时，`probe-defense-cause` 与 `probe-breadth-flip` **都漏了 `readFileSync` 的 import** ——
  前者被我自己那个**宽 catch**吞成"200 粒全读不动"且**退出码 0**（差点把崩溃当成"没体质"），后者直接崩（`node --check` 只抓语法抓不到 ReferenceError）。
  ⇒ 改法：catch 只认 `e.code`（编程错误一律上抛）· 一粒没量到也**点名跳过数并非零退出** · D150 里补"`probe-breadth-flip` 必须跑得通 + 必须有点名跳过"的断言（这道断言正是这类错误的兜网）。
- 另：扫池工具的"认货"过滤发现的 **`docs/artifacts/*.bak` 里混着 2 个 index.html 备份**（1451 个 .bak 中 2 个非包）⇒ 已给 DS 一条小账（非包备份建议换后缀，D82 与扫池工具都会再撞）。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交。未做：没装/没改判据、没换槽、没改规则。`np-test 200/200` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.215 — 门 **D153**（新）：把"产品口径"这个词钉成**两个**（5 人槽 ε=0.2/k5/soft、**2 人困难槽其实是 ε=0**），因为本夜所有口径结论的唯一凭据就是 `ui.js` 那两行代码（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）
- 立这条门的当场它就抓到**我自己文档里的洞**：我在日志/记忆里写了整晚"评测口径 ≠ 产品口径"，但 `docs/METHODOLOGY.md` 里**从来没有**那句关键区分（"评测口径 = 2 人口径 ≠ 5 人口径"）⇒ 断言红了一次，补进第 54 条才绿。
  ⇒ 同族老账第 N 次复发：**"写给自己的文档"必须能被机器验一次**，否则它只是我的记忆。
- 钉四件事：`ui.js:464` 的四参数字面量 · 2 人困难槽 `pickChampion(state, 1, legal, c, 0.15)`（那里有刻意注释"播放口径：与训练口径一致"）· METHODOLOGY 必须留着"两槽口径不同"这段 · D118 代理栏必须继续显式带 `0.2/'soft'`。
- 同批收工杂项：E9/E10 的 11 粒当选包归置到 `docs/artifacts/e9-out/`（`.gitignore:27` 的 `*-out*/` 覆盖 ⇒ 不入库、D82 只扫顶层不递归）；§H-35 给待裁件 (iii) 的数据答复（同一格 n=70 偏 +2pt 但 **95% 带是 ±11pt**，旧账 `co1s8-band1` G5[multi] n=60 读 28% FAIL → n=300 读 22% PASS 全在带内 ⇒ **该印 ±1.96SE**）；§H-36 产物归置；§H-33 记录 `BEAD_W` 已被 DS 四档三 seed 跑尽（我本来要开的那条臂因此取消）。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交。未做：没装/没改判据、没换槽、没改规则。`np-test 200/200` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.214 — 新量具 `tools/probe-bead-loop.mjs`：**"珠浪费率高"这一个总量里藏着两种相反的病**（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）：现役是 **② 买得起却没射 68.1%**（选择），`v7cmin4-82` 是 **① 下一回合买不起 83.3%**（经济）——同一个"浪费"指向两层完全不同的修法
- **拆法**：在**被评席下一次决策的那一刻**看它的合法表里电磁炮买不买得起（不重建状态），把"蓄能后的下一次决策"分三桶：① 买不起 / ② 买得起却没射 / ③ 买得起也射了。60 局 · 5 席镜像 · ε=0：

| 包 | 样本 | ① 买不起（经济） | ② 买得起没射（选择） | ③ 射了 | 真源 `chargeProfile`（按珠子计） |
|---|---|---|---|---|---|
| 现役 3p | n=385 | 1.0% | **68.1%** | 30.9% | 浪费率 48.2%（得珠 382 / 过期 184） |
| `v7cmin4-82` | n=1617 | **83.3%** | 11.7% | 5.0% | 浪费率 88.2%（得珠 1811 / 蓄能 30.2 次/局） |

- **② 里它到底在干了什么**（直接印出来）：现役 262 次里 **256 次在按 ジ**、6 次又蓄能 ⇒ 不是珠被别的卡抢走，是**纯粹继续攒**；`v7cmin4-82` 是 ジ 143 / 蓄能 35 / 聚能环 11。
  ⇒ 这把 §H-27 与 §H-1（大雷"可达时成交率 0.0%"）接成**同一条病**：**它把"攒"当成了动作本身**，所以"空蓄能"和"空攒钱"不是两条。
- **两条口径卫生**（都是我自己踩过的）：① 本表**按决策计**、真源**按珠子计** ⇒ 天然不等，所以读数旁边并排印真源那行并写明"**不许拿一个去校验另一个**"（我第一版看到 48% vs 68% 以为算错，差点把发现当 bug 删）；
  ② 窗口必须先判 `beadAlive`（珠只留一回合，而"下一次决策"可能隔两三回合）⇒ 那批单列 `beadGone` 不混分母；第一版没判，造出过 8 次假"买不起"。
- **E9（§H-26，超出交接的第四件）跑完并否掉我自己的预注册**：四格配方（基线 / 只上桌 / 只兑现键 / 都开 · seed 77 · 300 代）实测**全部无响应**（上桌两臂倍差 0.54）⇒ `EPIRUS_COUNTER_OPPS` **不是**这体质的来源，交接推荐的那根旋钮不必因此加反作用读数；
  同时 `sp77a`≡`sp77c`、`sp77b`≡`sp77d` **权重逐字节相同** ⇒ `EPIRUS_SEL_LAND` 在 seed 77 上一次人也没换（与 09-24 记的咬合率 3/6 同源）。⇒ §H-21 那 32% 的体质**不在我能自由摆的这两根键上**，而 META 不记 env 让归因**卡住**（这条实验的价值在此）。
- ⚠️ 同一条记录里还写了我一次**近失错误**：中途两次路径拼装炸掉、其实没跑出汇总表，我却把别处的数字（3.73，来自 `co1s6-band1`）串成了"本次结果" ⇒ 规矩硬化为"**读数只能来自带表头的那一次输出文件**，跨文件串数字 = 造数"。
- 门 **D152**（新）：钉这把尺别长回一个总量 —— 复用 `build`/`chargeProfile`（不许第二份实现）· 必须有 `beadAlive`/`beadGone` · 必须写明"按决策计 vs 按珠子计不许互相校验" · ② 必须印"实际出了什么卡" · 三桶互斥。
- 产物卫生：**本版无新 `.bak` 落仓**（E9 臂的 band 文件全在 `mktemp`，复测量用的副本放在 `node_modules/.e9/`）；`results/*` 只读未提交。未做：没装/没改判据、没换槽、没改规则。`np-test 199/199` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.213 — 把这条病落到**玩家会遇到什么**上：换 `cbs1s2-band2` 不会让你更难赢，**会让你赢得更无聊**（120 局 · `saver=hold`：局长中位 20 → **41 回合**（p90 46，几乎撞 47 上限）、只攒钱那位夺冠率 **0.0% → 13.3%**、同席连防最长 9 → **35 回合**）（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）
- `probe-defense-cause.mjs` 加"玩家侧代价"一行：局长中位/p90、平局率、**攒钱者夺冠率**与游程长度并排。加这一行的理由是防误读：**只有设防率的报告会被读成"AI 变弱了"**，而实际发生的是它**开始奖励龟缩**（与 METHODOLOGY 48"空爆/空防"同族，这次在对手侧）。
- ⚠️ 三条边界写在读数旁边：① 极端装配（1 席纯攒 + 4 席同包），真实局里 4 席不同包、人也会打 ⇒ **两档之间的差**才是可用读数；② 平局率两档都是 3% ⇒ 别把"局长 41"讲成"永不结束"（最后是终局收缩收的）；③ 攒钱者夺冠 0.0%（现役）本身说明这装配的题出得偏难 ⇒ 绝对值不当结论。
- 门 D151 加一条钉：必须印玩家侧代价那一行。§H-23 落账。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交。未做：没装/没改判据、没换槽、没改规则。`np-test 198/198` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.212 — 那个响应**不是 band2 的孤例，是今晚这批臂的体质**：最近 44 粒里 13/41 个不同权重（**32%**）有"对手有钱→转防"（倍差最高 10.6×、连防 47 回合），而线上包只有 0.16×（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）
- `probe-defense-cause.mjs` 加 `--quiet=1`（一粒一行 + 末尾按倍差降序的跨包汇总表）与 `--saver=hold|cycle` 两档；扫最近 44 粒臂包（60 局/粒）。
- ⚠️ **本条的 32% 是低估，以 v1.5.214 之后的 §H-30 / METHODOLOGY 60 为准**：倍差这把尺只对"响应型"有效，另有 14 粒**对手 0~1ep 时就已经设防 20~97%** 的"恒定龟缩型"被它读成"无响应"；两类合并 **27/45 = 60%**。同一条修正也撤回了"这体质会奖励龟缩（攒钱者 0%→13.3%）"的家族版表述（三类攒钱者胜率中位都是 0%，那 13.3% 只是 band2 一粒的特性）。
- **为什么这个区分值钱**：个例 ⇒ 换候选；体质 ⇒ 换候选没用（再抽一颗骰子还有 1/3 概率同样体质），要动的是训练场或判据。代价差一个量级，而**单粒包的读数永远说不出比例**。
- **配套两条卫生**：① 扫之前**剥壳去重** —— 44 粒其实只有 **41 个不同权重**：`cbs1s3/5/6` 与 `cbig1s3/5/6` 是三对逐字节相同的包（sha1 `43e904e3`/`9c97bcb6`/`b2f0776e`，与 09-24 记的 `EPIRUS_SEL_LAND` 咬合率 3/6 同源）⇒ 不去重会把 32% 报成 34%，还会把"两粒包都有病"当两条独立证据；
  ② 汇总表必须并排印三桶分子分母（`ep0~1 → ep5~9 → ep≥10`）+ 连防长度，只印倍差就会有人拿短局行（n=41）与长局行（n=9000）比大小。
- 顺带得到的：**这张分布就是将来"设防持续性"那一栏线该画在哪的底稿**（倍差 2？连防 5？），有分布就别拍脑袋。今晚"两槽不动"的结论不变，这张表不是判据、也没装进任何判据。
- 文档：METHODOLOGY **58**（抓到病先扫全池问个例还是体质 + 去重与三桶并印）；日志 §H-21，并把 H-15/H-16/H-17 的钟点改成读表值。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交。未做：没装/没改判据、没换槽、没改规则。`np-test 198/198` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.211 — 给 v1.5.210 那条因果补上**安慰剂窗口**与"挨打"对照（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）：两档在对手 ep 分岔之前**逐字重合**（r4 12.1% / r5 16.6%，两档同为 619/650 个决策），分岔之后才分开；只取"没掉过血"的子样本后上升照旧（`ep≥10` 51.8% → **52.3%**）
- 补的正是 v1.5.210 那条读数**还欠的两件事**：① `cycle` 档的对手每几回合会真打出一发 ⇒ 两档差值可能是"挨过打才防"而不是"看见钱才防"；② 没有"分岔前重合"的证据 ⇒ 两档的任何差都可以是装配本身的差。
- **①的做法**：加第二条同格式曲线，只统计"**自上次自己决策以来一点血都没掉**"的那些决策（决策时直接读 `state.p[pid].hp` 的净变化，记 `unhurt`）。结果 `cbs1s2-band2`：
  `ep2~4` 11.9%→9.6%、`ep5~9` 33.0%→**32.7%**、`ep≥10` 51.8%→**52.3%** ⇒ 掉血与否几乎不动读数，那条上升不是"被打出来的"。
- **②的做法**：两条曲线并排印出来做**分岔前/分岔后**对照。`cbs1s2-band2`（没掉血子样本 · 括号内为该回合对手平均 ep）：
  `r4 (3.0/3.0) 12.1% vs 12.1%` · `r5 (4.0/4.0) 16.6% vs 16.6%` ⇒ **逐字重合**；
  `r6 (5.0/3.0) 29.7% vs 15.8%` · `r8 (6.9/3.0) 37.2% vs 14.8%` · `r10 (8.8/3.1) 38.4% vs 11.3%` · `r12 43.3% vs 14.9%` · `r14 45.2% vs 14.9%` ⇒ **分岔恰好发生在 ep 分岔处**，1.9~3.4 倍。
  同一套配到**线上包**：`r6 5.8% vs 7.5%`、`r8 7.9% vs 5.7%`、`r10 5.5% vs 3.5%` —— 互有高低、无系统方向；桶那一侧 `7.9 → 6.2 → 5.3 → 1.6%` 单调**下降** ⇒ 线上包没有这个响应（v1.5.210 的结论加固，未翻）。
- ⚠️ 两条边界照实写进读数：`cycle` 档打到 r12+ 的局本来就少（n 从 619 掉到 282）⇒ 后段倍差分母薄；且只在"1 席攒钱替身 + 4 席同一只包"这一种装配里成立，真实 5 人局 4 席未必同包 ⇒ 早报口径是"**这粒候选在产品口径下有这个响应、而门看不见**"，不是"玩家一定会遇到 41 回合连防"。
- 门 **D151** 加两条钉：必须有"没掉过血"子样本曲线（`unhurt`/`LASTHP` 在）· 必须与主曲线同格式，好做分岔前后对照。METHODOLOGY **57** 补"配对之外还要一个安慰剂窗口"那一节。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交。未做：没装/没改判据、没换槽、没改规则。`np-test 198/198` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.210 — **用户实机那条病找到了，而且凶手是一粒候选不是线上包**：`cbs1s2-band2` 有"对手有钱→我就防"的响应（同回合配对 2~3 倍、设防率最高 51.8%、同席连防最长 **41 回合**），线上包同场 1.6% / 9 回合（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）
- **新量具 `tools/probe-defense-cause.mjs`**（只读，`import { build }` 复用 v1.5.207 的口径搬运）：装配 = 1 席"攒钱替身" + 4 席被评包，按对手**当下手里的 ep** 分桶读设防率，并单独读**同一席连续设防最长回合数**（"维持很久"是游程长度问题，设防率答不了）。
- **两档替身是为把因果那一问做对而加的**：`hold`（钱一路堆 ⇒ 第 r 回合 ep≈r−1，**ep 轴与回合轴重合**，单档只能得到"越晚越不防"）与 `cycle`（堆到 4 就全花掉 ⇒ ep 被钉在 0~3）。配对结果：
  `cbs1s2-band2` 在第 6/8/10/12/14 回合，`hold` 设防 **29.8 / 38.1 / 38.1 / 42.8 / 44.8%**，`cycle` **14.8 / 17.0 / 12.6 / 16.4 / 19.0%**，而两档在 ep 还没分岔的第 4~5 回合**逐字重合**（15.3% / 19.8%）
  ⇒ **分岔恰好发生在 ep 分岔处**，这才是"看到响应"。同一套配到线上包：两档各回合都在 3~8%、无系统差 ⇒ **线上包没有这个响应**。
- **跨包读数**（产品口径 · 200 局 · `ep0~1 → 5~9 → ≥10` 设防率 / 同席连防最长 / ≥3 连的局占比）：
  现役 `7.9% → 5.4% → 1.6%`（下降）/ 9 回合 / 29.0%；**`cbs1s2-band2` `7.9% → 33.0% → 51.8%`（6.6 倍）/ 41 回合 / 90.5%**；`cbs1s5-band1` `→ 18.7% → 20.9%`（2.7 倍）/ 8 / 77.0%；
  `co1s8-band1`、`v7aim3-93`、`v7cmin4-82` 全是**下降**一档（连防 4~5 回合）。⇒ **病在 `cbs1s*` 这一族（`EPIRUS_SEL_LAND=1` + 上桌臂的产物）身上**。
- **机制是自增强回路**：4 席同一只"响应型"包 + 1 席只攒不打 ⇒ 没人能打穿 ⇒ 局长飙到 25+ 回合 ⇒ 攒钱席 ep 一路涨 ⇒ 设防率涨到 51.8% ⇒ 更打不穿。
  ⇒ 用户那句"维持很久"其实被**低估**了：连防能到 41 回合 ≈ 整局。**因此今晚候选结论翻转一半**：`cbs1s2-band2`（我 01:00 还记它"四栏最均衡"）与 `cbs1s5-band1` **不加换槽建议**，两槽照旧不动。
- **实机侧交叉验证（只读用户自己的 `results/*`）**：设防占比 `results/band2` **22.5%**（单席最多 52 次，6 份记录）· `results/31` 19.0% · `results/93` 0.8% ⇒ 用户报病那局测的正是 band2，方向与合成场一致。
  ⚠️ 实机记录**不当判据**（人类对手、局少、口径混杂），只当"不是只在模拟里看到"的旁证。
- **门 D151 被自己的第一版测死了（好事，如实记）**：它原本去验"一回合内 5 条 `action` 全先于结算事件"，实测**一局 64 次结算落在 action 之前/之间、只有 13 次干净分界** ⇒ 假定不成立；
  而我第一版从事件流前缀和重建"对手 ep"**只加不减**（花了不扣）⇒ 量的是"一辈子挣过多少"。改成**决策时直接读 `state.p[0].ep`** + 回合号用 `state.round`（实测单调）。
  现在 D151 钉的是：桶变量必须实读（禁 `epAtRound` 那类重建）· 两档替身必须在 · 必须印"按回合号曲线 + 同一回合号配对"· 每桶并排印平均回合（让轴重合看得见）· 分母下限与自适应合并 · 空桶印 `—（n=0）` 不许印 0.0% · 游程长度单独印。
- 文档：METHODOLOGY **56** 就地改写那条被我写错的"桶口径"（假定→被自己的门测死→实读），新增 **57**（"单档装配答不了因果"与配对设计）。
- 产物卫生：**本版无新 `.bak` 落仓**、`results/*` 只读未提交。未做：没装/没改判据（"镜像格加设防持续性"只写进早报请裁）、没换槽、没改规则。`np-test 198/198` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.209 — 把用户那句"碰到攒钱的就疯狂防御并且维持很久"拆成**两个各自可判的问题**（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）：① **因果方向不支持**（设防率随对手 ep 反而下降：9.3% → 4.6% → 3.4%）② **"很久"= 同席连防 4 回合、只在产品口径出现**（ε=0 那边恒 1 回合、≥3 连 0.0%）＋ 新量具 `tools/probe-defense-cause.mjs` ＋ 门 **D151** ＋ 一处**自我更正**（v1.5.208 我"倾向换成净兑现"，被锚点表否掉）
- ⚠️ **本条已被 v1.5.210 就地推翻一半**（同一夜、同一批量具修完口径之后）：那句"因果方向不支持"是在**错误的 ep 读数**（事件流前缀和，花了不扣）与**单档装配**（ep 轴与回合轴重合）上得到的；修正后配对测量证明 **`cbs1s2-band2` 确实有"对手有钱→就防"的响应**，只是**线上包没有**。⇒ 以 v1.5.210 为准。
- **新量具 `tools/probe-defense-cause.mjs`**（只读）：装配 = 1 席"攒钱替身"（买得起就出攻击卡、否则出ジ ⇒ ep 单调涨）+ 4 席被评包；按**该回合开始时**对手累计 ep 分桶读设防率，并读**同一席连续设防最长回合数**。口径搬运继续 `import { build }`（同 D150 的单一来源纪律）。
  分桶口径的依据是**实测事件序**：同一回合内 5 条 `action` 全部先于 `ep/damage/heal` 结算 ⇒ 事件流前缀和 = 回合开始值（门 **D151** 把这条不变量钉成功能测试，引擎若改成交错必先红）。
- **两个问题各自的答案**（现役 3p · 产品口径 · 200 局 · 出手事件 8167 次）：
  ① 对手 `ep0~1` 设防 **9.3%**（n=2431）→ `ep2~4` **4.6%**（4772）→ `ep5~∞` **3.4%**（964）⇒ **没看到正相关**，"他攒钱所以我防"这条因果在这只包里找不到（`cbs1s2-band2` 是平的：6.9/8.3/7.4/8.9%）；
  ② "维持很久"= 全表最长**同席连防 4 回合**、≥3 连只占 **4.5%** 的局；而 ε=0 对照那边**最长 1 回合、≥3 连 0.0%、设防率 0~0.8%** ⇒ 与 H-12 并起来：**"疯狂防御"与"维持很久"都是运行时探索的产物，门禁那一侧结构性看不见**。
- **两处方法缺陷我先犯了再修**（写在读数旁边，不偷偷重跑）：① 最高桶只有 n=20 却被算成一个数（4 次中 1 次看着像 20.0% 的发现）⇒ 加 `--bucket-min`（默认 40）+ **从最高桶往下自适应合并** + 行尾印"该桶分母 < 40"；
  ② `ep 桶` 与 `回合数` 是**同一条轴**（替身只攒不花 ⇒ 第 2 回合≈ep0~1、第 12 回合≈ep5+）⇒ 每桶并排印**该桶平均回合**（2.1 / 8.1 / 12.4），并把结论强度从"看到负相关"**降到"没看到正相关"**；真要分离需要一个 **ep 会振荡的对手**（下次的活）。落账 METHODOLOGY **56**。
- **自我更正（推翻我自己上一版的倾向）**：v1.5.208 我写"倾向 (b) 把广度门换成净兑现"。给 `probe-breadth-flip.mjs` 加上**分位数 + 两端锚点**后（165 粒）：净兑现挂 3 在 ε=0 挡掉 **150 粒（91%，线在 p90=2.92 之上）**，而且会**挡掉已知好的 `v7divK-31`（净兑现 2.23）**，与已知退化的 `cbig1s1-band4`（1.77）只差 0.47 ⇒ **"先要能分开已知好与已知坏"这条资格它没过**（附录 B2-6 / C-4 同规）。
  对照 `G ≥ 3`：ε=0 挡 85 粒（52%，线正好压在中位数 2.90 上）、产品挡 8 粒（5%）。⇒ 真相是**这两列在任何单一口径上都分不开两端，差别只在"挡多少"**；
  有数据支持的选项变成：**先固定口径，再把线改成"该口径下档案分布的分位数"**（ε=0 的 p50=2.90 天然把 `cbig1s1-band4`(2.12) 与 `v7divK-31`(3.06)/`aim3`(3.81) 分开）。**属判据改动 ⇒ 只写进早报请用户裁，门一条没动。**
- 门 **D151**（新）：钉"一回合内 `action` 必须先于结算事件"这条**量具立足的引擎不变量** + 分母下限/自适应合并必须在 + 空桶印 `—（n=0）` 不许印 0.0% + "平均回合"与混淆声明必须印。
- 产物卫生：**本版无新 `.bak` 落仓**。未做：没装/没改判据、没换槽、没改规则。`np-test 198/198` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.208 — **`G<3` 这条广度门线挡掉的档案包，53% 是"口径挡的"**（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）：160 粒档案包双口径复量 ⇒ 被挡 **84 粒 → 8 粒**、跨界 **76 : 0**（单侧）；同一条线搬到产品口径只剩 **5% 阻挡率 = 没有判别力** ⇒ 结论不是"换口径"，而是"这条线该换成与探索无关的定义"
- **新量具 `tools/probe-breadth-flip.mjs`**（只读、不落盘）：`docs/artifacts/*.bak` 共 1451 粒、每 9 取 1 得 160 粒 · `mirrorHealth` 60 局 × 5 席（确定性 ⇒ 每格一次）· 门线 `G≥3`。**搬运手法复用 v1.5.207 的 `build()`**（`import { build } from './probe-layer-caliber.mjs'`），不写第二份；为此把 `probe-layer-caliber.mjs` 改成**被 import 时不跑 main**（与 `behavior-profile.mjs` 同规）并导出 `build`（顺带返回 `hardwired`，让复用方的自检用现算数量而不是写死 4）。
- **实测**：`G(multi)<3` 被挡 **84 粒（53%）→ 8 粒（5%）**；`G(long)<3` **93 → 4**；净兑现 `<3` **148 → 117**；跨界 **"ε=0 挡 → 产品过" 76 粒 : 反向 0 粒**，ΔG 中位 **+2.51**（157 正 / 3 负）⇒ 方向**完全单侧**，预注册 ①"系统性偏严"成立。
- **但把它写成"所以该换口径"就错了**：搬过去之后阻挡率只剩 5% ⇒ 一条几乎不挡人的线与一条挡一半人的线**都不是判据**。三个真选项（全属判据侧，等用户裁）：
  **(a)** 留在 ε=0 把线抬高；**(b)** 换成**与探索无关的广度定义**（净兑现两口径都还严：148→117 —— 本夜数据里唯一两侧都有分辨力的那把尺，**我倾向这条**）；**(c)** 双口径并报、两栏都过才算。
  ⇒ 落账 METHODOLOGY **55**：**换判据必须同时报"端点（现役）+ 全池分布（多少粒被挡）+ 跨界方向计数"，缺一样就会把"失去判别力"读成"修好偏严"**。
- **同一个坑长在我自己的另一件量具上**：`probe-kill-reward.mjs` 的 Chooser 也写死 `policyChooserN(params, 0.15)` ⇒ 加 `--temp/--eps/--epsk/--epsmode`（默认 `0.15/0` ⇒ 历史读数逐字不变，实测改前后 `bundled-champion-3p` 那行 `0.0%/0.00/0%` 一字不差）。
- **用这个开关重读 E3 的 G7「破龟群」草案**（同种子配对 · 120 局 · 破防场）：现役 ε=0 读 `1st 0.0% / 归因伤害 0.00 / 终场存活 0%`，产品口径读 `90.0% / **4.63** / **43%**`
  ⇒ 草案"现役必红"在两口径下**都成立**（不会被口径翻掉），但**红的原因只剩一个**：**`伤害≥4/局` 不是分辨栏，`终场存活≥50%` 才是**（三粒候选两口径都是 79~93% 存活 / 5.78~10.28 点）。
  ⇒ 若将来真立这条门，**只立存活那一栏**；另记 `破防 1st` 的分辨力大部分是 ε=0 给的（现役 0%→90% 后与候选只差 8~10pt），这是 E5 那条"绝对值要打折"的第三次确认。
- **门 D150 加钉三条**：复用方必须 `import { build }` 且不得自己写 `HARDWIRED`/`runInNewContext`（同一手法只许一份）· 量具被 import 时不得跑 main · 正则与期望数量必须同一份常量（现算）。
- 预注册卫生：`probe-breadth-flip` 第一版在 n=3 的前置检查上就打印"②两向差不多"这种荒唐话（分母只有 3 粒）⇒ 补 `n<30 或被挡<10 ⇒ 判样本不足`。
- 产物卫生：**本版无新 `.bak` 落仓**；日志时间戳改用读表值（H-12 00:52 / H-13 01:12 / H-14 01:13）。未做：没装/没改判据、没换槽、没改规则。`np-test 197/197` · `spec 52/52` · `smoke OK` · 指纹 `ebdbff36`。

## v1.5.207 — 超出交接的一条（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）：把**整个评测层**搬到产品口径后，**五道门 2/4 粒结论相反**；而用户实机报的"碰到攒钱的就防很久"在评测口径下**结构性不可见**（设防率 0% vs 6%、最长连设防 1 回合 vs 4 回合）
- **新量具 `tools/probe-layer-caliber.mjs`**（只读、不落盘、仓库一字不改）：口径靠两条内存手法搬 —— ① 装载前把 `evo.js` 源码里 4 处 `policyChooserN(params, 0.15)` **在内存里**换成带 `temp/eps/epsK/epsMode` 的版本；② `audit-lib.mjs` 是 ESM 不进气泡，但它调 `W.EpirusTrainer.policyChooserN` ⇒ **装载后把沙箱里那个属性包一层**（并原样透传已传 ≥3 参的调用）。
  两条路**必须自证生效**：印"写死处实测 N 处 → 替换了 N 处 / 经包装调用 M 次"，任一不成立 `exit 9` 作废整张表 ⇒ 防的是"两栏一样被读成口径无关"。期望数量**从源码现算**，不冻结常数（第 52 条那条账）。
- **五道门（`feasibilityOf` 同一份纯函数，两口径各算一遍 · 120 局/格）**：`co1s8-band1` 从 **✗ `G 2.89<3`+场B 0.01** 变成 **✅ 全过**（G 4.52 · 场B 0.37）；`cbs1s2-band2` 的三条 fail 里有**两条广度 fail 消失**（只剩场B）；现役则从 ✅ 全过变成 ✗ 座位偏座（20.8pt ≥ 线 19.2）—— 但这一条是**单次抽签**（`probe-seat-caliber` 的配重复带宽 [5.0~24.0]）⇒ 只当"要去配重复核验"的信号。
- **方向性结论**：**广度族在 ε=0 系统性偏低** —— 4/4 粒 `ΔG(multi)` 全正（+1.45 ~ +4.40）、净兑现同向（+0.37 ~ +1.01），因为 `G` 数"非ジ出手的种类/熵"而**探索本身就制造种类** ⇒ `G<3` 这条线在评测口径下对**窄型候选偏严**。
  **但不是整体变松**：现役同一批种子 反弹墙伤害 **12.17 → 5.05**（−59%）、蓄能 **4.99 → 2.09 次/局**（珠浪费率 19%→31%）在降，`G(multi) 4.24→6.30`、`G(long) 3.30→5.39`、场B 0.45→1.19、密度 0.441→0.889 在升 ⇒ **要搬口径必须按列重标定线**（D145 对座位栏做过同样的事），"整体换口径"这个说法本身是错的。
- **对到用户 09-24 实机观察的那条**：`fieldRate('active')`（1 席"只会攒钱+出攻击卡"的替身 vs 4 席被评包）—— 现役设防率 **0% → 6%**、**最长连设防 1 → 4 回合**、无威胁时设防率 0%→7%；`cbs1s2-band2` **2% → 8%**、连设防 1 → 4。
  ⚠️ **这一栏五道门与 D118 的产品代理栏都不看**（代理栏测的是真桌出招份额）⇒ "防御维持很久"不是门太松，是**门里没有这一格**。这条进了早报第一条。
  另记一格以前没量过的：**口径 × 装配 = 2×2**，我此前所有广度结论都在"镜像装配 + ε=0"那一格里。
- **门 D150**（新）：钉这份量具的**搬运证据链**而不是某个读数 —— 只读（无 `writeFileSync`）· `exit 9` 自检必须在 · 期望数量现算（禁止 `patched !== 4` 这种冻结）· 包装层必须透传 ≥3 参调用 · 同时印"门的输入"与"门不看的设防持续性"· **两栏至少一列必须不同**（否则测量没打开）· **`--eps=0` 对照下两栏必须逐字相同**（差异只能来自口径）。
- **自我更正一条（写日志时抓到）**：我在 §H-12 初稿里引了两个没跑过的数（`G(long) 4.04→2.08`、以及一处"probe-field-baseline 的 12.2%→34.1%"）—— 现算/复跑后真值是 **3.30→5.39**，而那个工具我根本没跑过 ⇒ 已就地改数并写明"未引用"。落账第 52 条的同一条雷：**注释与草稿里的数字也要当场复跑**。
- 产物卫生：**本版没有新 `.bak` 落进仓库**（D82 无需点名）；臂的 band 文件仍写在 `mktemp` 目录。
- 未做（不越权）：没装/没改任何判据（`G<3` 偏严这条只是**量出来**，要不要按列重标定属用户裁定）、没换槽、没改规则。`np-test 197/197` · `spec 52/52` · `smoke OK` · 指纹仍是 `ebdbff36`。

## v1.5.206 — E7 收尾（Qoder · 只读与工具面，**引擎/两槽/判据一字未动**）：**座位极差在两口径下判定相反**，而真正的原因是**分母**（ε=0 下判胜只有 26~30%）＋ 新量具 `tools/probe-seat-caliber.mjs`（配对 + 与真源自检）＋ 门 **D149**
- **E7 第一半（座位口径）· 实测**（5 粒包 × n∈{60,100,200,400} × 3 个 seed 基址取中位数 · multi · 5 席）：
  - **n 的噪声压得住**：v1.5.202 那条"线随 n 标定"（26.7 / 21.0 / 14.5 / 10.3pt）把 20 格里 18 格判住了；n=60 同一粒包换 seed 基址能从 10.9pt 跳到 27.3pt ⇒ **交接 §4-E7 的"一律 n=400"是对的且必要**。
  - **口径压不住**：两栏在同一条线上**判定相反**的 2 格全在 `cbs1s2-band2`（今晚四栏最均衡的那粒候选）——n=60 评测 **30.8pt ⛔** vs 产品 **12.1pt ✓**、n=100 评测 21.4 ⛔ vs 产品 12.2 ✓。
  - **机制是分母不是偏置**：它在 ε=0 的**判胜率只有 26~30%**（正好压在 `audit-lib.mjs:415` 的 `decisiveRate<0.3 ⇒ 'unjudgeable'` 线上抖）⇒ 极差算在不到 1/3 的局上；同批种子在 ε=0.2 soft 下判胜 98%、极差 3.1pt（n=400）。现役在 n=400 两栏 6.2 / 5.5pt ⇒ **不在争议区**。
  - ⇒ 与 E5/H-6 同源的那句话第三次成立：**"平局多"的包，评测层的每道门都在小分母上读数。**
- **E7 第二半（示范覆盖复量）· 实测**（`train-3p.mjs 40 3 12 16` · seed 31 · 与门 D134 同一套 env）：覆盖席 `{0:265,1:110,2:122}` = **53/22/25**，
  历史两档 59/26/15（v1.5.186 修后）与 60/23/17（09-23 夜）⇒ **0 号席支配度在降、1/2 两席已由"44:33"变成"110:122"**，v1.5.186 那批修的东西没有回潮（D134 的 ≥3 席、单席 ≤80% 远未触线）。
  但**密度天花板原封不动**：`tries=3910 / fired=497 = 12.7%`、未开火原因 87% 是 `被 only 过滤`（教师无动作 0、买不起 0）⇒ **Q-14 ①（抬密度）仍然只卡在 `IMIT_ONLY`/`IMIT_BETA` 这两块板子上**。
  顺带一条 E1 的**独立确认**：本臂 `bigT` 出手 `{imit:497, native:728}` 而 `nativeEcon` 只有 **2 次**（0.16%）⇒ 训练桌上与评测桌上"大雷不来自原生经济"是两条量具、同一个结论。
- **`probe-ideal-champion` 的【5】【6】现在在输出里自报口径**（源码里有、输出里没有 = 读表的人看不见）：那两条走 `seatSymmetry`（`audit-lib.mjs:403`）/`mirrorHealth`（`evo.js:2554`），
  内部各自 `policyChooserN(params, 0.15)` ⇒ **完全不消费 `--temp/--eps`**，ε 扫描时逐字不变。
  ⚠️ 我第一版把这件事写成"给 DS 的新账（13 处写死）"，随后查 `v1.5.152` 发现 **DS 早就有意为之并已有裁定**（METHODOLOGY 49 第 499 行、门 D118 的"产品代理栏"）⇒ 已当场降级为**自我更正**：
  真正剩下的只有半句 —— 产品代理栏目前**只覆盖"真桌出招份额"那一族**，**座位/广度/G4/G5 还没有产品口径的第二栏**。要不要补一条"只记录不阻断"的座位代理栏属**判据侧**，**列进早报请用户裁，我没动**。
- **新量具 `tools/probe-seat-caliber.mjs`（只读，不落盘）的方法学**：`seatSymmetry` 不收 Chooser 注入 ⇒ 按它逐行复刻一份可注入口径的版本，
  跑之前先在 n=100/`seed0=12000` 上比对 5 席**回胜利计数**，不等就 `exit 8` 作废整张表（实测 `10/18/16/16/20` 逐字相同）；两栏**配对**（同 seed 序列、同 `slotSalt`）⇒ 差值只来自口径。
  修它自己的过程中踩到一次**假零带宽**：`--reps` 换的如果只是重复同一写死 seed 序列，噪声带会打成 `[15.2~15.2]` ⇒ 噪声带要靠换 `seedBase`，落账 METHODOLOGY **53**。
- **门 D149**（新）：钉"两个旋钮必须分开 + 调用点真的传四个参数 + 【5】【6】在**输出里**自报口径 + 那两行在 ε 扫描下必须逐字相同（这条相等本身就是写死的证据）"。
  钉法当场抓到我自己一处错：按旧笔记写 `evo.js:403`，实测 `mirrorHealth` 的写死处在 `evo.js:2554` ⇒ 门红，改成实测值（**行号要实测，别抄注释**）。
- 产物卫生：本臂 band 文件全写 `mktemp` 目录（`EPIRUS_BAND_DIR`），仓库只多了一个 `.gitignore:10` 已覆盖的 `docs/artifacts/train-3p-out.js` ⇒ **本版没有新 `.bak` 落进仓库**（D82 无需点名）。
- 文档：`METHODOLOGY` 第 **53** 条；`RESEARCH-LOG-2026-09-24-qoder-handoff.md` 加 **§H-9/H-10/H-11**，并在文件头**更正那批时间戳**（H-1…H-8 的钟点是估的不是读表的，最多超前 ~2.5 小时；次序可信、绝对时刻不可信，此后每条先 `date`）。
- 未做（不越权）：**没装**任何新门（G7 破龟群仍是草案）、**没换**槽、**没改**规则。`np-test 196/196` · `spec 52/52` · `smoke OK` · 指纹仍是 `ebdbff36`。

## v1.5.205 — 按 DS 交接跑的一晚（Qoder）＝ **三条口径级纠正 + 一个新量具**：① 大雷不是"够不着"，是"**可达时成交率 0.0%**"（同场电磁炮 32.0%）② 交接 §2 的"破防场 0%胜/100%平"是 **ε=0 评测口径**，产品口径（ε=0.2 soft）下现役在那格 **87.5% 胜** ③ 广度读数**把空净化算成一张有效卡**（挡掉 112 次空转后 `G 3.83→2.95`，从过广度门变成被它挡）＋ 修掉 `probe-g4-anatomy` 那个每次跑都自我否定的过期期望值 ＋ 门 **D148**

> **跑的是 `docs/HANDOFF-FOR-QWEN-2026-09-24.md` §9 的顺序，只改一处**（E3 的前置我上午已量完 ⇒ 直接进"门草案"，省出的时间给 E4/E5）。
> 全文 `docs/RESEARCH-LOG-2026-09-24-qoder-handoff.md`（§H-1~H-7），规矩落账进 `docs/METHODOLOGY.md` **第 49~52 条**。
>
> ### ① 新量具 `tools/probe-ep-reach.mjs`（E1+E2 · 只读 · 门 D148）
> 现役 · multi · 5 人 · 200 局/场，决策点 n=1736 / 9400 / 4871：
>
> | 场 | 决策时刻 ep 均值 | `ep≥5` | 大雷**真放出去** | 电磁炮**可达时成交率** | ep 支出结构 |
> |---|---|---|---|---|---|
> | 脚本池 | 0.81 | 0.1% | **0.0%** | 100%（n=8，太小） | 狙击 58.3% · 枪 37.0% |
> | 破防场 | 0.49 | 0.0% | 0.0% | 卡根本不在合法表 | **枪 100.0%**（ep 永远停在 0~1） |
> | 镜像场 | **8.23** | **44.8%** | **0.0%** | **32.0%**（n=299） | 狙击 40.2% · 枪 23.2% · 蓄能 22.1% · 电磁炮 13.2% |
>
> ⇒ **交接 §3.1"99% 的决策时刻够不着门槛"只在脚本池/破防场成立**：镜像场 `ep≥5` 占 **44.8%**（改价到 2ep 则 72.2%），
>   而大雷**真放出去始终 0.0%**；**同一批决策里电磁炮的可达时成交率 32.0%** ⇒ 决定性读数是"**可达时成交率**"而不是"可达率"：
>   **不是钱不够，是这档钱宁可攒着也不放大雷**（同场同钱的另一张贵卡它会用）。
>   ⇒ **改价买不到大雷**（单价 5→3→2 三档，成交率一直 0.0%）—— 我 §E1 预注册押的就是这个方向，押中了。
>   ⇒ 破防场另给出一条机制：收入 4800ep、支出 4600ep **100% 花在枪上**，`ep` 永远 0~1、"选 ジ 时枪买得起"= **0.0%**
>     ⇒ 它跑的是 **`ep=0→ジ、ep=1→枪(必被挡)` 的极限环**，结构上攒不到穿防的钱。
>
> ### ② E5：评测口径 ≠ 玩家看到的口径（这条改写了交接 §2 第 3 条）
> 接 ε 扫描时先踩坑：**`policyChooserN` 的第 2 个参数是温度、第 3 个才是探索率** —— 第一版扫温度读数纹丝不动
> （`0.47/0.43/0.43/0.44`），差点对整夜写"ε 悬崖不存在"（METHODOLOGY 第 50 条）。换成真杆 + 带 `epsK/epsMode`：
>
> | 口径 | 破防场平局率 | 受评席绝对胜率 | 终局存活 | 镜像 电磁炮/序列 次/局 |
> |---|---|---|---|---|
> | `eps=0`（**门禁/评测**口径） | **100.0%** | 0.0% | 0.0% | 0.43 |
> | `eps=0.2, k=5, soft`（**产品**：`ui.js:464`） | **12.5%** | **87.5%** | 40.8% | 0.18 |
> | `eps=0.2, uniform` | 44.2% | 55.8% | 9.2% | 0.26 |
> | `eps=0.4, k=5, soft` | 3.3% | 96.7% | 48.3% | 0.08 |
>
> ⇒ 「现役在破防场 0% 胜 / 100% 平 ⇒ 它不会清场」**只是 ε=0 的读数**；玩家看到的是 87.5% 胜。
>   门禁用 ε=0 是有意的（可复现、评包本身），**错的是把评测口径的绝对值写成产品事实** ——
>   我上午那一整条破防叙事（§K-4~K-11）与交接 §2 都栽过一次；**相对排序不受影响**（所有包同口径量）。
> ⇒ 悬崖真实存在（序列 0.43→0.18→0.08），但同一条杠杆另一头是**反拖平**；且**形状比大小值钱**（同 ε=0.2：soft 12.5% 平 vs uniform 44.2% 平）
>   ⇒ v1.5.141 那次"降 ε + 加 `epsMode='soft'`"的裁定，今晚第一次在破防场上量出价值。
>   ⇒ **交接 §0 的"两条来路（改价 / 改判据）"漏了第三条：运行时探索的形状与大小。**
>
> ### ③ E6 复测顺手抓到一个**广度量具的洞**
> `cbs1s2-band2` 在修好的引擎上重跑 `promote --dry`：`✅ 候选自己全过`（G4[long] 33% · G5 1%/0%）＋ 2 格 `UNRUN:弱`
> （DS 的 v1.5.203 处置法判为"强包形状"，只记录）—— **但被 `G 有效技能数 2.95 < 3` 阻断**，
> 而修闸前它是 multi 5.16 / long 3.83（过），净兑现 `3.25(5 种) → 2.88(4 种)`。
> ⇒ **`G`/`净兑现` 数的是非ジ出手的种类与熵 ⇒ 一次"清除 0 枚"的空净化也算一张有效卡**（这是本节写下这条时的猜测）——
>   **随后同一夜被自己否证，见 §⑥**；`cbs1s2-band2` 的 `G 3.83→2.95` 实际是**行为真变窄**，不是"计数少一张卡"。
>
> ### ④ E4（交接清单外我加的）＋ 修掉一个自我否定的量具
> - `probe-g4-anatomy.mjs` §A 的期望值硬写 **75%/62%**（那是 v1.5.144 **之前**那件线上包），现役同装配实测 **38%/35%**
>   ⇒ **每次跑都打印「⛔ 复现失败 ⇒ 下面的读数先别读」**：装配没错、读数没错，**是自检把自己废掉**（而门还把这两个数字逐字钉住）。
>   已修：期望值换成在位包现值 + **旁边写明"哪件包 / n / 日期"**，门钉改成"必须写清来历 + 不许再引用历史数字"（METHODOLOGY 第 52 条）。
> - §C 反事实（60 局/格）：`forceAttack`（买得起枪就别攒着）对 **`v7divK-31` 是 80%→15%（long）/ 62%→23%（multi）** —— 我预注册押"掉不到 60% 以下"，**被推翻**；
>   但同一招对**现役是 38%→45%（更差）** ⇒ 不是"该装的规则"，是**每只包弱点各不相同**；
>   且 `v7aim3-93` 的「只枪」只有 7% 却被「瞄威胁者」打 73% ⇒ **修一格不等于修一族**（min-over-counters 仍是正解）。
> - §D：训练池的 `pickGunSpam` 与门禁的「只枪(打最肥·击杀优先)」在**同一只包上差 −37pt ~ +17pt（方向随包变）**
>   ⇒ 这正是我昨晚"加了上桌仍死在只枪 77~84%"的机制解释：**池子那条线 ≠ 门禁那条线**。
>
> ### ⑤ E3 的交付物 = **一条门草案，未安装**（安装属判据改动）
> `G7「破龟群」`：1 席被测 + 4 席 `pickGuardSpam` · multi · 5 人 · 240 局 · **判据 = 有归因伤害 ≥4.0 点/局 且 终场存活 ≥50%**；现役进**参照行**。
> 判别力（实测）：124 粒当选带里 **22 粒（18%）过线**；反例 `v7cmin4-82` 读 1st 100% 却只有 2.48 点 / 14% 存活，
>   `cbig1s1-band4` 读 1st 100% 而 G5 里一只龟能赢它 86~89%
>   ⇒ **`破龟群 1st`（下限）→ `归因伤害/终场存活`（幅值）→ `G5`（反向装配）三条一起读才立得住**。
> ⚠️ 装它之前要接受：按**产品口径**现役在那格本已 87.5% 胜 ⇒ 这条门的价值只在评测口径上成立，写进门的时候必须标 ε。
>
> ### ⑥ §E 广度"含多少空转"——**这一条推翻了我自己上一条的猜测**（`METHODOLOGY` 51.1）
> 空转一律**按事件**判（`purify.curses===0` / 该回合无 `reason==='天火'` 的伤害 / `beadExpire`），镜像 40 局：
>
> | 包 | 空净化 | 空天火 | 蓄能珠过期 | G 原始 → 扣空转 | 差 |
> |---|---|---|---|---|---|
> | 现役 | **0** | **0** | 270 | 4.15 → 4.14 | +0.3% |
> | `cbs1s2-band2` | 0 | 0 | 4 | 2.99 → 2.92 | 2.1% |
> | `co1s8-band1` | 0 | 0 | 4 | 2.63 → 2.58 | 2.1% |
> | `v7aim3-93` | 0 | 0 | 0 | 4.03 → 4.03 | 0.0% |
> | `v7cmin4-82` | 0 | 0 | **744** | 4.83 → **5.79** | **−19.9%（扣了反而升）** |
>
> ⇒ 三条把上面 §③ 的猜测改写成事实：
>   ① 空转对广度的贡献 **≤2.1%**，不是主因；② **`G` 对"去掉重复动作"不单调**（熵被高频重复压低，去掉就回升）
>     ⇒ **「扣空转后再套 `G≥3` 门线」这个改法不成立**，方向不可控；要治空转得另立一列（空转出手占比）或走 §N24 的命中加权口径；
>   ③ 净化/天火全 0 是 **v1.5.199 与 v1.5.139 两道菜单闸的回归证据**，而**空转真正的大头是蓄能珠过期**（现役 270 颗 / `v7cmin4-82` 744 颗）——那正是第 48 条与"第六道珠经济未闭环"在说的事，**与广度无关**。
> ⇒ 门 D148 因此新增两小条：空转判据必须来自事件（不许养第二份卡名清单）；`G 原始 → 扣空转` 必须**并排印**，
>   否则人会默认"扣了必然变小"（实测会涨 19.9%）。
>
> **只读与红线**：本版只动 `tools/`（新量具 + 两处工具修正）与 `docs/`；**引擎与两槽一字未动**、`results/*` 未提交、规则指纹仍 `ebdbff36`；
>   未跑 `train-best`、未跑非 `--dry` 的 promote。`np-test 195/195`（新增 D148）· `spec-run 52/52` · `smoke OK`。


## v1.5.204 — 把「理想冠军」压成 **7 个能跑的读数**（新量具 `tools/probe-ideal-champion.mjs`）＋ 现役逐条实测：**6 条未达标**，而它们都是**同一个最优解的侧面**（方法学记入 `docs/METHODOLOGY.md` 第 46 条）

> **缘起**：用户 2026-09-24 与千问分析出「理想冠军」7 条规格。它此前几轮都以**愿望表**结束 —— 因为是用形容词写的。
> 本版把它压成**能直接跑出来的读数**，并当场量了现役 `v7cmin4-31`（`multi`，每场 200 局）。
>
> **前提（用户的分析，本版的出发点）**：5 人局按"**活着的人里血最多者胜**"结算 ⇒
> **"少挨打、让别人互相消耗、用便宜卡补刀"就是理性的**；进攻越猛越容易被挡、被别人捡便宜。
> **今天所有失败读数都是这个最优解的不同侧面**，不是"训练没到位"。
>
> | # | 性质 | 可验收阈值 | 现役实测 | |
> |---|---|---|---|---|
> | 1 | 会为大招攒钱（跨期选择） | 原生 **大雷 ≥0.15 次/局** | **0.00 次/局**（镜像 / 脚本池 / 只枪场 / 会防场 **四场全 0**） | ✗ |
> | 2 | 按对手姿态换线（条件性，不是 spam） | def 与 nodef 场 **1st 差 ≤5pt** | **枪 21.0%→2.2%（Δ−18.8pt）· 蓄能 0.2%→15.2%（Δ+15.0pt）· 电磁炮 Δ−0.1pt** | ✗ |
> | 3 | 会清场，不是拖平 | 破防场 **受评席 ≥50% 胜** | **0.0% 胜 / 100% 平局 / 终场存活 0%** | ✗ |
> | 4 | 能完成 ≥2 回合序列（蓄能→下一回合电磁炮） | **>0.5 次/局** | 镜像 **0.42**（有序列的局 32%）· 脚本场 0.01 | ✗（贴线） |
> | 5 | 座位无偏 | **极差 ≤10pt** | n=100 **12.5pt**（✗）· n=400 **6.3pt**（✓） | 半 |
> | 6 | 广度当约束不当目标 | **净兑现 G≥3 且 ≥4 种打上血** | 出手 G **4.21**（9 种）而**净兑现 G 2.63**（4 种 / 4881 次落地） | ✗ |
> | 7 | **不要的两条** | 不再往 gFit 加第 6 项 | ① 造成过伤害的卡只有 4 张、**3 张从未被挡过**（只有 `gun` 被挡过）⇒ "怕拦截"不是成因；② 镜像场**设防率 0.5%**（26/4871 次决策）⇒ "逼防"基本不存在 | 已确认 |
>
> **第 2 条的口径本轮被当场修正**（这条最值钱）：原按"def 场与 nodef 场的 1st 差"量，结果
> `4×纯防御` 场实测 **100% 平局**（谁都打不死谁）⇒ 1st **不可判**；而"2 防 + 2 枪"场 100% vs "4×只枪"场 5.4%
> 那 94.6pt 差是**难度差、不是换线**。⇒ 改看**出招分布位移**：包**确实对姿态有反应**，
> 但换的方向是"**少打多充**"（枪大降、蓄能大涨），不是穿透线（电磁炮几乎不动）。
>
> **结论（用户的判断，与仓里历史一致）**：**1 和 2 已被四次证否"用奖励面能买到"**
> （`BLOCK_W` / `WIDTH_W` / `BIGCARD_W` / `CHAIN_W` 两种形状 + 示范族 + 补贴率）——
> 它们都在**"挑池子里已有的东西"，挑不出池子里没有的**。所以理想冠军只有两条来路：
> **改价**（规则侧：ep 收入或大雷费用 ⇒ 让"攒钱"这条路真的通）或 **改判据**（把破防场 / 被压制场纳入主判）。
>
> **新增**：`tools/probe-ideal-champion.mjs`（**只读**量具，7 条各一项，参数 `--pack/--games/--mode`，可复跑）；
> `docs/METHODOLOGY.md` 新增**两条**：
> - **第 46 条** —— 「理想冠军」规格 + 逐条读数 + 三条方法学收获（规格必须可量化 / "1st 差"不是万能的姿态判据 /
>   判据的样本量要与线匹配）。
> - **第 47 条** —— **今晚门禁/度量层工作的复盘：8 条"会复发的形状"**（每条都带当晚实测数）：
>   ① "传了没人读"这一族要**单一来源 + 铺到每个入口 + 不许手抄名单**（`KILL_REWARD` 是第 9 例）；
>   ② 第三态（UNRUN）**必须有处置语义，且按方向分**（弱=只记录 / 强=阻断 / 缺=阻断）；
>   ③ **"被打穿"必须优先于"不可判"**（实测抓到 UNRUN 盖住真 FAIL）；
>   ④ **静默跳过 = 把测量关掉**（D58/D59 改成红）；
>   ⑤ **线要与样本量匹配**（座位 43.2pt@n=60 vs 26.6pt@n=400；固定 30pt 线在 n=400 会放它过去）；
>   ⑥ **从算式出发不实测会错在量级**（当晚三条自我更正：抬 n 的真实代价 50 秒 / "两把尺子"其实是两只包 / `maxDraw` 是死门不是误杀）；
>   ⑦ **删除判据必须是"真实边"**（注释与文档里的字面提及不算引用；反斜杠路径骗过正则，差点误删两个）；
>   ⑧ **账本判据要问反方向**（D82 只查单向 ⇒ 误删 6 个被点名的产物而门禁全绿）。
>   外加一条**流程**规矩：**后台长任务不要挂在前台等、中途不汇报、最后汇报一次**（用户当晚指出两次）。
>
> **回归**：`np-test 194/194` · `spec-run 52/52` · 规则指纹仍 **`ebdbff36`**（本条不动引擎）· 两槽线上包与 `results/*` 未动。
> **红线**：未训练、未 promote、未落盘新 `.bak`。

## v1.5.203 — UNRUN 终于有**处置语义**（原来它从不进 `fails` ⇒ 基线退化的候选可**零阻断通过**）＋ 实测抓到它**盖住真 FAIL**：`UNRUN` 把"基线那格不可判"与"候选被打穿"混成了一格

> **来的路**：门层审计的头号关切 —— `tools/promote-champion.mjs` 把 `UNRUN` 单独收集、醒目打印"不得当作通过"，
> 却**从不把它送进 `fails`**，全靠一句 print 兜着；而门（D67）只钉了那句话的**文本**。
> 用户批准"试一下"。
>
> **① 先量（12 个真实候选包 × `GATE4_GAMES=60` 扫描）**：UNRUN 其实**很罕见**（12 个里只有 1 个产生），
> 而且**全是"基线 0% ⇒ 候选压着克制表打"这一个方向**。
> ⇒ **无脑阻断是错的**：它会挡住"强到克制表量不出来"的候选 —— 而本仓为"误挡强候选"栽过
> （G5 在 n=60 把 `co1s8-band1` 按 28% 挡掉，n=300 复测 4%/22%，见 v1.5.195）。
>
> **② 但同一次扫描抓到一个更严重的东西：UNRUN 在盖住真 FAIL。**
> G4 的可判窗口 `[8%,32%]` 把两件不相干的事混在一格里：`judgeable` 说的是**基线那格**有没有判别力，
> 而这条门真正的 headline 是 `worst[1]` —— **最克它的那一行脚本赢了几成**，这与基线可判性**无关**。
> 实测 `v7xn8c-31-band4`：multi 格最克脚本 **65% > 60%**（真 FAIL），却因为基线 0% 而整格报 UNRUN
> ⇒ **旧口径下它零阻断通过**。
>
> **③ 修法（两件）**
> - **被打穿优先**：`hardFail = worst[1] > G4_MAX` 先判；`isUnrun = !hardFail && !judgeable`。真 FAIL 不再被盖。
> - **UNRUN 带方向、各有处置**（新 `tools/unrun-policy.mjs`，纯函数单一来源）：
>
>   | 种类 | 含义 | 处置 |
>   |---|---|---|
>   | `UNRUN:弱` | 基线 <8%：候选压着克制表打 | **只记录**（阻断会把最强候选挡在门外） |
>   | `UNRUN:强` | 基线 >32%：候选打不过最弱的克制脚本 | **阻断** |
>   | `UNRUN:缺` | 读不出 / NaN | **阻断**（同 `audit-lib` 的探针缺失守卫） |
>   | 任意 + **参照行** | 线上包 / 元测试 | 只记录（与参照行的 FAIL 同待遇） |
>   | 未知种类（旧格式） | — | 保守判红 |
>
>   `promote-champion` 现在把该阻断的送进 `fails`，并且打印**分开标**【阻断】/【只记录】——
>   不再是一句含糊的"不得当作通过"。
>
> **④ 实测复核**：`v7xn8c-31-band4` long 格 → `UNRUN:弱`（最克 58% ≤ 60 ⇒ 只记录）、
> multi 格 → **`FAIL`**（65% > 60，不再被盖）；现役包两格仍 `PASS`（38% / 35%）⇒ D67 的 `refPassG4` 不受影响。
>
> **⑤ 新门 D147**：纯函数 6 组合（弱/强/缺 × 候选/参照）+ "三处必须真接通"的静态钉 +
> **hardFail 优先级**（UNRUN 不许盖住真 FAIL）。`np-test` **193 → 194**。
>
> **代价**：零新增运行成本（gate-drafts 只多一个后缀；promote 的判定是纯函数）。
> **红线**：未训练、未 promote、两槽线上包与 `results/*` 未动、未落盘新 `.bak`。

## v1.5.202 — 判据级四条按实测定案：G4/G5 默认样本量 **60→300**（+35 秒）· G6 逐包行降级为**记录** · **座位极差线改为随 n 标定**（固定 30pt 在 n=400 会**放走**真偏置包）· G4 `--force` 例外判定抽成纯函数**并被门覆盖**

> **来的路**：用户裁定「后面的东西先测量再研究」。四个只读测量脚本（进程内真引擎）的结论在
> `docs/REVIEW-2026-09-24-criteria-measurement.md` 与 CHANGELOG 的 `measure(0924)` 条目里；
> 本轮把其中**四条**落成代码（用户批准 ①②④⑤ 并在座位那条上要求"保留判定、能弄得更好就调"）。
>
> **① `tools/gate-drafts.mjs`：`GATE4_GAMES` 默认 60 → 300。**
> 依据：**参照行就是线上包自己** —— 同一只包、同一场地、同一代码，基线读数在 n=60→300 之间摆动 **7pt**
> （long `27%→20%`、multi `10%→13%`），阈值处标准误 `5.59pt → 2.50pt`。原先"真值压在阈值的包有 43.1% 被判 FAIL"
> 不是推演：本仓曾据此把一粒**头对头更强**的候选按 28% 挡掉，n=300 复测 4%/22%（v1.5.195 / `b728545`）。
> 代价**实测**（不是估算）：整支 gate-drafts `N4=60 → 15 秒`、`N4=300 → 50 秒`；D67 的子进程超时 600 秒 ⇒ 12 倍余量。
> ⚠️ 阈值一个字没动（25%/60%），只是把样本量给够。**D67 自己仍显式传 `GATE4_GAMES=60`**（跑得快）⇒
> 顺手加一条静态钉把**默认值**钉住，防止哪天被静默改回去而没人发现。
>
> **② G6 的逐包行：`gate()` → `记录`（不再是 PASS/FAIL）。**
> 实测两档（n=60 与 n=300）里 `G6[线上包]` **都是 FAIL**（16.7% / 18.5%）、**与 n 无关**；四代包（含线上）全部 3.5~22.5%
> ⇒ 是"能力未长出"，不是某包退化（`gate-drafts` 自己的结语就这么写）。而"每次都红"的代价是真实的：它会训练读者忽略红行。
> ⚠️ **`G6[元测试]` 保留为门** —— 它证明量具有判别力（91% vs ~0%），那是真的门。promote 的 parser 只把 G6 记进 `recorded`，不受影响。
>
> **③ 座位极差线：固定 30pt → 随 n 标定（该 n 下均匀零分布的分位，默认 p99）。**
> 用户明确**要保留这条判定**（"以前经常训练出有严重座位偏置的冠军"）。查了 CHANGELOG/文档，这条线上真实发生过三次：
> **73.5pt**（`docs/REVIEW-3P.md:29` 破防脚本三座 1st 率 73.5/14.5/0.0）、**43.2pt**、**26.6pt**；
> 而 **v1.5.69 修的正是"座位惩罚从未触发（我自己的错）"** ⇒ 判据失准在这里是**复发过**的病。
> 旧固定线有两个毛病：
> (a) **不灵敏** —— 它坐在零分布的最大值上（n=100 的 2 万次零分布 p99=20.0 / **max=30.0**）⇒ 对 20~30pt 的"中等偏置"没有判别力；
> (b) **更危险的是会漏掉真偏置** —— 那只被换掉的 `v7new5_005-31` 在 n=60 读 43.2pt、**n=400 只读 26.6pt**
>     ⇒ 固定 30pt 线在 n=400 **放它过去**。而本仓一直在抬 n（D59 走 20→60→400）⇒ 这是活的风险。
> 新口径不是"放宽/收紧阈值"，而是让阈值**跟着样本量走**（n 越大，真偏置的读数与噪声同比缩小）。
> 种子固定 ⇒ 判定可复现。实测锚点：`n=50 → 30.0` · `n=100 → 21.0` · `n=200 → 14.5` · `n=400 → 10.3pt`。
> **判定演练**：三个历史读数在各自的 n 上都被抓住；现役 `v7cmin4-31` 三档都 `ok` 且有余量
> （n=100 读 **12.5** / 线 21.0；n=200 读 **10.3** / 线 14.5；n=400 读 **6.3** / 线 10.3）。
>
> **④ `tools/gate4-exception.mjs`（新）：G4 `--force` 越线例外的判定抽成纯函数。**
> 原来这段判定只活在 D67 里，且被 `refPassG4` **短路** —— 只要在位包的 G4 有一个模式 PASS（现在的常态）它就**从未被求值**。
> 也就是说它的**第一次真正运行**会发生在"正要发一只过不了门的冠军"的那次提交里：最不能出错的时刻，却也是它唯一没被验证过的时刻。
> 现在它是纯函数，**D146** 用合成 meta 把每种组合逐一跑过。冻结豁免 id `caecc92f` 同时收成单一来源（原先 np-test 手抄一份）。
>
> **⑤ 把嵌在 `t('D26 …')` 里的 D33 块提成独立用例 `D33b`**（搬运 34 行，判定一个字没改）。
> 它失败时会报成 `✘ D26 网络形状必须被钉住…`，把读者指向网络形状常量 —— **而那是对的**。这是"名字与内容不对应"的典型。
>
> **新门**：**D145**（座位线随 n 标定：锚点区间 + 对 n 单调 + 三个历史读数必须被抓住 + 现役包必须有余量 + 不许回到裸 `spread >= 30`）、
> **D146**（G4 例外判定：9 种合成 meta 组合 + 静态钉"D67 必须真调那个纯函数"）。
>
> **回归**：`np-test 190 → 193`（+D33b/D145/D146）· `spec-run 52/52` · 规则指纹仍 **`ebdbff36`**（本条只动 `tools/`）·
> 两槽线上包与 `results/*` 一字未动。**红线**：未训练、未 promote、未落盘新 `.bak`。
>
> **没做的那两条（连理由一起记）**：`HEALTH.maxDraw` 的 n **不动** —— 实测它是**死门不是误杀**（门的口径是 `allAliveTied`，
> 现役包 0/800；我先前引用的 49.7% 只在"真值恰好=0.20"时成立）。UNRUN 的处置语义（结构性，要三处同步 + 重记例外）
> 留待下一版。

## measure(0924 · DS · 只读测量 · **不升版本** · 未 promote) —— 判据级的四条实测：G4/G5 抬 n 只要 **+35 秒** · 座位极差确认死支 · `maxDraw` 是**死门**不是误杀 · `minG` 才是真阻断的那条

> 用户裁定「后面的东西先测量再研究」。四个只读测量脚本（进程内跑真引擎，不训练、不落盘、不改文件），
> 全文见 `docs/REVIEW-2026-09-24-criteria-measurement.md`。
> **结论推翻了本仓（含我）先前三条基于推算的判断**，逐条记在下面。
>
> **① G4/G5 的 `n=60`：确认是"掷硬币"，而且修起来只要 +35 秒。**
> `GATE4_GAMES` 是 env 可覆盖的 ⇒ 跑的是**门上同一条代码路径**，不是自己复刻的 harness。同一只包 `v7cmin4-31`：
> **参照行基线**（= 线上包自己）在 n=60 → 300 之间摆动 **7pt**（long `27%→20%`、multi `10%→13%`）；
> 阈值处 SE `5.59pt → 2.50pt`。而现役包自己离两条线都很远（G5 读 0% vs 线 25%；G4 最克 38%/35% vs 线 60%）
> ⇒ **临界风险属于候选包，不属于现役包** —— 这正是 `b728545` 那晚一粒**头对头更强**的候选被 n=60 的 28% 挡掉的原因（n=300 复测 4%/22%）。
> 代价实测：`N4=60 → 15 秒`、`N4=300 → 50 秒`（D67 给它的子进程超时 600 秒 ⇒ **12 倍余量**）。
> **⚠️ 更正**：我在 v1.5.201 条目里写「抬 n 会顶爆 D67 的子进程超时，所以更该走区间判」——**那是没实测的估算，收回**。
> 不需要"区间判 + 续采样"那套复杂逻辑，改 `tools/gate-drafts.mjs:215` 一行即可。
>
> **② 座位 `spread ≥30pt` @ n=100：死支确认；而「两把尺子」那条是我写错的。**
> 均匀零假设（5 席全 `eps=1.0` 随机，n=100 × 24 次）mean **11.2** / p95 17.1；现役包 mean **11.6** / p95 17.9
> —— **统计上分不开** ⇒ **现役包没有任何可检出的座位偏置**；而包里记的 12.5pt 只是中位数（零假设中位数也在 11 附近），
> **从来不是"均衡"的证据**。2 万次蒙特卡洛下 n=100 的零分布 p99=20.0 / **max=30.0** ⇒ 30pt 坐在零分布最大值上。
> **⚠️ 更正**：我写的「同一个量有两把尺子（`meta` 12.5 vs D59 的 26.6）」**不成立** —— 26.6 属于 **`v7new5_005-31`**
> （v1.5.114 用 `--force` 换掉的那只偏置包，见 `CHANGELOG.md:2757`），现役 `v7cmin4-31` 在 n=400 只读 **6.5–6.7pt**。
> 是**一只包的两段历史**。盐实验（同一循环只换 `slotSalt`）也推翻了我"内联盐造出假极差"的假设：随机盐**更高**（14.1 vs 12.6）。
>
> **③ `HEALTH.maxDraw` @ n=8：不是误杀，是一条永不响的**死门**——而我把口径量错了。**
> 门的"平局率"是 `allAliveTied(st.p)/G`（`js/train/evo.js:2574` 唯一自增点、`:2610` 返回），**不是** `winner === 'draw'`。
> 实测真实 `T.mirrorHealth(live, 800, 5, 'multi').drawRate` = **0.00%**（0/800）；复刻抽样 n=8 × 200 次、n=40 × 40 次，
> 被判红比例**都是 0%**。（我上一版用的 18.8% 是"无胜者率"，不是门的读数。）⇒ **优先级低，不动 n。**
> 真正在**阻断**的是同一条探针上的 `effSkills < minG(3)`：n=8 × 200 次 `<3` 的比例 **0.0%**
> （mean 3.95 / p05 3.42 / **min 3.14**），但 n=8 的估计**系统性偏低 0.16**（3.95 vs 真值 4.11）
> ⇒ 余量只有 ~0.5σ，且偏置方向对候选不利。
>
> **④ G6 靶向率 ≥40%：实测恒假。** 两档运行（n=60 与 n=300）里 `G6[线上包]` **都是 FAIL**（16.7% / 18.5%），**与 n 无关**；
> 它只记录不阻断 ⇒ 对每次跑门禁的净贡献只是**两条永远红的行**（而"永远红"会训练读者忽略红行）。
>
> **建议清单（按"便宜且确定"排序，全部待裁定）**：① `GATE4_GAMES` 60→300（+35 秒）；② G6 降级为打印或删；
> ③ 座位极差支线删掉，或 `MIN_N_SPREAD` 50→400 且线 30→15pt；④ D67 用**合成 meta** 覆盖 `--force` 例外判定
> （无行为变更，让它第一次运行发生在门里而不是发版时）；⑤ 把 `np:2117` 的 D33 提成独立用例；⑥ UNRUN 加处置语义；
> ⑦ `HEALTH.promoteGames` 8→20（**会改变训练行为** + 需重记 `CLI_ARM_BASELINE`）。`maxDraw` 的 n **不动**。
>
> **共同教训**：上面三条更正**都是"从算式出发、未实测"的错误** —— 算式的**方向**没错，但**量级**要靠量。
> 与 `docs/METHODOLOGY.md` 那条老账同族："判据要用够用的样本，而不是放宽阈值"；前提是**先量出样本够不够**。
>
> **红线**：本轮**只读** —— 不训练、不落盘、不改任何 `.js`/`.mjs`、未 promote、两槽与 `results/*` 未动。

## v1.5.201 — 门/工具减重：删掉 **50 个"无任何代码边"的工具（4,330 行）**＋ 修好 6 处"需要但写错了"的门（D58/D59 静默跳过、D82 半个契约、D45 钉注释、D61/D73 固定字符窗、D123/D127 硬编码哈希）

> **来的路**：v1.5.200 之后做了一轮独立审计（用户问"度量层是不是太多了"）：
> 引擎 ~1,962 行 vs `tools/`+`server/` 21,360 行 ≈ **10.7×**；`tools/np-test.mjs` 单文件 5,868 行、964 个断言里
> **502 个（52.1%）以 `.indexOf(...)` 为第一条件**。三条只读审计线（D 门层 / 脚本清单 / 阻断-vs-只记录）
> 结论都留在 `docs/REVIEW-2026-09-24-gate-layer-audit.md` 与 `docs/REVIEW-2026-09-24-metrology-audit.md`。
> 用户裁定：**先合并再做**（已 `git merge --ff-only` 到 `main`），然后"没用的门删掉、需要但写错的看一下有没有办法改"。
>
> **① 删除判据 = 只认真实代码边**（`spawnSync(process.execPath, ['tools/X', …])` / `import './X'` / `readFileSync('tools/X')`）。
> **注释与文档里的字面提及不算边** —— 本仓有 6 个文件的"可达"只来自散文注释，纯 grep 的可达性审计会严重高估。
> 按此判据 **65 个文件无任何真实边**；再逐个排除"README/文档记载的手动入口或唯一量具"后 **删 50 个 / 4,330 行**：
> L2'「ep cliff」簇 12 个（该线 v1.5.115 已结案，且**同一份日志解析器被抄了 4 份**：`l2-axis-join` / `l2-e1-check` /
> `l2-epdepth-axis` / `l2-width-price`，后两个还是同一脚本的两个输出列）、环/ep 探针 6 个、`*-diag` 5 个、
> 一次性报告器 10 个、一次性探针 15 个、真孤儿 2 个。
> `tools/*.mjs`：**92 文件 / 19,411 行 → 46 文件 / 15,278 行**（文件 −50%，行 −21%）。
>
> **② 明确保留（"没人 spawn" ≠ 没用）**：`smoke` `battle-test` `eval-3p` `np-probe`(README:85,118) `spec-run`(README:83,107,112)
> `promote-champion2p` `count-spectrum`(唯一的座位数/残命轴量具) `parse-battle-log`(读用户私有 `results/*.txt`)
> `ui-probe` `start-train-server.cmd` `cleanup-epirus.ps1`。两条差点被误删、复核后留下的：
> `tools/skill-report-cmp.mjs`（被 `tools/skill-report-compare.cmd:63` 调用 —— **反斜杠路径骗过了第一版正则**）
> 与 `tools/l2-bookkeep.mjs`（名字带 `l2-`，实际是**通用的 D82 记账生成器**，是 D82 纪律的工具）。
>
> **③ 修好的门（"需要但写错了"）**：
> - **D58 / D59 静默跳过 ⇒ 红**。线上多人包读不出时原来 `console.log` 一句「跳过…多半是训练中」然后 **PASS** ——
>   在 "190/190" 里**完全看不见**，而 D59 被跳过的正是守 v1.5.114 那条 43pt 座位回归的 400 局哨兵。
>   现在 `ok(false)`：**不可判 = 红**（这本来就是门自己写的原则），报错里说明"训练进行中请先停训练再跑门禁"。
> - **D82 两个毛病一起修**：(a) 时窗内无新产物时退化成 `ok(cd.length > 1000)`（该文件 307KB ⇒ **恒真断言**，等于这条门在干净检出上不存在）；
>   (b) 只查"在场但没点名"**半个契约**。现在两个方向都查，反向只查**最新条目**那一块
>   （历史条目里被清理的产物属正常，全查会永久红）。② 只认**显式产物点名行**（`> 产物点名（D82）：a.bak, b.bak`）——
>   第一版把正文里随口提到的 `js/bundled-champion.js.bak` 与范围写法 `train-3p-out-band1..6.bak` 都当成了账本条目，当场误报两条（新门第一跑就红，两种误报都真实发生过）。
>   补这半条的直接起因：**本轮我误删了 6 个 09-22「排练臂」产物，整套门禁全绿、没有一条发现。**
> - **D45 钉注释 ⇒ 行为断言**：原来 4 条里有 2 条要求 `policy.js` 出现「幻影选项」、`resolve.js` 出现「自己给自己套不算」——
>   注释一改就红，而它描述的 bug 可以在注释仍在的情况下复活。改成直接枚举候选：
>   `Pol.candidatesFor(st, 0, [{key: R.SK.HOLO}])` 的候选数必须 == 存活对手数，且**不许有 `target == null`**
>   （引擎会把 null 退成"第一个存活对手"⇒ 送盾给别人却按"给自己"归因）。
> - **D61 / D73 固定字符窗 ⇒ 锚点**：D61 是 `lib.slice(i0, i0 + 4000)`（实测最远钉点离锚 **2114** 字符，余量只剩 ~1.9KB），
>   D73 是 `src.slice(i, i + 1500)` —— **代码一挪出窗口，红出来的信息会读成"实现错了"，而真相是"实现搬了"**。
>   D61 改为新增 `fnBody(src, anchor)` 按**配对花括号**取整个函数体；D73 改为"锚点之后第一次 `writeBundleMP(pack`"的就近判据。
> - **D123 / D127 硬编码哈希合一**：产物基线 `aa743488cc` 原来在两个用例里各写一遍 ⇒ 合成单一定义 `CLI_ARM_BASELINE`。
>
> **④ 量清楚之前不动（连同理由写进复核件 §7.3）**：
> 座位 `spread ≥30pt` @ n=100（均匀零假设下 p99=20pt、max=30pt ⇒ "死支"成立，**但同一个量本仓有两把尺子**：
> 包 meta 读 12.5pt、D59 的 `mirrorHealth` 读 ~26.6pt；且带先手顺序的 5 席本就不是均匀零假设 ⇒ 先对齐尺子再定线）；
> G4/G5 的 n=60（更可能对的是**按 ±1.96SE 判不可判**，而不是把 n 抬到 300 —— 那会顶爆 D67 的子进程超时）；
> `HEALTH.maxDraw` @ n=8（改它要动 `js/train/evo.js` ⇒ 改变训练落盘行为 + 基线失效，属独立小版本）；
> UNRUN 不进阻断、D67 的 `--force` 校验器在现役包通过时是死代码、D33 藏在 D26 用例里。
>
> **回归**：`np-test 190 / 190` · `spec-run 52 / 52` · `smoke SMOKE OK` · 规则指纹仍 **`ebdbff36`**（本版只动 `tools/` 与文档）。
> **红线**：两槽线上包与 `results/*` 一字未动；本版**无新增 .bak**（D82 的反向检查会自证这一点）。
>
## v1.5.200 — 黑键闸收成**单一来源**并铺到**每个**训练入口（`server/knob-guard.mjs`）＋ `EPIRUS_WALL_MS=0` 终于真的是"关闭"（`server/wall-cap.mjs`）＝ 实测：同种子 KR=1 vs KR=0 在 `train-best` 与页面训练服务上**逐字节相同**（这就是"击杀奖励训练结果没有变化"的一种真身）

> **来的路**：v1.5.199 之后做独立复核（`docs/REVIEW-2026-09-24-killreward-ds.md`），量到一件事：
> `EPIRUS_KILL_REWARD` **全仓只有一个读取点**（`tools/train-3p.mjs:164`）。从别的入口跑，它被**静默忽略** ——
> 这就是本仓栽过至少 9 次的"传了没人读"那一族（`FEAS_N` / `ECON_ENV_KEYS` / `pierceKeys` / `HOLO_GIFT_MAX` /
> 承诺局 `% 3` / `countBigCards` / `EPIRUS_PASSIVE_FIELD` / `EPIRUS_DIV_*` / 本次）。用户裁定：先做前两条。
>
> **① 判定收成单一来源**（`server/knob-guard.mjs`，与 `econ-env.mjs`/`fight-env.mjs`/`train-env.mjs` 同处一个目录）。
> 原先只有 `train-3p` 自己一份 IIFE，所以**只有它**有这道闸。现在四个入口都挂：
> `tools/train-3p.mjs` · `tools/train-best.mjs` · `tools/train-fast.mjs` · `server/train-server.mjs`（长驻进程在**起监听之前**就拒绝）。
>
> **② 判据升级：不再手抄名单。** 旧口径是"`ECON ∪ FIGHT` 名单 − 本工具闭集"，于是**新旋钮天生不在名单里** ⇒
> 永远测不到（`EPIRUS_KILL_REWARD` 就是这么漏的）。新口径：
> **全仓（`tools/` + `server/`，排除 `js/`）里有人真读的键 − 本入口读得到的键 = 暗键 ⇒ `exit 6`**，
> 而"本入口读得到"由 `entry` 的**传递 import 闭包**扫出来（不手抄名单）∪ `extraReadKeys`。
> `EPIRUS_ALLOW_DARK=1` 放行；`REMOVED_TRAIN_KEYS`（已删除的键）仍响亮拒绝。
>
> **实测（这就是"没有变化"的真身，两个入口各一对）**：
>
> | 入口 | KR=0 | KR=1 | 判语 |
> |---|---|---|---|
> | `tools/train-3p.mjs`（**接了线**） | `a[]`=`42fa1202f8d1` | `1e86bf8fd78e` | **5689/5689 个参数全不同** ⇒ 规则真生效 |
> | `tools/train-best.mjs` | pack=`c67521d82ff32d2a` | `c67521d82ff32d2a` | **去掉 ts 后逐字节相同** ⇒ 静默 A/A |
> | `server/train-server.mjs`（页面训练场） | pack=`ad5baadcd32aaec8` | `ad5baadcd32aaec8` | 同上 |
>
> **③ `EPIRUS_WALL_MS=0` 的语义与注释相反（本轮实测踩到，属"注释骗人"那一族）**：
> `train-server.mjs` 两处判定都是 `if (Date.now() - t0 > cap)`，**没有 `cap > 0` 这一半**，
> 而同文件 `:247`/`:476` 的注释写着「`EPIRUS_WALL_MS=0` **关闭**（纯按代数收敛）」。
> 我拿它去关上限 ⇒ 第一次检查就成立 ⇒ 训练**当场中止**并回 `训练超时上限（30 分钟）`，两轮 server 实验变成空炮。
> 现在语义收进纯函数 `server/wall-cap.mjs` 的 `wallCapExceeded` / `wallCapOf`（`cap <= 0` 或 NaN ⇒ 关闭），
> 两处判定同时换成它 ⇒ **注释与实现终于一致**。
>
> **我自己在实现里犯的两个错（都留在门里，不留成"以后注意"）**：
> ① 第一版把"import 了名单模块"当成"本入口读得到它" ⇒ `EPIRUS_FIGHT_WHISTLE` 传给 `train-3p`
>   从 `exit 6` 变成**静默通过**（假阴性，正是本版要治的病）—— 被 **D122** 当场抓住。
>   改法：名单模块（`*-env.mjs`）的键**必须显式声明**（`train-3p` 声明 `SELF_ENV_KEYS`，server 声明 ECON∪FIGHT∪TRAIN）。
> ② 同一处正则只认 `/` 分隔符，而 Windows 上闭包返回 `server\fight-env.mjs` ⇒ 这条跳过规则**整条失效**（同一个假阴性又出现一次）。
>   两条都写进 D143 的断言里（"只 import 名单来点名 ⇒ 不许算读得到"）。
> ③ 中间还试过"只认点号取值才算读"，结果把 `audit-lib` 的 `pick(o,'EPIRUS_AGGR_GAMES',20)` 那种**真读**判成不读
>   （会把活键误报成黑键）⇒ 最终按**文件级**区分，而不是token 形态。
>
> **门**：新增 **D143**（四个入口都挂 + 名单模块不许自动继承 + 行为：KILL_REWARD→`train-best`/COUNTER_OPPS→`train-fast`/KILL_REWARD→训练服务 三条都必须 `exit 6`，逃逸口必须放行）
> 与 **D144**（`wallCapExceeded(0, 1e12)===false` 等 7 条纯函数口径 + 静态钉"不许再留裸的 `Date.now() - t0 > cap`"）。
> **D122 的静态钉改指单一来源**（行为断言**一个字没改**——门守的是行为，不是实现放在哪个文件）。
> `np-test` **188 → 190**；`spec-run 52/52`；`smoke OK`；**规则指纹仍 `ebdbff36`**（只动 `tools/` 与 `server/`，不在指纹五件套里）。
>
> **⚠️ 记账：我自己踩的坑（不藏）**：验证矩阵里我漏传了 `EPIRUS_TB_OUT`，于是 `train-best` 按它的默认行为
> **写了线上 2P 槽 `js/bundled-champion.js` 并改了 `index.html` 的缓存戳**。已 `git checkout` 还原，
> 并用 `git diff --exit-code` 逐位核对（干净）；`train-best` 覆写前轮换的本地 `js/bundled-champion.js.bak` 也被我的迷你臂顶掉过，已用线上包恢复。
> ⇒ **顺手量出一条真问题（本版不动，等裁定）**：`tools/train-best.mjs` **没有 `EPIRUS_PUBLISH` 门槛**
> （`train-3p` / `train-fast` 都有），默认就写线上槽 + 改缓存戳；而 np-test 现有的那条
> "用了 `-out.js` 落点就必须有 `EPIRUS_PUBLISH`" **覆盖不到它**（它写的是 `dest`，不是 `-out.js`）。
> 改它等于改掉 README 记的推荐用法（`node tools/train-best.mjs 4 600` 就是产出冠军），属**行为变更** ⇒ 留给用户。
>
> **⚠️ 另一笔记账（我把它写下来，因为它不该只活在聊天里）**：清理自己的实验残留时，我把 `docs/artifacts/train-3p-out-band1..6.bak`**一起删掉了** ——
> 那是 **09-22「排练臂」**那轮的产物账本，CHANGELOG 第 1763 行点过名（其中 band1/band2 更早已经被我的迷你臂**覆写**过）。
> `.bak` 全部未跟踪 ⇒ **不可恢复**。整套门禁**全绿、没有一条发现**：D82 只查了一半契约（`在场但没点名`），
> 反向的「点过名却不在场」它看不见（已记进 `docs/REVIEW-2026-09-24-metrology-audit.md` §2-6b）。
> 教训同族：「产物点名」这条纪律**只在写入方向有闸**，删除方向没有。
> **红线**：两槽线上包与 `results/*` 一字未动（且本轮逐位证明了与 HEAD 一致）；无新落盘 `.bak`（D82 不欠账）；
> 未 promote、未动 `main`。**本版没有新增任何实验产物。**
>
> **给下一步**：复核件 `docs/REVIEW-2026-09-24-killreward-ds.md`（击杀奖励线"没有变化"的三层根因）与
> `docs/REVIEW-2026-09-24-gate-layer-audit.md`（139 条 D 门逐条分级：12 条真空 / 0 条失效 / 7 对重复）与
> `docs/REVIEW-2026-09-24-metrology-audit.md`（三条审计线汇总：规模 / 阻断-vs-只记录 / 失准阈值 / 死重清单）**随本版一起入库**。

## v1.5.199 — 第三个"恒亏动作"：**空净化进菜单闸门**（`hasPurgeable` · 门 D142）＋「这一手蓄能攒哪种珠」收成单一来源 `js/ui/bead-choice.js` ＝ **线上包逐位不变，实测把某只候选 200 局里 112 次"清除 0 枚"的净化全挡掉**

> **用户实盘报的三件事，先分清哪些是"修过又回来"**（详见 `docs/RESEARCH-LOG-2026-09-24-qoder-night.md` §K-14 与下面三条）：
> **① 空蓄能/珠经济**：真修过（v1.5.82 ep<2 菜单摘蓄能 · v1.5.76 `BEAD_W` 奖励 · **v1.5.144 换包**才拿到"蓄了会花珠"的冠军）。
>   它回来的原因是**当年把第六道定成"只记录不阻断"**（`audit-lib.mjs:512`：「收紧后连在位包也不达标，翻闸会把所有候选挡死」）⇒ 带 0.0% 花珠率的包照样能过 `promote --dry`。本版**不动这条**（属判据）。
> **② 空净化**：**从来没修过** —— 同族那道菜单闸门（`evo.js` 的 `gated`）只有两个分支：天火无活符咒（v1.5.139）与蓄能 ep<2（v1.5.82）。**本版补第三个分支。**
> **③ 疯狂防御**：门在（v1.5.26 的 `noThreatStanceRate > 0.6`），但**判据按定义看不见这个场景** —— 它把"威胁"定义为"对手累计 ep ≥ 5（大雷门槛）"，
>   而用户报的正是"**对面一直有钱** ⇒ 就一直防"，落在当年裁定为"合理防御"的那一侧。实测：同一只包在攒钱桌上架势占比 10.0%、第 7 回合就开始、局长 68.5，
>   而体检的 E 栏读 **0%**。**本版不动这条**（要动就是改判据），只把这个盲区记进日志。
>
> **做了什么（②）**：`js/train/evo.js` 的菜单级"严格必废"闸门加第三分支 —— 自己身上没有任何持续状态 ⇒ 净化必然"清除 0 枚"（花 3 ジ 放空气）⇒ **从菜单摘掉**（训练/浏览器同享此菜单，键级探索也就永远采不到它）。
>   清单**逐项对齐 `resolve.js` 的 `purgeSelf`**（R60/v1.5.110 的清除清单：符咒/梦魇/挑衅/藤甲火弱/地雷/避雷针/大雷禁用）。
>   ⚠️ 这是"同一份清单写在两个文件里"，**故意如此**：`resolve.js` 是指纹五件套，不能为了共享一行代码去动它（换代 = 全部历史基线作废）。
>   漂移风险改由**门 D142 逐字段与引擎对账**兜住：给席位只挂某一种持续状态 ⇒ 真出一次净化 ⇒ 判据必须"事前 true / 事后 false"；全空时必须 `curses===0` 且不产生 heal。
>
> **门 D142 还钉了四件容易只写不验的事**：① 判据导出可喂构造态（不钉文本）；② **正反双对照** —— 强制"只要买得起就出净化"，
>   绕过 `econBase` 的对照组**必须测得出非零空净化**（实测 12 局就有），走了闸门必须归 0 ⇒ 防"它本来就没出"的假绿（seam 2 那一族）；
>   ③ 菜单作用点直测（干净 ⇒ 摘掉；挂一枚符咒 ⇒ 必须还在，不许把好牌一起摘）；④ `bead-choice.js` 的纯函数口径 + `index.html` 必须在 `ui.js` **之前**加载 + `ui.js` 里不许再留内联 `elec>boom?'boom':'elec'`。
>
> **顺手收的第七例"两处各写一遍"**：`ui.js` 里 `p.elec > p.boom ? 'boom' : 'elec'` 原本有**三份**（2P 主循环 / 多人主循环 / 人类阵亡后的观战循环）。
>   读代码得到的事实（非推断）：**冠军策略返回的动作里 `bead` 基本恒为 null** ⇒ "这一手攒电珠还是爆珠"一直是**界面层替 AI 决定的**，
>   三处一旦漂移就是"你活着时和死后，同一个包打法不同"（最难查的那类）。本版**只收拢、不改行为**（含"相等时仍取电珠"这个原口径）。
>   ⚠️ 剩下的更大问题（珠类型该不该进动作面/引擎默认 `'elec'` 合适不合适）**没动**，记在下面待裁。
>
> **实测影响（同种子逐局事件签名比 HEAD 版与工作区版 · 40 局/格）**：
>
> | 装配 | 线上包 `v7cmin4-31` | 候选 `cbs1s2-band2`（长程） | 候选 `co1s8-band1`（长程） |
> |---|---|---|---|
> | 逐位对比 | **multi/长程/3 人 三格全部逐位相同** | 不同（预期：它真的在空转） | **逐位相同** |
> | 净化（括号内＝空转） | 0（0）→ 0（0） | **112 次里 112 次空转 → 0** | 0（0）→ 0（0） |
>
> ⇒ **线上包不受影响**（它从不净化，且菜单少一个候选也没扰动它的抽样 ⇒ 不需要重新验收、不用换包重发）。
> ⇒ 反过来说：**`cbs1s2-band2` 那 112 次 3 ジ 空放是真病**（200 局里每局 0.56 次），本版顺手把它治了；
>   但这只包的行为已经因此变了 —— **我早报里给它的读数（广度 3.25/破防 4.95 点/考卷 38.9）是"修之前"的数**，要再评估它必须重测。
>
> **待裁定（我没动）**：① 第六道珠经济要不要真的阻断（翻了现役自己就红）；② "合理防御"的威胁判据要不要改成随对手 ep 边际递减 / 给 `maxRun` 加阻断线；
> ③ 珠类型要不要纳入策略动作面（现在归 UI 决定）。
>
> **门与状态**：`np-test 188/188`（新增 D142）· `spec-run 52/52` · `smoke SMOKE OK`（页面真跑，验证 `bead-choice.js` 加载顺序与三处调用）·
> **规则指纹仍 `ebdbff36`**（只动 `js/train/evo.js` 与 `js/ui/`，非指纹五件套）· 两槽线上包与 `results/*` 一字未动。


## v1.5.198 — 「上桌 + 广度当选键」六只臂 ⇒ **"广度 ↔ 破防/战斗门"不是因果拉扯**：出货 `cbs1s2-band2`，**今晚第一粒 `promote --dry` 零阻断、且广度与破龟群两栏都胜过现役**的包（我只测没换槽）

> **为什么跑这一批**：v1.5.197 的结论是"卡住候选的是广度门"。那有两种解释 —— (i) 广度和破防/战斗门**结构性互斥**，(ii) 只是今晚的臂没**同时**被这两栏选过。
> 区分它们只要把两个已有的开关一起打开再训一轮：`EPIRUS_COUNTER_OPPS=1`（把龟放上训练桌）**+ `EPIRUS_SEL_LAND=1`（当选面按净兑现广度排序）**× 6 种子 × 1500 代 × 种群 16。
>
> **答案 = (ii)**。`cbs1s2-band2`（seed2）：
>
> | 栏 | 现役 `cmin4-31` | `cbs1s2-band2` | 谁赢 |
> |---|---|---|---|
> | `promote --dry` 阻断项 | —（它在 G6 靶向率上有 2 条**参照行**红） | **零阻断 · 自检通过**（`✅ 候选自己全过`） | 候选 |
> | 净兑现广度（5 人 · multi） | 2.66（**3 种**打上血） | **3.25（5 种）** | **候选**（用户从最开始在意的那一栏） |
> | 有效技能数 G（multi / long） | 4.44 / — | **5.16 / 3.83**（两模式都 ≥3） | 候选 |
> | **破龟群**（1 席 vs 4 席只防御不还手） | **0.00 点/局 · 0% 活着收场 · 0% 胜** | **4.95 点/局 · 85% 活着 · 五格镜像外全 100% 胜** | **候选（现役这一栏是零）** |
> | G4 最克格（long/multi，n=300） | 38% / 35% | **35% / 30%** | 候选（略优） |
> | G5 防席夺冠（long/multi） | 0% / 0% | 1% / 0% | 平 |
> | 反弹墙 主动伤害 | 21.0/局 | 8.3/局（零伤害局 0%） | 现役 |
> | 通吃全局最弱格（除 2P） | 10.8%（脚本池长程） | 11.7%（同格） | 平（候选略优） |
> | 对现役场（3P/4P/5P/长程） | — | **68.3 / 34.2 / 24.2 / 25.8%** | 候选 |
> | **A 考卷（5 人 · 三个种子都稳）** | **54.4~57.1%** | **38.7~38.9%** | **现役（差 17pt，A 在 v1.5.94 已降级为"只作诊断"）** |
> | G6 靶向率（只记录） | 18.5% | **11.0%** | 现役（两者都远低于 40% 参照线） |
>
> **两条方法论副产品**：
> ① **`EPIRUS_SEL_LAND` 是活的、但咬合率只有 3/6**：日志里它明确改判（`2.40 → 2.55，胜负分差 −2.1pt 在同分带 tol=0.03 内`），
>   而 seed 3/5/6 的当选者与**只开上桌**的同种子臂**逐位相同**（按本仓规矩剥壳只哈希 `a` 数组：`a1553d73`/`6871940c`/`bbb7df45` 两两相等）
>   ⇒ 它是"**在带内才换人**"的排序键，不是必改判的开关。评"某键没用"之前先看**同分带有几粒**。
> ② **"最弱格"必须跨场取 min**（v1.5.197 §① 那条更正的根因）：`probe-cross-mode` 每行只印本行的局部最弱，读的人极易把镜像场那一列当全局。
>
> **⚠️ 我给这粒包打的折（不许把它当成"已经能上线"）**：
>  · 它是**在一把尺上筛过 48 粒新臂 + 248 粒历史产物之后**取的最大值 ⇒ **赢家诅咒仍然在**；下一步该做的是**换评估种子 + 加局数复评 + 真机 `battle-test`**，不是直接换槽。
>  · A 考卷差 17pt 是真的（虽然那一栏"连续两轮与稳健性反相关"），G6 靶向率还退了一步；这两条我都摆在表里，不藏。
>  · **我没有换槽、没有 promote、没有动 main**（红线）。要推进需要用户逐次点头。
>
> **D82 产物账本（本轮 6 粒当选带，`docs/artifacts/`；同臂其余 30 粒落选带是我的试验残留，已删）**
> `cbs1s1-band6` `cbs1s2-band2` `cbs1s3-band3` `cbs1s4-band3` `cbs1s5-band1` `cbs1s6-band4`
> （配方全部 = `tools/train-3p.mjs 1500 3 12 16` + `EPIRUS_COUNTER_OPPS=1` + `EPIRUS_SEL_LAND=1`，seed 见 arm 名尾号。）


## v1.5.197 — 把"破龟群过关"的 **22 粒全包**筛完（全门 n=300 + 考卷 + 通吃三场）＝ **卡住候选的不是通吃、是广度门**（`co1s7-band6`/`tb1-band1` 通吃与现役打平、G4/G5 全过、龟群 0.00→6.9~7.6 点/局，但 `G 有效技能数` 都 <3）＋ 顺手更正我上一版拿错的基线 ＋ 一条给"防龟"这条线降温的事实：**4 只龟那面墙在产品里搭不出来**

> **① 更正（这次是在两个方向上都偏过）**：v1.5.196 §③ 把现役的"通吃最弱格"写成 **15.0%**，那是**只读镜像场**那一列；
> 现役的**全局最弱格 = 脚本池·长程 10.8%**。⇒ 修正后：`cbig1s2-band1`（11.7%）在通吃这点上**略优**于现役，不是"更脆"。
> **这条更正把结论从"上桌换来的是通吃变差"改成"上桌换来的是广度变差"** —— 而广度恰恰是用户从最开始就在意的那一栏。
>
> **② 22 粒"破龟群过关"（`归因伤害 ≥4/局` 且 `终场存活 ≥50%`）全筛一遍**：
>
> | 包 | 破龟群 伤害/存活 | G4 · G5（n=300） | 考卷 | **通吃全局最弱格（除 2P）** | 净兑现 | 死因 |
> |---|---|---|---|---|---|---|
> | **现役** | **0.00 / 0%** | 38%/35% · 0%/0% PASS | **54~57%** | **10.8%**（脚本池长程） | 2.66（3 种） | —（就是没有破龟群） |
> | **`co1s7-band6`** | 7.61 / 79% | 32%/37% · 23%/17% **全 PASS** | 27.4~30.1%（300 代） | **10.8%**（与现役打平） | 1.99（2 种） | **广度门 G 1.99<3**（塌到枪 82.6%） |
> | **`tb1-band1`** | 6.90 / ≥50% | 38%/31% · 0%/0% **全 PASS** | — | **11.7%**（略优于现役） | 1.33（2 种） | **广度**（同上族） |
> | `cbig1s2-band1` | 7.1 / 97% | 60%/45% · 12%/4% **全 PASS** | **49.1~49.5%** | 11.7% | 2.15（3 种） | 通吃/门/考卷都过，**广度差现役 0.51** |
> | `co1s8-band1` | 8.7 / 78% | 全 PASS | 29.5~31.9% | 9.2%（4P） | 2.03（4 种） | 4P 那一格 + 考卷（预算） |
> | `v7xn10a-band3` `v7xn10b-band5` `v7xn11a-band3` | 8.4~9.1 / 100% | 56%/44% · 0%/0% PASS | 34.6% | **0%**（镜像 3P/5P/长程 `0%胜/100%平`） | 1.48 | **镜像退化**（见 ③） |
> | `kr1s4-band2` | 9.7 / 70% | 37%/15% · 0%/0% PASS | 28.9% | **0%**（镜像 4P 全平） | 1.51 | 镜像退化 |
> | `v7xn8c-31-band4` `v7xn7a-31-band5/6` | 10.4~11.2 / 89~100% | **UNRUN**（基线 0%）·「只枪」100% FAIL | 42.3% | 0%（镜像 4P/5P 全平） | 0.00 | 镜像退化 + G4 不可判/红 |
> | `v7xn15b-band4` `v7xn16a-band1` | 5.2 / 11.0 | 21%/24% PASS · 0%/0% ／ 「只枪」58% · G5[multi] **31% FAIL** | 40.8% | 9.2% ／ — | 1.04／1.93 | G5 ／ 通吃 |
>
> **③ "镜像格"是这批包里最常见的死因，而 G4/G5 都不看它**：`v7xn10a/b`、`v7xn11a`、`kr1s4`、`v7xn7a`、`v7xn8c` 全部在**某一格的镜像场**里读 `0% 胜 / 100% 平`。
>   这不是学术洁癖 —— 产品里 **AI 席位跑的就是同一包** ⇒ "AI 互相打"就是镜像场，镜像必平 = 整桌不动。
>   现役在四格镜像里最差也有 15.0%/平 27% ⇒ **任何"按某一栏挑出来的更强包"都必须先看镜像格**；这一栏我记进证据，**没有**变成门（要不要加属判据改动）。
>
> **④ 给"防龟"这条线降温的一条事实（读引擎＋实测，不是猜）**：**4 只龟那面墙在产品里搭不出来** ——
>   §N48 自适应场实测**现役包设防率恒 0%**（它从不挂防御），而所有 AI 席跑的就是这一包 ⇒ "1 席被测 vs 4 席只防御不还手"是**合成压力场**，不是会发生的对局。
>   产品里真会发生的龟缩是"**一个人类玩家只防御 vs 4 席 AI**"，那正是 **G5 的装配**，而**现役在那里防席夺冠 0%（long/multi 都是）＝ 它已经在打龟**。
>   ⇒ 所以 v1.5.195/196/197 这条线交付的是**鲁棒性（会凿穿纯防御）＋ 一把新量具**，**不是"治好了产品里的龟缩"**。
>   产品侧真正还缺的仍然是**广度**（最好的`cbig1s2-band1`：门全过、考卷 49%、破龟群 7.1 点/局，但净兑现 2.15 < 现役 2.66）。
>   **⇒ 早报建议**：**不换包**；下一步该动的是"广度这一栏为什么在门里过不了"（`EPIRUS_BREADTH_FLOOR` 量的是训练桌 N=3，v1.5.196 §⑤），**不是再加一条防守判据**。
>
> **本轮全部只读**：探针/门/审计不落产物；两槽线上包与 `results/*` 一字未动；规则指纹仍 `ebdbff36`。本轮**没有新增 `.bak`**（只复测已有产物 ⇒ D82 不欠账）。


## v1.5.196 — 大预算"上桌"课程九只臂（1500 代 × 种群 16）＝ **A 考卷那 25pt 差证实是"预算"不是"课程"（回到 48~50%）**，但按仓库自己的"通吃＝最弱格"口径，**今晚没有任何一粒候选全面胜过现役** ⇒ 不换包，破防这条线的正解在"当选键"里

> **跑的东西**：`EPIRUS_COUNTER_OPPS=1`（把「只防御不还手／只枪／只狙击」放上训练桌，**规则一字不改**）× 9 个种子 × **1500 代 × 种群 16 × 12 局**
> （= 昨天那些 300 代 × 种群 8 臂的 7.5 倍预算），回评一律 `probe-kill-reward` 规则档 0（纯现状世界）＋ shipped 行为门 **n=300**（见 v1.5.195 §④：n=60 与阈值同量级，那次差点误判）。
>
> | 臂 | 考卷（3 种子） | G4 最克格 / G5 防席夺冠 | **破龟群：1st / 归因伤害 / 终场存活** | 头对头 vs 现役 | 通吃最弱格（除 2P） |
> |---|---|---|---|---|---|
> | **现役 `cmin4-31`** | **54.4~57.1%** | 38%/35% · 0%/0% | 0% / **0.00** / 0% | 19.0 / 14.2 / 16.3% | **15.0%** |
> | `cbig1s1-band4` | 48.1~50.1% | 「只防御」**87%/90%** ⇒ FAIL · G5 **86%/89%** ⇒ FAIL | 100% / **1.00** / **0%** | 22.0~27.9% | — |
> | **`cbig1s2-band1`** | **49.1~49.5%** | 60%/45% · 12%/4% ⇒ **全 PASS** | 100% / **7.1** / **97%** | 11.3~20.4% | 11.7% |
> | `cbig1s3-band3` | 37.2~38.2% | 22%/36% · 19%/**33%** ⇒ G5[multi] FAIL | 99~100% / **2.2** / 3% | 33.3~38.3% | — |
> | `cbig1s4-band2` | — | — | 0% / 0.00 / 0% | 34.3% | — |
> | **`cbig1s5-band1`** | 38.4~40.6% | 「只枪」77%/84% ⇒ **G4 FAIL** · G5 0%/4% PASS | 100% / **9.8** / **92%** | 26.3% | 17.5%（镜像 5P） |
> | `cbig1s6-band4` | — | — | 100% / 1.00 / 0% | 15.3% | — |
> | `cbig1s7-band4` | — | — | 100% / **6.2** / **100%** | 21.0% | — |
> | `cbig1s8-band5` / `cbig1s9-band4` | — | — | 0% / 0.00 / 0% | 22.7% / 7.7% | — |
>
> **四条结论**：
> ① **预算假设坐实（推翻我上一版的"考卷差 25pt 是候选的代价"）**：同一条课程，300 代臂考卷 29.5~33.9%，**1500 代臂 48.1~50.1%**（现役 54~57）
>   ⇒ 那 25pt 里绝大部分是**训练预算**，不是"上桌"这门课。（对照臂 `kr0s*` 同规格散布 23.9~41.6% 也指向同一件事。）
> ② **破龟群在大预算下依然买得到，但只有 3/9 是真凿穿**（幅值 ≥4 点/局 **且** 活着收场 ≥50%：`cbig1s2` 7.1/97% · `cbig1s5` 9.8/92% · `cbig1s7` 6.2/100%）。
>   其余的"100% 破防"是**下限读数**（1.00 点 / 0% 存活 = 蹭一发后与龟一起被终局收缩清场、按伤害判胜）——
>   最刺眼的反例是 `cbig1s1-band4`：破防 1st 读 **100%**，而 G5 里**一只龟能赢它 86~89%** ⇒ **shipped 门当场拦下**。
>   ⇒ v1.5.195 那两栏**单独用也不够**：`破龟群 1st`（下限）→ `归因伤害/终场存活`（幅值）→ `G5`（反向装配）三条一起读才立得住。
> ③ **扫兴但必须写的一条（v1.5.197 更正了"现役那一格"的口径）**：按本仓自己的"通吃＝**最弱格**"口径（`probe-cross-mode`，除 2P），
>   `cbig1s2-band1` 11.7% · `co1s8-band1` 9.2%（它 4P 格只有 9.2%）· `cbig1s5-band1` 7.5%（长程格）。
>   它们的 5P 头对头、破龟群、靶向率（62.4% vs 现役 18.5%）都明显更好，**代价花在"最弱那一格"和"广度/兑现"上**。
>   ⚠️ **我第一版把现役写成"15.0%"，那是只看镜像场的数** ⇒ 现役的**全局最弱格 = 脚本池长程 10.8%** ⇒ 修正后的结论：
>   `cbig1s2-band1` 在通吃这一点上其实**略优**于现役（11.7 vs 10.8，120 局分辨力之内），**真正卡住候选的不是通吃，而是广度门**（详见 v1.5.197）。
>   ⇒ **今晚没有一粒能同时满足**（考卷≥现役 · G4+G5 全过 · 破龟群幅值≥4 且存活≥50% · 通吃最弱格≥现役 · 净兑现≥现役）⇒ **不换包**（两槽一字未动）。
> ④ **这条线真正的落点不是"换个更能打的包"，而是"当选键里根本没有'对面不打你时你会不会收尾'这一项"**。
>   最小正确动作：把 `破龟群幅值`做成**训练内的第二排序键**（与 D124 的"当选面 3P 第二栏"、`EPIRUS_SEL_LAND` 同一形状），
>   让它和通吃/考卷**一起参与当选**，而不是拿它去换掉现役包。**这属判据改动 ⇒ 等用户裁定，我没有实现、也没动任何键。**
>
> **给门禁的一条待办（不是判据改动）**：`G4/G5` 默认 `GATE4_GAMES=60`，而阈值是 25%/60% —— 60 局的 SE 与阈值同量级
> ⇒ **门的 PASS/FAIL 在临界包上是掷硬币**（今晚 `co1s8-band1` 先被判 28% FAIL、n=300 复测 22% PASS）。
> 建议改成 n=300 或在门里印 ±1.96SE 带。要不要动、怎么动，属 DS 侧与用户裁定 ⇒ 我只把证据记在这里。
>
> **D82 产物账本（本轮 9 粒当选带，`docs/artifacts/`；同臂其余 45 粒落选带是我的试验残留，已删）**
> `cbig1s1-band4` `cbig1s2-band1` `cbig1s3-band3` `cbig1s4-band2` `cbig1s5-band1` `cbig1s6-band4` `cbig1s7-band4` `cbig1s8-band5` `cbig1s9-band4`
> （全部 = `tools/train-3p.mjs 1500 3 12 16` + `EPIRUS_COUNTER_OPPS=1`，seed 见 arm 名尾号；无一进槽。）


## v1.5.195 — 破龟群栏（`归因伤害/局` + `终场存活`）＝ **原来那列"破防 1st"是下限判据** ＋ 门 D141 ＋ **§K-2 那条"击杀奖励能当课程"的机制归因被我自己推翻**：不用改规则，把龟放上训练桌就够了

> **① 先修量具（这也是今晚最重要的一条工具改动）**
> `probe-kill-reward --fields=guardwall` 那格的判胜口径我**读错了含义**：4 席"只防御不还手"不打人 ⇒
> 到 `终局收缩` 时**五个人一起被清场**（实测局长恒 47）⇒ 走的是 `resolve.js` 的 `alive.length === 0` 分支
> =「**累计有归因伤害**最高者胜」。所以 `破防 1st = 100%` 只等价于"47 回合里至少蹭到一发"，是**下限**判据。
> ⇒ 加两栏：**`归因伤害/局`**（凿墙的幅值）与 **`终场存活`**（>0% 才是真把龟打死，不是被清场后判分赢）。
> 归因表达式与引擎**同形**（`damage && e.source != null`）⇒ 门 D141 同时钉"同形 + 两栏存在 + 不是死读数"
> （现役在破龟群场 = 0.00/0%，但在头对头场 > 0 ⇒ 那个 0 是"打不穿"而不是"读不出"）。
> ⚠️ 中途踩到一次**判据选错**：第一版我用"局长 < 回合上限"当"提前收场"，读出来**人人 100%** ——
> 因为那局是被收缩清场的（47 回合 ≪ 上限 100）。教训：新栏必须先问"它能不能不等于 1"。
>
> **② 推翻 §K-2：把"会破防"归因给击杀规则是错的**
> 补种子到 n=6，并加跑两批新臂：**`co1s*`** = 现状规则 + `EPIRUS_COUNTER_OPPS=1`（v1.5.172 就有的开关：
> 把「只防御不还手／只枪／只狙击」三个判据原型放上训练桌，**规则一字不改**）；**`cr1s*`** = 上桌 + 规则 1。
>
> | 组 | 训法 | 能破防（1st >50%） | 破防场逐位（1st%） | 头对头中位 |
> |---|---|---|---|---|
> | 现役 | — | 0/1 | 0 | 14~19% |
> | `kr0*` | 现状规则、普通桌 | **1/6** | 0,0,0,0,100,0 | 20.8% |
> | `kr1*` | 规则 1 | 5/6 | 100,15,83,100,100,100 | 27.5% |
> | `kr2*` | 规则 2 | 3/6 | 60,94,0,26,0,43 | 22.5% |
> | **`co1*`** | **现状规则 + 上桌** | **7/9** | 100,98,88,5,100,100,100,100,0 | **33.8%** |
> | `cr1*` | 上桌 + 规则 1 | 5/9（seed1-6 子集 5/6） | 27,100,100,100,0,100,0,0,0 | 24.6% |
>
> ⇒ "能破防"确实能被训出来，但**把它训出来的不是击杀奖励，而是"对手里有一只龟"**。
> 同种子符号检验：`上桌 vs 普通桌` 5胜0负1平、`规则1 vs 普通桌` 5胜0负1平 ⇒ **两者等价**，而上桌**不改规则、不换指纹、免费**。
> 而在"已经上桌"之上再加规则 1，破防反而 5/9（不如只上桌的 7/9）⇒ **击杀奖励这条线到此结案**
> （§N49 已证它作为上线规则在龟缩场结构上失效，今晚证它作为课程也没有增量）。
> 机制解释也归位了：普通训练桌里**根本没有不还手的防御者**，规则档只是间造了"要杀人才有分"的压力；
> 这与 §N35（`v7xn22a` 长成广度却死在 G4「只防御不还手 85% 击败」——"判它的对手从不出现"）是同一个病的**正解**。
>
> **③ 顺手做了一次 248 粒海选（把 `docs/artifacts` 里所有"当选带"过一遍同一把尺，换种子 `777001`）**
> 结论打脸：**现役包不是"最能破龟群的"，只是"从没按这一栏选过"** —— 历史臂里 **39/87 能破防**，
> 幅值榜前几名全是老包（`v7xn7a-31-band5/6` 11.15 点/局、`v7xn16a-band1` 11.02、`v7xn8c-31-band4` 10.40，均 100% 破防 + 活着收场）。
>
> **④ 两栏冲突（这条必须用户裁定，我没有动槽）**： shipped 的 **G5 是"1 席龟 vs 4 席被测"**，现役 **PASS（防席夺冠 0%）**；
> 而我这片**"1 席被测 vs 4 席龟"**里现役是 0.00 点/局、0% 活着收场 ⇒ **现有验收门禁看不见"龟群场"这个病**。
> 今晚 33 粒新臂里**两栏都过**的只有三粒：`kr1s4-band2`（G5 0%/0% · 9.70 点 · 存活 70%）、
> `co1s7-band6`（23%/17% · 7.61 · 79%）、`cr1s4-band2`（13%/13% · 7.59 · 98%）。
> 三批独立种子（`777001`/`555001`/`314159`）都稳的候选是 **`co1s8-band1`**：
> 头对头 **41.7 / 34.0 / 36.7%**（现役 14.2 / 17.8 / 19.0）、破龟群 **8.7 点/局 · 存活 78~83%**、脚本池 16.7~17.3%（现役 5.3~8.0%）、
> 净兑现 2.03（**4 种**，现役 2.66 只有 3 种）。
> ⚠️ **我先用 n=60 误判过它**：那一次 `promote --dry` 卡在 `G5[multi] 防席夺冠 28% > 25%`；把样本提到 **n=300 复测 ⇒ G4/G5 全格 PASS**
> （G5 4%/22% · G4 最克格 43%/22% · G6 靶向率 62.4% vs 现役 18.5%）⇒ **`--dry` 自检全绿、零阻断**（只剩两条"参照行"—— 是**现役自己没过**）。
> 记进 CHANGELOG 的理由：**阈值 25% 与 n=60 的 SE（±5pt）同量级 ⇒ 60 局的 PASS/FAIL 就是掷硬币**。这条对门禁本身是个待办（G5 该报噪声带）。
> **但候选不是没有代价**：canonical 5 人考卷（`tools/eval-5p.mjs`，三个种子都稳）现役 **54.4~57.1%**、`co1s8` **29.5~31.9%**、`co1s7` 28~30%
> ——A 列在 v1.5.94 已降级为"只作诊断"（且记录在案"连续两轮与稳健性反相关"），但掉 25pt 不该被这句话糊过去 ⇒ **取舍交给用户**（②）。
> 另外两粒体检各自卡在：`co1s7-band6` ⇒ 广度 G 1.99 < 3（塌到枪 82.6%）；`cr1s4-band2` ⇒ G4[multi]「珠爆发」62%（n=300 复测 66% ⇒ **这条是真的**，不是噪声）。
>
> **⑤ 给 DS 的一笔账：`EPIRUS_BREADTH_FLOOR`（v1.5.170 广度准入线）量的是**训练桌的 N=3**，不是产品的 5 人**
> 反例（实测、非推断）：`cb1s1-band1` 被线以"净 G(落地) **3.95**（4 种）"选中，而同一粒包在 5 人桌只有 **1.93**（3 人 3.95 / 4 人 3.47 / 5 人 1.93）。
> ⚠️ 这**不是噪声**：`mirrorHealth` 完全确定性（我拿 8 个种子重打，读数一字不差）⇒ 是**口径**。
> 后果：线可以"通过"一粒在 5 人桌上比现役还窄的包。要不要把线改到 5 人桌（或两栏都要），属判据改动 ⇒ **等用户裁定**，我没动。
>
> **D82 产物账本（今晚新增 33 粒当选带，`docs/artifacts/`；同臂其余 165 粒落选带是我的试验残留，已删）**
> · 补种子（300 代）：`kr0s4-band1` `kr0s5-band4` `kr0s6-band4` · `kr1s4-band2` `kr1s5-band1` `kr1s6-band6` · `kr2s4-band1` `kr2s5-band3` `kr2s6-band5`
> · 上桌（现状规则）：`co1s1-band1` `co1s2-band2` `co1s3-band4` `co1s4-band2` `co1s5-band2` `co1s6-band1` `co1s7-band6` `co1s8-band1` `co1s9-band4`
> · 上桌 + 规则 1：`cr1s1-band2` `cr1s2-band6` `cr1s3-band6` `cr1s4-band2` `cr1s5-band4` `cr1s6-band1` `cr1s7-band5` `cr1s8-band1` `cr1s9-band3`
> · 上桌 + 广度线（600 代 × 种群 12）：`cb1s1-band1` `cb1s2-band4` `cb1s3-band4` `cb1s4-band6` `cb1s5-band1` `cb1s6-band6`
> **没有一粒进槽**（红线：不换槽 / 不 promote / 不动 main / 不提交 `results/*`）。
>
> **待用户裁定（明早）**：① 要不要把"破龟群（1 席 vs 4 龟：归因伤害 ≥4/局 且 活着收场 ≥50%）"**补进验收门**——
> 补进去意味着现役包自己就是红的（它 0.00 点/局）；② **`co1s8-band1` 现在全部阻断门都过（n=300）**，代价是 A 考卷 54→30 ——
> 换不换包是判据取舍，我不动槽；③ 广度准入线要不要改到 5 人桌口径（§⑤）；
> ④ 击杀奖励这条线按"结案"处理（不上线、也不当课程用）；⑤ G5/G4 的 n=60 要不要提到 n=300 或在门里印噪声带（今晚就是它差点让我误判一粒候选）。


## v1.5.194 — 把两条击杀规则提成**单一来源库** `tools/kill-reward-lib.mjs` ＋ 训练侧接线（`EPIRUS_KILL_REWARD=0|1|2`）＋ 门 D140 ＝ 九只臂跑完：**这两条规则作为"上线规则"无效，但作为"训练课程"有效**（规则臂 4/6 会破防 · 对照臂 0/3 · 现役 0/1）

> ⚠️ **本版标题里"作为训练课程有效"这半句已被 v1.5.195 §② 推翻**：机制不是"奖励击杀"，而是"训练桌上有没有一只龟"——
> 不改规则、只把龟放上桌（`EPIRUS_COUNTER_OPPS=1`）拿到 7/9，比规则 1 的 5/6 更好。九只臂的**读数**仍然有效。

> **为什么要单独一个模块（而不是在训练脚本里再写一遍）**：`probe-kill-reward`（量具）和 `train-3p`（训练）
> 必须跑**同一份规则**，而这次两遍会跑在**不同进程**里、比对的是**冠军产物** —— 一旦漂移，出的就是
> "训出来的冠军和量出来的冠军根本不是一套规则"这种最坏的错（本仓为"同一规则写两遍"栽过六次：
> `FEAS_N` / `ECON_ENV_KEYS` / `pierceKeys` / `HOLO_GIFT_MAX` / 承诺局 `% 3` / `countBigCards`）。
> ⇒ 探针里那份内联实现（3455 字符）**已删**，两边都 import 这一个 lib。
>
> **用户点名的三条细化，落法**：
> ① **蓄能算进开销** ⇒ 电磁炮/激光眼那手若需珠则"开销 +1"，于是同优先级里它占优（等价于多花 1 ep）；
> ② **无来源的天火、地雷也要回 ep** ⇒ ⚠️ **不改 `damage.source`**（R57 规则语义：它们本就必须无主，
>   而且 `source:null` 是大雷连带的输入和"终局收缩"哨兵的依赖），而是**另补三个归因字段** `mineFrom` / `fireFrom` / `transferBy`；
> ③ **转移伤害造成的伤害＝其转移的伤害** ⇒ 功劳默认记给**转移者**（`--kr-transfer=source` 可切成"记给原攻击者"）。
> 引擎文件在**内存里**打补丁（`resolve.js` 加三个字段、`play.js` 在 `resolveActions` 之后 `endTurn` 之前挂钩子）
> ⇒ **仓库引擎一字未动，规则指纹仍是 `ebdbff36`**。补丁锚点找不到就**抛错**（静默没打上＝三档跑同一个引擎＝假实验）。
>
> **修掉的两个自己造的 bug**（都记进门 D140）：
> · 训练侧的**行为式读回**第一版用 `EPIRUS_CHAMPION_3P` 下注 —— 那一刻冠军包还没进沙箱 ⇒ unpack 拿到 `null`
>   ⇒ `shapeOf(null)` 抛。改成"脚本池跑一局"（钩子是否生效与谁下注无关）。
> · 规则 1 的"同优先级取开销最高"第一版读卡表 `R.byKey[k].cost` —— 而**聚能环/激光眼/过载炮的 `cost` 是 `null`**
>   ⇒ 被当成 0，比较结果错。改成 `buildCostOf(R, S)` 问引擎的 `S.computeCost`。
>
> **九只臂（300 代 × 3 人 × 8 局 × 种群 8 · 3 个种子 × {规则1, 规则2, 对照=规则关}）**，
> 回评一律**把规则关掉**（档 0 ＝ 纯现状世界 ⇒ 量的是"包学到了什么"而不是"规则给了什么"）：
>
> | 臂 | 训于 | **破防场 1st（规则关）** | 平局率 | 头对头 vs 现役 | 净兑现 `landG` | 奖励发放（规则开） |
> |---|---|---|---|---|---|---|
> | 现役 `cmin4-31` | — | **0.0%** | 100% | — | 2.66（3 种） | 0（无此规则） |
> | `kr1s1` | 规则1 | **100.0%** | 0% | **26.0%** | **3.21（5 种）** | 0.85 |
> | `kr1s2` | 规则1 | 16.7% | 83% | 26.7% | 2.69（3 种） | 0.99 |
> | `kr1s3` | 规则1 | **81.3%** | 19% | 20.7% | 2.13（4 种） | 0.89 |
> | `kr2s1` | 规则2 | **63.3%** | 37% | 21.3% | 2.00（2 种） | 0.93 |
> | `kr2s2` | 规则2 | **93.3%** | 7% | 20.7% | 1.67（3 种） | 0.94 |
> | `kr2s3` | 规则2 | 0.0% | 100% | 16.0% | 1.00（1 种） | 0.98 |
> | `kr0s1` | **对照** | 0.0% | 100% | 14.0% | 1.00（1 种） | 0.84 |
> | `kr0s2` | **对照** | 0.0% | 100% | **36.7%** | 1.00（1 种） | 0.87 |
> | `kr0s3` | **对照** | 0.0% | 100% | 22.0% | 1.00（1 种） | 0.97 |
>
> **三条结论（按可信度排）**：
> ① **方向干净的那条**：破防能力 **规则臂 4/6 > 50%，对照臂 0/3，现役 0/1**，而且**规则关掉之后仍然成立**
>   ⇒ 它是包学到的能力，不是规则的副产品。机制：奖励击杀 ⇒ 训练压力偏向"能把人打死"的打法，
>   而破防场要求的恰恰是"杀得动不还手的防御者"。所以 §N49 的判语要补一句：
>   **击杀奖励作为上线规则在龟缩场结构上失效（那里根本没有有来源的死亡），但作为训练课程/筛选器是有效的。**
> ② **否定的那条（很重要）**：把"学过规则的包"放回规则世界，它的提升**不比对照臂大**（对照 `kr0s2` 从 14.0%
>   跳到 46.7%，比任何规则臂都猛）⇒ **"学没学过这条规则"几乎不重要，规则本身只是在挑"本来就爱杀人"的打法**
>   ⇒ 别指望"先改规则再让 AI 学"，**直接在现状规则下按"能不能破防/杀得动人"当选**是同一件事的省事做法。
> ③ **种子方差主导**：同一套代码三种子，头对头 14.0%~36.7%、对照臂广度全塌到 1 种 ⇒ 今晚所有单臂 Δ
>   都要按"n=3 的种子噪声"读；`kr0s2` 头对头最高（36.7%）却广度最差（1 种）——**胜率与广度再次不是一回事**（与 §N24/§N32 同向）。
> - **预注册打分（§K-1 的表，跑臂前写死）**：P1 ✗（规则臂不比对照强）· P2 **部分过**（`kr1s1` 现状世界 26.0% > 现役 16.7% 且广度 3.21 > 2.66）
>   · P3 只有 `kr1s1`/`kr1s2` 过线 · P4 **规则臂 4/6 vs 对照 0/3** · P5 ✓（规则臂发放次数普遍更高，机制确实生效）。
>
> **顺手给探针加的三样**：`--field=guardwall`（4 席只防御不还手＝破防栏）、`--field=vs --opp=<包>`（头对头）、
> 以及"出手 G→净兑现 G"广度列。⚠️ 广度列第一版**缓存键用的是场名而不是包名** ⇒ 九行读数全同（假数据），已修。
>
> **D140（新门 · 语义逐条钉，不许只在真对局里"顺带验到"）**：9 个合成局面 ——
> 单点按 pri 付 / 同 pri 取高开销 / 仍并列都不回 / 蓄能 +1 翻转同优先级判定 / 规则 2 按有效伤害付且 overkill 不付 /
> 地雷·天火走 `mineFrom`·`fireFrom` / 转移记给可切 / **真无主的死亡（铁索传导·禁用扣血·终局收缩）一律不回也不许崩** /
> 补丁锚点漂了必须抛 / 补丁**行为中性**（档 0 与未打补丁 40 局逐位相同）。
>
> **D82 产物账本（24h 内落盘的 9 个 `.bak`，逐名点名 · 都是臂的带内候选，`docs/artifacts/`）**：
> `kr1s1-band2` `kr1s2-band6` `kr1s3-band4`（规则 1 世界 ×3 种子）·
> `kr2s1-band2` `kr2s2-band6` `kr2s3-band6`（规则 2 世界 ×3 种子）·
> `kr0s1-band2` `kr0s2-band4` `kr0s3-band3`（**对照**：同代码同种子、规则关着训）。
> 表里每一行都来自这 9 个文件；带内其余落选带与冒烟产物（`krsmoke-band1/2`）是我的试验残留，已删。
> **没有一只进槽**（红线：不换槽 / 不 promote / 不动 main / 不提交 `results/*`）——`kr1s1-band2` 值得用户看一眼，
> 但"n=3 种子"这一条不足以支撑换槽。
>
> **待用户裁定**：① 转移伤害归因默认记给**转移者**（另一种读法已做成开关，要不要换）；
> ② 激光眼"续招"那次不吃珠（费用 2）⇒ 现在**不**按 +1 计，要不要计；
> ③ 结论 ② 指向的省事做法——**直接按"破防场 1st"当选**——要不要我做一版量出来（比上线新规则便宜得多）。


## v1.5.193 — 击杀奖励实验台 `tools/probe-kill-reward.mjs`（用户给的两条防龟规则）＝ **两条都防不了龟，原因很干净：龟缩场里没有一次死亡"有来源"** ＋ 门 D139

> **要量的东西**（用户 09-23 夜给的规则，方向来自 Gemini 的"给击杀发奖励"）：
> **规则 1** 打死人回 1 ep —— 集火时只回给"所用技能**优先级最高**"的那位，同优先级取**开销最高**，仍并列则**都不回**；
> **规则 2** 每个参与者按**造成的伤害**回等量 ep —— 但"技能结算时目标血量已归零"的那份不给（overkill 不付费）。
>
> **怎么做的（关键：不动仓库引擎）**：在内存里给 `js/core/play.js` 打补丁（`resolveActions` 之后、`endTurn` 之前挂钩子），
> 每个规则档各起一个 vm 沙箱 ⇒ 三档只差规则，包/种子/对手全同。
> "优先级"直接取卡表字段 `R.byKey[key].pri`（小雷 5 · 大雷 4 · 默认攻击 3 · 枪 2 · 狙击/净化 1），归因取 `damage.source`。
> ⚠️ 锚点找不到就**抛错**：第一版我用 `\n` 拼锚点而工作区是 CRLF ⇒ 当场抛了。静默没打上补丁 = 三档跑同一个引擎 = 整份实验是假的。
>
> **读数（multi · 300 局/格 · 同种子配对 ⇒ 每个 Δ 都带 1.96·SE）**
>
> | 场 | 包 | 规则 1 Δ胜率 | 规则 2 Δ胜率 | 奖励发放（次/局） |
> |---|---|---|---|---|
> | **破防（防龟靶心）** | **现役 `cmin4-31`** | **+0.0（逐位相同）** | **+0.0（逐位相同）** | **0.00 / 0.00** |
> | 破防 | `aim3-93`/`divK-31`/`cmin4-82` | +0.0（本来就 100% 胜） | +0.0 | 0.90 / 0.13 / 0.16 |
> | 脚本池 | 现役 | −1.7 噪声内 | −1.0 噪声内 | 0.99 / 0.93 |
> | 脚本池 | `aim3-93` | −3.7 噪声内 | **−6.0 显著** | 0.98 / 1.01 |
> | 脚本池 | `cmin4-82` | −1.3 噪声内 | **−4.0 显著** | 0.94 / 0.92 |
> | 镜像 | `cmin4-82` | **+5.3 显著** | +0.3 | 0.40 / 0.49 |
>
> **三条结论**：
> ① **破防场里"没变化"不是"没效果"，是根本没触发** —— 现役包在那场 `0% 胜 / 100% 平 / 局长 47 / 五席全死`，
>   而**奖励发放 = 0.00 次/局**：那些死亡没有一次是"被某个**有来源**的伤害打死的"（都是原型制御反弹/自损，`source=null`）
>   ⇒ **任何"按击杀归因"的奖励都碰不到龟缩场**（与 v1.5.37 那次"`voided.by` 是原因字符串不是 pid ⇒ 奖励无法归因"同族）。
> ② **能触发的地方普遍不帮忙**：脚本池场发放 ~1 次/局，但规则 2 对两只宽包**显著为负**（−6.0 / −4.0）⇒ 它奖励"参与伤害"，
>   而参与伤害在龟缩场不发生 ⇒ 实际是放大强弱差、偏向本来就会打的包，不是修龟。
> ③ 唯一显著的正面（镜像场 `cmin4-82` +5.3pt）方向也不干净：那是"多一发 ep 多一发大招"的复利，与防龟无关。
> **⇒ 建议放弃"击杀奖励"这个方向，或换归因**：想治的其实是"没人愿意先动手"，可观测标志不是"谁打死了谁"，而是"**防御方是否被迫消耗**"。
> 三条候选（都属**规则换代**，需用户裁定 + 指纹必换）：(a) 缩短 `maxRounds` / 到上限不再按血量判胜；
> **(b) 防御族要花钱**（现在 `guard`/`reflect` 是 0 ep ⇒ 设防免费，这才是龟的根）；**(c) 把"破防场能赢"升成上线判据**（不动规则，代价最小）。
> 我的倾向是 **(b) 或 (c)**。
>
> **门 D139** 钉的是**实验台本身**：① 补丁必须真打上（锚点漂了 ⇒ 工具必须非 0 退出，不许静默跑成 A/A）；
> ② 破防场里现役必须"三档读数逐位相同 **且** 发放 = 0"（把这条否证钉住，防止将来有人以为它有效）；
> ③ 脚本池场必须"发放 > 0.3 次/局 ≥2 格"（否则上面那些"逐位相同"只是"永远不发放"的同义反复）；
> ④ 输出必须带**配对噪声**（±1.96SE），不然 ±5pt 抖动会被当结论。
>
> 未动线上包 · **规则指纹 `ebdbff36` 未变**（引擎文件一字未改，补丁只在内存里）· **门禁**：`np-test 185/185`（新增 D139）·
> `spec 52/52` · `smoke OK` · 本版无新落盘 `.bak`。

## v1.5.192 — 拦截探针加**自适应场**（`champ`：其余席 = 同一只包）⇒ **撤回 v1.5.191 里"大雷买的是逼防/压制"那句解读**：逼防通道在可测的意义上不存在，所以"逼防收益项"先别做

> **来龙去脉**：用户 09-23 晚说"那就试一下"（把"逼对手设防"做成收益项）。按本仓规矩，做收益项之前必须先**把标度量出来**
> （`WIDTH_W`/`CLEAR_W` 那几次凑标度的教训）⇒ 于是给 `tools/probe-intercept-cost.mjs` 加了一口**自适应场**：
> 其余四席 = 同一只包的策略 chooser（会看状态），并记"对手这一手是防还是打"。
> 固定脚本席的设防是**规定动作**，它涨不涨跟我怎么打无关 ⇒ "逼防"这种因果量在脚本场里**结构上测不到**（这是我 v1.5.191 那张表的口径漏洞）。
>
> **读数把上一轮的解读否证了**：
> - 镜像场的对手设防率 **基线 0% ⇒ 任何强制臂下仍是 0%** —— 现役这只包**从不设防**；`每局逼防次数` 全为负（大雷 −0.18 · 枪 −0.03 · 狙击枪 −0.17）。
> - 对手"进攻次数"确实从 96.5/局 掉到 24~45/局，但那是**被打死了**（死人不出手），不是被吓住。
> - **⇒ 逼防收益项不该建**：唯一会看状态的对手没有"防"这个动作可被逼；拿脚本场的设防率当标度 = 把规定动作记成自己的功劳。
>   **v1.5.191 里那句"它买的是压制（对手把回合花在设防上）"由我撤回。**
>
> **但这轮量到一条更干净的**（120 局/格 · 混合场 · 两臂同垫钱 ⇒ 钱相同，差别只在用不用它）：
>
> | 臂 | 1st | 造成伤害/局 | 打死对手/局 | 对手进攻/局 | 局长 |
> |---|---|---|---|---|---|
> | 基线（冠军自由发挥） | 65% | **8.53** | 3.65 | 35.5 | 34.8 |
> | 强制只用大雷 | **99%** | **6.33** | 3.99 | **8.1** | 46.9 |
> | 强制只用枪 | 41% | 2.18 | 3.41 | 41.5 | 36.9 |
>
> ⇒ 大雷臂**伤害反而更低**（6.33 < 8.53）却 1st **+34pt**，同时对手进攻 35.5→8.1 次/局、局长 34.8→46.9。
> 它买的确实不是"打死人"，但也不是"设防"——更像**对手腾不出手 / 我活得更久**。
> 可测口径应当是"**对手进攻行动数 ÷ 对手回合数**"（被压制度），它在镜像场里也非平凡（对手真会打我）。
> ⚠️ **这条也还没证成因果**：残血对手本来就更少出手 ⇒ 要下一轮做"同伤害不同卡"的对照才能定 ⇒ 所以本版**不动目标函数**。
>
> **代码**：`FIELDS` 加 `champ`（`adaptive: true`）；对手 chooser 走同一个 `baseSel()` 流并同样禁用被测卡（保持归因口径一致）；
> 每臂记 `oppGuard/oppAtk` ⇒ 新增**逼防效应表**（设防率 基线→强制 · 对手进攻 基线→强制 · Δ自适应 1st · **每局逼防次数**）；
> `--json` 合并成**一份**输出（原来写了两个互斥分支，第二条会把 `rows/judged` 整份丢掉）。
>
> **门禁**：`np-test 184/184`（D138 复跑通过，含新场与单份 JSON）· `spec 52/52` · `smoke OK` · 指纹 `ebdbff36` 未变 · 两槽线上包未动 · 本版无新落盘 `.bak`。

## v1.5.191 — 新量具 `tools/probe-intercept-cost.mjs`（拦截代价表）＝ **用户那条"AI 怕被拦截所以不用"的直觉，量出来方向是反的** ＋ 门 D138
> ⚠️ **本节的"大雷买的是压制（逼对手设防）"这句已被 v1.5.192 撤回**（自适应场实测：对手设防率恒 0%）。
> 表里的三条读数（18 张卡没被挡过 / 枪与激光剑是理性回避 / 大雷被挡 93% 却仍 +32pt）**全部仍然成立**，被撤的只是对第三行的机制解释。

> **问的什么**：现役包广度差，是不是因为"它倾向于完全不用可能被拦截的技能"（用户 09-23 晚的原话）。
> 这句话里混着两件事，必须拆开：① 这张卡**真会被挡**吗、挡一次代价多少；② 在会防的场里用它到底亏不亏。
> **三口对手场**（会防 / 混合=半数席设防 / 不防 / 只反弹）· 强制 ε=1（合法且买得起就用这一张）·
> 默认 `--grant=card`（**两臂同垫**到刚够这张卡 ⇒ 钱相同，差别只在用不用它）· 现役包 · 100 局/格。
> 归因走发射端：`blocked{via,by}` / `reflect{via,from}`；⚠️ **`blocked` 没有 `source`** ⇒ 所有对手一律**禁用被测卡**，否则被挡率会混进别人的手。
>
> **读数（21 张里 18 张被挡率 = 0%）**
>
> | 卡 | 混合场被挡率 | Δ混合 | Δ不防 | 拦截代价 | 判读 |
> |---|---|---|---|---|---|
> | 真正的落雷（大雷） | **93%** | **+32pt** | +69 | −37* | **L 惰性回避** |
> | 激光剑 | 90% | −23 | +69 | −92* | **R 理性回避** |
> | 枪 | 88% | −24 | +69 | −93* | **R 理性回避** |
> | 其余 18 张 | **0%** | −61…+33 | — | — | **N 拦截解释不成立** |
> | 摄魂指法 | 0% | — | — | — | **U 未测到**（0.0 次/局） |
>
> **⇒ 三条结论**：① "怕被挡"对绝大多数卡**不成立**（它们根本没被挡过）；② 真会被拦的三张里，**枪/激光剑是理性回避**
> （两场符号翻转：会防 −24/−23、不防 +69 ⇒ 躲它没毛病）；③ **大雷是反例且是最重的那张** —— 被挡 93%，
> 但在会防的场里强制用它**仍然 +32pt**（伤害只有 6.3/局）⇒ 它买的是**压制**（对手把回合花在设防上），不是伤害。
> **所以直觉方向反了**：现役收敛到的恰恰是最容易被打防的**枪**，躲开的是被挡最多却仍净赚的**大雷**。
> 广度差的真成因不是"怕拦截"，而是**目标函数里没有"逼对手防御"这个收益** —— 这正是 Q-12（min-over-counters）的一种具体化。
>
> **⚠️ 我中途判错两次（都写进工具头注，并由门 D138 钉住）**：
> ① 第一版拿 `Δ混合 < 0` 当"理性"的证据 —— ε=1 的"强制只用这一招"自带"只会一招"的惩罚，任何弱卡两场都为负
>    （实测 ジ −47/−11、雷击之枪 −61/−25）⇒ 会把"单调"读成"被挡" ⇒ 改成要求**两场符号翻转**；
> ② 第二版又把判读整个交给交互项（`Δ混合−Δ不防` 为负），于是把大雷判成"理性回避"，与它 `Δ混合=+32pt` 直接矛盾
>    ⇒ 判读看**绝对水平**，交互项退回当**机制列**。
> 另两个是本仓老坑、我又踩了一遍：**没测到 ≠ 不成立**（猛攻场里受评席 4 回合就死 ⇒ 贵卡一次都没出手 ⇒ 0% 被挡率是假的
> ⇒ 现在单独成 **U** 类并优先判，`--grant=0` 时实测就落到 U）；**对手池用单一 `pickAggro` 会让基线贴 100%**
> ⇒ Δ 只能 ≤0，R/L 结构上不可能出现（那是设计错，不是结论）⇒ 进攻侧改成轮换线池。
>
> **口径边界（必须一起读）**：ε=1 + 定向垫钱测的是"每回合都用、钱刚好够"的世界，**不是**"该不该为它攒钱"；
> `reflect` 场基线 100% ⇒ 无分辨空间；`def` 场基线 20% ⇒ 全员每回合设防时冠军自由发挥本身很差。
> 与今天早些时候的 Q-14 读数不冲突：那张表说"垫钱的世界里大雷能赚"，而 Q-14 说的是"这份赚**长不进原生经济**"。
>
> **门 D138**（全判产物，不钉文本）：① 归因活着 —— 枪必须真出手且被挡率 >10%，并必须记下"被哪张防御卡挡的"（`blocked.by`）；
> ② `U` 必须能单独成类且优先于 N/R/L（跑 `--grant=0 --only=drain` 实测判 U）；③ 判据形状从 `--json` 逐条复核
> （R 必须符号翻转、L 必须"真被挡 + Δ混合>0"、N 只能给被挡率 <10%）；④ 分类合计与判读规则必须印出来。
>
> 未动线上包 · **规则指纹 `ebdbff36` 未变**（只加 `tools/` 与门禁）· **门禁**：`np-test 184/184`（新增 D138）· `spec 52/52` · `smoke OK` · 本版无新落盘 `.bak`。

## v1.5.190 — 三把量具各修一处"会把读不出当成没价值"的病：`skill-report` 判定过显著性 · 通吃矩阵按**除 2P** 最弱格排序 · 边际价值表把**钱墙 / 算力**拆开 ＋ 门 D137 ＋ §N46 两批读数（Q-3 / Q-9 / Q-10）

> **动因**：用户 09-23 傍晚裁"连带这条线不做了，继续做剩下的" ⇒ 挑了两件纯量具/证据活：**① 历史包过通吃矩阵**（Q-3 缺的那半份证据）、
> **② skill report 的可测量性**（Q-9 措辞与门槛 / Q-10 算力）。两件都在跑的过程中各撞到一个**结构性**问题，所以本版是"改尺 + 读数"，**没有动任何判据、产物或线上包**。
>
> **1) `tools/skill-report.mjs`（Q-9）**：`runCondition` 现在**逐场留胜负向量** ⇒ 与基线**同种子逐场配对**求差（`pairedDiff`）⇒ 每行有真配对 SE。
> 判定门槛从 `|Δ|>0.5pt` 换成 **`|Δ|>1.96·SE`** —— 默认口径（126 组 × 6 局 = **756 场/条件**）实测配对 1.96·SE **中位 ±3.4pt、最差 ±4.6pt**
> ⇒ 旧门槛**比本工具自己的噪声还小**，有一部分四象限标签其实是贴给噪声的。分不出的统一标"噪声内"，每行另印"要辨 `--target`（默认 2pt）需几局/组"（SE∝1/√n），
> HTML 加两张卡（过显著性几张 / 噪声内几张）与一句写死的规矩。**⚠️ 我自己的假设被数据否证一半**：抬门槛后**只有 1 张**卡从"有判定"变成"噪声内"
> （mono-spam 的效应本来就有 ±10~80pt）⇒ 门槛该改，但它**不是**"21/30 读不出"的成因 —— 那 21 张在**边际价值表**里。
>
> **2) `tools/probe-skill-marginal.mjs`（Q-10）**：汇总新增"**病因拆开**"一行 —— `机会≈0` 的判据是 `chance`（每局几次机会），
> **这个量与局数无关** ⇒ 加算力救不了它，只有换口径（`--rich=card`）才动得；`噪声内` 才是算力问题（顺带报"全部辨到目标 SE 最多需几局"）。
> 实测（现役包 · 30 张 · 80→250 局 = ×3.1 算力）：可测 `long 原生 3→2`、`multi 原生 0→4`（**基本没动**，还出现阈值边界抖动式的倒退）；
> 换定向垫钱口径 `long 10→14`、`multi 10→12` 且 `机会≈0` **归零** ⇒ **对 Q-10 的答复是"别给算力，给口径"**。
>
> **3) `tools/probe-cross-mode.mjs`（通吃矩阵）**：3P 包塞进 2P 格是**结构性 0% 胜 / 100% 平**（今天 9 粒里 7 粒如此，含现役）
> ⇒ 含 2P 的"最弱格"对这批包恒 0、排序失效。改成**双列打印**（含 2P | 除 2P）+ **按"除 2P 最弱格"排序**，并在"读法"里写死这一条。
> ⚠️ 顺手记我一个假修法：第一版把 JSON 导出改成 `rows: rows`，而 `rows` 是**每个环境的局部数组**、顶层拿不到 ⇒ 改成对跨环境的 `out` 做"同环境内按 `weakestMulti` 降序"的稳定排序。
>
> **门 D137**（三向，全部**判产物不判打印**）：① 跑真 `skill-report` 并从 `--json` 逐行复核不变式 —— 贴四象限标签的卡必须 `|Δ|>1.96·SE`、
> 承重/陷阱必须 `|Δ_lost|>1.96·banSE`、每行必须带 SE、价码与"可测量性"汇总必须在（`--target=999` 不能改判定这件事也被写进 JSON 的 `targetPt`）；
> ② 跑真矩阵断言 `weakestMulti` 存在 + **JSON 里确实按它排序** + 必须存在"含 2P ≠ 除 2P"的行（否则这条改动是空操作）；
> ③ 跑真边际表要求"病因拆开"行在 + 不许出现"需 0 局"这种假价码。
>
> **§N46 的 Q-3 答复（只量没换）**：九粒包 × 四环境 × 五格 ⇒ **没有任何一粒在三栏上都赢现役**。
> 破防栏：现役**只有 4P 能赢，其余四格 0% 胜 / 100% 平**（第三次证实 §N37"G5 绿 ≠ 能赢"），而 `v7divK-31`（五格全 100%，唯一连 2P 也能）/
> `v7cmin4-82` / `v7cmin3-93` 除 2P 全清；对现役栏最弱格最高的是 `v7divK-82` 26%（3P 场上 81% vs 现役 28%）与 `v7cmin3-82` 22%（现役 16%）；
> 净兑现 `landG` 最高的是 `v7aim3-93`（3.27~3.52 · 4~5 种）。⇒ **不建议以"通吃"为由动槽**；要做第三个可导入包，理由必须指明**补哪一栏**（现役最实在的缺口是破防）。
>
> 未动线上包（两槽照旧：2P `v7xfer44c13-2p` · 3P `v7cmin4-31`）· **规则指纹 `ebdbff36` 未变**（本版只动 `tools/`）·
> `docs/skill-report.html` 按**原口径**重生成（5 人 · multi · 126 组 × 6 局 ⇒ 与在库版可比；我中途误用默认 N=3 跑过一次，已 `git checkout` 撤销后按 5 人重跑）。
> **门禁**：`np-test 183/183`（新增 D137）· `spec 52/52` · `smoke OK` · 本版**无新落盘 `.bak`**（臂外产物只写 `docs/artifacts/` 的都在 v1.5.189 点过名）。

## v1.5.189 — 示范归因：把**教师的手**从**包自己的手**里分出来（`tagImitActions` · 只记账不进 fit）＝ **推翻了我昨天那句话**，这条线的结论改成"教得出'有钱就打'，教不出'愿意攒钱'" ＋ 门 D136

> **为什么做**（Q-14 结案后留下的唯一欠账）：`makeEconChooser` 的 override 是直接 `return ta` ⇒ 注入的那一手与原生出手在事件流里
> **没有任何区别**。后果不是"某个奖励项没生效"，而是**"示范到底学会了没有"这个问题在臂上根本没有读法** ——
> 我是靠"W=0.5 + 示范全关"那条单变量对照才看出苗头（见 v1.5.187/188），而那条对照只能证明"没有教师就没有链"，说不清"包自己会不会打"。
>
> **改了什么**（全部在 `js/train/evo.js`，非指纹文件；**只加记账，不进 `gFit`、不动 rng**）：
> `makeEconChooser` 的本局台账 `rec.imit`（按卡名计"教师替该席伸了几次手"，懒创建 ⇒ 示范关时零开销）
> \+ `tagImitActions(events, pend, seat, {subsidy})` 局末按事件顺序给该席 `outcome:'ok'` 的 `action` 事件补 `e.imit = true`
> \+ 累计账 `imitAttribution()` / `resetImitAttribution()`（`resetImitStat()` 顺带清，防串代）\+ CLI 逐代与臂末印
> **分子（注入）· 原生 · 且原生再按"补贴局 / 原生经济局"分桶**。
> ⚠️ 诚实标注一条精度边界：**同一张卡被打多次时"哪一条是注入的"顺序相关、不保证；条数保证** ⇒ 对外只承诺计数。
>
> **★ 读数（同配方 60 代 × 3 人 × 8 局 × 种群 8 · seed 1 · `arm=attr60d`）**：账先对上（`fired=140 == tagged=140`、`unmatched=0`），然后——
>
> | 口径 | 注入 | **原生** | 原生里在补贴局 | **原生里在原生经济局** |
> |---|---|---|---|---|
> | 大雷 · 全臂 | 140 | **1973** | 1973 | **0** |
> | 大雷 · 退火窗口后（gen≥30，β≡0） | 0 | **1756** | 1756 | **0** |
> | 其余全部卡 · 全臂（对照，证明分桶不是恒零尺） | 0 | 57109 | 19061 | **40021** |
>
> **⇒ 推翻我 v1.5.187/188 里的一句话**："非注入出手恒 0"是在**最优个体读数 + 产物口径**上量的（那几个打印点确实 0），
> 但**种群层面包自己大量打大雷**，只是**一次都不为它攒钱**。所以这条线的真结论不是"选择不给它付钱"，而是
> **示范 + 补贴教得出"有钱就打"，教不出"愿意为它攒钱"** —— 与 DS 的"钱≠意愿"互为镜像（他证"给钱教不会用卡"，我证"教会用的那半只活在给钱的世界里"）。
> **还能动的只剩一处**（属判据层，我没动）：把连带/贵卡收益项的**计数口径**限定成"只在原生经济局里数" ⇒ 那才是跨期选择的信号。
> 但按今天这张表，开这一刀之后这一项**必然仍是 0** ⇒ 更支持 Q-14 走 ③ 结案（代码保留、默认关）。
>
> **⚠️ 我自己又踩了一次本仓那条同族病（写进坑清单）**：第一版把打标记挂在"本局有注入"上 ⇒ **退火窗口之后整段没有读数**
> （臂日志停在 gen 27，我差点把它读成"窗口后没人打大雷"）。这就是"量具自己砍掉要测的那半"，与 v1.5.187 的"读数跟着开关关"同源。
> 现在每局都过一遍（仅 `IMIT_OVERRIDE` 开着才过 ⇒ 不用示范的臂零开销），并让门 **D136** 专门钉：
> ①归因口径（只数该席 `outcome:ok`；`voided` 不记 ⇒ 与 `countBigCards` 同口径不可刷；条数保证、逐条身份不保证）
> \+ ②**空台账也必须计数** \+ ③`unmatched` 必须可见（注入 3 次只有 1 次成事件 ⇒ 记 2）\+ ④**β=0 时 fit 逐位不变**（且必须先证明归因真的跑过，防"空枪证明"）
> \+ ⑤真跑一臂：窗口后仍要有读数、`原生经济` 桶不许是恒零尺、臂末 `unmatched=0`。
>
> **零变化**：`W=0` 复跑臂（`chainrate0c`）六粒 band 的权重哈希与 `chainbase` / `chainrate0` **逐位相同**
> （`c0a6f85115 / 5e7a4cc40d / 559a3f6c3e / 1912a6672d×3`）⇒ 归因对行为零影响（默认关 + 示范关两条路都零开销）。
> 未动线上包（两槽照旧）· **规则指纹 `ebdbff36` 未变** · **门禁**：`np-test 182/182`（新增 D136）· `spec 52/52` · `smoke OK`。
> **D82 记账（本次 24h 内**留在盘上**的 `.bak` 逐名 · 全在 `docs/artifacts/`，git 未跟踪）**：
> `attr60d-band1.bak` · `attr60d-band2.bak` · `attr60d-band3.bak` · `attr60d-band4.bak` · `attr60d-band5.bak` · `attr60d-band6.bak`（这只臂就是上面那张归因表的来源）·
> `chainrate0c-band1.bak` · `chainrate0c-band2.bak` · `chainrate0c-band3.bak` · `chainrate0c-band4.bak` · `chainrate0c-band5.bak` · `chainrate0c-band6.bak`（零变化对照）。
> 另：调参过程中落盘后被我删除的中间臂逐名 `attrprobe-band1..6.bak` · `attrprobe2-band1..6.bak` · `attr60-band1..6.bak` · `attr60b-band1..6.bak` · `attr60c-band1..6.bak` · `chainrate0b-band1..6.bak`
> （前四只是 6 代探针与被 v1.5.189 修正前的实现产物；`chainrate0b` 被 `chainrate0c` 取代。它们的读数已在 §N45/§N44 记录，文件已删、未提交）。

## v1.5.188 — Q-14 裁定件 ②：连带收益项从**绝对计数**换成**按率付**（`链数 / 该席大雷出手数`）＝ **治好了"吃掉目标函数"，但仍未出货** ＋ 拿到这条线真正的机制解释 ＋ 门 D135 升级

> **改了什么**（用户 09-23 午后裁 **Q-14 = ②**）：`chainBonus` 从 `W × min(1, 链数/1)` 换成
> `W × min(1, 该席连带数 / 该席大雷出手数)`。新计数器 `countBigTCasts` 只认 `outcome === 'ok'` 的 `R.SK.BIG_T`
> （与 `countBigCards` 同口径 ⇒ 被无效化的不算，不可刷；卡从声明取，不写字面卡名）。**0 出手 ⇒ 0 分**，显式不走 `0/0`。
> 分子分母**都不受权重门控**；`bigTChainReward()` 连 `shape:'rate'` 一起读回 ⇒ CLI 读不到 `'rate'` 就 `exit 7`
> （这次改的就是形状，"权重读回了"证明不了项按哪种方式进 fit —— 同一族"seam 2"病）。臂上每代把**分子/分母/率一起印**
> （只印分子时"1 条 / 1 次出手"与"1 条 / 8 次出手"在尺子上同形，而这正是换形状要分开的那两件事）。
>
> **预注册在先、四臂同种子（判据写在 §N44 开头，跑之前落盘）**：base `chainbase`（W=0，旧计数形代码）· `chainrate0` · W=0.15 · W=0.5 · 外加 **W=0.5 但示范全关**：
>
> | 臂 | 考卷 1st | 净落地 `G` | 大雷出手/局 | **连带/局** | CLI 自评 3P |
> |---|---|---|---|---|---|
> | base W=0 | 35.4% | 3.29 | 0.00 | 0.00 | 43.9% |
> | 率形 W=0（复跑 ⇒ 零变化对照） | 35.4% | 3.29 | 0.00 | 0.00 | 43.9% |
> | **率形 W=0.15** | **38.5%** | 2.00 | **0.00** | **0.00** ✗ | 59.2% |
> | **率形 W=0.5** | 36.8% | **1.00** ✗ | **0.00** | **0.00** ✗ | 50.4% |
>
> - **① 形状确实治好了那个病**：v1.5.187 计数形 W=1.5 把考卷打到 **23.5%**、净落地 **1.24**；率形两档考卷 **36.8%~38.5%**（不降反微升）
>   ⇒ "一发幸运链吃满 W"这个失效模式**没了**。但**判据 1（出货）仍未过**：两档 × 三格 ⇒ 出手与连带全 0；且 W=0.5 档净落地 **跌破 1.5 广度准入线**（预注册的红线，按约判"买坏了"）。
> - **② 今天真正值钱的一条（机制解释，已钉进门 D135 ④b）**：把示范关掉、只留 `W=0.5` ⇒ 60 代里**"出手"恒 0、链恒 0**。
>   ⇒ 这一项的非零信号**只能来自教师注入的那几手**：`makeEconChooser` 的 override 直接 `return ta`，事件流里**没有任何标记**区分
>   "这一发是包自己选的"与"教师替它选的" ⇒ 分子分母数到的都是**教师的手**，不是这个包的策略。
>   ⇒ **①（抬密度）与②（换率形）都改不了"这一项在评谁"** —— 这不是剂量问题，是**归因对象**问题。
>   剩下的唯一实招是 **④ 给注入打标记、只数非注入出手**；但同一批数据说：**非注入出手在这族群里恒 0** ⇒ 打完标记这项仍是空枪（只是不再骗人）。
>   **⇒ 我的建议转 ③ 结案**：代码保留（默认 0、中性已证），下一版不再往 `gFit` 加第五项；要动就去动**判据层**（Q-12 min-over-counters）或**价码层**（规则侧，用户裁）。
> - **③ 零变化的证法又学一条**：第一次比 `.bak` **整文件哈希**不相等，差异全在头注与 `META` 的 `arm`/`ts` ⇒
>   判"权重有没有变"必须**剥壳只哈希 `a` 数组**。剥壳后 `chainrate0` 六粒与 `chainbase` **逐位相同**
>   （`c0a6f85115` · `5e7a4cc40d` · `559a3f6c3e` · `1912a6672d`×3）⇒ 默认关确实一字不变。
>
> **门 D135 升级**（原来只判"权重推得动 fit"，现在判的是这套新语义）：分母口径四例（`voided` 不算 · 别人席不算 · 贵卡非大雷不算 · 链事件本身不算）
> \+ `shape` 读回 \+ W=0 时分子**与分母与率**三者都在 \+ **`W=4` 但分母 0 ⇒ fit 一分不许变**（"0 出手却吃到连带奖励"的防伪钉）
> \+ 横幅必须印读到的形状 \+ **两臂对照真跑**（示范开着 ⇒ 印分子/分母/率；示范关掉 ⇒ 出手与链恒 0）。
>
> 未动线上包（两槽照旧）· **规则指纹 `ebdbff36` 未变**（只动训练侧与门禁）· 历史臂 `chainw15`（计数形 W=1.5）**作废为"可复现配方"**，读数仍算证据但形状已被本取代。
> **门禁**：`np-test 181/181` · `spec 52/52` · `smoke OK`。
> **D82 记账（本次 24h 内新落盘的 `.bak` 逐名 · 全部 `docs/artifacts/`，均未被 git 跟踪）**：
> `chainrate0-band1.bak` · `chainrate0-band2.bak` · `chainrate0-band3.bak` · `chainrate0-band4.bak` · `chainrate0-band5.bak` · `chainrate0-band6.bak` ·
> `chainrate015-band1.bak` · `chainrate015-band2.bak` · `chainrate015-band3.bak` · `chainrate015-band4.bak` · `chainrate015-band5.bak` · `chainrate015-band6.bak` ·
> `chainrate05-band1.bak` · `chainrate05-band2.bak` · `chainrate05-band3.bak`（**当选**）· `chainrate05-band4.bak` · `chainrate05-band5.bak` · `chainrate05-band6.bak` ·
> `chainteacherless-band1.bak` · `chainteacherless-band2.bak` · `chainteacherless-band3.bak` · `chainteacherless-band4.bak` · `chainteacherless-band5.bak` · `chainteacherless-band6.bak`。
> （`chainrate015-band1.bak` 是该臂**当选**粒；`chainrate0-band*` 与 `chainbase-band*` 的逐位相同就是上面的零变化证据。）

## v1.5.187 — 大雷**连带收益项**接进 `gFit`（DS 交接 §2b 的"唯一待做"）＝ **接线成功 · 判活成功 · 验收未过**（产物仍 0.000/局，而 W 一大就吃掉目标函数）＋ 门 D135

> **做了什么**（照 DS 的接线清单逐条；出厂默认 0 ⇒ 不加项、行为一字不变）：
> `js/train/evo.js` 新增 `BIGT_CHAIN_W`（默认 0）+ `countBigTChain(events, seat, R)`（**只认 `e.type === 'bigTChain' && e.from === seat`**
> —— DS 交接 §3 坑#1 的教训：`voided` 事件没有 `reason`，原因在 `by`；连带的主标记也不是 `damage.reason`）
> ⇒ 进 `gFit` 的形状 `BIGT_CHAIN_W × min(1, 该席本快照链数 / 1)`（标度依据：教师当席位实测 0.35~0.55 次/局 ⇒ 0→1 是完整梯度）
> \+ `setEconomyReward({bigtChainW})` / `economyReward()` / `reset` / `bigTChainReward()` 全套 \+ `server/econ-env.mjs` 三处 \+ CLI 下达（`exit 7` 拒静默空转）\+ 门 **D135**。
> **关键设计（这条线差点被我判死）**：读数 `chainEvents` / `chainPerGame` **不受权重门控** —— 只有 W>0 才统计的话，
> "链数为 0"与"作用点死了"两种世界**长得一模一样**。
>
> **判活这一仗我差点判错**（诚实记录）：先跑 W=0 / 0.05 / 0.15 三档 ⇒ 输出**逐字节相同** ⇒ 我下了"死作用点"的结论。
> 补了不受门控的读数再跑 **W=1.5** ⇒ `gen 0 bestFit=1.011 … **连带=1 条/0.125每局**`，band trainFit 从 base 臂的
> `1.245/1.003/0.979` 抬到 `1.712/1.697/1.631` ⇒ **权重是活的，之前只是链数真的是 0**（三档都太小、看不到一条链）。
>
> **验收（判据按用户/DS 口径写死：产物**连带 ≥ 0.2/局**；不要求出手率；不许打坏强度/`G`/座位极差）⇒ 未过**：
> 同一配方（`IMIT_OVERRIDE=1` + `IMIT_ONLY=bigT` + `IMIT_TEACHER=pickBigTChain` + `IMIT_FRAC=0.5` + `REGEN_SLICE=0.25`）·
> 同种子 · 60 代 × 3 人 × 8 局 × 种群 8 的两臂（`chainbase` vs `chainw15`），产物各取当选粒（`chainbase-band3` / `chainw15-band4`）
> 用 `_tmp_chain.mjs` 各 200 局/格 × {脚本池·镜像·长程} 量：
>
> | 格 | 大雷出手/局 | **连带/局** | 判据 0.2 |
> |---|---|---|---|
> | base（W=0）三格 | 0.00 | **0.00** | ✗ |
> | W=1.5 三格 | 0.00 | **0.00** | ✗ |
>
> 代价还不止这些（`_tmp_scan.mjs` 同考卷 + 净兑现广度）：考卷 1st 现役 `v7cmin4-31` **48.1%**（净落地 `G` 2.66）→ base 臂 **35.4%**（3.29）
> → W=1.5 臂 **23.5%**（**1.24**）；CLI 自评 3P（28 对 × 20 局）43.9% → **23.8%**。⇒ 权重抬到"看得见梯度"的量级时，**它把名次适应度盖掉了**，
> 选出来的是一粒**又窄又低分**的包（一发链 = `+1.5`，而胜率项全幅只有 0~1）。
> **⇒ DS 的诊断成立**（缺失的一环确实是"选择不付钱"），但**"按事件绝对计数付钱"这一形状买不到行为**：链的**出现率**本身就 ~0
> （`tries=1855 / fired=219` ⇒ 注入密度 ~12%，即每 8 局才 1 局被教师接管一次，再乘"这局轮得到大雷、买得起、且真打出连带"）。
> 留给用户的 **Q-14（三选一）**：① 抬**密度**（`IMIT_BETA` 上限 0.12 与 `IMIT_ONLY` 过滤是当前的两个天花板）；
> ② 换**标度**为率（`链数 / 大雷出手数` ⇒ 0 出手仍是 0，但"一发带链"不再 = +1.5）；③ 结案这条线，承认"选择层买不动贵卡"。
>
> **顺带被 D77 抓了一次（好事）**：DS 的清单写"econ-env 三处 **+ CLI 读 `EPIRUS_BIGT_CHAIN_W`**"，而这**两半互斥** ——
> D77 ① 按整名扫（`tools/train-3p.mjs` 在读的清单里），字面量一出现就红。改法不是放行，而是**让 CLI 走同一个读取点**：
> 值经 `readEconEnv(process.env).bigtChainW` 拿，"本工具认识它"的资格**从单一来源反推**（逐个 econ env 名喂 0.5，看 `readEconEnv`
> 读出哪个奖励键，与 `CLI_ECON_REWARD_KEYS` 对上才算）⇒ 不新增第二份名单。三档实测与改前逐字一致：`abc` / `-1` ⇒ **exit 7**、`0.05` ⇒ exit 0 并读回 `0.05`。
> ⇒ 两处门的钉法随之各改一处，都是**钉排版被行为取代**那一族：D135 的横幅从"印 env 名=1.5"改成"印**下达 1.5 ⇒ 读回 1.5**"；
> **D101** 红过一次是因为它钉了 `+ blockBonus + widthBonus + bigBonus));`（带尾巴的求和式）⇒ 我在末尾接 `+ chainBonus` 就误伤，
> 已改成与 D100 同形的 `+ bigBonus`（只钉"这一项进了 gFit"，不钉顺序与结尾）。**这条留给 DS 看**：文本钉还会咬到"往右加一项"这种最正常的改动。
>
> 未动线上包（两槽照旧：2P `v7xfer44c13-2p` · 3P `v7cmin4-31`）· **规则指纹 `ebdbff36` 未变**（只动训练侧与门禁）。
> **门禁**：`np-test 181/181`（新增 D135）· `spec 52/52` · `smoke OK`。
> **D82 记账（本次 24h 内落盘的 `.bak` 逐名）**：`docs/artifacts/chainbase-band1.bak` · `chainbase-band2.bak` · `chainbase-band3.bak`（**当选**）·
> `chainbase-band4.bak` · `chainbase-band5.bak` · `chainbase-band6.bak` · `chainw15-band1.bak` · `chainw15-band2.bak` · `chainw15-band3.bak` ·
> `chainw15-band4.bak`（**当选**）· `chainw15-band5.bak` · `chainw15-band6.bak`；另：判活过程中我跑的三粒一代探针产物
> `probechain-band1.bak` · `probechain-band2.bak`（1 代 × 种群 4，无评估价值）**已删除**，未提交。

## v1.5.186 — 承诺局/补贴局的**相位按代旋转**：修掉"切片与座位轮换锁死"（复核 DS 交接 §3 坑#8 结案）＋ 门 D134

> **发现路径**：读 DS 的交接 `docs/HANDOFF-2026-09-23-ds.md` §3 坑#8（他自己标"未结案"）⇒ 我先假设是"教师读 `lastTarget` 与登记顺序有关"，
> 换成不读 `lastTarget` 的 `pickBigTFocus` 重跑**偏置依旧**（372/16/18）⇒ 我的假设被自己的实验否掉。真正的因是**周期锁死**：
> 受评席是 `seat = g % n`，而承诺局用 `g % 3`、补贴局用 `g % step` ⇒ **n=3（CLI 臂的默认人数！）时 `g ≡ c (mod 3)` 把 `g % 3` 钉成常数**，
> 于是"带补贴的那几局"永远评估同一个席位。
>
> **修前的实测**（`EPIRUS_IMIT_*` + `REGEN_SLICE=0.25`，`countImitInject().seats`）：
> `n=3 · 60 代` ⇒ 覆盖席 **`{0:236, 1:20, 2:1}`（92% 在 0 号席）**；`n=5 · 每代 12 局` ⇒ `{0:32,1:43,3:80,4:49}` —— **2 号席整晚一次都没拿到**。
> ⇒ 一切"给钱 / 示范"类读数都混着席位运气（DS 今天"只给钱"与"示范"两族臂都在这个分布上测的 ⇒ 结论要打折，已在交接更正里注明）。
> 这正是用户 09-22 已裁过的那族病（"要么删掉，要么调整成不至于座位偏置"）。
>
> **改法（两处 + 一个单一来源）**：新增 `sliceHit(g, step, gen)`（相位按代旋转，与 v1.5.160 `killSeatFor` 的 `(g + gen*3)` 同一招）
> 与 `isCommitGame(g, gen, hGene)` ⇒ `scoreMemberN` 的承诺局、`regenForGame` 的补贴切片、`killSeatFor` 的"承诺局不许注"三处**全部改走它**。
> 顺带结案一个同族隐患：原来"承诺局"这个概念在 `killSeatFor` 与 `scoreMemberN` **各写了一遍 `g % 3 === 0`**（本仓栽过四次的"两处各写一遍"）。
> **不传 `gen` ⇒ 相位 0 ⇒ 与旧版逐字相同**（历史臂仍可复现；新调用点必须显式传）。
>
> **⚠️ 我这次改的门自己也被这条药治了一次**：D123 里"承诺局不许注入"那格**原来也自己写了 `g % 3 === 0`** ⇒ 相位一旋转它就变成"用旧口径判新代码"的红。
> ⇒ 门同样不许复制判据。现改成调 `T.isCommitGame`。
>
> **修后实测**（同一条命令、同种子）：覆盖席 `{0:95, 1:42, 2:24}` ⇒ **三个受评席都拿到了补贴/示范**（92%/8%/0.4% → 59%/26%/15%）。
> 诚实记一条副作用：**总注入次数 257 → 161**（`tries` 基本不变、`被 only 过滤` 1698→1784）⇒  spread 之后单席密度下降，
> 是否影响"示范学得会"要 DS 那条线自己复验（本门只钉**覆盖**，不钉总量）。
>
> **门 D134**：`sliceHit` 纯函数（三代命中集互不相同 + 单代内确实只落 1 席 = 锁死的形状本身 + 跨代并集覆盖 3 席）+
> 缺省 `gen` 走旧口径 + `killSeatFor` 与 `isCommitGame` 判据一致（40 代 × 8 局全扫）+ 代码里不许再有裸 `g % 3 === 0`（注释行不算）+
> **真跑一臂判产出的覆盖席**（≥3 席、单席 ≤80%；修前 92% 正是反例）——判作用点不判横幅。
> np 179→**180/180** · spec 52/52 · smoke OK · 两槽包**未动**（权重哈希 `f256cb100f` / `cb42add78f`）。
>
> **交接更正（给 DS，三条都是实测）**：① §0"3P 槽仍是 `v7press3-91` 系"不对 —— 槽内是 `cb42add78f` = **`v7cmin4-31`**（`v7press3-91` 是 `0e01571f24`，不在槽里）；
> 他所有"产物"读数其实都跑在真槽包上 ⇒ 结论不受影响，但按名字找包会被带偏。② §2 步 7/8 的连带率我复跑为 multi **0.57**/long **0.49**
> （他 0.35/0.55）⇒ 数量级与"都过 0.2 门槛"成立，但**排序反了** ⇒ 这两个数是场地相关的，别当常数用。
> ③ "连带作废与连带事件一一对应"我量到 **84%/86%**（发射端 `if (!qIsDef && qa && qa.key !== MINI_T)` 本来就漏防御者与互雷）
> ⇒ 若收益项按"连带事件"付，约 15% 付在没作废的那半上；按事件付还是按作废付是**判据选择**，交接里没区分。

## v1.5.185 — 修正**我自己的量具 bug**（门 L2 那一族）：连带的两半收益都真实（作废 0.35/0.55 次/局）

### 1. bug 在量具里，不在游戏里

上一节我写"**连带作废 = 0 次**" ⇒ 那是**误读**：`setVoid` 发的事件是 `{ type:'voided', pid, by }`
—— **没有 `reason` 字段**（原因在 `by`），而我按 `e.reason` 筛 ⇒ **结构上必然数到 0**。
这正是门 **L2** 的原话："**筛错字段 ⇒ 把'没发生'当结论**"（我自己又犯了一次）。
另：连带的主标记是 **`{ type:'bigTChain', from, to, kind }`**（每个交互者一条），比 `damage.reason` 更全
（`.reason` 只覆盖"吃到伤害的那部分"，防御者不吃伤害但**同样被算作连带**）。

### 2. 用正确标记复量（集火教师当 0 号席 · 40 局/模式）

| 模式 | 大雷 | **连带事件 `bigTChain`** | （其中"攻击被反"类）| 连带伤害 | **连带作废 `voided(by=连带)`** |
|---|---|---|---|---|---|
| multi | 1.35/局 | **0.35/局** | 0.07 | 0.70/局 | **0.35/局** |
| long | 1.18/局 | **0.55/局** | 0.33 | 1.10/局 | **0.55/局** |

⇒ **"作废"那一半完全活着**，且与连带事件**一一对应**（每个交互者既吃 2 点、又丢一手）⇒
连带的**两半收益都真实** ⇒ 收益项该给多重现在有了依据 ✓

### 3. 交接

新增 **`docs/HANDOFF-2026-09-23-ds.md`**（自包含）：一页状态 · 本会话交付 · **大雷/连带完整结论链（8 步）** ·
唯一待做（连带收益项 + 接线清单 + 验收判据）· **八条继承的坑** · 文档地图。
⇒ 下一步**只剩实现**：连带收益项（用 `type:'bigTChain'` 计数、`from` 归因、`min(1,chains/1)` 标度）+ 门 + 一臂验收（≥0.2/局）。

## v1.5.184 — **连带是真实可达的**（拿集火教师当席位直接量）：0.35~0.55 次/局，**已超过用户的 0.2 门槛**

### 1. 量法（**零新代码**）

把刚写的集火感知教师 `pickBigTChain` **直接当 0 号席的 policy**、其余 4 席用现役冠军 ⇒ 逐事件数：
大雷出手 / 连带事件（`damage.reason` 含 `真正的落雷·连带`）/ 连带伤害 / 连带作废。

### 2. 读数（40 局/模式）

| 模式 | 大雷出手 | **连带事件**（用户门槛 0.2）| 连带伤害 | 连带作废 |
|---|---|---|---|---|
| multi | 1.35/局（每 0.7 局一发）| **0.35/局** ✓ | 0.70/局 | 0 |
| long | 1.18/局 | **0.55/局** ✓ | 1.10/局 | 0 |

### 3. 结论（这条件链的最后一块拼图）

- **连带真实存在且可达**（0.35~0.55 次/局，**已达用户的验收线**）✓ ⇒ 机制没问题、**集火触发器也确实在挑时机** ✓
- ⇒ 所以"产物 `0.000` 次/局"**不是规则/机制问题，而是"选择不给它付钱"** ⇒ **连带收益项就是那缺失的一环**
- ⇒ 顺带拿到**收益项的标度依据**：真打起来 ~0.35~0.55 次/局 ⇒ 用 `min(1, 连带/1)` 就有完整 0→1 梯度
- ⚠️ **连带作废 = 0 次**（两个模式都是）—— 值得下一轮查：`setVoid` 的连带分支是否只对**特定交互**生效
  （若"作废"这一半收益从未兑现，则连带的实际价值只有"伤害"那一半 ⇒ 会影响收益项该给多重）。

## v1.5.183 — 集火感知教师（用户洞察）：**注入翻倍、卡面依旧 0.000** ⇒ 示范活不过"选择"

### 1. 实现（用户洞察 → 可测信号）

用户："现在的冠军有**一定程度的集火能力**，既然有集火就会可以**大量连带**，这可能是一个挑时机的方法" ⇒
集火 = 大雷值钱的时刻（`resolve.js:884-909` · N6/N22：与**被劈中的目标**交互的第三方**各受 2 点 + 出手作废**）。
信号取**决策时刻的公共信息** `p[q].lastTarget / lastTarget2`（`state.js:12/187`，v1.5.19 定的口径）：
**≥2 席指向同一个人 ⇒ 判为集火目标**，劈它；**没有集火就不打**（与 `pickBigTFocus`"买得起就打"正相反 ——
乱放=白扔 5 ジ，正是用户警告的浪费）。条件按 v1.5.37 的教训取**状态相关且稀有**。
新增 `focusTarget` + `pickBigTChain`（`js/train/bots.js`，走 `global.EpirusBots` 注册表 ⇒ **CLI 零改动**，`EPIRUS_IMIT_TEACHER=pickBigTChain` 读回 `true` ✓）。

### 2. 读数（钱 0.25 + `only=bigT` + `frac=0.5` · seed 31/81 · 60 代）

- **注入统计**：seed31 `tries=1955 · fired=**257**`（全 bigT）· seed81 `tries=2057 · fired=**288**`
  ⇒ 比"买得起就打"的教师（fired=126）**翻倍** ✓（且覆盖席又一次偏向 0 号席：236/20/1 与 272/16）
- **强度**：43.5% / 58.1%（均值 50.8% vs 只给钱 48.2%，**离散极大 ⇒ 噪声内**）；⚠️ seed81 名人堂出现**重复个体** = 种群塌缩信号。
- ★ **产物卡面（决定性）**：**大雷出手 `0.000 次/局` · 连带事件 `0.00/局` · 连带伤害 0 · 连带作废 0** ✗
  （对照用户的验收门槛 **0.2/局**）

### 3. 结论（这一族"AI 留着一张好卡不用"的完整诊断）

**示范（哪怕时机对、量也足）活不过"选择"**：策略不采用它，因为在**原生经济**里这张牌不划算，
不用它的个体赢 ⇒ **示范在对选择环境说一句立刻被擦掉的话**。
⇒ 所以 **"连带收益项"不是可选项、而是必需的那一半**（原计划排第二，数据把它提到第一）：
必须让**打中连带**这件事**在 fit 里付钱**（且**归因到落雷施法者** —— 注意连带伤害的 `source` 是**被劈中的目标**，
归因需要"同一次结算内该席打出过大雷"这一跳），才可能跨过 `0.2/局`。

### 4. 产物点名（D82）

`v7c1s31-band1.bak` `v7c1s31-band2.bak` `v7c1s31-band3.bak` `v7c1s31-band4.bak` `v7c1s31-band5.bak` `v7c1s31-band6.bak`
`v7c1s81-band1.bak` `v7c1s81-band2.bak` `v7c1s81-band3.bak` `v7c1s81-band4.bak` `v7c1s81-band5.bak` `v7c1s81-band6.bak`
= **有效实验产物**（**未上线**）。

## v1.5.182 — 判据改写（用户裁定）：**追"连带"，不追"使用率"** —— 门槛只有 ~1 发 / 4~5 局

### 1. 用户裁定（原文意思）

> "大雷**乱用**本来就是很容易浪费，重要的是尽量能够**打出连导**。而且也没有必要训练的使用率很高，
> **四五局能用一次就行了**。"

⇒ 我前几轮的判据（"卡面使用率是不是从 0% 涨上来"）**是错的透镜**：
① 高使用率**不是目标**（乱放=浪费 5 ep）；② 真正的目标是**连带**（resolve.js:884-909 · N6/N22：与目标交互的第三方**各受 2 点电伤**并被
`setVoid` **作废**其出手）；③ 可接受率是**每 4~5 局一发**（≈ **0.2 次/局**）。

### 2. 按新口径量今天的冠军（30 局 × 5 席 · 逐事件）

| 模式 | 大雷出手 | **连带事件** | 连带伤害 | 连带作废 |
|---|---|---|---|---|
| multi | **0.000 次/局**（每 ∞ 局一发）| **0.00 /局** | 0.00/局 | 0 |
| long | **0.000 次/局** | **0.00 /局** | 0.00/局 | 0 |

⇒ 差距**是真的**（它一次都不打、连带从未发生），但门槛**很低**（0.2 次/局）⇒ 这是本项目少见的
"**目标明确且可达**"的缺口。判别用的事件标记：`damage.reason` 含 **`真正的落雷·连带`** ✓

### 3. 由此改写下一臂的设计（两条，缺一不可）

1. **教师必须"会挑时机"**（回应用户"乱用容易浪费"）：现有的 `pickBigTFocus` 是"买得起就打" ⇒ 正是乱用；
   需要一个**连带感知**的教师：**只在"当场有 ≥2 个会与目标产生交互的对手"时才打**（条件必须**状态相关且稀有** —— v1.5.37 的教训：
   旧条件 `ep>=2` 在 45% 的决策都成立 ⇒ 教师实际在教"见人兜里有 2 ジ就砸小雷"）。
2. **奖励要付给"连带"，不是"出手"**：新增按 `reason==='真正的落雷·连带'` 计数的收益项（并归因到**落雷的施法者**，
   注意连带伤害的 `source` 是**被劈中的目标**而不是施法者 ⇒ 归因需要"同一次结算内该席打出过大雷"这一跳）。
3. **验收判据（照用户口径写死）**：连带事件 **≥ 0.2 /局**（≈1 发/4~5 局），且**不要求**出手率更高；同时不许把
   `multi`/`long` 的强度、`G`、座位极差打坏。

## v1.5.181 — 示范注入的**开火计数 + 原因分解**：一跑就把"三种配置全 0.00%"解释清楚了

### 1. 仪表（`countImitInject()` / `resetImitStat()`，位置 = `makeEconChooser` 里**唯一**的注入出口）

记 `tries`（轮到过）· **`fired`**（真的替换受评席）· **未开火原因三分**：`noTeacherAction`（教师无动作）·
`unaffordable`（教师那张卡买不起）· `filteredByOnly`（被 `only` 拦下）；另记 `seats`（覆盖哪些席）与 `keys`（注入了哪张卡）。
CLI 退出前必报（`process.on('exit')`），且 **`tries > 0 && fired === 0` ⇒ `exit 8` 响亮失败** ——
不再让"输出是否与无示范臂逐字节相同"这种**反推法**上场（§N12：横幅读回证明不了作用点发生；`KILL_FIELD` 在 v1.5.160 补 `countKillSeats()` 后正是这样收口的）。

### 2. 一跑就结案（seed 31 · 30 代 · 钱 0.25 + 教师 `pickBigTFocus` + `only=bigT` + `frac=0.5`）

```
示范注入统计：tries=1120 · fired=126 · 未开火原因：教师无动作 0 / 买不起 0 / 被 only 过滤 994
  · 覆盖席 {"0":116,"1":10} · 注入的卡 {"bigT":126}
```

⇒ **注入真的发生了 126 次、全是 `bigT`** ✓（994 次是教师选了别的卡、被 `only=bigT` **正确拦下** ✓）
⇒ **但学出来的策略仍 `0.00%` 用它** ⇒ **卡住的不是机制、不是教师、不是钱，而是"示范发生在哪个世界"**：
注入只落在**买得起它的（补贴）局**里，而**选择**是在"混了原生局的种群"上做的 ——
原生局里大雷 `缺ジ 100%` ⇒ 策略当然还是不用它 ✓
⇒ 与边际扫描完全自洽（**大雷只在"有钱的世界"里是 +17.5/+22.5**）。
⇒ 附带观察（不是结论）：注入**偏向 0 号席**（116 : 10）—— 值得下一轮用同一个计数复核"是不是首手席更容易达到买得起"。

### 3. 下一臂（**判据已可一眼读出**）

把补贴率**拉到 1.0**（每局都是"有钱的世界"）+ 同一个教师/`only` ⇒ 若 `fired` 仍 > 0 而**卡面开始上升**，
则"钱必须在**选择环境**里持续存在"这条结论被证实；若卡面仍 0%，则说明**奖励/选择面**才是拦路虎（下一步查 fit 里那 126 次注入的收益）。

### 4. 产物点名（D82）

`v7b4s31-band1.bak` `v7b4s31-band2.bak` `v7b4s31-band3.bak` `v7b4s31-band4.bak` `v7b4s31-band5.bak` `v7b4s31-band6.bak`
= **有效实验产物**（**未上线**）。

## v1.5.180 — 真·双侧臂（钱 + **会打大雷的教师**）：卡面**依然 0.00%** ⇒ 缺的是"示范注入的开火计数"

### 1. 关键发现：项目里**早就有会打大雷的 bot**

`js/train/bots.js` 的全局注册表（`global.EpirusBots`）里已有 **`pickBigTFocus` / `pickBigTRandom` / `bigtHubTarget`**
⇒ 不必手写教师，走"全局注册表"这条先例路（复核者 v1.5.96 记过：不往 `BOT_PICKS` 加键以免改默认训练）即可：
`EPIRUS_IMIT_TEACHER=pickBigTFocus` ⇒ 读回 `true` ✓

### 2. 三种配置的对照（都开：钱 0.25 · `only=bigT` · `frac=0.5`（β(gen0)=0.12 行为式读回）· 60 代 · seed 31/81）

| 组 | 强度（当选 3P 1st）| `G_eff` | ジ占比 | **真正的落雷** | 地雷 |
|---|---|---|---|---|---|
| 只给钱（0.25）| 48.2% | 4.01 | 56.1% | **0.00%** | 0.00% |
| 钱 + 示范（教师 `heavyfire`）| 50.1% | 4.22 | 43.6% | **0.00%** | 0.00% |
| **钱 + 示范（教师 `pickBigTFocus`，真会打）** | 48.9% | 2.85 | 49.4% | **0.00%** | 0.09% |

⇒ **即使教师真的会打大雷、钱给足、窗口开着，这张卡依然 `0.00%`**（强度全部落在噪声内）✗
⇒ **结论：问题不在教师、也不在钱，而在"示范的注入是否真的落到动作上"** ——
而这一族**恰恰没有开火计数**：`KILL_FIELD` 在 v1.5.160 补 `countKillSeats()` 之前，我也只能"推断"而不能"读数"；
本轮我为"示范到底触没触发"绕了三圈（先误判空枪 → 补窗口读 β → 再看卡面仍 0%）**正是缺这个计数的症状**。

### 3. 下一步（**唯一推荐**）

给示范族补一个**开火计数**（`countImitInject()`：本代/本局"教师动作真的替换了受评席"多少次 + 覆盖几席），
再补一条门（"要了示范却 0 次注入 ⇒ 响亮失败"）。有了它，本轮那三种配置**一眼就能分辨**是
"从未注入 / 注入了但没被选择 / 注入了也被惩罚掉"，不必再靠"输出是否逐字节相同"来反推。
（对照先例：`KILL_FIELD` 的 `KILL_STAT.fired/seats` + §N12 的教训"横幅读回证明不了作用点发生"。）

### 4. 产物点名（D82）

`v7b3s31-band1.bak` `v7b3s31-band2.bak` `v7b3s31-band3.bak` `v7b3s31-band4.bak` `v7b3s31-band5.bak` `v7b3s31-band6.bak`
`v7b3s81-band1.bak` `v7b3s81-band2.bak` `v7b3s81-band3.bak` `v7b3s81-band4.bak` `v7b3s81-band5.bak` `v7b3s81-band6.bak`
= **有效实验产物**（**均未上线**）。

## v1.5.179 — 示范族接进 CLI（Q-8 最小版）+ **"空枪"陷阱** + 双侧臂：**教一张卡需要一个会打它的教师**

### 1. 示范族下达（`tools/train-3p.mjs`）—— Q-8 的最小版本

按 `KILL_FIELD`/`REGEN_SLICE` 同一范式接通 `EPIRUS_IMIT_OVERRIDE / _ONLY / _TEACHER / _SUBONLY`
（**没有 setter ⇒ `exit 7` 拒静默空转** · 下令后读回 · setter 抛错也 `exit 7`）。
凭证：`示范族已下达：EPIRUS_IMIT_ONLY=bigT ⇒ 消费点读回 "bigT"` ✓（非法卡名 `BIG_T` 被**响亮拒绝** ✓ —— 卡键是 camelCase）。

### 2. ★ "空枪"陷阱（我踩到并修掉，值得记）

只设 `_OVERRIDE/_ONLY/_TEACHER` **什么都不发生**：退火窗口 `IMIT_UNTIL` 默认 **0** ⇒ `imitBetaForGen()` 恒 0
⇒ 覆盖从不触发。我第一版双侧臂因此跑出**与"无示范"臂逐字节相同**的输出（同一串 bestFit / 同一批 trainFit /
同样的 1st）⇒ 不是"效果为零"而是**从未触发**。修法：补 `EPIRUS_IMIT_FRAC` ⇒
`imitUntil = floor(gens × frac)`（服务端口径 `train-server.mjs:171`）+ **行为式读回** `β(gen0) > 0`
（不然只读回一个数，等于横幅自证）。凭证：`示范窗口已下达：EPIRUS_IMIT_FRAC=0.5 ⇒ imitUntil=30 代 · 行为式读回 β(gen0)=0.12` ✓

### 3. 双侧臂读数（钱 0.25 + 示范 `bigT`，教师 `heavyfire`，seed 31/81，60 代）

| 组 | 强度（当选 3P 1st）| 出手 `G_eff` | ジ占比 | **真正的落雷** | 摄魂 | 电磁炮 |
|---|---|---|---|---|---|---|
| 只给钱（0.25）| 49.2 / 47.2（均 48.2%）| 4.01 | 56.1% | **0.00%** | 0.00% | 0.01% |
| **钱 + 示范**（**真触发**）| 52.5 / 47.6（均 **50.1%**）| 4.22 | **43.6%** | **0.00%** | 0.00% | 0.00% |

⇒ 示范**确实改变了行为**（ジ 56%→44%，`G_eff` 略升，强度 +1.9pt 噪声内），
⇒ **但大雷使用率仍是 `0.00%`** ⇒ **教师 `heavyfire` 压根不打大雷**（`only=bigT` 过滤下**无牌可教**）。
⇒ **结论**：`教一张卡 ≠ 打开示范开关`，**得有一个真的会打它的教师**（项目里已有先例：`makeAntiRingTeacher`
就是为"反环"专门写的教师；`setImitPlan` 支持按段换教师）。下一臂应做 **`bigT` 专用教师**（合法且买得起时打大雷），
再做"钱 + 真教师"的双侧臂 —— 那也是"大雷 +17.5/+22.5 最大正边际"能不能被兑现的唯一路径。

### 4. 产物点名（D82）

`v7b1s31-band1.bak` … `v7b1s31-band6.bak` · `v7b1s81-band1.bak` … `v7b1s81-band6.bak` ·
`v7b2s31-band1.bak` … `v7b2s31-band6.bak` · `v7b2s81-band1.bak` … `v7b2s81-band6.bak`
（逐名展开：`v7b1s31-band1.bak` `v7b1s31-band2.bak` `v7b1s31-band3.bak` `v7b1s31-band4.bak` `v7b1s31-band5.bak` `v7b1s31-band6.bak`
`v7b1s81-band1.bak` `v7b1s81-band2.bak` `v7b1s81-band3.bak` `v7b1s81-band4.bak` `v7b1s81-band5.bak` `v7b1s81-band6.bak`
`v7b2s31-band1.bak` `v7b2s31-band2.bak` `v7b2s31-band3.bak` `v7b2s31-band4.bak` `v7b2s31-band5.bak` `v7b2s31-band6.bak`
`v7b2s81-band1.bak` `v7b2s81-band2.bak` `v7b2s81-band3.bak` `v7b2s81-band4.bak` `v7b2s81-band5.bak` `v7b2s81-band6.bak`）
= **有效实验产物**（`v7b1s*` 为**空枪反例**，`v7b2s*` 为真触发档；**均未上线**）。

## v1.5.178 — 补贴率旋钮 `EPIRUS_REGEN_SLICE`（门 D132）+ "只给钱"实验：**钱能买到强度，但买不出贵卡**

### 1. 补贴率从硬编码常量变成旋钮（`v1.5.177` 的代码，本节一并记账）

- **动因（数据）**：全卡边际扫描证明"**钱是系统性的墙**" —— long 里只有 **59%/18%/3%** 的回合买得起
  **1/2/3 费**，multi 更紧（30 张卡 **0 张**可测）；而要跑"给钱 + 教卡"的双侧实验，**必须先能调"给多少钱"**，
  而它此前是硬编码常量 `REGEN_SLICE = 0.08`（`evo.js:1986`）。
- **实现**：`let REGEN_SLICE` + `setRegenSlice`/`regenSlice` + 导出 + CLI 接线（`tools/train-3p.mjs`）
  —— 纪律与 `KILL_FIELD` 完全一致：**没有 setter ⇒ `exit 7` 拒静默空转** · 下令后**读回消费点** ·
  **超界也算被拒**。**默认 0.08 ⇒ 不下达时逐位等于旧行为**。
- ⚠️ **修掉我自己的 NaN 陷阱**：第一版守卫写成 `Math.abs(got − NaN) > 1e-9` ⇒ **恒 false** ⇒
  `EPIRUS_REGEN_SLICE=abc` **静默 exit 0 跑完**（正是本项目最恨的那一族）；修后复验 **`abc ⇒ exit 7`**、
  **`5.0（超界）⇒ exit 7`**（⚠️ 退出码必须**不接管道**读，否则 `Select-Object -First` 会给假值）。
  新增门 **D132** 把这条链（含 NaN 安全）全钉住。

### 2. "只给钱"实验（判据跑前写死）

冷启动 · 60 代 · 3 人 · 8 局/代 · 种群 8 · seed 31/81 · **单变量 = 补贴率 0.08（出厂）vs 0.25（3 倍钱）**。
接线凭据：每臂都打印 `补贴率已下达：regenSlice=… ⇒ 消费点读回 …` ✓

| 组 | 强度（当选冠军 3P 1st）| 出手 `G_eff` | ジ占比 | **摄魂** | **大雷** | **电磁炮** | 地雷 |
|---|---|---|---|---|---|---|---|
| 补贴 0.08（出厂）| 50.3 / 50.4（均 **50.4%**）| 2.63 | 46.0% | **0.00%** | **0.00%** | 0.00% | 0.00% |
| 补贴 0.25（3 倍钱）| 49.2 / 47.2（均 **48.2%**）| **4.01** | 56.1% | **0.00%** | **0.00%** | 0.01% | 0.00% |

（广度为**种群级**：各 12 个包 × 5 席自对局 × 8 局 · `G_eff(出手)=exp(熵)`）

### 3. 结论（回答"垫钱就能加强度不是说明环没学会嘛"）

- **给钱确实改变了行为**（出手 `G_eff` **2.63 → 4.01**，跨过 `G≥4` 那档；ジ占比 46% → 56%）
- **但贵卡使用率仍是 0%**（摄魂/大雷/地雷 全 `0.00%`）⇒ **"缺钱"不是"不用贵卡"的充分解释**
- 强度还略降（**−2.2pt**，2 seed 噪声内）⇒ 白拿的 ep 被花在"**更多手**"而不是"**更值的牌**"上
⇒ **正确表述**：`垫钱能买到强度` = **它本来就在用的那几张便宜牌打得更勤**，**不是**长出了贵卡。
⇒ 结合全卡边际扫描（**大雷 +17.5/+22.5 = 最大正边际**）⇒ **"钱"与"教"必须同时做**；
而**"教"那一侧在 CLI 上还没接**（复核者 Q-8）⇒ 下一步按 `KILL_FIELD`/`REGEN_SLICE` 的同一范式把示范族
（`setImitOnly`/`setImitSubOnly`/`setSubBead`…）接进 CLI，再做双侧臂。

### 4. 产物点名（D82）

`v7m0s31-band1.bak` `v7m0s31-band2.bak` `v7m0s31-band3.bak` `v7m0s31-band4.bak` `v7m0s31-band5.bak` `v7m0s31-band6.bak`
`v7m0s81-band1.bak` `v7m0s81-band2.bak` `v7m0s81-band3.bak` `v7m0s81-band4.bak` `v7m0s81-band5.bak` `v7m0s81-band6.bak`
`v7m1s31-band1.bak` `v7m1s31-band2.bak` `v7m1s31-band3.bak` `v7m1s31-band4.bak` `v7m1s31-band5.bak` `v7m1s31-band6.bak`
`v7m1s81-band1.bak` `v7m1s81-band2.bak` `v7m1s81-band3.bak` `v7m1s81-band4.bak` `v7m1s81-band5.bak` `v7m1s81-band6.bak`
= **有效实验产物**（**均未上线**）。

## v1.5.176 — **全卡边际价值扫描**（30 张 × 两口径）：把"该练的卡"与"该改价的卡"分开

> 工具：`node tools/probe-skill-marginal.mjs --mode=long --games=40 --eps=0.25 [--rich=card]`（**只读** · 每口径 **6 秒**）
> 基线 1st=12.5% · 噪声地板 5.0pt。

### 1. 原生世界（今天的钱袋）：**唯一可测的只有 ジ**；23 张"噪声内" + **6 张"机会≈0"**

- **机会≈0（结构性不可测 ⇒ 先解决前提）**：**电磁炮（缺珠 100%）** · **激光眼（缺珠 100%）** ·
  **真正的落雷（缺ジ 100%）** · 避雷针（99%） · 双枪射手（90%） · 镜面反射（90%）。
- **"缺ジ"是多数卡的墙**（合法率随费用单调塌）：**cost 1 → 59%** · **cost 2 → 18%** · **cost ≥3 → 3%**
  ⇒ 也就是说 long 里**只有 59% 的回合有 ≥1 ep、18% 有 ≥2、3% 有 ≥3**。
- ⇒ 这就是"**光垫钱 +41~46pt**"的卡片级解释：冠军**买得起的只有 0~1 费那一档** ⇒ 73% ジ、贵卡全灭。

### 2. 定向垫钱（"不缺点钱的世界"）：**7 张可测** ⇒ 该练的 / 该改价的分开

| 档 | 卡（Δ1st ± 配对 SE）|
|---|---|
| **正边际（值得让它多用）** | **摄魂指法 +22.5 ± 6.7** · **真正的落雷 +17.5 ± 8.7** · ジ +17.5 ·（雷击之枪 +10 / 转移伤害 +7.5 / 地雷 +7.5，噪声内）|
| **负边际（有钱也亏 ⇒ 是价/效果问题）** | **过载炮 −35.0 ± 9.8** · **聚能环 −25.0 ± 9.3** · 贴贴 −20.0 ± 8.2 · 全息屏障 −17.5 ± 8.7 ·（天火 −15 / 蓄能 −17.5 / 净化 −12.5 / 原型制御 −12.5，噪声内）|

⇒ **① Q-13b 方向成立**：**摄魂是正边际最大的卡之一（+22.5pt）**，比复核者镜像强制口径的 +4.8pt 更大
⇒ 值得练它；⚠️ 但原生世界里它"**缺ジ 97%**" ⇒ 练它的同时必须给钱。
⇒ **② 环落在"负边际"档（−25.0 ± 9.3）** ⇒ **"教环"是错方向**；对 Q-2 对症的是**改价 / 放大收益**。
⇒ **③ 过载炮是全表最差（−35pt）**，它是 `cost: null`（按使用次数循环）⇒ 值得单独审它的价。

### 3. 一条被"使用率读法"坑掉的信号（新 skill report 存在的理由）

原生世界里 23/30 张"噪声内" + 6 张"机会≈0" ⇒ 拿**使用率**读表会把"**买不起**"读成"**没价值**"。
新口径把"**机会/局**"与"**缺什么**"并排 ⇒ 才能分辨"结构性不可测 / 样本不足 / 真的负边际"。

### 4. ⚠️ 一个待大 n 复核的候选坑（**不是结论**）

**枪**：原生 Δ **−2.5 ± 6.7（噪声内）**，但**合法率 100%**、且它是冠军 **native 用量第二高（14.8%）**；
定向垫钱档 **−20.0 ± 10.9（噪声内、量级大）** ⇒ 值得**大 n 复核它是不是"用得多但亏"的坑**。

### 5. 状态

无产物（该工具**只读**：不写文件、不换包）· `np-test 178/178` · 指纹 `ebdbff36` 未变 · 线上包未动。

## v1.5.175 — 分支合并 + 试训练 + **"收割压广度"在干净单变量下被否证** + 聚能环的边际价值

### 1. 合并与推送

`qoder-explore-0922night` 的 3 个提交已推（`95390c1..acfe3f7`），并**快进合并进 main**（`fd9cba3..acfe3f7`）。
合并后复验：`np-test 178/178` · `spec 52/52` · 指纹 `ebdbff36`。

### 2. 试训练（30 代 · 3 人 · 8 局/代 · 种群 8 · 热启动自现役冠军）

`EPIRUS_HOTSTART=1 EPIRUS_SEED=31 EPIRUS_ARM=v7ds1 node tools/train-3p.mjs 30 3 8 8`
⇒ 横幅 `[train-3p] 热启动：以现有冠军为种子` ✓（§N8 的 banner 义务）· 当选冠军 3P **1st=49.2% top2=88.1%** ·
名人堂零攻击局 **0%**（退化包闸在 ✓）· **耗时 13.8s** · 产物写 `docs/artifacts/train-3p-out.js`（**未碰线上包** ✓）。

### 3. ★ Q-6「收割压广度」：**干净单变量对里不成立**（否证）

配方：冷启动 · 60 代 · 3 人 · 8 局/代 · 种群 8 · seed 31/81/82/91 · 单变量 = `EPIRUS_KILL_FIELD` 0 vs 0.125。
**接线凭据**：每个 kill-on 臂都打印 `收割席注入已下达：killField=0.125 ⇒ 消费点读回 0.125（每 8 局注 1 席
pickKillSecure · 相位按代旋转 · 只注多人局 · 避开承诺局）` ✓

- **强度**：当选冠军 1st — kill 关 50.3/50.4/49.3/53.1（均 **50.8%**）· kill 开 44.3/46.8/56.5/48.6（均 **49.1%**）
  ⇒ **−1.7pt，噪声内**。
- **广度**（独立量的**种群级**：8 臂 × 6 band = 各 24 个包，5 席自对局 × 8 局，`G_eff(出手)=exp(熵)`）：

| 组 | 出手 G_eff 均值 | 中位 | ジ占比 | 退化(≤1 种) | G<3 | G≥4 |
|---|---|---|---|---|---|---|
| kill=0（关）| 2.74 | 1.60 | 47.6% | 3 | 15 | 6 |
| **kill=0.125（开）** | **3.25** | **2.58** | 50.5% | **1** | 16 | **7** |

⇒ **开启收割注入后反而略宽**（均值 +0.51 · 中位 +0.98 · 退化包更少）⇒ **"KILL_FIELD 压广度"在干净单变量下否证**；
此前 `xn15c/d` 的窄化更可能来自**与启动方式/配方同变**（复核者自己就把那条标成"尚未结案"）。
⇒ 结论：**保留 `KILL_FIELD`**（它没压广度、强度侧也是噪声内）；真正压广度的是**奖励型**收割
（`v7xn10b` 的纯ジ退化：存活型 rank 主梯度下"零出手混到收缩哨声"是捷径）。
⚠️ 一个反例级观察：kill-on 的 seed 82 名人堂里出现过 **零攻击局 92%** 的退化包（**未当选** ⇒ 闸挡住了）。
⚠️ 边界：我的臂是 60 代/pop8/冷启动，与 `xn15c/d` 配方不同 ⇒ 这是**否证它的普遍性**，不是否定他那两条读数。

### 4. ★ Q-2「垫钱⇒聚能环没学会」：**钱是瓶颈 ✓，但"教环"救不了（环在 long 下学会了也亏）**

今天的冠军在 long 的卡面（20 局）：ジ 73.1% · 枪 14.8% · 狙击 7.4% · **电磁炮 2.0%** · **聚能环 0.2%（5 次/20 局）** ·
大雷/地雷/净化/摄魂 **全 0**。用 `tools/probe-skill-marginal.mjs --mode=long --only=ring`：

| 口径 | 机会/局 | Δ1st | 光垫钱 |
|---|---|---|---|
| 原生世界 | **1.23**（合法率 3% ⇒ "97% 的回合买不起"）| −1.7 ± 1.7（噪声内）| +0.0pt |
| 定向垫钱 `--rich=card` | **21.25** | **−23.3 ± 7.3（可测 · 负边际）** | **+45.0pt** |

⇒ **不是"没学会"，是"学会了也亏"**：把环垫到刚够之后，多打环 **−23.3pt**（3 ep 只换回 ≈+0.46 伤害/次，
机会成本远大于收益）⇒ **补钱的方式不能是环**；要动的是**贵卡的费用/收益**或**非环的 ep 收入通道**（规则级）。
（详见 `docs/RESEARCH-2026-09-23-ds-ring-and-harvest.md`）

### 5. 产物点名（D82）

`v7ds1-band1.bak` `v7ds1-band2.bak` `v7ds1-band3.bak` `v7ds1-band4.bak` `v7ds1-band5.bak` `v7ds1-band6.bak`
`v7k0s31-band1.bak` `v7k0s31-band2.bak` `v7k0s31-band3.bak` `v7k0s31-band4.bak` `v7k0s31-band5.bak` `v7k0s31-band6.bak`
`v7k0s81-band1.bak` `v7k0s81-band2.bak` `v7k0s81-band3.bak` `v7k0s81-band4.bak` `v7k0s81-band5.bak` `v7k0s81-band6.bak`
`v7k0s82-band1.bak` `v7k0s82-band2.bak` `v7k0s82-band3.bak` `v7k0s82-band4.bak` `v7k0s82-band5.bak` `v7k0s82-band6.bak`
`v7k0s91-band1.bak` `v7k0s91-band2.bak` `v7k0s91-band3.bak` `v7k0s91-band4.bak` `v7k0s91-band5.bak` `v7k0s91-band6.bak`
`v7k1s31-band1.bak` `v7k1s31-band2.bak` `v7k1s31-band3.bak` `v7k1s31-band4.bak` `v7k1s31-band5.bak` `v7k1s31-band6.bak`
`v7k1s81-band1.bak` `v7k1s81-band2.bak` `v7k1s81-band3.bak` `v7k1s81-band4.bak` `v7k1s81-band5.bak` `v7k1s81-band6.bak`
`v7k1s82-band1.bak` `v7k1s82-band2.bak` `v7k1s82-band3.bak` `v7k1s82-band4.bak` `v7k1s82-band5.bak` `v7k1s82-band6.bak`
`v7k1s91-band1.bak` `v7k1s91-band2.bak` `v7k1s91-band3.bak` `v7k1s91-band4.bak` `v7k1s91-band5.bak` `v7k1s91-band6.bak`
= **有效实验产物**（**均未上线**）。

## v1.5.174 — 长程摄魂窗口 `drainHpMax` **3 → 2**（用户裁定）＝**指纹换代 `0f931cb7` → `ebdbff36`** ＋ 结案一份挂了 12 版的"裁定与实测互斥"

> **这条改动的性质先说清楚**：它**不是修 AI**——现役冠军在长程里**根本不打这张卡**（下面有数）。它是关掉一份档案矛盾 + 收回人类玩家一扇过宽的窗。
>
> **档案矛盾（不是我推断，是文件自己打脸）**：v1.4.0 在**同一次提交**里同时写下
> ① `rules.js` 的 `long.drainHpMax: 3`（后来还被门 D2 钉成断言），和
> ② `RULES-NP.md` N24 的实测表 `5血 ≤1 → 34.4%(+1.8pt) / ≤2 → 32.3%(−0.3pt) / ≤3 → 29.5%(−3.1pt)` ＋ 结论"**门槛对平衡是负向的**"。
> 两段话谁也没关掉谁，`git log -S drainHpMax` 与 CHANGELOG 里**查不到任何撤销记录** ⇒ 用户记的"DS 说没必要、所以就没改"从未落到配置上。
>
> **用今天的引擎重跑（`RESEARCH-LOG-2026-09-23` §N42，全部实测）**：
> ① 官方考卷 `eval-5p`（35 组合 × 1400 局 = **49000 局/档**）在 ≤1/≤2/≤3 三档**读数逐位相同**（冠军 59.3% / 对照 7.7% 一字不差）；
> ② 逐事件数：{脚本池, 镜像} × {≤1,≤2,≤3} 共 2400 局里**现役冠军摄魂出手 = 0 次**（镜像场每局有 **14.5 次**"合法且买得起"的机会，一次都不接）
> ⇒ **DS 当年的"降强度"说的是会打这张卡的旧 AI（他表里注入率 2.4%）；今天这条规则只作用于人类玩家，对 AI 惰性**。
> ③ 唯一有信号的是"强制使用"口径（假想会用这张卡的 AI，同种子配对 n=400）：
>    镜像 `≤2−≤1 = +4.8±1.3pt`（超 2SE）而 `≤3−≤2 = +1.8±1.1`（噪声内）；脚本池 `≤2−≤1 = −1.5±1.2`（噪声内）而 **`≤3−≤1 = −3.5±1.7`（超 2SE）**
>    ⇒ **两个场都指向 ≤2 是拐点**（上行几乎全在 HP2 这一档拿到，HP3 那一步反而翻负）⇒ 用户裁定取 **≤2**。
> ④ ⚠️ 同时记下一个我自抓的**假效应**：在 `temp=0.15` 的随机抽样场里三档"自然态"1st 差到 −3.8pt 且"超 2SE"——
>    但主体出手 = 0 ⇒ 只能是 `legal` 集合变长扰动 softmax 分母的抽样噪声，**不许当强度证据**（官方考卷逐位相同正好反证）。
>    这条写进门与文档，是因为它长得太像一个真结论。
>
> **变化范围（新旧引擎同种子配对 400 局，不是估计）**：镜像 **0/400 局**胜负不同；脚本池 23/400 局不同、Δ=+1.3±1.2pt（噪声内，两态出手都是 0）
> ⇒ **AI 行为零变化**；变的只有人类玩家的可用窗口（长程 HP≤3 → HP≤2，即用户那条 `results/摄魂.txt` 里第 8 回合 HP2 的摄魂从此不合法）。
> 两槽包**权重逐字节未动**（`f256cb100f` / `cb42add78f` 复验相同），只重记 `meta.rulesFingerprint/fingerprintRefresh`。
>
> **顺手修的一条语义**：`state.js` 原来是 `const dmax = (state.mode && state.mode.drainHpMax) || 1` ⇒ **写 0 会静默变成 1**，
> "把这张卡在某模式下关掉"这个选项根本不存在（我做"永不可用"对照组时被它骗过一次——`drainHpMax:0` 跑出来与 `≤1` 逐位相同才识破）。
> 现在显式 `!= null` 判定 ⇒ 0 是真 0。门 D2 钉住这三档语义（0 关 / 1 原口径 / 2 现长程）。
>
> **门**：D2 重写（`drainHpMax=2` + HP2 可用 / HP3 与 HP4 必须拒 + `0` 真关掉 + `1` 走 R25 原口径）；
> D131 的逐档扫描表改成 `long:2`，并新增"**真配置**的卡面提示必须写 ≤2、且不许残留上一代的 ≤3"。np **178/178** · spec 52/52 · smoke OK。
> 文档：`RULES-NP.md` N24 的"裁定=3"就地标注被本版取代，并补一段"裁定与实测互斥的结案"（含上面四条数）。
>
> **仍待裁（不动）**：要不要顺手把这张卡**训练进 AI 的行为里**（强制口径显示镜像 +4.8pt 是可拿的上行）——那是训练侧目标函数的事，与本版规则无关。

## v1.5.173 — 用户实测"摄魂 bug 没解决"追到的真凶：**卡面文案与引擎门槛不同源**（引擎是对的，那句话说错了）

> **先说这条记录里到底发生了什么**（`results/摄魂.txt`，长程 5 血 · 冠军难度 · 26 回合 · 玩家1 淘汰）：把玩家1 的血量逐回合算出来是
> `5 →(R2 -1) 4 →(R5 -1-1-1) 1 →(R7 摄魂 +1) 2 →(R8 摄魂 +1=3，随后两发狙击 -2) 1 →(R12 摄魂 +1) 2 →(R14 激光剑 -2) 0 死亡`。
> 三次摄魂分别在 **HP 1 / 2 / 1** ⇒ **全部落在长程窗口 `drainHpMax=3` 之内，引擎没有放水、也没有闩锁**。
> 直接按模式扫一遍（`computeCost` + `legalActions` 双口径）：
> `long`：HP5 拒/灰 · HP4 拒/灰 · HP3~1 允许/亮；`multi`、`standard`：只有 HP1 允许/亮 ⇒ **HP 回到 4/5 时它是打不出来的**（用户描述的"回上去了仍可用"在引擎侧不成立）。
>
> **那用户看到的是什么？** 是**一句自相矛盾的话**。`SK.DRAIN` 的 `desc` 写死"仅限 HP≤1"（R25 原口径），
> 而长程的窗口按 `state.mode.drainHpMax=3` 放行（`RULES-NP.md` N24 当年裁定："5 血下那扇窗太窄"）
> ⇒ 在 **HP 2** 上格子按规则**应该亮**（也确实亮），可鼠标移上去写着"仅限 HP≤1" ⇒ 读出来的就是"血回上去了摄魂还能用 = bug 没修"。
> 我上一轮（v1.5.166）只验了引擎合法性就写"没能复现"，**没有去查游戏对玩家说了什么** —— 报告口径错在这里：玩家的观察是真的，解释被一句过期文案带偏了。
>
> **改法**：`js/ui/ui.js` 组卡面提示时，把那句话里的数字换成**本局真值**（`st.mode.drainHpMax`，缺省 1）；
> 长程显示"仅限 HP≤3（本模式放宽，见 RULES-NP N24）"，2 人/多人仍显示"仅限 HP≤1"。
> **不动 `rules.js`**（那是指纹五件套之一，为一个显示字符串换代不值），也**不动任何判据**：`computeCost` 的门槛逻辑逐字未变。
> 顺手轮换 `index.html` 的 `?v=` 缓存戳（否则老浏览器继续拿旧 `ui.js`，用户会以为又没修）。
>
> **门 D131**：三模式各自扫 HP 全档 ⇒ `long` 只在 ≤3 允许、`multi`/`standard` 只在 1 允许（**判引擎行为**）；
> 且 UI 提示必须**跟着模式变数字**（判文案）：`long` 的提示里必须出现 `≤3`、`standard` 必须出现 `≤1`，两者不许相同。
>
> **留给用户的一条裁定（Q-13）**：窗口本身要不要改回 `HP≤1`？现在长程是 ≤3，来自 N24 的当年裁定（用户自己提的"调到 2 或 3"）。
> 如果实际手感是"太宽"，那是**规则改动**（会连带影响训练口径与两槽读数），要用户点；我这次只把"说的"改成"做的"。

## v1.5.172 — 把**判它的对手放上训练桌**（`EPIRUS_COUNTER_OPPS`）+ 矩阵加「破防栏」+ 边际价值表给出"测不准的价码" · 门 D130

> **起因是一条很有价值的失败样本（`v7xn22a`）**：把预算拉到 `400 代 × n5 × 每代 12 局 × pop16` + 热启动 + 关收割席，
> 今晚终于长出了**兑现口径**上的真多样性：各格净 `G(落地) 3.51~3.93（4 种真卡打上血）`，而现役包只有 `2.19~2.73`；珠浪费 0%（现役 42%）、座位极差 7pt（现役 13pt）。
> 然后 `promote --dry` 把它砍在**行为门**上：`G4 无一行脚本能以 >60% 击败它` —— 最克它的两行是「只防御(不还手)」85% 与「只枪 1ジ压制」82%，另有 `G(long) 2.03 < 3`、`场B 清场 0.00`。
> ⇒ **"广"不是终点，"广而且不被一行脚本吃"才是**；这条臂因此把问题从"要不要预算"换成了"预算换来的广度死在哪一关"。
>
> **查下去是一处结构性缺席**：`tools/gate-drafts.mjs:223` 那张 `COUNTERS`（G4/G5 用的原型）里，「只防御(不还手)」= `guard → 否则 ジ`；
> 而 `train-3p` 的 fitness 对手表 `OPPS` 九个里最接近的 `defend` 是**会还手**的防御 —— 纯不还手的 `EpirusBots.pickGuardSpam` **早就存在，只是从来没被当过训练对手**。
> ⇒ **验收用的对手分布与训练用的对手分布不是同一张桌子**，适应力学不出来不是权重没调对，是它没机会被遇见。
> 修法沿用 v1.5.150 的 `EPIRUS_XN2REF=exam` 那条思想：**对着产品判据本身训 ⇒ 目标与验收一致**。
>
> **改法（三件，全部默认关 ⇒ 既有路径逐字不变）**：
> ① `EPIRUS_COUNTER_OPPS=1` ⇒ 把 `cnt:guardSpam / cnt:gunSpam / cnt:snipeSpam` 推进 `OPPS`（9→12）；
>    任一原型在 `EpirusBots` 里缺席 ⇒ **exit 4**（少一个就是一根空枪，不许静默少放）；生效名单写进 `meta.recipe.counterOpps`。
> ② `tools/probe-cross-mode.mjs` 加**破防栏**（其余 4 席全 `pickGuardSpam`）——见下面那条独立发现。
> ③ `tools/probe-skill-marginal.mjs` 的判定列改成**带价码**：`当前只能分辨 ≥2.8·SE` 与 `要 SE→3pt 需 ~N 局`（`--target-se=`）。
>    动因（§N36）：long 模式 30 张卡判完是 `可测 9 · 噪声内 21 · 机会≈0 **0**` —— §N22 那套口径已经把**结构性不可测清空**，
>    剩下 21 张不是"测不到"而是"测不准"（60 局配对 SE ≈ 5.9~7.7pt ⇒ 只能分辨 ≥14~22pt；要 ±3pt 得 ~350~500 局/卡）。
>    ⇒ 用户那条"让更多技能可测量"现在有了可行版本：**障碍已不在口径，在统计功效**；再往下要么接受"只对 |Δ|>15pt 的卡下结论"，要么给它几百局的算力（两者都不该我自拍，见夜日志 Q-9/Q-10）。
>
> **门 D130**：`pickGuardSpam` 必须**只防不还手**（构造态里手上真有可出的攻击卡也不许出 —— 把它改成会还手 = 把量具改没了）+ 三个原型都在 `EpirusBots` +
> 接线三钉（默认 0 / 真推 `OPPS` / 进 `SELF_ENV_KEYS`）+ 缺对手 exit 4 + 真跑一臂验横幅与 `meta.recipe.counterOpps`。np 176→**177**。
>
> **§N37 的独立发现（顺手但重要，且它关系到一条既有门的判别力）**：新加的破防栏（30 局/格）读出来是三种完全不同的形状 ——
> `v7xn14a-band2` **五格全胜 100%**、`v7xn22a` 四格 100%（4P 平）、**现役 `v7cmin4-31` 在 2P/3P/5P/长程全是 `0% 胜 / 100% 平`**、`v7xn15c-band5` 全 0%（它真的输）。
> 而 `G5` 报给现役的是 **PASS（防席夺冠 0%）** ⇒ **"防席赢不了"并不等于"我能赢"**：一支会把整场拖成互撤的包在 G5 里是绿的。
> 这不是要改 G5 的判据（那是裁定权），而是说**矩阵这一栏补上了 G5 看不见的第三种结局**；建议把它像 v1.5.152 的产品代理栏那样**先只记录**（Q-11）。
>
> **臂 `v7xn26a` 读数（`xn22a` 逐字同配方 + 唯一变量 = 判据原型上桌）——方向对，但够不到线，而且代价实测**：
>
> | | `xn22a`（桌上没有） | `xn26a`（桌上有了） | 线 |
> |---|---|---|---|
> | G5 防席夺冠 long / multi | 85% / 72% | **42% / 32%** | ≤25% ⇒ **仍 FAIL**，但方向正是这条开关该打的 |
> | G4 最克它的脚本 | 只防御 85%（long） | **换成** 只枪 90%（long）· multi 转 **PASS 60%** | >60% ⇒ long 仍 FAIL |
> | 同尺考卷 | 40.1% | 36.8% | −3.3pt |
> | 净 `G(落地)` / 兑现率 | 2.77 / 22% | 1.98 / **49%** | 广度换成了"打得着" |
>
> ⇒ **结论比"有用/没用"更有信息**：把某个反例放上桌子，确实把**那一个**弱点压下去（G5 从 85%→42%），
> 但适应性压力会**把可 exploited 的那条线挪到别处**（long 的克制者从"只防御"变成"只枪"，还更狠了），并且要付考卷分与兑现广度。
> ⇒ 这一条直接指向下一问（**没做，等裁**）：要的不是"多一个对手"，而是**把整张 6 行克制表当向量进适应度**（每一行都不得 >60%），
> 那是判据级改动（会重定义"好包"），且按今晚的价码看，代价是几个 pt 的考卷分。记成 Q-12。
>
> 产物点名（D82）：`v7xn26a-31.bak`、`v7xn26a-band1.bak`、`v7xn26a-band2.bak`、`v7xn26a-band3.bak`、`v7xn26a-band4.bak`、`v7xn26a-band5.bak`、`v7xn26a-band6.bak`
> 明细：`docs/RESEARCH-LOG-2026-09-23-qoder-night.md` §N34/§N35/§N36/§N37。

## v1.5.171 — 测量侧读得到 2P 外壳产物了（`audit-lib.loadChamp` 的退回分支）——`champ-audit` 对一整类候选一直打"(读不出冠军包)"

> **怎么发现的**：§N31 拿历史池与今晚臂冠军横量"考卷 × 净兑现广度"时，`v7xn14a-band2`（§N14/§N15 那条线上最被看好的候选）
> 在 `tools/champ-audit.mjs` 里整行是 `(读不出冠军包)` —— 而**同一粒文件** `tools/probe-cross-mode.mjs` 吃得下（它走 `js/champion-pack.js` 的 `extract`，D110 的单一来源）。
> 两读因不同源 = 本仓第四次同一族病（`FEAS_N`/`ECON_ENV_KEYS`/`pierceKeys`/`HOLO_GIFT_MAX` 同型）。
>
> **真因**：`audit-lib.loadChamp` 的正则只认 `EPIRUS_CHAMPION_3P` ⇒ 凡 **2P 外壳**（`window.EPIRUS_CHAMPION = {…}`，即 `train-best` 的 band-save 与 2P 槽 bundle）一律 `return null`。
> 表现是"读不出"而不是"读错" ⇒ 它不会骗人，但**会让人以为这类候选没法量**，于是跨槽比较长期缺席。
>
> **改法**：只在 3P 外壳缺席时退回 2P 外壳；负向断言 `(?!_3P|_META)` 必须有 —— 产物里 `EPIRUS_CHAMPION_META` **先出现**，贪婪匹配会把账当包。
> 主路径（3P）**一字未动** ⇒ 既有体检读数逐位不变（D82/D16 同批复跑核对）。退回后仍有些列是 `?`（`A 考卷`/`D 长程反弹墙` 那几列走 `tools/eval-5p.mjs`，它按 5 人槽读包），
> 这是**已知边界**、不是新病：至少 B/C/G/座位/墙伤害这几列现在能看了。
>
> 明细：`docs/RESEARCH-LOG-2026-09-23-qoder-night.md` §N31。

## v1.5.170 — 广度**准入线** `EPIRUS_BREADTH_FLOOR`（默认关）：把"塌成一种卡"当**不合格**，不当排序键 · 门 D128
>
> **动因是 §N28 的一条实测，不是审美**：两对种子（long/multi）四粒冠军里**三粒**的净兑现只剩一种卡（`G(落地) 1.00~1.23`）
> ⇒ 塌缩不是意外而是常态。而 §N25 已经把"广度当排序键"这条路否掉了两次（同分带里只剩 1 粒 ⇒ 咬不动；咬动了换上来的还是过不了硬门槛的病包）。
> 剩下的唯一姿势是**线**：不达标 = 不合格；达标者之间**仍按胜负分**排（不让广度换胜率）。
>
> **口径**：`rejectNarrowWinners(entries, floor)`（`tools/pick-best.mjs`，纯函数）判 `mirrorHealth` 的**净** `effSkillsLand ≥ floor` **且** `landedKeys ≥ 2`。
> 两条而不是两条之一：`landedKeys ≥ 2` 就是"塌缩"的定义（只有一种卡打上血），**G 再高也不许绕过它**（门里钉了 `G=9.00/1 种 ⇒ 仍不合格`）；
> `≥ floor` 防"两种但一种占 99%"。标定（`mirrorHealth(20,5,'multi')` 实测）：现役 `2.66（3 种）` · 2P 槽 `2.98` · 今晚三粒塌缩冠军 `1.00~1.24` · 最宽那粒 `1.75（3 种）` ⇒ 草案线 **1.5**。
> **⚠️ 这条线目前是训练侧读数，不是门禁判据**——要不要进 `promote`、线画在哪，属判据改动，**要用户裁定**（夜日志 Q-7）。
>
> **行为与"不许静默"**：全池不合格 ⇒ `meta.breadthFloorAllNarrow=true` + stderr 响亮指向"走向②：该回去改奖励面"，**不退回"不过滤"**；
> 当选者的 `landG/landedKeys` 写进 `meta.recipe.breadthFloor` ⇒ D128 拿**产物**判这条线生没生效（判效果不判横幅，§N11 那条纪律）。
> 默认 `0` ⇒ 整段不跑；D127 的默认关基线哈希 `aa743488cc` 本版仍然绿（同一次复跑顺带证明"共用一次 `mirrorHealth`"的重构没动行为）。
>
> **第一臂读数（`v7xn23a` 冷启动 · 关掉收割席 · seed 31）**：池子里**第一次同时有宽候选和窄冠军** ——
> `band4` 净落地 `2.76（3 种）` 而考卷只有 `27.9%`；当选的 `band3` 净落地 `1.00（1 种）` 考卷 `40.6%`
> ⇒ **在这个配方尺度上，"要宽"的明码标价 ≈ 12.7pt 考卷分**（`xn23b` = 同臂开线 1.5 的对照在跑，读"付了这 12.7pt 之后通吃矩阵长什么样"）。
> 另：`v7xn19a`（冷 + 收割席开 + 开线）报的是 `剔除 6/6 ⇒ 全池塌缩` ⇒ **收割席压力本身就是塌缩来源之一**（与 §N28 的 Q-6 同向）。
>
> **§N31 顺带把两句我自己的话降级**：① "考卷偏爱窄包"不成立 —— 历史 20 包同尺横量 `Pearson r(考卷, 净兑现G) = +0.234`（弱正相关，几乎无判别力）；
> ② "这条管线产不出宽包"也不成立 —— 产线包普遍是宽的（`v7cmin4-82 净落地 3.80（4 种）`、`v7cmin3-31 4 种`、2P 槽 `2.98`），塌缩集中在**今晚这种 100 代小配方**。
> 而"宽的历史包能不能上线"另有一关：`v7cmin4-82` 体检 `D 长程反弹墙 5%`、`反弹墙伤害 4.30`（现役 21.00）、`珠浪费 86%` ⇒ **它过不了墙那道硬门**，只是"值得量"，不是"可以换"。
>
> 产物点名（D82）：`v7xn19a-31.bak` `v7xn19a-band1.bak` `v7xn19a-band2.bak` `v7xn19a-band3.bak` `v7xn19a-band4.bak` `v7xn19a-band5.bak` `v7xn19a-band6.bak` ·
> `v7xn20a-31.bak` `v7xn20a-band1.bak` `v7xn20a-band2.bak` `v7xn20a-band3.bak` `v7xn20a-band4.bak` `v7xn20a-band5.bak` `v7xn20a-band6.bak` ·
> `v7xn20b-31.bak` `v7xn20b-band1.bak` `v7xn20b-band2.bak` `v7xn20b-band3.bak` `v7xn20b-band4.bak` `v7xn20b-band5.bak` `v7xn20b-band6.bak` ·
> `v7xn21a-31.bak` `v7xn21a-band1.bak` `v7xn21a-band2.bak` `v7xn21a-band3.bak` `v7xn21a-band4.bak` `v7xn21a-band5.bak` `v7xn21a-band6.bak` ·
> `v7xn23a-31.bak` `v7xn23a-band1.bak` `v7xn23a-band2.bak` `v7xn23a-band3.bak` `v7xn23a-band4.bak` `v7xn23a-band5.bak` `v7xn23a-band6.bak` ·
> `v7xn23b-31.bak` `v7xn23b-band1.bak` `v7xn23b-band2.bak` `v7xn23b-band3.bak` `v7xn23b-band4.bak` `v7xn23b-band5.bak` `v7xn23b-band6.bak` ·
> `v7xn22a-31.bak` `v7xn22a-band1.bak` `v7xn22a-band2.bak` `v7xn22a-band3.bak` `v7xn22a-band4.bak` `v7xn22a-band5.bak` `v7xn22a-band6.bak`
> 明细：`docs/RESEARCH-LOG-2026-09-23-qoder-night.md` §N29/§N30/§N31。

## v1.5.169 — CLI 终于能**训长程**了（`EPIRUS_TRAIN_MODE`）＋产物自带配方 `meta.recipe`

> **动因（用户 09-22 的目标原话）**："理想情况下应该炼一个 5 血长程能通吃其他模式"。查了一下这句的**前一半在仓里从来没跑过**：
> `js/train/evo.js` v1.4.0 就有 `setTrainMode`（门 D10 一直钉着"`setTrainMode('long')` ⇒ 建局 5 血"），但**CLI 侧从来没有这个键** ⇒
> `TRAIN_MODE` 恒 `'multi'`，而 `evo.js:28` 的注释自己就写着"**5 血冠军从来没被训过**"。也就是说"通吃长程"这句话一直是**愿望**，不是一条实验记录。
>
> **改法**：`server/train-env.mjs`（训练分布旋钮的单一来源）加第二个键 `EPIRUS_TRAIN_MODE` ⇒ `train-3p` 下发前先用 `R.MODES` 认名字，
> 三条纪律一起上：① **不认就 `exit 7`**（绝不允许"要了 long、静默训成 multi"—— 那是 §N11 烧掉两臂的那一族）；② 下达后**读回消费点**并印出建局尺（`hp=5`）；
> ③ 生效值进产物 `meta.recipe`。
>
> **`meta.recipe` 是这版的第二件东西**（同一条纪律的推广）：以前"这臂开了什么旋钮"只在**日志**里，而日志会滚走、`.bak` 会留下来 ⇒ 事后复盘只能靠文件名猜。
> 现在每粒产物自带 `arm / seed / gens / games / pop / xn2w / xn2g / selLand(+games/tol) / kill{req,fired,seats,names} / trainMode / trainModeEffective`。
> 实测（臂 `v7xn17a`）产物里读出 `"trainMode":"long","trainModeEffective":"long","kill":{"req":0.125,"fired":516,"seats":["0","1","2","3","4"]}` ⇒ **横幅与日志都不是证据，产物自己才是**。
>
> **首臂读数（预注册判据判负 + 我自己的一次过度归因被复现打回）**：`v7xn17a` = 与 `v7xn15c` **逐字同配方**只把训练模式换成 `long`。
> 主判（跑前写死）= 通吃矩阵（`tools/probe-cross-mode.mjs`，40 局/格）`脚本池` 环境的**最弱格**：seed 31 对 `long 7.5%` vs `multi 12.5%`、seed 32 对 `long 5.0%` vs `multi 7.5%`
> ⇒ **两对种子都是长程臂更差 = 预注册走向 ③（负结果入档）**；而四臂的池最弱格**全都 ≤ 现役包的 12.5%** ⇒ 这条配方（100 代 × 种群 8 × 每代 8 局 · XN2W=1 · KILL_FIELD=0.125）在**跨模式鲁棒性上没有产出增益**。
>
> **⚠️ 我只看了 seed 31 一臂就写下一句错话，这里就地撤回并留档**：当时读到"长程臂各格 `G(出手)` 只有 1.00~2.41、净兑现 1 种、平均回合从 17.5 拖到 49.9"，
> 就下结论说"**长程模式下的最优解就是便宜卡反复蹭 + 拖到收缩**"。**seed 32 的复现把它否了**：同一模式那臂反而是今晚**最宽**的一粒（各格 `G(出手) 5.52~6.74`、净兑现 1.5~2.0），
> 而它的 multi 孪生臂才塌成"只有 `枪` 打上血"（净兑现 `1.00（1 种）`）。⇒ **"广度塌缩"是臂级（种子）方差，不是模式属性**。
> 附带一条也降级：期末考卷与广度**不同向、但也不是系统性反向**（seed 31 长程臂考卷更高更窄、seed 32 长程臂考卷更高也更宽）⇒ "考卷偏爱窄包"只能记成**未定**。
> 教训按本仓老规矩写死：**一臂一线索不叫发现，叫噪声的一个样本**（今晚第二次由复现推翻我自己的因果句，第一次是 §N25 那个污染口径）。
> 该问的问题因此换掉了：不是"long 还是 multi"，而是"**为什么同一配方会有一半的臂塌成一张卡**"⇒ 提案见夜日志 §N29（把广度当**准入线**，不是当排序键）。
>
> **产物点名（D82 · 把 §N25/§N28 这几臂的全部落盘一次点齐，含每条臂的带内候选 `band1..band6`）**：
> `v7xn15a-band1.bak` `v7xn15a-band2.bak` `v7xn15a-band3.bak` `v7xn15a-band4.bak` `v7xn15a-band5.bak` `v7xn15a-band6.bak` `v7xn15a-31.bak` `v7xn15a-2psh.bak` ·
> `v7xn15b-band1.bak` `v7xn15b-band2.bak` `v7xn15b-band3.bak` `v7xn15b-band4.bak` `v7xn15b-band5.bak` `v7xn15b-band6.bak` `v7xn15b-31.bak` `v7xn15b-2psh.bak` ·
> `v7xn15c-band1.bak` `v7xn15c-band2.bak` `v7xn15c-band3.bak` `v7xn15c-band4.bak` `v7xn15c-band5.bak` `v7xn15c-band6.bak` `v7xn15c-31.bak` ·
> `v7xn15d-band1.bak` `v7xn15d-band2.bak` `v7xn15d-band3.bak` `v7xn15d-band4.bak` `v7xn15d-band5.bak` `v7xn15d-band6.bak` `v7xn15d-31.bak` ·
> `v7xn15f-band1.bak` `v7xn15f-band2.bak` `v7xn15f-band3.bak` `v7xn15f-band4.bak` `v7xn15f-band5.bak` `v7xn15f-band6.bak` `v7xn15f-31.bak` ·
> `v7xn17a-band1.bak` `v7xn17a-band2.bak` `v7xn17a-band3.bak` `v7xn17a-band4.bak` `v7xn17a-band5.bak` `v7xn17a-band6.bak` `v7xn17a-31.bak` ·
> `v7xn17b-band1.bak` `v7xn17b-band2.bak` `v7xn17b-band3.bak` `v7xn17b-band4.bak` `v7xn17b-band5.bak` `v7xn17b-band6.bak` `v7xn17b-31.bak`
> （`v7xn15*` = §N25 兑现广度线；`v7xn17a/b` 与它们的 `multi` 孪生 `v7xn15c/f` 只差一个变量：训练模式。）
>
> 明细：`docs/RESEARCH-LOG-2026-09-23-qoder-night.md` §N28。

## v1.5.168 — 当选面加「硬门槛预筛」＋送盾阈值收进单一来源，并把"改判"拆成**三格归因**（§N25 · L1 臂的反例自证）

> **为什么要有这一版**：v1.5.167 的兑现广度排序键**咬了，但咬错了**。臂 `v7xn15a`（`EPIRUS_SEL_LAND=1`）带内改按 `landG` 取大者后选上了 `band5`，
> 体检实测：`考卷 35.1%` · **`C 盾/局 11.9`**（> `HOLO_GIFT_MAX=6` ⇒ 这是 `promote` 里**连 `--force` 都不放行**的硬门槛）· **`D 长程反弹墙 0%`** · `反弹墙伤害/局 3.20` · `E 无威胁摆架势 49%`。
> 也就是**用 0.8pt 训练分换来一粒过不了硬门槛的包** —— 广度这条线不该有能力把不可上线的候选换上来。
>
> **改法（两处）**：① `bandPickByLand` 先剔 `gateOk === false` 再算带（未提供 `gateOk` 的旧调用点行为与 v1.5.167 **逐字一致**）；
> ② `train-3p` 给每粒候选算 `gateOk = mirrorHealth.holoOtherPerGame <= HOLO_GIFT_MAX`，而 `HOLO_GIFT_MAX` 从 `promote-champion` 里那个字面量 `6` 抽进 `audit-lib` ——
> 同一阈值两处各写一遍正是本仓栽过四次的那类漂移（`FEAS_N`/`ECON_ENV_KEYS`/`pierceKeys` 同型）。
>
> **顺带修掉一条我自己写下的误导性打印**：L1b 那臂报"未改判"，可产物其实**换了包**。原因是打印只回答"带内排序键换没换人"，
> 而预筛**换掉了池**（6 粒 → 2 粒）。现在三格分开报：`改判（排序键换人）` / `改判（是预筛选掉的，不是排序键）` / `未改判（池与排序键都没换人）`。
> ⇒ 判据级教训：**只要一个开关能改变产物，它就必须说清是哪条机制改的**，否则读数人会把它当成"没开"。
>
> 产物点名（D82）：`v7xn15b-31.bak`、`v7xn15b-2psh.bak`、`v7xn15b-band1.bak`…`v7xn15b-band6.bak`（同臂 L1 侧见 v1.5.167 的点名行）
> 明细：`docs/RESEARCH-LOG-2026-09-23-qoder-night.md` §N25(b)。

## v1.5.167 — 第二把广度量具 `effSkillsLand`（**出手熵 → 兑现熵**，只数真卡名）；当选面排序键 `EPIRUS_SEL_LAND`（默认关）· **本版结论：只做读数，不当判据**

> **动因（用户原话）**："现在的 `v7cmin4-31` 虽然 `G_eff` 有 4.44，但他真的只用 6 个技能，感觉像是为了刷 eff 只用那些最容易被测到的技能。"
> 这条怀疑**量化成立**：`G_eff = exp(出手次数的熵)` 只看"发起了几种"⇒ 一张常出手常被防住的卡与一张少见但每次掉血的卡**在同一把尺上等价**。
> 现役包 multi 镜像 **出手:落地 = 537:278**（**约一半出手没变成伤害**）。
>
> **改法**：`mirrorHealth` 本来就有 `landByKey`（按 `damage.via` 归因）⇒ 用同一套熵数它，新增 `effSkillsLand / landedKeys / landedTotal / landedFiltered`；
> `promote-champion` 的可行性那行**并排印** `兑现 G 4.44→2.66（3 种打上血）`（multi 与 long 都印）。**只加读数、不动任何既有阈值** ⇒ 两槽所有旧门读数逐位不变。
> 现役包净读数：`G(出手) 4.44 → 兑现 2.66（3 种真卡）`，长程 `3.43 → 2.48（4 种）`。
>
> **⚠️ 发布前自查抓到一个方向性口径 bug（这一版差点把一根假轴当真轴发出去）**：`landByKey` 统计的是 `damage.via`，而 `via` 里有一批取值**不是一张卡**——
> `deliverDamage` 在 `via` 缺省时会回落到**中文 reason**（`resolve.js:305`）⇒ `终局收缩`（规则白送的全员 −1）入账；另有 `headshot`（爆头 = 狙击的**结果修饰**，`resolve.js:1207`）、
> `原型制御·转移`（`resolve.js:270` 只给 reason）、`dream`/`chain`/`counter`/`taunt`。
> **污染不是"数字偏大"而是方向错**：不滤就等于**奖励"拖到收缩阶段还活着"的包**、惩罚主动进攻的包。实测同臂 6 粒候选的非卡名占"落地" **10%~41%**（`band4`：293 次落地里 104 次是收缩）。
> 后果可查：§N25 的 L1"改判"整条是这个 bug 的产物 —— 污染口径下 `band5` 的 `落地G=2.62` 是全场最大（比 `band2` 只高 **0.01** ⇒ 噪声级差决定换人），
> 净口径下它只有 `1.51`（6 粒里排第 5），最大者换成 `band1=1.61`。 ⇒ 口径改成**只数 `R.byKey` 认得的键**，并把滤掉的量记成 `landedFiltered` 自证。
> 已知**偏保守**的一处：`原型制御·转移` 是真卡伤害但键名不是 `SK.proto`，被一并滤掉（代价 = 少数一种卡；收益 = 永不把非卡的血算成卡）。
>
> **为什么不把"落地"写进奖励**：仓里有前例 —— "奖励贵技能落地"的探针会选出**乱挥双枪**的冠军（实测 −27pt，`evo.js:evalSubsidyProbe` 头注）
> ⇒ 它最多当**同分带内的排序键**（胜率不为广度让路、带外者永不参与），这就是 `EPIRUS_SEL_LAND`（默认 0）。
> **而它现在仍未被证明有用**：净口径 + 预筛后重跑的同配方 A/B（`v7xn15c` 关 / `v7xn15d` 开）读出 `同分带 1/6 · 未改判` —— 本配方 6 粒的胜负分差距大于 `tol=0.03`，**排序键根本没得咬**，两臂冠军是同一粒。
> ⇒ 结论：**`净兑现 G` 当读数已被证明有用**（它一把拆穿了 `band5`：出手 938 次/20 局是现役的 1.8 倍、兑现率只有 29%），
> **当排序键目前既没优势也没被否证** ⇒ 保持默认关；下次别急着再加第三根排序键，先问"带里到底有几粒"。
>
> **门 D127**：`effSkillsLand` 与 `effSkills` 同时可读 + **独立复算净熵逐位相等** + `landedFiltered` 等式 + "这份读数里必须真出现非卡名 via，否则本断言是空枪" + `bandPickByLand` 九向（带外永不参与 / 缺 `landG` 记 0 / 换人必须报 `tieBrokenBy=land`）+ **默认关基线哈希钉 `aa743488cc`**。np 172→**174**。
>
> 产物点名（D82）：`v7xn15a-31.bak`、`v7xn15a-2psh.bak`、`v7xn15a-band1.bak`…`v7xn15a-band6.bak`；同配方 A/B 的净口径臂：`v7xn15c-31.bak`、`v7xn15c-band1.bak`…`v7xn15c-band6.bak`、`v7xn15d-31.bak`、`v7xn15d-band1.bak`…`v7xn15d-band6.bak`
> 明细：`docs/RESEARCH-LOG-2026-09-23-qoder-night.md` §N24/§N25。

## research(0923 · qoder 夜班两件新量具) —— 技能**边际价值**（机会式 ε 注入 + 定向垫钱）与 **G_eff 出手 vs 落地** · 不升版本

> `tools/probe-skill-marginal.mjs`（第三套技能口径）+ `tools/probe-cast-vs-land.mjs`（把"有效技能数"拆成发起与兑现）。都只读、不写产物。
>
> **为什么要第三套**：`skill-report` 的 mono-spam Δ 对辅助/防御卡是**结构性负**（每回合只防不攻必输：反弹 −69 / 八卦 −79 / 贴贴 −82 / 天火 −82 ⇒ 那是口径的价不是卡的价）；
> 对**要铺垫的卡**直接失效（电磁炮强制命中 4%、摄魂 6%、激光眼 0% ⇒ 判"实验未生效"）——"每回合都打它"= 永远不许先备珠，**强迫自己反而把它逼没了**。
> 新口径改测"愿不愿意用"：ε 概率、仅在该卡合法且买得起时替换冠军一手，其余照打，同种子同对手配对，SE 取逐局胜负差；
> 另加 `--rich=card`（**定向垫钱**：只把被测卡垫到刚够、两臂同垫）。**long 模式可读卡片从 1~2 张（mono-spam）/3 张（原生 ε）提升到 14 张**（multi 12 · 2P 槽 14）。
>
> **发现 1｜长程是经济瓶颈，不是偏好瓶颈**：`光垫钱` 列（同垫不注入 vs 原生）在 long 下一律 **+41~46pt**（multi 只有 +17.5~+36）
> ⇒ "它不用大卡"首先是**手里没那笔钱**。任何不动经济的"奖励广度"旋钮，量的都是同一个瓶颈的第二遍。
>
> **发现 2｜用户的怀疑成立且可量化**：现役 `v7cmin4-31` 门禁读数 `G=4.44`，但**兑现过伤害的技能只有 3~4 种**、
> multi 镜像下出手:落地 = 530:237（**55% 的出手没变成伤害**）、long 镜像 995:494。`G_eff` 只看发起了几次，不看打没打上
> ⇒ "真实多样性"该盯 `G_eff(落地)`。下一步：`mirrorHealth` 加 `effSkillsLand`（它本来就有 `landByKey`）+ 体检并排印 + 当选面加默认关的 `EPIRUS_SEL_LAND`。
>
> **发现 3（我自己的错，两个）**：① 第一版把补贴挂在 chooser 里，而合法表在 chooser **之前**就算好 ⇒ 电磁炮补了珠也进不了 `legal`（"合法率 90% 却机会 0.27/局"这组自相矛盾才暴露它）⇒ 改挂回合开始钩子；
> ② `--rich=6` 无差别补贴把基线打到 **1st=100%**（天花板饱和 ⇒ 所有 Δ 挤成 0）⇒ 才改成"定向垫钱 + 光垫钱列自证"。
>
> 明细：`docs/RESEARCH-LOG-2026-09-23-qoder-night.md` §N22/§N23。

## v1.5.166 — **R48「回魂复活回合」只免花费、不免条件**（用户报的摄魂 bug 追到的三处短路）＝**指纹换代 `71b5927f` → `0f931cb7`**，行为变化范围已配对量清

> **用户报的是"长程模式下出现过一次 hp=1、之后血回上去了摄魂指法仍然可用"。先说结论：时间闩锁没能复现**——
> `computeCost` 在 hp 1→4 立刻判非法 ✓、`attemptAction` 在 hp=4 返回 `invalid` ✓、`legalActions` 不含它 ✓（2P/3P 都一样）；
> 260 局长程的 **5.2 万次**合法性查询 **越界 0 次**；`infiniteEnergy` 回合末即清（且复活者回合结束就死）、复活时 hp=1 本来就在窗口内；
> 连边是铁索连环（一次性伤害共享，与可用性无关）；页面日志会把被拒的一手标成"（无效）"。
>
> **但顺着查出来一处更根本的规则实现错误**（用户描述的"满血也能摄魂"在这条分支上是真能发生的）：`infiniteEnergy` 把**条件**连同**花费**一起免掉，三处短路——
> `state.js:computeCost` 开头就 `return {ok:true}`、`state.js:attemptAction` 干脆不走 `computeCost`、`play.js:legalActions` 直接 `push(affordable:true)`。
> 规则原文（`docs/RULES-2P.md:336`）说的是「**一切花费为 0**（含蓄能产物无限）」⇒ 免的该是 `ep/hp/珠子` 这些**花费**，
> 而不是：摄魂的"仅限自己 HP≤`drainHpMax`"、电磁炮的"需 1 枚电珠"、激光眼首次"需 1 枚爆珠"、过载炮第 2 次"需 ≥1ジ"、第 3 次"自损 1 血"。
> **一处短路同时绕过了这 5 个判据**（这也是为什么它值得修，尽管很难碰到）。
>
> **改法**：`rawCost` 照原判据查条件；`computeCost` 只在条件通过后把花费归零并打 `free:true`；`attemptAction`/`legalActions` 取消短路 ⇒ 非免费回合的行为**一字不变**。
>
> **变化范围（实测，不是"我觉得影响很小"）**：新旧引擎同种子配对复现 **6480 局**（standard/multi/long × 9 种出招风格）
> ⇒ 触发过回魂的只有 **13 局**、新旧读数不同的 **4 局**、**胜者全部相同**；差异全在复活回合内部（谁多打 1 点伤、谁少扣 1 血）。
>
> **指纹换代**（`state.js`/`play.js` 都在五件套）：`71b5927f → 0f931cb7`。两槽全部读数**逐位不变**——
> 2P `99.00% / 最差 82.50%`；3P 考卷 `54.4%`、五道 `座位 12.5pt · G(multi) 4.44 · G(long) 3.43 · 墙 21/局 · 场A 35% · 场B 0.33`
> ⇒ 按 v1.5.157 先例把新指纹与这段证据写进两个 bundle 的 `meta.rulesFingerprint`/`fingerprintRefresh`（**权重逐字节未动**，D16 绿）。
>
> 门 **D126**：满血免费回合三个条件门都不许绕过 + 合法表不得出现 + 真在窗口内则合法且**不扣资源** + 过载炮两步花费 + 普通回合回归 + **反抄静态钉**（三处短路的字面形态都不许复活）。np 172 → **173/173** · spec 52/52 · smoke OK。
>
> **两处自我纠正**：① 把 `p.lastTarget` 改成"只在成功一手写"被 **D26 判红**（v1.5.19 的理由是"手势指向谁公共可见"）⇒ 属判据级取舍，**已回退**，记成待裁 Q-1；
> ② 我第一次算的指纹 `2a3179fb` 是回退**之前**的值，拿它重记过一次 meta 后 D16 仍红 ⇒ 真值 `0f931cb7`；所有读数与配对复现都在回退之后跑，证据成立、只是号错过一次。

## v1.5.165 — 浏览器训练路径的 feasibility 补上**第四道（G(long)）**，并修掉一处"标签与实测量不符"（§N20 · 用户批准）

> **两个问题一起暴露**：`train-server` 记 `meta.feasibility` 那处 ① **不传 `G2`** ⇒ v1.5.145 用户裁定"两个模式都判"在浏览器路径上根本不存在；
> ② 它把**训练模式**当 `G` 喂（`mirrorHealth(finalParams, …, mode)`）⇒ 训练长程时记下的那格其实是 long 的读数，却被 `feasibilityOf` 打印成 `G(multi)`。
>
> **改法**：显式跑两遍、与 CLI 同尺 —— `G = mirrorHealth(finalParams, FPN.games, 5, 'multi')`、`G2 = …, 'long'`，
> 传进 `feasibilityOf`，`[feasible]` 行同时打印 `G(multi)` 与 **`G(long)`**。仍属**只记录不阻断**（出厂门槛在 `promote-champion`；
> `feasibilityReject` 那格走的是 `[health]` 健康门槛，与本版无关，已核对未被当 veto 用）。
>
> **运行时验证（不是静态钉）**：拿现役 3P 槽 `v7cmin4-31` 走一遍新序列 ⇒ `G(multi)=4.44`、`G(long)=3.43` —— **与 CLI 体检记给它的两个数完全一致** ✓；
> 反证：把 `G2` 压成 1.2 ⇒ 第四道立刻判红（`G(long) 1.20 < 3`）⇒ 这一栏真的在咬，不是摆设。
>
> 门 **D125②c** 三条：必须显式跑 multi 与 long 两遍 / 不许再拿训练模式当 `G` 喂 / `G2` 必须真传进 `feasibilityOf`。
> np **172/172** · spec **52/52** · smoke OK；指纹仍 `71b5927f` ✓（`train-server.mjs` 不在五件套）；两槽未动。
> 附带：用户自行删除远端分支后，本会话按其指示删掉最后一条本地分支 `qoder-explore-0921night`（已完全并入 main，`2d95f7c`）⇒ 仓库只剩 `main`。

## v1.5.164 — 服务器路径的**体检读数**与 CLI 同尺：`mirrorHealth(finalParams, 40, n, mode)` → `(FPN.games, 5, mode)`（§N19 · 用户批准）

> **前提纠正（我自己的错，见 v1.5.162 那条勘误）**：不存在"两种 G 量具"——`audit-lib.selfPlay` 从 v1.5.19 起就是 `evo.js:mirrorHealth` 的三行转发。
> 真问题是**同一个量具被喂了不同参数**：全仓 9 个 `mirrorHealth` 调用点里，8 个都是 `(20 局, 5 席)`，
> 只有 `train-server` 记 `meta.feasibility` 那一处是 `(40 局, n 席)` ⇒ 浏览器训练路径记下的 `G(multi)` 与 CLI 体检**不可直接比**
> （镜像自对局的 G 同时依赖局数与桌子大小：少一个对手就少一路攻击机会 ⇒ 分布更窄；`g-calib` 存在的理由正是标定这种 n 敏感性：n10:2.99 / n20:4.24 / n40:4.54）。
>
> **改法**：那处改传 `(FPN.games, 5, mode)`（`FPN = audit.feasPlan(process.env)`，v1.5.162 已有的单源），并在 `[feasible]` 行**自带尺子**
> `〔n=20/aggr40/seat100 · G=mirrorHealth(20 局, 5 席)〕` ⇒ 以后谁再动 n/席位，日志一眼看得出来。
>
> **明确没动的那一处（重要）**：同文件 `[health]` 行 `mirrorHealth(finalParams, HGD.games, HGD.n, mode)` 是**产物决定点的健康门槛**，
> 按当前训练人数评估是**有意的**（判的就是"这张桌子上能不能打"）⇒ 不属于体检读数，D125②b 把它钉成两行断言，防止以后有人"顺手统一"把门槛挪走。
>
> 门 **D125②b** 新增四断言：体检那处必须 `(FPN.games, 5)` / 旧 `(40, n)` 不许复活 / 健康门槛必须继续用 `HGD.n` / `[feasible]` 必须印尺子。
> 影响面：只改**记录口径**（那条路只记录不阻断，出厂门槛在 `promote-champion`）⇒ 已有包与两槽读数不受影响；指纹仍 `71b5927f` ✓。
> np **172/172** · spec **52/52** · smoke OK。
>
> **仍未闭的下一条（等裁定，我没动）**：服务器路径的 `feasibilityOf` **不传 `G2`** ⇒ 浏览器记的 feasibility 里没有 v1.5.145 那条"long 模式也要判"的第四道；
> 补它属于**加判据**（会让更多 `fails` 进 meta），不是统一参数，所以留给裁定。

## v1.5.163 — 删除死键 **`EPIRUS_PASSIVE_FIELD`**（用户 09-22 裁定"那还是算了…要么删掉"）＝**整族拆除 + 已删键也要响亮拒绝**，行为零变化已证

> **删的是什么**：v1.5.65 放的"按密度注入 4 席全被动局面"这条训练分布旋钮。**它自落地起一局都没开过火** —— 消费点 `evo.js` 查
> `BOT_PICKS['farmer'|'deepsaver']`，而那张表从来没有这两个键（它是对手池注册表，`buildOpps`/`champVsBaseline` 都 `Object.keys` 它，加键会改默认训练）
> ⇒ 条件恒假、每局都走正常池抽。两道门当时都判在**变量**上（序号谓词 + 横幅"消费点读回"），没一道判在**效果**上
> ⇒ 臂 `v7xn10c`/`v7xn11a` 各 100 代白跑（都是 7′ 的逐字节复现），直到 §N11 才查出来。
>
> **拆了哪几处**（一次拆干净，别留半死状态：setter 还在、作用点没了 = 最坏的那种）：
> `js/train/evo.js`（变量族 + setter + 读回器 + 加载时兜底读 + `passiveFieldAt` + 注入分支 + 4 个导出符号，**留两行墓碑**说明为什么别再这么写）、
> `server/train-env.mjs`（键与字段；`TRAIN_ENV_KEYS` 现只剩 `EPIRUS_KILL_FIELD`）、
> `tools/train-3p.mjs`（闭集项 + 下达块）、`server/train-worker.mjs`（同名块）。
>
> **新增一条闸**：`REMOVED_TRAIN_KEYS` 单一来源 + `detectRemovedKnobs` ⇒ 谁再传 `EPIRUS_PASSIVE_FIELD`，**`exit 6` 并说明"删于哪一版、为什么、用什么替代"**
> （静默忽略等于把"传了等于没传"换个形态留下）。
>
> **零变化证明（不是声明）**：D123③ 那条基线（seed 7 · `3 3 6 4`）改前后冠军权重 sha1 同为 **`aa743488cc`** ✓ —— 因为这一族本来就一次都没生效过。
> 门 **D122** 的两格随之改写：③a 已删键 ⇒ exit 6；③b **空枪检测改用活键 `EPIRUS_KILL_FIELD`，判"开火计数 + 覆盖 ≥2 个受评座位"，不再判横幅**；
> ④ 静态清单 `DEAD_LITERAL` **清空** ⇒ 从此往 `js/` 里加任何 `process.env.EPIRUS_*` 字面读都立刻红。
> **D123/D125 的断言同步反向化**：`passiveFieldAt`/`setPassiveField`/`PASSIVE_EVERY` 现在必须**不存在**（复活一次就等于再烧一臂）。
> np **172/172** · spec **52/52** · smoke OK；`evo.js` 不在指纹五件套 ⇒ **指纹仍 `71b5927f` ✓**、两槽读数不需重记（本版零行为变化）。
> 产物点名（D82）：（无新增）

## v1.5.162 — 可行性五道的**样本量收进单一来源** `audit-lib.feasPlan`（§N17 · 用户批准）+ 门 D125 —— 顺带翻出服务器路径把 n **硬编码**成 60/20/20

> **病（§N16 实测，不是猜）**：同一个候选在三条路径上读出不一样 —— `promote-champion` 的 `selfPlay`/`reflectWall` 吃 `--games`（默认 **20**）、
> `train-best` 的 3P 栏默认 **120** ⇒ 同包 **墙 17.85 vs 18.04、G(multi) 4.76 vs 4.80**；统一时又翻出 `train-server` 把
> **seat 60 / wall 20 / aggr 20 硬编码**在服务器路径里（而 `champ-audit` 的注释自己写着"座位探针 ≥100 才有判别力"）。
> ⇒ "五道"看着是一个名字，其实是**三把尺子**；这四个 n 的默认值当时抄在 4 个文件里（本仓"量具抄两遍"家族的又一例）。
>
> **改法**：`FEAS_N_DEFAULTS` + `feasPlan(env, over)`（优先级 **调用点显式 override > env > 默认**），
> 四个入口（`promote-champion` / `train-best` 3P 栏 / `champ-audit` / `train-server`）全部改读它；`train-best` 的 `EPIRUS_TB3P_GAMES` 不设时**与体检同尺**。
> 并且**读数必须自带尺子**：体检行现在印 `（与训练落盘同源 · 样本量 n=20/aggr40/seat100）`，3P 栏印 `[3P栏 n=20/aggr40/seat100]`。
>
> **门 D125**：`feasPlan` 优先级/非法值回落（`abc`、`0` 都不许变成 NaN 或 0 局空读数）+ **反抄扫描**（`EPIRUS_(SEAT|AGGR|DENSITY|CHARGE)_GAMES || <数字>` 只许出现在 `audit-lib`）
> + 行为式（迷你收口臂不设 `TB3P_GAMES` ⇒ 必须打印体检那把尺子）。**门第一次跑就抓到我把优先级写反（env 赢了显式 override）**，已修。
>
> **零变化证明**：`GATE4_GAMES=40` 下重跑现役 `v7cmin4-31` 体检 ⇒ 座位 12.5pt · G 4.44 · G(long) 3.43 · 墙 21 · 场A 35% · 场B 0.33 **逐字同改前** ✓
> （唯一变化是多印了尺子）。`train-server` 那处**只记录不阻断**（出厂门槛在 `promote-champion`），所以改的是读数口径、不是判定；
> 它那里还有一处分叉**没动**（**勘误见下**）：记 `feasibility` 的那行 G 用的参数与其他调用点不一致。
>
> **勘误（同一晚，写 v1.5.163 时读到函数体才发现，我上一条写错了）**：原话「server 用 `mirrorHealth`、CLI 用 `selfPlay` ⇒ 两种量具」**不成立**——
> `audit-lib.selfPlay` 从 **v1.5.19** 起就是 `mirrorHealth` 的三行转发（`return W.EpirusTrainer.mirrorHealth(params, GAMES||20, 5, mode)`）⇒ **同一个函数**。
> 真差别在**参数**：全仓另外 8 个调用点（`selfPlay`/`g-calib`/`screen-champ`/`np-test`）席位都**硬编码 5**、games 用 20，
> 只有 `train-server:692` 记 `feasibility` 那一处传 `(40, n, mode)` ⇒ **games 40、席位数 = 当前训练人数（3~5）**
> ⇒ 同一个包在 3 人桌读的 G ≠ 5 人桌读的 G，浏览器路径记进 `meta.feasibility` 的那份**与 CLI 体检不可直接比**。
> `g-calib` 同时跑 20 与 40 做标定，恰恰证明"G 对 n 敏感"是真实问题（它自己的注释就这么写着）。统一方案（走 `feasPlan` + 席位固定 5，或明写"按训练人数"并打印）等裁定 —— 那条路**只记录不阻断**，不动判定。
> **教训同 §N11：读到函数体再下结论。**
> np **172/172** · spec **52/52** · smoke OK · 指纹仍 `71b5927f` ✓（改动全在 `tools/`+`server/`）。

## research(0922 · qoder §N16 = §N14 同配方重跑，唯一变量是"当选面有了 3P 眼" · 不升版本 · 未 promote · 两槽未动) —— **P1 不是纸面修复：上一臂那个"3P 双 0"的当选者被否决了**

> 训练部分与 §N14 **逐字复现**（三候选 `score`/`avg` 全同：−1.000·88% / 0.964·98% / 0.966·98%）⇒ 单变量成立。
> 第二栏读数：`候选2 ✗（反弹墙 0.00 ≤0.5 · 场B 0.00 <0.3）` ← **正是 §N14 的当选者**；`候选1/3 ✅`；择优改选 **候选3** ⇒
> `v7xn16a-2p.bak` 与 `v7xn14a-band2.bak` **权重逐位相同 `bf32314213`** ⇒ §N14 已读过的两栏直接搬用：**2P 考卷 98.25%/最差 85% ✓ 过门**、**3P 五道全过**。
>
> **但预注册第三格仍不成立**：它 `G4[long]` 最克「只枪」65% > 60% ✗、`G5[multi]` 防席夺冠 34% > 25% ✗；
> 叠加 §N14 发现 B（场B 4.00/局 是**软靶刷出来的**）⇒ 真实位置是"**两栏都要的候选存在了，但要的是软靶上的收割，硬防守面前没进步**"。
> ⇒ 路 B（对手池/收割席）**到此为一段落：不加密度档、不再换旋钮**；3P 槽不换（它本来也没过行为门，而换包须用户裁定）。
>
> **新发现（口径家族）**：同一句"五道"在两个入口**样本量不同源** —— `promote-champion` 的 `selfPlay`/`reflectWall` 吃 `--games`（默认 20；`GATE4_GAMES=120` 只作用于 G4/G5），
> 而 3P 栏把 `n=120` 喂给全部量具 ⇒ 同一候选读出差 **墙 17.85 vs 18.04、G(multi) 4.76 vs 4.80**。差得小，但正是"阈值是刀锋/两处各写一遍"那一族。
> 建议（未动，等裁定）：把 `feasibilityOf` 输入所用 `n` 收进单一来源（或两处共用同一常量并都打印 `n`）。
>
> 产物点名（D82）：`v7xn16a-2p.bak`、`v7xn16a-band1.bak`（`v7xn16a-out.js` 是 `EPIRUS_TB_OUT` 的原始落点，非 `.bak`、内容与 `-2p.bak` 相同）
> ⚠️ 本臂候选**未 promote**；`EPIRUS_TB_OUT` 全程未触碰 2P 槽与 `index.html` 缓存戳（跑完 `git status` 干净）。

## v1.5.161 — 当选面补上**第二栏**：`train-best` 收口时终于看得见 3P（P1 · §N14 实证"band2 被挤掉、当选者 3P 场B/墙双 0"）+ 门 D124 + 产物可改道

> **病（实测，不是猜）**：`train-best` 的 fit 就是 2P 考卷，3P 那一栏**从来没进过选择**。
> 于是收口臂 `v7xn14a` 里两栏都更强的 `band2`（考卷 **98.25%/最差 85%** · 3P 五道全过 · 场B **4.00/局** · `score=0.916` **高于**当选者 `band1` 的 0.891）
> 只因容差带内 hill05 3.38 < 5.79 被挤掉，而当选的 `band1` 在 3P 侧是 **场B 0.00 + 墙 0.00/局** —— 本臂目标被自己人扔掉。
>
> **改法**：`EPIRUS_TB3P=1` ⇒ 每粒训练候选跑一遍 **3P 可行性五道**（复用 `audit-lib` 的 `selfPlay`/`reflectWall`/`aggressionProfile`/`seatSymmetry`/`densityProfile`/`chargeProfile` + **同一个 `feasibilityOf` 阈值**，不另抄一份），
> 不过者经**纯函数 `vetoBy3p`**（`tools/pick-best.mjs`）取消当选资格；**全不过 ⇒ `exit 9` 响亮**，绝不"退回单栏硬选一个烂的"。默认 0 ⇒ 一行都不跑。
> - **缺栏 = 不合格**：`col3p` 未测的候选直接判不合格（"没测 ≠ 过了"—— §N11 那两臂的病就是"读到了值就当生效了"）。
> - **在位参照不参与否决**（它是"不回归层"的基线，不是竞争者）；"只有在位参照"不误判成全否决。
> - **口径写进读数行**（`[3P栏 n=120]`）⇒ 没人能把 n=20 的数字和 n=120 的混着比。
>
> **顺手一条**：`EPIRUS_TB_OUT=<路径>` 产物改道 —— 门与探针从此**不必碰 2P 槽**，也修掉一处真实副作用：
> `train-best` 经 `writeBundleMP` 同族逻辑顺手改 `index.html` 的 `?v=` 缓存戳（§N14 实测：槽内容还原了、戳没还 ⇒ 工作区脏）。
> 现在**只有真覆写槽时**才动戳；且**改道改彻底**（带内候选也随 `TB_OUT` 走 —— D124 首跑就被 D82 抓住一粒 `tb31-band1.bak`，正是漏改 band-save 所致）。
>
> **默认关逐位不变（实证不是声明）**：`git archive HEAD` 抽一份改动前的仓库，同 seed 同参数各跑一次 ⇒ 冠军权重 sha1 同为 `f256cb100f` ✓。
> 门 **D124**：纯函数四向 + 在位豁免 + 空池不误判 + "否决必须发生在择优之前" + 必须复用 audit-lib + 行为式跑一粒迷你臂（**只认"真打印了 `[3P栏 n=20]`"**，不认横幅；收场只许 0 或 9）。
> np **171/171** · spec **52/52**；改动全在 `tools/`（消费点不在指纹五件套）⇒ **指纹仍 `71b5927f`** ✓。两槽未动（3P=`v7cmin4-31` · 2P=`v7xfer44c13-2p`）。
> 产物点名（D82）：（无新增 —— D124 与 A/A 对照跑都写临时目录；上一版漏改 band-save 产生的 `tb31-band1.bak`/`d124-band1.bak`/`d124-band2.bak` 属**测试副产品**，已删除）

## research(0922 · qoder §N14 收口臂 = band5 → `train-best` 3×500 · 不升版本 · 未 promote · 两槽未动) —— **预注册分支②命中：收口把刚买到的收割卖回了 2P；同跑里另有两条形更硬**

> 跑通且起点确证是 band5（候选 0 印出 `avg wr=64%`，与 §N13 考卷逐字相符）⇒ 16 worker、3 候选 × 500 代、槽位按规程临时换→跑→还原，`git status` 干净。
>
> | 候选 | 2P 考卷 | 3P 场B | 3P 其余 | 行为门 |
> |---|---|---|---|---|
> | **band1（当选 = `v7xn14a-2p.bak`）** | **98.13%/最差 80.00% ✓** | **0.00** ✗ | **墙 0.00/局** ✗ · 场A 22% · G 4.77/G(long) 4.58 | ✅ G4/G5 全过 |
> | band2（候选3，**被择优挤掉**） | **98.25%/最差 85.00% ✓** | **4.00/局** | ✅ 五道全过（墙 17.85 · 场A 27% · G 4.76/4.89） | ✗ G4[long] 最克只枪 65% · **G5 防席夺冠 34%>25%** |
> | 对照 | 现役 2P 槽 c13 = 99%/82.5% | 现役 3P 槽 cmin4 = 0.33 | — | — |
>
> **A（预注册那一格）**：`train-best` 的 fit 就是 2P 考卷 ⇒ 收口**必然**拿 3P 去换：当选者考卷 64.25%→98.13%，代价是场B 0.47→0.00、墙 10.5→0.00。
> **"两阶段课程"对 3P 不是中性的**，这一次卖掉了两栏。
>
> **B（读数要打折的一条，比 A 值钱）**：band2 的 **场B 清场 4.00/局 不许当"学会收割"入账** —— 同一份体检里的反证是
> **G5[multi] 面对"只防御不还手"防席夺冠 34% > 25%**（杀得动靶子、杀不动硬的），而场B 本就是**无压场**、且局长变长（自对局 72.4 回合 vs band1 的 36.6）
> ⇒ **清场数能被"杀得不费劲"刷出来**。记进口径家族：**场B 清场单栏不作上线理由，必须与 G5 成对读**（与 §N13 发现 C"搭便车"是同一枚币的反面）。
>
> **C（P1 的第二次实证，且升级为"当选者选错了"）**：band2 的 `score=0.916` **高于**当选者 band1 的 0.891，两栏读数也都更强，
> 只因容差带内 **hill05 5.79 vs 3.38** 被挤掉 —— 而 hill05 择优正是 v1.5.147 我为"别为 0.005 的熵噪声扔掉宽包"加的。**它上次挡住了另一头，这次也挡住了本臂的目标**：
> 择优函数看不见 3P 栏。⇒ 下一步唯一干净的动作是 **P1（把 3P 栏接进 `train-best`/`train-3p` 的当选面）后重跑同一配方**，不加新旋钮、不再滑密度。
>
> 产物点名（D82）：`v7xn14a-2p.bak`、`v7xn14a-band1.bak`、`v7xn14a-band2.bak`、`v7xn14a-3psh.bak`、`v7xn14a-band2-3psh.bak`、`v7xn13a-band5-2psh.bak`
> （`-3psh`/`-2psh` 都是**读另一栏体检用的壳副本**，权重与对应 `band*` 逐位相同；`v7xn13a-band5-2psh.bak` 是本臂的**起点壳**，`train-best` 只认 `js/bundled-champion.js` 那层外壳 ⇒ 换槽必读它；`-3psh` 先例见 `v7xfer44-3psh`）
> ⚠️ 三粒全部**未 promote**、3P 槽仍 `v7cmin4-31`、2P 槽仍 `v7xfer44c13-2p`。明细见 `docs/RESEARCH-LOG-2026-09-22-qoder-night.md` §N14/§N15。

## research(0922 · qoder §N13 臂 `v7xn13a` = 收割席注入第一臂 · 不升版本 · 未 promote · 槽位未动) —— **场B 买到了 0.47（现役 0.33），但同一粒的 2P 考卷塌到 64%；且臂内部就反相关**

> 义务三条全在场 ⇒ **本臂有效**：`热启动 ✓` · `killField=0.2 消费点读回 ✓` · **`开火计数：注入 841 局 · 覆盖受评座位 5 个 [0,1,2,3,4]`** ✓
> （仓里第一次有"收割压力"真的进到棋局里 —— 对比 §N11 那条"横幅说下达了、一局未注"的死键。耗时 77.3s）
>
> **判据读数（n=120 · 门禁镜像 ε=0 · 指纹代 71b5927f）**：① 场B 清场 **0.47/局** ≥0.3 ✓✓（7′=0.10 · 现役=0.33）
> ③ 2P 考卷 **64.25%/最差 0.00% ⇒ 过门 = 否** ✗✗（7′ = 96.63%/65.00% ✓）② 场A 21%（线 25%）· 墙 10.5 · G 4.12/**G(long) 5.22**（比 7′ 还高）· `G4[long]` 最克珠爆发 66% ✗（7′ 同型 63%）
> ⇒ 命中预注册那一格「**①中③掉 ⇒ 两栏并报、等用户裁**」⇒ **不滑第二档密度、不换第三根旋钮**。
>
> **发现 A：同一臂内 2P 与场B 反相关**（band-save 的第一次实战回报；三粒干净候选各一次独立考卷 + 每轮还原槽）
> `band4` trainFit **1.022（比当选者高）**·场B **0.00** ✗·场A 25%·**2P 考卷 97.25%/最差 80% ✓** ‖
> `band5`（当选）1.020·**场B 0.47** ✓·场A 21%·2P 64.25%/0% ✗ ‖ `band6` 1.019·2P 77.69%/0% ✗
> ⇒ 在这一配方下"会收割"与"2P 撑得住"不是同一粒候选的两个面 —— **跨 N 到本臂仍未闭合**。
>
> **发现 B（比数值值钱）：`train-3p` 的当选面没有 2P 栏。** 终局是"名人堂用新种子**只按 3P 重验**取最优"⇒ 它看不见考卷，
> 于是把 trainFit 更高的 `band4` 让给了 `band5`。与 §N9 同族（promote 有闸、2P 有 `vetoDegenerate`、**CLI 当选面什么都没有**）—— 这次缺的是**跨 N 臂的第二栏**。
>
> **发现 C（注入自己造出的失败模式，已拦住）**：6 粒名人堂里 trainFit 最高的 **3 粒全是零攻击 100%**（1.066/1.030/1.022）⇒
> "桌上有人替你清场"会让**不动手**变划算（搭便车）；§N9 的退化闸当场剔除 3 粒 ✓（不拦就是第二个 `xn10b`）。
> ⇒ 这条压力方向对、但**顺带奖励搭便车**，下次动它要连"反搭便车"一起设计。
>
> 产物点名（D82）：`v7xn13a-31.bak`、`v7xn13a-2psh.bak`、`v7xn13a-band1.bak`、`v7xn13a-band2.bak`、`v7xn13a-band3.bak`、`v7xn13a-band4.bak`、`v7xn13a-band5.bak`、`v7xn13a-band6.bak`
> ⚠️ `band1..3` 是**被退化闸剔除的零攻击粒**（留档作反例，不许当候选读）；`band4` 是"2P 强/场B 零"、`band5` 是"场B 强/2P 塌"——两栏不可同时要。明细见 `docs/RESEARCH-LOG-2026-09-22-qoder-night.md` §N13。

## v1.5.160 — 训练分布新旋钮 **`EPIRUS_KILL_FIELD`（收割席注入）**：给桌上放一个"会抢你收割"的对手（用户 09-22 裁定：场B 缺口走对手池）+ 门 D123

> **为什么是对手池而不是奖励**：奖励侧已经否证过——§N8 的 `xn10b`（`CLEAR_W` 真生效后第一跑）带内最高分被"纯ジ龟包"拿走
> ⇒ **奖励收割 ≠ 学会收割**。而缺口是真的：7′ 场B 清场 **0.10/局**、现役 `v7cmin4-31` 同读法 **0.33** ⇒ 要买回 0.23 的差。
> 机理：`pickKillSecure`（v1.5.156 已在注册表）坐在一席上专挑最残的下手 ⇒ "该打死的不打死，别人替你打" ⇒ **转化第一次有了价格**。
>
> **三条设计约束全部来自今天用两臂白跑换来的教训**（`v7xn10c`/`v7xn11a` 都是 7′ 的逐字节复现）：
> - **不查 `BOT_PICKS`**，直接取 `Bots.pickKillSecure` —— 那张表是对手池注册表（`buildOpps`/`champVsBaseline` 都 `Object.keys` 它，`evo.js:1799` 自己警告"加键会改默认训练"）；这正是 `PASSIVE_FIELD` 死掉的那个接缝。
> - **自带开火计数** `countKillSeats()`，且 CLI **跑完自证**：一局未注 ⇒ `exit 8`（横幅只能证明变量到位，证明不了效果发生）。
> - **去座位偏置**（用户 09-22 裁掉"每 8 局注入"的病）：相位按代旋转（步长 3，与 `games=8`/`n=5` 互质）、席位由 `(gen+g)` 决定且**永不为受评席**、**跳过不进 fit 的承诺局**、**只注多人局**（2P 那条切片的全部意义是对着考卷基准打，注进去等于换参照 ⇒ 目标与验收脱钩）。覆盖 <2 个受评座位同样 `exit 8`。
> - **不拿纯攒钱型当陪练**：用户原话"囤一点 ep 放大招也是有好处的" ⇒ 只攒不打的 `farmer`/`deepsaver` 不许进这条分支（D123 用文本+名单双向钉住）。
>
> **接线**：`server/train-env.mjs` 单源加 `kill` 字段 ⇒ `tools/train-3p.mjs`（一进程一臂 ⇒ 计数才有意义）+ `server/train-worker.mjs`（同路，计数在此为跨任务累计、只作观测）。
> 顺手修掉一处同族隐患：两端原来都用 `hasTrainOverride(trainEnv)` 判"要不要下达" ⇒ 只下达 `kill` 时会把 `passiveField` 一并打成 `undefined`；现在**逐旋钮各看各的**。
>
> **默认 0 ⇒ 逐位不变 ✓**：`3 代 × 3 人 × 6 局 × 种群 4`（seed 7）的基线产物哈希 `aa743488cc` 已由 **D123③** 钉住；
> `tools/probe-knob-seam.mjs` 同步加行复验：`EPIRUS_KILL_FIELD ✓ 开火`，而 `EPIRUS_PASSIVE_FIELD` 仍 ✗（等"删 or 调"的后续裁定，本版没动它）。
> 门 **D123**（静态两钉 + 单元三条 + 行为三跑 + 一条"闸自己会响"的反证：`EPIRUS_KILL_FIELD=1e-9` ⇒ exit 8）。np **170/170** · spec **52/52**。
> ⚠️ 消费点全在 `js/train/evo.js`（不在指纹五件套）⇒ **指纹仍 `71b5927f` ✓**，跨 N 臂读数与 v1.5.157 之后可比。
> 产物点名（D82）：（无新增 —— 门与探针都写系统临时目录）

## research(0922 · qoder §N11 暴露密度臂＝**作废** · 不升版本 · 未 promote) —— **第七例静默空转：v1.5.159 修好了"值到不了变量"，但"变量到不了局面"还断着**

> **一句话**：臂 `v7xn11a`（7′ 配方 + 单变量 `EPIRUS_PASSIVE_FIELD=0.34`）的产物与 DS 的 7′ **5689 维权重逐位相同**
> ⇒ 它既不是"暴露密度买不动收割"（判据①不中），也不是新候选 —— **这个旋钮在作用点上从未开火过一局，自 v1.5.65 起。**
> 明细与三条证据见 `docs/RESEARCH-LOG-2026-09-22-qoder-night.md` §N11。

> **证据（三条，都可复跑）**：① `v7xn7c-31` / `v7xn10a-31` / `v7xn10c-31` / `v7xn11a-31` 四个 `.bak` 的权重数组 sha1 同为 `69b43cc27d`（对照现役 `v7cmin4-31`=`1286d16776`），文件只差 `"ts"`；
> ② 受控对照：同 seed 冷启动、**只**改本旋钮（`0.125` vs `1`）⇒ 产物逐位相同（若注入活着，PF=1 意味着**每局**四席全被动，不可能同）；
> ③ 定位：`js/train/evo.js:1210/1212` 的判据是 `passiveField && BOT_PICKS['farmer'|'deepsaver']`，而 `BOT_PICKS`（`evo.js:117-136`）**没有这两个键**
>   （`pickFarmer` 在 `js/train/bots.js:292`、`pickDeepSaver` 在 `:880`，只被 `tools/train-3p.mjs:174` 放进了 CLI 对手池）⇒ `else if` 恒假、每局都走正常池抽。
>
> **为什么两道门放行（这条比"又一个死键"值钱）**：两处判据都判在**变量**上而非**作用点**上 ——
> `np-test:1379-1381` 钉序号谓词 `passiveFieldAt(0)===true`，`np-test:4502-4504`（D122③）钉横幅"消费点读回 0.34"（setter 确实把值打进了变量）。
> 家族层面的真缺口：**奖励族每个旋钮都有 `setX`+`countX` 一对，本旋钮只有 setter+取值读回、没有"注入了几局"的计数** ⇒ 死作用点不可见。
>
> **修法规格（动的是训练分布 ⇒ 等用户/DS 裁定，我没动代码）**：① **只改消费点的取函数**：`evo.js:1210/1212` 的 `BOT_PICKS['farmer'|'deepsaver']` 换成直接取 `Bots.pickFarmer`/`Bots.pickDeepSaver`（`bots.js:292`/`:880`，导出见 `:927`/`:930`）。
>   ⚠️ **不要往 `BOT_PICKS` 加键** —— 那张表是**对手池注册表**（`buildOpps` 与 `champVsBaseline`(1595) 都 `Object.keys(BOT_PICKS)`，`evo.js:1799` 的注释明确警告"往里加键会改默认训练"）⇒ 加键会连带改默认池与基线均值，那是**第二个**没登记过的分布变更。仓里现成的正确路子就是 `setImitTeacherByName` 的"走全局注册表、默认池一字不动"（`:1801-1802`）。
>   ② 加 `countPassiveFields()` 观察者，并把门改成**行为式**（固定 seed 下 PF=1 产物必须 ≠ PF=0，或注入计数 >0，别再钉横幅）；
> ③ **可比性必须选边**：改完 ① 之后"从未开火过的默认 0.125"会突然每 8 局注入一次 ⇒ v1.5.159 那句"默认 = 0.125 ⇒ 行为逐位不变 ✓"当场变假话。
> (a) 保留 0.125 ＝ 承认"修好这次才是它第一次跑"，之后所有臂与之前的臂不可直接比（先例 v1.5.130 动池）；(b) 随修把默认改 0（只许显式下达）＝ 保住逐位不变，"暴露密度"这一问仍挂着等真臂。**qoder 倾向 (b)**（把"修量具"和"做实验"分开）。
>
> **对研究线的影响（别记反）**：§N11 预注册里"①不中 ⇒ 只剩对手池"那条**不触发**（它需要一个活着的臂）⇒ **"暴露密度买不买得动收割"至今仍未评估**（§N8 的 xn10c 与本次 xn11a 是同一个问题问了两次、都没问出口）。
> 7′ 的场B 缺口两条路都还开着：**(A) 修上面 ①②+跑一个真暴露臂**、**(B) 对手池改造**（`pickKillSecure` 进池；v1.5.156 只裁了它"不当教师"，池侧接线至今任何地方都没做）。两者都动训练侧 ⇒ 等 GO。
>
> 产物点名（D82）：`v7xn11a-31.bak`、`v7xn11a-2psh.bak`、`v7xn11a-band1.bak`、`v7xn11a-band2.bak`、`v7xn11a-band3.bak`、`v7xn11a-band4.bak`、`v7xn11a-band5.bak`、`v7xn11a-band6.bak`
> ⚠️ 这 8 个是**作废臂**留档，权重与 `v7xn7c-31.bak` 逐位相同 ⇒ **不许当新候选读**（先例：`v7xn10a`/`v7xn10c` 同为逐字节复现）。

> **§N12 · 由这次的教训上升成量具**：新增 `tools/probe-knob-seam.mjs`（**旋钮"作用点开火"对照屏**，63 秒跑完 10 行，只写系统临时目录 ⇒ 不欠 D82 的账）。
> 判据是**行为式三比**：逐代/名人堂轨迹 + 全部带内候选权重 + 终产物权重，三者全同才判"这个键一局都没改变评估分布"。
> 屏上结论：跨 N 一族（`XN2W`/`XN2G`/`XN2REF`/`XN2SCRIPTS`）、`CLEAR_W`（**v1.5.154 的接线经此复验**）、`HOTSTART`、`ANCHOR`（热启动下）**全部确有作用**；
> 唯 `EPIRUS_PASSIVE_FIELD` 在 `=1`/默认/`=0` 三个值下**逐位相同** ⇒ 死作用点坐实（"关掉它居然也不变"本身就是证据）。
> 顺带一档轻的同类问题：**冷启动下 `EPIRUS_ANCHOR>0` 照样打印「锚定正则 λ=3」，但 `train-3p.mjs:305` 要求 `seedParams` ⇒ 永远不锚** ⇒ 横幅与效果不一致。
> 方法论副产：**只比终产物哈希会误判**——第一版就是把 `XN2REF`/`XN2SCRIPTS` 判成"与 base 相同"（3 代小预算下 hall 6 粒里只有 3 个不同向量，终产物相同是巧合）⇒ "产物逐位相同"要先说清比的是哪一层。
> 门提案（等上面的 ① 修完再立，否则必红）：**D123** 固定 seed 下 `EPIRUS_PASSIVE_FIELD=1` 的产物必须 ≠ 默认产物（走本屏判据，别再钉横幅）。

> **用户裁定（09-22 20:4x）**：这一路「要么删掉，要么调整到没有座位偏置」，并指出**囤一点 ep 放大招本身是有效打法** ⇒ 拿"4 席纯只攒不打"当罚对象是错的。
> 代码级推断（seam 2 死着，测不了）：默认 0.125 若真开，`PASSIVE_EVERY=8` 在 `GAMES=8` 下**只有 g=0 那一局**被注入 ⇒ 恒 0 号座（`seat=g%n`）、恒 `farmer`（`g%2`）、且 g=0 同时是**不进 fit 的承诺局**（8 个体里 2 个白打）——三处错位正对上裁定要消的那条。
> ⇒ 机制路 A（真暴露臂）**降级**为"只测会收割的被动对手"；"删 or 调（含去偏置的步长/相位 + 反证门 + 默认归 0）"的可执行规格见夜日志 §N12 附录。**等 GO 才动代码。**

## v1.5.159 — **`EPIRUS_PASSIVE_FIELD` 从"死键"升级为"可下达"**（§N10 逐旋钮接通第一步 · CLI 与 server 两端同路）

> **病**（qoder §N10 审计的"CLI 黑旋钮"）：`js/train/evo.js` 里"把 4 席全被动局面注入评估分布"的旋钮
> `EPIRUS_PASSIVE_FIELD` **只在加载时字面读 `process.env`**，而 **CLI（`tools/train-3p.mjs`）与 server
> （`server/train-worker.mjs`）用的都是手搭沙箱对象**（`{ console, Math, … }`，**没有 `process`**）
> ⇒ **传进去的值永远到不了消费点，静默吃默认 0.125** ✗ ⇒ 历史上任何"想调它"的臂都是**空枪**。
>
> **修法（与 `setEconomyReward` 族完全同构，不另造机制）**：
> - 新增 **`server/train-env.mjs`**（纯函数，与 `econ-env.mjs`/`fight-env.mjs` 同形）：
>   `TRAIN_ENV_KEYS` · `readTrainEnv(env)` · `hasTrainOverride(o)` · `trainEcho(o)` ⇒ **env 解析的单一来源**；
> - `js/train/evo.js`：`PASSIVE_FIELD` 由 `const` 改为**可被 `setPassiveField(v)` 覆盖**（并加 `passiveField()`
>   读回器），**默认仍 0.125 ⇒ 行为逐位不变** ✓；`process.env` 兜底读**故意保留**（D122 的静态声明表据此不变 ✓）；
> - **两端都接**：`tools/train-3p.mjs`（历史臂跑的地方 ✓）与 `server/train-worker.mjs`，都按
>   `if (hasTrainOverride(...)) T.setPassiveField(...)` 下达；**缺 setter ⇒ 抛错 / exit 7**（照 `S4_W` 那条
>   "宁可炸，不可静默降级"的 A/A 事故教训 ✓）。
> - ⚠️ **不构成指纹换代**：消费点是 `js/train/evo.js`，**不在 `FINGERPRINT_FILES` 五件套**里 ⇒ 指纹仍 `71b5927f` ✓。
>
> **空枪检测（本条的判据，实测 ✓）**：`EPIRUS_PASSIVE_FIELD=0.5 node tools/train-3p.mjs 1 3 2 2`
> ⇒ 打印 `训练分布旋钮已下达：passiveField=0.5 ⇒ 消费点读回 0.5（每 2 局注入一次"4 席全被动"局面）` ✓；
> **不传** ⇒ 该行完全不打印（默认路径逐位不变 ✓）；**传 0** ⇒ `（注入已关闭）` ✓
> （修掉了我第一版日志里"传 0 ⇒ 每 1 局注入一次"的**错话** ✗ —— 那会把"关闭"读成"最频繁"）。
>
> **门 D122 升级**（原用例的期待已过时，改为更强形态）：① exit-6 用例改用**仍是死键**的 `EPIRUS_FIGHT_WHISTLE`
> （CLI 至今不 import fight 族 ⇒ 传它本就无效 ✓）；② 逃逸口用例同键；③ **新增空枪检测用例**：
> 传 `EPIRUS_PASSIVE_FIELD=0.34` ⇒ 必须 exit 0 **且** stdout 出现 `passiveField=0.34` + `消费点读回 0.34` ✓。
>
> **仍不能下达的（留给用户裁）**：econ/fight 两族在 `train-3p` 上**依然无法生效** —— 仓里给它们指定的
> CLI 路径是 `tools/ring2-run.mjs`（同样机制、已接通 ✓）⇒ 要不要把这两族也接到 `train-3p`（方便、但会
> 扩大 CLI 的旋钮面），或维持现行分工，请用户定 ✓。
>
> **历史复核义务**：凡**曾**在 `train-3p` 臂里设过 `EPIRUS_PASSIVE_FIELD` 的结论，实际都跑在**默认分布**
> （每 8 局注入一次）上 ⇒ 跨该旋钮的 A/B 实际是 **A/A**，需按此复核（DS 自己的臂 1–9′ 未用过该键 ⇒ 不受影响 ✓）。
>
> 门：np-test **169/169**（**D122 ✔** 升级版）· spec 52/52 · smoke OK · D8 三方 = v1.5.159 · 指纹仍 `71b5927f`。

## v1.5.158 — 价值表补「场B 清场」读数（`aggressionProfile` 加 `seatAct` 钩子）⇒ **当次即证伪：该列对脚本线已饱和、不能用来挑教师**

> **动机**：v1.5.156 的 `pickKillSecure` 胜率 28% < `pickGunFocus` 35% / `pickAggro` 38%，但它要证的是
> **"清场/收割"**这一项 ⇒ 需要一个能判"会不会把击杀收干净"的读数。按仓规**不另写一套场景**，
> 而是在单一真源 `tools/audit-lib.mjs` 的 `aggressionProfile` 上加**换主角钩子**：
> ```
> opts.seatAct   // 默认 null ⇒ 本函数读数**逐位不变**（与既有 forceAttack/forceTurtle 钩子同规矩）
> const raw = seatAct ? seatAct : T.policyChooserN(params, 0.15);
> ```
> **零变化已验证**：无钩子 / 显式传 `null` 两种跑法，3P 包读数 = **场A 还手 35% · 场B 清场 0.33/局 · 场B 胜率 100%**，
> 与 v1.5.157 体检输出**逐位一致** ✓（`JSON.stringify` 相等 ✓）。
>
> **价值表新增列**（`tools/script-value.mjs`）：`场B清场/局`（+ 饱和标记）· `无压进攻%` · `场A还手%`，
> 全部复用同一套官方口径（4 席只ジ · 主角席轮换 · 清场哨兵 `reason === '终局收缩'`）。
>
> **⚠️ 当次即证伪（这才是本条的价值）**：四条**性质完全不同**的脚本线，场B 清场**全是 4.00/局 = 上限**：
> | 线 | 胜率 | 场B清场/局 | 无压进攻 | 场A还手 |
> |---|---|---|---|---|
> | `pickKillSecure` | 28% | **4.00** | 50% | 49% |
> | `pickGunFocus` | 35% | **4.00** | 50% | 49% |
> | `pickAggro` | 38% | **4.00** | 50% | 49% |
> | `pickBeadBurst` | 17% | **4.00** | 20% | 16% |
> ⇒ **场B 清场对脚本线无判别力**（场B 是给**训练包**当门的：包会因 ε/温度不出手 ⇒ 0.10 vs 0.33 有判别力；
> 而**脚本永远出手** ⇒ 谁都能把 4 席只ジ清完）。能判别的只有**无压进攻%**与**场A还手%**。
> ⇒ **结论**：**教师/脚本线的价值不能用脚本侧指标判**，只能用"**它带出来的训练包**的场B"判（即臂 7′ 那条路）✗✓。
> 工具已把这条写进自身输出（饱和行打 `⚠饱和·对脚本无判别力`），避免下一个人再误读。
>
> **对 `pickKillSecure` 的如实裁定**：按可测轴它**不优于** GunFocus/Aggro（胜率低 7~10pt、场B 同饱和）⇒
> **不进教学/教师计划**；但 28% > 池基线 18% ⇒ 作为**对手线**留在池中（对手靠多样性有价值）✓。
> ⇒ 避免了第三次"空示范" ✓（代价：一次 60×2 局的量测，几分钟）。

## v1.5.157 — **删除死键「火弱持续」`EPIRUS_FIREWEAK_PERSIST`**（用户 09-22 裁定）＝ **一次指纹换代**（`be6c2195` → `71b5927f`，**行为零变化**）

> **用户原话**：「火弱持续这个规则删掉吧」。**删的是训练侧实验开关，不是规则本体** —— R22「藤甲火弱：
> 一切火焰伤害 +1」**保留** ✓；删掉的是"Next 不清除、每回合重新武装（直到被火焰伤害兑现）"这个**只存在于
> 死键背后**的实验分支。
>
> **为什么它是死键**（qoder §N10 审计 + 本会话复核）：读点是
> `resolve.js` 里 `const FIREWEAK_PERSIST = (typeof process !== 'undefined' && process.env && … === '1')`
> ⇒ **浏览器没有 `process`**（产品行为恒不变，这是原设计 ✓），而**所有训练/评估工具的 vm 沙箱也没有
> `process`** ⇒ **任何 CLI 臂都打不开它** ⇒ 三处（`:159-163` 条件清空 · `:293-294` 常量 · `:304` 兑现即消费）
> 全部坍缩成"无条件执行原默认分支"。
>
> **改动**：`js/core/resolve.js` 三处删除（`:163` 改为无条件 `p.fireWeakNext = false;`，并留注释说明原委）；
> 门 **D122 的静态声明表**跟着删掉 `'js/core/resolve.js'` 一项（`js/train/evo.js` 的 `PASSIVE_FIELD` **保留**
> —— 它仍是 js/ 里的字面死读，D122 继续钉着）；`tools/train-3p.mjs` 的 CLI 侦测名单去掉该键
> （键已不存在 ⇒ 现在传它照样 exit 6）。
>
> **⚠️ 指纹换代 = 全链重记（本仓规矩）**：`js/core/resolve.js` 是 `FINGERPRINT_FILES` 五件套之一 ⇒
> 指纹 `be6c2195` → **`71b5927f`** ⇒ **此前所有成绩/读数都属于旧指纹代**，跨代比较必须注明。
>
> **零变化证明（双向，强形式）**：
> - **2P**：`node tools/promote-champion2p.mjs js/bundled-champion.js`（该工具**不动一个权重字节**，只重测考卷
>   + 补记 meta）⇒ **19 基准 × 40 局 = 760 局：平均 99.00% / 最差 82.50% / 过门=是**，与旧指纹下记的
>   `examScoreAtBuild: 0.99` **逐位一致** ✓；回读自检"权重逐字节未变 ✓"；meta 已补记为 `71b5927f`。
> - **3P**：`node tools/promote-champion.mjs js/bundled-champion-3p.js --dry`（**只读，一个文件都不写**）⇒
>   **5P 考卷 1st = 54.4%**，与旧记 `examScoreAtBuild: 0.544` **逐位一致** ✓；**五道可行性全过** + G4/G5 行为门 ✅。
>   ⚠️ 3P 侧**故意没走换包流程**（`promote-champion` 非 dry 会动槽位，用户规则要求不 promote）⇒
>   改为"只读体检拿证据 + **定点刷新 meta 指纹**"，并在 meta 里留 `fingerprintRefresh` 说明原委 ✓。
> - 门：`np-test`（169，含 **D16 两个包指纹**与 **D122**）· `spec-run`（52）· `smoke` 全绿。
>
> **影响面**：行为零变化 ⇒ 两个线上包、所有历史对比、`results/*` 的读数**结论都不变**；变的只有"指纹代"
> 这一个记账标签。**留给规则的题**（不是工程债）：要不要*正式评估*"火弱持续"这条备选规则？若要，必须走
> 指纹项目流程（声明新一代 + 全链重测重记）。

## v1.5.156 — 新增收割型对手 `pickKillSecure`（DS 09-22 · 为跨 N 的「把击杀收干净」缺口准备的示范/对手线）

> **为什么写它**：跨 N 的臂 7′（`docs/RESEARCH-LOG-2026-09-22-ds.md` §11）**两场都拿得出手**，唯一缺口是
> 「**把击杀收干净**」（场B 清场 0.10 < 0.3/局 ＋ G4[long]「珠爆发」63% ✗）。而池子里**没有**这条线 ——
> 最接近的 `pickGunFocus` 打的是**最肥**的（`mpLeader`）✗，本线打**最残**的（`oppPidOf` = 血量最低的存活对手 ✓）。
>
> **口径**（只做一件事，与 `*spam` 族一致 —— 它是对手/教师线上的**专才**，不是冠军）：
> ① 本回合能一击必杀 ⇒ 走**最便宜**的那个必杀（枪 1 → 狙击/坦克/激光剑 1 → 电磁炮 2），把击杀兑现；
> ② 没有必杀 ⇒ 把伤害压在**血量最低**的对手身上；③ 都打不动 ⇒ 攒ジ。
> ⚠️ 取目标只用 `mpKillable`/`oppPidOf`（**不按 pid 取**）⇒ 不引入座位身份通道（D50/D58 家族）。
> ⚠️ `js/train/bots.js` **不在指纹五件套**里 ⇒ 本版不动指纹、不影响任何已有成绩的可比性 ✓。
>
> **价值表实测**（1 席脚本 vs 4 席现役冠军 · multi · 60 局 · seed0=90210 · 与 G4 同装配）：
> `pickKillSecure` 胜率 **28%** ✓（> 基线 18% ✓）· 对照 `pickGunFocus` **35%** · `pickAggro` **38%**。
> ⇒ **如实记**：新线**可用但不是最强**（按胜率选教师的话 GunFocus/Aggro 更好）；
> 本线要证明的是**"收割"这一项本身**（场B 清场 / G4[long]珠爆发），而价值表默认栏是**大雷系**的 ✗ ——
> **下一步（交给接手者）**：给价值表/探针补一个**场B 读数**，用它判这条线值不值（先量价值再投入，别做第三次"空示范"）。

## v1.5.155 — **CLI 黑旋钮侦测**（DS 09-22 裁定 · 落实 qoder §N10）+ 门 D122：静态钉住"引擎里字面读 env"的清单

> **裁定**（DS）：采纳 qoder §N10 的提案 —— **"传了等于没传"必须在启动时就响**，不许跑完一整臂才发现。

> **① 运行期侦测**（`tools/train-3p.mjs`）：启动时把 env 与两份名单比对 ——
> · **本工具闭集**（12 键）：ANCHOR/ARM/BAND_DIR/CLEAR_W/HOTSTART/PUBLISH/SEED/SEEDPACK/XN2G/XN2REF/XN2SCRIPTS/XN2W；
> · **server/引擎侧名单**（单一来源）：`ECON_ENV_KEYS`（`server/econ-env.mjs`）∪ `FIGHT_ENV_KEYS`（`server/fight-env.mjs`）
>   ∪ `{EPIRUS_PASSIVE_FIELD, EPIRUS_FIREWEAK_PERSIST}`（`js/` 里"加载时字面读"的两个死键）。
> 命中"名单内、但本 CLI 读不到"的键 ⇒ 打印名单 + **`exit 6`**；有意为之用 **`EPIRUS_ALLOW_DARK=1`** 放行。
> ⚠️ 只盯这份名单、**不是"任何 `EPIRUS_*`"** —— 否则用户 shell 里随便一个无关旧旋钮（如 `EPIRUS_NO_PROXY`）
>   会让所有 np-test 迷你臂 exit 6（那是误伤）。实测：`PASSIVE_FIELD` ⇒ 6 ✓ · `DIV_W` ⇒ 6 ✓ · `NO_PROXY` ⇒ 0 ✓ ·
>   `ALLOW_DARK=1` ⇒ 0 ✓ · 闭集内照常 ✓。

> **② 静态门 D122**（更要紧的一半）：扫 `js/**/*.js`，把"字面读 `process.env.EPIRUS_*`"的清单与**声明表**逐一比对
> （声明表 = `{js/core/resolve.js: [EPIRUS_FIREWEAK_PERSIST], js/train/evo.js: [EPIRUS_PASSIVE_FIELD]}`）⇒
> **新增一处 ⇒ 红**（要么改走宿主 setter、要么显式登记为死键）⇒ 这样"第 5/6 例静默空转"从"下次小心"变成"**下次跑不起来**"。
> D122 同时行为式验证 exit 6 与逃逸口（迷你臂写临时目录 ⇒ 不欠 D82）。
> ⚠️ 顺带记一条设计债：`EPIRUS_FIREWEAK_PERSIST` 读在 **`js/core/resolve.js`——一个指纹文件**里 ⇒ 想真正接通它
>   必须动指纹（全线重测重记）⇒ 目前只能"登记为死键 + 运行期响亮拒绝"。
> 产物点名（D82）：（无新增）

## v1.5.154 — train-3p **当选面退化闸**（§N9 · qoder 09-22 午后）+ 门 D120/D121 + CLI 暗旋钮审计（§N10）

> ⚠️ **本条由 DS 代补**：qoder 的提交标题写了 `v1.5.154`，但 CHANGELOG 里没有对应条目（版本序列当时是 153→152），
> 于是 D8 读到的三方仍是 v1.5.153、**看不出版本缺口**。补记如下（内容全部来自 `docs/RESEARCH-LOG-2026-09-22-qoder-night.md` §N8/§N9/§N10）。

> **① 当选面退化闸**（`tools/pick-best.mjs` 新纯函数 `rejectDegenerateWinners` + `train-3p` 名人堂过滤）：
> 阈值与 promote 的"零攻击 ≥90% 阻断"对齐（界内 0.89 放行）；`densityProfile.zeroAtkRate`（`audit-lib` **单一来源**）；
> **全退化 ⇒ `best: null` + `meta.degenerateOnlyWinner=true` + ⛔ 响亮**（产物照写，下一道 promote 自己会砍）。
> 动因：§N8 暴露**三处不一致** —— promote 有闸、2P `pick-best` 昨夜加了 `vetoDegenerate`、**CLI(train-3p) 当选面什么都没有**
> ⇒ `xn10b` 让"纯ジ龟包"以带内最高 trainFit 当选。**真反例复验**：`xn10b` 当选者 zeroAtkRate=**100%**（会被剔）、
> 现役 `cmin4`=**0%**（不误伤）。门 **D121** 三向（合成表让位 / 阈值界 / 全退化 null + 接线两钉）。

> **② 门 D120**（`fix(train)`）：`train-3p` 接通 `EPIRUS_CLEAR_W` —— 09-22 的"臂 a"**实为 7′ 逐字节复现**
> （开关传了没人读，**第五例静默空转**）；现在 >0 会打进 evo 沙箱 + 回显生效值 + 拒绝即 `exit 5`。

> **③ §N8 的两个真发现（比臂本身值钱）**：
> - **CLEAR_W 否证**（生效后的 `xn10b`）：收割奖励在本配方下**长不出击杀**，反而把包推向**纯ジ退化**（G=0 · 零攻击 100%）
>   ⇒ "**奖励收割 ≠ 学会收割**"（与 v1.5.103 的警告同型）；**不滑第三个值、不换第四根旋钮**（第四次复发警告生效）。
> - **第六例静默空转 = 沙箱 env 黑洞**：`train-3p` 的 vm 沙箱**没有 `process`** ⇒ `js/` 里"加载时读 `process.env`"的旋钮
>   在 CLI 下一律等于默认（`PASSIVE_FIELD` / `FIREWEAK_PERSIST` 字面死读；DIV_*/CHARGE_MIN_EP/SUB_BEAD/BEAD_SEED… 靠 server 下发）
>   ⇒ **历史上用 `train-3p.mjs` 手动跑的臂一律是"默认经济"**（本条对**已记录的 CLI 臂读数**是复核义务）。
>   （注：DS 09-22 的臂 1–9′ 只用闭集内的键 —— XN2W/XN2G/XN2REF/XN2SCRIPTS/ARM/ANCHOR/HOTSTART/SEEDPACK/SEED/BAND_DIR —— ⇒ 不受影响；
>   更早用 `tools/ring2-run.mjs`（server 路径）的臂也不受影响。）

> **④ §N10 的裁定（DS）**：**采纳"黑旋钮侦测 + `exit 6`"**，并再加一道**静态门**——
> 运行期：`train-3p` 启动时把 env 里出现的 `EPIRUS_*` 与"本工具闭集 ∪ 引擎侧真正接通的项"比对，
> 出现名单内的**暗键** ⇒ 打印名单 + `exit 6`（留 `EPIRUS_ALLOW_DARK=1` 逃逸口给有意为之的情形）；
> 静态：立门钉"**被塞进 vm 沙箱的 `js/` 文件里不许字面读 `process.env`**"（要么走宿主 setter，要么显式声明为死键）⇒
> 这才是把"第五/第六例"从"下次小心"变成"下次跑不起来"的那一刀。**未开工**（属新立项，等 DS/用户排期）。
> 产物点名（D82）：`v7xn10a-band1.bak`、`v7xn10a-band2.bak`、`v7xn10a-band3.bak`、`v7xn10a-band4.bak`、`v7xn10a-band5.bak`、`v7xn10a-band6.bak`、`v7xn10b-2psh.bak`、`v7xn10b-band1.bak`、`v7xn10b-band2.bak`、`v7xn10b-band3.bak`、`v7xn10b-band4.bak`、`v7xn10b-band5.bak`、`v7xn10b-band6.bak`、`v7xn10c-2psh.bak`、`v7xn10c-31.bak`、`v7xn10c-band1.bak`、`v7xn10c-band2.bak`、`v7xn10c-band3.bak`、`v7xn10c-band4.bak`、`v7xn10c-band5.bak`、`v7xn10c-band6.bak`

## research(0922 · DS 臂 7′/8′/9′＝热启动修正后重跑跨 N · 不升版本 · 未 promote) —— **跨 N 成立** ✓✓（前两轮"两败"是冷启动 bug）

> 见 `docs/RESEARCH-LOG-2026-09-22-ds.md` §11。三臂都确认 `热启动：以现有冠军为种子` ✓（起点 = 过门 2P 种子 `v7xn6-2p`）：
> · **7′（XN2G=4 · W=1）**：2P 考卷 **96.63% / 最差 65.00% / 过门 = 是** ✓（比种子仅 -1.1pt）
>   ＋ 3P **只挂场B 清场 0.10 < 0.3** ✗（座位 10.6pt ✓ · **G 4.32 / G(long) 3.62** ✓✓ · 墙 17.15 每局 ✓ · 场A 25% ✓）
>   ⇒ **本仓第一次"两场都拿得出手"的候选**；
> · **8′（XN2G=16）**与 **9′（XN2G=16+λ=3）**：都掉进**退化包**（零攻击局 100% · G 1.00/0.00，被门抓住 ✓）
>   ⇒ **2P 排练过多 ⇒ 学会"龟住拿分"、彻底放弃 3P** ✗；
> · **9′-band1** = **97.75% / 70.00% 过门** ✓（与种子逐位相同 ⇒ 锚定确实护住 2P ✓），但当选者退化 ⇒ 锚定太强会锁死起点；
>   ⚠️ 9′-band1 的 3P 侧**未测**（时间盒），下一个接手的人补测。
> **⇒ 交接**：从 `docs/artifacts/v7xn7c-31.bak` 继续，下一臂单变量 = **场B 清场**。
> 产物点名（D82）：`v7xn7c-31-band1.bak`、`v7xn7c-31-band2.bak`、`v7xn7c-31-band3.bak`、`v7xn7c-31-band4.bak`、`v7xn7c-31-band5.bak`、`v7xn7c-31-band6.bak`、`v7xn7c-31.bak`、`v7xn8c-31-band1.bak`、`v7xn8c-31-band2.bak`、`v7xn8c-31-band3.bak`、`v7xn8c-31-band4.bak`、`v7xn8c-31-band5.bak`、`v7xn8c-31-band6.bak`、`v7xn8c-31.bak`、`v7xn9c-31-band1.bak`、`v7xn9c-31-band2.bak`、`v7xn9c-31-band3.bak`、`v7xn9c-31-band4.bak`、`v7xn9c-31-band5.bak`、`v7xn9c-31-band6.bak`、`v7xn9c-31.bak`

## v1.5.153 — **热启动静默失败**修掉（两臂白跑的根因）+ 锚定正则（"冻结/分区"立项的最小形式）

> **用户裁定**（09-22）：按建议执行 —— 2P 槽**不换**（保 c13）· `v7xn6-2p` 作可导入的第三包 · **开"冻结/分区"立项**。

> **① 修掉一个会让整臂白跑的静默失败**（`tools/train-3p.mjs` · 门 **D119**）：
> 热启动原正则只认 `window.EPIRUS_CHAMPION_3P`，而 `train-best` 产的包是 **2P 外壳** `window.EPIRUS_CHAMPION`
> ⇒ `EPIRUS_SEEDPACK=<2P 包>` 时 `try/catch` **静默不热启动** ⇒ **臂 7/臂 8（跨 N 的 W 轴与排练轴）
> 实际是冷启动跑的** ✗ —— 预注册前提没成立，两条结论**作废**（见下方 ⚠️ 更正）。
> 修法：① 兼容两种外壳；② **`EPIRUS_HOTSTART=1` 却读不出种子 ⇒ `exit 5`**（与 `EPIRUS_XN2REF` 同一条
> "拒绝静默退化"规矩）。顺带纠正：种子的真实 3P 水平是 `gen0 fit=0.897 · 1st=38%`（不是"全灭"——
> 那是我读冷启动 gen0 得出的误判 ✗）。

> **② 锚定正则**（`EPIRUS_ANCHOR=λ`，默认 0 ⇒ 行为逐字不变）：向量化进化没有梯度，"冻结"用适应度表达 ——
> `fit' = fit − λ·mean((θ−θ_seed)²)`（离种子越远，需要越高的原始分才配赢）。
> 量级标定（3 代）：漂移 RMS ≈ **0.206**（均方 ≈0.0425）⇒ λ=1 仅惩罚 fit 的 ~5%（太温和），有效档取 **λ=3**。
> 进度行新增 `距种子=`（也是本次发现静默失败的探针）。
> ⇒ 这是"冻结/分区"立项的**最小可测形式**：先看"锚定能不能在找回 3P 的同时留住 2P"，再谈架构级冻结。

> ⚠️ **更正（同批）**：`research(0922 · DS 臂 7 ...)` 与 `research(0922 · DS 臂 8 ...)` 两条里"两败/排练无效"的
> 读数来自**冷启动**臂 ⇒ 对"从过门 2P 种子出发"这个问题**不成立**；修正后的重跑见 `research(0922 · 臂 7′/8′/9′)`。
> 产物点名（D82）：`train-3p-out-band1.bak`、`train-3p-out-band2.bak`、`train-3p-out-band3.bak`、`train-3p-out-band4.bak`、`train-3p-out-band5.bak`、`train-3p-out-band6.bak`

## research(0922 · DS 臂 8＝排练臂 · 不升版本 · 未 promote) —— **排练治不了遗忘**（它是零和的）⇒ 跨 N 研究线今年的终点

> ⚠️ **本条的读数来自冷启动臂（热启动静默失败）⇒ 结论作废**；更正与重跑见 v1.5.153 与 `research(0922 · 臂 7′/8′/9′)`。

> 见 `docs/RESEARCH-LOG-2026-09-22-ds.md` §10。单变量 = 2P 排练量 `EPIRUS_XN2G` **4 → 16**（起点仍是过门的 `v7xn6-2p`）：
> `v7xn8a-31`（W=1）2P 考卷 **65.00%/0%** ✗ + 3P **G 1.88/G(long) 1.99** ✗ + **墙 0.00 每局** ✗；
> `v7xn8b-31`（W=0.25）2P **73.88%/2.5%** ✗ + 3P G 2.15/2.17 ✗ + 墙 0.30 ✗；两者代理栏电磁炮 **0.00** ✗。
> ⇒ 排练量抬 4 倍：2P 没留住、3P 广度照旧塌、**破墙反而塌得更狠**（17.95→0.00/0.30）⇒ **零和**。
> **跨 N 三条轴全部试过**（切片结构 臂 1-5 · W 臂 7 · 排练量 臂 8）⇒ **"一包吃两场"用现有机制做不到**；
> 唯一过门的产物来自**单一目标**训练器（`train-best` = 考卷即 fit ⇒ `v7xn6-2p` 97.75%/70% ✓）。
> 剩下两条路：**冻结/分区**（治遗忘的正规药，需改训练代码 ⇒ 单独立项+预注册+门）或**极大预算**（pop 32+，贵且未必成）。
> **产品线结论**：**两条产线分开、不混训** —— 2P 走 `train-best`（用 `v7xn6-2p` 当种子/候选）· 3P 继续 `v7cmin4-31`。
> 产物点名（D82）：`v7xn8a-31-band1.bak`、`v7xn8a-31-band2.bak`、`v7xn8a-31-band3.bak`、`v7xn8a-31-band4.bak`、`v7xn8a-31-band5.bak`、`v7xn8a-31-band6.bak`、`v7xn8a-31.bak`、`v7xn8b-31-band1.bak`、`v7xn8b-31-band2.bak`、`v7xn8b-31-band3.bak`、`v7xn8b-31-band4.bak`、`v7xn8b-31-band5.bak`、`v7xn8b-31-band6.bak`、`v7xn8b-31.bak`

## research(0922 · DS 臂 7＝跨 N 适应度第一问 · 不升版本 · 未 promote) —— **两败**：不是"混合失败"，是"**遗忘**"

> ⚠️ **本条的读数来自冷启动臂（热启动静默失败）⇒ 对"从过门 2P 种子出发"不成立**；更正与重跑见 v1.5.153 与 `research(0922 · 臂 7′/8′/9′)`。

> 预注册/读数见 `docs/RESEARCH-LOG-2026-09-22-ds.md` §9。两臂（W=1 / 0.25 · 100 代 · N=5 · pop8 · seed31）
> 起点 = **过 2P 考卷门**的 `v7xn6-2p`（97.75% / 最差 70% ✓），2P 切片 = 考卷 20 基准。
> **结果**：`v7xn7a-31` 2P 考卷 **63.88%/0%** ✗ + 3P **G 1.23 · G(long) 1.29 < 3** ✗；
> `v7xn7b-31` 2P **68.12%/0%** ✗ + 3P G 2.97/2.32 < 3 ✗；两者的**产品代理栏电磁炮都是 0.00/局** ✗
> ⇒ 没有任何一粒同时过两场，**而且比"单向"更糟**。
> **机制（三阶段连起来看）**：阶段 1（臂 2）练会 2P 忘掉 3P → 阶段 2（臂 6）2P 收口成功但没找回 3P →
> 阶段 3（本臂）找回部分 3P（1st 0%→45%）却把 2P 忘掉（97.75%→64~68%）⇒ **每阶段牺牲上一场**。
> **下一步（按代价）**：① **排练**而非轮替（把 2P 局数 `EPIRUS_XN2G` 从 4 提到 8~16 比调 W 更对症）；
> ② 加大预算（pop 8→32+）；③ 冻结/分区（治遗忘的正规药，本臂没有这个机制）。
> **近期产品结论**：别指望"一包吃两场"——2P 用 `train-best`（考卷=fit）、3P 用 `v7cmin4-31`，**两者不混训**。
> 顺带：`G(long) < 3` 是 v1.5.145「广度两个模式都判」**第二次在真臂上抓红** ✓。
> 产物点名（D82）：`v7xn7a-31-band1.bak`、`v7xn7a-31-band2.bak`、`v7xn7a-31-band3.bak`、`v7xn7a-31-band4.bak`、`v7xn7a-31-band5.bak`、`v7xn7a-31-band6.bak`、`v7xn7a-31.bak`、`v7xn7b-31-band1.bak`、`v7xn7b-31-band2.bak`、`v7xn7b-31-band3.bak`、`v7xn7b-31-band4.bak`、`v7xn7b-31-band5.bak`、`v7xn7b-31-band6.bak`、`v7xn7b-31.bak`

## research(0922 · DS 臂 6＝阶段 2 收口 · 不升版本 · **未 promote**（切 2P 槽待用户裁定）) —— 两阶段课程成立 ✓

> 预注册见 `docs/RESEARCH-LOG-2026-09-22-ds.md` §8。配方：把线上 2P 槽**临时**换入臂 2 的产物 `v7xn2-31`
> 当种子（`train-best` 从磁盘槽读种子），跑 `node tools/train-best.mjs 3 500`，取走产物后**按 sha256 逐字节还原槽** ✓。
> **结果**：当选候选 `v7xn6-2p`（500 代/113.8s · hill05 3.71 · 种类 4）——
> **2P 考卷 平均 97.75% / 最差基准 70.00% / 过门 = 是** ✓（种子同口径 59.38% / **0.00%** / 否 ✗ ⇒ 结构性洞被补上）；
> **与现役 2P 冠军 c13 头对头 120-0（100%）** ✓；与 c13 考卷对照 97.75/70.00 vs 99.00/82.50（对脚本略弱、对学会的对手全胜）。
> ⇒ **两阶段课程成立**：阶段 1（3P 训练器 + 2P 强参照切片 · 70 秒）撬开"能不能打 2P"，阶段 2（`train-best` · fit=考卷）补广度。
> 顺带：夜班的 **`vetoDegenerate` 退化闸当场拦下一个 500 代退化候选** ✓（种类=0）。
> **未 promote**：切 2P 槽要用户点头（c13 考卷更强、候选头对头全胜 ⇒ 典型权衡）。产物点名（D82）：`tb1-band1.bak`、`v7xn6-2p.bak`

## v1.5.152 — 体检并报「**产品代理栏**」（真桌 1+4 · ε=0.2 soft · 只记录不阻断）+ `behavior-profile` 实现单一来源化

> **用户裁定**（09-22）：把"真桌 ε=0.2"接进体检与技能报告，**只记录、不阻断**（它有判别力之前不拦人）。

> **为什么需要这一栏**（DS §7 实测）：同一包 `v7cmin4-31`、同一 **ε=0**、长程 20 局 ——
> **镜像**装配（5 席同包 · 门禁/体检判的就是它）电磁炮 **4.30/局**；**真桌**（1 冠 + 4 脚本）只有 **0.10/局**
> ⇒ **装配单独值 43 倍、ε 只值 2 倍**（0.10 → 0.20）。而"真桌 + ε=0.2"与**真机**几乎重合
> （电磁炮 0.20 对 0.20 · 蓄能 0.30 对 0.40）⇒ 它才是"玩家会看到什么"的那一栏。
> （千问 §23 的"同一包两套口径差 4 倍"是把 ε 与装配混算的结果 ✗，已更正。）

> **实现**：
> - `tools/behavior-profile.mjs`：`fieldProfile` 改为 **导出**，并加 "被 import 时不跑 main" 的守卫
>   （`RUN_AS_MAIN`）⇒ 体检可以直接借它的实现，**不必抄第二份**（本仓"同一份名单抄两遍"栽过三次）。
> - `tools/promote-champion.mjs`：可行性行之后新增一行 **产品代理栏**（真桌 1+4 · ε=0.2 soft · 长程 20 局），
>   打印 电磁炮/蓄能/防御类/集火/最大ep/局长；**不进任何判定**（`feas` 的结论逐字不变）；
>   `EPIRUS_NO_PROXY=1` 可关掉（省约 30 秒）· `EPIRUS_PROXY_GAMES` 可改局数。
> - 门 **D118** 钉住：那一栏必须打印、必须借 `behavior-profile` 的实现、必须写明"只记录不阻断"、可关掉，
>   并守住导出与 `RUN_AS_MAIN` 守卫。

> **现役 `v7cmin4-31` 实测（本版实测）**：`可行性 ✅ 五道全过（G(multi) 4.44 · G(long) 3.43 · 墙 21/局 · 场A 35%）`
> ＋ `产品代理栏：电磁炮 0.20/局 · 蓄能 0.30/局 · 防御类 6.1% · 集火 36.9% · 最大ep 3 · 局长 29.5`
> ⇒ 与**真机**（电磁炮 0.20 · 蓄能 0.40 · 防御类 **24.8%**）对照：贵卡频率**吻合**，防御差 4 倍
> ⇒ 那 4 倍是"**真人席在场改变了威胁结构**"（被打就防），不是模拟能补的。
> 产物点名（D82）：（无新增）

## v1.5.151 — 口径工具修正（DS 09-22 · 用户追问"ε=0 下防御 0% 也不是很对吧"查出）：防御类**从规则表取** + 行为剖面**多装配并报**

> **① 真机栏的防御类长期是漏数**：`tools/log-behavior.mjs` 用的是手写正则
> `/防御|反弹|八卦阵|原型制御|金钟|镜面/` —— 匹配不上真卡名「**金刚盾**」（写成"金钟"）、漏了
> 「无极变速/藤甲/全息屏障」（同为 `CAT.DEFENSE`）、还列了不存在的「镜面」。
> 改成**从 `js/core/rules.js` 取表**（`cat === CAT.DEFENSE` 的全部卡 ⇒ 单一来源；取不到就响）。
> 修后真机栏（`results/31` 五局 AI 席）：防御类 **27.20/局 = 占 AI 出手 24.8%**（原漏数报 23.0%）。

> **② 装配是比 ε 更大的口径因素**（本轮最重要的发现）：同一包 `v7cmin4-31`、同一 **ε=0**、长程 20 局：

| 装配 | ε | 防御类 | 电磁炮/局 | 蓄能/局 | 最大ep | 局长 | 决胜 |
|---|---|---|---|---|---|---|---|
| **镜像**（5 席同包 · 门禁/体检用的就是它） | 0 | 0.0% | **4.30** | 5.05 | 76 | 46.1 | 100% |
| **真桌**（1 冠 + 4 脚本） | 0 | 0.0% | **0.10** | 0.20 | 4 | 27.3 | 100% |
| 镜像 | 0.2 | 4.8% | 1.00 | 1.60 | 8 | 23.1 | 100% |
| **真桌** | 0.2 | 6.1% | **0.20** | 0.30 | 3 | 29.5 | 100% |
| **真机**（用户实机 5 局 · AI 席） | — | **24.8%** | **0.20** | 0.40 | — | 27.4 | — |

> ⇒ **装配单独值 43 倍**（4.30 → 0.10），**ε 单独只值 2 倍**（0.10 → 0.20）；千问 §23 那个"4 倍口径差"
> 把两个因素混在一起算了。而且"**真桌 + ε=0.2**"与真机几乎重合（0.20 vs 0.20 / 0.30 vs 0.40）
> ⇒ **最接近产品的模拟不是门禁那套镜像，而是真桌装配 + 浏览器 ε**。
> ⇒ 产品含义：门禁/体检里的"贵卡能力"数字（4.30/局）**是镜像局特有**的；玩家看到的是 0.2/局 那一档。

> **③ 因此撤回我 09-22 的"ε 按人数分档"提案**（如实入账）：它建立在**镜像装配**上量的"ε 代价"之上 ✗；
> 在**真桌装配**里 ε=0.2 反而**提高**电磁炮（0.10 → 0.20）⇒ 降 ε 会让产品侧的贵卡更少 ⇒ **不做**。
> 真正把真机防御推到 24.8% 的，最可能是"**真人席在场改变了威胁结构**"（被打就防），不是 ε。

> **④ 工具**：`tools/behavior-profile.mjs` 新增 `--field=self,pool`（一次并报多个装配，默认口径不变）；
> 门 **D117** 钉住以上两条（含"取不到防御类卡必须响"）。产物点名（D82）：（无新增）

## research(0922 · DS 跨 N 第 3 批：臂 5＝阶段 2 · 不升版本 · 未 promote) —— 课程制在这个预算下**不成立**

> 预注册见 `docs/RESEARCH-LOG-2026-09-22-ds.md` §5，读数见 §6。**单变量**：只把热启动点从 3P 冠军换成臂 2 的产物
> （`v7xn2-31`，已会打 2P：对现役 2P 冠军 95%），其余与臂 4 逐字相同（`EPIRUS_XN2REF=exam` · W=1 · 100 代 · pop8 · 73 秒）。
> **结果：几乎没动** ✗ —— 考卷 **59.38% / 最差基准 0%**（与热启动点**逐位相同**）、头对头 95% 不动、
> band 谱系无系统性上移（band2 56.19%）⇒ 预注册的否证分支成立：**在过拟合单参照的起点上扩广度同样不行**。
> **五臂结论**：便宜的路只治"能不能打 2P"（70 秒 → 95~100% ✓）治不了"广不广"（考卷 55~59% ✗，必有一个基准 0% 全败）；
> 把验收塞进 fit 的两条做法（直接塞／课程制）都失败；**广谱 2P 冠军的产地是 `tools/train-best.mjs`**
>（它的 fit 本身就是考卷 `evalChamp`）—— 夜班就是用它从 `tgt-44` 造出 c13（99%/82.5%）的。
> **建议的省时路线**：阶段 1 用 3P 训练器 + 2P 强参照切片把血统"撬"会打 2P（70 秒 ✓），
> 阶段 2 把这条血统当 **`train-best` 的热启动种子**收口（与造 c13 的唯一差别 = 种子更好）⇒ **等用户裁**（要算力）。
> 产物点名（D82）：`v7xn5-31-band1.bak`、`v7xn5-31-band2.bak`、`v7xn5-31-band3.bak`、`v7xn5-31-band4.bak`、`v7xn5-31-band5.bak`、`v7xn5-31-band6.bak`、`v7xn5-31.bak`

## research(0922 · DS 跨 N 第 2 批：臂 3/臂 4 · 不升版本 · 未 promote) —— "训什么对手就得到什么专家"，而**多样惩罚者给不出梯度**

> 承 v1.5.150（切片换 2P 强参照 + 带内落盘 + 门 D116）。用户「那就继续吧」⇒ 又跑两臂（各 ~70 秒，单变量）：
> · **臂 3**（`EPIRUS_XN2SCRIPTS=1`：参照 + 多人池）：2P 头对头 **0%** ✗、考卷 49.75% ✗ ⇒ 混入弱对手把信号**平均稀释**掉；
> · **臂 4**（新增 `EPIRUS_XN2REF=exam`：直接用 2P 考卷那 20 个基准当对手，单一来源 `tools/p2-baselines.mjs`）：
>   2P 头对头 **0%** ✗、考卷 40.25%（band1 53.75%）✗、3P 侧 **G(long) 2.88 < 3** ✗ ⇒ 两场都没过。
> **四臂结论**：梯度要求参照"**可被打裂**"而不是"多样" —— 3P 血统包对每个脚本基准都是 100% 惨败（局长 7.0 回合），
> 突变体也一样 ⇒ 平均值长期贴 0、选择看不到差异；而对**学出来的 2P 冠军**存在"跨门槛就开始赢"的可攀爬路径
> ⇒ 臂 2 才拿到 95~100%（但那是**特化手**，考卷 55~59% ✗）。**"把验收标准塞进 fit"不成立** ✗。
> 下一步正解形状（预注册待写）：**两阶段课程** —— 阶段 1 用单个 2P 冠军撬出 2P 能力（70 秒 ✓），
> 阶段 2 在**2P 冠军血统上**用考卷基准扩展广度（起点不再是 0 ⇒ 平均值才有区分度）；或把 pop 8 → 32+ 重试"多样"。
> 另：臂 4 的 `G(long) 2.88 < 3` 是 **v1.5.145「广度两模式都判」首次在真臂上抓红** ✓（那条新门不是摆设）。
> 全部读数见 `docs/RESEARCH-LOG-2026-09-22-ds.md` §3/§4。产物点名（D82）：`v7xn4-31-band1.bak`、`v7xn4-31-band2.bak`、`v7xn4-31-band3.bak`、`v7xn4-31-band4.bak`、`v7xn4-31-band5.bak`、`v7xn4-31-band6.bak`、`v7xn4-31.bak`

## v1.5.150 — §N6 跨 N 混适应度**修好**：2P 切片的对手从"多人池"换成 **2P 强参照**（+ 带内候选落盘）· 门 D116

> **病**（DS 09-22 实测 · `docs/RESEARCH-LOG-2026-09-22-ds.md` §2）：`EPIRUS_XN2W` 的 2P 切片原来拿**多人池**当对手，
> 而现役包对池子脚本在 2P 里 **0% 胜率** ⇒ 人人 ≈0 分 ⇒ 该切片是**常数** ⇒ 加常数不改变排序
> ⇒ 第一臂 `v7xn1-31.bak` 与热启动**逐字节相同**（空枪：白跑 70 秒，什么都没产出）。

> **改法**（`tools/train-3p.mjs` · 默认关的行为逐字不变 · 门 **D116** 行为式钉住）：
> ① 切片对手 = `XN2_OPPS`：`EPIRUS_XN2REF`（逗号分隔包路径，**默认 = 现役 2P 冠军** `js/bundled-champion.js`）；
>    `EPIRUS_XN2SCRIPTS=1` 可并回多人池（默认不并 —— 实测混入弱对手会把梯度稀释掉，见臂 3）；
>    **参照包读不出 ⇒ `process.exit(2)`**（拒绝静默退化：无梯度的切片等于白跑一整臂，正是本次踩的坑）；
> ② **带内候选落盘**（移植自 `train-best` 的 band-save）：hall 里每粒写 `<ARM>-band<k>.bak`，meta 自证
>    `xn2w/xn2g/xn2refs`；目录可用 `EPIRUS_BAND_DIR` 指向临时目录（门因此能行为式测它、又不欠 D82 的账）；
> ③ banner 打出 `2P对手=ref:…`（非空枪自检就靠这一行）。

> **结果（臂 2 · 单变量只换对手 · 70 秒 · W=1 · 100 代 · pop8 · seed31）**：现役 3P 包 2P H2H **0%** →
> `v7xn2-31` **95%**、`band1` **100%**（band2/3/4 = 95/63/87%）⇒ 预注册判据（≥20%）**5/5 命中**，梯度回来了。
> **但**"头对头赢冠军"≠"好的 2P 冠军" ✗：它们的 **2P 考卷只有 55.5~59.4%、最差基准 0%** ⇒ 考不过 2P 门，
> 是**专克现役冠军的特化手**。臂 3（`EPIRUS_XN2SCRIPTS=1`，参照+多人池）反而 H2H **0%**、考卷 49.75% ✗
> ⇒ **"切片用哪个对手集，就训出那个对手集的专家"**；下一臂的单变量 = 对着**考卷自己那 20 个基准**打 + W 下调。
> 产物点名（D82）：`v7xn2-31-band1.bak`、`v7xn2-31-band2.bak`、`v7xn2-31-band3.bak`、`v7xn2-31-band4.bak`、`v7xn2-31-band5.bak`、`v7xn2-31-band6.bak`、`v7xn2-31.bak`、`v7xn3-31-band1.bak`、`v7xn3-31-band2.bak`、`v7xn3-31-band3.bak`、`v7xn3-31-band4.bak`、`v7xn3-31-band5.bak`、`v7xn3-31-band6.bak`、`v7xn3-31.bak`

## research(0922 · DS 跨 N 第一臂 · 不升版本 · 未 promote) —— `EPIRUS_XN2W` 的 2P 切片**没有梯度**（臂为空枪）

> 用户 09-22「试一下训练」：跑了千问 §N6 的跨 N 混适应度第一臂（`v7cmin4-31` 热启动 + `EPIRUS_XN2W=1` ·
> 100 代 · N=5 · pop8 · seed31 · 70.7 秒）。主判据（2P 迁移 ≥20%）读数 **0/120**，但复核发现
> **产物与现役逐字节相同**（5693 个数值项 0 处不同）⇒ 这一臂是**空枪**：`train-3p` 在提升不足时保留现有冠军，
> 而 N6 的 2P 切片是对**多人池**打的 —— 实测现役包对 `pickBalanced`/`pickGunSpam`/`pickAggro`
> 在 2P 里**全是 0% 胜率** ⇒ 人人 ≈0 分 ⇒ 该切片是**常数** ⇒ 加常数不改变排序 ⇒ 选择完全由 3P 侧驱动。
> 结论与两条修法（切片换 2P 强参照 / 带内候选落盘）见 `docs/RESEARCH-LOG-2026-09-22-ds.md` §2。
> 新增可复用探针 `tools/probe-2p-h2h.mjs`（参照组复现夜班 0-120 的同型读数：0-60、局长 7.0）。
> 产物点名（D82）：`v7xn1-31.bak`（= `train-3p-out.js`，与现役逐字节相同）。

## research(0922 · 夜产物**全名**点名 · DS 代记 · 不升版本) —— D82 只看**完整文件名**，夜班账里写的是短名

> 夜班账（本文件下方 `### 夜班账`）用的是短名（`v7xfer44c10-2p`），而 **D82 按 `*.bak` 完整文件名匹配** ⇒ 会被判"查无字"。
> 本条只补**全名**（D82 的唯一要求），不改夜班账、不改判据。

- `v7xfer44c10-band1.bak`
- `v7xfer44c11-band1.bak`
- `v7xfer44c11b-2p.bak`
- `v7xfer44c11b-band1.bak`
- `v7xfer44c11b-band2.bak`
- `v7xfer44c12-band1.bak`
- `v7xfer44c12-band2.bak`
- `v7xfer44c12-band3.bak`
- `v7xfer44c13-band1.bak`
- `v7xfer44c13-band2.bak`
- `v7xfer44c13-band3.bak`
- `v7xfer44c6-band1.bak`
- `v7xfer44c7-band1.bak`
- `v7xfer44c7-band2.bak`
- `v7xfer44c7-band3.bak`
- `v7xfer44c8-band1.bak`
- `v7xfer44c9-3psh.bak`
- `v7xfer44c9-band1.bak`
- `v7xfer44c9-band2.bak`
- `v7xfer44d6w-band1.bak`
- `v7xn-smoke.bak`

### 夜班账（分支 qoder-explore-0921night · 不占版本号 · 随做随记 · 明细见 docs/RESEARCH-LOG-2026-09-22-qoder-night.md）

- N1 报告件（3P 壳只读复制 · 不 promote）：v7xfer44-3psh.bak、v7xfer44b-3psh.bak ⇒ 结论：tgt-44 家族 5P 全不过门
  （tgt-44 墙 0.00/场B 0.00/G5 防席 58%；xfer44 只挂场B 0.07 且 G4/G5 全过；xfer44b 挂场A 19% + G4 只枪 81%）。
  回答用户之问："2P 调得好 ≠ 5P 能打"——在现有门体系下成立，家族无 5P 合格品。
- N2 产物（预登记，band-save 新机制首跑）：v7xfer44c6-2p.bak、v7xfer44c7-2p.bak、v7xfer44c8-2p.bak、v7xfer44c9-2p.bak、
  v7xfer44c10-2p.bak、v7xfer44c11-2p.bak、v7xfer44c12-2p.bak、v7xfer44c13-2p.bak、v7xfer44d6w-2p.bak
  与各臂全部 *-band*.bak ⇒ 读数与判据结案见夜日志 §N2/§N3。
- N2/N3 实产物点名：v7xfer44c10-2p、v7xfer44c10-band1、v7xfer44c11-2p、v7xfer44c11-band1、v7xfer44c12-2p、v7xfer44c12-band1、v7xfer44c12-band2、v7xfer44c12-band3、v7xfer44c13-2p、v7xfer44c13-band1、v7xfer44c13-band2、v7xfer44c13-band3、v7xfer44c6-2p、v7xfer44c6-band1、v7xfer44c7-2p、v7xfer44c7-band1、v7xfer44c7-band2、v7xfer44c7-band3、v7xfer44c8-2p、v7xfer44c8-band1、v7xfer44c9-2p、v7xfer44c9-band1、v7xfer44c9-band2、v7xfer44d6w-2p、v7xfer44d6w-band1。（band-save 首跑全留档）
- 夜产物点名（含 B 段/N6）：v7xfer44c11b-2p、v7xfer44c11b-band1、v7xfer44c11b-band2、v7xfer44c9-3psh、v7xn-smoke；旧清单：v7xfer44-2p、v7xfer44-3psh、v7xfer44b-2p、v7xfer44b-3psh、v7xfer44b-band1、v7xfer44c10-2p、v7xfer44c10-band1、v7xfer44c11-2p、v7xfer44c11-band1、v7xfer44c11b-2p、v7xfer44c11b-band1、v7xfer44c11b-band2、v7xfer44c12-2p、v7xfer44c12-band1、v7xfer44c12-band2、v7xfer44c12-band3、v7xfer44c13-2p、v7xfer44c13-band1、v7xfer44c13-band2、v7xfer44c13-band3、v7xfer44c6-2p、v7xfer44c6-band1、v7xfer44c7-2p、v7xfer44c7-band1、v7xfer44c7-band2、v7xfer44c7-band3、v7xfer44c8-2p、v7xfer44c8-band1、v7xfer44c9-2p、v7xfer44c9-band1、v7xfer44c9-band2、v7xfer44d6w-2p、v7xfer44d6w-band1。
- 夜班修（spec 定值化）：D95 去 spawn 化当晚就抓到潜伏 1/8 抖动的场景用例 **R23c**（裸 Math.random 撞上爆头判定，
  连跑五次红一次——也是"下午那次 158/159"最可信的事后解释）。修法：11 处场景用例 + game() 默认 rng 改判定恒败
  常量 noJudge()（要判定胜的本就显式喂 seqRng；Fuzz 自带 mulberry 不受影响）⇒ spec 连跑 6 次全 52/52；
  D95 加防回归钉"spec 不许再出现裸 Math.random"。

## v1.5.149-night（分支 qoder-explore-0921night · **未出版本、未动槽** · 序列窗锁 + 退化闸 + hill 择优 + D95 去 spawn + spec 定值化 + N6 开关）

> **序列窗锁（用户 09-22 晨 GO）**：`policyChooserN` 的 soft 分支里，**链上状态整回合作废探索** ——
> 判据 `seqLockedTurn(state,pid)` 挂**状态**不挂"贪心采样结果"（D115 首版实测：temp0.15 的贪心本身随机，
> 采样后判锁在 RAILGUN 贪心率<1 的持珠态上 40 抽样只锁住 11 ⇒ 泄漏），条件 = 持电珠 ∨ 持爆珠 ∨ 上一手=蓄能 ∨ 身上有自己贴的存活符咒。
> 只作用于 `epsMode==='soft'` ⇒ 训练/评测/门禁读数逐字不变。**D111 的 ②③ 随之升级**："收尾卡并入探索集"
> 被更强语义覆盖（链上根本不改判）——白班"抽得到"与夜班"不撒手"两条同题裁定撞车，取夜裁（数据背书），
> 断言改写并留字（用户可推翻）。**D115** 三向守：判据 3正3负 / 真链上态 ε=1 soft 与 ε=0 逐手相等（4/4 态 × 40 抽样）/
> 反证无 epsMode 的 ε=1 必打断（防假绿）。
> **悬崖复测（self/long · 20 局/点 · 锁后）**：电磁炮 ε=0.1 1.10（锁前 0.90）、ε=0.15 1.10、ε=0.2 1.00（锁前 0.95）；
> 镜像破局 98-100% 保住；昏手 4.8-7.6% 与锁前同级；蓄能 1.60-2.05/局。**如实记两条边界**：
> ① 4.30(ε=0)→1.0-1.1 的**残差主要是"储蓄税"**：探索在解锁的起点手把 ep 池花掉（最大 ep 76→8）⇒ 蓄能机会消失——
> **起点锁（ep≥3 即锁）没上**：它会把中期噪声几乎清零、镜像破局有归零风险，属产品口味问题，留给 NEXT-2 预算制一并裁；
> ② 对照线校准：用户 09-21 晚裁过"**每局 0.35 个电磁炮我觉得足够了**"⇒ 浏览器口径现在 1.0/局已 3 倍过线，
> 锁的功劳真实但不戏剧；戏剧的部分（4.30）仍只活在 ε=0 的门禁读数里——这正是 §23 三栏要分开报的原因。
> 其余分支内容（D104⑦⑧⑨⑩/hill05 择优/vetoDegenerate/band-save/D95 去 spawn/spec noJudge 定值化/D114/EPIRUS_XN2W）
> 逐条见本版上方的"夜班账"与夜日志。**合 main 与转正版本号等用户 GO。**

## v1.5.147 — 择优的"能量项"换掉：**divNorm(归一 Shannon) → hill05(Rényi-0.5 有效技能数) → distinct → divNorm** · 带内候选全落盘（xfer44 反例 · 用户批准 + gemini 建议）

> **动因（0921 实测反例）**：xfer44 臂（tgt-44 热启动进 2P 专训器）三候选 avg 98~99% 全进容差带，
> 旧序按 divNorm 独裁：**0.194 vs 0.189** —— 0.005 的 60 局采样熵噪声把"种类=2 的两件套打花"选成了冠军，
> 杀掉"种类=5"的候选（且 train-best 只写当选者，候选 3 连复盘机会都没有）。
> 病根与 D104 当年同族：**择优必须择决策者口中的量**——要的是广度（用出几张卡），
> 归一 Shannon 却把"两卡摇匀"记成与"五卡有主次"同价（用户点名：这游戏的技能分布本就不该均匀）。
>
> **实现**：
> - `evo.js coverageEntropy/champEntropy` 增 **`hill05 = (Σ√p)²`** 与 **`effSkills = exp(H)`** 两列
>   （Hill 数家族 D_q=(Σp^q)^{1/(1-q)}；q=0.5 长尾按 √p 计权；effSkills 与门禁 `mirrorHealth.effSkills` 同量纲
>   ⇒ **择优第一次和上线判据讲同一种语言**）。打印里那处 `Math.exp(divNorm·log28)` 的倒推近似一并换成直读。
> - `tools/pick-best.mjs`：带内排序键 **hill05 → distinct → divNorm**；旧对象无新列 ⇒ `||0` 兜底退回老序（历史可比）。
> - `tools/train-best.mjs`：**带内全候选**写 `docs/artifacts/<EPIRUS_ARM>-band<k>.bak`（meta 标 `selected`），
>   修"落选者无碑"。
> - 门 **D104** 扩三条用例：⑦复刻 xfer44 反例（divNorm 高而 hill05 低的窄包**不许**赢）、⑧无新列退回 distinct、
>   ⑨band-save 写盘点存在性。
>
> **实测**：`node --check` 三文件 OK · spec **52/52** · np-test **160/160（D104⑦⑧⑨ ✔）** · 指纹 `be6c2195` 未变（evo/tools 不在五件套）。
> ⚠️ 如实记两条边界：① hill05 在 60 局样本下对"偶发一手稀有卡"比 exp(H) 更敏感 —— 这是**有意**的
>   （广度的定义就含"真出手"），但意味着带内并列时它可能抬举抽奖型包；G4/五道门仍在终点拦着，先不动样本量。
>   ② xfer44 首跑产物 `v7xfer44-2p.bak`（种类=2 的当选者）与未保存的候选 3 属**旧口径**产物。
>   **复跑 xfer44b（seed 5）实测定档**（产物按 D82 点名：`v7xfer44b-2p.bak` 当选者 / `v7xfer44b-band1.bak` 带内唯一粒=当选者）：
>   三候选全部收敛到 2 卡最优（种类=2、hill05=2.00），带=1、无广员可择 ⇒
>   新排序没有凭空造出广度——**它只在带内真有广员时才有牙**（seed 4 那次的候选 3 现在会活到 `band<k>.bak` 里）。
>   两臂当选者同为一家族：H2H 对现 2P 冠军 **120-0**、四脚本席（含 combocounter）**100%**、5P G(multi)≈4.1 不烂。
>   ⇒ "5P 热启动 + 2P 域内微调"可复现；"微调长出广度"未发生，广度出路仍是带内保广员（换 seed 重掷/调种群）。

## v1.5.146 — 三件零规则风险小修（qoder 09-21 审计提出 · 用户批准）：**D95 去 spawn 化** · **D113 chooser 免疫"直喂原始包"** · **README 卡数口径 28→27**

> **① D95 去 spawn 化**（`tools/np-test.mjs`）。病：D95 的"spec 必须全绿"半边原先 `spawnSync` 跑
> `tools/spec-run.mjs`，而 Windows 上 spawn 偶发**空 stdout**（RESEARCH-LOG §22 NEXT 自报过这条；
> 09-21 审计复现：与另一条命令并发时五次里红一次 158/159，干净复跑四次 159/159）。
> 旧实现的"重试两次 + 退回源码用例计数"只压低假红概率、还给门塞了第二口径。
> 改法：spec 套件**进程内 vm 沙箱直跑**（与 spec-run 同五文件、同 document stub、同 innerHTML 读法），
> 没有子进程就没有抖动 ⇒ 读不到结果从"降级记录"升级为**真红**（加载抛异常 / 没全绿 / 条数 <52 三种都响）。
> `tools/spec-run.mjs` 本体保留（CI/命令行仍用）。
>
> **② D113 + `normChampParams` 守卫**（`js/train/evo.js`）。病（审计写对战台**实测踩中**）：把
> `window.EPIRUS_CHAMPION[_3P]` 的原始外壳不经 `EpirusPolicy.unpack()` 直接喂 `pickChampion`/`policyChooserN` ⇒
> **不抛任何异常**、`forwardCands` 对 25 个候选输出逐字相等的均匀分布（实测全 0.002）⇒ 工具会静默得出
> "冠军只出ジ"这类**假读数**（同包 unpack 后复跑 balanced **50/50 胜**、ep5 富态 argmax=狙击）。
> 修法（守卫只放 `evo.js`，**不碰指纹文件 policy.js**）：`policyChooserN` 入口 ① Float64Array ⇒ 原路
> （`instanceof` 跨 vm realm 会误伤既有路径 ⇒ 用"数值 length + 首元素是数"的鸭子判据）；
> ② 带 `a` 数组的原始壳 ⇒ **自动 unpack**（行为修复，不是口径变更）；③ 其它输入 ⇒ **显式抛错**；
> ④ `null` 保持既有"LEGACY 兜底"活路。门 **D113** 行为式守全四臂：真 3P 包直喂 vs unpack **逐 seed 同手**、
> W 分布必须**出尖**（峰相对均匀基线 ≥4 倍：两级 softmax 把 joint 峰的上界压到恰好 0.5，
>    首版写 `>0.5` 把**正确实现**判红了 0.5000 —— 本仓"阈值必须留口径余量"又添一例）、`{}` 与维度错的假包必抛、`null` 仍可建 chooser。
>
> **③ README 卡数口径**：`28 个可用技能`→ **27**（卡表共 31 张 − 多人专用 3 张：双枪/镜面/全息；
> v1.5.7 全息移出 2P 后 README 一直没跟上，`docs/RULES-2P.md:215-219` 是对的）。
> 顺带把"已知限制"里"均匀用 28 张"的满值参考改为"27~30（按模式）"、隐藏技注明"不是可点的卡，是结算触发"。
> 并按"README 只留当前版本"的既定口径**把头部历史版本块压回数行**（v1.5.145~134 的细节在 CHANGELOG 全有底 · 用户指令）。
>
> **实测（本版落盘前）**：`node --check` evo/np-test OK · spec **52/52** · np-test **160/160（D95 新口径 + D113 ✔）** ·
> smoke **SMOKE OK** · battle-test **BATTLE OK**（均无 JS 错误）·
> `rulesFingerprint` = **be6c2195 未变**（不碰五件套 ⇒ 两槽成绩逐字沿用、无需重录）。
> ⚠️ 另如实记：把 np-test 放到满 CPU 负载下连跑 4 次，**未能复现**审计当天那次 158/159 ⇒ 肇事门仍未指认；
> D95 去 spawn 化消掉的是其中最可疑的一族（空 stdout），其余 spawn 门（gate-drafts / econ-env 自检 / D106 probe）保持原样。
> ⚠️ 如实记边界：守卫放在 chooser 入口 ⇒ **绕过它直接调 `P.forwardCands` 仍可静默均匀**（policy.js 是指纹文件，
> 那条线要动就得单独立项 + 全链重记 —— 本版按"小修不碰指纹"执行）。

## v1.5.145 — 门禁口径（用户裁定）：技能广度 **两个模式都判** + 最大单卡占比**明确排除ジ并打出卡名**

> **动因（用户追问）**："这个包在不探索的时候技能广度非常差，是怎么通过门禁上线的？"
> 查证：阻断项 `G` 只取 `selfPlay(..., 'multi', G)`（`tools/promote-champion.mjs:92`），而同一份体检打印的
> "技能广度 S"取 `breadthProfile(..., 'long', 20)`（`:218`）⇒ **同一屏两个模式**：门判 multi（现役 4.44 ⇒ 过）、
> 产品常用 long（现役 `breadthProfile` G_eff **2.79 < 3**）⇒ 违反本仓"打印机必须打印门所判的那个量"（METHODOLOGY 44）。
> 用户裁定两条：① "**两个模式都判**"；② "**最大单卡占比要排除 ji**"。
>
> **实现**（不动任何阈值，只把判据覆盖到第二个模式）：
> - `tools/promote-champion.mjs`：新增 `spL = selfPlay(W, params, 'long', G)`，与 multi 一起传进
>   `feasibilityOf({ G: sp, G2: spL, G2name: 'long' })`；可行性行同时打 `G(multi)` 与 `G(long)`。
> - `tools/audit-lib.mjs`：`feasibilityOf` 增 `o.G2`（**同一条线 3**；不给 ⇒ 与旧行为逐字一致 ⇒ 历史调用点/单测不动），
>   返回值带 `G2/G2name`。`breadthProfile` 的 `maxCardShare` **本来就**排除ジ（计数条件 `key !== R.SK.JI`），
>   现补返回 `maxCardKey` 并打印"最大单卡占比（**非ジ**）X%（卡名）"—— 读者可自证不含ジ。
> - 门 **D112** 守住以上全部（两模式都要有自对局 G 且都进 `feasibilityOf`、计数排除ジ、打出卡名、打印写明"两个模式都判"）。
>
> **实测（现役 `v7cmin4-31` 体检）**：`node --check` OK · np-test **159/159（D112 ✔）** ·
> `可行性：✅ 五道全过（座位 12.5pt/ok · **G(multi) 4.44 · G(long) 3.43** · 墙 21/局 · 场A 35% · 场B 0.33/局）`
> ⇒ **两个模式都过**（现役包在这条新门下站得住）。⚠️ 但**同一个 long 模式两个量具分歧**：
> `selfPlay` 3.43（门用的）vs `breadthProfile` **2.79**（打印的）⇒ 恰好骑在门线上；打印里已显式告警，
> **是否改成"取更严的那个"留待用户裁定**（DS 不自行改判据）。
>
> ⚠️ **训练侧口径未动**：fit/形状项仍在 `multi` 上优化 ⇒ 新门与训练目标不是同一个模式
> （已列进 `docs/HANDOFF-FOR-QWEN-2026-09-21.md`：要么把训练口径也移到 long，要么这条线会成为"死线"）。

## v1.5.144 — **换包：`v7cmin4-31` 上 3P 槽**（本仓第一个"蓄了会花珠"的冠军：长程电磁炮 **4.30 次/局**、反弹墙主动伤害 **21 点/局**、考卷 54.4%、五门+G4/G5 自然全过零 `--force`）

> **用户 09-21 晚裁定**："每局 0.35 个电磁炮我觉得足够了" ⇒ 判据从"≥0.5 且 ≥2/3 seed"下调为**行为可见即可**；
> 又指示"继续训练，看看有没有其他基础都通过、电磁炮超过 0.34/局的冠军" ⇒ 起 **`v7cmin4`**
> （= `v7cmin3-82` 热启动接力 + 配方逐字：`CHARGE_MIN_EP=3` · `BEAD_SEED=0.5` · `SUB_BEAD=1` · `IMIT_SUB_ONLY=1` ·
> plan 含 `pickBeadBurst:0.33:railgun` · `DIV_W=0.3` · `S4_W=0.3/GAMES=6` · `CLEAR_W=0.1` · `TRAIN_SEAT_GAMES=24` · 22 席池）。
>
> **四格对照**（长程 5 席自对局 20 局/格 · ε=0；`tools/behavior-profile.mjs` + `audit-lib.reflectWall`）：
>
> | 包 | 蓄能/局 | 持珠·买得起 | **电磁炮/局** | **反弹墙主动伤害/局** |
> |---|---|---|---|---|
> | `v7cmin3-82`（上一候选，被墙挡下未上） | 1.10 | 21 / 21 | 0.35 | **0.00** ✗ |
> | **`v7cmin4-31`（现役）** | 5.05 | 96 / **95** | **4.30** | **21.0** ✓ |
> | `v7cmin4-82` | 71.80 | 1414 / 65 | 1.70 | 4.3 |
> | `v7cmin4-93` | 0.00 | 0 / 0 | 0.00 | 0.00 |
>
> ⇒ **珠线第一次同时满足"练得出"与"打得穿龟反"**：上一粒卡在破墙（ep 全押珠），这一粒两格都过 ⇒
> 也再次证明**"彩票表"不是"练不出"**：`cmin3` 那批三档 × 三 seed 只有 2/12 格非零，而热启动接力一批就有 2/3 达标。
> **上线判据**：考卷 54.4%（只作诊断）· 座位 12.5pt · G 4.44 · 场A 35% · 场B 清场 0.33 · 墙 21/局 ⇒ **五道全过**；
> `GATE4_GAMES=120` 的 G4 两模式 + G5 两模式 **候选自己全过**，**自然上线、没有 `--force`**。
> 技能报告已重生成（`docs/skill-report.html`）。缓存戳 `c5108da → mua…` 由 promote 自动重打。
> ⚠️ 如实记两条：① 这粒的**靶向率 11.6% 低于现役**（G6 只记录不阻断，"能力未长出"那一族）；
> ② 激光剑/坦克在自对局里零命中（不阻断项，反弹墙里也不出，说明它破墙走的是**狙击/珠**那条线）。
> 产物（`.bak`，gitignored，按 D82 点名）：`v7cmin4-31.bak`（**已上槽**）`v7cmin4-82.bak`（蓄能刷屏 71.8/局，未上）`v7cmin4-93.bak`（0/0/0，未上）。
> 2P 槽未动（本版没碰指纹文件，`rulesFingerprint` 仍是 `be6c2195` ⇒ 2P 考卷 0.9831 依然有效）。
> **提醒**：老用户 localStorage 里存着旧冠军，要试新包得在页面点「**用内置冠军**」。

## v1.5.142 — 前台探索加三条**状态规则**：贴贴/蓄能要 ep≥3、持珠并入电磁炮、有符咒并入天火（用户裁定）

> **动因（用户实机 + DS 归因）**：用户报"贴贴很频繁却不引爆、攒不到 ep、到不了电磁炮"（`results/82` 三局：贴贴 1/1/3、
> **天火 0/0/0**、电磁炮 0）。DS 只读归因（`docs/RESEARCH-LOG-2026-09-21-ds.md` §10）：
> · **贴贴 100% 来自 ε 探索**：同包同装配（long 5 血自对局 20 局/格）`ε=0` ⇒ 贴贴 **0.00/局**，前台 `ε.2 soft` ⇒ **2.70/局**；
> · **天火在两档都是 0.00/局** ⇒ 探索**抽不到收尾卡** —— v1.5.139 的键级探索只在"网络打分 top-K 键"里均匀挑，
>   而天火从没被训练过、分低到进不了 top-K ⇒ **探索只能付铺垫成本、结构性地拿不到收益**；
> · 顺带解释了"攒不到 ep"：观测最大 ep 从 **72 → 7**（线上）/ **6 → 4**（`v7bead3-82`）。
>
> **本版实现用户三条裁定（全部只作用于 `epsMode==='soft'`，即浏览器运行时口径）**：
> ① **铺垫卡 ep 门槛**：`贴贴(CURSE)` / `蓄能(CHARGE)` 在 **ep<3** 时**不许进探索集**（低 ep 花掉必然过期或白贴）；
> ② **蓄能之后**：手上持电珠（`elec/boom`）或上一手是蓄能 ⇒ 把 **电磁炮 / 激光眼** 并入探索集（珠子才有出口）；
> ③ **贴贴之后**：自己还有存活符咒（`state.p[*].stickers` 里 `owner===pid`）或上一手是贴贴 ⇒ 把 **继续贴 / 天火** 并入
>   （用户口径："**性价比最高的是贴三张然后引爆两次**" —— 符咒引爆后**仍存**，一次三张、窗口内可再引爆）。
> `epsMode` 缺省与 `eps=0`（训练/评测/门禁）的读数**逐字不变**。
>
> **门 D111**（行为式，`tools/np-test.mjs`）：`ε=1 + soft` 下 ① ep=1 时 200 次抽样贴贴/蓄能各 **0** 次；
> ② 持电珠时电磁炮 **>0** 次；③ 有存活符咒时天火 **>0** 次。另断言规则被 `epsMode==='soft'` 守住。
> 实测：spec 52/52 · np-test **158/158** · smoke SMOKE OK · battle BATTLE OK · 指纹 `be6c2195` 未变（本版不碰引擎与权重）。
>
> ⚠️ 边界如实记：这条修的是**前台噪声的形状**（探索不再只付成本）；**贪心策略本身仍完全不使用贴贴/天火**
> （ε=0 时两者皆 0.00/局）⇒ "要不要让策略自己长成一条贴贴线"仍是**训练侧**问题（已交千问）。

## v1.5.141 — 界面新增「**导入冠军包**」：吃得下训练产物 `.bak`（按外壳名自动落 2P 槽 / 多人槽，困难档即时生效）

> **动因（用户要求）**："你的 82 上线了我才能实机试，或者你可以给界面加一个选冠军包的功能。"
> 页面原本的「导入」只吃**自己导出的 JSON**（`Champ.store.importJSON`），而训练产物是
> `window.EPIRUS_CHAMPION[_3P] = {…};` 外壳 ⇒ **候选包没法直接拿去实机试**。本版把这一步补上。
>
> - 新增 **`js/champion-pack.js`**（纯函数、不碰 DOM）：`extract(text)` 吃三种输入 ——
>   ① 页面导出的 JSON ② 产物 `.bak`/`.js`（含外壳，**槽位由外壳名判定**：`_3P` ⇒ 多人、否则 2P）
>   ③ 兜底（从第一个 `{` 切到最后一个 `}`，容忍前置注释）。返回 `{ok, pack, slot, source}` 或 `{ok:false, reason}`；
>   **兼容性校验仍归 `P.checkPack`**（本模块只管"取出来"）。
> - `index.html`：脚本引入 + 文件选择器 `accept` 加 `.bak,.js,.txt` + 按钮改名「导入冠军包」（带用法 tooltip）。
> - `js/ui/ui.js` 的 `importChamp`：先剥壳 + `P.checkPack` 校验，再按槽位分流 ——
>   **多人槽**写 `window.EPIRUS_CHAMPION_3P` + `localStorage['epirus.champion3p']` + `resetMultiChampCache()`
>   ⇒ 对局「困难」档**即时生效**；**2P 槽**维持原路径（只是喂剥壳后的 JSON）。还原用页面上已有的「用内置冠军」。
> - 门 **D110**（`tools/np-test.mjs`）：**真跑一遍**解析器 —— 拿仓里 tracked 的真 bundle 当输入，
>   断言"外壳形态与纯 JSON 形态**逐字节相等**"（保真）+ 垃圾必拒 + 2P 外壳（含前置注释）判成 2p +
>   `index.html` 真的加载了这个模块（否则页面里 undefined ⇒ 静默失效）。
> - 自证（落盘前手工跑过）：`.bak` ⇒ `slot=3p/source=bundle` ✓；`JSON.stringify(pack)` ⇒ 与外壳形态逐字节相等 ✓；
>   `'这不是包'` ⇒ `{ok:false, reason:'parse-error'}` ✓；带注释的 2P 外壳 ⇒ `slot='2p'` ✓。
> - 指纹不变（**`be6c2195`**，本版没动引擎与两槽权重）；两槽成绩沿用。
>
> ⚠️ 与产品无关的一点如实记：本版是**界面/工具**改动（v1.0.0 的 2 人玩法冻结不受影响，只是多了一个导入入口）。

## research(0921 · DS 班 · 不升版本 · 全部未 promote) —— "教师示范为什么失败"：**不是学生学不会，是那件事从来没被示范过**

> 承接千问 §5/#21 结案时移交的"特征面设计题"。完整过程/预注册/读数：`docs/RESEARCH-LOG-2026-09-21-ds.md`。
> 代码侧：新增 **`EPIRUS_SUB_BEAD`**（补贴局连"珠"一起补，默认关）+ np-test 接线断言 7 条。
> 产物（`.bak`，均 gitignored，此处按 D82 点名，**未 promote**）：
> - 臂 `v7bead1`（唯一变量：三段计划 `pickBreakDef:0.33` → `pickBeadBurst:0.33`）：`v7bead1-31.bak` `v7bead1-82.bak` `v7bead1-93.bak`
> - 臂 `v7bead2`（+ `EPIRUS_SUB_BEAD=1` + `IMIT_SUB_ONLY=1` + `pickBeadBurst:0.33:railgun`）：`v7bead2-31.bak` `v7bead2-82.bak` `v7bead2-93.bak`
>
> 三条硬读数（DS 实测，口径见日志）：
> ① **教师取证**：`pickHeavyFire`（千问 `v7t2` 用的那段"贵卡教师"）**贵卡出手 0 次、观测最大 ep=2**
>    ⇒ 它**结构上买不起** cost≥3 的卡 ⇒ 那一臂的"贵卡示范段"**是空的**（= 本仓 v1.5.37 老教训第三次复发）；
>    `pickDeepSaver` 示范的几乎只有 `bigT`（= 实测**亏本**的那张，纯攒大雷 15% < 基线 18%）；
>    只有 `pickBeadBurst` 真在走**有价值**的链（`charge:69 railgun:67`，贵卡出手占 19.9%）。
> ② **价值表**（1 席脚本 vs 4 席现役冠军 · n=60）：基线 18% · 只枪 **7%**（旧红门确实关了）· 纯攒大雷 15% ·
>    `pickBeadBurst` **30%（全场最高）** —— 而现役冠军最凶的 G4 格正是「珠爆发」30%（同一件事）。
> ③ **两臂都否证**（花珠率 **0.0%**）：`v7bead1` 三粒 0.0%（93 粒：蓄能 5 次/得珠 5/**全过期/放炮 0**）；
>    `v7bead2`（把"放炮"做成补贴局里随时可负担、非空枪自检 `subBead 生效 = true` 已过）**三粒仍 0.0%**
>    （31：蓄能 0.03/得珠 1/放炮 0；82/93：全 0），且 31 挂 `G4-multi 只枪 62%`、82 挂 `场B 0.17` ⇒ 干预**无效且略有害**。
> ④ **诊断（三层）**：(i) 千问两条否证的证据链**不成立**（`v7t2` 的教师实测贵卡出手 0 次、最大 ep=2 ⇒ 空示范；
>    `v7t1` 的标准教师示范的几乎只有**亏本**的大雷）；(ii) 我补的两条（教师对 + 目标可负担）**也否证** ⇒
>    示范通道/可观测性/可负担性**都不是瓶颈**；(iii) 剩下的解释 = **珠经济与"名次主梯度"直接冲突**：
>    补贴局**确实进 fit**（`evo.js:1187-1194` 只有承诺局不进），"花珠"**本来就有专门奖励**（`BEAD_W=0.05`，
>    `evo.js:1156/2015`）却仍是 0.0%，而 `gFit` 主项是 `base`（名次，0~1 量级）与 `imitB·imit`
>    ⇒ 珠/贵卡那些 `0.0x` 项**只是微扰**。⇒ 珠**在特征里**（`policy.js:203` `me.elec/me.boom`、
>    `:240` **"电磁炮前摇"** `o.elec>0 && o.ep>=2`、动作侧 `cost`/`energyNeeds`）⇒ 千问"形状/特征面缺连接"的
>    定位**方向不对**，真命题是"**这条线在当前适应度算术下不划算/不被保留**"。
> ⑤ 下一步（未跑）：(a) `BEAD_W` 0.05 → 0.5 的**标度臂**（配 `SUB_BEAD`）——抬 10 倍仍 0% ⇒ 是结构不是钱；
>    (b) 若 (a) 否证 ⇒ 承认珠经济是"**针对某条脚本的窄反制**"（我的价值表也支持：只在 G4 那种
>    "1 席脚本 vs 4 席家族"的形状下 +12pt），**考卷里有它就够了**，不必逼冠军自己长出来。
> ⑥ **标度臂 `v7bead3` 已跑，结果是正面的 —— ⑤(b) 那条推论作废**（`.bak` 按 D82 点名）：
>    `v7bead3-31.bak` `v7bead3-82.bak` `v7bead3-93.bak`（= `v7bead2` 逐字 + **唯一变量 `EPIRUS_BEAD_W=0.5`**，未 promote）。
>    · seed 31：花珠率 **1.9%**（>0，本仓第一次）· seed **82：花珠率 85.7% ⇒ `--dry` 判「珠经济闭环 ✓**
>      （得珠 7 / 花掉 6 / 浪费 14%）⇒ '经济动作有出口'的第一个形态」** · seed 93：0.0%。
>    · 三粒**五道门 + G4/G5 全过**（82：座位 12.1pt · G 6.72 · 场A 22% · 场B 清场 4/局）。
>    · **电磁炮真的出手了**（我另测 20 局自对局）：`v7bead3-82` **railgun 4 次**，**线上包 0 次** ⇒
>      本仓第一个会放电磁炮的包。⇒ 结论：**0.05 是微扰（诊断对），但抬到 0.5 就买得到（推论错）** ——
>      "是结构不是钱"**撤回**；正确表述是"**珠经济的价格高于 0.05 量级、低于 0.5 量级**"。
>    · 代价如实记：目标分在 2/3 seed 上被压低（93：first 0.481 → **0.399**），但**门禁三粒全绿** ⇒ 代价没落在门的轴上。
>    · 记账修复：`EPIRUS_BEAD_W` 只接了 setter、没让 `economyReward()` 读回 ⇒ **D77 红**（`beadW(未接受)`，门按设计抓到）；
>      同时 `setEconomyReward({reset:true})` 没复位 `BEAD_W` ⇒ 状态泄漏。两处已修（读回 + 复位回 0.05）。
> ⑦ **上线判据补全（n=120 全表 + 考卷）**：`v7bead3-82` = **多人 3 血考卷 1st 43.9%**（现役 43.1%，40 局噪声 ±8pt ⇒ **打平**）·
>    座位 12.1pt · G 6.72 · 场A 22%（近 20% 地板）· 场B 4/局 · G4 最凶格 53%/30% · G5 0%/0% ·
>    **体检零阻断（自然过，不需要 `--force`）**。
>    ⇒ **定档：不是"更强"，是"平级 + 买到贵卡经济"**（考卷打平、座位/G/场A 三格略退、换来花珠率 85.7% 与电磁炮 4 次）。
>    **DS 建议**：先试玩；promote 由用户定。若要"严格升级"，下一根杠杆是 **`BEAD_W` 的中间档扫描**
>    （0.05 无经济、0.5 过冲 ⇒ **0.15~0.25 很可能同时保住座位与场A**），两臂 ≈25 分钟。

## v1.5.140 — **多人场打到只剩 2 人时，`双枪/镜面/全息` 必须像 2 人场一样禁用**（用户裁定）· UI 防御档内"摆出"排回"挡下"之前 · 指纹 **be6c2195**

> **本版一并转正 v1.5.135~v1.5.139**：那五组改动先落在分支 `qoder-bugs-0921` 上、当时按"不升版本"记，
> 所以 CHANGELOG 里查不到那几个号，而代码注释一直在引用它们。现役版本自本条起 = **v1.5.140**，
> D8 三方一致（CHANGELOG = README = index.html）。按时间顺序，本版实际含的语义变化：
> - **v1.5.136**（用户实机报的三处 · `resolve.js`/`state.js`/`ui.js`）：转移伤害改成"把本回合所受的可转移伤害转给**指定的一个人**"
>   （不是"弹回使用者"）；地雷时效 `mineRound`：**当回合埋当回合炸**的那波不可转移，后续 buff 雷的波**跟随触发来源**可转移；
>   `原型制御` 的弹回伤害在 UI 上不再写成"反弹"。指纹 a78c28cb → de02be9e。
> - **v1.5.137**（用户裁定 09-20 深夜）：**转移伤害不干扰狙击**，可干扰的是激光眼这类"带攻击效果"的技能。→ 72a9c1b5。
> - **v1.5.138**（指向盲五修 · 用户报"挑衅没判对谁使用"起）：`TAUNT` 合规必须**对挑衅者本人**出攻击卡（`tauntBy` 名单）、
>   雷击之枪成环判定走环状遍历、电磁炮反制必须**指回炮手**、八卦阵计入 GUARD_FAMILY、`resolveTarget` 兜底从 `opp[0]` 改 `saltPick`。→ 56402bbf。
> - **v1.5.139**（用户裁定：ε=0.25 全候选太糙 ⇒ "贴贴不引爆/空爆"昏手）：运行时选择器 = **top-K(5) 个不同技能键上的均匀探索**（`policyChooserN(params,temp,eps,epsK)`）
>   + 菜单级"严格必废"闸门两项（ep<2 不蓄能 属 v1.5.82 旧门 / **天火：施法者自己没有 age≤3 的活符咒 ⇒ 从菜单剔除**）。非指纹面（evo/ui）。
> - **v1.5.140**（本块主体 · 用户裁定 + 实机抓到）：见下。

> **① 残局规则（指纹面 `state.js`）**：`canUseSkillInMode` 过去只看 `state.mode.skills`，于是多人场**打到只剩 2 人**时
> 仍允许 `双枪射手 / 镜面反射 / 全息屏障` —— 而 2 人场是禁的。用户口径："多人场在进入 2p 状态时也应当和 2p 的情况一样"。
> 实机证据在 `results/93-2`：22 回合与 28 回合那两局，冠军在残局 2 人时把 1ジ 砸在全息屏障上（对单目标无意义）。
> 现按**当前存活人数**动态判定（`alive<=2` ⇒ 三张不可用，回到 3 人自动恢复），引擎与训练两侧同源生效。→ 指纹 **be6c2195**。
> 反证用例：`tests/spec.js` **R62**（4 人在场 ⇒ 三张都在菜单；杀到 3 人 ⇒ 仍可用；杀到 2 人 ⇒ 菜单里没有 + `attemptAction` 被拒；救回 1 人 ⇒ 恢复）。
> *注：编号避让 —— v1.5.129 已有一条 R61（被无效化的一手不写 `lastSkill`），新用例记作 R62。*
> **② 事件顺序（UI 面 `ui.js`）**：用户实测 R9/R10/R23 抓到"先输出『挡下伤害』再输出『摆出架势』"。
> 根因是 `evDisplayRank` 防御档一律返回 `[0,0]` ⇒ 同档内退化成写入序。新增 `EV_SUB_RESULT` 子档表：
> 摆出架势类 sub=0，其**结果**类（`blocked/reflect/voidImmune/curseBlock/rodBlock`）sub=1。门：**D38** 加断言（一律 `[0,0]` = 老 bug 复现）。
> **两槽成绩重录**（权重逐字节未变，只换考卷）：3P `v7n1-93` 多人 3 血考卷 1st **41.9% → 43.1%**（残局少一张废卡是正方向的）、
> 2P 内置冠军 **0.9831 不变**（2 人场本就禁这三张 ⇒ 该改动对 2P 是**恒等**，正好当对照组）。np-test **156/156**、spec **52/52**。


## research(qoder 0921 午班) P2/P3 · BEAD_W 中间档扫描 + 会传导的大雷线 · 不升版本 · 未 promote

> 接 DS 的交接件 `docs/HANDOFF-FOR-QWEN-2026-09-21.md`（P1-P4）。过程账：`docs/RESEARCH-LOG-2026-09-21-qoder.md` §12-§13。
> **P1（只读验证他 v1.5.142 的三条前台规则）**：用 `git show 56a9302:js/train/evo.js` 临时取回他改之前的 `soft` 做同装配对照
> （跑完已还原）。**他的 §10.2 读数独立复现一致**（5 席同包 · long · 20 局/格 · ε.2 soft ⇒ 贴贴 **2.70/局**、天火 **0.00/局**）；
> 现役（三条规则后）= 贴贴 **0.00** ✓ 规则①生效，但**天火仍 0.00** ✗ —— ③ 的前提"自己有存活符咒/上一手贴贴"
> **只能由贴贴自己造出来**，而贴贴刚被①掐掉 ⇒ 两条互相抵消；D111 绿是因为它喂**构造态**（门验接线，没验实局可达）。
> 用户 09-21 午间裁定："贴贴本身强度偏低，不用死磕" ⇒ **不再投入**（P4 撤项），只留这条记账。
> **代码侧（P3）**：`js/train/bots.js` 新增 `pickBigTFocus`（DS §8.3 两信号逐字实现）+ **对照组** `pickBigTRandom`
> + 取证工具 `tools/script-value.mjs`（G4 装配 · 直接数 `bigTChain`/连带 `voided`/传导每施放）。
> 读数（n=60 · multi · 4 席现役冠军）：新线 **胜率 15% · 传导/施放 0.66**，对照线 **15% · 0.66（六项读数逐字相同）**，
> `pickBreakDef` 15% · 0.63，`pickDeepSaver` 20% · **0.42**，`pickHeavyFire` **大雷 0.00/局、最大 ep=2**（DS 的空示范实锤复现），
> `pickBeadBurst` **30%**（= DS 表里的 30%）⇒ **预注册"传导>0.43 且 胜率>18%"= 半中**（传导 ✓ / 胜率 ✗）。
> ⚠️ 否证只有一半干净：传导 0.66 与随机取目标**完全一样** ⇒ "会选点"这一维**在 chooser 时刻没有可读输入**
> （第三方普遍 ep=1~2、`lastTarget` 指向自己）⇒ 能下的结论是"**5 ジ 换大雷在这具场里不划算**"，
> **不能**下"选点没用"。要真检验得让脚本看得见"本回合已宣言的指向"（要动引擎公共信息面）⇒ 交用户裁定是否立项。
> 产物（`.bak`，gitignored，按 D82 点名，**未 promote**）：
> - 臂 `v7bead15`（= `v7bead3` 逐字，唯一变量 `EPIRUS_BEAD_W=0.5 → 0.15`）：`v7bead15-31.bak` `v7bead15-82.bak` `v7bead15-93.bak`
> - 臂 `v7bead25`（同上，`EPIRUS_BEAD_W=0.25`）：`v7bead25-31.bak` `v7bead25-82.bak` `v7bead25-93.bak`
>   终裁判据按 DS 交接件 P2 改在**长程口径**（长程自对局 电磁炮 ≥1 次/局 才算"用户能看见"），读数见 RESEARCH-LOG §14。
> - 臂 `v7bead100`（同上，`EPIRUS_BEAD_W=1.0`，作"再抬 4 倍会不会变成普遍出手"的对照）：`v7bead100-31.bak` `v7bead100-82.bak` `v7bead100-93.bak`
>
> **P2 终读（三标度 × 三 seed 的"长程电磁炮/局"全表）**：0.15 ⇒ 0.00/0.00/0.00；0.25 ⇒ **0.50**/0.00/0.00；0.5 ⇒ 0.00/**0.20**/0.00
> ⇒ **"0.15~0.25 是甜点"这条交接件假设否证**：价格旋钮买到的是**中彩票的概率**，不是频率（最高格也差判据 2 倍）。
> 下一根建议跑 DS §5 **提案过但从未跑**的**状态分布塑形**（开局带珠 + 足 ep ⇒ 把两回合序列降成一回合），而不是继续滑标度。
> **§9 v7u1 终裁**：判据① n=120 三粒 G4/G5 **全绿**（最坏格 52%，线 60%；G5 最坏 10%）；判据② 只有 `v7u1-82` 人数谱过现役
> （N2 47 / N3 **52** / N5 28）但它挂五门（场B 清场 0.23 < 0.3）⇒ **按预注册判"不换包"，槽位未动**；
> `v7u1-93` = 考卷 **55.9%**（现役 43.1%）· 有效技能 **6.02**（现役 4.57）· 场A **37%** · 但 N3 退 10pt 且 2P 互殴 **0-120**
> ⇒ 记为"2 人席特化 + 更宽卡池"的**下一臂起点包**，不是换包候选（考卷按仓库规矩只作诊断）。
> 同场把人数谱基线纠正了：**v1.5.140 的规则修复本身**让在位包 N3 33%→**43%**、N5 13%→**23%**，而 **N2 32%→32% 一字未动**
> （2 人场本就禁那三张 ⇒ 自带恒等对照）。工具：`tools/count-spectrum.mjs` 收进仓库 + 新增**残局胜率**与 `--duel` 正面 2 人对决。



> **门禁审计（用户 09-21 指示"不要写太多断言，审一下有没有没用甚至纯负面的，删了"）**：
> - **阳性对照先跑再删**：把 `tests/spec.js` 里 `R59 地雷时效` 改成 `R59 时效X` —— 行为逐字节没变、spec 仍 52/52，
>   np-test **D95 当场红** ⇒ 这类"spec 必须留下 RNN 的反证用例"的断言测的是**字符串**不是行为（本仓今天把一条 R61 改 R62 避让重号，
>   正好是它误伤的那类）。**删 9 条**（8 条钉 spec 标题 + 1 条钉 CHANGELOG 里 `ringRun2` 这个短语），
>   换成**一条运行时断言**：np-test 直接 `spawnSync tools/spec-run.mjs` 并要求"全绿且条数 ≥52"
>   ⇒ 既保住原意图（整条用例被静默删除要响），又**新增**了原来没有的效力（spec 失败以前只有单独跑 spec 才知道）；
>   这条新断言本身也过了阳性对照（阈值临时改 99 ⇒ 立刻红）。断言总数 720 → 712。
> - **审过但保留（附理由，不删）**：① `G6 靶向率 ≥40%` 那些 `FAIL` 行——它不是噪音，`promote-champion` 要解析它做"只记录"列，
>   而同文件的 **G6[元测试]** 是有判别力的那条（能分开"会瞄"与"只瞄最肥"）；② `RULES-2P 必须记下 R56~R60` 这类**规则账本**钉文本——
>   它守的是"文档与实现同步"（v1.5.143 之前那条自相矛盾的 R10 措辞就是 bug 温床），没有别的门覆盖；
>   ③ D82 里 `cd.length > 1000` 那条近似恒真的兜底——它是**故意**的（该分支若无断言会被本仓 L1 抓红），删了反而倒退。
> - 仍待用户点头的第二批（同类零判别力、但守的是"注释里的理由"）：`pol.indexOf('幻影选项')`、
>   `必须写明 wall 口径没有靶向判别力`、`必须写明标定理由 45%`、`energy 档必须标注"含空转/过期珠"`、`s.indexOf('参照行')` 共 5 条。

> 产物（`.bak`，gitignored，按 D82 点名，**未 promote**）：
> - 臂 `v7beadseed`（**状态分布塑形**：`v7bead2` 逐字 + 唯一变量 `EPIRUS_BEAD_SEED=0.5`，`BEAD_W` 回到默认 0.05）：
>   `v7beadseed-31.bak` `v7beadseed-82.bak` `v7beadseed-93.bak` —— 三粒 `zero-promote-check` 3/3 有提升，但
>   **§15 主判据① 否证**：长程 5 席自对局 电磁炮 **0.05 / 0.00 / 0.00 次/局**（判据 ≥0.5 且 ≥2/3 seed）。
>   ⚠️ 有一条**比"没做成"更有用的副作用读数**：`v7beadseed-31` 的**蓄能 10.70 次/局**（现役 0.00，全仓最高），
>   得珠 428 / 过期 427 / **花掉 1** ⇒ 失败点从 DS 那代的"不敢蓄"变成了"**蓄了照样不放**"。
>   这条直接顶掉 DS §5 的机制假设：**瓶颈不是"两回合信用分配"**（我已经把序列压成"开局就能放"的一回合，仍然不放），
>   而是"放炮这个动作在当前适应度算术里没有相对优势"⇒ 按 DS §7(b) 收束：**珠经济定位为"针对某条脚本的窄反制"，
>   留在考卷里，不再逼冠军自己长**（`v7bead3-82` 那粒 0.5 档的"闭环 85.7%"是**转化率**，与本判据的**频率**不是同一件事）。
>   代码侧新增旋钮 `EPIRUS_BEAD_SEED`（`evo.js`+`paralleltrain`+`train-worker` 三处，默认 0 ⇒ 逐字不变；**对称注入**全席，不只给受评席）。
> - 臂 `v7cmin3`（= `v7beadseed` 逐字 + 唯一变量 **`EPIRUS_CHARGE_MIN_EP=3`**，训练侧蓄能门槛 2→3）：
>   `v7cmin3-31.bak` `v7cmin3-82.bak` `v7cmin3-93.bak` —— **机制判据第一次中**："有珠"与"买得起电磁炮"从
>   **0.4%（1/226）** 变成 **100%（21/21）**；行为判据仍差一口气（电磁炮 **0.35 / 0.00 / 0.00 次/局**，要 ≥0.5 且 ≥2/3），
>   两粒干脆不蓄能了（门槛抬高的代价）。⇒ 以前"花珠率≈0"里**有一部分是可得性被菜单卡死**（假瓶颈），
>   拆掉之后剩下的才是真瓶颈="放炮相对不划算" ⇒ **DS §7(b) 的"窄反制"定档这次才带证据**；
>   再往前只剩**规则面**（电磁炮降价 / 电珠存活期放宽），要动指纹文件、全链重记 ⇒ 交用户裁定，不建议顺手做。
>   `zero-promote-check` 3/3 有提升（门槛没把包练坏）；**未 promote、槽位未动**。
>   同版把 **D71** 里钉 `(pp.ep || 0) >= 2` 字符的断言换成**真跑 `econBase`**（默认口径 + 抬到 3 + 复位各验一次）——
>   它在我加旋钮的当场就红了，正是本轮清掉的那类零判别力文本钉。



## research(qoder 0921 白班2) v7u1 臂产物记账 + 运行时随机性 soft 模式 · 不升版本 · 未 promote

> 产物（`.bak`，gitignored，按 D82 点名）：`v7u1-31.bak` `v7u1-82.bak` `v7u1-93.bak` ——
> 预注册见 `docs/RESEARCH-LOG-2026-09-21-qoder.md` §9：v7t1 命令**逐字去掉 `EPIRUS_BIGCARD_W`**（= v7n1 配方），
> 唯一变化 = 引擎语义 **be6c2195**（残局 2 人禁 MULTI_ONLY），HOT=现役 `v7n1-93`。三粒都落了、
> `zero-promote-check` **3/3 相对起点有提升**（自评 31=0.430 / 82=0.470 / 93=0.459，top2 0.69/0.62/0.70）。
> **终裁（n≥120 五门 + G4/G5 + 人数谱 + 残局胜率）未做，槽位未动、未 promote。**
>
> 另：运行时随机性按用户实机反馈（"防御偏多、丢了集火和滚环"）改 **v1.5.141 口径**——非指纹面（`evo.js`/`ui.js`）：
> `policyChooserN` 新增 `epsMode='soft'`（探索**不许**覆盖贪心选定的防御/聚能环 + 探索集里不放防御键），浏览器从 `ε=0.4 uniform`
> 改到 **`ε=0.2 soft`**。量具 `tools/behavior-profile.mjs`（新）· 同包同装配 40 局/点：防御 26.4%→**9.1%**、
> 聚能环 0.0%→**1.5%**、集火 22.0%→**34.1%**、镜像破局仍 100%、胜率 30%→33%；代价如实记：被无效化出手 3.0%→6.2%。
> 读数表与理由钉在 `js/ui/ui.js` 那一行的注释里。

## research(qoder 0921 夜班1) t 系臂产物记账 · 不升版本 · 全部未 promote

> 分支 `qoder-bugs-0921`（新语义 56402bbf 下的训练探索；另含 #19 指向盲五修 f42891d + #20 键级探索 36a6a88）。判据与否证账：`docs/RESEARCH-LOG-2026-09-21-qoder.md` §1-3。
> 产物（`.bak`，gitignored，按 D82 点名）：
> - `v7t1-31.bak` `v7t1-82.bak` `v7t1-93.bak` —— BIGCARD_W=0.1 唯一变量臂：**①贵卡否证**（大雷/电磁炮/小雷/地雷三粒全 0%），
>   但 ②零回退（G4 三粒全 PASS 最坏 58% · G5 0~7%）；"没学会的强招=大雷+11pt/电磁炮+9.4pt"=value-blind 签名。
> - `v7t2-31.bak` `v7t2-82.bak` `v7t2-93.bak` —— heavyfire 教师段臂：**①同样否证**（贵卡三粒 0%，价值分反而 +15~18pt 不出手）；
>   ②不回退（G4 31/93 全绿最坏 30%、G5 0~8%；82 唯一红=激光剑格 61% 贴线 1pt）。**双重否证定论**：贵卡出口是特征面设计题，臂路收束（RESEARCH-LOG §5）。
> - `v7t2p-2p.bak` —— 2P 专训臂（train-best 热启动现 2P 冠军，新语义下 3×500）：H2H **120-0** 现冠军（temp0.8 仍 119-1）、20 基准 avg 99%、
>   N2 脚本场 97%=现冠军。**"先立"成立**；短板=窄体专家（有效技能 3、落点几乎只剩 snipe+gun）⇒ 交用户裁定，未 promote、槽位未动（git 还原）。

## research(qoder 0920 · 晚班 N1/N3 记账 · 不升版本 · 未 promote)

> 过程与预注册判据：`docs/RESEARCH-LOG-2026-09-20-qoder.md` §19/§20。
> 新产物（`.bak`，gitignored，按 D82 点名）：`v7n1-31.bak` `v7n1-82.bak` `v7n1-93.bak` —— 第五行（打龟穿透占比）臂，
> Q10 配方逐字、唯一变化 = 形状评分器四行→五行。**31/93 三条预注册判据（n=120：G5-long ≤25%、G4 两模式 ≤60%、
> 打龟穿透占比 ≥50%）全中 = 史上第一个全判据零阻断代**；82 只枪格 90% 红（血缘方差，如实记不救）。
> 首批 meta 带 `rulesFingerprint=a78c28cb`（N3 落地）。**未 promote**（N4 裁定件等用户，推荐 v7n1-93；不再需要 --force）。
> 配套：`tools/v2v4-lib.duelAssembly` 加打龟归因三列 · `server/shape-scorer.mjs` 五行化（复用第四行对局，零额外成本）·
> `server/train-server.mjs` 两处落盘写指纹。np-test 156/156。

## research(qoder-research-0920 · 通宵臂产物记账 · 不升版本 · 全部未 promote)

> 分支 `qoder-research-0920`（第三方 AI 接管训练研究一晚）。完整过程与预注册判据：`docs/RESEARCH-LOG-2026-09-20-qoder.md`。
> 线上包 sha1 三度核对未变（9012a71dc7ae）。产物（`.bak`，均 gitignored，此处按 D82 点名）：
>
> - Q1 模仿教师臂：`v7aim1-31.bak` `v7aim1-81.bak` `v7aim1-82.bak` `v7aim1-91.bak` `v7aim1-92.bak` `v7aim1-93.bak`
>   （82=全仓第一个攻击解过 G4：只枪 5%/8% · 打枪手 5.00/局 · 场B 0.80；窄体：座位 35pt/G 2.76，不可换包）
> - Q1b 接力臂（首次真热启动，起点=v7aim1-82）：`v7aim2-31.bak` `v7aim2-82.bak` `v7aim2-93.bak`
>   （3/3 保住攻击解；82 挂 G2.00 + 新最克格「瞄威胁者」75% ⇒ 过一格≠过全表）
> - **Q1c 降教师臂（教师占比 0.5→0.25 + 热启动 v7aim1-82）：`v7aim3-31.bak` `v7aim3-82.bak` `v7aim3-93.bak`**
>   （31 只枪回潮 75%；82 滑向经济刷屏（95 回合/场B 0.07）；**93 = 全晚最完整的一粒** —— 只枪 **10%/8%**（n=40 快筛；
>   DS 复测 n=60 = **8%/7%**）· G **3.27** ✓ · 场A 28% ✓ · 场B 清场 **1.32** ✓ · 墙 10.4 ✓，但 `--dry` 判
>   **座位 33pt** ✗ + **新最克格「瞄威胁者」68%/78%** ✗ ⇒ **仍不可换包**（5 项阻断，未 `--force`）。
>   ⚠️ 本行是事后补记（Q1c 落盘晚于本记账块 ⇒ D82 会红"24h 内的 .bak 没点名"）；补上它分支才自洽。）
> - Q3 环×贵卡臂（BASE 谱系）：`v7rb1-31.bak` `v7rb1-82.bak` `v7rb1-92.bak`（2/3 长成 CLEAR_W 长寿刷收缩被动解，95-102 回合/局）
> - Q3b（线上包热启动候选②）：`v7rb2-31.bak` `v7rb2-82.bak` `v7rb2-92.bak`（共存两代 0/6 ⇒ §4-2 建议按带证据否证结案）
>
- Q1f 复验臂（新语义）：`v7aim1f-31.bak` `v7aim1f-82.bak` `v7aim1f-93.bak`（31/93 双格绿，结论在修复后成立且更强）
- Q4 四段课程臂：`v7cur3-31.bak` `v7cur3-82.bak` `v7cur3-93.bak`（82=**第一个五道门全绿的包**·场B 清场 4.0/局全仓最高；红在 G4 只枪 90% —— 课程段互相覆盖 ⇒ 三角跷跷板成立，P2 形状法升为唯一杠杆）

> - 三段课程臂：`v7cur2-31.bak` `v7cur2-82.bak` `v7cur2-93.bak`（31=史上第一次四门同时绿：墙2.15/G6.4/座位17/场A26 · 唯红=场B 清场 0.00 · 73回合/局 —— 与 “刷收缩”草案判据同体）

> - ★★ **现行犯修复（白班）**：`policy.js:397` `if (!tid …)` 把**目标=0 号座**判成“无目标” ⇒ 镜像局全员集火 0 号（v7aim1-82 首死 120/120）。
>   修：`tid == null` + 新门 **D108**（镜像等性价对+不对称对照）。指纹 `00e732a7→a78c28cb`；两包 meta 按 v1.5.129 先例重记（权重逐字节未变）。
>   np-test **155/155** · spec 45/45 · smoke/battle OK。取证链与后果见 `docs/RESEARCH-LOG-2026-09-20-qoder.md` §9。

- P1 课程臂（分段教师 deepsaver→aimdefender · HOT=v7aim2-82）：`v7cur-31.bak` `v7cur-82.bak` `v7cur-93.bak`（**未 promote**）
>   · 93=行为门 G4/G5 **全表首次两模式绿**（只枪 20/10 · 瞄威胁 33/10 · 场A 34 · 场B 0.8 · 座位 22 ✓）但挂 墙 0.00 + G 2.88；82=G 6.44 但座位 44pt/墙 0/场B 0.00；31=退化（只枪 100）。三粒**齐挂反弹墙 0.00** ⇒ 课程把火力全押瞄准轴。
> 代码侧（本分支）：`pickAimDefender` 教师 · `EPIRUS_RING_W` 接 econ-env（默认关）· `RING2_HOT` ·
> G6 读回跨行修复 · **G4IMPL 实现身份双绑（阳性对照：POOL 不变/IMPL 变）** ·
> **账目更正：NP 臂的“热启动=最新冠军”此前是假的（runner 覆写 EPIRUS_BUNDLE_IN、恒拷 v1.3.58 BASE；近期全部臂与线上包同谱系 d13d3c856c6cff62）**。

## v1.5.134 — 「聚焦枪手」进池跑了一臂：**第一次有包过了唯一红门**（G4 只枪 **45%**，理想线 45%）—— 但它是**靠变被动**过的（场B 清场 **0.00** ⇒ 挂 G5，**不上线**）· 机制拆成「花钱 vs 瞄准」（**瞄准是主因**）

> 承接 v1.5.133（机制 = 龟；规则级天花板 62pt）。本版把 v1.5.133 的**池子单杠杆**跑了：自定义池 = §3 标准池
> （与 `r61fix` 六臂**逐字相同**：`CLEAR_W=0.1` / `DIV_W=0.3` / `TRAIN_SEAT_GAMES=24` / IMIT pickDeepSaver 0.5+OVERRIDE）
> **+ G4 那格自己的只枪线 ×2**（新对手 `pickGunFocus`）。
> **⚠️ 预注册预测（跑臂**之前**写在 `js/train/bots.js` 与 `server/opp-pool.mjs` 的头注里）：这次池子单杠杆"很可能不动 G4"。**
> **实测把它否了** —— 见 §1/§2。**读数（本版实跑）**：`spec 45/45` · `np-test 154/154`（exit 0）· `smoke` OK ·
> `battle-test` OK · 引擎指纹 **`00e732a7`**（本版**没动引擎、没换线上包**）。
> ⚠️ **D82 记账**：本版臂的 6 个产物 `v7gf1-{31,81,82,91,92,93}.bak` 已在本文中点名（未 promote）。

### 1. 臂：池子单杠杆（受控 A/B，唯一变量）

- 新对手 **`Bots.pickGunFocus`**（`js/train/bots.js`）+ 池子登记 **`{name:'gunfocus', fn:'pickGunFocus'}`**
  （`server/opp-pool.mjs`，**追加在尾部** ⇒ `OPP_DEFAULT = slice(0,9)` 不变 ⇒ 既有协议一行没动）。
  口径 = **G4 那格自己的线**（击杀优先 → 再压血量最高者，与 `pT` → `T.pickTargetN` 同口径）。
  **自检（本版实测）**：把它当 G4 的脚本席跑，`long 75% / multi 62%` —— 与 G4 那格**逐字相同** ✓
  （同时证明它真的在开枪：不 fi 的话会读成基线 ~18%）。
- 池子（产物 meta 里可查）：`random,…,gunspam,guardspam,gunfocus,gunfocus` ⇒ **与 `r61fix` 六臂的池子逐字相同，只多这两份权重** ✓。
- 臂产物 meta 的 `opps` 已核对含 `gunfocus,gunfocus`（= "新对手真的进了池子"的非空枪证据 ✓）。

### 2. **结果：2/6 过了 G4（史上第一次），但 2/2 都是"变被动"过的** ⇒ **不上线**

| 候选 | G4 只枪（long/multi） | 最克的那一格 | 场B 清场/局（门槛 0.3） | 场B 造伤/局 | 场B ジ% |
|---|---|---|---|---|---|
| **v7gf1-92** | **45% / 53%** ✅ | 只枪（就是它） | **0.00** ✗ | 0.60 | 66% |
| **v7gf1-82** | ≤55% / ≤35% ✅ | 瞄威胁者 55%/35% | **0.00** ✗ | 1.00 | 4% |
| v7gf1-91 | 87% / 73% ✗ | 只枪 | 3.40 ✓ | 11.13 | 49% |
| v7gf1-93 | 88% / 80% ✗ | 只枪 | 4.00 ✓ | 11.60 | 65% |
| v7gf1-31 | 77%(只防御) / 63% ✗ | 只防御 | 0.20 ✗ | 3.15 | 56% |
| v7gf1-81 | 75%(只防御) / 93% ✗ | 只防御 | 0.00 ✗ | 1.38 | 67% |
| ~~线上包~~ | 75% / 62% ✗ | 只枪 | 1.00 ✓ | 3.10 | 22% |

- **权威判据**：`node tools/promote-champion.mjs docs/artifacts/v7gf1-92.bak --dry --exam-first=0` ⇒
  **⛔ 体检未过（2 项阻断）**：`G5[v7gf1-92/long] 面对"只防御不还手"必须能清场（实测防席夺冠 37% · 阈值 ≤25%）`
  ⇒ **已中止、未换包**（没有用 `--force`）。两个独立量具互相印证：`场B 清场 0.00/局` ⇄ `G5 防席夺冠 37%`。
- ⇒ **文档里那条「场B 清场 ↔ 抗只枪」在"训练出来的包"上又复现了一次**（0.00/1.00 两端都在），
  也就是说：**池子这一根杠杆买到的还是"避免那场赛跑"（变被动），不是"赢那场赛跑"**。
  v1.5.133 的规则级反事实证明这条互斥**不是必须的**（§3）⇒ **差距在训练信号/训练形状**。

### 3. 机制拆成两个杠杆：**「瞄准」比「花钱」更载重**

在 G4 自己的装配里，对**同一个包**（线上包）只改**一个**决策规则（n=60 · long/multi · seed0=90210）：

| 杠杆 | 只枪 long | multi | 打在枪手身上/局 | 枪手余血 | 改判 |
|---|---|---|---|---|---|
| 不动（基线） | 75% | 62% | 3.27 | 1.73 | — |
| **`aimGunner`：只改目标**（伤害卡全打枪手，**不多花一分钱**） | **0%** | **0%** | **5.00** | **0.00** | 420 / 230 |
| `forceAttack`：只改花钱（买得起枪就开枪，目标照旧） | 13% | 17% | 4.85 | 0.15 | 1759 / 1677 |
| 两个都改 | **0%** | **0%** | 5.00 | 0.00 | 2683 / 1754 |
| `forceTurtle`：完全不还手 | 100% | 100% | 0.00 | 5.00 | 7594 / 3809 |

⇒ **主杠杆是"往谁身上打"**：家族本来就打出 11.33/局（long），只是**摊开了** —— 枪手留 1.73 血赢赛跑；
**把同样的伤害引到枪手身上**（5 血）就 100% 拿下，且**不额外花钱**（改判只 420 次）。
- 配套：`duelAssembly()` 新增**伤害归属**计数（`dmgToScriptedPerGame` / `dmgByScriptedPerGame` / `champDmgPerGame`），
  探针 `probe-g4-anatomy.mjs` 的解剖行现在打印这一列。

### 4. 跨包检验：决定那格的是「打在枪手身上的伤害」（r = **−0.82**），不是「攒钱比例」（r = +0.23）

13 个包（线上包 + 6 个 `r61fix` + 6 个 `v7gf1`）在同一装配（只枪 · long · n=60）下：

| 包 | 只枪格 | ジ占比 | 打在枪手身上/局 | 枪手余血 |
|---|---|---|---|---|
| 线上包 | 75% | 62.2% | 3.27 | 1.73 |
| **v7gf1-92** | **45%** | 55.1% | **3.83** | 1.17 |
| **v7gf1-82** | **17%** | 60.5% | **4.82** | 0.18 |
| r61fix-31 | 100% | 68.5% | 1.45 | 3.55 |
| r61fix-82 | 100% | 67.6% | **0.40** | 4.60 |
| r61fix-92 | 95% | **56.0%** | 2.25 | 2.75 |

⇒ **r(只枪格, 打在枪手身上) = −0.815** vs **r(只枪格, ジ占比) = +0.233** ⇒
"攒钱比例"解释不了多少（`r61fix-92` 攒得最少却仍是 95%），**"往不往枪手身上打"才是那条轴**。
- 这也解释了"臂更能清场却更差"：`r61fix` 六臂的**场B 清场 1.00~4.00、造伤 4.7~12.0/局**（比线上包还猛），
  但**只枪局里打在枪手身上只有 0.40~2.63/局** ⇒ 它们的进攻**没落在枪手身上**。
- ⚠️ 同时它给出一条**剂量警告**：对**已经会瞄准**的包（v7gf1-82）强行 `forceAttack` 反而**变差**
  （17% → 33%，Δ+16pt）⇒ "多开枪"不是越多越好，主因在**目标选择**。

### 5. 下一根杠杆（未做，等裁定）：**训练形状**，不是再加对手

- 事实链：**池子里已经有两条枪线**（`pickGunSpam` 打最肥 · `pickGunFocus` 击杀优先），而且 `pickGunSpam`
  在同一个 harness 里比 G4 那格**更狠**（83%/77% vs 75%/62%）⇒ "池里没有这种压迫"**不成立**；
  加 `gunfocus` 也确实把 G4 推动了（2/6 过门）—— 但**买到的是被动解**（§2）。
- 原因（可检验的解释）：**训练局的压力座次份额与考卷不同** —— 训练是"1 席候选 vs 4 席对手"
  （枪手的目标里只有 ~1/4 是候选），考卷是"4 席我的家族 vs 1 席枪手"（**子弹全落在我家**）。
  v1.5.89 的头注早就把机制写成"低密度不受罚"，可**形状**没变，所以池子只能买到"躲"。
- ⇒ 下一臂应改**形状**：用现成的单一来源装配 `playAssembly(k=4)`（= V4 的形状：**4 席我的家族 vs 1 席外部人**）
  作为训练评价的另一项，让"家族赢下与单个武装外部人的赛跑"进适应度 ⇒ 直接练 §3 那条"打枪手"的行为。
  代价：训练路径要加一项评价（`server/train-server.mjs` 的评分处）+ 一臂（本版实测一臂 ≈ **15 分钟**）；
  **训练期不写线上包**（`ring2-run.mjs` 结束会还原）。

---

## v1.5.133 — G4「只枪」那格**查出机制**：不是玄学，是**龟**（买得起枪却攒着不出手）· 文档里那条「场B 清场 ↔ 抗只枪 互斥」被**反事实否证** · 顺手修好一个长期打「读失败」的探针

> 承接 v1.5.132：G4[long] 的「只枪」格（线上包 **75%**，阈值 60%）是全仓**唯一已知红门**（D67 的 `--force` 例外）。
> 09-19 已证明"换包不是出路"（6 个 `r61fix` 臂包在这一格 **80~100%**，**7 个包里现包最好**）⇒ 只能**针对性干预**。
> 本版把"到底输在哪"变成了**因果读数**：只改被测席**一个**决策规则，只枪胜率 **75% → 13%**（long）/
> **62% → 17%**（multi），而**场B 清场 1.00 → 1.00 一点没掉**。
> **读数（本轮实跑）**：`spec 45/45` · **`np-test 154/154`（exit 0）** · `smoke SMOKE OK` · `battle-test BATTLE OK` ·
> 规则指纹 **`00e732a7`**（本版**没动引擎、没换线上包**）。
> 自检：新探针 §A 必须**逐字复现**已记录的 **75%(long)/62%(multi)**；修好的 `probe-aggr` 必须复现体检画像里已有的
> **场A 30% / 场B 清场 1.0**（两个都是文档里写过的数 ⇒ 对上了才敢读后面的读数）。

### 1. 新探针 `tools/probe-g4-anatomy.mjs`：在 G4 自己的装配里做解剖 + 双向反事实

- **口径与 `gate-drafts.mjs` 的 G4 逐字相同**（`duelAssembly()`：seed0=90210、`seat = g%5`、
  `slotSalt = h32(seed0 + g*2246822519)`、被测席 `T.policyChooserN(params, 0.15)`）。
  **§A 自检**：必须复现 75%/62%；复现不出来就**不许读后面的数**（先分清"量具坏了"还是"包坏了"）。
- **§B 解剖**（同一 seed 序列、只换脚本席）：

  | 行 | 脚本席胜 | 均回合 | ジ占比 | 终局被测存活 | 脚本席余血 |
  |---|---|---|---|---|---|
  | 基线（5 席同包） | 18% | 44.6 | 60.1% | 0.78/4 席 | 0.28 |
  | **只枪（1 席脚本）** | **75%** | **33.0** | **62.2%** | **0.25/4 席** | **1.73** |

  ⇒ 不是"被打死"，是**赛跑输掉**：枪手每回合削血，被测席 62% 的决策在攒钱；枪手自己通常也只剩 1.73 血。
- **§C 反事实**（只改被测席**一个**决策规则；`forced` = 实际改判次数，=0 即**空枪**，本行无信息）：

  | 钩子 | 只枪 long | Δ | multi | Δ | 改判次数 |
  |---|---|---|---|---|---|
  | 不动（线上包） | 75% | — | 62% | — | — |
  | `forceAttack`（**买得起枪却选ジ → 改判开枪**，目标 `T.pickTargetN`） | **13%** | **−62pt** | **17%** | **−45pt** | 1759 / 1677 |
  | `forceTurtle`（任何非ジ输出 → ジ） | **100%** | +25pt | **100%** | +38pt | 7594 / 3809 |

  ⇒ **双向都动**（一个方向掉 62pt、反方向升到 100%）且改判次数非零 ⇒「**只枪吃龟**」是**因果成立**的机制，
  而且**天花板有 62pt**（不是"再调调就 2~3pt"）⇒ 方向值得投。
- **§D 池子那条线**：`Bots.pickGunSpam`（**打最肥**、无击杀优先）在同一个 harness 里是 **83%(long) / 77%(multi)**，
  比 G4 那格（`pT` → `T.pickTargetN`，**击杀优先再打最肥**）**更狠 8 / 15pt**
  ⇒ "训练池没覆盖这类对手"这个解释**站不住**（池里那条线更难，包照样不会应对）—— 记下来是为了**别再花一轮去试它**。
- 可行性核对（决定"能不能学"）：`js/train/policy.js:376-380` 把「**可负担余量 `ep-cost`**」和「是否可施展」
  都喂给了网络，且**买不起的招不进候选表**（`policy.js:585-597`）⇒ 选ジ 而没开枪时，**枪就在候选表里**、
  网络**看得见**它付得起 ⇒ 这是**训练信号**问题，**不是可观测性/特征问题**（本仓栽过这类：D50/D58 座位身份通道）。

### 2. 文档里那条「场B 清场 ↔ 抗只枪 单调互斥」**被反事实否证**

- 原文（`docs/HANDOFF-2026-09-19.md` §2.1 表内）：`CLEAR_W` 0.05 → 0.1 ＝ 「**场B 清场 ↔ 抗只枪是单调互斥**」
  （0.05：G4 ✓ / 场B **0.00**；0.1：场B **1.0** ✓ / G4 **76/67%** ✗）。
- 但那句话的读法是**沿一个旋钮比较两个不同的包**（标量化 `base + CLEAR_W·clears` 的两个最优点）——
  它证明的是"**这个旋钮买不到两个都要**"，**不是**"游戏里不存在两全的策略"。
- 本轮在**同一个包、同一 seed 序列**下只改一个决策规则（§1 的 `forceAttack`）就同时拿到了：
  **只枪 75% → 13%**（G4 那格离阈值 60% 还有 47pt 余量）**且 场B 清场 1.00 → 1.00**（门槛 ≥0.3，还是满分）。
  同一次跑里 `forceTurtle` 把场B 清场打到 **0.00**（→ 挂门）⇒ **这条轴有判别力**（不是量具死了）。
- ⇒ 结论：**互斥是那个旋钮（标量化）的性质，不是游戏的性质**。要同时要两样，得改**训练信号/形状**，
  而不是在 `CLEAR_W` 上滑。⚠️ 诚实的边界：`forceAttack` 是**规则覆盖**（不是重训出来的网络），
  所以它证明的是"**环境里存在两全的行为**"，不等于"现在的训练管线能自己找到它" —— 后者要跑臂（见 §5）。

### 3. G4 的装配收成**单一来源**（新门 **D107**）

- 病：该装配原先**只写在 `gate-drafts.mjs` 内部** ⇒ 任何"想解剖 G4 输在哪"的探针都只能抄一份
  （本仓"同一规则两处维护必然漂移"栽过四次）。现在本体在 `tools/v2v4-lib.mjs` 的 `duelAssembly()`
  （带 `countKeys` / `forceAttack` / `forceTurtle` 钩子），`gate-drafts` 只留一个**薄壳 `duel()`**（调用点与判词一字不动）。
- **重构验收 = G4 全表逐字不变**：重跑 `gate-drafts` 对 `g4-check.log` 全部对账 —— 线上包 long
  `基线 18 · 只防御 13 · 只枪 75 · 只狙击 7 · 激光剑 13 · 坦克 12 · 滚环 0 · 瞄威胁 2 · 珠爆发 30`，
  multi `基线 10 · 3 · 62 · 10 · 32 · 43 · 0 · 0 · 15` ⇒ **一格没动**（`mulberry32`/`h32` 两支实现本来就同源）。
- **D107** 守它：gate-drafts 必须 import `duelAssembly`、且**不许再出现**那句 `h32(seed0 + g * 2246822519)`。

### 4. 修好 `tools/probe-aggr.mjs`（它**一直在打「读失败」**）+ 给场A/场B 加**诊断钩子**（新门 **D106**）

- 实测：`node tools/probe-aggr.mjs 4 js/bundled-champion-3p.js` ⇒ 每行 `读失败: Cannot read properties of
  undefined (reading 'toFixed')`。原因：它读的字段名（`atkOld`/`dealt`/`taken`/`rounds`）与 `aggressionProfile()`
  实际返回的（`atkOldWhitelist`/`dealtPerGame`/`takenPerGame`/`roundsPerGame`）**漂移**了，`undefined.toFixed()`
  抛错被 catch 吞成一行 ⇒ **全仓没有一个能用的场A/场B 打印器**，而这两条轴正是 G4/五道门争论里被反复引用的。
- 它还**从来没打印过 `clearedPerGame`** = 场B 真正被门禁判的那一列（阈值 **≥0.3/局**；D60 明确：场B 判据
  **只能是清场数**，胜率是规则红利）⇒ 只打了胜率/平局。**两处都修**，并接上 `AGGR_HOOK=attack|turtle`。
- 修好后复现体检画像（**校准点**）：场A 新进攻 **30%**（文档记 30% ✓）· 场B 清场 **1.00**（文档记 1.0 ✓）。
  加钩子后的完整三行（n=60 · 同 seed）：

  | 钩子 | 场A 进攻% | 场A 造伤/局 | 场B 造伤/局 | **场B 清场/局** | ジ% | 改判 |
  |---|---|---|---|---|---|---|
  | 关 | **30%** | 1.68 | 3.10 | **1.00** | 22% | 0 |
  | `attack` | 49% | 2.63 | **5.15** | **1.00** | 15% | 125 |
  | `turtle` | 0% | 0.00 | 0.00 | **0.00**（挂门） | 100% | 1760 |

  ⇒ "买得起就开枪"这条规则**同时**把两边都做对了（场B 清场不掉、造伤还涨 66%）。
- 钩子加在**单一真源** `audit-lib.aggressionProfile(W, params, GAMES, opts)` 上、**默认关闭**（读数逐位不变；
  五道门/体检/`D55` 的调用点一行没改），与 `playAssembly()` 的 `ablate` 同法。
- **D106** 守它（**行为**断言，不钉文本）：真跑一次探针（1 局）断言输出**没有「读失败」**、且**必须含「清场/局」**；
  再把真源返回的键与诊断字段对账，并断言"默认关闭钩子时 `forced === 0`"（钩子不得改变基线）。

### 5. 还没做的（下一步的**可证伪预测**，等裁定）

- **没有跑的**：一次**一次性臂**（不改默认池）。设计：`RING2_POOL` 自定义池 = §3 标准池 + **G4 那格自己的只枪线**
  （击杀优先、`T.pickTargetN`）×2，其余配方与 `r61fix` 那 6 个臂**逐字相同**（`CLEAR_W=0.1`/`DIV_W=0.3`/
  `TRAIN_SEAT_GAMES=24`/IMIT pickDeepSaver 0.5+OVERRIDE）⇒ 这是**池子这一个杠杆**的受控 A/B。
  **预测（可证伪）**：只枪格从 75% 降下来（理想 ≤60%），且 **场B 清场 ≥0.3 不掉**；若它**不动** ⇒
  "多喂这一类对手"不够，下一根杠杆是**训练形状**（4 席自家族 + 1 席脚本外部人 —— 现在协议里没有这个形状，
  而 G4 恰恰是它）。成本：6 seed ≈ 11 分钟 + 量 G4/场A/场B/V4 约 5 分钟；训练期不写线上包。
- ⚠️ **D67 的绑定有一个已知缝**：`G4_POOL_ID` 是「**键序 + 阈值**」的 sha1（`gate-drafts.mjs:284-289`）
  ⇒ **改某一格的实现（不改键名/阈值）时这个 id 不变**，旧包的 `--force` 例外会继续免检。
  本轮没改任何一格的行为所以没触发，但把只枪线做成池子里共享的 bot（`pickGunFocus`，与 `pickBeadBurst` 同法）
  时就会踩到 ⇒ 真要动那一格，得**同时**把实现身份并进 id（另立一条）。
- **V4 门的阈值（10%/席）仍是草案**，等用户裁定（`tools/np-test.mjs` 的 `V4_MIN` 一个常量）。

---

## v1.5.132 — 给 **V4「满桌同包」立门**（新门 **D105**；阈值是草案待裁定）· **V2 被实测证明"没有判别力"** · 三装配抽成单一来源

> 承接 v1.5.131：那次查出 **R61 让线上 3P 包在 V2/V4 上掉了 24.7 / 1.1pt 而没有任何门看着**，同引擎的 6 seed 臂
> 又把 V4 撒在 **1.8~15.0%/席** ⇒ 本轮把"**量后果**"的那条轴立成门（用户已否掉"数动作投向谁"）。
> **读数（本轮实跑）**：`spec 45/45` · **`np-test 152/152`（exit 0）** · `smoke SMOKE OK` · `battle-test BATTLE OK` ·
> 规则指纹 **`00e732a7`**（本版**没动引擎**）。标定（n=600 · seed 7777 · **同一份实现**）：
> 线上包 **V4 16.1%/席 · V2 10.0%** ｜ **零权重常数策略 V4 3.4% · V2 10.2%** ｜ 6 个臂包 V4 1.8/5.1/2.5/5.8/15.0/4.0。

### 1. 装配抽成**单一来源** `tools/v2v4-lib.mjs`（探针与门共用）

- 原先 V1/V2/V4 的装配**只写在** `tools/probe-ring-ablate.mjs` 里；`np-test` 想守它只能再抄一份或钉文本
  （本仓"同一规则两处维护必然漂移"栽过四次，见 `server/opp-pool.mjs` 文件头）。
- 现在：`ASSEMBLIES`（三个装配的定义）+ `playAssembly()`（轮座/播种/消融钩子）+ `rates()`（每席位）+ `measureAll()` 全在一处；
  探针改为 import 并**删掉自己那份**。`tools/crowding.mjs` 量的是**另一个轴**（对手固定、改同族席位份数 k=1/2/4），
  其读数在文档里被引用过 ⇒ **本轮不合并**（合并就得重测那批数），已在模块头写明。
- **重构的对账（行为等价的硬凭据）**：`node tools/probe-ring-ablate.mjs 600` 重构前后**逐位相同** ——
  V1 85.3% · ring 出手 386 次 · 非零对 2/600 · p=1.000 ｜ V2 10.0% · 292 · 17/600 · p=0.332 ｜ V4 16.1%/席 · 782 · 119/600 · p=0.142。

### 2. 新门 **D105**：**V4 立门（≥10%/席，草案）** + **V2 只记录不阻断**

- **为什么是 V4**：它是**产品形态**（满桌同包 ⇒ 会不会跟自己人互杀），且**有判别力**（见 §3）；公平份额 **20%/席**。
- **阳性对照**（本仓规矩：门必须能看见"坏"）：**零权重常数策略 V4 = 3.4%** ⇒ 被 10% 的门**挡下** ✓；线上包 16.1% ⇒ 余量 6.1pt ✓；
  **6 个臂包里 5 个被挡**（1.8/5.1/2.5/5.8/4.0）⇒ 判别力足够。
- **阈值是草案、待用户裁定**（v1.5.104 给 G4 立 45%→60% 时定的规矩：**阈值与标定理由写在一起**）；
  改它只需改 `np-test.mjs` 里的 `V4_MIN` 一个常量（判词自动跟上）。
- 另加两条**单一来源**断言（探针必须 import 该模块、且不许再出现对手名清单）⇒ 这条门不会与被测对象一起漂移。

### 3. ⚠️ 顺带实测出一件**更重要**的事：**V2 这条轴没有判别力**

- 本次量到：**零权重常数策略在 V2 上拿到 10.2%，与线上冠军的 10.0% 一模一样**。
  ⇒ 我上一版草案里的 `V2 ≥ 8%` **连垃圾都挡不住**（近恒真）—— 若照那样立上去，就是一条"**看着在、其实没判**"的门
  （本仓最忌讳的形态）。⇒ 改成**只记录不阻断**，并加一条**判别力自检**（现状必须与地板同档 ±4pt；哪天这条红了
  ⇒ V2 变有判别力了，**那时**再按 G4 的规矩给它标定阈值并升成阻断门）。
- 这不否定 §4-12 的发现，反而把它的解释钉得更死：**R61 之前同一个包在 V2 上是 34.7%（明显高于"什么都不做"），
  现在 10.0% = 就是"什么都不做"** ⇒ 那次引擎改动把它从"会打整局"打回了"跟常数策略一样"。
  同理 §4-4 的"禁狙击 **+15.2pt**"也更好读了：它把 V2 抬到 **25.2%**，即**抬到地板之上**。
- 教训写进 `docs/METHODOLOGY.md` **§F.43**：**给一条轴立门之前，先量"什么都不做"的水平**。

## v1.5.131 — **「用内置冠军」按钮清不掉 2P 的本机冠军**（handoff §4-10）· 顺带让 2P 侧如实标注"我在打哪个包"

> 病（本次逐行核出，原先只记在 handoff §5 的"坑"里）：2P 的本机冠军存在 **`epirus.champion.v3`**
> （`js/train/trainer.js:6`），而 **`useBuiltinChampion()` 只清 `epirus.champion3p`**（3P 键）
> ⇒ **本地训过冠军的用户永远打自己那个旧包**：v1.5.130 刚把线上 2P 冠军从"被珠爆发线 100% 打穿"
> 修到 `examGateOk:true`，**这一整类用户拿不到**；而按钮 tooltip 写着"清除本机自训/导入的冠军、改回内置冠军"= **假承诺**。
> **读数**（`tools/battle-test.mjs` 新增段 · CDP 真 Chrome）：种两个键 → 点按钮 ⇒ **两个键都变 `null`**、
> `对手AI=2人冠军（内置）`、`tr-has=内置冠军 · tools/train-best.mjs · 6×600 代`。
> **反证（跑过）**：把 `ui.js` 那句 `localStorage.removeItem('epirus.champion.v3')` 删掉
> ⇒ 本段 **3 条断言立刻红**（`SUMMARY: BATTLE FAILED`）。
> 其余门禁不变：`spec 45/45` · `np-test 151/151` · `smoke SMOKE OK` · 规则指纹 **`00e732a7`**（**没动引擎**）。

### 1. 修：`useBuiltinChampion()` 清**两个**槽位（`js/ui/ui.js:340`）

- 两个键一起清；提示如实点名清的是哪个槽位（实测：`已清除本机冠军（2 人），改回内置冠军（多人包已加载）`）。
- **2P 侧也要重开一局**（原先只判 `B.multi`）：2P 的 chooser **每次决策都重读** `Champ.store.load()`
  （`ui.js:574`）⇒ 清键**下次决策即生效**，重开只是让这一局从干净状态开始。
- ⚠️ **"2P 老用户"这条路的另一半在测试里**：`battle-test` 原先只**种/只断言 3P 键**（`:118-167`）
  ⇒ 这正是这个 bug 能活到现在的原因。新段把 2P 槽位**镜像**补上（种两个键 → 点按钮 → 断言两个都清）。

### 2. 顺带：2P 侧**如实标注"我在打哪个包"**（同一病根的第 2、3 个症状）

- `aiInfo()`（`ui.js:374`）原先 2P 只写 `'2人冠军'` ⇒ 用户**看不出**是本机旧包还是内置包
  （3P 侧早就有 `本机自训/导入 | 内置` 的标注）⇒ 现在两边对齐（实测 `对手AI=2人冠军（本机自训/导入）`）。
- `renderChampState()`（`ui.js:786`）**原先连内置包也标成"已保存本地冠军"**（`store.load()` 在本地缺失时会回退内置，
  `c` 照样是真值）⇒ "当前冠军"永远说"已保存本地冠军"，用户**看不到"用内置冠军"到底有没有生效**。现在按来源分三种情形。
- **单一来源**：本地/内置的判定**只在** `trainer.js` 的 `store.load()` 里，新增 **`store.lastSource`** 回传
  （与既有 `lastInvalidReason` 同族）—— 不让 ui.js 重判一遍（本仓"同一规则两处维护"栽过四次）。

### 3. 顺带**结案** handoff §4-1 的"5 席侧那半条"：**NP 侧不是洞**（我上一条评估的一处更正）

- 上一版把"5 席侧同一格仍 **30%(long)/15%(multi)**"写成了**遗留缺陷**；本次核实后**不是**：
  G4 的 `duel()`（`tools/gate-drafts.mjs:256-268`）是「1 席脚本 vs 4 席冠军」，而**基线格**（冠军对冠军 5 席）
  实测 **18%(long) / 10%(multi)** ⇒ 珠爆发只是基线的 **1.5~1.7 倍**、**离 60% 阈值（用户裁定 v1.5.104）还差 30~45pt**。
  同一张表里真正红的是「**只枪**」**75% / 62%** —— 那是**已知的 `--force` 例外**（§0b-3 / D67 分支②），不是新问题。
- **本次实跑（n=60/格）**：`G4POOL` 仍是 **`960dfe0b`**（⇒ 改 `evo.js` 的**训练池**确实**没有**动 G4 克制表 ✓）·
  `long` 基线 **18%** · 珠爆发 **30%** · 只枪 **75%** ｜ `multi` 基线 **10%** · 珠爆发 **15%** · 只枪 **62%**。
- ⇒ **不跑 NP 臂、不改 `server/opp-pool.mjs`**：那是给不存在的问题花 11 分钟，还让此后 NP 臂读数不可比。
  这段推理其实**早就在** `gate-drafts.mjs:242-248` 的注释里 —— 教训：**引用旧读数前先读它自己的口径注释**
  （"**覆盖度**" ≠ "**缺陷**"）。另：若将来只为**覆盖度**加这条线，`opp-pool.mjs` 的 `OPP_DEFAULT`
  只取 `OPP_SPECS` 前 9 个 ⇒ **默认臂一字不动**。

### 4. 顺带做完 handoff §4-4：**狙击大 n 复跑**（n=120 → 600）—— 效应**确认且更大**，但查出一张**过期的表**

- 量具 `tools/probe-ring-ablate.mjs`（同 seed 7777、同轮座、只把 chooser 输出的那张卡换成ジ、逐局配对、符号翻转精确检验），
  键 = `ABLATE_KEY=snipe`。**本次实跑 600 局/臂**：

  | 装配 | 基线 | 禁狙击后 | 差 | 非零配对 | p |
  |---|---|---|---|---|---|
  | V1（1 席 vs 4 random） | 85.3% | 61.3% | **−24.0pt** | 204/600 | **7.3e-26** |
  | **V2（1 席 vs 4 balanced）** | **10.0%** | **25.2%** | **+15.2pt** | **103/600** | **3.0e-22** |
  | V4（4 席自家族 vs 1 balanced） | 16.1%/席 | 15.8%/席 | −0.3pt | 239/600 | 0.698 |

- ⇒ **§4-4 的疑问（"+5.8pt 是不是小 n 噪声"）有答案：不是噪声** —— n=600 下它**长到 +15.2pt**、
  非零对 **7/120 → 103/600**、p **0.016 → 3e-22**。方向与记录一致（**狙击在"打整局"场里净亏**），**量级更大**。
  V1 的 −24.0pt（p=7e-26）同时证明**量具仍然是活的**（阳性对照有效）。
- ⚠️ **顺带查出的事**（比上面那条更重要）：记录里的 **V2 基线是 34.2%**（`docs/AUDIT-2026-09-19-post-merge.md:130/138`），
  而本次**同工具 / 同 seed / 同装配 / 同包 / 同模式（`multi` 是硬编码）**读到 **10.0%**。差异的**唯一来源是引擎指纹**：
  审计那批自述跑在 **`61a7711c`**，此后**只有 `e7fc9fd`（v1.5.129 的 R61）动过引擎** ⇒ 现在是 **`00e732a7`**；
  工具自 `5374535` 起未再改，包 `v7press3-91` 权重逐字节未变。
  ⇒ 按 `tools/rules-fingerprint.mjs` 自己的规矩，**那张表已「过期」**；而 **D16 只守两个包的 meta，守不到文档里的表**。
  **✅ 当天就查清了**（独立 worktree @ `2ccd558`——R61 之前——跑同一命令，**引擎是唯一变量**；不动主工作区、不碰 `results/`）：
  V2 **34.7% → 10.0%** · V1 85.5% → 85.3% · V4 17.2 → 16.1%/席 ⇒ **那 24pt 就是 R61**；
  且 V2 里的**狙击出手数 1132 → 716 次**（−37%）⇒ **行为真的变了**，不只是数值漂移。
  ⇒ **第二个结论（更重要）**：**引擎一改，所有"在旧语义下训出来的包"都成了次优解** —— `v7press3-91` 就是这种包，
  而 **V2/V4 没有任何门** ⇒ 这 24.7pt 静默丢了两个版本没人发现。补救两条：**重训**（在当前引擎下）或**明确接受并记录**；
  但**判断"有没有变差"只能用同引擎的数字**（跨指纹的绝对值不可比）。⇒ 新待办 HANDOFF **§4-12**（建议与 §4-3 合并做：先有量具再换包）。

### 5. §4-12 的第一条臂：**"重训就能追回那 24.7pt" 被否证**（同引擎对照，n=600）

- 臂用 §3 的标准命令（池子字符串与线上包 meta 的 `opps` **逐字相同**）⇒ **唯一变量是引擎**。
  6 seed 全部"有提升"（工具自评），**线上包未被训练改动、结束已还原**（`git status` 里 `js/bundled-champion-3p.js` 无改动 ✓）。
  产物 `docs/artifacts/r61fix-{31,81,82,91,92,93}.bak`。
- **同一次运行内**对照 V1/V2/V4 的每席位胜率（`probe-ring-ablate.mjs 600`，同 seed）：

  | 包 | V1（vs random） | V2（vs 4 balanced） | V4（同族 4 席 · 产品形态） |
  |---|---|---|---|
  | **现包 `bundled-champion-3p.js`** | **85.3%** | **10.0%** | **16.1%/席（七个里最高）** |
  | r61fix-31 / 81 / 82 / 91 / 92 / 93 | 49.0 ~ 85.5% | **0.0 ~ 20.0%** | **1.8 ~ 15.0%/席** |

- ⇒ ① **V2 上"重训"不是解**：6 个 seed 落在 **0.0 ~ 20.0%**（最好的 81 = **20.0%**，是现包的 2 倍；**中位数仅 ~2%**）
  ⇒ 这条轴由 **seed/协议方差**主导，**不是"旧权重过时"这一个原因**。
  ② **V4 上新包全部 ≤ 现包** ⇒ **本条臂没有一个候选值得 promote**。
- ⇒ 真正的修法是让 **V2/V4 变成判据**（= **§4-3**）：它们现在**既不在臂的选择标准里、也没有门** ⇒ 臂把包送到哪里就是哪里。
  这与 §4-3 原本的动机（"满桌同包无人量"）是**同一件事的第二条证据**，而且这次带着**一次真实的 24.7pt 损失**。
- ⚠️ 同一张表还读出一个**形态警告**：`r61fix-93` 在 V1 里 **ring 出手 10389 次**（现包 386 次），
  且**禁环反而 +33.5pt**（p=2.1e-54）⇒ "环上头"的退化包能**通过现有的选择标准** —— 又一个"这条轴没有门"的例子
  （对照 §4-2：环消融卡在"要有既打环又会花贵卡的包"，现在手上多了一个**反面**样本）。
- **记账（D82）**：`r61fix-31` / `r61fix-81` / `r61fix-82` / `r61fix-91` / `r61fix-92` / `r61fix-93`
  （`.bak`，共 6 个）+ `ring2-*-r61fix.log` 已入 `docs/artifacts/`；**未 promote**。


<!-- v1.5.145（DS 整理 · 用户要求「changelog 压缩一下」）：v1.5.130 **之前**的条目已压成下面这份一行索引；
     正文没丢 —— 全文在 git 历史里：`git log -p CHANGELOG.md`（或 `git show <旧提交>:CHANGELOG.md`）。
     保留界限 = v1.5.130：它**及之后**的条目逐字保留（D82「24h 内落盘的 .bak 必须在 CHANGELOG 被点名」
     只看最近一天的产物，而最近一天的条目都 ≥ v1.5.130）。 -->

## 历史版本索引（v1.5.0 ~ v1.5.129 · 已压缩 · 共 181 条）

- v1.5.130 — **2P 冠军的「珠爆发」洞堵上了**（池子 + **热启动** + **不回归层**）· 择优工具的一个**假承诺**被实测反例修掉
- v1.5.129 — **R61**（被无效化的一手不写 `lastSkill`）· 复核实锤的「**珠爆发**」线进池 · **D67 改判为"已记录的例外"**
- v1.5.128 — 门禁清理（用户指示）：**删自指门 · 把脆弱的源码文本钉换成运行时检查**
- v1.5.127 — 贵卡奖励臂 `v7big1` 实测：**机制成立但稀疏**（1/6 seed 长出卡面）+ 环消融的"合格实验"**仍未满足**
- v1.5.126 — **贵卡出手奖励**（`EPIRUS_BIGCARD_W`，默认关）：先让它学会用"那几张它不会用的卡"
- v1.5.125 — 两臂实测：**广度收益项（0.2）失败**（推向"乱打"）· **热启动换新冠军成功**
- v1.5.124 — **广度收益项**落地（`EPIRUS_WIDTH_W`，默认关）+ 热启动换成新冠军 + 环消融的**混淆更正**
- v1.5.123 — **换包**：`v7press3-91` 上线（用户裁定：优先"广度 + 可玩性 + 场B 清场"）
- v1.5.122 — E4 臂实测：**奖励"挡下"没能买回 G4，反而把包推向龟**（论点否证）
- v1.5.121 — **E4 落地**：给"挡下伤害"独立计分（`EPIRUS_BLOCK_W`，默认关）
- v1.5.120 — `CLEAR_W` 剂量对照：**场B 清场 ↔ 抗只枪压力是单调互斥的**（第三次独立确认）
- v1.5.119 — 加压批次（池里把"门禁对手线"加权）：**动了行为门，但代价落在清场**
- v1.5.118 — **R24 原型制御"≥3 各自转移"按用户口径重做**（改成决算式）+ 铁索传导豁免
- v1.5.117 — 修正特征后的重训（`v7fix1`）+ **无教师对照**（`v7noteach1`）：教师有用，但行为门照旧
- v1.5.116 — 用户实盘报的两个 bug：**原型制御 ≥3 未转移**（R24 从未实现）+ **"无根据集火"的根因是死特征**
- 第三方复核者代记 · 2026-09-19 通宵（**未发版：不 bump 任何版本号**）
- v1.5.115 — 第三方复核者代记（用户委托动手）：修 `D59` 的小样本红 + **两臂恢复聚能环实验全部否定**
- v1.5.114 — **换包**：`v7new5_005-31` 上线（唯一通过行为门的产物；`--force` 越过 1 条并留痕）
- v1.5.112 — 重训批次记录：**给训练池补上"只枪"压迫线**值多少（配对实验）
- v1.5.111 — 环的判据换成**段长口径**（复核 §4）+ 地雷"再埋"的措辞裁定
- v1.5.110 — **R60**：净化清除**自身全部持续状态**（含增益）；写入点收成一处
- v1.5.109 — **R59**：地雷改成"**当前回合 + 其后两个回合**"的自身 buff（用户裁定，取代 R38 的"持续直到被触发"）
- v1.5.108 — **R58**：藤甲火弱改成"**贴上当回合 → 下回合结束**"（用户裁定）⇒ 多覆盖一回合
- v1.5.107 — **R57**：天火**不需要目标**（用户报的 UI bug 根因）；⚠️ 并发现**规则一改，线上冠军退化成龟壳**
- v1.5.106 — **R56**：同层内**资源型技能先结算**（用户裁定）⇒ 过载炮的清除**含目标本回合收入**
- v1.5.105 — 修 R10：**被无效化的聚能环不续计**（用户裁定；打断者不再给对方送 +3）
- v1.5.104 — **换包**：`v7seat24-31` 上线（G4 阈值经**用户裁定** 45% → 60%）
- v1.5.100 — 门地形量具 + 两条候选的判定（第十轮复核 `AUDIT-RESPONSE-v1.5.100.md` 的采纳与不采纳）
- v1.5.99 — `EPIRUS_IMIT_SUB_ONLY`：**"只教目标卡"的示范只在补贴局里发生**
- v1.5.98 — **只示范目标卡**（`only`）：让示范只作用于"教师真要教的那张卡"
- v1.5.97 — **分段教师计划**（两段课程）：`EPIRUS_IMIT_PLAN="教师:占比,…"`，每段各自退火
- v1.5.96 — **真示范**（override）：让策略真的走一遍教师那条线；教师可按名字选；D84 守门
- v1.5.95 — "转化率"探针（第九轮复核 §5-2/§7-2 的验收量具）：把**收入饥饿**与**转化拒绝**分开数出来
- v1.5.94 — 采纳第九轮复核（`docs/AUDIT-RESPONSE-v1.5.93.md`）：**补记最后 12 个产物** + 退化包 reject + A 降级为诊断 + 记账守门 D82
- v1.5.93 — 类间那一层换成**功能角色表**（8 角色，从 `rules.js` 声明字段推导）+ 试验臂加到 6 seed
- v1.5.92 — 类间广度权重 `DIV_CAT_W`：可给"**跨角色**广度"加权（默认 0 = 旧口径逐位不变）
- v1.5.91 — 技能广度 **S 的权威定义**（`目标 = H + T·S`）：按 `rules.js` 声明的 `cat` 分层，**不按伤害加权**
- v1.5.90 — 把"输出密度 / 经济出口"（第八轮复核 §6 的**命门**）机械化：新探针 + 第 6 道判据（**只记录**）+ 压制密度对手进池
- v1.5.89 — 修"经济/熵奖励 env 到不了 worker"的**静默半开**（单一真源 + 回执 + 守门 D77）+ 两条被作废的臂拿到真读数
- v1.5.88 — 退火强迫 + 破墙硬过滤（默认关）+ 落盘阻断开关
- v1.5.87 — 熵口径与门禁对齐（用户裁定 A）+ "固定分母"的因果验证
- v1.5.86 — 熵项固定分母（病因 + 修法）+ 对手注册表第 3 次"加名字漏一处"
- v1.5.85 — 温度 T（熵权重）的现状与曲线；顺带撤回一组无效的 2P 读数
- v1.5.84 — 结论：本规则下"上环 / 存钱放大招"**更弱**（先验收益，未训练）
- v1.5.83 — 珠子闭环诊断（只读）：**机会几乎不出现，出现了又选不中**
- v1.5.82 — 蓄能经济门槛（用户裁定）：ep<2 不许蓄能；顺带更正"天火是珠子卡"的错误
- v1.5.81 — 诊断：冠军"放不出强技能"的机制（**只诊断，未改行为**）
- v1.5.80 — UI 三条（第八轮复核 §5）：先在真页面复现，再按证据修 —— ① 可取消 ② 有反馈 ③ 不可复现
- v1.5.79 — 威胁靶向奖励的第一版是**静默空操作**（已修）：v7tgt4 整臂读数作废
- v1.5.78 — 第七轮复核（`docs/AUDIT-RESPONSE-v1.5.77.md`）的采纳、独立复现与自我更正
- v1.5.77 — 快记：带 P1 的重训（`v7f3`）结果 —— **P1 目标达成（蓄能白扔消失），但强度/座位仍是取舍**
- v1.5.76 — P1：给"把蓄能珠花掉"加窄奖励（只认花掉，两次封顶）
- v1.5.75 — F1+F2 后的重训裁定：**250 代不够、500 代治好了 G 塌缩**；全息屏障的偏爱消失，珠子浪费仍在
- v1.5.74 — F1 + F2：候选表只收买得起的招 + **两级采样**（技能层 → 条目层），槽位不再等于概率
- v1.5.73 — 两个探索性问题的机制级答案（用户实测 4 局后提出）：**候选"槽位数"直接变成采样概率** + **买不起的招也在候选表里**
- v1.5.72 — 线上冠军换成 `v7tgt2-52`：**三条臂里唯一全过门的候选**（且首次未用 `--force`）
- v1.5.71 — 第五轮复核的**三条更正全部接受**：门槛收进单一真源 + 座位判据的样本量守卫 + 场B 判据钉死为清场数
- v1.5.70 — v17 裁定入档：座位惩罚生效后的真实结果 + 设计结论（**未再盲改**）
- v1.5.69 — 修 v1.5.68 的**静默空操作**：座位惩罚从未触发（我自己的错）+ 噪声地板标定
- v1.5.68 — 让演化**看得见偏置**：阈值式座位惩罚（用已经在打的 mirror 局，零额外成本）
- v1.5.67 — 五道上线门槛做成"**决定产物的那一行**"的可行性判定（复核 §4 的 ④）
- v1.5.66 — L7 第七处：候选**目标枚举顺序**也是身份通道 —— ⚠️ **v1.5.71 降级**：它是"通道之一"，不是成因
- v1.5.65 — 场 B 的两块前置：**训练暴露度**（①）+ **multi 终局收缩与全灭判胜**（②）
- v1.5.64 — 修"换回来的不是 eco-34"（容器显式旧口径标记 + 等价性守门）+ 训练零暴露度诊断
- v1.5.63 — 换回 **eco-34**（v7 嵌入版，用户裁定）+ 旧形状包上线通路打通 + CHANGELOG 精简
- v1.5.62 — **F 正式换成"场 A 被集火还手率"（≥20%）**（用户裁定）+ eco-34 历史记录核查
- v1.5.61 — F 的口径重做（双场实测；**门槛未改**，据用户质疑）
- v1.5.60 — 覆盖熵权重旋钮 `EPIRUS_DIV_W`（训练侧；默认不设 => 行为一字不变）
- v1.5.59 — 蓄能空转量化（`wastedChargeRate`）+ 反弹墙真信号 + 候选筛选器；换冠军**有据暂缓**
- v1.5.58 — 体检并报**严格胜率**（第五轮复核 §4-1/§4-2）：`1st` 把并列算第一，已可见
- v1.5.57 — 第五轮独立复核（Qoder）的修正：**座位对称性进体检** + 体检口径/表头修复 + 我的一处单位错误
- v1.5.54 — 引擎兜底改为**按每局盐轮换**（L7 第四次）：无显式目标时不再恒定取最小索引
- v1.5.53 — 结算起点改为**每局随机相位**（L7 的第三次应用）+ 决定性诊断：**偏置是训练造出来的**
- v1.5.52 — 槽位顺序改为**纯函数哈希**（不再借任何随机流）；**双目标技能有序成对进入决策**
- v1.5.51 — 槽位顺序改为**每次决策内一次随机洗牌**（破"确定性身份映射"；本会话第三次同类修正）
- v1.5.50 — 座位偏置二分（6 seed/格）：**泄漏不在三个特征块里**，转向训练动力学 + 新线索"0 号座 3%"
- v1.5.47 — 换冠军 `v7seat2-51`：**去座位偏置** + 首个"零 `--force`"的干净换冠军
- v1.5.46 — **修座位偏置**（复核 §3，最高优先级）：并列不再按 pid 升序
- v1.5.45 — **回退冠军到 `v7wall-31`**：v7r2-35 的"会打断环"被复核证实是**第 4 回合仪式**
- v1.5.44 — **换冠军 `v7r2-35`：首个"会打断环"的冠军**（供用户/复核实测）
- v1.5.43 — ② 的负结果 + 18 种子批量："逃过冻结"≠"学会打断环"，堵点在**选择哲学**
- v1.5.41 — "打断环"**配方确认但很脆弱**（窗口 20/40/80 对照）+ 七次尝试谱系
- v1.5.39 — 定向强制探索：**两臂都因"算术陷阱/训练冻结"作废**（结论：探索问题仍未测到）+ 结构障碍入库
- v1.5.38 — "打断环"第五次尝试：**信号层修好后依然是 0**（干净的否定结果，进入最后一枪）
- v1.5.37 — 按第三方第三轮复核修"可归因"与"全息幻影候选"（**都不动参数量、不升 PACK_VERSION**）
- v1.5.36 — 新增审计建议的两列 H/I（4 风格同场 / 1 冠军 vs 4 只ジ），**并标记口径待对齐**
- v1.5.35 — 终局平局口径修正（用户裁定）+ battle-test 看门狗止血
- v1.5.34 — 根因修复："全息屏障送盾"不是学坏了，是**目标没进决策**（用户质疑换来的正解）
- v1.5.33 — 训练"静默冻结"检测（`tools/zero-promote-check.mjs`）：零提升不再无声无息
- v1.5.32 — 换冠军 `v7wall-31`（换掉"送盾给对手"的 v7wall-33）+ **新增不可 `--force` 越过的硬门槛**
- v1.5.31 — "打断环"课题：**动作级手段全部打满**（奖励 / 池子 / 课程示范 / 暴露度），结论转"结构路"
- v1.5.30 — 换冠军 `v7wall-33`（修掉 D 列退化）+ 破墙奖励（方案 b）+ **"打断环"三次尝试的实测结论**
- v1.5.28 — 修门禁（方案 1+2）：**反弹墙穿透卡零命中 = 阻断** + 自对局穿透条件降级为提示
- v1.5.27 — **换冠军：v7press-36**（首次"标准持平 + 打环翻倍 + 多样性翻倍"的干净提升）+ F 门槛重标定
- v1.5.26 — **重定义 E 指标**（方案 B）：只把"没有大雷威胁时还一直摆架势"算病
- v1.5.25 — 惩罚被动的窄奖励（方案①）+ **出现首个"两个都要"的候选 v7press-36**
- v1.5.24 — 环奖励**退火**（方案 A）+ 实验结论：打环能力练出来了，但仍是"偏科"
- v1.5.23 — 修"死亡结算跑到伤害之前" + **训练信号：教它惩罚开环的人**（首次真正生效）
- v1.5.22 — 结算事件行的**显示顺序**（用户澄清的"防御优先"真意）+ 技能格按钮回到规则顺序
- v1.5.21 — 页面回合列表/导出三处修正 + 底部技能格恢复"防御优先"
- v1.5.20 — UI 两处修正（连带电伤文案 / 技能格子顺序）+ 指纹重记 + README 瘦身
- v1.5.19 — 一次性加全参数量（v7 特征）：候选 =(技能,目标,珠类型) + 关系块 + 效果快照块
- v1.5.18 — 处置第三方独立复核：A 批（工程/门槛）+ B 批（规则层定稿）
- v1.5.17 — 镜面反射"生效时机"改回优先级口径（用户二次裁定：大雷先结算 ⇒ 复制不会发生）
- v1.5.16 — 镜面反射回归原始规则：**没有"无可复制"**（非伤害类复制到自己身上 + 对 t2 空指）
- v1.5.15 — 修蓄能珠类型的信息泄漏（三处）+ 技能格子显示顺序改成防御类优先
- v1.5.14 — 先手激励实测（能治"集体防御"但代价太大）+ 冠军体检新增 E/F 两列
- v1.5.13 — 修狙击枪"被干扰"判定（多人局 bug，用户对局记录发现）+ 口径澄清（冠军 vs 风格脚本）
- v1.5.12 — 回滚"长程默认开哨声惩罚"（实测是**空操作**，证据：两条臂产出逐字节相同）
- v1.5.11 — 终局收缩参数化（起点/每回合扣血可在页面调）+ 长程训练默认开哨声惩罚 + 换冠军自动跑技能报告
- v1.5.10 — 终局收缩升级为**全局规则**（100 回合后每回合全员 −1 血）+ 页面新增「用内置冠军」按钮
- v1.5.9 — 研究结论入库（哨声惩罚实测 + 防御型池子在 multi 下不划算）+ 对战测试覆盖长程模式
- v1.5.8 — 熵只统计非ジ + 反摆烂哨声惩罚 + 浏览器对战测试 / 冠军体检（上线冠军 long-33 → eco-34）
- v1.5.7 — 线上冠军换成 `long-33`（修 v1.5.4 造成的成绩崩塌）+ 全息屏障在 2P 灰掉 + 规则指纹守门
- v1.5.6 — 经济 shaping 门槛按 (人数,模式) 参数化（用户锚点）+ 恢复技能熵奖励；2P 体检
- v1.5.5 — 全息屏障第二轮修正：**被小雷无效化的时机**（漏读 `voided`）
- v1.5.4 — 规则修正：**全息屏障是"给别人套的盾"**（此前被做成了原型制御的重复品）
- v1.5.3 — 复合适应度（把风格表现写进 fit）；以及那条"已被记过一次"的错再犯一次 + 更正技能报告的假信号
- v1.5.2 — 训练池能放「风格化冠军」当对手；以及"加名字漏一处"的第三次
- v1.5.1 — 技能报告补上「模式」口径；它顺手揭开了挂很久的 P0-d 悬案
- v1.5.0 — 长程（5 血）冠军终于能训练了（接线 + 两道守门 + 一个新坑）
- v1.4.14 — 第三次『加名字漏一处』：对手池补 `ringspam` + 未知名字**开跑前中止**
- v1.4.13 — 三件卫生事项（用户批准）
- v1.4.12 — 多 seed 配对（n=6）：加反弹墙无显著影响 ⇒ 不改默认池、不换冠军
- v1.4.10 — 消融（`--ban`）成为 report 的主口径
- v1.4.11 — 用户纠正：**小雷不是陷阱卡**，它的价值是反制（我错在考卷里没有"环"）
- v1.4.9 — 对手池抽成单一来源（静默漂移坑）+ 反弹墙实验的诚实结论
- v1.4.8 — 终局收缩替代硬截断（用户方案）+ 查清"反弹墙为什么不在训练池里"
- v1.4.7 — 处置第十轮独立复核：确认反弹墙(P0-c) + 更正我两处结论 + 修 3 处小问题
- v1.4.6 — report 自己说清"哪些测不了、该用什么命令" + 一键入口 + REVIEW-5P
- v1.4.5 — 补齐"测不了"的四个口径：消融/开珠/连招/条件注入
- v1.4.4 — 多 seed 配对 A/B（n=4）：12 对手均值 +4.9pt 且方差更小；skill-report 支持多版本对比
- v1.4.3 — 对手池 A/B（从零冷启动）：符号 2/2 支持加对手，但**幅度未定**
- v1.4.2 — 更正 v1.4.1 的归因：38.4% → 21.8% **不是**轮换造成的
- v1.4.0 — 长程模式（5 血）：拉长赛程**只能解冻一部分**被锁死的卡
- v1.3.60 — 条件触发型技能的专用器材 + 用户纠正的符咒机制
- v1.3.59 — skill-report「一堆死技能」主要是**报告自己的测量失败**
- v1.3.58 — 首个 5 人冠军：3 人与 5 人**同时**变强，且座位中性
- v1.3.57 — 座位效应与平局问题：查出**两个方向相反的**成因并都修掉 + 首个 5 人冠军
- v1.3.56 — 查实「浏览器训练不可复现」的真因（**并纠正我自己在 P3.3 写错的归因**）+ 补推送
- v1.3.55 — 搭好 5 人局评测（P3.5「改考卷」），而**它否掉了「等 5 人局」这个方向**
- v1.3.54 — 深经济"货架问题"从猜测变成**有效证据**；(c) 方案落地但门槛默认关闭
- v1.3.53 — 按 Claude 的复核：把能自动化的教训全部改写成会失败的用例
- v1.3.52 — WR_TOL 改显式参数 + repro-check 补 D/E 断言（千问建议）
- v1.3.51 — 修过期日志文本：打印真实 `OUT_PATH`（原硬编码线下路径，**正是它误导我**）
- v1.3.50 — 修 CLI 不可复现的真正根因（千问定位）：**热启动读自己的产物**
- v1.3.49 — train-3p 不可复现：排除三个假设（**仍未找到根因**）
- v1.3.48 — 任务2 完成：训练工具不再覆写线下冠军；任务1（train-3p 可复现）**未解决**
- v1.3.45 — N23 激光眼单发即破整个防御族（用户裁定）
- v1.3.44 — 修 v1.3.43 的回归：`BIG_T_EXEMPT` 扩张撞坏 R23'（spec 36/37 → 37/37）
- v1.3.43 — N22 大雷效果传导：按用户裁定实现（防御族格挡 / 沿作用目标传导 / 反弹只挡不反击）
- v1.3.42 — 修大雷结算：豁免小雷（旧代码对照 + 用户裁定）+ **发现并修正一个空用例**
- v1.3.41 — 集火脚本 `pickFocusFire` + 转移伤害的**决定性**结论：当前 meta 下不可用
- v1.3.40 — 转移伤害诊断：**能赚的充要条件是"单回合承伤 N≥2"**（用户直觉被算术证实）
- v1.3.39 — 新增 `tools/repro-check.mjs`：可复现性看门狗（千问建议）
- v1.3.38 — 噪声下限复测：**千问的预期没有应验**，但这个比较本身有缺陷
- v1.3.37 — worker 分种子：结果与 worker 数无关 + 个体间独立（千问跨机问题 ①②）
- v1.3.36 — 训练可复现：千问定位到两处 Node 作用域的随机/时间源（**我的假阳性也被证实**）
- v1.3.35 — 火弱持续性：训练侧模拟（3×200）→ **无可测效果**，且意外成为噪声下限标定
- v1.3.34 — WR_TOL 多跑对照（各 3 次）：**0.03 胜出，我之前的收紧是错的**
- v1.3.33 — WR_TOL 单变量对照：**单跑一次分不出差异，方差盖过效应**
- v1.3.32 — 补齐 N20 用例（35/35）+ 用新规则重训一次 AI 的实测结论
- v1.3.31 — 修 v1.3.19 引入的双枪 `via` 回归（N20b 关闭）+ N20 全绿
- v1.3.30 — N20 地雷 AoE 重写（用户裁定）+ 3 条 N≥3 spec 用例
- v1.3.29 — 地雷触发改为「近 5 回合架势频率 + 对手当前 ジ 数」（用户方案）
- v1.3.28 — 地雷改用「历史频率」触发（方案 a）；`lastSkill` 滞后问题的确认
- v1.3.27 — P1：深经济对手 `deepsaver`（先写脚本 → 实测它真的会攒）
- v1.3.26 — 冠军换成 div555（v5→v6 无损升级）+ 工具回归确认
- v1.3.25 — P0：存档版本兼容（形状自适应）→ A/B 证据链恢复
- v1.3.24 — 修 v1.3.23 引入的回归：冠军包版本不兼容 + 模仿默认关闭
- 附录 A：当前有效契约 / 常量（改这些必须同步守门）
- 附录 B：训练问题复盘（2026-09 全期；用户要求）
- 附录 C：方法论规则（踩过的坑 → 规则）
- 附录 D — 熵/温度 T 与"原型制御空白"（v1.5.86 的分析与读数，已折进上面的版本条目）
- 附录 E — 熵项分母修正落地 + 对手注册表修复（v1.5.86 的细节与读数）
- 附录 F — 固定分母的**因果验证**：同样 T×5，旧分母 0 产物、新分母 2/2 健康产物
- 附录 G — 固定分母修好后的候选：**过全部行为门 + 多样性更高 + 强度不掉**
- 附录 H — v1.5.89：经济/熵奖励 env 的**静默半开**已修（第八轮复核 §2 的落地）+ 两条被作废的臂拿到**真读数**
- research(qoder 0920 深夜) 语义修第二轮：转移/地雷/原型制御UI（指纹 a78c28cb→de02be9e · 不升版本）
- research(qoder 0920 深夜2) 狙击×转移裁定 + 运行时 ε-greedy（指纹 de02be9e→72a9c1b5 · 不升版本）
- research(qoder 0920 晚班2) 换包 v7n1-93 + D105/V2 标定升级 · 不升版本（main 未动）- §N8 臂产物点名：v7xn10a-31.bak（**空转臂=7′ 的逐字节复现**，因 EPIRUS_CLEAR_W 在 CLI 无人读；留档作反例）、v7xn10a-2psh.bak（考卷用壳副本）、v7xn10a-band1..6.bak、后续真臂 v7xn10b-31.bak 与其 band*。
- §N8 臂产物点名（结案）：v7xn10a-31/2psh/band1..6、v7xn10b-31/2psh/band1..6（CLEAR_W 生效档=否证：退化龟包当选 ⇒ train-3p 当选面无退化闸）、v7xn10c-31/2psh/band1..6（作废档=第六例静默空转：train-3p 沙箱无 process ⇒ PASSIVE_FIELD 类 env 全黑）。详见 RESEARCH-LOG-2026-09-22-qoder-night.md §N8。
- §N8 全名补点（D82 逐字 · 24h 窗口）：v7xfer44-2p、v7xfer44-3psh、v7xfer44b-2p、v7xfer44b-3psh、v7xfer44b-band1、v7xfer44c10-2p、v7xfer44c10-band1、v7xfer44c11-2p、v7xfer44c11-band1、v7xfer44c11b-2p、v7xfer44c11b-band1、v7xfer44c11b-band2、v7xfer44c12-2p、v7xfer44c12-band1、v7xfer44c12-band2、v7xfer44c12-band3、v7xfer44c13-2p、v7xfer44c13-band1、v7xfer44c13-band2、v7xfer44c13-band3、v7xfer44c6-2p、v7xfer44c6-band1、v7xfer44c7-2p、v7xfer44c7-band1、v7xfer44c7-band2、v7xfer44c7-band3、v7xfer44c8-2p、v7xfer44c8-band1、v7xfer44c9-2p、v7xfer44c9-3psh、v7xfer44c9-band1、v7xfer44c9-band2、v7xfer44d6w-2p、v7xfer44d6w-band1、v7xn10a-2psh、v7xn10a-31、v7xn10a-band1、v7xn10a-band2、v7xn10a-band3、v7xn10a-band4、v7xn10a-band5、v7xn10a-band6、v7xn10b-2psh、v7xn10b-31、v7xn10b-band1、v7xn10b-band2、v7xn10b-band3、v7xn10b-band4、v7xn10b-band5、v7xn10b-band6、v7xn10c-2psh、v7xn10c-31、v7xn10c-band1、v7xn10c-band2、v7xn10c-band3、v7xn10c-band4、v7xn10c-band5、v7xn10c-band6、v7xn2-31、v7xn2-31-band1、v7xn2-31-band2、v7xn2-31-band3、v7xn2-31-band4、v7xn2-31-band5、v7xn2-31-band6、v7xn6-2p、v7xn7a-31、v7xn7a-31-band1、v7xn7a-31-band2、v7xn7a-31-band3、v7xn7a-31-band4、v7xn7a-31-band5、v7xn7a-31-band6、v7xn7b-31、v7xn7b-31-band1、v7xn7b-31-band2、v7xn7b-31-band3、v7xn7b-31-band4、v7xn7b-31-band5、v7xn7b-31-band6、v7xn7c-31、v7xn7c-31-band1、v7xn7c-31-band2、v7xn7c-31-band3、v7xn7c-31-band4、v7xn7c-31-band5、v7xn7c-31-band6、v7xn8a-31、v7xn8a-31-band1、v7xn8a-31-band2、v7xn8a-31-band3、v7xn8a-31-band4、v7xn8a-31-band5、v7xn8a-31-band6、v7xn8b-31、v7xn8b-31-band1、v7xn8b-31-band2、v7xn8b-31-band3、v7xn8b-31-band4、v7xn8b-31-band5、v7xn8b-31-band6、v7xn8c-31、v7xn8c-31-band1、v7xn8c-31-band2、v7xn8c-31-band3、v7xn8c-31-band4、v7xn8c-31-band5、v7xn8c-31-band6、v7xn9c-31、v7xn9c-31-band1、v7xn9c-31-band2、v7xn9c-31-band3、v7xn9c-31-band4、v7xn9c-31-band5、v7xn9c-31-band6。
- §N9 落地（D121）：train-3p 当选面退化闸（rejectDegenerateWinners 纯函数 + densityProfile 单一来源 + degenerateOnlyWinner meta）；真反例 xn10b 当选者 zeroAtkRate=100% 复验会被剔除、现役 0% 不误伤；np 168/168。本程无新产物（smoke band 走 /tmp）。

