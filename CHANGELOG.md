## v1.5.59 — 蓄能空转量化（`wastedChargeRate`）+ 反弹墙真信号 + 候选筛选器；换冠军**有据暂缓**

### 1. 新探针 `chargeProfile`（`tools/audit-lib.mjs`）+ 体检列 `蓄能/局` / `珠浪费`
事件是真源（先打印过，未猜字段）：`{type:'action',key:'charge',outcome:'ok'}` / `{type:'bead',kind,delta:±1}` / `{type:'beadExpire'}`。
`wasteRate = 过期珠 / 得珠` ⇒ 用户实感的"蓄能→珠过期→再蓄能"变成可量化指标：

| 候选 | 蓄能/局 | 珠浪费率 |
|---|---|---|
| **线上包**（v7wall-31）| **17.2** | **84%** |
| `v7seat2-51` | **1.8** | 83% |
⇒ 线上冠军每局蓄能 17 次、**84% 的珠白攒**；候选只蓄能 1.8 次/局（基数小 10 倍）。

### 2. D 列（反弹墙）的口径真相
`1st` 在这个场里几乎全是并列：线上包 **1st=100% 但严胜仅 12.5%（并列 87.5%）**、候选 1st=32.5%/严胜 0%。
⇒ 真正有判别力的是**反弹墙伤害/局 + 穿透命中数**（新增列），而不是 1st 或严胜。

### 3. 新工具 `tools/screen-champ.mjs`（候选筛选器）
一次量完全部硬指标：座位极差（`seatSymmetry`）/ 反弹墙伤害与穿透命中（`reflectWall`）/ 活跃场进攻（`fieldRate`）/ G。
**判据**：座位 ≥30pt 判偏 · 反弹墙伤害 ≤0.5/局 = "反弹墙瘫" · 活跃场 <25% = 不进攻 · G <3 = 打法坍缩。

### 4. 换冠军：**有据暂缓**（实测表见下）
| 候选 | 座位极差 | 反弹墙伤害/局 | 穿透命中 | 活跃场 | G | 结论 |
|---|---|---|---|---|---|---|
| 线上包 v7wall-31 | **61pt** ✗ | 7.25 | 84 | 22% ✗ | 5.15 | 偏座 + 不进攻 |
| v7seat2-51 | **9pt** ✓ | **0.00** ✗ | 0 | 29% ✓ | 3.47 | 反弹墙瘫 |
| v9-36 | 25pt ✓ | 17.75 ✓ | 355 | 37% ✓ | **1.70** ✗ | 只差 G |
| v9-31 | 29pt ✓ | 17.80 ✓ | 356 | 32% ✓ | **2.46** ✗ | 只差 G |
| v9-46 | 39pt ✗ | 20.00 ✓ | 400 | 33% ✓ | 1.67 ✗ | 偏座 + G |
⇒ **无候选三全**：高 D 的候选全部 G<3（打法坍缩）；G 高的线上包偏座且不进攻。故**暂缓换包**，先训练找三全候选。

### 5. 附带证据：健康门槛确实在"整臂拦下"
`v10` 臂（6 seed，未开 `EPIRUS_ALLOW_HEALTH_FAIL`）**6/6 全被拦、0 产物**；`v9` 臂（开开关）12/12 有产物
⇒ 训练候选池的可得性完全取决于该开关（`v1.5.56` 引入，默认关）。

### 6. 门禁
`np-test 102/102`。

---

## v1.5.58 — 体检并报**严格胜率**（第五轮复核 §4-1/§4-2）：`1st` 把并列算第一，已可见

### 1. 病（复核实测 + 我复现）
`tools/eval-5p.mjs:311` 的 `1st` 走 `T.rankOf`，其排序键 `(存活, hp, taken, dealt)` 在平票时按本局种子洗牌
⇒ **被动场里"多打出 1 点伤害"就能拿第一**。我在 farmerwall 上实测：

```
[冠军] 1st=97.5%   严胜=0.0%   并列=97.5%
```
⇒ **"I 对被动 95~97%"整列都是并列洗牌的产物**，不代表"会打被动场"（引擎真正判胜 0~4%）。

### 2. 修（便宜、不动历史列）
`eval-5p.mjs` 每列并报三个数：`1st(并列计) / 严胜(引擎判胜) / 并列(名次第一但没判赢)`；
`tools/audit-lib.mjs` 的 `exam()` 解析出 `strict`/`tie`；`tools/champ-audit.mjs` 体检表新增 **A严胜** 列。

### 3. 读数（本版实测）
| 场 | 1st | 严胜 | 判断 |
|---|---|---|---|
| A 考卷（standard）| 41.4% | **40.4%** | 差别小 ⇒ **A 列基本可信** |
| farmerwall（I 列）| 97.5% | **0.0%** | ⇒ **该列在"熬满"场里不可用** |
| mix4 / reflectwall | 同类机制（复核上轮已发现"100% 但 52/60 局全灭平局"）| 待逐场重测 | 与 I 列同族 |

### 4. 未做（诚实登记）
**硬门槛改走严格口径**这一步本轮没做：先让三列并联可见（避免历史列不可比），下一轮再把门槛切到严格口径。

### 5. 门禁
`np-test 102/102`。

---

## v1.5.57 — 第五轮独立复核（Qoder）的修正：**座位对称性进体检** + 体检口径/表头修复 + 我的一处单位错误

### 0. 我先认错：上一轮"多数候选已修好"的结论**不成立**
我那张表的"极差"列算的是**胜场数差**，却拿去和**百分点阈值 18** 比 ⇒ 推出"中位 18 ⇒ 多数已修好"。
按百分点重算：`97/2/2/0/0` 的极差是 **97pt**（我印 57）、`100/0/0/0/0` 是 **100pt**（我印 39）。
正确中位约 **33pt** ⇒ **多数候选仍然偏座**（复核的判断对）。本版把口径钉死在百分点上。

### 1. 核实（实测，100 局、5 席同策略、引擎判胜）
| 对象 | long 各座胜率 | 极差 | multi 极差 |
|---|---|---|---|
| **线上包 = `v7wall-31`**（`v7\|0d987a62\|5689`，与 `docs/artifacts/v7wall-31.bak` 一致）| 84% 4% 6% 4% 2% | **82pt** | **51pt** |
| `v7seat2-51` | 13% 26% 26% 20% 15% | 12pt | 16pt |
| `v9-31` | 12% 16% 21% 26% 25% | 14pt | 17pt |
⇒ **玩家真正遇到的对手（线上包）才是最偏的那个**，而此前所有体检项都看不见这件事。

### 2. 新探针 `seatSymmetry(W, params, mode, GAMES)`（`tools/audit-lib.mjs`）
5 席同一策略、每局带盐、**回报一律百分点**；附带判据：**前置"分出胜负 ≥30%"**（平局过多时"各座≈0%"是空读数，
如某候选 93% 平局时的"极差 3pt"）+ **极差 ≥30pt ⇒ 判偏**；`max/min` 比值一并回报供参考。
`tools/champ-audit.mjs` 新增 **座位极差** 列，默认 **100 局**（20 局时噪声足以淹没 30pt 阈值）。

### 3. **座位对称性进上线体检**（`tools/promote-champion.mjs`）
按复核 §6 的"比值 + 前置条件"思路：`verdict==='biased'` ⇒ **阻断上线**；`unjudgeable`（平局过多）⇒ 警告并明确
"别把它当均衡"，不计入阻断。⇒ 现行线上包在新门槛下 **FAIL（60pt）**，即复核要求的 **G3 接成已知红**。

### 4. 复核指出的其它两处，已修
- **体检表头与列错位**（`champ-audit`）：H/I 原先印在 D 之后、表头却排在末尾 ⇒ 按表头读整体错两格；现按表头顺序印。
- **`1st` 口径把并列算第一**（`eval-5p.mjs:311` 走 `rankOf`）：复核的 farmerwall 实测（引擎判胜 4% / 判平 96% /
  `rankOf==1` 99%）确认 ⇒ **A/D/H/I 四列在"熬到上限全员存活"的场里都会虚高**。
  本版先记录（本轮不改口径，避免历史列不可比）：**下一轮**给每列并报"严格胜率 / 并列率"，硬门槛改用严格口径。

### 5. 换冠军的取舍（实测表，见本节末）
没有候选能同时满足：线上包 D=100%/G=5.15 但座位 60pt；`v7seat2-51` A=50.9%/座位 14pt/F=30% 但 D=25%；
`v9-31` D=100%/座位 18pt 但 G=2.46（过不了 G≥3）。⇒ 换冠军**暂缓**，先跑带座位门槛的臂找"三全"候选。

### 6. 门禁
`np-test 102/102`（D50–D54 不变）。

---

## v1.5.54 — 引擎兜底改为**按每局盐轮换**（L7 第四次）：无显式目标时不再恒定取最小索引

### 1. 病（三处，全部在 `js/core/resolve.js`）
```js
oppOf:      const o = aliveOpps(state, pid); return o.length ? o[0] : null;   // 恒定最小索引
mirrorRefs: if (t1 == null || ...) t1 = opps.length ? opps[0] : null;         // 同上
            for (const o of opps) { if (o !== t1) { t2 = o; break; } }        // 恒取最小 ≠ t1
```
⇒ 无显式目标（self 类效果贴在对手身上、镜面反射的 t1/t2）**一律落到索引最小的存活玩家** = **0 号座**
⇒ "0 号座是所有人的默认靶子"（实测 0 号座每局必死 60/60 的一部分来源），也是"多目标技能第二目标不可表达"那条的另一半。

### 2. 修法
新增纯函数 `saltPick(state, arr, seq)`：**盐 0 ⇒ 保持旧口径 `[0]`**（页面与既有对比基线不变）；
有盐 ⇒ 按 `(salt, round, seq)` 轮换（**不消耗任何随机流** —— 借流的三个坑 v1.5.51 已踩过）。
三处兜底全部改用它；导出 `oppOf` / `saltPick` 供守门直测。

### 3. 新守门 **D53**
盐 0/无盐 ⇒ 旧口径 · 有盐 ⇒ 默认作用者覆盖全部候选且大致均匀（40 盐 × 5 回合）· `oppOf` 行为同验 ·
源码断言三处兜底必须已移除 + `saltPick` 必须存在。

### 4. 门禁
`np-test 101/101`；指纹按铁律重记（`fc20996a → 53f064c5`）。

### 5. 下一步（本轮同时启动）
12 seed 大验收臂（v7l7）：同时检验"引擎层三条 L7 修复 + 特征层随机槽位"能否消掉训练诱导的座位偏置；
样本量从上一轮的 2 个不同策略提上去。

---

## v1.5.53 — 结算起点改为**每局随机相位**（L7 的第三次应用）+ 决定性诊断：**偏置是训练造出来的**

### 1. 修：`turnOrder` 的确定性相位
`js/core/resolve.js` 原为 `start = (round - 1) % n` —— **确定性映射** ⇒ 第 1 回合**永远**从 0 号座开始。
（该行上方注释本身就写着上一次修"永远按座位升序"时也是改成另一个**确定性**映射。）
修法：`start = ((salt % n) + round - 1) % n`，盐取 `state.slotSalt`（每局不同、纯函数、**不消耗任何随机流**）。
**盐 0 保持旧口径**（页面与既有对比基线不变）；2 人局恒等顺序（v1.0 冻结）。导出 `turnOrder` 供守门直测。

### 2. 新守门 **D52**
25 个不同盐 ⇒ 第 1 回合起点**覆盖全部 5 座**且大致均匀 · 盐 0 ⇒ 第 1 回合 = 0 号座、第 3 回合 = 2 号座 · 2 人局 = `0,1` · 源码断言（用盐、旧相位必须已移除）。

### 3. 🔑 决定性诊断（本版最重要的一条）
| 对象 | 各座夺冠率（60 局）| 各座死亡数 | 结论 |
|---|---|---|---|
| **热启动种子 v1.3.58** | **22% 16% 22% 16% 24%** | 43/44/43/49/38 | **本身几乎均衡** |
| 训练产物 `v7phase-31..34`（同一权重）| 39% 15% 15% 15% 17% | 39/51/50/49/46 | 强烈偏座 |
| 训练产物 `v7phase-35..36`（同一权重）| 0% 30% 26% 23% 21% | 60/42/47/45/46 | 反向极端 |
⇒ **偏置不是引擎层、也不是特征层造成的，而是训练过程造出来的**（从均衡种子出发、产出偏座策略）。
⇒ 6 个 seed 只产出 **2 个不同策略**（探索近乎冻结）⇒ 验收样本量实际只有 2，**不足以判定修复效果**。

### 4. 已知尚未修的确定性身份映射（下一项）
引擎 `oppOf` / `aliveOpps` 兜底 = **"索引最小的存活对手"**（无显式目标时的默认作用者、镜面反射 t2 兜底）。
它正是"多目标技能两个目标不可调换"那条的另一半。**注**：本版尝试统计"无目标出手比例"的探针**无效**
（猜错了事件字段名 ⇒ 报 100% null ⇒ 实际什么都没测到）—— 下一轮先**打印真实事件对象**再统计，不再猜。

### 5. 门禁
`np-test 100/100`（含 D50/D51/D52）；指纹按铁律重记（`d44d68b3 → 7902661f`）。

---

## v1.5.52 — 槽位顺序改为**纯函数哈希**（不再借任何随机流）；**双目标技能有序成对进入决策**

### 1. `slotRand` 重做：`__slotHash(salt, round, i)`，`salt = state.slotSalt`（调用方输入，缺省 0）
同时满足四条约束：① **不消耗任何随机流**；② **可复现**（同种子同结果）；③ 同一决策内天然一致；
④ 跨局不可预测由调用方"每局传不同盐"提供（L7：并列不许有任何确定性身份映射）。

**过程中查出并钉死三个真缺陷**（每个都配了反证型守门）：
| 缺陷 | 后果 | 守门 |
|---|---|---|
| v1.5.51 借 `__rng()` 当随机键 | 消耗采样流 + 特征对隐藏状态敏感 ⇒ **D22 红**（蓄能珠类型泄漏 [66,76]）| D50：**槽位键不得使用任何随机流** |
| 从 `state.rng` 抽盐 | 改变对局行为 ⇒ **D26 红** | D50：禁 `state.rng`/`rng.next` |
| 计数器盐（每 state 递增）| **破坏"同种子同结果"** ⇒ **D13 红** | 盐改为调用方输入、缺省 0 |
（旧版"缓存随机键 `SLOT_RAND`"一并移除；D50 改为断言其**不存在** + 纯函数哈希存在。）

### 2. 双目标技能：`target2` 声明 + **有序成对枚举**
- `rules.js`：`mk(SK.DUAL_GUN,…)` / `mk(SK.MIRROR,…)` 经 **`opts.extra`**（`mk` 唯一注入通道）声明 `target2: 'enemy'`；
- `candidatesFor`：**有序成对枚举** `(t1,t2)` ⇒ 5 人局 **12 个候选**（4×3）、两目标必不同、**(1→2) 与 (2→1) 是两个不同候选**
  ⇒ **"两个目标不可调换"的语义可表达**（镜面反射 t1=复制对象 / t2=输出对象）；
- 此前 `target2` 恒 `null` ⇒ 第二个目标由引擎兜底（`mirrorRefs` 的"索引最小对手"）填
  ⇒ **这是 0 号座每局必死 60/60 的一部分来源**，也是"角色不可表达"的根因；
- 新守门 **D51**：数据声明 + 全部有序组合 n×(n−1) + 两目标不同 + (1→2)/(2→1) 都在 + 单目标技能口径不变。

### 3. 过时测试假设修正
D26 原先硬编码"玩家 1 在槽 1"（pid 升序时代遗留）⇒ 改为**按 pid 反查槽位**（T/B 块布局：槽 0 = 我，槽 1..4 = 对手槽顺序）。

### 4. 确认
`choose` 采样是**单次抽样 + CDF 扫描**（`policy.js:534-537`/`633`）⇒ 候选数 4→12 **不扰动随机流**（已排除第二个坑）。
指纹按铁律重记（`300ad368 → d44d68b3`）。门禁：`np-test 99/99`。

### 5. 紧随其后（未完成，按序）
① 每局盐接进训练主循环（`state.slotSalt` 由每局种子派生）⇒ 训练里槽位顺序才真正跨局不可预测；
② 去掉引擎兜底（`mirrorRefs` 自动填 t2 / `oppOf` 索引最小兜底）⇒ 无显式目标即不生效；
③ 重训一臂后**复测验收**（各座夺冠率 ~20%、0 号座死亡数与其他座持平）。

---

## v1.5.51 — 槽位顺序改为**每次决策内一次随机洗牌**（破"确定性身份映射"；本会话第三次同类修正）

### 1. 完整诊断链（三层排除 + 一层定位）
| 层 | 测试 | 结果 |
|---|---|---|
| 引擎 | **随机策略** 5 座自对局 | 无偏（各座 23/15/21/25/16，极差 7 局）|
| **状态特征** | 完全对称局面下逐维比较五座 | **0/213 维不对称** ⇒ 无座位编码 |
| **开局策略** | 5 个同一冠军坐满 5 座，看第 1 回合选择（20 局 × temp 0/0.15）| 各座都是 `ji` 主导、分布同形 ⇒ 开局无座位差异 |
| **中后期动力学** | 各座死亡次数 + 提高温度重测 | **0 号座死亡 45~48/50（其他 34~42）、夺冠 3~9%**；temp 0.5 仍偏（甚至更明显）⇒ **结构性、非协调假象** |

### 2. 机制（指向 v1.5.46 我自己的修复）
- 旧版并列按 **pid 升序** ⇒ 开局同血时"槽位 0"永远是 0 号座 ⇒ 上线冠军 **0 号座夺冠 81%**；
- **v1.5.46 改成"按回合起点轮转"** ⇒ 仍然是一个**确定性映射**：每回合"回合起点"占所有人的槽位 0，
  策略学出"打槽位 0"⇒ **回合起点被集火**；而**第 1 回合起点 = 0 号座**、且对局在前几回合就定局 ⇒ 0 号座先死 ✓
- ⇒ **L7 教训的完整含义：并列不许有任何确定性的身份映射**（"更对称的确定性映射"也不行）。

### 3. 修法
`oppSlots` 的并列分支改用 **`slotRand`**：**每次决策内一次随机洗牌**，
按 `state + round` 缓存 ⇒ 同一决策内所有调用（features/actionFeatures/多候选打分）**特征一致**，
跨座位/回合**不可预测** ⇒ "打槽位 0"不再是可利用习惯。
守门 **D50** 同步：必须用 `slotRand`、**旧 `_rot` 必须已移除**（反断言）、随机键必须按 `state+round` 缓存。

### 4. 可证伪验收（正在跑）
重训一臂（6 seed、无强迫）后量新候选：**各座夺冠率应回到约 20%**（现在 3~9%）
且 **0 号座死亡次数应与其他座位持平**（现在 45~48 vs 34~42）。两条都满足才算修好。

### 5. 连带
`policy.js` 属指纹集 ⇒ 按铁律重记两个包（`be74b3d3 → 300ad368`），3P 权重不变。

---

## v1.5.50 — 座位偏置二分（6 seed/格）：**泄漏不在三个特征块里**，转向训练动力学 + 新线索"0 号座 3%"

### 1. 读数（`EPIRUS_FEAT_MASK`，各格 6 seed，long·120 局）
| 格 | 掩掉 | 典型各座夺冠率 | 极差 | 判决 |
|---|---|---|---|---|
| `v7m2_noTarget` | `target` 块 | **3%** 23% 25% 21% 28% | 24~30 | 仍偏 |
| `v7m2_noEffects` | `effects` 块 | 3% 26% 19% 26% 25%；另有 4 个 **98%** 1% 1% 1% 0% | 24 / **117** | 仍偏（且更极端）|
| `v7m2_noBead` | `bead` 块（对照）| **3%** 23% 25% 21% 28%；另有 50% 15% 15% 11% 10% | 30 / 44 | 仍偏 |

⇒ 三格都仍偏 ⇒ **泄漏不在 `target`/`effects`/`bead` 这三个特征块**（掩掉它们并不能去掉偏置）⇒ 按事先定的判读规则**转向训练动力学**。

### 2. 新线索（更锋利，下一步就查它）
反复出现的模式是"**0 号座只有 3%**" —— 这是 **0 号座的系统性劣势**，**方向与上线冠军那个 81% 优势相反**。
⇒ 说明**训练或评测路径里有某处系统性削弱 0 号座**。候选排查点（按可疑度）：
① `rankOf` 的并列洗牌在**训练打分**中的使用；② 评测/训练里**各座 chooser 的构造顺序**与温度；
③ `featuresV7` base 段是否存在与座位相关的量；④ 训练里"只给被测座补资源"的那段评测逻辑（`seat = g % N`）在**跨座比较**时的口径。

### 3. 意外收获：**冻结不是普遍现象，是强迫/环奖励臂特有的**
| 臂类型 | 逃逸率 |
|---|---|
| 带强迫（`EPIRUS_RING_FORCE_EPS`）/ 环奖励窗口 | 约 **1/6** |
| **本轮三格（无强迫、无环奖励）** | **6/6 全部有提升** |
⇒ "训练静默冻结"应记为**那些实验臂特有**的现象，而不是流水线的常态。

### 4. 待办（顺序）
1. 查 **0 号座劣势**（上表列的四个候选点）；2. **G3 固化成真门槛**；3. 替换无判别力的 `ringWallProbe` + 新增 `wastedChargeRate`；
4. 环 exploit：开环节拍随机化 + 反证型守门。

---

## v1.5.47 — 换冠军 `v7seat2-51`：**去座位偏置** + 首个"零 `--force`"的干净换冠军

> 起因：第三方复核 §3 发现**座位偏置**（5 席同冠军自对局 **0 号座夺冠 81%**，其余 3~7%，期望 20%），用户要求优先修。

### 1. 偏置的调查链（每一步都有"能失败"的对照）
| 步骤 | 测试 | 结果 |
|---|---|---|
| 引擎/代码在**对局**层有偏？ | **随机策略** 5 席自对局 | ❌ **无偏**（各座 23/15/21/25/16，极差 **7 局**）|
| 训练时**被测座位固定**？ | 查 `scoreMemberN` | ❌ 座位是**轮转**的（`seat = g % n`）|
| `oppSlots` 并列按 pid 升序（v1.5.46 已修）？ | 修 + 重训 | **部分有效**：仍有 4/5 候选严重偏（87/78/59/21 局）|
| **未训练的种子**是否偏？ | 冻结产物（= 热启动种子） | ✅ **均衡（4 局）** ⇒ 偏置是**训练中学出来的** |

⇒ 收窄为：**偏置可学习、来源在特征/训练信号**（不在引擎、不在被测座位）。**二分计划**（`setFeatMask`，下一步）：
逐块掩掉各训 2 seed，看哪块掩掉后 G3 极差塌回 ⇒ 优先怀疑**动作特征里的"目标槽位下标"**（同一目标在不同座位下槽位不同 ⇒ 可学"打槽位 0"），
其次每对手槽位块；base 聚合块作对照（应对称、预期无变化）。

### 2. 换冠军：`v7seat2-51` → `js/bundled-champion-3p.js`（**未用 `--force`**）

| 指标 | **新：v7seat2-51** | 旧：v7wall-31 |
|---|---|---|
| **座位夺冠极差（G3）** | **4 局 ✅** | **91 局 ❌** |
| 标准考卷 1st（200 局） | **48.5%**（项目最高）| 44.0% |
| **F 活跃场进攻率** | **29% ✓（无需 `--force`）** | 24% ✗ |
| E 无威胁摆架势 | 0% ✓ | 0% ✓ |
| G 有效技能 / 回合 / 平局 | 4.92（**13 种**）/ 23.9 / 0% | 5.40（10 种）/ 30.1 / 0% |
| H 混合场 / I 对被动 | 80% / 100% | 80% / 95% |
| **D 长程反弹墙** | **23%**（已知退化，如实记录）| **100%** |

- **首次出现"全过门槛、不需要 `--force`"的换冠军**（`auditForced` 应为 false）；
- **代价（如实记录）**：D 反弹墙 100% → 23%（复核 §3-1 那条能力线又变弱 —— 但注意 D 的 100% 部分来自"同归于尽后按血量第一"的口径）。

### 3. 待办
1. **把千问的 G3 固化成真门槛**（`tools/gate-drafts.mjs` 作单一来源）：它已证明有判别力
   （在 45/46/48/49 上红、在 51 与种子模型上绿）⇒ 可作提交前的硬门，防止再引入带偏置的冠军；
2. **偏置来源二分**（`setFeatMask`，见 §1）；
3. **替换我那个没有判别力的 `ringWallProbe`**（G1 已证明它对"会瞄/仪式"策略都报 40 次）⇒ 环课题改用 `probeField`/`probeFieldNoRinger`；
4. 环 exploit 本身（节拍随机化 + 反证型守门）继续排队。

---

## v1.5.46 — **修座位偏置**（复核 §3，最高优先级）：并列不再按 pid 升序

> 复核实测：同种子族、5 席全部加载**同一个**冠军包、temp 0.15、200 局 ⇒
> **0 号座夺冠 81%（long）/ 59%（multi）**，其余座位 3~7%（对称期望 20%）。
> 且 `resolve.js` 的 `turnOrder()` 每回合轮转行动起点 ⇒ **不是"先手"造成的**。

### 1. 根因（一行代码的并列分支）
```js
// js/train/policy.js  oppSlots(state, pid)  —— 修前
a.sort(function (x, y) { const d = state.p[x].hp - state.p[y].hp; return d !== 0 ? d : x - y; });
```
开局全员同血 ⇒ 并列全部走后一项 ⇒ 1~4 号座看别人时 **"槽位 0"永远是 0 号座**，
而 `FEAT_S` 里 4 槽 × 10 维是**硬编码下标** ⇒ 网络完全可以学出"槽位 0 该怎么对待"
⇒ **与实力无关的座位身份泄漏**（即本项目 `L7` 教训"并列不许按 pid 升序"在另一处的复现）。

### 2. 修法（v1.5.46）
并列分支改用**与 pid 无关、且每回合变化**的轮转：
```js
const _ts = ((((state.round || 1) - 1) % _n) + _n) % _n;
const _rot = function (i) { return (((i - _ts) % _n) + _n) % _n; };
a.sort(function (x, y) { const d = state.p[x].hp - state.p[y].hp; return d !== 0 ? d : _rot(x) - _rot(y); });
```
实测：`pid=1` 的"槽位 0"从**恒为 0** 变为随回合变化 `[0,2,2,3,4]` ✓。
守门 **D50**（行为 + 源码双断言）：不同回合下槽位 0 不得恒为同一人；必须用 `_rot`；旧的 `x - y` 必须已移除。

### 3. 影响面（为什么这条优先级最高）
它给**一整类"5 座同一冠军"的指标**打上折扣：B 伤害/局、平局率、`mirrorHealth`（健康门槛的数据源）、D 长程反弹墙，
以及复核自己上一轮的 D 项对照。

### 4. 连带：指纹重记（铁律）
`policy.js` 属指纹集 ⇒ `57108d06 → **be74b3d3**` ⇒ 重测重记两个包（3P 权重不变仍为 `v7wall-31`）⇒ D16 转绿，`np-test 98/98`。

### 5. 偏置修复后现役冠军的新读数（**基本不变** ⇒ 纯缺陷修复，无连带行为变化）
A 考卷 42% · B 伤害/局 13.8 · 回合 30.1 · **D 反弹墙 100%** · **E 无威胁 0% ✓** · **F 24% ✗（`--force`）** ·
G 5.40（10 种）· H 混合场 80% · I 对被动 95% · 平局 0%。

### 6. 仍未做（复核文档 §2/§4 指出的，按优先级）
1. **环 exploit 一点没动**（回退后的现状）：长程 1 席连开环 + 4 席冠军 ⇒ **滚环者夺冠 75%、落地伤害 0.0**，
   而冠军 40 局只出 **2 次**小雷 —— 用户那两局就是这么输的；
2. **两条反证型守门**（换人反证：把唯一滚环者换成不滚环 ⇒ 小雷出手必须显著下降；判别场：1 环 + 3 快攻 + 1 冠军 ⇒ 出手必须 > 0）；
3. **训练侧解耦**：环流对手**开环节拍随机化**，打断"环 ↔ 回合数"共线；并确认 `opps` 里 `ringspam` ×3 是否有意；
4. `auditForced=true` 仍是 F 24% < 25%。

---

## v1.5.45 — **回退冠军到 `v7wall-31`**：v7r2-35 的"会打断环"被复核证实是**第 4 回合仪式**

> 依据：用户实测两局（`results/epirus-battle-20回合.txt` / `-21回合.txt`）+ 第三方复核复刻。
> **实测证据（原文）**：20 回合局里 **玩家1 出【聚能环】**，四个冠军座**全部【雷击之枪】打彼此**（两次"互雷成环"自废），
> **唯一在开环的玩家1 一枪没挨**；21 回合局里 **玩家1 出的是【ジ】（无人开环）**，四座**照样四发齐射**。
> 复刻场（1 席连开环 + 4 席冠军，long，40 局）：小雷 187 次（**第 4 回合 ×160**、第 8 回合 ×20、其余 7），
> **砸在滚环者身上 0 次**、160 次出手时**场上根本没有环**；判别场（1 环 + 3 快攻 + 1 冠军）小雷 **0 次**；
> 自对局（无环）小雷 **560 次 = 9.7% 出手** ⇒ 把反环卡当常规动作在烧。

### 1. 处置：回退冠军
`v7r2-35` 的收益（"自然小雷 25"）是**假验收**产物 ⇒ 回退到 **`v7wall-31`**（标准 44.0% / D 100% / E 0% ✓ / 全息套别人 2.0 ✓）。
用 `--force` 越过 F 24%（差 1pt 属噪声带），meta 留痕。

### 2. ⚠️ 我的验收标准**第二次**失效（必须记进方法论）
探针场地是**固定节拍**的滚环场（每回合无脑开环）⇒ "**环出现**"与"**回合数**"**共线** ⇒
只要在第 4 回合砸小雷就必然"命中"⇒ `miniTCasts > 0` **由场地构造保证、不可能失败**。
- 第一次同类错误：`miniTCasts ≥ 5` 可被 ε-强迫自证（复核 §5-5 已警告过，我没把它贯彻到**探针场地构造**）；
- **新规矩**：验收断言必须能**失败**，且**探针必须自带判别臂**（同回合数、但把环换成非环 ⇒ 出手数必须显著下降）。

### 3. 下一步（已排好序，先做前两条再做训练）
1. **两条反证型守门（np-test）**：
   - **换人反证**：把场上唯一的滚环者换成不滚环 ⇒ 小雷出手数**必须显著下降**（现在不降 ⇒ 它读的是回合数）；
   - **判别场**：1 滚环者 + 3 快攻 + 1 冠军 ⇒ 小雷出手数必须 **> 0**（现在 = 0 ⇒ 一有真实压力动作就消失）。
2. **训练侧解耦**：把环流对手的**开环节拍随机化**（先攒够再等几回合 / 中途断一环），
   打断"环 ↔ 回合数"的共线 —— 否则**换任何奖励形状都会重新拟合回合数**；
   并确认 `opps` 里 `ringspam` 出现 **3 次**是有意还是拼接产物（它放大共线）。
3. 之后才谈是否重训。

---

## v1.5.44 — **换冠军 `v7r2-35`：首个"会打断环"的冠军**（供用户/复核实测）

> 用户裁定：先换上，让用户与第三方复核**实测**该行为，再决定是否继续投资这条线。

### 1. 新老对照

| 指标 | **新：v7r2-35** | 旧：v7wall-31 |
|---|---|---|
| **自然小雷次数**（20 局对 4 席环流，**关掉强迫后量**）| **25** | 0 |
| **由我造成的作废**（`voided` 且 `byPid === 我`）| **25** | 0 |
| 环墙 1st（long·200 局）| **42.5%** | 26.0% |
| 标准考卷 1st（200 局）| 40.2% | **44.0%** |
| D 长程反弹墙 | 100%（穿透 **344**）| 100%（穿透 84）|
| E 无威胁摆架势 / G / 平局 | 0% ✓ / 5.11（8 种）/ 0% | 0% ✓ / 5.15（11 种）/ 0% |
| **F 活跃场进攻率** | **11% ✗（`--force` 越过）** | 24% ✗ |
| 全息套给别人（硬门槛）| 0 ✓ | 2.0 ✓ |

### 2. 成功配方（三要素，缺一不可；前七次失败的原因见 v1.5.31~v1.5.43）
1. **奖励可归因**（v1.5.37）：`setVoid` 带 `byPid` —— 否则"我打断"与"别人打断、我恰好放过小雷"同分；
2. **开环判据 = 动作事件**（同版）：该座**真的施放了聚能环**（含首次）—— 旧 `delta ≥ 2` 漏首次、还把避雷针 +4 ジ 误判；
3. **退火强迫**（v1.5.40）：格内 ε=1.0 只在前 **40** 代生效 ⇒ 产物来自**无强迫的后段**，验收才有意义。

### 3. 已知代价与风险（**如实记录，供复核重点验证**）
- **标准考卷 −3.8pt**（44.0% → 40.2%）；
- **F 11% < 25%**（活跃场进攻率）⇒ 常态对局可能**偏被动**；
- **能力命中率 ≈ 1/24**（"逃过冻结" ≠ "学会"：18 种子批量里 3 个逃逸、0 个学会）⇒ 本候选是**确定性样本**，不代表配方已稳健；
- 根因（v1.5.42 的负结果）：**堵点在选择哲学** —— 强迫让形状分变差 ⇒ 探索期个体永远不算 `better` ⇒ 提升链不留它们。

### 4. 请实测时重点记录的三类现象
① 我方开环时它**没**出小雷的回合；② **该打断却没打断**的场面特征（是否满血/是否已连开多回合）；
③ **该进攻却摆架势**的回合（对应已知短板 F 11%）。这三类可直接喂给下一步"改选择哲学"的课题。

---

## v1.5.43 — ② 的负结果 + 18 种子批量："逃过冻结"≠"学会打断环"，堵点在**选择哲学**

### 1. ②（探索期不被健康门槛惩罚）实测：**没治好脆弱性**
| | 修 ② 前（窗口 40）| 修 ② 后（v7r2）|
|---|---|---|
| 逃逸 seed | 1/6（35）| **1/6（还是 35）** |
| 该 seed 的自然小雷 / 可归因作废 | 25 / 25 | **25 / 25（完全复现）** |
⇒ **堵点不在健康门槛**：`finishStep` 要求 `better`（形状分/胜率变好），而**被强迫出来的小雷让形状分变差**
⇒ 探索期个体**从来不是 better** ⇒ 连进入提升候选的机会都没有（健康与否都轮不到）。

### 2. 18 种子批量（41~58，同一配置）：**逃逸 3 个（45/46/52），学会 0 个**
```
逃逸 3 / 零提升 15      v7r3-45 小雷 0 / 作废 0 / 穿透 291
                        v7r3-46 小雷 0 / 作废 0 / 穿透 257
                        v7r3-52 小雷 0 / 作废 0 / 穿透 359
```
⇒ **"逃过冻结"与"学会打断环"是两件事**：逃逸是必要不充分条件。

### 3. 全部证据汇总（窗口 × 种子）
| 配置 | 种子 | 逃逸 | 学会（自然小雷>0）|
|---|---|---|---|
| 窗口 20 | 31–36 | 0 | 0 |
| **窗口 40** | 31–36 | 1（35）| **1（35：小雷 25 / 作废 25）** |
| 窗口 40 | 41–58 | 3 | **0** |
| 窗口 80 | 31–36 | 1 | 0 |
⇒ **能力命中率 ≈ 1/24**（且是确定性的：同 seed 复现）。

### 4. 结论（B 的目标用"多种子"这条路走不通）
- "先做稳再换"**无法靠堆种子达成**（1/24 的产率，且 3/3 逃逸者都没学会）；
- 要稳健，必须动**选择哲学**：探索期允许"变差仍能推进"（例如按**新颖度**入档/保留 explorer 分支，
  或探索期把 `better` 判据从形状分换成行为新颖度）—— 这是独立课题、风险更高；
- 现有可上线的**唯一确定性样本**：`v7r2-35`（= `v7force2-35` 同配置复现）——
  标准 40.2%（现役 44.0%）· 环墙 42.5%（现役 26.0%）· D 100%（穿透 344）· E 0% ✓ · **F 11% ✗** · 自然小雷 25 / 可归因作废 25。

### 5. 待用户裁定（三选一）
(a) **换 `v7r2-35`**（`--force` 过 F）：用 −3.8pt 标准分换"会打断环"+环墙 +16.5pt；
(b) **立独立课题**：改选择哲学（新颖度入档/探索期换判据）后再重训；
(c) **维持现役**，把本课题归档为"能力可学、当前搜索留不住"的研究结论（七次尝试谱系已完整入库）。

---

## v1.5.41 — "打断环"**配方确认但很脆弱**（窗口 20/40/80 对照）+ 七次尝试谱系

### 1. 成功配方（首次真正学会"打断环"，v1.5.40 实测）
三条**缺一不可**：
1. **奖励可归因**：`setVoid` 带 `byPid`（v1.5.37）—— 否则"我打断"与"别人打断"同分（反例 A=1.5 / B=0.5）；
2. **开环判据 = 动作事件**（该座真的施放了聚能环，含首次）—— 旧 `delta >= 2` 漏首次、还把避雷针 +4 ジ 误判；
3. **退火强迫**：格内 ε=1.0 只在前 **40** 代生效 ⇒ 产物来自**无强迫的后段**，验收才有意义。
   - 验收（复核 §5-5 要求，非恒真）：**关掉强迫后的自然小雷 > 0** 且 **`voided` 且 `byPid === 我` > 0**。

### 2. 窗口对照（每臂 6 seed，池 D）
| 窗口 | 零提升检测 | 自然小雷 | 判决 |
|---|---|---|---|
| 前 **20** 代 | **0/6 有提升** | 全 0 | 作废（冻死更彻底）|
| 前 **40** 代 | 1/6 有提升 | **seed 35: 25 / 由我作废 25** | ✅ 唯一成功 |
| 前 **80** 代 | 1/6 有提升 | 全 0 | 逃过冻结的也没学会 |
⇒ **窗口很窄、可复现率低（1/6）**：不是"配方不对"，而是**提升闸门会把探索期的个体整个拒掉**。

### 3. 脆弱的机制（与 v7both/v7soft 的"奖励冻死"同源）
强迫出来的小雷降低了成员的**形状分/体检指标** ⇒ 提升被拒 ⇒ 冠军冻在热启动种子 ⇒ 整臂零提升。
⇒ 结构性修法（下一步）：**让提升闸门不惩罚"探索期"**（例如只按**强迫窗口之后**的世代判定健康/择优，
或允许冠军来自后段的世代），而不是继续调 ε/窗口。

### 4. 已知最佳候选 `v7force2-35`（**尚未上线**，待更多候选对比）
标准考卷 40.2%（现役 44.0%）· 环墙 **42.5%**（现役 26.0%）· D 反弹墙 100%（穿透 344）·
E 无威胁摆架势 **0% ✓** · F 活跃场进攻 **11% ✗** · G 5.11（8 种）· 平局 0% · 自然小雷 25 / 可归因作废 25。
⇒ 用户选 **(B) 先做稳再换**：先凑出"标准不掉 + 会打断环"的候选，再动冠军。

---

## v1.5.39 — 定向强制探索：**两臂都因"算术陷阱/训练冻结"作废**（结论：探索问题仍未测到）+ 结构障碍入库

### 1. 实现（只影响"有滚环者且我付得起小雷"那一格）
- `ringForceTarget(state,pid,legal)`：仅当**有人 `ringStreak ≥ 1` 且我付得起小雷**时返回该目标，否则 `-1`；
- 包装层：**只在概率门内**改动作，其余一律**原样返回学习到的动作**（**刻意不复用教师槽做常开 ε** —— 那会用 heavyfire 扰动全局）；
- env 通道：`EPIRUS_RING_FORCE_EPS` → 主线程 + **worker 独立进程**都接；并加**审计轨迹**
  `[ringforce] worker eps=... (env=...)` —— 实测打印 `eps=1 (env=1)` ✓（**通道无误**）；
- 守门 **D47**（7 条：目标格判定四例 + 必须带概率门 + 非目标格必须原样交回 + worker 必须能走 env）⇒ `np-test 95/95`。

### 2. ⚠️ 两臂都**不能作为证据**（各自的原因都实测钉死了）
| 臂 | 设定 | 零提升检测 | 判决 |
|---|---|---|---|
| `v7force` | 格内 ε=5% | 2 有提升 / **4 零提升** | **算术陷阱**：目标格只占决策 **0.15%**（复核实测 7/4549），又乘 5% ⇒ 实际强制 ≈ **0.0075%**，整跑几乎没强迫 ⇒ 那 2 个提升的 seed 也等于没测 |
| `v7force1` | 格内 ε=1.0 | **6/6 零提升** | **训练整体冻死**：产物 = 热启动种子 ⇒ 探针量到的是**种子**，`miniTCasts=0` **无效** |

⇒ **"探索是不是瓶颈"这个问题，到现在仍然没有被有效测试过**（不能下任何结论）。

### 3. 新发现的结构性障碍（值得单列）
**在当前"健康门槛 + 提升闸门"下，强制探索会把训练冻死**：被强迫出来的小雷降低了成员的形状分/体检指标
⇒ 提升被拒 ⇒ 冠军冻在种子（与 v7both/v7soft 的"奖励冻死"同源，只是触发者从"奖励"换成"强迫"）。
⇒ 正确的做法应是**强迫也要退火**（早期强制把动作示范出来，之后关掉、交给奖励），
且**验收必须同时看 `zero-promote-check`**（否则会在"量到种子"的情况下误判成"学不会"——本次差点如此）。

### 4. 方法论新增
**"在稀有格子上再乘 ε" = 等于没做**：`P(格子) × ε` 才是实际干预率，稀有格子上 ε 必须取 1 甚至配合退火。
（同族教训：阈值不能定在噪声带里、门槛要按已知好/坏样本标定。）

---

## v1.5.38 — "打断环"第五次尝试：**信号层修好后依然是 0**（干净的否定结果，进入最后一枪）

### 1. 本臂做了什么（把审计指出的三堵墙**全部拆掉**之后的第一臂）
- 奖励**可归因**：`setVoid` 带 `byPid`，反例 A=1.5 / **B=0.5**（此前同分）；
- 开环判据 = **"该座真的施放了聚能环"的动作事件**：反例 **D=0.5**（首次已识别）、**E=0**（避雷针 +4 ジ不再误触发）；
- 教师条件 `ep ≥ 2` → **`ringStreak ≥ 1`**（审计实测触发率 45% → 0.15%）；
- 池 D（**4 席 ringspam**）保证暴露度；示范期 0.8 + 退火环奖励。

### 2. 结果（`v7attr`，6 seed，20 局对 4 席环流）

| 冠军 | 自然小雷次数 | **由我造成的作废** | 对开环者伤害/局 | 反弹墙穿透命中 |
|---|---|---|---|---|
| v7attr-31 … -36（全部）| **0** | **0** | 2.00~2.45 | **339~372** ✓ |

- **验收两条（自然小雷 > 0、`byPid===我` 的作废 > 0）全部不达标**；破墙线完好（说明本臂没"换偏"）。
- 探针同步增强：`ringWallProbe` 新增 **`voidedByMe`**（由我造成的作废数）—— 旧工具只数 `voided` 总数（含别人造成的），
  在 §5-1 修好之前**根本量不出归因**。

### 3. 结论（五次尝试的完整谱系）
| 手段 | 自然小雷 |
|---|---|
| 奖励常开 / 退火 / 密集 | 0 |
| 池子（1 席 / 4 席 ringspam）| 0 |
| 课程示范（0.5 / 0.8）| 1 → 0 |
| 暴露度 + 示范 + 接力 | 0 |
| **再加上"可归因 + 判据含首次 + 教师条件正确"** | **0** |

⇒ **信号层正确是必要条件、但不足够**：在奖励已经"该给的都给对"的情况下，这个动作**依然没有被采样到**。
⇒ 剩下的只有两条：**① 探索**（训练期对"有滚环者且我付得起小雷"这一格做 ε-强迫，**但验收必须在关掉强迫后量自然行为**）；
**② 表达力**（按审计 §5-4：每对手槽位加 1 维 `ringStreak/3`，**+5 维**，不新造概念）。
顺序：先 ① （便宜、且是审计认可的最后一枪），不行再 ②。

---

## v1.5.37 — 按第三方第三轮复核修"可归因"与"全息幻影候选"（**都不动参数量、不升 PACK_VERSION**）

> 依据：`docs/AUDIT-RESPONSE-v1.5.35.md` §5-1/2/3（打断环失败的硬障碍）与 §2-1（全息修复没生效且前提错）。

### 1. 打断环：奖励终于**可归因**（复核 §5-1 的三个硬障碍全修）
| 障碍（复核实测） | 修法 | 验证 |
|---|---|---|
| `voided` 事件**不记施法者**（`by` 是原因字符串）⇒ "我打断"与"别人打断、我恰好放过小雷"**同分** | `setVoid(state, pid, by, **byPid**)` 加第 4 参；小雷/大雷作废目标的调用点补施法者 | 反例 **A=1.5 / B=0.5**（此前 1.5 与 1.5）|
| 教师条件 `对手 ep ≥ 2` 在真实自对局里 **45%** 的决策都成立，而"有人真在滚环"只有 **0.15%** ⇒ 教的是"见人兜里有 ジ 就砸小雷" | 教师条件改 **`ringStreak ≥ 1`** | D43 新增"只是兜里有ジ ⇒ 不得砸小雷"的反例 |
| 开环判据 `delta ≥ 2` **漏掉环的第一次**（delta 是 1 然后 2）⇒ 越早打断越没分；且把"避雷针 +4 ジ"误当开环 | 判据改为**"该座真的施放了聚能环"的动作事件** | 反例 **D=0.5（首次已识别）**、**E=0（非环不触发）** |

- 守门 **D39/D43 按新事实重写**（D39 直接用复核给的 A~E 五反例）。
- **验收标准同步修正**（复核 §5-5）：原先 `miniTCasts ≥ 5` 是**恒真式**（自己加 ε-强迫必然满足）⇒ 改为
  **关掉强迫后的自然小雷数 > 0** 且 **`voided` 且 `byPid === 我` 的次数 > 0**。

### 2. 全息屏障：删掉**幻影"自己"候选**（复核 §2-1）
- 复核证明 v1.5.34 那条修复**没生效且前提错**：`holoShieldFrom` 明写"自己给自己套不算"（**这张卡本来就不能给自己**，
  自保用原型制御），而 `targetOf()` 在**声明阶段**就把 null/自己退成"第一个存活对手" ⇒ 我加的"自己"候选是**幻影**：
  实测被选中 **35/330 次**，每次都把**错误归因**喂回训练。
- 修：**候选只给真实对手**（holo 候选 5 → 4，无 `null`）；**撤销 v1.5.34 的 holo 特例**（恢复引擎原语义）。
- **C 门槛处置（写清理由）**：撤掉旧的"全息 > 2 次/局"普通条件（送盾是这张卡的**定义**、不是 bug），
  只保留**硬门槛"套给别人 > 6 次/局"（不可 `--force`）**，并留档实测分布（v7wall-33 18.7/局 vs v7wall-31 2.0~2.9/局）。
- 守门 **D45 重写**（候选不得含幻影 + 引擎"不能给自己"语义仍在 + v1.5.34 特例必须已撤销）。

### 3. 连带：指纹重记（铁律）
动了 `js/core/resolve.js` ⇒ 规则指纹推进 ⇒ 重测重记两个包（3P 权重不变仍为 `v7wall-31`；2P 重跑考卷）。

---

## v1.5.36 — 新增审计建议的两列 H/I（4 风格同场 / 1 冠军 vs 4 只ジ），**并标记口径待对齐**

> 依据：第三方第三轮复核 `docs/AUDIT-RESPONSE-v1.5.35.md` §4-1/§4-2（A 考卷看不见"综合强度"，"面对不还手的对手"无指标覆盖）。

### 1. 新增场地与列（纯测、不动参数量、不升版本、不重训）
- `tools/eval-5p.mjs` 新增两个**具名场地**：
  - **`mix4`** = `['aggro','defend','wall','farmer']`（**4 个不同风格同时在场**，最接近"人来打"的构造）；
  - **`farmerwall`** = `['farmer','farmer','farmer','farmer']`（4 席只按ジ）。
- `tools/champ-audit.mjs` 新增两列：**H 混合场 1st**（`--mode=long --field=mix4`）、**I 对被动 1st**（`--mode=multi --field=farmerwall`）。
- 验证场地**确实生效**（不是静默回退）：
  `--field=mix4 : aggro defend wall farmer（允许重复）` / `--field=farmerwall : farmer farmer farmer farmer` ✓

### 2. ⚠️ 口径分歧（**未对齐前不得据此下结论**）
首测 `v7wall-31`：**H = 80%、I = 100%**；而复核同场地给我的是 **混合场胜 17%**、**农夫场胜 0% / 平 100%**。
两者都在量真东西，但**语义不同**：

| | 本方工具 | 复核脚本 |
|---|---|---|
| 判据 | 引擎**按血量排名**的"1st"（`rankOf`：存活 > 血量 > 受伤少…）| `state.winner` 的**胜/平**（`checkOver` 语义：全员存活即平）|
| 模式 | H 用 long(5血)、I 用 multi(3血) | 混合场/农夫场都跑了 multi 与 long 两档 |
| 差异后果 | "终局全活但血更多"⇒ 记 1st | 同一局记"平" ⇒ 0% 胜 |

⇒ **待对齐**：要么 H/I 改成与复核同口径（改用 `state.winner` 的胜率），要么明确"1st 与 win 是两把尺子"并同时报两栏。
**在对齐之前，不拿 H/I 做换冠军决定**（本会话的教训：指标没对齐就下结论 = 又一次"用错误指标做决定"）。

### 3. 待办（复核 §7 的"只做三件事"，按此推进）
1. **信号层一次改完**：`setVoid` 加 `byPid`（现在 `by` 是原因字符串 ⇒ 奖励**结构上无法归因**，复核用反例证成）+
   教师条件改 `ringStreak >= 1`（现在 `ep >= 2` 宽 300 倍：45% 决策 vs 0.15%）+ 开环判据**含首次**（delta 是 1 然后 2）+
   D39 用复核给的 A~E 五个反例重写；
2. **删掉全息屏障的"自己"幻影候选**（`holoShieldFrom` 明写"自己给自己套不算"⇒ 该候选被选中 35/330 次，
   每次都是策略与引擎语义不一致的样本）+ 给 C 门槛**写清理由的处置**（重标定，或"送盾给领先者才罚"）；
3. 本轮已完成 H/I 两列（含口径分歧标记）。

---

## v1.5.35 — 终局平局口径修正（用户裁定）+ battle-test 看门狗止血

> 用户追问："全员平局不是应该按血量排名次吗？" —— 对，而且**规则里本来就写着**：
> `js/core/rules.js:134`「（不是被打死，是到上限**按血最多者胜**结束）」。⇒ **游戏本身按血量排名次**，
> 错的是**我们的体检指标**：它把"全员存活"一律记成平局 ⇒ 血量高的胜局被误记成"和"
> （复核两轮都提到的"农夫场 0% 胜 / 100% 和"就是这个假象）。

### 1. 平局口径：只有**血量也相同**才算平局（用户裁定）
- 新增纯函数 `allAliveTied(p)`：**全员存活 且 血量完全相同** 才为真；
  血量有差 ⇒ 那是**按血量的胜局**；血量也相同 ⇒ 不计胜（用户："那样可以判负"）。
- `mirrorHealth` 的 `drawRate` 改用它（此前是 `every(p => p.hp > 0)`）。
- **守门 D46**（行为断言）：同血 ⇒ 平局；不同血 ⇒ 非平局；有人死 ⇒ 非平局；全死/空 ⇒ 非平局。
- ⚠️ 影响面：所有"平局率"读数（champ-audit 的平局率列、`promote-champion` 的 `drawRate > 0.2` 门槛）
  从此量的是**真平局**；长局"按血量分胜负"的场（如农夫场/墙场）不再被误记为和。

### 2. `battle-test` 看门狗止血（复核 §4-3）
- 该门用**墙钟**判定（180s），而长局对峙（60 回合上限）是分布的尾部 ⇒ 概率性 FAILED（复核实测 5 次里 1 次）。
- 本次先把窗口放宽到 **300s** 止血；**真修法**（下一轮）= 在出招循环里**按状态等待**：轮询
  `window.EpirusUI.B.state.over`，到终局/回合上限立即收尾。
- 验证：`battle-test` 退出码 0 / `SUMMARY: BATTLE OK` / 0 JS 错误。

### 3. 规则侧的"终局收缩不可达"（复核 §4-1，**待用户裁定后另做**）
`multi` 的 `maxRounds = 60` 而收缩起扣默认 100 ⇒ 收缩永不触发；用户已选**方案 ②**
（终局仍全员存活 ⇒ 按血量排、血量也相同则不计胜），本条与 §1 的体检口径是**同一件事的两面**，
等下一轮做"收缩起点/判负"落地时一起处理。

---

## v1.5.34 — 根因修复："全息屏障送盾"不是学坏了，是**目标没进决策**（用户质疑换来的正解）

> 用户实测（`results/epirus-battle-14回合 (1).txt` / `-20回合.txt`）：冠军每局 **18.7 次全息屏障，100% 套在别人身上**
> —— 之后提出关键质疑：**"为什么送盾还需要专门惩罚？"** 顺着这句查下去，发现根本不需要惩罚。

### 1. 根因链（三处都核过）
| 层 | 事实 | 后果 |
|---|---|---|
| 规则数据 | `holo: target="other"`（cost 1 / defense）| — |
| **决策侧** | `candidatesFor` **只展开 `target==='enemy'`**，`'other'` 落进 else ⇒ holo **只有 1 个候选、`target=null`** | **AI 没有选择目标的余地** |
| **引擎侧** | 结算走 `const t = oppOf(state,i)`，而 `oppOf` 在 null 时兜底为"**第一个存活对手**" | 每发盾**必然送给编号最小的对手**（人类座位=玩家1）|

⇒ "送盾"是**管道默认值**造成的系统性行为，**不是网络学出来的偏好** ⇒ **惩罚治症状、会污染奖励面；修管道才对症**。
（原计划的"送盾惩罚"已撤销。）

### 2. 修复（两处，缺一不可）
- **决策侧**（`js/train/policy.js`）：`target:'other'` 的候选展开为 **自己（null）+ 每个存活对手**
  ⇒ 实测 holo 候选 **1 → 5**（`[null,1,2,3,4]`），目标回到决策。
- **引擎侧**（`js/core/resolve.js`）：holo **不再走 `oppOf`** —— 显式目标照用，**无目标 = 套在自己身上**
  ⇒ 实测 `target=null ⇒ holoSet{pid:0,target:0}`、`target=3 ⇒ target:3`。
- ⚠️ 第一版只改了"null 时套自己"却**没生效**：因为 `oppOf` **自己就会兜底**，`t == null` 的分支永远进不去
  —— 教训：**兜底逻辑会吃掉你后加的判空**（同族：本轮的门槛标定、"静默冻结"）。

### 3. 连带：指纹重记（铁律）
动了 `js/core/resolve.js` ⇒ 规则指纹变化 ⇒ 按铁律重测重记两个包（3P 权重不变仍为 `v7wall-31`，2P 重跑考卷）。
守门 **D45**：`candidatesFor` 必须展开 `target:"other"`、holo 不得走 `oppOf` 且 null 必须=自己。

### 4. 方法论新增（本轮最值钱的一条）
**"AI 行为奇怪" 先问"它有没有选择权"** —— 在加奖励/惩罚之前，先查这个动作的**目标/选项是否真的进了决策**。
本轮三例同源：holo 的目标没进候选（送盾）、`ep≥2` 当环信号（噪声）、`G` 只量熵不量关键卡。

---

## v1.5.33 — 训练"静默冻结"检测（`tools/zero-promote-check.mjs`）：零提升不再无声无息

### 1. 问题（本轮实测两次）
`v7both` / `v7soft` 两臂 **6/6 seed 的产物逐字节等于热启动种子**（一次提升都没发生），
而流程把它们当正常产物写盘、还拿去评估 —— **整轮实验被静默烧掉**。
机制：奖励把顶端个体推向**不健康区域** ⇒ 健康门槛拒掉每一次提升 ⇒ 冠军冻在种子（兜底逻辑又把它写了出来）。

### 2. 修法：零提升检测（可脚本捕获）
- 新增 **`tools/zero-promote-check.mjs`**：按训练的起点口径 `embedLegacy` 之后比 **权重 SHA1**，
  逐 seed 判定"有提升 / 零提升"，**发现零提升即 `exit 3`**。
- 用法：`node tools/zero-promote-check.mjs <臂标签> <种子文件> [seed1,seed2,...]`
- **双向验证过**（用已知臂做对照，这是它可信的依据）：

  | 对照臂 | 结果 |
  |---|---|
  | `v7soft`（已知冻死）| **6/6 零提升** ✓ 抓到 |
  | `v7wall`（已知正常）| 4 有提升 / **2 零提升** ✓ 正确区分（也解释了当时"2 个 seed 落到基线"的真相）|

### 3. 守门
**D44**：必须用权重哈希（不能靠分数）、必须 `exit 3`、必须先 `embedLegacy` 再比 ⇒ `np-test 92/92`。

### 4. 待办（顺序不变）
② **送盾惩罚**（窄惩罚、事件级可归因；该动作每局发生 18.7 次 ⇒ 能立刻见效）；
③ **环特征结构路**（按用户纠正口径：*有人真的施放了聚能环* + 我付得起小雷，升 PACK_VERSION + 重训两包）；
④ 工程债：`battle-test` 等待条件改 `state.over`、`multi` 终局收缩可达。
另：把 `zero-promote-check` 接进 `ring2-run` 收尾（自动跑），避免依赖人工记得执行。

---

## v1.5.32 — 换冠军 `v7wall-31`（换掉"送盾给对手"的 v7wall-33）+ **新增不可 `--force` 越过的硬门槛**

> 起因：用户实测两局（`results/epirus-battle-14回合 (1).txt` / `-20回合.txt`）发现冠军**全息屏障用得过多**。
> 实测坐实：**v7wall-33 每局 18.7 次全息屏障，且 100% 套在别人身上**（把"原型制御架势"白送对手）——
> 而这正是换冠军时那条被 `--force` 越过的条件（"全息屏障 > 2/局，会把盾套给对手"）。

### 1. 新增"硬门槛"（`--force` 也不放行）
- `promote-champion` 现在分两级：**普通阻断条件**（伤害/平局/G/E/F/全息总数…，可 `--force`，记 meta）
  与 **硬门槛**（不可越过）：① **全息屏障套给别人 > 6 次/局**（把套盾当主业）；② 自对局穿透卡零命中。
- `mirrorHealth` 新增 `holoOtherPerGame`（**单独统计"盾给了非自己"**，此前只有总数 ⇒ 看不出送人）。
- **阈值标定（又一次"阈值不能定在噪声带里"的教训）**：第一版取 `>2` 时把 `v7wall-31`（**2.0/局，刚过线**）也挡了；
  实测分离度是 **病态 18.7**（v7wall-33）vs **偶发 2.0**（v7wall-31）⇒ 硬门槛取 **>6**，`>2` 留在普通条件里。

### 2. 换冠军：`v7wall-31` → `js/bundled-champion-3p.js`

| 指标 | **新：v7wall-31** | 旧：v7wall-33 |
|---|---|---|
| 标准考卷 1st（200 局） | **44.0%** | 47.7% |
| D 长程反弹墙 | **100%**（墙上 7.25 伤害/局、穿透命中 84）| 100%（18.50 / 370）|
| **全息屏障/局**（自对局） | **2.0**（其中套给别人 2.0）| **18.7**（100% 送人）|
| E 无威胁摆架势 / F 活跃进攻 | **0% ✓** / 24% ✗（差 1pt，`--force`）| 25% ✓ / 20% ✗ |
| 有效技能 G | 5.24（10 种）| 4.01（10 种）|
| 自对局回合 / 伤害每局 | 27.6 / 13.7 | 24.9 / 13.3 |

- 用 `--force` 越过 1 项（**F 24% < 25%**，差 1pt 属噪声带）；**硬门槛全过**。
- 技能报告：坑=无、没学会的强招=无。

### 3. 顺带修复（用户报"冠军不可用"）
- 真因：训练收尾 `ring2-run` 用快照**还原 `index.html`** ⇒ 把脚本**缓存戳回滚**成旧值 ⇒ 浏览器在旧 URL 下继续用
  **缓存的 v6 冠军包** ⇒ `unpack` 返回 null ⇒ 页面显示"冠军包缺失/不兼容，已回退"。
- 修：`index.html` 全部脚本换成新戳 `?v=9e472e93`（正统 cache-busting）。**流程欠账**：`ring2-run` 收尾应"还原后再重新打戳"。

---

## v1.5.31 — "打断环"课题：**动作级手段全部打满**（奖励 / 池子 / 课程示范 / 暴露度），结论转"结构路"

> 用户实测驱动："我用聚能环的时候，冠军也没有做任何动作打断我"。
> 本版记录四次尝试的完整证据与结论，**为下一枪（结构/特征）留下可复现的判据**。

### 1. 先纠正一个测量假象（重要）
`--field=ringwall` 的**胜率**会骗人：v7press-36 的环墙胜率 **85.5%**，但新探针 `ringWallProbe`
实测它的**小雷次数 = 0**、对开环者伤害仅 **0.95/局** ⇒ 那 85.5% 是**熬赢**（比谁先被终局收缩耗死），不是打断。
⇒ 本课题此后**只看行为**（小雷次数 / 对开环者伤害），不看胜率。

### 2. 四次尝试（全部实测，全部失败）
| 手段 | 臂 | 小雷次数（20 局对 4×开环者）| 备注 |
|---|---|---|---|
| 环奖励常开 | v7ring / v7anneal | **0** | 环墙胜率 85.5% 但行为为 0（熬赢）|
| 奖励 + 池（1 席 ringspam）| 同上 | 0 | 池子是弱杠杆（第 4 次同型结论）|
| 奖励常开（去掉退火）| v7both / v7soft | **0** | **两臂 6/6 seed 零提升**：环奖励驱动的被动型过不了健康门槛 ⇒ 提升全被拒 ⇒ 训练冻结在热启动种子 |
| **课程示范**（反环教师 `setAntiRingTeacher`，模仿 0.5 / 0.8）| v7teach / v7teach2 | **1 → 0** | 第一次让动作"可达"（1 次），但**不可复现**，加长示范期与加大接力（0.10）后回到 0 |
| **暴露度**（池 D：4 席 ringspam）+ 示范 + 接力 | v7expo | **0** | 机会从 1/15 提到 4/18，行为仍为 0 |

- **根因**：`小雷打环` 这个动作**历史上从未被出过**，而"奖励/示范"都只能强化"已经发生过"的动作；
  动作级课程把它推到了"可达"（1 次），但没有把它变成**可复现**的行为。
- 副产品：`v7expo` 的候选保住了破墙线（穿透命中 339~349）⇒ 两个信号互不干扰，说明**框架是干净的**，缺的是"让网络看见机会"。

### 3. 本轮新增的基础设施（都是可复用的）
- `ringWallProbe`（行为探针：小雷次数 / 对开环者伤害 / 作废数）；
- `setImitTeacher` / `makeAntiRingTeacher` / `setAntiRingTeacher`（**可配置教师**，此前教师写死 heavyfire）+ worker env 通道（`EPIRUS_IMIT_TEACHER` / `EPIRUS_IMIT_GENS`）；
- 环奖励窗口 `RING_G0/G1`（示范期不干扰，教师退火后接力）+ 密集分**每局至多一次**（防刷分）；
- 训练池 `RING2_POOL=C`（墙对手）/ `D`（4 席环流）；
- 守门 **D39/D40/D42/D43** ⇒ `np-test 91/91`。

### 4. 下一枪：结构路（②，需用户裁定）
把"**某个对手 `ep ≥ 2` 且我付得起小雷**"做成**显式特征**（状态或候选特征），让网络在每个决策点直接看到"这里有活可干"，
而不是靠奖励/示范去发现。代价：**升 `PACK_VERSION` + 重训 3P 与 2P 两个包**（按用户"要加就一次全加"的原则，
建议一次把环相关的指示量加齐：各座 `ep`、我方可负担的小雷、对手是否在连环比）。
验收判据不变：`ringWallProbe.miniTCasts ≥ 5/20 局` 且标准考卷不掉、破墙线不掉。

---

## v1.5.30 — 换冠军 `v7wall-33`（修掉 D 列退化）+ 破墙奖励（方案 b）+ **"打断环"三次尝试的实测结论**

> 起因：第三方复核 `docs/AUDIT-RESPONSE-v1.5.27.md` 判定 v1.5.27 的冠军 **D 长程反弹墙 85%→0%**
> （面对 4 面反弹墙一枪未发、丢了唯一能穿反弹的激光剑线），用户 53 回合实测同型
> （`results/epirus-battle-53回合.txt`：开局一轮齐射后四个座位连续 25+ 回合只摆架势）。用户裁定先修门禁、再做 (b)。

### 1. 门禁（v1.5.28 已发）+ 本轮新增
- `reflectWall`（与 D 列同口径：4 席 reflectspam · 长程 · 轮座）：量**主动伤害/局、零伤害局、按卡落地命中**；
  阻断条件 `pierceLand === 0`；穿透卡清单**从 `R.skills` 推导**（激光剑/坦克/狙击枪/电磁炮）。
  分离性：eco-34 12.25 伤害/242 命中（过）vs v7press-36/v7f-35/v7anneal-34 全 0（挡）。
- `ringWallProbe`（**量行为、不量胜率**）：面对 4 席 ringspam 时冠军的**小雷次数**、对开环者伤害、作废次数。

### 2. 方案 (b)：破墙奖励（`PIERCE_W`，默认 0.04，两次封顶）
- 窄条件（只认能穿反弹/穿防御的卡）+ 可归因（`damage.source === seat`）+ **必须落地命中**（只出招不算）。
- 对手池新增 **`RING2_POOL=C`** = 12 对手 + ringspam + **reflectspam + guardspam**（没有墙对手时这条奖励永远触发不了）。
- **效果实测（臂 v7wall）**：

  | 冠军 | 墙上伤害/局 | 穿透卡命中 |
  |---|---|---|
  | **v7wall-33** | **18.50** | **370** |
  | v7wall-35 | 17.95 | 359 |
  | eco-34（历史最好） | 12.25 | 242 |
  | v7press-36（v1.5.27 上线）| 0.00 | 0 |

  ⇒ **D 列从 0% 回到 100%，且超过历史最好的 eco-34**；(b) 的修法有效。

### 3. ⚠️ "打断环"三次尝试的实测结论（**这条路打满了**）
- **池子是弱杠杆（第 4 次同型结论）**：把 ringspam 放进池子（v1.5.20 长程臂、v7ring/v7anneal/v7both/v7soft）都没有换来"会打断"。
- **环墙胜率会骗人**：v7press-36 的环墙胜率 85.5% 看似极好，但探针实测 **小雷次数 = 0**、
  对开环者伤害仅 0.95/局 ⇒ 那 85.5% 是**熬赢**（比谁先被终局收缩耗死），不是打断。用户实测也证实"我用聚能环时它没有任何动作"。
- **常开环奖励会把训练冻死**：`v7both`/`v7soft` 两臂 **6/6 seed 全部零提升**（产物逐字节等于热启动种子，极差 0.0），
  根因是环奖励驱动的个体是"等着惩罚开环者"的被动型 ⇒ **过不了健康门槛** ⇒ 每一次提升都被拒 ⇒ 冠军冻结。
  对照：**退火版**（v7wall/v7press）能出候选 ⇒ 环奖励**不能从第 0 代就打开**。
- 密集信号做过两版（每次 +0.5 / 每局一次 +0.5），目的是 bootstrap 一个"从未被出过"的动作；
  但它要么不触发、要么（配合常开奖励）把种群推向不健康区域。
- **结论**：环线需要**另一类手段**（课程/初始化/示范），而不是再加一个奖励项 —— 见下方待办。

### 4. 换冠军：`v7wall-33` → `js/bundled-champion-3p.js`
- **标准考卷 47.7%**（200 局，**全项目最高**；现役 v7press-36 是 44.8%）；
- **D 长程反弹墙 100%**（现役 **0%**；退化的那条线修回来了）；
- 环墙胜率 25.5%（参考价值低，见 §3）；体检 E 25% ✓、G 4.01 ✓、**F 20% < 25% ⇒ `--force`，meta 留痕 `auditFails`**。

### 5. 守门
`D39`（环奖励：窄条件/可归因/不刷分/**bootstrap 每局至多一次**）、`D42`（破墙奖励：只认穿透卡/必须落地命中/可关闭）等
⇒ `np-test 90/90`、`spec 36/36`。探针 `reflectWall` / `ringWallProbe` 已入库，后续尝试一律量**行为**而非胜率。

### 6. 待办（下一步，用户已同意试）
- **环线课程/示范**：复用现成的"脚本教师模仿"机制（`setImitUntil`/`imitBetaForGen`），
  用一个"有人开环就出小雷"的教师政策在前 N 代做示范（bootstrap 从未发生的动作），再退火交给奖励；
  验收判据 = `ringWallProbe.miniTCasts > 0` 且标准考卷不掉。

---

## v1.5.28 — 修门禁（方案 1+2）：**反弹墙穿透卡零命中 = 阻断** + 自对局穿透条件降级为提示

> 依据：第三方第二轮复核（`docs/AUDIT-RESPONSE-v1.5.27.md`）+ 用户 53 回合实测 `results/epirus-battle-53回合.txt`。
> 复核判决：**"全方位优势"不成立** —— G 4.15→6.55、F 22%→32%、重击 0.0→0.7 是真升；
> 但 **D 长程反弹墙 85%→0%**（机制：**唯一能穿反弹的激光剑线整个丢了**，墙上主动伤害 0.00）与
> **E 16%→46%**（用新口径衡量也退了 3 倍）是真退。用户实测同一形状：**开局一轮齐射后，四个座位连续 25+ 回合只摆架势**。
>
> 本轮按用户裁定只做"1+2"（**先把尺子修对**），训练侧修信号留到下一轮。

### 1. 【1】反弹墙探针 + 阻断门槛（`reflectWall`）
- 新增 `tools/audit-lib.mjs` 的 **`reflectWall(W, params, mode, GAMES)`**：与 `champ-audit` 的 **D 列同口径**
  （4 席 `reflectspam`、长程 5 血、每局轮座），统计冠军的**主动伤害/局、零伤害局、按卡落地命中**。
- **为什么必须单独测**：v1.5.27 的冠军在**自对局**里激光剑命中 **20** 次（自对局根本测不出这个病），
  但在**反弹墙**里 **0** 次 ⇒ D 列归零。门槛必须量在"墙"上。
- **门槛**：`reflectWall.pierceLand === 0` ⇒ **阻断**（穿透卡清单**从 `R.skills` 推导**：激光剑/坦克/狙击枪/电磁炮，
  规则改了它自动跟着变，不硬编码）。meta 增记 `reflectWallPierceLand` / `reflectWallDmgPerGame`。
- **分离性实测（20 局）**：

  | 冠军 | 墙上主动伤害/局 | 穿透卡命中 | 新门槛 |
  |---|---|---|---|
  | eco-34（历史最好） | **12.25** | **242**（狙击 130 / 激光剑 109 / 电磁炮 3） | ✅ 通过 |
  | v7press-36（v1.5.27 上线） | 0.00 | **0** | ⛔ **被挡（本可拦住这次回归）** |
  | v7f-35（v1.5.19 上线） | 0.00 | 0 | ⛔ 被挡 |
  | v7anneal-34 | 0.00 | 0 | ⛔ 被挡 |

### 2. 【2】自对局穿透条件**降级为提示**（数据驱动，避免"挡住所有人"）
- 复核 §4-2b 建议"每张穿透卡在**自对局**里 ≥1 命中"。实测：**坦克与电磁炮是所有冠军（含 eco-34）都没用过**的卡
  ⇒ 照字面做会把**每一个**冠军挡在门外（与 E/F"阈值定在噪声带里"同型）。
- 故该条件改为 `console.warn` 提示（数据仍进 meta），**真正的阻断只由反弹墙那条负责**。

### 3. 守门（D41 增补 6 条）
- `promote-champion` 必须调用 `reflectWall`、`rw.pierceLand === 0` 必须存在、**自对局穿透条件不得是阻断**；
- `audit-lib` 必须导出 `reflectWall`、穿透卡清单必须从 `R.skills` 推导（不许硬编码）。`np-test 89/89`。

### 4. ⚠️ 后果（如实报）：**现役冠军 v7press-36 现在过不了新门槛**
- 它通过"自对局 G 6.55 / 自身伤害 13.1 / 平局 0%"却在反弹墙前"一枪未发"⇒ 按新尺子它**不该上线**。
- 待用户裁定：**(a)** 回退到 eco-34（过新门槛、墙上 12.25 伤害，但 F 22% < 25% 仍差 3pt）；
  **(b)** 按复核 §5-1 走下一步"训练侧修信号（把破墙这条线显式补回 fitness）"后重训再决定；
  **(c)** 暂留 v7press-36 并把它记为已知退化。

### 5. 本轮未做（复核里的其余待办）
- multi 终局收缩不可达（§4-1，挂了三轮：`maxRounds=60` 让 `SUDDEN_DEATH=100` 永不触发）；
- `D29` 从"逐条点名"改成"整表穷举"（§4-4：坦克花费、狙击破防仍只有指纹在响）；
- `battle-test` 概率性失败（§4-3，1/5 FAILED，等待条件应改成 `state.over`）；
- `candidatesFor` 对 `target:'other'`（全息屏障）不展开 ⇒ `shippedAs` 措辞应加范围限定（§4-5）；
- 2P 侧 AI 标签无条件报"2人冠军"（§4-6，5 分钟）；
- `EPIRUS_FIREWEAK_PERSIST` 仍在 core（§4-7，卫生问题）。

---

## v1.5.27 — **换冠军：v7press-36**（首次"标准持平 + 打环翻倍 + 多样性翻倍"的干净提升）+ F 门槛重标定

### 1. F 门槛 35% → 25%（用户裁定，与 E 同批重标定）
- 实测**所有冠军**的 F（活跃场进攻率）都在 **22%~35%**：v7f-35 34%、v7anneal-34 35%、v7press-36 32%、eco-34 22%
  ⇒ 35% 属于"阈值定在噪声带里"，**连现役冠军自己都过不了**（当年靠 `--force` 推上去）。
- 25% 仍能挡住"正常对局里也不进攻"的形状（eco-34 22% 会被挡），但放过均衡型。
- 守门 D41 增补两条断言（门槛必须挂在新阈值上、旧 35% 必须移除）。

### 2. 换冠军：`v7press-36` → `js/bundled-champion-3p.js`（**未用 `--force`，门槛全过**）

| 指标 | **新：v7press-36** | 旧：v7f-35 | 再上一版：eco-34 |
|---|---|---|---|
| 标准考卷 1st（200 局） | **44.4%** | 44.8% | 42.8% |
| 环流墙 1st（5 血·4×环spam，200 局） | **85.5%** | 23.0% | 74.5% |
| 有效技能数 G（自对局 20 局） | **6.83（13 种）** | 3.03（9 种） | 4.24（9 种） |
| E 被动场（**新口径**：无威胁时摆架势） | 46% ✓ | 1% ✓ | 16% ✓ |
| F 活跃场进攻率 | 32% ✓ | 34% ✓ | 22% ✗ |
| 自对局回合 / 伤害每局 | 36.4 / 13.1 | 25.6 / 13.8 | 30.0 / 13.2 |
| 平局率 / 全息屏障每局 | 0% / 0.0 | 0% / 0.0 | 0% / 0.0 |

- **换前体检自动放行**（`promote-champion` 的 6 项阻断条件全过）：40 局考卷 42.5%、G 6.83、平局 0%、E 46%、F 32%；
  `rulesFingerprint = 6fa2f8e6` 已写入包，缓存戳 `?v=mtzugnpt → mtzygfkj`。
- **技能报告**（自动跑，`docs/skill-report.html`）：**坑（常用却亏损）：无**；
  消融 Δ_lost：反弹 **+4.0pt**（承重）、防御 −0.4、八卦阵 −1.9、藤甲 −1.7；
  **没学会的强招：真正的落雷 +4.2pt**（下一步的靶子）。
- ⚠️ 提醒（沿用 REVIEW §11.1）：**localStorage 里已有冠军的用户不会被这次换包影响**。

### 3. 为什么这次是"干净提升"
- 这是项目**第一次**出现"**标准分与现役持平 + 打环能力 ×3.7 + 有效技能数 ×2.3**"的候选，且**不需要 `--force`**。
- 做出它的两个信号都是**窄条件 + 可归因**的：`RING_W`（对手开环时我打断他，退火）与 `PRESS_W`（对手没威胁时我还在推进）；
  两者都只从**事件流**重建，**不动引擎**（无指纹变更、无需重记 2P 包）。

---

## v1.5.26 — **重定义 E 指标**（方案 B）：只把"没有大雷威胁时还一直摆架势"算病

> 用户裁定：*"有人攒 ji 而自己也攒并没有什么问题；当初是有人攒 ji 结果冠军就一直出防御类技能，感觉非常奇怪，所以加上了 E。
> 并不是说不能出防御，特定情况下反而是要出的，比如看到一个对手攒到 5 ji 的时候防一下大雷，但总不能每回合都这样。"*

### 1. 旧的 E 口径确实在**误伤合理防御**（实测）
旧口径 = "被动场里摆架势的回合占比 > 85% 判病"。10 局被动场实测：

| 冠军 | 旧 E（总占比） | **新 E（无威胁时摆架势）** | 无威胁最长连摆 | 判读 |
|---|---|---|---|---|
| v7f-35（现役） | 3% | 1% | 1 | ✓ |
| eco-34 | **89% ✗（判病）** | **16% ✓** | 3 | **旧口径误判**：它主要在"对手攒过 5 ジ 之后"才防，合理 |
| v7press-36 | **95% ✗（判病）** | **46% ✓** | 2 | **旧口径误判** |
| v7anneal-34 | 100% ✗ | **100% ✗** | 4 | 真病：全程无威胁也每回合防 |
| v7ring-34 | 62% | 11% ✓ | 2 | ✓ |

### 2. 新口径（`tools/audit-lib.mjs`）
- `stanceProfile(rows, threatByRound)` —— **纯函数**（可单测），产出：
  - `noThreatStanceRate`：**对手ジ < 5（大雷门槛，即"没有威胁"）的回合里**摆架势的占比 —— **主门槛**；
  - `maxNoThreatRun`：无威胁回合里的最长连续摆架势（**只作参考**：实测区分度不足，因为"无威胁窗口"只有前 ~5 回合）；
  - `stanceRate`/`maxRun`：旧口径保留，仅用于对照打印。
- `fieldRate` 负责**重建回合**（每回合每玩家最多一条 `action` 事件 ⇒ 见到重复 pid 即新回合；`round` 字段并非所有事件都有）并在回合内累计被动座位（pid 0）的 `ep` 得出威胁标志。
- **门槛**：`promote-champion.mjs` 从 `fPass.stance > 0.85` 改为 **`fPass.noThreatStanceRate > 0.6`**；
  meta 增记 `passiveNoThreatStanceRate` / `passiveNoThreatRounds` / `passiveMaxNoThreatRun`；
  `champ-audit` 的 E 列改为显示新口径（表头 `E无威胁摆架势`）。
- **守门 D41**：行为断言（有威胁的回合不计入无威胁回合 / 2/3 占比 / 连摆 = 2 / 只在该防时防 ⇒ 新口径 0% 而旧口径 50%）+ 源码断言（门槛必须挂在新口径上、旧口径必须已移除）⇒ `np-test 89/89`。

### 3. 副作用（待用户裁定）
- **E 修好后，v7press-36 的唯一门槛缺口变成 F**（活跃场进攻率 32% < 35%）。
  但实测**所有冠军都在 22%~35%**：v7f-35 34%、v7anneal-34 35%、v7press-36 32%、eco-34 22%
  ⇒ F 的 35% 门槛同样是"把所有人都挡在门外"的形状，建议一并重新标定（或降为参考列）。

---

## v1.5.25 — 惩罚被动的窄奖励（方案①）+ **出现首个"两个都要"的候选 v7press-36**

> 用户选 ①：把体检的 **E 列（被动场架势率）** 也纳入训练信号 —— 起因是 v7anneal-34 卡在 E=100%。

### 1. 实现：`countPressRounds` + `PRESS_W`（默认 0.03，三次封顶）
- **回合重建**（事件层，不动引擎）：一局里**每回合每玩家最多一条 `action` 事件** ⇒ 见到已出现过的 pid 的 `action` 即新回合
  （`round` 字段并非所有事件都有，故不能直接用）。
- **窄判据**：只记"我这个回合**打中了**、且这一回合**一点没挨打**"的回合 ⇒ 奖"对手没威胁时我还在推进"，**不奖乱打**
  （没伤害不算 / 非我不算 / 挨了打不算）。
- `setPressReward(0)` 可关；**守门 D40 为行为断言**（6 条：回合重建正确、只奖没挨打回合、非我不记、无伤害不记、两回合只记对的、可关闭）。
- 与**环惩罚退火**叠加（`ringWeightAt`），两条信号互不干扰；`np-test 88/88`。

### 2. 实验（臂 `v7press`：池 B 含 ringspam + 环退火 + 惩罚被动，6 seed）
- 配对 vs v6 控制臂：标准 Δ=−1.93（t=−0.32）、环墙 Δ=−2.50（t=−0.24）、自评 Δ=+0.52 ⇒ 均不显著；
  但 **seed 36 单项极亮**：标准 +19.0pt、环墙 **+40.0pt**。
- **200 局高精度复核 + 体检（判决表）**：

  | 冠军 | 标准(3血) | 环流墙(5血·4×环流) | G 有效技能 | E 被动架势 | F 活跃进攻 | 回合 |
  |---|---|---|---|---|---|---|
  | **v7press-36** | **44.4%** | **85.5%**（历史最好） | **6.83（13 种）**（历史最好） | **95% ✗** | **32% ✗** | 36.4 |
  | v7f-35（现役） | **44.8%** | 23.0% | 3.03（9 种） | 3% ✓ | 34% ✗ | 25.6 |
  | eco-34（再上一版） | 42.8% | 74.5% | 4.24（9 种） | 89% ✗ | 22% ✗ | 30.0 |

- **结论**：**首次出现"标准分与现役持平 + 环墙高 62pt + 技能多样性翻倍"的候选**；
  唯一门槛缺口是 **E/F**（与 eco-34 当年同一形状）。
- ⚠️ **E 列可能是"资源型打法"的误伤**：对纯ジ的被动对手，"最优解"本就可能是**攒资源/开环**而非互殴 ——
  这一点需要用户裁定（E 的阈值是否该按"是否推进胜势"重定义，而不是"是否摆架势"）。

---

## v1.5.24 — 环奖励**退火**（方案 A）+ 实验结论：打环能力练出来了，但仍是"偏科"

> 用户裁定按方案 A 试：把 `RING_W` 退火（"先拿标准分、后学打环"）。

### 1. 实现：`ringWeightAt(gen)` 退火曲线
- `RING_G0=100 / RING_G1=200`（默认）：**gen ≤ g0 权重为 0** → `g0..g1` 线性升 → `g1` 之后满额 `RING_W=0.04`；
  `setRingRamp(g0,g1)` 可调、`ringReward()` 回显 `{w,g0,g1,at0,atEnd}`。
- **守门 D39 扩到 11 条**：原 6 条归因/边界断言 + 新增 4 条退火断言（第 0 代=0 / g0 前=0 / g1 后=满额 / 中段线性）+ 可调性。
- 窄条件判据**未放宽**（对手 `ep delta ≥ 2` ⇒ 在开环；我打中他或小雷作废他才记分）。

### 2. 实验（臂 `v7anneal`：池 B 含 ringspam + 退火奖励，6 seed）
- 配对 vs v6 控制臂：标准 Δ=−4.03（t=−0.71）、环墙 Δ=−2.08（t=−0.11）、自评 Δ=−0.35 ⇒ **均不显著**；
  6 个 seed **仍只有 seed 34** 练出来，其余落回"健康但弱"兜底（15.8% / 10%）。
- **200 局高精度复核 + 体检（判决）**：

  | 冠军 | 标准(3血) | 环流墙(5血·4×ringspam) | 体检 |
  |---|---|---|---|
  | **v7anneal-34**（退火） | 29.8% | **81.0%**（历史最好） | G 3.66 ✓ / F 35% ✓ / 回合 19.6 ✓ / **E 100% ✗** |
  | v7ring-34（常开奖励） | 30.5% | 68.5% | — |
  | v7f-35（现役） | **44.8%** | 23.0% | G 3.03 / E 3% ✓ |
  | eco-34（再上一版） | 42.8% | 74.5% | E 89% ✗ |

- **结论**：① **训练信号方向正确且有效** —— 打环能力从 23.0% 一路练到 **81.0%**（超过老冠军 eco-34 的 74.5%），
  这是项目**第一次**真正拿下"惩罚开环者"；② **退火没有改变偏科**：该候选标准分仍只有 29.8%（现役 44.8%），
  差 15pt，**不换冠军**；③ 失败模式很稳定：6 个 seed 里 5 个被**健康门槛**拦回"健康但弱"的角落 ——
  说明瓶颈已从"学不会打环"转移到"**健康与强度在当前适应度下仍对立**"。
- **下一步候选（待用户定）**：把 **E 列（被动场架势率）** 也纳入奖励（现在只惩罚"开环不打断"），
  或走**按模式分冠军**（把 v7anneal-34 当长程/环流专用包，`ui.js` 按模式选包）。

---

## v1.5.23 — 修"死亡结算跑到伤害之前" + **训练信号：教它惩罚开环的人**（首次真正生效）

> 用户 22 回合实测：本版 UI 修的都挺好，只剩一处"死亡结算出现在伤害结算之期（之前），有点搞笑"；
> 同时批准"可以加训练信号了"。

### 1. 死亡结算必须排在伤害之后（纯显示层）
- 第一版显示档把 `death` 归到"中立"（tier 1）⇒ 它跑到攻击伤害（tier 2）**前面** ⇒ "先死再掉血"。
- 修：`death` → `[2, 1]`（攻击档**之后**）。守门 **D38** 增加断言"**死亡结算必须排在伤害之后**"。

### 2. 训练信号：打断开环者的**窄条件 + 可归因**奖励（`js/train/evo.js`）
- **背景**：用户实测"对手连开 9 回合聚能环把ジ刷到 +3/回合，四个冠军座位零反应"；
  而项目**三次**改对手池（把 `ringspam` 放进池子）都判定"池子是弱杠杆"⇒ 缺的不是陪练，是**信用分配**。
- **实现（不动引擎，避免指纹变更与两个包重记）**：
  - "对手在开环"的判据 = 他有一次 `ep` 事件 **`delta ≥ 2`**（环第 2 次起 +2/+3，ジ只 +1）；
  - "我打断了他" = 同一局里 **`damage.source === seat`** 打中开环者，或我用了**小雷**而他被 `voided`；
  - 奖励 `ringBonus = RING_W × min(1, 打断次数/2)`，`RING_W` 默认 **0.04**（两次封顶）⇒ 方向性引导，不动主目标。
  - 边界：不是我打的不记 / 没人开环不记 / delta=1 不算 / 同一开环者一局只记一次 / `setRingReward(0)` 可关。
- **守门 D39 是行为断言**（合成事件流直接验上面 6 条边界），`np-test` 87/87。

### 3. 实验结果（臂 v7ring：池 B 含 ringspam + 新奖励，6 seed）
- 配对 vs v6 控制臂：标准 Δ=−3.95（t=−0.69）、**环墙 Δ=−5.42（t=−0.36）**、自评 Δ=−0.39 ⇒ **均不显著**；
  6 个 seed 里只有 **seed 34** 真练出来，其余落到"健康但弱"兜底（15.8% / 10%）。
- **200 局高精度复核（判决表）**：

  | 冠军 | 标准考卷(3血) | 环流墙(5血·4×ringspam) |
  |---|---|---|
  | **v7ring-34**（新信号） | 30.5% | **68.5%** |
  | v7f-35（现役 v1.5.19 上线） | **44.8%** | 23.0% |
  | eco-34（再上一版） | 42.8% | **74.5%** |

- **结论**：**训练信号第一次真正教会了"打环"**（23.0% → 68.5%；此前三次改池子全部失败），
  但这一候选是**偏科生**（标准考卷 44.8% → 30.5%）⇒ **不换冠军**。
- **同时暴露一件事（需要用户裁定）**：老冠军 **eco-34 在环墙上 74.5%，远强于现役 v7f-35 的 23.0%，标准分只差 2pt**
  ⇒ 现役冠军的"标准分 +2pt / 集体防御 89%→3%"是用**打环能力**换来的。
  下一步候选：把 `RING_W` 调小或退火（先拿分再学打环），或改成**多目标择优**（标准分不劣于现役 + 环墙最大）。

---

## v1.5.22 — 结算事件行的**显示顺序**（用户澄清的"防御优先"真意）+ 技能格按钮回到规则顺序

> **缘起**："防御优先"这个词我连着两轮理解错了（v1.5.20 撤掉格子重排、v1.5.21 又把它加回来），
> 用户第三次澄清才说清：**"防御优先"指的是回合日志里"结算事件行"的显示顺序**；
> **技能格按钮**反而要用规则顺序（能量在最上面）—— 而且是**纯显示层**的事，"与后端实际实现无关"。

### 1. 结算事件行的显示排序（`js/ui/ui.js`）
用户给的三条规则，实现成"给每条事件算一个显示档 → **稳定排序**"：
- **档位（tier）**：`0` 防御类 → `1` 中立 → `2` 攻击类 → `3` **镜像复制**（复制出来的一律最后 ⇒ **原技能必然在它前面**）；
- **子档（sub）**：`0` 普通 / **被无效**（`voided`/`voidedBy`）→ `1` **使其无效**（`cancel`/`clash`/`thunderRing`）
  ⇒ 满足"**被无效的先显示、使其无效的后显示**"；
- 同档内用**原始下标**做稳定排序 ⇒ 与三条规则无关的事件**一行都不会被打乱**。
- ⚠️ 只排**渲染用的副本**（`list.map(...)`），`state.events` 本身绝不排序 —— 引擎怎么结算就怎么结算。
- 守门 **D38** 钉住：两个函数存在、`logEvents` 渲染排序后的副本、三个档位表 + 使其无效子档存在、
  **不得对入参 `list` 直接 sort**、同档必须稳定。

### 2. 技能格按钮回到规则声明顺序
- v1.5.21 我把 `SD_GRP`"防御优先"重排加回了技能格 —— 那是用户明确不要的（"能量类跑到防御类下面了，不符合直觉"）。
- 本版删掉重排，回到 `R.skills` 顺序（**能量 → 攻击 → 防御 → 特殊**）；**守门 D37 已翻转为"不得再做格子重排"**。

### 3. 关于"同时行动"的一处确认
- 回合标题行里的玩家条目（`玩家1=【ジ】 玩家2=【枪→玩家4】 …`）表示**同时使用技能**，
  顺序**不参与**任何重排（用户明确），只有下面的结算事件行按上述三规则显示。

### 4. 回归
- `spec 36/36` · `np-test 86/86`（新增 D38）· `smoke SMOKE OK` · `battle-test BATTLE OK`。

---

## v1.5.21 — 页面回合列表/导出三处修正 + 底部技能格恢复"防御优先"

> **起因**：用户实测报三个 bug（证据是他手动复制的 `results/epirus-battle-34回合 (1).txt`），
> 并**澄清 v1.5.15 的"防御优先"指的是对局过程中底部那个技能格** —— 我 v1.5.20 误把它撤掉了，本版恢复。

### 1. 死掉的玩家不再在回合列表里出【ジ】
- 记录第 7 回合仍显示 `玩家2=【ジ】`，而玩家2 在第 6 回合已死（**很老的问题**）。
- 修：`roundLineParts()` —— 本回合**没出手**且已死 ⇒ 显示 `【已淘汰】`；
  ⚠️ **本回合刚死但出过手的人仍显示他用了什么**（否则会吞掉致命一击那回合的信息）。

### 2. "观战"回合必须显示技能行
- 原来人类死后只写 `第 N 回合（观战）`，看不到任何人用了什么（用户原话："变成了观战而不显示使用技能了"）。
- 修：观战回合也走 `roundLineParts()` ⇒ `第 N 回合（观战）：玩家1=【已淘汰】 玩家3=【枪→玩家5】 …`。

### 3. "导出对局记录 / 上局记录"不再在玩家死亡处截断
- 根因：`autoRunRest()`（人类死后自动打完剩余回合）**从不追加 `B.transcript`**，而导出/上局记录都读它
  ⇒ 导出只到玩家死前，用户只能手动复制。
- 修：抽出 `pushTranscript()` 单一真源，正常回合与观战回合**共用**；观战回合同时 `persistBattle()`。
- **守门 D37**：钉住两个单一真源函数存在、死人不许出ジ、观战回合必须 `roundLineParts()+pushTranscript()+persistBattle()`、
  以及底部格子保持防御优先（防我再次误撤）。

### 4. 底部技能格恢复"防御优先"
- 撤销 v1.5.20 的"回到规则声明顺序"改动，恢复 `SD_GRP = { defense:0, attack:1, energy:2, special:3 }`；
  仍然**只重排显示**，`R.skills` 的顺序（牵动 AI 合法集与训练口径）一字未改。

### 5. 顺带观察（记录里的新证据，供训练信号用）
- 用户死后 玩家3/5 从第 9 回合一路熬到第 34 回合（约 25 个回合互相出ジ、偶尔狙击互抵）
  —— 这是"熬"现象在**真实对局**里的又一次实证，与 `champ-audit` 的 G/回合列同源。

---

## v1.5.20 — UI 两处修正（连带电伤文案 / 技能格子顺序）+ 指纹重记 + README 瘦身

> **起因**：用户实测 v1.5.19 的冠军（19 回合对局记录 `results/epirus-battle-19回合.txt`）后报了两个 UI 问题。

### 1. 大雷连带的**显示**错了（不是伤害错了）
- 真源是**用户裁定的 `np-test N22`：`b/c/d 各受 2 点`**，`resolve.js` 的实现也是 `amt: 2`；
  但 `js/ui/ui.js:660` 把日志文案写死成"受 **1** 点电伤" ⇒ 同一回合里出现"受 1 点电伤"与"受 2 点伤害"两条互相矛盾的日志。
- 修：文案 → **2 点**；并更正 `js/core/resolve.js` 里一条同样说"1 点"的**过期注释**（原文只写"连带伤害"没给数值，2026-09 已按 2 点定案）。

### 2. 技能格子顺序回到规则声明顺序
- v1.5.15 曾把显示顺序重排成"防御类 → 攻击类 → 能量 → 特殊"（理由：玩家先看摆什么架势）；
  实测**违反直觉** —— 能量类（ジ/蓄能/聚能环）被排到防御类**下面**，而它们是每回合最先看的资源行。
- 修：删掉 `SD_GRP` 重排表，直接用 `R.skills` 顺序（**能量 → 攻击 → 防御 → 特殊**），与规则文档、
  以及 AI 的合法集遍历顺序一致。**只动显示**，`R.skills` 本身一字未改。

### 3. 指纹重记（规则铁律的必然结果）
- 上面第 1 条动了 `js/core/resolve.js`（**哪怕只是注释**）⇒ 规则指纹 `444286ce → 6fa2f8e6`，
  `np-test D16` 立刻红。按铁律重测重记：3P（`--force`，权重与成绩未变）+ 2P 各重记一次，
  两个 bundle 的 `rulesFingerprint` 现在都等于当前值（成绩 45.5% / 94.7% 未变）。
- ⚠️ 这条也提醒：**"只改注释"照样会触发指纹**（指纹按文件内容算，不看语义）—— 是刻意的保守设计。

### 4. README 瘦身（用户要求）
- 头部原本堆了 **50 行**版本流水（v1.4.0 → v1.5.19 逐版摘要）⇒ 收成 **7 行"当前版本"摘要**，
  历史一律指向 `CHANGELOG.md`（**README 只留当前版本**）。
- 过时描述按实更新：`np-test 84/84`、`policy.js 特征 v7 / FEAT_S=213`、
  「已知限制」段按当前冠军实测重写（ジ 59% / 枪 20% / 狙击 13%、电磁炮与激光眼 0%、G=3.03）。

### 5. 长程 + 环流对手的实验（用户要求）：**能打环了，但仍不如老冠军**
- 臂 `v7long`：`RING2_MODE=long`（5 血）+ `RING2_POOL=B`（13 对手，**含 `ringspam`**），6 seed，两个考卷。
- **配对 vs v6 控制臂（n=6）**：3 血标准 Δ=**−2.58pt**（t=−0.82）、5 血标准 Δ=**+2.25pt**（t=0.53）⇒ **均不显著**。
  原因是 6 个 seed 里只有 **seed 31** 真练出来（5 血 49.9% / 3 血 38.6%），其余 5 个落到"健康但弱"的兜底（15.8% / 13.6%）。
- **决定性读数（用户抱怨的"不会打环"，4×环流墙场 · 5 血 · 40 局 1st）**：
  | 冠军 | 环流墙 1st | 3 血标准 | 5 血标准 |
  |---|---|---|---|
  | **v7long-31**（池里含环流对手） | **40.0%** | 38.6% | **49.9%** |
  | v7f-35（现役，3 血练） | 20.0% | **45.5%** | — |
  | eco-34（上一版） | **72.5%** | 42.8% | — |
- ⇒ **结论**：往池子里放环流对手**确实提升了打环能力**（20% → 40%），**但仍显著不如 eco-34 的 72.5%**
  —— 与项目当年 `ring2` 实验"**池子是弱杠杆**"的结论一致，这次在 **5 血**下复现。
  推论：要真正教"惩罚开环者"，该动的是**训练信号**（把"对手在开环时打断他"做成可信用分配的奖励/课程），
  而不是往池子里掺一个名字（这是第三次同型结论）。

---

## v1.5.19 — 一次性加全参数量（v7 特征）：候选 =(技能,目标,珠类型) + 关系块 + 效果快照块

> **起因**：用户要求"加参数一次性搞定，不然每次加参数都得重训"（HANDOFF §3 的四项：① 珠类型 ② 镜面复制内容 ③ 自身架势 ④ 目标选择）。
> **结果**：四项一次加全 —— `FEAT_S` 123→**213**、`FEAT_A` 14→**22**、`paramCount` 3337→**5689**、`PACK_VERSION` 6→**7**。
> 配对实验（同 seed / 同池 / 同热启动起点，n=6）标准考卷 1st **22.18% → 37.53%（Δ=+15.35pt, t=3.74, 5正/1负, p<0.05）**。

### 1. 特征与结构（`js/train/policy.js`）
- **候选体系**：网络不再只给"技能 key"打分，而是给 **候选 = (技能, 目标, 珠类型)** 打分
  （`candidatesFor` / `forwardCands` / `chooseCandidates`）；「蓄能」展开成 电珠/爆珠 两个候选。
- **动作特征 +8**：珠类型 2 维（否则两个蓄能候选输入**逐位相同** ⇒ 永远同分）+ 目标相对 6 维
  （目标血量 / ジ / 收割窗口 / 有前摇 / 有架势 / 是最脆的一个）。
- **状态特征 +90**：① **T 关系块**（5 玩家 × 4 维）="上一手指向自己/我/别人 + 他复制到了哪张卡"；
  ② **B 效果快照块**（5 玩家 × 14 效果）="谁身上挂着什么、谁给的"（**符号 = 自施(+)/他施(−)**，镜面复制来的架势因此自动落位）。
  ⇒ **加一个新 buff = `EFFECTS` 表加一行**；而"含义"（反弹架势意味着枪会被弹回）仍交给网络学。
- **引擎新增公共信息** `p.lastTarget/lastTarget2`（`state.actions` 每回合被清空 ⇒ 此前决策时完全看不到"上一手谁打了谁"）。
- **性能**：候选数 10→56，但配合"状态段预计算 + 决策级缓存"反而更快（**158 局/秒 vs 旧口径 90 局/秒**）。

### 2. 兼容（`docs/PARAMS-PLAN.md` §3 的承诺兑现）
- `shapeOf` 改成**按参数总量查表**（v5/v6/v7 各一条）—— v7 起 `FEAT_A` 也会变，只靠 `(len-49)/24` 反推会把
  v6 包切成 115+22（**静默错位**，正是这套机制存在的理由）。
- 新维度**一律追加在末尾** ⇒ v6 前缀逐位相同、v5 = v6 再去掉"跨得过环启动线"那一维（守门 **D26** 逐位比）。
- **`embedLegacy`**：旧形状**逐位等价**嵌入新形状（新列填 0）⇒ 训练仍可从 `champion-5p-v1.3.58.bak` 热启动
  （守门 **D34** 用独立手算参考对比，偏差 0）；**测量工具一律保持原生形状**读旧包
  （否则 chooser 路由切到候选口径，历史基线分数不可比 —— eco-34 复现 41.5% 是标定锚点）。

### 3. 实验与决策（用户裁定：全上 → 不行再二分法去掉后两个）
- 臂 v7a/v7b（12 seed，**无门槛**）：考卷最高 **48.4%**，但体检发现**分最高的全是"熬"解**
  （G 有效技能数 1.00~1.66、回合 50~60；eco-34 是 G 4.24 / 30 回合 / 伤害 13.2）⇒ **不能上线**。
  对照：v6 控制臂本身更退化（p12-36 平局 **80%**、p12-34 盾 **43.8 次/局**）⇒ **这是训练信号错配，不是 v7 引入的病**。
- **训练信号加固**：新增 `mirrorHealth`（自对局体检，与 `champ-audit` 的 G 列**同一实现**）+ 换冠军 / 落盘的**健康门槛**。
  **教训（METHODOLOGY 新增第 26 条）**：门槛加在 `finishStep` 或 `pickChampionByWinRate` 上都是**空操作**
  （12 个 seed 的权重与未加时**逐字节相同**）；只有加在**落盘那一行**才拦得住 —— 门槛要加在"决定产物的那一行"。
- 上线冠军换成 **`v7f-35`**（seed 35 过门槛）：200 局考卷 **45.5%**（eco-34 42.8%）、G **3.03**、回合 25.6、
  **E 被动场架势率 89% → 3%**（"集体防御"被治好）；用 `--force` 越过 F 列（34% < 35%，该列 10 局样本噪声大，已留痕 `auditFails`）。
  已知风险：长程反弹墙 **0%**（eco-34 85%）、F 34%、重击 0 次/局。
- **2P 包按 v7 重训**（`PACK_VERSION` 变了 ⇒ 不重训页面直接拒收）。顺带修两个真 bug：
  ① `train-best` 在"现有冠军胜出"时**回写旧形状包**（应为嵌入后的新形状）；
  ② 健康门槛拒掉首次提升后 `t.champion` 为 null ⇒ `champVsBaseline`/`evalChamp` 三处崩溃。

### 4. 新增/更新守门（各带反证）
- **D26**（更新）：常量 213/22/235/5689/7 + **三代形状**（v5 3313 / v6 3337 / v7 5689）+ v6/v5 裁剪逐位 + 游戏侧拒收 v5/v6 包。
- **D33**（新）：候选枚举（3 人局里 枪 必须展开 2 个候选、蓄能必须 2 个珠类型）/ 珠类型与目标**真的进输入** /
  掩码只改取值不改形状 / **旧包走旧口径**（target 必须逐位等于 `pickTargetN`）。
- **D34**（新）：`embedLegacy` 与"按 v6 布局手算的参考"逐位等价 + `loadAny` 对 v6/v7 的分流。
- **D35**（新）：健康门槛存在、默认开、阈值与 `champ-audit` 判读口径一致、**与体检共用同一实现**。

### 5. 未做（下一步，用户已选方向 A）
- **把"健康"折进适应度**（而不只是门口筛）：每个个体额外打 `MIRROR_GAMES` 局"5 座同一策略"，
  只把**非ジ动作分布 / 伤害 / 回合**折进已有项（**不**折胜负——镜像局的第 1 名无意义）。
  现状证据：只筛不教 ⇒ 6/7 个 seed 回落到"健康但弱"的冠军（考卷 15.8%），而健康与高分在当前适应度下是对立的。

---

## v1.5.18 — 处置第三方独立复核：A 批（工程/门槛）+ B 批（规则层定稿）

> **起因**：`docs/AUDIT-RESPONSE-v1.5.17.md`（复核人 Qoder）复现了交付门 A1~A10 与冠军成绩（41.4%），
> 但给出三类新问题：① 冠军有一个**可复现的行为空洞**；② 6 个**工程缺陷**（含两个"安全网"形同虚设）；
> ③ 7 条"**早就定稿、没人再质疑**"的规则数据**没有任何行为断言在守**（改坏了只有 D16 指纹会响）。
> **用户裁定**（本次会话开头）：**A 与 B 都做；快速模式与欧皇模式删掉；"摆墙"只修 `firstW` 接线，
> 阈值与开关等看数据再定。**

### 1. A 批：工程与门槛（**不改 `js/core/*`、不动参数量、不重训**）

1. **`firstW` 接线漏项**（复核 §5-3）。`server/train-server.mjs` 的触发条件写的是
   `whistlePen != null || dealW != null`，而 worker 三个 env 都判 ⇒ **只设 `EPIRUS_FIGHT_FIRST` 时
   worker 开、主线程不开**：产物静默少一份适应度、日志完全正常（与 v1.5.0 的 mode 半开同型）。
   历史两轮 fstA/fstB 因为同时设了 dealW 才侥幸没暴露。
   - 修法不是"补一个 `||`"（那正是它第二次犯错的方式），而是**单一来源**：新增 `server/fight-env.mjs`
     （env 名 + "要不要覆写"只有一份），server 与 worker 都从它导入；
   - worker 回执从"只回 `whistlePen`"扩成**整份** `{whistlePen,dealW,firstW}`，服务端按三项比对；
   - 两处**过期注释**（还写着"哨声惩罚长程自动 0.5"，v1.5.12 已回滚）一并更正；
   - 守门 **D24**：`EPIRUS_FIGHT_*` 只允许在 `fight-env.mjs` 里被读，出现第二个读取点就红（与 D7 同族）。
2. **2P 冠军包补 meta**（复核 §5-1）。`js/bundled-champion.js` 此前**一个 meta 字段都没有**
   ⇒ `D16`（"成绩已过期"）只罩住多人包；最后一次重训是 v1.3.47，此后 11 次提交动过 core，
   其中 v1.5.7 还把【全息屏障】移出 2P 卡表 —— 没有任何机械检查能发现"这个包是在旧规则下训的"。
   - 新增 `tools/promote-champion2p.mjs`：**不动一个权重字节**，实测当前规则下的 2P 考卷并把
     成绩 + 新指纹写进 meta（含回读自检 + 刷缓存戳）；
   - 2P 基准表（19 个）抽成单一来源 `tools/p2-baselines.mjs`（原先在 Node 侧被抄了多份），
     `train-best.mjs` 与它共用；⚠️ 键序是行为输入（考卷种子 = `20260207 + i*977`）；
   - `train-fast.mjs` / `train-best.mjs` 以后写包时**一并记指纹**；
   - **顺带修掉一个真 bug**：`tools/train-fast.mjs` 的"输出保护"是
     `const __OUT = cond ? __OUT : '<路径>'` —— **自引用（TDZ）**，脚本一加载就
     `ReferenceError`（工具等于坏的），而末尾写入又硬编码了线下路径 ⇒ 保护双层失效。两处一起修；
   - 守门 **D16 扩到两个包**（2P 与 3P 都必须记指纹与成绩）。
   - **实测**：2P 包在**当前规则**下 19 基准 × 40 局 = 平均 **94.74%**、最差基准 **72.50%**、
     全部过门（严格 >50%）⇒ **溯源缺陷，产物本身是好的**（与复核的判断一致）。
3. **播种：两个真因 + 守门重写**（复核 §5-2）。
   - **真因一**：`server/train-server.mjs` 的 2P 入口 `runTrain` **整段没有任何播种**
     （`makeTrainer → makePolicy / evo.breed()` 全走宿主随机）—— 而 `js/bundled-champion.js`
     正是这条路的产物。已按 N 人路径同一套补上（`P.setRng` + `__seedSandbox`）；
   - **真因二**：`tools/diag.mjs` 是**第 6 个**训练入口，既不在 REPRO 的扫描表、也不在 REPRO2 的名单里，
     而且完全没播种 ⇒ 每次跑出的诊断对局都不同。已补；
   - **守门 REPRO2 重写**：原实现是**字符串存在性检查**（对硬编码的 5 个文件 grep `seedSandbox|setRng`），
     所以"同一个文件里 `runTrainN` 有播种、`runTrain` 没有"照样通过 —— 守门守的是**文件**不是**入口**
     （这正是本次两个真因能长期存活的原因）。现在：(a) 名单按 **`.makeTrainer(` 调用点反查**（不再硬编码）；
     (b) server 的两个入口**按函数体切分**、各自要求播种；(c) 可复现性交给 **D25 实测**。
   - 守门 **D25**（新）：同 seed 跑两遍**逐字节相同**、换 seed **必须不同**（两条都要，后者防"播种了没人用"）。
   - 守门 **D26**（新）：钉住 `FEAT_S=123 / FEAT_A=14 / FEAT_N=137 / HID=24 / PACK_VERSION=6 /
     paramCount=3337`，验证 `shapeOf` 对新旧形状的反推（v5 必须被标 `legacy`），
     并**逐位**校验 v5 裁剪规则 = "v6 去掉运行时探测到的那一维"。
     （`PARAMS-PLAN.md` 承诺过这条守门，但**它并不存在** —— 复核 §5-7 实测。）
4. **体检指标从"只打印"变成"阻断条件"**（复核 §7-4(1)）。
   - 指标（B/C/E/F/G）抽成共享库 `tools/audit-lib.mjs`：`champ-audit.mjs`（打印表格）与
     `promote-champion.mjs`（**门槛**）共用同一份 —— 原先要拿它当门槛就必须再写一遍（本项目栽过四次的事）；
   - `champ-audit` 新增 **G 列 = 有效技能数**（**非ジ**成功出手分布的 `exp(熵)`）；
   - `promote-champion` 的 6 项全部**阻断**（原先只有 3 条 `console.warn` + "决定权在你"）：
     伤害/局<5、平局率>20%、全息屏障>2/局、**E 被动场架势率>85%、F 活跃场进攻率<35%、G 有效技能数<3**；
     越过必须 `--force`，且把越过的条件写进 meta（`auditFails`/`auditForced`）留痕；
   - 守门 **D27**：指标必须单一来源 + 阻断路径必须存在（`exit(6)`）+ `--force` 必须留痕。
   - ⚠️ **实现时发现阈值本身有坑**（值得留档）：G 在**小样本下系统性偏低** —— 同一个冠军 eco-34 实测
     **n5=3.08 / n10=2.99 / n20=4.24 / n40=4.54 / n80=4.21 / n160=4.21**（罕见技能没出现时熵被低估）。
     复核建议的 `G<3` 在 n=10 下**落在噪声里**（会把"样本不够"读成"打法坍缩"）。
     ⇒ `promote-champion` 的自对局默认局数 **10 → 20**，且 `G` 的样本下限写进注释与告警。
     重新读数后 eco-34 的 G=**4.24**（不再触发），**真正被卡住的是 E=94% / F=21%** 这两项。
5. **`battle-test` 不再弄脏工作区**（复核 §5-9）：截图默认落点从**被 git 跟踪**的
   `results/battle-0{1,2,3,4}.png` 改到未跟踪的 `docs/artifacts/battle-out/`（已进 `.gitignore`）。
   复核者实测"照抄验收单里的命令会脏工作区、还得 `git checkout --` 还原"。

### 2. B 批：规则层定稿（改了 `js/core/*` ⇒ 规则指纹变 ⇒ 末尾统一重记一次）

6. **删除快速模式与欧皇模式**（用户裁定）。此前 `MODES.fast` 无入口但**引擎分支还在**
   （`state.js`/`play.js` 各一条 `modeKey === 'fast'`）、连带 `guardLimit: 2` **纯死字段**（全库零消费者）
   与 `p.guardStreak`（只写不读）；`MODES.lucky` 无入口、无测试、零引用；
   而 `tests/spec.js` 的 R52 用例**还在跑**（守一条不可达路径）。复核 §3-2 的原话是
   "**有实现、有测试、无入口是最容易骗过自己的形态 —— 作者本人都以为删了**"。
   - 一次删干净：`rules.js` 两个模式 + `guardLimit`、`state.js`（字段 + 分支）、`play.js` 分支、
     `ui.js` 的 `MODE_NM` 标签、`spec.js` 的 R52 用例（⇒ **spec 37/37 → 36/36**）；
   - `docs/RULES-2P.md` 新增 **§8.1「本程序不做的模式」**，显式登记 快速 / 欧皇 / **吸血鬼模式**
     （原始规则有、本程序不做）；
   - 守门 **D28**：`MODES` 的每个 key 都必须能在 `index.html` 的 `#sel-mode` 选到，**反向**也查
     （页面不许有 `MODES` 里不存在的幽灵选项），并要求 RULES-2P 里有"本程序不做"的登记。
7. **补 8 条"只有指纹在响"的规则数据行为断言**（复核 §2-2 的 17 个变异注入）。
   - 守门 **D29**：判定 p=**严格** 0.5（0.49 胜 / 0.5 负 / 0.51 负）、**爆头必须三轮全胜**
     （2 胜 1 负 ⇒ **不**爆头）、小雷豁免名单恰好 = 防御/反弹/原型制御（+ 防御族**穷举表态**：
     每张防御卡必须显式归入"豁免/不豁免"，新增卡不归类就红，这正是复核 R1 建议的机械化）、
     大雷 5 ジ / 全息屏障 1 ジ（走 `attemptAction` **行为**口径而不是只比常量）、
     雷系集合含电磁炮且避雷针情形 A 真的把它无效化；
   - **修掉一条恒真测试**（复核 §2-3 的教科书样本）：`tests/spec.js` 里那条名叫
     「三判定全胜=爆头」的用例，构造是 `seqRng([0.1,0.1,0.1])` ⇒ 把 `judge3` 改成 `judge`（只判一次）
     **照样绿** —— 它锁住了一个输出，没锁住规则里的那个"3"。已补一组"胜、胜、负"的判据。
8. **铁索连环改成一次性**（用户裁定，按原文修正；复核 §3-1）。原文是「**下一次**当其中一个角色
   受到伤害时，另一个也受到相同伤害」⇒ 共享过一次**两侧连边同时解除**；此前实现成"持续状态、
   只有一方死亡才解除"，等于把一次性触发做成了持久光环。要再连必须重新互勾一次摄魂指法。
   - 守门 **N8 改写**（边共享 / 不递归 / 一次性 / **对称解除**）+ **D30**（走真实摄魂路径，
     并钉住"合二为一保持 `>=2`"：3 人同轰同一目标**只额外 +1 一次** —— 这是用户同批裁定）。
   - `tests/spec.js` 的 R45 用例原先连续打两次、指望连边还在（同型错误语义），已按新语义改写。
9. **复制避雷针 / 复核 R2：结论是"无需改代码"，但顺带修掉一个真实偏差**。
   - R2 提议把"复制到避雷针"那一支从 ④b **提前到 ② 之前**，好让它挡本回合在飞的雷。
     **实现时实测证明那是空操作**：① 层的避雷针语义是"**当回合有任何雷系技能 ⇒ 它们全部无效**"（全场），
     而"镜面反射能复制到避雷针"的**前提**就是 t1 本回合真的用了避雷针 ⇒ ① 必然已经把那一回合的雷清空了。
     用户要的**结果**（复制者不会吃到那个雷）本来就成立，机制来自 t1 的**真**避雷针。
     ⇒ **没有**把提前落位写进代码（"有实现、有测试、无入口"正是审计点名的最危险形态；
     本项目 §14.7 的教训也是"为了让某个例子成立去改结算语义是危险动作"）；
   - 改为把事实**钉成守门 D31**（三场景）：复制避雷针这一局里 ① 必须仍然全场无效化那个雷
     （若将来有人把 ① 收窄成"只无效化指向避雷针使用者的雷"，D31 立刻红）；
     **真实偏差**：免雷窗口（R31 情形 B）先前**挡不住电磁炮** —— 电磁炮在 `R.LIGHTNING` 里
     （影响避雷针回馈/大雷传导口径），但它的投递从不查 `rodGuard`。已按 ②/③ 同口径修；
     以及**回归场景**：复制的是**防御架势**时，"大雷 pri4 先于镜面反射 pri3"（v1.5.17）仍然完全成立。
   - `docs/RULES-NP.md` N14 的避雷针条目按上述结论重写（原"不追溯"表述作废）。
10. **规则指纹重记**：`8cd4d731` → **`b2ea7f0b`**。按项目铁律用
    `node tools/promote-champion.mjs docs/artifacts/eco-34.bak --exam-games=40` 重测重记；
    它**首次实战**了第 4 条的新门槛：eco-34 被 **E/F 两项卡住**（`--force` 越过后写进 meta）。

### 3. 回归与反证（这一版每条新守门都做了"改坏 → 应红 → 改回"）

| 项 | 结果 |
|---|---|
| `node tools/spec-run.mjs` | **36 / 36**（删掉 R52 后 37 → 36） |
| `node tools/np-test.mjs` | **80 / 80**（新增 D24~D31 共 8 条） |
| `node tools/smoke.mjs` | **SMOKE OK**，0 JS 错误 |
| `node tools/battle-test.mjs` | **BATTLE OK**，0 JS 错误；19 次真实伤害、全息屏障 0 次；截图落 `docs/artifacts/battle-out/` |
| `node tools/rules-fingerprint.mjs` | `8cd4d731` → **`b2ea7f0b`**（两个 bundle 的 meta 都已同步） |
| `node tools/promote-champion2p.mjs` | 2P 考卷 94.74% / 最差 72.50% / 过门 ✓；权重逐字节未变 |
| 反证 5 条 | **全部达标**：D28（塞一个页面选不到的模式）、D29（`judge3`→`judge`）、D30（不断边）、D31（免雷窗口不挡电磁炮）、D26（v5 裁剪**砍错维度**而非长度错） |

### 4. 教训（写给下一个会话）

- **"守门"要看清它守的是文件还是入口**：REPRO2 只做字符串存在性检查，于是"同一文件里另一条路有播种"
  就能让整条未播种的入口长期活着（而它产出的是线上 2P 包）。现在的判据是**调用点反查 + 按函数体切分 + 实测幂等**。
- **阈值先量样本敏感性**：G 的 `n5=3.08 / n10=2.99 / n20=4.24` 说明"用 10 局卡 G<3"是把样本量读成打法坍缩。
  凡是"算完就打印"的指标，变成门槛**之前**都要先问它收敛了没有。
- **可证明是空操作的改动不要落地**：R2 的提前落位能自洽地写出来，但① 层已经覆盖了它 ⇒ 留着就是
  第二个"有实现、有测试、无入口"。正确做法是把**事实**变成守门（D31），而不是把推理变成代码。
- **"两处各写一遍"这一版又抓到三处**：反摆烂 env（server/worker）、体检指标（audit/promote）、
  2P 基准表（多处）。三次都抽成单一来源并配守门（D24/D27/*）。
- **复核者给的命令也会脏工作区**：`battle-test` 原先覆写被跟踪的 `results/*.png` ⇒
  只读复核变成了"改文件"，已改到未跟踪目录（同类问题见 §5.2-13 的"两处各写一半"）。

### 5. 本版**未做**（留给下一步，需用户拍板）

- **加参数量 / 重训**：`PACK_VERSION` 仍 6、参数量仍 3337。复核 §7-1 推翻了 `HANDOFF §②` 的"路线 A 不动参数量"
  （动作特征是纯 key 查表 ⇒ 拆「蓄能(电)/(爆)」两个候选会拿到**逐位相同**的特征 ⇒ 同分），
  并建议维度优先级 **④目标选择 > ③自身架势 > ①珠类型 > ②镜面复制内容**（按"该分支的实测访问率"排）。
- **反摆烂的取值**：`FIRST_W` / `WHISTLE_PEN` / `SUDDEN_DEATH` 仍未默认开启（用户："等看数据再定"）。
  现在有 E/F/G 三列 + 阻断条件当数据了。
- **待裁定**：`docs/AUDIT-RESPONSE-v1.5.17.md` §2-2 其余注入点（八卦阵/金刚盾等链路的胜率依赖已由 D29 钉住）、
  §3-1 的两张低概率卡已裁定（铁索按原文、合二为一保持 `>=2`）、§5-4/§5-5/§5-6 与 R1 的处置见上。

---

## v1.5.17 — 镜面反射"生效时机"改回优先级口径（用户二次裁定：大雷先结算 ⇒ 复制不会发生）

### 1. 规则修正（推翻了 v1.5.16 的一处实现）
用户原话：「我觉得你对优先级的理解是对的，**大雷优先于镜面反射**，所以**反弹并没有被复制成功**，是我错了，这里改过来吧。」
- **含义**：大雷(pri4) / 小雷(pri5) 都在镜面反射(pri3) **之前**结算，而大雷的"目标非防御类技能一律无效化"
  会把镜面反射**废掉**（镜面反射是特殊类，**不算防御族**）⇒ **复制根本不会发生**，目标该挨打就挨打。
- **改法**（`js/core/resolve.js`）：
  · 复制来的架势**只从"镜面反射真正结算那一刻"起生效** —— 落到状态字段 `p.copiedGuard`（`applyMirrorSelf` 在 ④b 写入，
    `startTurn` 清空），`guardOf` 读它；**撤掉** v1.5.16 的"声明即生效"（`effectiveKeyOf` 与 `guardOf` 的派生分支）。
  · 大雷的"非防御类无效化"、小雷的防御族豁免、大雷连带的防御豁免**全部回到"看行动本身的 key"**（镜面反射不再被豁免）。
  · **撤回** v1.5.16 加的 `snapT2`（"空指参与大雷连带"）：既然这次施法已被废，那个"空指"根本不会产生
    ⇒ 连带判定回到原口径（只看 `targetOf`）。
  · `js/core/state.js` 的 `freshPlayer` 新增 `copiedGuard: null`。
- **守门 `np-test D23②` 改写**：a 复制 b 的【反弹】并指向 c、d 用大雷打 a ⇒ 断言 **a 的镜面反射被无效化**、
  **a 实打实挨 2 点电伤**（3 → 1）、**没有产生任何复制**、b 有自己的反弹所以连带打不动它、c 不受影响。
  ①（复制来的反弹挡得住随后结算的 ⑤ 枪并把它弹回）保持不变 —— 它正是"从结算那一刻起生效"的正例。
- **反证三条**（各自红在不同断言，实测）：① 把 v1.5.16 的两处改回去（架势从声明派生 + 大雷豁免镜面反射）⇒
  「a 的镜面反射必须被大雷无效化」红；② 自效果表失效（复制【ジ】不给 ep）⇒ 「+1 ep」红；
  ③ t1 被作废时仍去复制（直读 `state.actions` 代替 `actionOf`）⇒ 「唯一合法的无可复制」红。
### 2. 用户三次澄清：**同优先级时防御类先出现**
用户原话：「复制的来的防御反弹之类**可以**挡得住同优先级对应能挡住的激光剑或坦克。**同优先级的时候防御类先出现**。」
- **含义**：镜面反射是 pri3，激光剑 / 坦克也是 pri3 ⇒ 复制来的**防御架势**必须在 pri3 的攻击**之前**落位，
  按原本的伤害矩阵生效（反弹挡得住坦克并弹回；而激光剑**穿反弹** ⇒ 挡不住）。
- **改法**（`js/core/resolve.js`）：新增 `mirrorGuardPass(state)`，在 **④ 默认优先级 3 的循环之前**调用，
  **只落防御族**的架势（`p.copiedGuard` + `guardSet{copied,early}` + 无极变速的 `guardNext`）；
  其余自效果（ep / 珠 / 雷 / 净化）仍留在 ④b 的 `applyMirrorSelf` 里结算（`applyMirrorSelf` 加了
  `!me.copiedGuard` 兜底，避免重复落位/重复发事件）。
- 与二次裁定不冲突：大雷（③）/ 小雷（②）都早于 `mirrorGuardPass` ⇒ 目标若被它们作废，
  `actionOf(m)` 为 null ⇒ 直接跳过 ⇒ 复制照样不会发生。
- **守门加两组**：`D23②c`（同优先级的坦克被复制来的反弹挡住并弹回）、`D23②d`（激光剑穿反弹 ⇒ 挡不住，
  伤害矩阵未被改坏）。**反证第四条**：删掉 `mirrorGuardPass` 的调用 ⇒ `D23②c` 红（got=2 want=3）。
- 回归：`spec 37/37`、`np-test 72/72`、`smoke SMOKE OK`、`battle-test BATTLE OK`。
  规则指纹 `e2241072` → `8cd4d731`（重测重记冠军成绩见 bundle meta）。

## v1.5.16 — 镜面反射回归原始规则：**没有"无可复制"**（非伤害类复制到自己身上 + 对 t2 空指）

> ⚠️ **本文的"生效时机：声明即生效"与"空指参与大雷连带"已被 v1.5.17 推翻**
> （用户二次裁定：大雷 pri4 先结算 ⇒ 镜面反射被无效化 ⇒ 复制不会发生）。其余部分（没有"无可复制"、
> 非伤害类复制到自己身上、自效果对照表、30 技能穷举守门）继续有效。


### 1. 规则修正（用户 2026-09-13 澄清；规则依据入库 `docs/RULES-NP.md` N14）
- **旧实现**：`copyEffect(key)` 返回 null（架势 / 自增益 / 能量 / 状态类）时只发一条 `mirrorNoEffect`「无可复制」
  ⇒ 事实上**大部分技能复制了等于没复制**。
- **新规则**：**所有技能都能复制**。非伤害类技能的效果**落在使用者自己身上**（"相当于自己也摆了那个架势 /
  也蓄了能 / 也架了雷"），同时对 t2 形成一个**"空指"**：指向保留、本身不产生效果，但**要参与大雷连带判定**。
  只有「t1 本回合行动不存在或已被作废」才是唯一的"无可复制"。
- **实现**（`js/core/resolve.js`）：新增 `applyMirrorSelf`（自效果表）、`mirrorRefs`（t1/t2 的单点真源，
  结算 / 架势判定 / 无效化判定 / 连带参与判定四处共用）、`effectiveKeyOf`（镜面反射的"有效技能" =
  它复制到的那张卡）、`guardKindOfKey` + `copiedGuardKind`（技能→架势的唯一真源）；大雷快照新增 `snapT2`，
  连带的"产生交互"判定改为认**两个**被作用者（镜面反射的 t1/t2、双枪的 target2）。
- **生效时机**：复制来的架势在**声明那一刻**就生效（`guardOf` 读"有效技能"）——
  这是用户例子的硬要求：「a 用镜面反射复制 b 的【反弹】、指向 c，d 用大雷打 a ⇒ c 吃连带，a、b 因有反弹不受影响」。
  同一张"有效技能"也接到大雷效果2 的防御族豁免、小雷豁免、激光眼破防上（否则复制来的盾会被这些判定漏掉）。
- 两个必须写清的细节：① 复制**蓄能**的珠类型取**复制者自己**的选择，**不读 t1 的**（t1 蓄哪种珠是隐藏信息，v1.5.15 刚修）；
  ② 复制**全息屏障**的自效果 = 原型制御式自保（v1.5.7 已确认两者防御等价）；复制**藤甲**只得到架势，
  不复制"贴给对手使其火弱"（那是对外效果 ⇒ 空指不产生效果）；复制**避雷针**只得到 3 回合免雷窗口，
  **不追溯**本回合已在飞的雷（① 避雷针那段早于镜面反射结算）。
- **守门 `np-test D23`**（7 组）：① 复制反弹后是真架势（挡枪 + 反弹回 1 伤）② **用户例子的端到端复现**
  ③ 复制ジ +1 ep ④ 复制蓄能的珠类型不泄漏 ⑤ t1 被作废 ⇒ 唯一合法"无可复制" ⑥ **全部 30 个技能**必须
  被显式归类（可复制伤害 / 自效果 / 仅空指 —— 新增技能不归类就红）⑦ 每个自效果技能真的复制一遍，看架势/资源是否到位。
  **反证三次全红**（各自对应不同断言）：① 让自效果表失效（复制【ジ】不再给 ep）⇒ D23③ 红；
  ② `guardOf` 改回只读 `a.key` ⇒ D23① 红（"复制到的反弹必须是真架势"实测 null）；③ 去掉大雷的 `snapT2` ⇒ D23② 红（c 不掉血）。
  ⚠️ 注意一个**反直觉点**：架势是**派生**的（`guardOf` 现算，不落状态）⇒ 删 `applyMirrorSelf` 的 default 分支
  **不会**让 D23 红（它影响的是日志事件与无极变速第二回合的 `guardNext`）—— 写反证要选"能真正改变行为"的那一处。
- 回归：`spec 37/37`、`np-test 72/72`、`smoke SMOKE OK`、`battle-test BATTLE OK`。
  规则指纹 `a69358d6` → `e2241072`（改了 `rules.js`/`resolve.js`）⇒ 已用 `tools/promote-champion.mjs` 重测重记冠军成绩。

### 2. 补文档债
v1.5.14 / v1.5.15 此前只有提交信息与 `docs/HANDOFF.md` 记录、没写 CHANGELOG（违反"一个版本一条"）——
本次补齐，见下面两条。

## v1.5.15 — 修蓄能珠类型的信息泄漏（三处）+ 技能格子显示顺序改成防御类优先

### 1. 信息泄漏（用户 2026-09-13 实测 `results/epirus-battle-57回合.txt` 提出）
规则：`蓄能` 蓄的是电珠还是爆珠**只有本人知道**，别人只知道"有人蓄了能"。旧实现三处泄漏：
- ① **AI 特征**（`js/train/policy.js`）：聚合项的对手 `.elec/.boom` + 逐对手槽位的 elec/boom 直接读了对手珠类型
  ⇒ 等于给 AI 开天眼。修法：对手珠信息只保留「**有人持珠（类型未知）**」；槽位数量不变（FEAT_S 仍 123）⇒ 旧权重仍可加载。
- ② **页面侧栏**：对手的珠类型照实显示 ⇒ 改成「珠 ?」。
- ③ **日志**："完成蓄能（电珠）" ⇒ 改成"完成蓄能（珠的类型只有他知道）"。**消耗**照旧公开
  （放电 / 激光眼本身已经暴露了类型，再藏没有意义）。
- 判据（`np-test D22`）：构造两个**只差"2 号位珠类型"**的状态 ⇒ 0 号位看到的特征向量必须**逐位相同**
  （旧代码差异下标 `[6,7,66,67]`）；同时必须仍能感知"有对手持珠"与"有对手刚蓄能"（不能把公开信息一起砍掉）。

### 2. UI：技能格子显示顺序改为 **防御类 → 攻击类 → 能量 → 特殊**（只重排显示，不动 `rules.js` 技能表顺序）

## v1.5.14 — 先手激励实测（能治"集体防御"但代价太大）+ 冠军体检新增 E/F 两列

### 1. 先手激励（`EPIRUS_FIGHT_DEAL` / `EPIRUS_FIGHT_FIRST`，**默认关**）
- 动机：多人局难度=冠军时多座冠军互相不敢先手，局面退化成"集体防御"（没人进攻）。
- 实测（12 对手池 + 6 seed，干净配对）：**5/6 seed 把"集体防御"打掉**（活跃场进攻率上去了），
  但标准考卷从 42.9% 掉到约 **29~30%（−13.6pt）**；长程臂 `fstB-34` 是最佳候选
  （考卷 35.3% + 16.3 伤害/局含 14.6 重击 + 被动场架势率 1%）。
- ⇒ **默认保持关闭**。这个代价本身就是结论：当前规则结构在惩罚进攻（详见 `docs/REVIEW-5P.md` §13）。

### 2. 冠军体检表 `tools/champ-audit.mjs` 新增两列
- **E 被动场架势率**：没人进攻的退化局面里该冠军摆架势的比例；
- **F 活跃场进攻率**：正常对局的进攻频率。
- "集体防御"就是这两列量出来的（`eco-34`：E 95% / F 24% —— 没人打它时它 95% 的时间在摆架势）。

## v1.5.13 — 修狙击枪"被干扰"判定（多人局 bug，用户对局记录发现）+ 口径澄清（冠军 vs 风格脚本）

### 1. 修 bug：狙击"被干扰"只认"**狙击手本人被攻击**"（多人局专属错误）
用户在 `results/epirus-battle-22回合.txt` 第 16 回合发现：玩家1 狙玩家5，玩家5 用**枪打玩家3**，
狙击却判了"被干扰"无效。
- **原始规则**（老 README 特殊 2）：「若被作用者或其余玩家对**该玩家**（＝狙击手）使用其它带攻击效果技能则狙击无效」；
  特殊 3 的爆头条件同样写「若**被作用者**使用的技能对**攻击者**（＝狙击手）没有影响」。
- **旧实现**：`const ta = actionOf(state, t); if (ta && ATK_EFFECT.includes(ta.key)) → 无效` ——
  **只看被击方用了什么技能、不看它打谁** ⇒ 多人局里目标去打第三个人也会废掉狙击。
- **为什么一直没发现**：2 人局里目标只有狙击手一个可打对象 ⇒ 旧实现恰好等价；且枪（pri2）本来就由
  优先级规则阻止狙击（pri1）⇒ `spec R13/57` 一直绿。
- **修法**：新增 `aimedAt(state, who)` —— 遍历所有**非自己**的行动，找"用攻击类技能（`ATK_EFFECT` ∪ `TRANSFER`）
  且目标 = who"的人；只有这样指着狙击手才判"狙击被干扰"。爆头条件仍只看**被击方本人**（与干扰是两个口径）。
- **守门 `np-test D20`** 4 条：① 目标打第三方 ⇒ 狙击必须**命中**（用户场景的复现，旧代码下必红）；
  ② 第三人打狙击手 ⇒ 无效；③ 目标自己打狙击手 ⇒ 狙击被废（多人局通常走优先级"被高优先级攻击阻止"，
  故只断言行为、不锁 `by`）；④ 2 人局口径不许回退。
- 修复后复现同一局面：事件为 `{"type":"damage","to":4,"reason":"狙击枪","via":"snipe","source":0}` ✓。

### 2. 口径澄清：你打的是**风格脚本**，不是冠军（这条不是 bug）
用户反馈"冠军枪使用率很高、与 skill report 不符"。对局记录头写着 `难度=读招反制` ——
那是**风格脚本** `st:combocounter`，不是冠军。实测（5 人 · 12 局 · AI 4 席）：

| AI 座 | ジ | **枪** | 其他 |
|---|---|---|---|
| **冠军 `eco-34`** | 53.1% | **0%** | 激光剑 11.6% / 狙击枪 11.2% / 反弹 8.5% |
| `st:combocounter`（读招反制，你这份记录的难度） | 34.3% | **34.3%** | 反弹 31.5% |
| `st:aggro`（激进快攻） | 50.7% | **49.3%** | — |

冠军的枪使用率在**真实对局（原生经济）与 skill report（富裕经济）里都是 0%** ⇒ 两边一致，不是口径冲突。
① 想看冠军就把难度选成「**冠军（最强）**」；② skill report 的「使用率」列是**富裕经济**口径
（每回合把 ep 补到 10），主口径是「Δ_lost 强度」列；"没学会的强招"那栏也要按 §7.6 读
（富裕经济会把"1 费整回合免伤"这类便宜可重复卡高估；原生经济 `--inject` 实测 **原型制御 −34.5pt**）。
已写进 `docs/HANDOFF.md` §5.2-18。

### 3. 冠军按流程重测重记
规则指纹 `a691e020 → 1510dafb`（改了 `resolve.js`）⇒ `D16` 立刻红。重测：`eco-34` 多人考卷
**40.6%（40 局）**（上次 42.9%，差异在 40 局噪声范围内），自对局 13.6 伤害/局、0% 平局 ⇒ **维持上线**。
（`promote-champion` 自动跑的 skill report 同步更新。）

回归：spec 37/37、np-test **69/69**（新增 D20）、smoke SMOKE OK、battle-test BATTLE OK。

## v1.5.12 — 回滚"长程默认开哨声惩罚"（实测是**空操作**，证据：两条臂产出逐字节相同）

### 为什么回滚
v1.5.11 我按推理把哨声惩罚设成"长程默认 0.5"（理由：长程上限 140、终局收缩到第 100 回合才起效）。
用户说"按你的意思做"之后，我**做了干净配对验证**：`lngC`（长程 · 自动 0.5）vs `lngD`（长程 · 显式 0），
其余全同（12 对手池 / 6 seed / 起点 v1.3.58 / 250 代 / 同引擎）——
**6 个 seed 里能比的 5 个，两份产物逐字节相同**，配对统计也完全一致
（自评 Δ=+2.06、标准考卷 Δ=+9.23、反弹墙 Δ=−8.33；因为两条臂本来就是同一个东西）；
而两份 meta 分别正确记着 `whistlePen:0.5/override:null` 与 `0/override:0` ⇒ **接线没问题**。
真正的原因是**这条规则在长程从不触发**：终局收缩 + 5 血 ⇒ 长程局几乎总以"有人被淘汰"结束
（v1.5.10 实测 12/12）⇒ `aliveEnd >= 2` 的哨兵局 ≈ 0%。
（它有用的是**多人 3 血**：上限 60、收缩不触发 ⇒ 哨兵局常见，v1.5.9 实测回合 55→14~39、重击 0→5~9。）

### 改动
- `js/train/evo.js`：`WHISTLE_PEN` 回到 **0（默认关）**，`whistlePenNow()` 只回显显式值；
  "按模式猜默认值"的代码删掉，理由写进注释（防止以后又凭推理加回来）。
- `np-test D18`：断言 multi/long **都**默认 0，且仍可显式开（env / `setFightReward`）。
- 长程臂 `lngC`/`lngD` 的产物与体检入库（REVIEW §11.8）：**没有比 `eco-34` 更好的冠军**
  （标准考卷均值 30.4% < `eco-34` 的 42.9%）⇒ **上线冠军不变**。
- 顺带：`server/*.mjs` 现在把**实际生效**的奖励参数打进日志（`[fight] 生效值` / `[eco] 生效值`）并写进产物 meta
  （`fightEffective`/`ecoEffective`），且 worker 会回报 `fightPen` 回执、服务端核对不一致就**中止整个 run**
  —— 防的是"服务端开了、worker 没开"这种静默半开（本项目栽过 4 次的老毛病）。

回归：spec 37/37、np-test 68/68、smoke SMOKE OK。

## v1.5.11 — 终局收缩参数化（起点/每回合扣血可在页面调）+ 长程训练默认开哨声惩罚 + 换冠军自动跑技能报告

> 三条都是"按你说的意思做"：前两条是我上轮提的开放项，第三条是你说的"以后每次冠军有大变化都做一下"。

### 1. 终局收缩参数化：`SUDDEN_DEATH` / `SUDDEN_DEATH_DMG` + **页面入口**
- `rules.js`：新增并导出 `SUDDEN_DEATH_DMG = 1`（每回合扣多少血，原先写死 1）；
- `state.js`：`createState(key, rng, n, opts)` 的 `opts` 现支持 `suddenDeath` / `suddenDeathDmg`
  —— 内部**浅拷贝**模式对象（`R.MODES[key]` 是全局共享的，直接改会串到之后所有对局；D19 ⑥ 专门钉这条）；
- `resolve.js`：扣血量走 `mode.suddenDeathDmg → R.SUDDEN_DEATH_DMG`；
- `index.html` / `ui.js`：对局控制区新增**「终局收缩 [起扣回合] [每回合扣血] 血/回合」**入口，
  改动即存 localStorage（`epirus.sudden`），下次打开自动读回。
- 守门 **D19** 扩到 6 条：全局默认、99 回合不触发、100 回合全员 −1、有防御也照掉、事件可追溯（不计战功）、
  模式可覆盖关掉、**opts 覆盖生效且不污染全局**。

### 2. 长程训练**默认开**哨声惩罚
`WHISTLE_PEN` 改为"自动"：**长程默认 0.5**、其余模式默认 0（显式 `EPIRUS_FIGHT_WHISTLE` 仍可覆盖）。
理由：长程上限 140，而终局收缩要到第 100 回合才起效 ⇒ 在那之前"熬"仍然划算；实测 0.5 能把这类摆烂改掉
（REVIEW §11.7：回合 55→14~39、cost≥3 重击 0→5~9）。
产物 meta 现在同时记 `fightOverride` 与 **`fightEffective` / `ecoEffective`**（只记 env 会漏掉"默认开的"那条）。
守门 **D18** 加固：multi 默认 0、long 默认 0.5、显式覆盖优先、reset 回到自动。

### 3. 换冠军自动跑技能报告（用户要求）
`tools/promote-champion.mjs` 写完 bundle 后**自动跑** `tools/skill-report.mjs`（人数取该包的 `n`，每条件 6 局）
并打印尾部摘要 —— "以后每次冠军有大变化的时候都可以做一下"从此不用记着做（`--no-skill-report` 可跳过）。
本次对 `eco-34` 的报告结论：**坑（常用却亏损）无 / 没学会的强招 无**；
唯一负项是它本来就少用的「八卦阵」（拿掉它反而 +5.6pt）⇒ 与"少用是对的"一致。

### 4. 顺带（指纹守门按流程走）
规则指纹 `d0642cfa → a691e020`（改了 rules/state/resolve）⇒ `D16` 立刻红，已按流程用 promote-champion 重测：
`eco-34` 多人考卷仍 **42.9%（40 局）**、自对局 13.6 伤害/局、0% 平局 ⇒ 线上模式零损失。
`tools/battle-test.mjs` 新增 3 条断言（终局收缩入口存在 / 可改 / 会持久化）。

回归：spec 37/37、np-test 68/68、smoke SMOKE OK、battle-test BATTLE OK。

## v1.5.10 — 终局收缩升级为**全局规则**（100 回合后每回合全员 −1 血）+ 页面新增「用内置冠军」按钮

> 两条都是用户裁定。规则改动会让**规则指纹变化**（⇒ 冠军成绩自动判过期），本轮已按流程重测重记。

### 1. 终局收缩 = 全局规则（不再只是 long 模式的专属字段）
用户原话：「可以把 100 回合之后扣血的机制直接做到**正常对战规则**里面，反正一般也打不了那么久。」
- `rules.js` 新增并导出 `SUDDEN_DEATH = 100`；`long` 模式不再自带该字段；
- `resolve.js` 判定改为 `mode.suddenDeath != null ? mode.suddenDeath : R.SUDDEN_DEATH`
  （模式仍可覆盖，写 0 = 该模式关掉）；
- 机制本身早就有（N25 终局收缩）：第 100 回合起每回合末对**每个存活玩家**施加 1 点伤害，
  `bypassGuards`（防御/反弹/原型/金刚盾/全息都不挡 —— 这是"场地收缩"不是攻击）、`noMine`、`source: null`（不计战功）；
- 效果范围：`standard`/`multi` 的上限都是 60 ⇒ **实际只在 long（上限 140）触发**，
  但**作为规则它现在是统一的**（将来任何模式把上限调过 100 都会自动接上）。
- 守门 **D19**：全局阈值=100、第 99 回合不触发、第 100 回合全员 −1、有防御也照掉、事件可追溯（不计战功）、模式可覆盖关掉。
- `D3` 随之改为断言"该模式**实际生效**的阈值"（原来断言模式自带字段，而字段已移除）。

### 2. 页面新增「用内置冠军」按钮（用户要求）
动机（REVIEW §11.1 实测）：`loadMultiChamp()` **优先读 `localStorage['epirus.champion3p']`**
⇒ 换内置冠军对"曾自训/导入过"的用户**完全无效**（我的浏览器对战测试就因此一直在打旧冠军）。
- `index.html`：对局控制区新增 `#btn-champ-reset`（"用内置冠军"，带 tooltip 说明）；
- `ui.js`：`useBuiltinChampion()` = 清 localStorage + 重置缓存 + 重开一局（多人·冠军难度下）+ 提示；
  `aiInfo()` 现在区分 **「3P 冠军（本机自训/导入）」** 与 **「3P 冠军（内置）」**，日志里能直接看出用的是哪个；
  ⚠️ 提示必须放在 `newGame()` **之后**（第一版写反了，被 battle-test 的断言抓到）。
- `tools/battle-test.mjs` 新增端到端验证：塞一个本机冠军 → 重载 → 断言日志显示"本机自训/导入" →
  点按钮 → 断言 localStorage 清空 + 提示含"内置" → 重开 → 断言显示"内置"。

### 3. 冠军成绩按流程重测重记
规则指纹 `3659eda2` → **`d0642cfa`**（`np-test D16` 立刻红 —— 正是它该做的）。
用 `tools/promote-champion.mjs` 重跑：`eco-34` 的多人 3 血考卷仍是 **42.9%（40 局）**、
自对局 13.6 伤害/局、0% 平局 ⇒ 新规则在 60 回合上限下不触发，**线上模式零损失**；指纹与成绩已重记回 bundle。

回归：spec 37/37、np-test **68/68**（新增 D19）、smoke SMOKE OK、battle-test BATTLE OK。

## v1.5.9 — 研究结论入库（哨声惩罚实测 + 防御型池子在 multi 下不划算）+ 对战测试覆盖长程模式

> 用户外出期间的自主研究。**上线冠军不变（`eco-34`）** —— 本轮所有新臂都没能同时超过它的考卷与打架活跃度。

### 1. 哨声惩罚（反摆烂的根因修复）实测：有效，但线上冠军维持 `eco-34`
对照设计：`eco2-*`（修好的熵、**无**哨声惩罚）vs `fgtA-*`（修好的熵 + `EPIRUS_FIGHT_WHISTLE=0.5`），其余全同。

| 冠军 | 考卷 | 伤害/局 | **cost≥3 重击/局** | 平局率 | 回合 |
|---|---|---|---|---|---|
| `eco2-33`（无哨声） | 31.4% | 12.7 | **0.0** | 0% | **55.2** |
| `fgtA-32` | 37.0% | 15.7 | **9.2** | 0% | **13.7** |
| `fgtA-36` | 25.0% | 15.0 | 7.8 | 0% | 15.3 |
| `fgtA-33` | 34.7% | 13.6 | 4.9 | 0% | 39.4 |
| `fgtA-34` | 37.0% | 11.0 | 0.1 | **20%** | 35.6 |
| **`eco-34`（线上）** | **45.6%** | 13.6 | 0.0 | 0% | 28.7 |

⇒ 哨声惩罚**确实把"熬"改成了"打"**（回合 55.2 → 13.7~39.4，重击 **0.0 → 4.9~9.2**，即资源真的转成了伤害），
代价是考卷分中位下降。**线上维持 `eco-34`**（考卷最高且自对局已能打起来）。
想在别处用它：设 `EPIRUS_FIGHT_WHISTLE=0.5`（server+worker 读同一 env）。

### 2. 防御型池子在 **multi 3 血**口径下不划算（修正 §10.2 的适用范围）
`fgtB-*`（哨声 + 15 对手 = 12 + `reflectspam/guardspam/protowall`）的体检：
`fgtB-31` 出现 **30% 零伤害率 / 30% 平局率**、`fgtB-33` 60 回合长局 ⇒ 不如同设置的 `fgtA-*`（12 对手）。
⇒ 精确说法：加单一防御 specialist 对**长程反弹墙**有效（§10.2 实测 0% 灾难点消失），
但**换不来更强的 multi 综合冠军** —— 它治的是"没见过的反制"，不是"多面手"。

### 3. `tools/battle-test.mjs` 增加长程模式覆盖
第二段：切到长程（5 血、3 人）+ 难度=冠军，主动打 25 回合，断言"有真实伤害 + 无套盾 spam + 无新增 JS 错误"。
实测（`eco-34`）：多人段 17 次伤害 / 0 套盾 / 0 错误；**长程段 11 次伤害 / 0 套盾 / 0 错误** ✓
（截图 `results/battle-04-long.png` 已人工复核：长程·3 人·难度=冠军·第 15 回合玩家1 HP 1）。

回归：spec 37/37、np-test 67/67、smoke SMOKE OK、battle-test BATTLE OK。

## v1.5.8 — 熵只统计非ジ + 反摆烂哨声惩罚 + 浏览器对战测试 / 冠军体检（上线冠军 long-33 → eco-34）

> 本轮发现集中在两件事：**考卷分会被"熬"骗**、**换包到不了 localStorage**。详见 `docs/REVIEW-5P.md` §11。

### 1. 技能熵：只统计**非ジ**动作（用户裁定）
熵是按**动作分布**算的，而"攒钱/等待"就是反复出ジ ⇒ 攒钱把熵压到极低 ⇒ 熵奖励其实在**惩罚攒钱**
（v1.5.7 实测：熵权重 0→0.06 后，最高 ep 44.8→12.8、ep≥3 决策占比 34.3%→5.8%）。
现改为只统计非ジ动作、归一化分母也排除ジ；`champEntropy`（选冠军那条）同源。
守门 **D17**：只出ジ ⇒ 熵 0；**掺进任意多ジ都不得改变熵**；分母排除ジ后"两种可负担非ジ均匀 ⇒ 归一化 = 1"。

### 2. 反摆烂：哨声惩罚 + 出手权重可调
动机（**本轮最要紧的发现**）：多人局胜利条件允许"熬到回合上限比血量"，而考卷**看不出**"熬"与"打"。
实测 `long-33` 在 5 座全是自己的自对局里 **20/20 局零伤害、60 回合平局**，考卷却还有 45.3%；
而熵修复反而让它**更摆烂**（`eco2` 56 回合 vs `eco` 25 回合）⇒ **熵只是二阶因素**。
实现：`rankCredit(rank, aliveEnd)` —— 终局还有 ≥2 人活着（没人被淘汰）⇒ 胜利按 `1-WHISTLE_PEN` 打折；
出手权重 `DEAL_W` 从写死的 0.01 改为可调。env：`EPIRUS_FIGHT_WHISTLE` / `EPIRUS_FIGHT_DEAL`（server+worker 两端）。
守门 **D18**。

### 3. 新工具：浏览器对战测试 `tools/battle-test.mjs`
真 Chrome + CDP：多人 5 人 + 难度「冠军」⇒ AI 座跑线上冠军；P0 摆烂出局后页面 `autoRunRest()` 自动打完；
全程收 JS 错误 + 截图 + 断言"打架活跃度"（伤害次数 / 全息屏障 ≤12 次 / 跑到终局）。
**第一次跑就抓到三件事**（§11）：① 页面测试其实打的是 `localStorage['epirus.champion3p']` 里的旧冠军；
② 旧冠军每回合**互套盾**打成 0 伤害僵局；③ 对局可以一路摆烂到底而"看不出来"。

### 4. 新工具：冠军体检 `tools/champ-audit.mjs`
四类指标：A 考卷 1st；B 打架活跃度（伤害/局、cost≥3 重击/局）；C 病理（盾/局、零伤害率、平局率、回合）；
D 长程口径 4 座纯反弹墙。**新规矩：换冠军必须过这张表** —— 单一 1st 率可以被"熬"骗。

### 5. 上线冠军：`long-33` → **`eco-34`**（v1.5.7 的换法有缺陷：它换上了"熬"型冠军）
| | long-33（v1.5.7 上线） | **eco-34（v1.5.8 上线）** |
|---|---|---|
| 多人 3 血考卷（40 局） | 41.0% | **42.9%**（top2 **66.4%** vs 49.7%）|
| 长程口径（40 局） | **57.0%** | 39.6% |
| 自对局伤害/局 | **0.0** | **13.6** |
| 自对局平局率 | **100%** | 0% |
| 全息屏障/局 | 0.0 | 0.0（v1.5.4 之后训练 ⇒ 不会误用这张卡）|

⇒ long-33 的分是"熬"出来的，换**真正会打**的 `eco-34`（multi 3 血训练产物、模式也对得上）。
⚠️ long-33 在长程口径分更高（57.0%），但那同样是摆烂（0 伤害；long 模式上限更长）—— 交互质量优先。

### 6. 顺带
- **训练锁** `docs/artifacts/.training.lock`：自跑器每个 seed 前会把 bundle 临时覆盖成热启动基线（v1.3.58），
  于是"训练期间读 bundle 的工具"必然误报（本轮 np-test D16 与体检表的 bundle 行各踩一次）
  ⇒ 现在：跑训练写锁、结束清锁；D16 见锁则跳过并说明；体检表给 bundle 行打"训练中"标记。
- 覆盖项（`EPIRUS_ECO_*` / `EPIRUS_FIGHT_*`）都记进产物 meta，便于复现。

回归：spec 37/37、np-test **68/68**（新增 D17/D18）、smoke SMOKE OK、battle-test BATTLE OK。

## v1.5.7 — 线上冠军换成 `long-33`（修 v1.5.4 造成的成绩崩塌）+ 全息屏障在 2P 灰掉 + 规则指纹守门

> ⚠️ 本轮三个决定都由用户裁定（我给了选项与数据）。前两个都是**v1.5.4 那张卡**引发的连锁。

### 1. 线上多人冠军：换成 `long-33`，替换被语义修正打坏的旧产物
千问体检 §3 报的、我也**独立复现**的：v1.5.4 只改了 `全息屏障` 的 `target`，权重一个字节没动，
线上冠军在同一考卷上从 **38.0% 掉到 15~17.8%**（`eval-5p --ban=holo` 立刻回到 38.6% ⇒ 缺口全来自这一张卡）。
现换成库内最强的 `long-33`（5 血训练产物）：
- canonical 多人 3 血考卷（`eval-5p 40 5 77000`）：**17.8% → 41.0%**（同场 `pickRandom` 10.6%）
- 同场 5 血口径（`--mode=long`）：**59.9%**
- 权重与 `docs/artifacts/long-33.bak` **逐字节相同**；只给 meta 补了 `shippedAs` /
  `examScoreAtBuild: 0.410` / `rulesFingerprint` 三个字段
- ⚠️ 口径提示：它是 5 血训练产物（meta `mode:"long"`），但在 3 血考卷上仍远强于旧产物 ⇒ 向上兼容

### 2. 全息屏障归入「多人专用」⇒ **2P 模式不再提供它**（用户裁定）
v1.5.4 之后它在 2P 里唯一的用法就是"给对手套盾"= 纯陷阱卡（AI 也从不选：400 局 0 次）；
而 2P 的自保**本来就有原型制御**、效果完全相同（用户原话："自保有原型制御可以实现完全相同的效果"）
⇒ 灰掉它零损失。实现：`MULTI_ONLY` 加 `SK.HOLO`（与 双枪射手 / 镜面反射 同一机制），多人模式不受影响。
守门：`np-test D14` ④b —— 2P 技能表不得含 HOLO、必须含 PROTO；多人模式必须含 HOLO。

### 3. 规则指纹守门（千问体检 §5-2 建议）
新增 `tools/rules-fingerprint.mjs`（`rules/resolve/state/play` 四个文件的 sha1 前 8 位，行尾归一化）；
bundle 的 meta 记 `rulesFingerprint` + `examScoreAtBuild`；**`np-test D16`**：
**bundle 记的指纹 ≠ 当前规则指纹 ⇒ 立刻红** ⇒ 逼你重测线上冠军再把新成绩/新指纹记回去。
这条把"产物与引擎错配"变成机械可查的一整类问题（v1.3.24 的包版本、v1.5.4 的语义版本都属于这一类）。
它**故意敏感**（连注释改动都会触发）：重记成本 = 一次考卷 + 一行 meta，漏掉的成本 = 交付质量静默下降。

### 4. 顺带：2P 体检结论入库
`RULES-2P.md` §全息屏障 记入"2P 不提供"；HANDOFF §4-8（2P 决策）与 §4-9（冠军被语义修正打坏 +
两条规矩）都更新为已解决。2P 模糊测试（4200 局）与点名复核结果见 v1.5.6 条目。

### 5. 两个实验（用户要的"验证奖励改动" + 千问体检 §5-3 的池子建议）—— 详见 `docs/REVIEW-5P.md` §10
> ⚠️ 先说一个**方法论发现**：v1.5.4 改全息屏障语义后，**整族旧产物都被打坏** —— 旧对照臂
> `ms2-p12`（12 脚本池 6 seed）标准考卷均值从 ~**37.5%** 掉到 **21.20%**。拿它当对照去测任何东西
> 都会被语义修正污染（第一版 `eco vs ms2-p12` 表面 +12.30pt 全是假信号）。
> ⇒ 改用"同一引擎 + `EPIRUS_ECO_*` 还原旧门槛"当对照（`rwd0`）。

- **奖励 A/B**（`rwd0`＝旧门槛 4/10/熵0 vs `eco`＝新门槛 5/10+熵0.06，其余全同）：
  标准考卷 29.93% → 33.50%（**+3.57pt, t=0.91 不显著**，实力无损）；
  但**行为被大幅改变**：最高 ep **44.8 → 12.8**、ep≥3 决策占比 **34.3% → 5.8%**、
  **出手种类 13.7 → 14.8**（熵奖励确实起效）。
  ⇒ **结论①：熵奖励与 ep 奖励互相打架** —— 熵按**动作分布**算，而"攒钱"就是反复出ジ ⇒ 熵极低
  ⇒ 熵奖励在**惩罚攒钱**，会把深经济（5 血最强的那条线）压掉。修法：`divW` 再降一个量级，
  或者**熵只统计非ジ动作**。
- **池子 A/B**（`eco` 12 对手 vs `def` 15 对手＝12 + reflectspam/guardspam/protowall，只差池子）：
  标准考卷 33.50% → 34.58%（+1.08，**没掉**）；训练自评 **+7.30（t=4.99，6正/0负）**；
  **长程反弹墙 64.17% → 93.33%**（`--mode=long --field=reflectwall`），
  逐 seed：`100/0/100/85/0/100` → **`100/100/100/100/60/100`**，**灾难点（0%）消失**。
  ⇒ **结论②：千问"缺的是见过、不是选不出"被支持**，并**修正**我 v1.5.2 的"池子是弱杠杆"：
  那两次加的是**复杂策略对手**，这次加的是**"见过就能反制"的单一 specialist**
  ⇒ 精确说法是「**加复杂策略对手是弱杠杆，加单一防御 specialist 不是**」。

回归：spec 37/37、np-test **65/65**（新增 D16）、smoke SMOKE OK。

## v1.5.6 — 经济 shaping 门槛按 (人数,模式) 参数化（用户锚点）+ 恢复技能熵奖励；2P 体检

### 1. ep 奖罚门槛：从写死的 4/10 改成按 (人数, 模式) 推导
用户回忆的"ep 奖励/惩罚"就是 `scoreMemberN` 里的 `stock` 项 —— 但它此前**门槛写死 4/10**、与人数和模式无关
⇒ 5 血里 11 ep 就被当囤积。用户给的锚点：**3 人局 → 奖励到 3 ep、惩罚 >10 ep**；
**5 人 · 5 血 → 奖励到 5 ep、惩罚 >20 ep**。现按：
- 奖励点 **T** = 3（≤3 人）/ 4（4 人）/ 5（5 人）；惩罚点 **C** = 10（3 血）/ 20（长程 5 血）
- 形状不变：0→T 线性升到满额 ⇒ T..C 不奖不罚 ⇒ 超过 C 按超出比例罚（2C 满额）

**幅度没动**（`STOCK_BONUS` 0.05 / `HOARD_PEN` 0.12），符合"奖惩不用给太多"。
提成纯函数 `economyStock(mEp, n, mode)` + `economyTargets(n, mode)`，`setEconomyReward()` 可覆盖。

### 2. 恢复技能熵奖励（Q3 曾把它移出目标函数）
`divBonus = DIV_W × divNorm`，**DIV_W = 0.06**（满额 +0.06，与 stock 的 +0.05/−0.12 同量级 ⇒ 仍远小于
胜负项 1.0/0.3）。Q3 的理由仍成立（熵本身不等于"见过那个状态"），故只当"别把自己塔成一招"的弱先验。

### 3. 守门
`np-test` 新增 **D15**：直接点纯函数语义 —— 3 人局 3/10、5 人 5 血 5/20、4 人落在中间、到 T 有奖励、
T..C 不罚、超过 C 开始罚、**同一个 15 ep 在长程 5 人必须比 3 人 3 血宽松**；熵奖励权重小而正；
`fit = 池子分 + 熵奖励 + 风格切片` 的组成自洽。D13 随之更新口径（"多出来的部分"现在要含 `divBonus`）。

### 4. 2P（冻结模式）体检：**没找到引擎 bug**
- 模糊测试 **4200 局**（3000 随机 + 1200 攻击倾向）：无异常、HP/ep 不越界、全部终止、winner 合法、无 NaN；
  2P 技能表正确排除多人专用卡（双枪射手 / 镜面反射）。
- 点名复核（与 `RULES-2P.md` 对账）：激光眼**单发即无效化原型制御**（N23，走无效化分支、不造成伤害）✓、
  过载炮**穿防御**✓、蓄能珠**未使用则次回合清空**（R9'）✓、地雷 2P 语义与冻结版等价（打人者吃 1 火伤）✓、
  双方互埋雷且都不出手 ⇒ 不触发 ✓。
- **唯一实质变化：全息屏障在 2P 变成"给对手套盾"**（v1.5.4 的语义修正；冻结版 v1.0.0 是自保）
  ⇒ 2P 里它成了陷阱卡，**需要裁定**（见 `docs/HANDOFF.md` §4-8）。
- 好消息：**2P 线上冠军完全不使用全息屏障**（400 局自对局 0 次；它只用藤甲当架势）
  ⇒ 这次语义修正**没有**打坏 2P 产物（与 5P 线上冠军的遭遇相反，见 HANDOFF §4-9）。

### 5. 顺带：导出 `policyChooser`（2P 版 chooser 工厂）
此前只导出了 N 人版 `policyChooserN`，"按页面口径量 2P 冠军的使用率"没有正规入口。

回归：spec 37/37、np-test 64/64（新增 D15）、smoke SMOKE OK。

## v1.5.5 — 全息屏障第二轮修正：**被小雷无效化的时机**（漏读 `voided`）

### 1. 问题（用户指出："全息屏障的效果时机应该高于小雷，这样他才能在随后被小雷无效化"）
v1.5.4 把全息屏障改成"给别人套的盾"时，我把盾做成了**从行动推导**（`holoShieldFrom` 扫 `state.actions`）。
但 `actionOf()` —— 引擎里"取本回合有效行动"的唯一入口 —— 本来就会**跳过被无效化的行动**
（`return a && !a.voided ? a : null`），而我绕过了它 ⇒ **小雷打了施放者，盾照样出现**。
实测（修前，4 人局 P0 给 P1 套盾 / 小雷打 P0 / P2 枪打 P1）：P1 血量 **3**（应 2）—— 屏障没被拆掉。

### 2. 规则依据（原始规则集「优先级原则」）
> 技能按优先级依次结算，如果优先级相同则同时结算；**未特别标注的优先级均为 3**；
> 优先级相同的攻击可以相互抵消，**优先级高的攻击可以阻止优先级低的攻击**

`雷击之枪 = 5`、`全息屏障 = 3（未标注）` ⇒ **小雷先结算** ⇒ 打**施放者**即可在屏障生效前把这次施法无效化
（即原文「雷击之枪需要对**使用者**作用才能使其无效」）；打**被套盾者**则拆不掉盾。

### 3. 改法
`holoShieldFrom` 改走 `actionOf(state, i)`（不再直读 `state.actions[i]`）。
附带好处：这条判定**与结算顺序无关** —— 以后万一调 `pri` 也不会再漏"被无效化"。

### 4. 守门用例（`np-test D14` 扩到 6 个情形，63/63）
①盾挡住打向被套盾者的枪 ②施放者不被自己的盾护住 ③原型制御仍自保（回归）④**套不到自己**（目标不含自己）
⑤**小雷打施放者 ⇒ 屏障被无效化**（且不留 `holoSet` 事件）⑥**小雷打被套盾者 ⇒ 盾仍在**（只是他自己的技能被无效化）。
**反证三次全红**：`target` 改回 `self` ／ 删掉 `holoShieldFrom` ／ 把 `actionOf` 换回直读 `state.actions`。

回归：spec 37/37、np-test 63/63、smoke SMOKE OK。

## v1.5.4 — 规则修正：**全息屏障是"给别人套的盾"**（此前被做成了原型制御的重复品）

### 1. 问题（用户裁定 + 原始规则集为据）
`js/core/rules.js:61` 里 `全息屏障` 的目标字段是 **`self`**、`js/core/resolve.js` 又把 `PROTO/HOLO`
判成同一个 `kind:'proto'` ⇒ 两张卡**同效果**（都是 1 费自保）。用户指出：**"写两个一模一样的技能干什么"**。
核对**原始规则集**（老仓库 `D:\code\Epirus\README.md` 的「全息屏障」条目）：
- 作用效果：「**给被作用者**施加一个"原型制御"」
- 手势：「双臂伸出挡住**被作用者**胸前」（对照原型制御是「双臂**竖立于胸前**」＝对自己）
- 特殊：「雷击之枪需要对**使用者**作用才能使其无效」
⇒ 三处都指向"**对别人用的盾**"；此前的 ⚠️ R18（仅限自己）作废。

### 2. 改法（改动面最小，且让既有判定**全部自动复用**）
- `rules.js`：`target: 'self'` → **`'other'`**（任一其他玩家），desc / 手势同步回原始措辞。
- `resolve.js`：`guardOf()` 里 `SK.HOLO` **不再**是施放者自己的架势；新增 `holoShieldFrom(state, pid)`
  —— 谁本回合把盾套在我身上，我就获得 `{kind:'proto', viaHolo:true}` 式架势。
  ⇒ 格挡 / ≥3 伤害各自转移给作用者 / 小雷豁免 / 大雷 3 回合禁用（R23'）**全部自动复用**——
  不必去逐个改判定，而那正是这类修正最容易漏一半的地方。
- 事件：屏障不再记 `guardSet`（那会被读成"施放者摆了个架势"，正是修正前的语义），改记独立
  `holoSet{pid,target}`，页面日志同步渲染。
- 守门用例 **np-test D14**（63/63）：盾挡住打向被套盾者的枪 / **施放者不再被自己的盾护住** /
  原型制御仍自保（回归）/ 套不到自己。**两次反证**：`target` 改回 `self` → 红；删掉 `holoShieldFrom` → 红。

### 3. 连带影响（都已写进规则文档）
- **2 人局**：唯一的"别人"就是对手 ⇒ 这张卡在 2P 变成"给对手套盾"（按原始规则本就如此——
  它是一张多人/结盟向的卡）。2P 规则集 v1.0 的冻结不受影响：这是 bug 修正（patch 号）。
- **对现有冠军无实战影响**：技能报告显示 **9 个冠军对全息屏障的使用率全是 0.0%**。
- **旧读数作废**：该卡在"只护自己"语义下测出的 mono-Δ / `--inject` 数值全部作废，要按新语义重测；
  并且按 v1.5.3 的教训，这类卡的读数**必须**配原生经济口径，不能只看富裕经济的 mono-Δ。
- 用户另一条仍有效的裁定：**结盟式打法不必强求**（当前策略没有结盟建模）⇒ "AI 不会用这张卡"是预期行为。

回归：spec 37/37、np-test 63/63（新增 D14）、smoke SMOKE OK。

## v1.5.3 — 复合适应度（把风格表现写进 fit）；以及那条"已被记过一次"的错再犯一次 + 更正技能报告的假信号

### 1. 新能力：风格切片 `setStyleSlice(opps, w, games)`
在**池子预算之外**给每个个体追加 k 局对指定对手的局，把 1st 率按权重并进 `fit`：
`fit = 池子那部分 + w × 风格局 1st 率`。用 `?styleopps=&stylew=&stylegames=` 打开（默认关闭 ⇒ 既有路径逐位不变）。
- 为什么是"追加"：§8 的池子实验之所以有代价，是因为 16 个对手仍只打 8 局 ⇒ 每对手练习量 −25%。
  切片**不动池子**，所以不摊薄（本轮 §9.1 的脚本对照场 +0.8pt 印证了这点）。
- 为什么走**原生规则**（`regen=0`）：量的是真实强度，不会把人推向"只在特殊口径下成立"的策略（§5.2 的教训）。
- **两端都要接线**（§5.2-17 的教训）：server 开设三参数 + 走统一解析器 + **开跑前解析** + 写进产物 meta；
  `paralleltrain` 随消息下发；worker 在**自己沙箱**里再设一遍并**回执 `styleGames`**；server 校验回执、不符即中止。
  守门 **np-test D13**（62/62）：关闭=0 局／打开=恰好 k 局／`fit−fitNoDiv == w×styleRate`／
  **池子那部分 fit 逐位不变**（追加而非替换）／两端都接线。

### 2. 实验结论：**没有收益，代价是通用实力**（详见 `docs/REVIEW-5P.md` §9）
控制臂 `ms2-p12-31..36`（12 脚本、无切片） vs 实验臂 `sty-31..36`（**同池** + 3 个风格冠军 × 2 局/代, w=0.5）：
- **标准考卷**（1400 局/臂, n=6）：37.48% → **31.50%**（**Δ=−5.98pt, t=−2.38, 1正/5负**）；单跑极差 3.8 → 13.2pt。
- **风格考卷**（n=4）：风格场平均 **−1.2pt**（t=−0.59）；**"练过"的三场全部不显著**（+2.5 / +4.2 / 0.0，
  而这三场正是被写进 fit 的）；留出三场互相抵消（`long-31` +10.0 / `long-33` −25.8 / 枪+墙流 −2.5）。
- **机制**：每代只追加 **2 局** ⇒ 风格率的估计**噪声极大**（30 局口径的单跑 sd 都有 2.7–21.5pt）
  ⇒ **等于把噪声当分数做选择**（选中的是"这 2 局运气好"的个体，不是真会打风格的个体）。
  **这条错项目已经记过一次**：v1.3.18 的结论是「**探针是门槛，不是分数**」。本轮我把风格率当分数加进 fit，又犯一次。
- **下一步**：先量清 k 局估计的噪声，然后把 k 抬到与池子同量级、或**改成门槛/band**（照 v1.3.18 的修法）。

### 3. 更正：技能报告那四个"没学会的强招"是**仪器的假信号**（详见 `docs/REVIEW-5P.md` §7.6）
`skill-report` 的 mono-Δ 是**富裕经济**口径（每回合把 ep 补到 10）⇒ 会把"1 费整回合免伤"这类
便宜可重复卡高估成 +30pt。换**原生经济**口径（`eval-5p --inject=`，能用就用）：
原型制御 **−34.5pt**、全息屏障 **−29.3pt**（抢着用输更多 ⇒ **冠军不用它们是对的**）；
真正的落雷 / 摄魂指法 的注入可用率只有 **0.3%** ⇒ 该口径**无测量功效**。
另：用户指出「全息屏障是给别人套原型制御」，但代码里 `js/core/rules.js:61` 的目标字段是 **`self`**、
`js/core/resolve.js:114` 又把 `PROTO/HOLO` 判成同一效果 ⇒ **实现与设计意图可能不一致**，
已记入 HANDOFF §4-7（**需要裁定**）。用户同时裁定：**结盟式打法不必强求**（当前策略没有结盟建模）。

回归：spec 37/37、np-test 62/62（新增 D13）、smoke SMOKE OK。

## v1.5.2 — 训练池能放「风格化冠军」当对手；以及"加名字漏一处"的第三次

### 1. 新能力：`champ:<仓库相对路径>` 冠军对手
训练池原先**只有脚本对手**（`js/train/bots.js` 的 if-else 人格）。但手上那几个 `.bak` 冠军
本身就是四种成型的流派（激光剑流 / 狙击枪+环流 / 坦克流 / 枪+墙流，画像见 `docs/skill-report-cmp.html`）——
要练"对不同风格都有反制能力"，这些才是真靶子。现在 `?opps=` 里可以这样写：

```
?opps=random,defend,champ:docs/artifacts/champion-5p-hA9.bak,champ:docs/artifacts/champion-5p-armB12f.bak
```

- 机制**单一来源**在 `server/opp-champs.mjs`：语法解析、包解包（`P.unpack`）、chooser 构造
  （`policyChooserN(params, 0.15)`，**与页面「困难」档同一条推理路径**）、按文件缓存。
- 为什么用路径当输入、不写死名单：§7 的约定是冠军包不入库（`*.bak` 被 .gitignore 忽略），
  写死会让仓库依赖本机文件；走 `?opps=` 则路径连同 seed/参数一起记进产物 meta，**天然可复现**。
- 对手池顺序**必须按 `?opps=` 原顺序**构造（轮换 `oi=(gen*3+g)%opps.length` 依赖它）。

### 2. 踩坑：加对手名时**又漏了一处**（同型事故第三次）
`server/train-server.mjs` 的**终局名人堂评估**那段自己又写了一遍"名字→函数"映射，
对 `champ:` 名给出 `undefined` ⇒ 训练跑完 250 代、进到名人堂评估才炸成 `sel is not a function`
（前几代的轮换只覆盖脚本下标，所以看不见这个洞）。
前两次分别是 v1.3.59（只补 worker）、v1.4.8（只补 server）、v1.4.14（漏 opp-pool）。
**这次不打补丁，改成结构性唯一入口**：`makeOppSelResolver()`（`server/opp-champs.mjs`）——
server 与 worker 都只经它把"名字"变成"chooser"，且**开跑前逐个名字真解一次**
（解析失败前移成中止，不再留到中途才炸）。
**np-test D12** 现在盯着这个形状：`train-server.mjs` 里**不得再有裸的 `B[BOT_FN_N[...]` 映射**，
两端都必须用 `makeOppSelResolver`；功能上还要求能从在库冠军包解出 params、对非冠军包**响亮抛错**。
反证：把解析器换回裸映射，D12 立刻红。

### 3. 新工具：`tools/style-exam.mjs` + `tools/style-exam-cmp.mjs`（风格考卷）
量"能不能打**成体系**的风格"：每个风格各开一场（其余 n−1 个座位全是该风格冠军）+ 混合风格场 +
脚本对照场；受试者可以是冠军文件**或脚本名**（用来量地板）；默认模式跟随受试冠军的 `meta.mode`。
配对汇总按场分组给 均值/Δ/sd/t/符号（口径与 `ab-analyze.mjs` 对齐）。
配套修了 worker 侧错误**穿透**：`paralleltrain.runOne` 现在会把 worker 的 `error` reject 出去，
而不是永远挂着等 30 分钟墙上时钟（本轮就是靠它把上面那个 `sel is not a function` 立刻暴露出来）。

### 4. 实验：拿四个风格化冠军当训练靶子（同池/同种子/同起点/同 worker 数，唯一差别是多这 4 个对手）
控制臂 = `ms2-p12-31..36`（12 个脚本对手）；实验臂 = `mix-31..36`（12 脚本 + 4 风格冠军 = 16 对手）。
- **判定① 标准考卷（1400 局/臂, n=6）**：均值 37.48% → 33.08%（**Δ=−4.40pt, t=−1.42, 不显著**），
  **但单跑极差 3.8 → 22.4pt（6×）** —— 4 个 seed 几乎不动、2 个崩了（−18.3 / −8.0）。
- **判定② 风格考卷（每场 30 局, n=4）**：风格场平均 **26.7% → 22.1%（Δ=−4.5pt, t=−4.21, 4/4 seed 为负）**；
  **留出场**（环经济冠军 long-31/33，没进过池）也没涨（−2.5 / −23.3pt）。
  唯一名义正值是混合场 +10.8pt（t=1.49）—— 8 个场里 1 个，多重比较下不可信。
- ⇒ **假设不成立**：加风格冠军当陪练**没有**换来"对多风格的反制力"，代价是**方差放大 6 倍**。
  ⚠ 本轮分不开"被强敌带坏"与"练脚本的量变少"（16 对手仍只打 8 局 ⇒ 每对手练习量 −25%）——
  要分离就得把每对手预算拉齐（`gpo≈11-12`）并同时重训控制臂。
- ⇒ 与 §6.4（ring2）合成一条方法论：**改对手池是弱杠杆**（两次干预：+1 脚本对手、+4 真实风格冠军），
  该改的是训练信号。**`mix-*` 不进线上**，线上冠军仍是 v1.3.58。详见 `docs/REVIEW-5P.md` §8。

## v1.5.1 — 技能报告补上「模式」口径；它顺手揭开了挂很久的 P0-d 悬案

### 1. `tools/skill-report.mjs` 现在是**模式感知**的
原先它只按 `multi`（3 血）建局 ⇒ 对 5 血冠军给出的其实是**3 血读数**，而报告里一个字都没提口径。
现在：`--mode=<key>` 可显式指定，**缺省跟随冠军产物自己的 `meta.mode`**（长程冠军自动按 5 血测），
报告头与 JSON 都写出模式。
**行为保持已验证**：用新代码重跑现役冠军 v1.3.58，`baseNative/baseRich` 与旧 JSON **完全相同**
（0.28968 / 0.50132），逐技能 use/Δ 序列**逐位一致**。
耗时：≈64 秒/冠军（3 血口径）、≈150 秒（5 血口径）。

### 2. 八版对比快照
`docs/skill-report-cmp.html` 现在含 8 个版本（v1.3.58 / hA9 / hB12 / armA9 / armB12f /
ms2-p12-31@5血 / long-31@5血 / long-33@5血），并新增三份报告
（`docs/artifacts/sr-{ms2-p12-31-long,long-31,long-33}.{json,html}`）。

### 3. 发现（详见 `docs/REVIEW-5P.md` §7.5）
- **5 血训练自己把「聚能环」学出来了**：long-33 用量 **38.2%**，消融 Δ_lost=**+19.3pt**（承重，
  拿掉环 1st 从 57.9% → 38.6%）。
- **无选择偏差**的 12 臂数据（5 血考卷）：5 血训练 **3/6** 用环、3 血训练 **1/6**；
  **两个重度用环的长程冠军正好是 5 血考卷最强的两个**（57.7% / 38.5%），另 4 个平均 32.7% ⇒ **差 +15.4pt**。
- ⇒ **P0-d 悬案结案**：「聚能环复利线没被教会」**不是**补贴不够、也不是池里缺环，
  而是**赛程太短、环回不了本**（3 血下没等复利生效就被打死）。换成 5 血它自己就长出来了。
- 另：每个冠军都是**单一流派**（2-3 招占全部出手）但流派完全不同（激光剑/狙击枪+环/坦克/枪+墙）；
  `原型制御 / 全息屏障 / 真正的落雷 / 摄魂指法` 在几乎每个冠军上都是"零使用但正 Δ"。
  > ⚠ **v1.5.2 更正**：这四招"零使用但正 Δ"是**富裕经济口径的假信号**，不是 AI 的盲区。
  > 换**原生经济**口径（`eval-5p --inject=`，能用就用）后：原型制御 **−34.5pt**、全息屏障 **−29.3pt**
  > （抢着用会输得更惨 ⇒ 冠军不用它们是对的）；而 真正的落雷 / 摄魂指法 的**可用率只有 0.3%**
  > ⇒ 该口径**无测量功效**，不能说"没用"。详见 `docs/REVIEW-5P.md` §7.6。

回归：spec 37/37、np-test 60/60、smoke SMOKE OK。

## v1.5.0 — 长程（5 血）冠军终于能训练了（接线 + 两道守门 + 一个新坑）

### 1. 症状：`?mode=long` 早就支持，但**训练路径没有一处用它**
`oneGameN(choosers, seed, n, opts)` 从 v1.4.0 起就支持 `opts.mode`（默认 `'multi'` = 3 血），
但全仓 **19 个调用点**里唯一传过 `opts` 的是评测工具 `tools/eval-5p.mjs:305`（所以 `--mode=long`
在评测里一直好使）。训练侧那 5 处（`scoreMemberN` / `evalN` / `champEntropy` / `evalEconProbe` /
`evalSubsidyProbe`）只传 `{regen: …}` ⇒ **历次训练（hA9 / hB12 / wall2 / ms2 / ring2…）全是 3 血**，
产物 meta 里连模式字段都没有（因为那个值永远等于默认值）。
（HANDOFF §4 原写"30 个调用点"是**笔误**，实测 19 个，已更正。）

### 2. 接线（沿用 `setWrTol` 的"调用方显式传入"模式）
- `js/train/evo.js`：新增模块级 `TRAIN_MODE` + `setTrainMode(m)` / `trainMode()`，上述 5 处建局
  全部带上 `mode`；默认 `'multi'` ⇒ 既有路径**逐位不变**。
- `server/train-server.mjs`：`?mode=` 可覆盖（未知模式**开跑前中止并进 SSE**），
  并把模式写进产物 meta（以后档案自带口径）。
- `server/paralleltrain.mjs` + `server/train-worker.mjs`：模式**必须随消息下发到 worker** —— 见下条。

### 3. 踩坑：`setTrainMode` 只改**本线程**，而打分跑在 worker 线程里
第一版我只在服务端调了 `setTrainMode`，结果 SSE 的 `best` 曲线与 multi 轮**逐位相同**
（gen 0/50/100/150/200 全一致，连 gen-50 的 0.6959666666666667 都一样）。根因：worker 各自是
独立沙箱，`TRAIN_MODE` 仍是 `'multi'` ⇒ **进化照旧 3 血，只有服务端那次终局评估用了 5 血**。
这种"半程生效"最隐蔽（日志看起来完全正常，只有对比 best 曲线才露馅）。
修法三层：① `paralleltrain` 把 `mode` 放进 `evalN` 消息；② worker 收到后 `setTrainMode(msg.mode)`，
并在每条结果里回执 `modeUsed`；③ 服务端校验回执，不符**立即中止并进 SSE**（不放口径混杂的产物出去）。
**反证**：同 seed 同预算，long 与 multi 的 gen-0 best 分别是 **0.5153 / 0.4986**、gen-50 是
**0.6015 / 0.6960** —— 从第 0 代起就分叉。

### 4. 守门用例（都会失败）
- **np-test D10**：`setTrainMode('long')` 后 `scoreMemberN` / `evalN` / `champEntropy` 建出来的局
  必须 `state.mode.hp === 5`（默认必须是 3）。反证：抹掉任意一处 mode 传参 ⇒ 立刻红
  （报 `实测 hp=3,3`）。
- **np-test D11**：模式必须随 `evalN` 消息下发到 worker、worker 必须回执、server 必须校验。
  （D10 抓不到这条 —— D10 测的是进程内的 `scoreMemberN`，正是本轮漏掉的那半边。）

### 5. 顺带发现：`workers` 是**会改变结果的行为输入**（差点让实验白跑）
同一协议、同 seed/池/起点，`EPIRUS_WORKERS=12` 与默认 16 训出的 seed 31 **权重不同**
（sha1 `A4A1A894…` vs `92AF513B…`，训练自评 0.3416 vs 0.3863）；而默认 16 下重跑与既有
`docs/artifacts/ms2-p12-31.bak` **逐位相同**（sha1 `92AF513B…`）。
⇒ §3.7 的"训练逐位可复现"要限定为**同 worker 数**；跑 A/B 必须两臂同 worker 数 ——
"少用几个 worker 省机器"等于**悄悄换了一个冠军**。已记入 HANDOFF §5.2-16。

### 6. 实验：长程冠军 vs 3 血冠军（同池/同 seed/同起点/同 worker 数，唯一差别是模式）
n=6 配对（`docs/artifacts/ring2-run-long.log`，`ab-analyze p12 r17`）：
- **3 血标准考卷 1st：37.48% → 34.15%，Δ=−3.33pt**（sd 4.42, t=−1.85，**1正/5负**）——即**代价**；
- **5 血标准考卷 1st：30.72% → 37.82%，Δ=+7.10pt**（sd 13.98, t=1.24，4正/2负）——即**目标收益**，
  但 n=6 下**不显著**；单跑极差 29.8pt。
- **技能使用分布**：出手种类**没变多**（16.7 → 15.3），但"用钱的深度"大幅上升 ——
  ep≥3 决策占比 **19.5% → 47.8%**、cost≥3 出手 **1.9% → 8.1%**、10+ 富余带 **5.8% → 22.2%**
  （前两项逐 seed **5正/1负**）。且这套深经济风格**延续到 3 血**（3 血考卷上 ep≥3 16.2% → 44.6%）
  ⇒ 这是 3 血掉分的机制。
⇒ **结论**：不要把线上默认冠军换成长程冠军（风险/收益不对称）；但"长程模式挂长程冠军"站得住。
**别按 5 血考卷挑 6 个里最好的那个**（挑噪声上尾）。详见 `docs/REVIEW-5P.md` §7。



### 1. 症状
`tools/smoke.mjs`（CDP 驱动真实 Chrome 跑真实 UI）报 **14 PASS / 4 FAIL**，四个 FAIL 全在同一条线上：
`#sel-mode` 里没有 `option[value="long"]` ⇒ **玩家无法从 UI 选到长程（5 血）模式**。
引擎侧一切正常（`np-test` D2/D3 通过、`tools/eval-5p.mjs --mode=long` 可用）⇒ 缺的只有页面入口。

### 2. 根因：写了消费者，没写生产者
v1.4.0 的交付清单（HANDOFF §2.1）写着「`js/ui/ui.js` + `index.html`：模式下拉加「长程（3-5人·5血）」」，
但**只有 `ui.js` 落地了**：
- `js/ui/ui.js:14` `MULTI_MODES = ['multi', 'long']`、`:11` `MODE_NM.long = '长程(5血)'` —— 都按 long 存在来写；
- `js/ui/ui.js:986-997` `syncModeOptions()` 只**切换已有 option 的 `disabled`**，从不创建 option；
- `git log -S 'value="long"' -- index.html` **为空** ⇒ 这个 option 从未进过 `index.html`。
于是 `syncModeOptions()` 里的 `if (multi && MULTI_MODES.indexOf(B.modeKey) < 0) B.modeKey = 'multi'`
永远把多人局按回 3 血 —— 这条兜底逻辑反而把缺失掩盖得体面。

### 3. 修法（一行 + 版本号三方同步）
- `index.html:30` 补 `<option value="long">长程（3-5人·5血）</option>`（紧挨 `multi`）。
- CHANGELOG / README / index.html 版本号同步到 **v1.4.15**（np-test **D8** 守护三方一致）。

### 4. 验证
- `node tools/smoke.mjs` ⇒ **SMOKE OK（18 PASS / 0 FAIL，JS errors = none）**，
  三张截图逐张目视复核：对局页 / 训练页 / **长程 5 血页（日志出现「五 血」+ HP 5）**。
- 回归：`spec 37/37`、`np-test 58/58`。

### 5. 教训：**没有门禁会自己变绿**
这个缺陷在仓库里躺了 **5 个版本**（v1.4.0 → v1.4.14）。期间「长程模式」被写进 HANDOFF §2.1 的交付清单、
被当作 §3.5（终局收缩）/§3.6（摄魂门槛）结论的前提，`smoke.mjs` 也从 v1.4.0 起一直红着 ——
**但没有任何测试或流程去跑它**，于是没人知道。已记入 HANDOFF §5.2-15。
（同类前科：v1.4.14 的「加名字漏一处」，那是"两处各写一遍"，这次是"两处各写一半"。）

## v1.4.14 — 第三次『加名字漏一处』：对手池补 `ringspam` + 未知名字**开跑前中止**

### 1. 症状
环实验（v1.4.11 设计的『把环经济流放进训练池』）跑到中途 SSE 报
`{"type":"error","msg":"sel is not a function"}`。

### 2. 根因
`ringspam` 只加进了 `tools/eval-5p.mjs` 的池与 `js/train/bots.js` 的导出，
**没加进 `server/opp-pool.mjs`**（v1.4.9 刚抽出来的单一来源）
⇒ 服务端 `BOT_FN_N['ringspam']` 未定义 ⇒ `B[undefined]` = undefined ⇒ 调用 `sel(...)` 时炸。
**这是『加名字漏一处』第三次**：v1.3.59 漏 server、v1.4.8 漏 worker、v1.4.14 漏 opp-pool 本身。

### 3. 修法（都朝『让失败变响』）
- `opp-pool.mjs` 补 `{ name: 'ringspam', fn: 'pickRingSpam' }`（16 → 17 个名字）。
- `train-server.mjs`：未知对手名**立刻中止**并把错误送进 SSE。原先只 `console.log`
  ⇒ 落在隐藏 server 进程的 stderr 里，用 curl 抓 SSE 完全看不到 —— 这正是 v1.4.8 静默降级成
  『12 对手』而我没察觉的原因。
- `train-worker.mjs`：启动时校验 `OPP_SPECS` 里每个 `fn` 都存在，typo 立刻炸在启动处。
- 新增 **np-test D9**：`opp-pool.mjs` 每个条目都必须能在 `EpirusBots` 上找到，
  且 `reflectspam` / `ringspam` 必须在池里（第三次踩的正是『名字没进池』）。

### 4. 顺带的 harness 改进
我自己的 runner 轮询原先只等 `"type":"done"` ⇒ 遇到 error 会空转到 15 分钟超时。
新模板同时检测 `"type":"error"` 并打印出来（这次就是靠 SSE 里的 error 才定位到根因）。

回归：spec 37/37、np-test 58/58。

## v1.4.13 — 三件卫生事项（用户批准）

### 1. 删死代码 `startTraining`（`js/train/trainer.js`：172 → 141 行）
零调用方（v1.3.56 查实过，本轮再查全仓仍只有"定义 + 导出"两处）。
它曾**误导过一次归因**：把"训练在跑"算到浏览器内的它头上，而实际跑的是 server。
保留 `quickEval` / `trainingCSV` / `saveTrainLogCSV`（这三个有调用方）。

### 2. `winner` vs `rankOf` 一致性：**在真实考卷口径上不复现复核的 ~15%**
探针（3 类场 × 300 局 = 900 局）：**"winner 不是 rank1" 的局数 = 0**。
只有 6 局（全在进攻型场）是引擎判 `draw` 而 `rankOf` 仍排出第一 —— 这是设计使然
（N10 的全灭/并列平局 vs 排名按 血量→承伤→伤害→洗牌 继续排）。
⇒ 第十轮复核 §6-7 那条（他们自己也标注"样本口径不干净、含高温网络"）**不成立**。

### 3. `docs/RULES-NP.md` 的过时口径
第 195 行原写"**镜面反射本版未实现**（见 N11）"，而同文件 184/274/294 行都写已实现（N14）
⇒ 改为"双枪射手与镜面反射在 N≥3 时启用（规则见 N14）"，并注明 N11 那句是早期设计稿残留（复核 §6-3）。

### 4. 顺带修掉一处**文档漂移**（本轮才发现，比单条缺失更严重）
CHANGELOG 缺 v1.4.10 / v1.4.12 两条，且 **README 停在 v1.4.6、index.html 停在 v1.4.1**（HEAD 是 v1.4.13）。
根因：我用 `sed -i 's/当前版本：v1.4.6/…/'` 假设"上一版的字符串"，而 A/B 任务对 `index.html` 的备份还原
打乱过它，且 **`sed` 不匹配时不报错** ⇒ 连续多次静默 no-op。
修法：一次性补齐条目 + 版本号改用**任意版本的正则**（`s/v[0-9.]+/vX.Y.Z/`）+
新增 **np-test D8**（README/index.html 的版本号必须与 CHANGELOG 最新 `## v…` 一致）——让这类漂移立刻变红。

回归：spec 37/37、np-test 57/57。

## v1.4.12 — 多 seed 配对（n=6）：加反弹墙无显著影响 ⇒ 不改默认池、不换冠军

12 次训练（6 seed × {12 对手, 12+反弹墙}，同起点 v1.3.58、同预算 250 代）+ 24 次评估（标准考卷 + 反弹墙各 1400 局）。
**内建复现校验通过**：seed 31 重跑出的两臂与归档 `hB12`/`wall2-31` **逐字节相同**。

| 指标 | p12(12 对手) | p13(12+墙) | 配对 Δ(n=6) | sd | t | 判定 |
|---|---|---|---|---|---|---|
| 训练自评 | 38.30 | 39.24 | +0.93 | 3.06 | 0.74 | 不显著 |
| 标准考卷 1st | **37.48%** | **35.65%** | −1.83pt | 4.28 | −1.05 | 不显著 |
| 反弹墙 1st | **47.50%** | **72.08%** | +24.58pt | **60.92** | 0.99 | **不显著** |

逐 seed 墙差分 `0 / +80 / −67.5 / 0 / +35 / +100`，**两臂各自极差都是 0→100pt**
⇒ 墙的表现由"这一跑落到哪个局部最优"决定，不由池子里有没有墙决定。
⇒ **不改默认池、不换冠军**（没有一臂能稳定超过现役 38.4%），`reflectspam` 留作 `?opps=` 旋钮。
**纠正**："wall2-31 三项全面优于现役"是 seed 运气（会话里第三次 n=1 读数在多 seed 下溶解）。

新增 `tools/ab-analyze.mjs`（配对表 + 均值/sd/se/t/符号检验 + **两臂单跑极差**）。
并把用户要求的两条流程写进 `docs/REVIEW-5P.md` §3：①长任务先报时间预估、跑中定期报进度；
②node 的 `/tmp` 路径坑（须 `cygpath -w`）。

## v1.4.10 — 消融（`--ban`）成为 report 的主口径

**动机**：mono-spam Δ 对"冠军已经在用"的技能会给出**相反结论**。实测同一张卡：
`枪` mono −15.5pt（读作"坑/别用"）但消融 Δ_lost **+3.8pt**（读作"该留着"）；
`雷击之枪` mono −44pt（读作"没用"）但消融 Δ_lost −2.6pt（读作"拿掉反而更好"）。
⇒ report 新增 **「消融 Δ_lost」列**：对使用率 ≥1% 的技能另跑一次"拿掉它"的同 seed 原生经济考卷，
Δ_lost = 原生基线 − 拿掉后（**正数 = 承重、负数 = 陷阱**），并由它主导判定：

| 技能 | 用量 | mono Δ | 消融 Δ_lost | 判定 |
|---|---|---|---|---|
| ジ | 66.3% | −49.7pt | **+22.2pt** | 承重 |
| 激光剑 | 25.2% | +22.5pt | **+21.4pt** | 承重 |
| 枪 | 3.8% | −15.5pt | **+0.5pt** | 承重（**不再是"坑"**）|
| 狙击枪 | 2.3% | +16.7pt | +0.0pt | 中性 |
| 雷击之枪 | 1.6% | −44pt | −2.6pt | **陷阱（本场）** ← 见 v1.4.11，反制卡须带场限定 |

阈值从 2% 降到 1%（否则 `雷击之枪` 1.6% 会被挡在消融之外，而它正是最该被标出来的那类）。

## v1.4.11 — 用户纠正：**小雷不是陷阱卡**，它的价值是反制（我错在考卷里没有"环"）

用户 2026-09-12：「小雷不至于是完全的陷阱卡，最典型的就是用来打别人的聚能环（使其失效中断）。
在多人有结盟的情况下也可以用来打电磁炮或大雷等，效率是很高的。」

### 1. 机制确认
- `resolve.js:789` `if (!(a && a.outcome === 'ok' && a.key === SK.RING)) p.ringStreak = 0;`
  ⇒ **被小雷 void 掉的那次环不是 `ok`，`ringStreak` 归零** ⇒ 3 ジ的投入与"+3 ジ/回合"的复利线一起报废。
- 聚能环是**全游戏唯一的"钱生钱"线**（`resolve.js:614`：首次 3 ジ→+1；连续第 2 次 0 ジ→+2；第 3 次起 0 ジ→**+3**）。
- 而池子里唯一的反制脚本 `pickComboCounter` **只对大雷/电磁炮/电系出防御，没有"对手在开环 → 出小雷"这条分支**
  ⇒ 这条反制线在池子里**从未被演练过**。

### 2. 实测（新增 `pickRingSpam` 开环经济流 + `--field=ringwall/ringmix`）
| 场 | 臂 | 1st | 5th |
|---|---|---|---|
| **5 血 · 4×环流** | 基线 | **2.5%** | 85.0% |
| | **`--inject=miniT`（优先小雷）** | **22.5%（+20pt）** | 60.0% |
| | `--ban=miniT`（拿掉它自然用的小雷） | 2.5%（**无变化**） | 85.0% |
| | `--inject=sword`（对照） | 7.5% | 72.5% |
| **5 血 · 中性场（无环）** | 基线 | 31.1% | 7.8% |
| | `--inject=miniT` | **5.4%（−25.7pt）** | 42.7% |

**同一个动作：有环场 +20pt、无环场 −25.7pt** ⇒ "小雷是陷阱卡"是**场依赖的结论**。
我 v1.4.5 与第十轮复核那轮阶梯都在"没有环/电磁炮/大雷"的场上测它，故都把它的反制价值测成了 0。
`--ban=miniT` 在环场上毫无变化 ⇒ **冠军那 1.6% 的小雷完全没瞄准环**，这条线它没学。

**另一个顺带的大发现**：**4×环流在 5 血下把现役冠军打到 1st=2.5%、场均承伤 5.90（≈满血上限）**、
终局血量 0.07 ⇒ 聚能环是**长程模式里真正的胜利路线**（第十轮复核的"环→bank8 出大雷"轨迹
在 5 人 5 血上是最优解，与此一致）。3 血下它反而退化（冠军 100%：环还没回本就被打死）。

### 3. 代码改动
- `js/train/bots.js`：新增 `pickRingSpam`（开环→续环→攒到 5 ジ兑现一次大件→濒死保命）；
  `pickComboCounter` 补"对手在开环 ⇒ 出小雷"分支。
- `tools/eval-5p.mjs`：新增 `ringspam` 与 `--field=ringwall/ringmix`；
  **`PAY_KEY` 改为从 `R.skills` 自动生成** —— 手写映射漏了 `miniT` 等，
  于是 report 的 INSTR 推荐 `--inject=miniT` 时脚本直接 `exit(1)`，而我在 grep 里只看到"没有输出"，
  差点当成"这一臂没信号"（"白名单与技能表两份维护"的又一例）。
- `tools/skill-report.mjs`：`miniT` 的「该用口径」标注为**反制卡**（指出本场无环 ⇒ 反制价值测不到）。

回归：spec 37/37、np-test 56/56。

## v1.4.9 — 对手池抽成单一来源（静默漂移坑）+ 反弹墙实验的诚实结论

### 1. 抓到一条静默失败：**"13 对手"的臂实际只跑了 12 个**
v1.4.8 我把四个 specialist 加进了 server 的 `BOT_FN_N`，但 **worker 的 `OPP_POOL` 是另一份独立清单**
（名字→函数引用，因为函数不能跨线程传）⇒ worker 的 `filter` **静默取子集**。
Worker 里那条响亮告警（v1.3.59 加的）**只写进隐藏 server 进程的 stderr**，而我是用 curl 抓 SSE 日志的
⇒ 完全看不到。我是靠"两个不同臂的**考卷分布逐位相同**"这个味道才察觉的。

**顺带得到一个强验证**：把 META 行去掉后，`wall-31` 与 `hB12`（同 seed/同池/同起点）**权重逐字节相同**
⇒ **训练是逐位可复现的**（v1.3.36 那次确定性工作的实证）。

**修法**：抽 `server/opp-pool.mjs` 作为唯一来源（16 个名字的 `OPP_SPECS`），server 的 `BOT_FN_N`
与 worker 的 `OPP_POOL` **都从它派生**；`OPP_DEFAULT` 明确默认池不含四个 specialist。
新增 **np-test D7**（反证：任一侧改回字面量名单立即红）——这是"两处各写一遍"第 3 次坑同类问题，
所以这次是结构上消除，而不是再补一次名字。

### 2. 反弹墙实验（墙这次真的在池子里）
2 seed × 250 代热启动同起点 v1.3.58，池 13（含 `reflectspam`，权重 1/13 ≈ 7.7%）：

| 臂 | 训练自评 | 反弹墙 1st | 标准考卷 1st | top2 |
|---|---|---|---|---|
| v1.3.58（现役） | 0.3764 | **0.0%** | 38.4% | 55.9% |
| **wall2-31**（含墙 seed 31） | 0.3865 | **100.0%** | **40.6%** | **64.4%** |
| wall2-33（含墙 seed 33） | 0.3679 | 32.5% | 36.1% | 56.9% |
| hB12（**不含**墙 seed 31，配对参照） | 0.3864 | **100.0%** | 37.2% | 57.9% |
| hA9（**不含**墙，9 对手） | 0.4028 | **85.0%** | 39.4% | 58.2% |

**三条结论**（与我预想不同，如实记）：
1. **低权重没有过拟合**：含墙臂的标准考卷 40.6% / 36.1% 不低于不含墙的 37.2% ✔
   （用户"免得过拟合"的担心在数据上不成立，7.7% 同权是安全的）。
2. **但洞也不是"加墙就能补"**：seed 31 **加不加墙都 100%**；seed 33 加了墙反而只有 **32.5%**，
   而且正好落在"谁也伤不了谁"的退化分布（32.5/15.0/20.0/7.5/25.0 —— 另一个场次也出现过这个五元组）。
3. ⇒ **这个洞是特定网络（v1.3.58 / armB12f = 0%）的性质，不是训练配方的性质**：
   hA9(85%) / hB12(100%) **从没见过墙**却已经不会自杀。
   故第十轮复核的 P0-c 对**现役产物**成立，但"把墙放进池子"不是可靠的修法。

**据此不换冠军**：`wall2-31` 三项指标全面优于现役（40.6/64.4/100%），但 **n=1**，
而本项目已标定同设置单跑极差 **8.6pt** ⇒ 需多 seed 配对（≥4 seed）才能判。
（下一次实验设计：4 seed × {12 对手池, 13 对手池} 配对，比较均值与方差，同时看反弹墙列。）

回归：spec 37/37、np-test **56/56**。

## v1.4.8 — 终局收缩替代硬截断（用户方案）+ 查清"反弹墙为什么不在训练池里"

用户三问：① 反弹墙这些原来不在训练池吗（我记得以前有）？② 硬截断可以改成 100 回合后每轮全员扣一点血；
③ 纯防御招没必要在训练里给很高权重，免得过拟合。

### 1. 查清了：**是被 v1.3.22 剔除的**（用户记忆正确）
`3462abf` v1.3.22(乙)「对手风格即难度档」先跑 `bot-audit.mjs` 审计 23 个对手，再按这些理由剔除：
- **退化**：`reflectspam`/`guardspam`/`baguaspam`（有效技能数 = 1.00，"只会一招"）、
  `wall`/`protowall`/`reflecttank`（**1st = 0%**，"自己赢不了，只会把局拖到上限"）、`random`/`farmer`/`whiff`；
  **拖沓**：`balanced`/`defend`；**重复**：`tankline` ≡ `heavyfire`。

**这是范畴错误**：审计问的是"它是不是一个像样的**对手/人格**"（能赢吗？打法多样吗？），
但训练池该问的是"它能不能**暴露冠军的洞**"——一个 0% 胜率的墙恰恰是最好的 exploit cursor。
而且**执行得不一致**：`random`/`wall`/`farmer`/`balanced`/`defend` 后来都还在默认池里（9 个里占 5 个），
只有反弹墙们没回来。

### 2. 终局收缩（用户方案，`MODES.long`）
`{ suddenDeath: 100, maxRounds: 140 }`：到 100 回合后每回合末**全员 −1 血**，
带 `bypassGuards`（场地收缩不是攻击 ⇒ 防御/反弹/原型/金刚盾/全息都不挡）与 `noMine`（不触发地雷）、
`source: null`（不计入"造成伤害"，但计入承伤 ⇒ `rankOf` 的承伤升序仍能区分名次）。140 只作安全网。

实测（150 局/格）：防守型场 `multi` **撞上限 150/150、终局 5 人全活**（谁都没死）；
`long` **平均 104 回合、收缩生效 150/150、撞安全网 0**。三类场共 450 局 **0 局**碰到安全网 ⇒ 硬截断消失。
**已知边界**：纯乌龟场里全员同时归零 ⇒ 走 N10"全灭平局"（引擎 winner=draw），
但训练/评测的名次走 `rankOf`（承伤升序可区分）仍是有意义的序。`multi` **未启用**（避免历史基线全部重标）。
规则文档见 **N25**；用例 **np-test D6**（反证：去掉 `bypassGuards` 或 `noMine` 立即红），D3 按新契约更新。

### 3. 权重（用户第三问）
四个 specialist 名字已补进 `BOT_FN_N`（可寻址、可用 `?opps=` 实验），但**默认池只加 `reflectspam` 一个**：
- 我方复验：**只有反弹是真洞**（冠军对 4×反弹墙 1st = **0.0%**，对 4×防御/八卦/原型墙都是 **100.0%**）
  ⇒ 另外三种墙加进去是纯粹的过拟合风险，没有收益；
- 它在 13 个对手里只占 **1/13 ≈ 7.7%**（与其他对手同权，不是加权）。
- 验收（第十轮复核 §8-1）：冠军在 `--field=reflectwall` 的 1st **0.0% → ≥50%**，
  同时**标准考卷不得比 12 对手基线（hB12 37.2%）明显变差** —— 2 seed × 250 代热启动已在跑。

回归：spec 37/37、np-test **55/55**。

## v1.4.7 — 处置第十轮独立复核：确认反弹墙(P0-c) + 更正我两处结论 + 修 3 处小问题
复核文档 `results/Epirus-Web-第十轮复核-5P与长程.md`（千问自建评测架，不复用我方工具，未改仓库文件）。

### 1. 已复验并升级为最高优先：P0-c「0 ジ 反弹墙」
1400 局/臂，我方架子独立复现：冠军对 4×`reflectspam` 的 1st = **0.0%**、场均承伤 **3.00**（= 满血 3）、
**自己的攻击被弹回 3.00 次/局**、终局血量 **0.00**、平局率 0%。天花板存在：`--pure=sword`（激光剑
`pierce:{reflect}`）与 `--pure=snipe` 都是 **100.0%**，而同一批纯招在中性场只有 34.6% / 3.1%。

### 2. 更正我自己的两处结论
1. `REVIEW-5P` §2-P0 的机制表述不完整：漏了 `聚能环` 的 **0 成本 +3/回合**复利收入线
   （首次 3 ジ→+1；连续第 2 次 0 ジ→+2；第 3 次起 0 ジ→+3）。历次补贴恰好绕开这条线
   ⇒ "补贴教会它花钱"这个前提从头就是反的，补贴类实验应停。
2. CHANGELOG v1.4.5 §3 写"电磁炮被坦克支配"是错的：电磁炮 **0.67** 伤害/ジ > 坦克 **0.50**，
   且穿防+穿反弹；真实约束是**时机**（珠只活一回合），不是价格。

### 3. 本轮修复（每条都配了会失败的用例或反证）
- `蓄能` desc 与 R9' 矛盾且 `ui.js:131` 直接展示给玩家 ⇒ 改为"只保留到下一回合，同类珠最多 1 枚"。
- 双枪第二发写死 `amt:1`/NORMAL/无 pierce（改数据表只影响第一发）⇒ 复用 `dgDmg`；
  新增 **np-test D5** 固化（反证：改回写死 1 立刻红 `got=1 want=2`）。
- 30 分钟墙上时钟上限 ⇒ `EPIRUS_WALL_MS` 可关（默认不变）。
- `RULES-NP.md` N1 范围声明补 `long`（5 血）。
- 新器材：`--pure=<技能>` 纯招口径（`--inject` 会从回落里漏出枪，测不了"单一卡片能否破墙"）、
  四种防御墙 `--field=reflectwall/guardwall/baguawall/protowall`、机制读数（被弹回次数/平局率/终局血量）。

`docs/REVIEW-5P.md` 加 **§5 第十轮复核处置**（逐条 8 项 + 我两处结论的更正 + 更新后的优先级）。
回归：spec 37/37、np-test 54/54（当时）。

## v1.4.6 — report 自己说清"哪些测不了、该用什么命令" + 一键入口 + REVIEW-5P

用户要求：① 更新 skill report；② 给一个简单启动方式好自己生成技能表；③ 已知情况写进 review（现在可以写 5P 版）。

### 1. report 新增「该用口径」列 + 覆盖表 + 覆盖率卡片
- 每个技能多一列 **该用口径**：冠军使用率 ≥2% ⇒ 橙色 `--ban=<key>`
  （消融 —— "强制 spam 它"对**已经在用**的技能没有意义）；否则按技能族查表：
  珠类门 → `--grant`；连招/序列 → `--plan`/`--combo`；反应条件 → `--smart`；辅助防御 → `--field`。
- 新增**覆盖表**两块：①「本口径量不到」逐条给出该用命令；②「冠军已经在用」列出该做消融的技能。
- 卡片新增 **Δ 本口径可量 27/30**、**冠军已在用 4** ⇒ 覆盖率一眼可见。
- 意义：报告不再把"这个口径表达不了"读成"这个技能弱"。

### 2. 一键入口（用户可自己操作）
- `tools\skill-report.cmd [冠军文件] [局数=6]` —— 生成 `docs\skill-report.html` 并自动打开；
  默认测当前线上冠军，传路径即测某个存档版本。
- `tools\skill-report-compare.cmd [局数=6] [标签…]` —— 逐版本测完再合成 `docs\skill-report-cmp.html` 对比页
  （默认 5 个版本 + current）。两者都支持 `EPIRUS_NO_OPEN=1` 只生成不开浏览器。
- ⚠️ 踩坑两条（已写进脚本注释）：
  (a) **`%*` 不受 `shift` 影响** ⇒ 我第一版把 `6` 当成了版本标签（`missing docsrtifacts.bak`），
      改成显式循环收集参数；
  (b) **`.cmd` 里的中文注释在 GBK 控制台码页下会把行拆成非法命令**
      （`'技?×' 不是内部或外部命令`）⇒ 两个脚本改为**纯 ASCII**（这正是仓库既有 `.cmd` 全英文的原因）。

### 3. `docs/REVIEW-5P.md`（交千问的 5P 版复查）
结构：一句话现状 / 已解决 10 项（带证据与版本）/ 尚存 P0–P3 / 方法论 7 条 /
**6 个请千问复查的问题** / 附快速复现命令。P0 仍是深经济锁死（缺口 21~41pt），
并把四类卡的瓶颈分开写清：攒钱耗时型（5 血可解冻）、反应型（条件率 0.2%）、
组合型（`age≤3` 算术上限）、连招型（3 ジ/2 伤被坦克 2 ジ/2 伤支配）。

### 4. 顺带修掉 label 推导（会污染对比页表头）
`skill-report.mjs` 的 `label` 与 `cmp` 工具的归一化都写成 `/^.*[\/]/`——经 heredoc 转义后落到文件里
变成 `[\/]`（只匹配 `/`），而 Windows 路径是反斜杠 ⇒ 表头显示**整条路径**、列宽翻倍。
改用**完全不含反斜杠**的写法：`/^.*[^0-9A-Za-z_.-]/`（剥目录）+ `/[.][A-Za-z]+$/`（剥扩展名）。
教训：**跨 heredoc 写正则必须回读字节验证**（本轮为此栽了三次）。

回归：spec 37/37、np-test 53/53。

## v1.4.5 — 补齐"测不了"的四个口径：消融/开珠/连招/条件注入

用户提问：「report 有一堆技能没办法测量……连招类的技能（蓄能、聚能环、贴贴）总不能测单技能结果」。
诊断：这不是技能弱，而是 **mono-spam 口径表达不了 4 类技能**。本轮把 4 个口径补齐，并**都跑出读数**。

### 1. `--ban=<技能>`：**消融（拿掉）** —— report 一直缺的镜像口径
`--inject` 测的是"强制用它"，只有**冠军根本不用**的技能才需要它；已用的技能只能靠"拿掉"来测。
实现：只过滤 chooser 拿到的 `legal`，冠军的 softmax 在剩余可负担动作上重算。

| 臂 | 基线 | 拿掉后 | Δ_lost |
|---|---|---|---|
| hA9 − 狙击枪（使用率 22.6%） | 39.4% | 29.9% | **−9.5pt** |
| armB12f − 坦克（27.0%） | 33.3% | 29.3% | **−4.0pt** |
| hA9 − 聚能环（5.5%） | 39.4% | 37.8% | −1.6pt |
| hB12 − 真正的落雷（1.8%） | 37.2% | 35.6% | −1.6pt |
| **对照**：hA9 − 激光剑（**使用率 0.0%**） | 39.4% | 39.3% | **−0.1pt ✔ 空操作** |

⇒ 反证成立（消融器材本身不改变行为），故其余读数可信。

### 2. `--grant=<elec|boom|epN>`：**回合开始钩子**，专治珠类门
根因：珠类技能的门在 `computeCost` 里判，而 `legal` 在 chooser **之前**算好
⇒ 任何"在 chooser 里补珠"的做法都太晚（实测强制命中 **0/40901 = 0.0%**）。
修法：`js/core/play.js` 的 `autoGameN` 新增第 4 个参数 `onRoundStart`（在 `startLegal` 之后、
`legalActions` 之前触发；默认 undefined，对线上/训练零影响）。

| 技能 | 旧口径 | 新口径（同补贴 A/B） |
|---|---|---|
| 电磁炮 | 命中 1/40901 = 0.0%（不可读） | 命中 **24.6%** → Δ **−0.3pt**（中性） |
| 激光眼 | 命中 0/40901 = 0.0%（不可读） | 命中 **48.8%** → Δ **+9.5pt**（**强招**） |
| （副产物）每回合白送 1 枚电珠 | 冠军 38.4% | 冠军 **51.2%（+12.8pt）** |

⇒ 电磁炮本身很强（白送珠 ⇒ +12.8pt），**但它真实的获取路径被经济支配**（见 3）。

### 3. `--plan=chargeRailgun`：**连招序列主体**（蓄能→电磁炮）
珠子时效 R9' 精确化（`js/core/resolve.js:790-796`）：
`const keep = p.beadNew || null; p.elec = keep === 'elec' ? 1 : 0;`
⇒ **只有"本回合新蓄"的珠才保留**；持珠那回合若打 ジ 攒钱，珠子会在回合末被清空。
又因 **蓄能不给 ep 收入**（唯一来源是 ジ 的 +1，`resolve.js:501`），连招必须一次到位：
**ep≥3 蓄能 → 下回合 ep=2 + 珠 → 电磁炮**。

- 第一版连招在 ep=1 就蓄能 ⇒ 28401 次决策只打出 **1** 次电磁炮（1st **0.8%**）；修正后打出 **4233** 次。
- 修正版结果：**1st 26.8%（Δ −11.6pt）**，但 **top2 69.4%、5th 仅 2.3%** ⇒ **活得久但赢不了**。
  轨迹成本 = 攒到 3 ep 的 3 个回合不攻击；而 3 ジ/2 伤被 **坦克的 2 ジ/2 伤**支配。
  ⇒ 连招"能跑通"不等于"值得用"，这个区分正是单动作口径永远给不出的。

### 4. `--smart=<卡>:<条件>`（v1.3.60 已有，本轮并入覆盖表）
条件型：转移（`lastTaken≥k`）实测 Δ≈0 且条件只成立 **0.2%**；摄魂（`hp≤k`）5 血 HP≤1 **+1.8pt**。

### 5. 覆盖表（每个技能族该用哪个口径）
| 技能族 | 原口径的问题 | 该用 | 本轮实测 |
|---|---|---|---|
| 冠军已在用（含连招内部件） | 强制 spam 对已用技能无意义 | **`--ban` 消融** | 狙击枪 −9.5pt / 坦克 −4.0pt / 聚能环 −1.6pt / 大雷 −1.6pt |
| 珠类门（电磁炮/激光眼） | 珠子补得太晚 ⇒ 命中 0% | **`--grant` + `--inject`** | 激光眼 **+9.5pt**；电磁炮中性 |
| 序列/连招 | 单动作表达不了序列 | **`--plan` / `--combo` + 承诺攒钱** | 蓄能→电磁炮 26.8%（赢不了） |
| 反应/条件 | 无条件 spam 必亏 | **`--smart`** | 转移条件率 0.2%；摄魂 5 血 +1.8pt |
| 辅助/防御 | mono-spam 结构性为负 | **`--field` 前置条件场** | 集火场 反弹 90.0% > 藤甲 85.0% > 防御 60.0% |

回归：spec 37/37、np-test 53/53。

## v1.4.4 — 多 seed 配对 A/B（n=4）：12 对手均值 +4.9pt 且方差更小；skill-report 支持多版本对比

### 1. 对手池 A/B（热启动同起点 v1.3.58、同预算 250 代、seed 1-4 配对）
| seed | 9 对手 | 12 对手 | Δ |
|---|---|---|---|
| 1 | 39.4% | 37.2% | −2.2 |
| 2 | 27.8% | 40.4% | +12.6 |
| 3 | 30.1% | 36.6% | +6.5 |
| 4 | 34.4% | 37.1% | +2.7 |
| **均值** | **32.9%** | **37.8%** | **+4.9pt**（3/4 正） |

- 配对 t≈1.57（df=3，p≈0.11）、符号检验 3/4（p=0.31）⇒ **方向一致但 n=4 未达显著**，定论需 8~12 seed。
- **方差**：9 对手组极差 **11.6pt**、12 对手组只有 **3.8pt** ⇒ 池子更宽时训练更稳
  （与前一轮"深经济 Δ −38.6 → −3.3pt"同向）。
- **修正我自己的说法**：我曾据 hA9（seed=1，39.4%）断言"简单续训 250 代就能超过现役 38.4%"——
  多 seed 下 9 对手组均值只有 **32.9%**（27.8~39.4）⇒ **那是 seed 运气**，不能当改进依据。
- 建议：把 `tankline`/`heavyfire`/`deepsaver` **加进训练池**（均值更好 + 方差更小），但**暂不替换现役冠军**。

### 2. skill-report 支持多版本横向对比
- `tools/skill-report.mjs` 新增 `--champ=<文件>`（冠军文件原先写死 ⇒ 无法按版本测）与 `--json=<文件>`（导出逐技能结果）。
  ⚠️ 训练实验进行中时 `js/bundled-champion-3p.js` 是**中途产物**，对比务必显式传 `--champ`（我踩过一次：
  没传时测到的其实是扫描进行中的中途冠军）。
- 新增 `tools/skill-report-cmp.mjs`：只读多个 JSON，产出技能 × 版本矩阵 + 判定计数矩阵 + 控制台文本表。
- 五个版本的报告与对比页：`docs/artifacts/sr-{v1.3.58,hA9,hB12,armB12f,armA9}.html`、`docs/skill-report-cmp.html`。
- `champion-3p-div555.bak` **测不了**：它是 `v=5 f=122` 旧包，当前 `PACK_VERSION=6 / FEAT_S=123` 不兼容
  （旧包的可复现线只到 v4）。

### 3. 对比里的主要发现（同口径：1 冠军 + 4 脚本、座位轮换、126 组对手场 × 6 局 = 756 局/条件）
- **战术分化极大**：v1.3.58 激光剑 25.2%；hA9 狙击枪 **22.6%** + 聚能环 **5.5%**；armB12f 坦克 **27.0%**；
  armA9 枪 **36.8%**（原生 1st 只有 22.6%，最弱）。hA9/armA9/armB12f **三版都放弃了激光剑**（0.0~0.3%）。
- **大雷终于被用上**：hB12 1.8%、hA9 1.3%（v1.3.58 为 0.0%）。
- **最稳定的"没学会的强招"**：原型制御（Δ +13~+30）、全息屏障（+14~+30）、摄魂指法（+5~+21）、
  真正的落雷（+13~+28）—— 使用率在所有版本都是 0.0%。
- **hA9 的"货架缺口"最大**：富经济 1st **66.1%** vs 原生 31.3%（差 34.8pt）⇒ 它最依赖钱，也最可能被经济机制解放。
- ⚠️ **判定阈值是刀锋**：`枪` 在 v1.3.58 使用率 **3.8%**，紧贴 4% 的"常用"阈值 ⇒ 它在"坑/死技能"之间摆动。
  边界技能请**读 Δ 不看标签**。

## v1.4.3 — 对手池 A/B（从零冷启动）：符号 2/2 支持加对手，但**幅度未定**

用户方案：把 `tankline`/`heavyfire`/`deepsaver` 加进训练对手池，并用**配对种子 A/B** 验证。
四臂均为 100 代冷启动（seed=1，fresh）+ 250 代热启动（seed=31），`n=5 pop=16 gpo=8`；
同对内**只有对手池不同**（9 vs 12），并在我先后用过的两种轮换上各跑一对：

| 臂 | 轮换 | 对手 | 训练自评 firstRate | 考卷 1st（1400 局） |
|---|---|---|---|---|
| v1.3.58（现役，热启动谱系） | — | 9 | 0.3764 | 38.4% |
| armA9 | `(g*5+idx)` | 9 | 0.2819 | 21.8% |
| armB12 | `(g*5+idx)` | 12 | 0.2894 | 23.1% |
| armA9f | `(gen*3+g)` 配对 | 9 | 0.2708 | 18.1% |
| **armB12f** | `(gen*3+g)` 配对 | 12 | **0.3439** | **33.3%** |

**结论（诚实的）：**
1. **符号 2/2 一致**：两对臂里都是"12 对手"更好 ⇒ 方向性证据支持加对手。
2. **幅度未定**：配对轮换那对 **+15.2pt**（自评 +7.3pt），idx 轮换那对只有 **+1.3pt**（自评 +0.8pt）。
   每格 n=1，而本项目已标定的**同设置单跑极差是 8.6pt** ⇒ **+15.2pt 不能当效应量**。
   要定幅度必须多 seed 配对（每条件 ≥4 seed + 逐 seed 差分 + 符号检验）。
   （这也再次说明 v1.4.1/v1.4.2 的教训：n=1 不得下因果判断。）
3. **从零冷启动 350 代仍低于现役冠军**（最好 33.3% vs 38.4%）⇒ 现役不替换；
   再次印证 v1.4.2 的结论：v1.3.58 的优势来自**热启动谱系**，不是配方。
4. 训练侧自评与考卷**同向**（两对臂都一致）⇒ 自评 `firstRate` 是可用的一等指标。
5. 各臂日志的代数已确认跑满（`maxGen=99` / `249`，各段 `done` 事件在案）。

**下一步（已在跑）**：用**热启动**从 v1.3.58 出发做 9 vs 12 的配对 A/B（两臂同起点、同 seed、同预算 250 代）
—— 这才是能直接回答"该不该把三个脚本加进训练池"的实验。

## v1.4.2 — 更正 v1.4.1 的归因：38.4% → 21.8% **不是**轮换造成的

v1.4.1 我据两个数据点断言"把轮换改成 `(g*5+idx)` 拆掉了同代配对评估，代价是 38.4%→21.8%/23.1%"。
**这个归因是错的**，而按它做的"修法"也救不回来。证据（训练侧自评与考卷一致 ⇒ 不是评测口径问题）：

| 产物 | 轮换 | 训练自评 firstRate | 考卷 1st |
|---|---|---|---|
| v1.3.58（现役） | `g % n`（同代配对） | **0.3764** | 38.4% |
| armA9 | `(g*5+idx)`（我改坏的） | 0.2819 | 21.8% |
| armA9f | `(gen*3+g)`（v1.4.1 的"修法"） | 0.2708 | **18.1%** |
| armB12 | `(g*5+idx)` | — | 23.1% |

配对轮换**没有把它救回来**（还略低 3.7pt）。

**真因是起点不同**：3P/5P 谱系是一长串**热启动链** —— 存档里 `champion-3p-*` 全部
`gens:1500`、自评 0.45~0.56；v1.3.58 的 100+250 是**从当时在线的 div555 谱系冠军热启动**的
（考卷表里"100 代版 34.7% > div555 30.8%"也只在热启动下才说得通，从零 100 代不可能超过 div555）。
而我的 A/B 两臂用了 `fresh=1`（**从零冷启动**）⇒ 350 代也只到 18~28%。
**所以 38.4% 不是"从零 350 代"的可比基线，我拿它当参照本身就错了。**

**教训（同类错误第二次）**：n=1 的单跑差 3.7pt 完全落在本项目已标定的噪声范围内
（同设置 3 次 firstRate 极差达 0.086），我却据此下了因果判断 —— 与 v1.3.12
"单次 17:62 就收回 WR_TOL"是同一个错误。**单跑不得归因。**

**保留 v1.4.1 的轮换修法，但把理由改成设计而非实测**：`(gen*3+g)` 既保住同代配对，
又能覆盖任意大的池子（原 `g % n` 在池子 > games+3 时会漏掉尾部队手）。np-test D4 同时保留 ——
它守的是"同代配对"这条**设计不变量**，不是那个被我算错的因果。

**池子 A/B 改用热启动重做**：两臂都从 v1.3.58 出发、同 seed 同预算（250 代），只差对手池（9 vs 12）。

## v1.4.0 — 长程模式（5 血）：拉长赛程**只能解冻一部分**被锁死的卡

用户提出「线下玩会通过多人混乱的场面实现平衡，但程序里面显示得太明显」，并建议做 5 血长程模式；
同时要求若做 5 血就把**摄魂指法**的启用血量从写死的 HP≤1 调成 2 或 3，并实测平衡影响。

### 1. 实现
- `rules.js` 新增 `MODES.long = { hp:5, 3-5人, drainHpMax:3, maxRounds:100 }`；
  `standard`/`multi` 口径**一律不变**（摄魂仍 HP≤1、上限仍 60）。
- 摄魂门槛改读 `state.mode.drainHpMax`（缺省 1）。
- **回合上限也做成了按模式配**：实测全局 `MAX_ROUNDS=60` 会把 5 血局截断 ——
  3 血平均 **43.3 回合**、5 血只有 **49.8**（+15% 而非 +67%），且 5 血时主体场均承伤只有
  **4.28 < 5**（不是被打死，是到上限按"血最多者胜"结束）。配 `maxRounds:100` 后 5 血为
  **72.3 回合 = 43.3 × 5/3**，与血量成正比。策略特征 `round/MAX_ROUNDS` 的分母同步跟本局上限走。
- UI：模式下拉加「长程（3-5人·5血）」；人数↔模式改为按 `option.disabled` 校验
  （原实现 3-5 人时无条件改回 `multi` 并**整体禁用下拉** ⇒ 永远只能 3 血）。

### 2. 平衡实测（1400 局/臂，主体 v1.3.58 冠军；3 血基线 38.4%、5 血基线 32.6%）
**摄魂指法门槛：放宽反而更差。**

| 模式 | 门槛 | 注入率 | 1st | Δ vs 同模式基线 |
|---|---|---|---|---|
| 3 血 | HP≤1（现状 R25） | 0.3% | 39.5% | +1.1pt |
| 5 血 | HP≤1 | 0.3% | 34.4% | **+1.8pt** |
| 5 血 | HP≤2 | 1.2% | 32.3% | −0.3pt |
| 5 血 | HP≤3 | 2.4% | 29.5% | **−3.1pt** |

"HP≤1"的机会率在 3 血/5 血**完全相同（0.3%）** ⇒ 那扇窗并没有相对变窄。
**结论：门槛数值对平衡是负向的，唯一有效旋钮是费用（3 ジ）。**

**轨迹类卡的三类瓶颈必须分开看：**

| 卡 | 3 血 Δ | 5 血 Δ |
|---|---|---|
| 地雷（3 ジ·触发型 AoE） | −1.3pt | **+4.0pt** ← 解冻 |
| 坦克（2 ジ） | +3.7pt | +0.4pt |
| 转移伤害（2 ジ·反应型） | −30.8pt | −28.1pt（无改善） |
| 贴贴×天火（叠 3） | 引爆 0 次 | **引爆 0 次** |

⇒ 5 血只解冻"攒 3 ジ"那一类（地雷）；反应型（转移需要同回合 N≥2）与组合型
（贴贴受 `age≤3` 与"1 ジ/回合"双重限制）**与赛程长度无关**。

### 3. 测试
np-test 新增 **D1/D2/D3**（multi 摄魂仍 HP≤1；long hp=5/门槛 3；终局上限按模式走）→ **52/52**；
`tools/smoke.mjs` 新增 6 条 UI 断言（long 可选、3 人时 standard 置灰、开局日志写 5 血、HP 显示 5，
已用截图人工复核）；并修掉冒烟脚本"成功后因 CDP 连接挂住事件循环、被看门狗强制 exit(3)"的假失败。

## v1.3.60 — 条件触发型技能的专用器材 + 用户纠正的符咒机制

用户纠正：**藤甲 = 贴火弱**（`rules.js:55` → `resolve.js:706 fireWeakNext`、`:192` 火伤 +1），
**贴贴 = 贴符咒**（`rules.js:69` → `resolve.js:678 stickers.push`），**天火 = 引爆自己的符咒**
（`rules.js:70` → `resolve.js:683`）⇒ 组合对是 **贴贴×天火**，不是藤甲×天火。

`tools/eval-5p.mjs` 新增四件器材：`--field=`（focusfire/focusme/focusme2/tank/mine）、
`--regen=`、`--smart=<卡>:<条件>`（正确用法而非贪心）、`--combo=curseStorm[:N]`（组合技主体），
外加**前置条件读数**（主体场均承伤/转移事件/火焰伤害事件）—— 没有它们，条件性卡的 Δ 无法解释。
每个口径都带自检计数，抓到我自己的两次错误（组合技第一版不攒钱 ⇒ 天火只引爆 8 次；
`--smart` 第一版 TDZ 崩溃）。

关键实测：**藤甲被免费的反弹严格支配** —— `guardOf` 读的是宣告者自己的动作，所以格挡挂在自己身上，
`target:'enemy'` 只决定火弱贴给谁 ⇒ 藤甲 = 反弹 + 1 ジ 买一个火弱 debuff。
坦克场（4×tankline）基线 40.0%：反弹 **100.0%** vs 藤甲 10.0%；集火场基线 0.0%：反弹 **90.0%** vs 藤甲 85.0%。

## v1.3.59 — skill-report「一堆死技能」主要是**报告自己的测量失败**

三处缺陷：①**强制命中率从未被测量** —— `play.js` 把 `cost.ok===false` 的技能从 `legal` 剔除，
而强制只在 legal 里筛 ⇒ 条件门技能永远强制不到、Δ 恒≈0，被旧判定算成"死技能"
（实测命中率：电磁炮 0%、激光眼 0%、摄魂指法 3%；`RICH` 6→10 后 大雷 11%→97%、避雷针 50%→96%）；
②`usage()` 硬编码 3 个探针 ⇒ N=5 时两个座位是 `undefined` chooser（一律出ジ）；
③对手场只有 2 种脚本填 4 个座位，且 usage 与强度的环境不同（两列不可比）。
修后 **死技能 22 → 1**（只剩双枪射手），"没学会的强招"5 个（全息屏障 +30.2 / 原型制御 +29.0 /
真正的落雷 +27.6 / 狙击枪 +16.7 / 坦克 +13.4）。报告：`docs/skill-report-5p.html`。

## v1.3.58 — 首个 5 人冠军：3 人与 5 人**同时**变强，且座位中性

按用户要求"训练 5 人冠军"，全程走 **server 路径**（= 浏览器点"开始训练"的同一机制）。

### 1. 训练
`n=5, pop=16, gpo=8`：先 **100 代冷启动**（`seed=1`，281s），再 **250 代热启动续训**
（`seed=31`，从上一版产物长出来）。产物 meta：
`{"n":5,"gens":250,"games":8,"pop":16,"firstRate":0.376,"top2Rate":0.549,"seed":31,"fresh":false,"hotstartFrom":"f6788d9cffdc0d18","workers":16}`
—— `hotstartFrom` 正是 v1.3.56 加的那条"可复现输入"记录，**第一次实际派上用场**。
存档：`docs/artifacts/champion-5p-g250.bak`。

### 2. 结果（考卷 1400 局/臂）
| 主体 | 5P 1st | 5P top2 | 3P 1st |
|---|---|---|---|
| **新 5 人冠军** | **38.4%** | **55.9%** | **47.5%** |
| `tankline`（此前最强脚本基线） | 38.5% | — | — |
| 100 代版 | 34.7% | — | 46.0% |
| div555（旧，v5 补零升级） | 30.8% | 44.0% | 45.1% |

**5 人 +7.6~9.3pt、3 人 +2.4pt，两边同时提升，并追平最强脚本基线。**
座位率 **37.5 / 37.5 / 41.8 / 38.6 / 36.8%（极差 5.0pt，每座位 280 局 ⇒ sd≈2.9pt，在噪声内）**
⇒ v1.3.57 的座位中性在真实训练产物上得到确认。

**为什么冷启动重训能同时变强**：div555 的 meta 写着 `v5->v6 无损升级（新特征权重置 0）`
—— **它从没用过 v6 的新特征**。冷启动重训才第一次真正用上。

### 3. 行为发生了质变：**它开始花钱了**
| 决策时 ep 分带 | 旧 div555 | **新冠军** |
|---|---|---|
| 0-1 | 28.0% | **64.8%** |
| 10+ | **26.0%** | **0.5%** |
| 主武器 | 坦克 13.7% | **激光剑 26.0%** |
| ジ | 75.1% | 66.5% |

旧冠军是个**囤积者**（26% 的决策发生在 ep≥10、最高 ep 58，却几乎不买贵的）；
新的**每次到手就花掉**（64.8% 的决策在 ep≤1），主武器从坦克换成激光剑。
⇒ v1.3.54 那条"钱不缺、缺的是想买"在这里被进一步印证：**能把钱花出去就能变强**。

### 4. 货架问题**没变**，而且暴露出更大的弱点
新冠军 `cost>=3` 出手仍只有 **0.1%** ⇒ 3~5 ジ 档依旧无人问津（与 v1.3.55 的 5 人局消融
`大雷−坦克=−19.4pt` 一致）。
更值得注意的是**深经济对手的杀伤力变大了**：

| | 含深经济对手 | 不含 | Δ |
|---|---|---|---|
| 新冠军 | 34.1%（1200 局） | **64.5%**（200 局） | **−30.4pt** |
| 旧 div555 | 29.0% | 41.0% | −12.0pt |

**根因假设（下一步要验证的）**：训练对手池 `OPP_NAMES` 是
`random/balanced/aggro/defend/wall/antidef/breakdef/mix/farmer` ——
**既没有坦克流、也没有深经济对手**（`pickDeepSaver` 直到 v1.3.55 才被恢复，且**从未进过训练池**）。
考卷里有、训练里没有 ⇒ 学不到。这正是下一步"多人技能研究"（藤甲/地雷/转移/大雷）
与"把 `tankline`/`heavyfire`/`deepsaver` 加进训练池"的动机，**两件事是同一件事**。

**回归**：spec 37/37、np-test **49/49**。2P 冠军（`js/bundled-champion.js`）未被触碰；
`index.html` 的 `?v=` 缓存戳随冠军更新（22 行，均为 script 版本号）。

## v1.3.57 — 座位效应与平局问题：查出**两个方向相反的**成因并都修掉 + 首个 5 人冠军

> 用户裁定：浏览器可复现性那条线**不用管**（研究用 server 路径 = 浏览器同一机制即可），
> 接着做 **座位效应与平局 → 训 5 人冠军 → 多人技能研究**。

### 1. 诊断先行：新工具 `tools/rank-diag.mjs`，**同一批对局只换名次规则**
关键设计：对同一批对局分别用四种名次规则算，差异只能来自规则本身；
且必须有 **pid 中性对照**。四种：`key3`（旧规则，末位并列→pid 升序）、
`key4`（+累计造成伤害降序）、`joint`（并列同名次）、`rand`（**先按种子洗牌再稳定排序**）。

⚠️ 第一版 `rand` 写错了：按 key3 分组、**组内**洗牌、组间按出现顺序拼 —— 而组间顺序恰好
就是 pid 序，等于没消除偏置。**对照本身也必须是对的**，这是本文件唯一的教训。
改成"洗牌在排序之前"后，对称场各座位 18.0/21.5/20.8/19.6/20.1%（极差 3.5pt）—— 理想值。

### 2. 成因一：`rankOf` 的末位平局按 pid 升序（**这是规则产物，不是引擎**）
`js/train/evo.js` 的 `rankOf` 比较器是 `alive → hp 降 → taken 升`，**没有 pid**，
完全并列时落到 `Array.prototype.sort` 的稳定性 = 插入顺序 = **pid 升序**。
对称场 800 局实测：**89.9% 的玩家处在并列组里**（66% 打满回合上限、65.4% 以 draw 结束）
⇒ 各座位 1st 率 **P0=61.9% / P1=12.9 / P2=8.4 / P3=9.4 / P4=7.5%（极差 54.4pt）**。
把末位平局换成每局种子洗牌后 → 极差 3.5pt。**那个梯度完全是名次规则造出来的。**

### 3. 成因二：**脚本的目标选择**偏向低 pid（这是真实机制）
对称场里各座位均承伤是平的（1.186/1.126/1.144/1.134/1.143），所以效应只能来自脚本。
真实考卷场（1 主体座位 + 4 脚本、槽位随局旋转）实测：

| 座位 | P0 | P1 | P2 | P3 | P4 |
|---|---|---|---|---|---|
| 均承伤 | **2.683** | 2.494 | 2.446 | 2.440 | **2.203** |
| 终局 hp | 0.45 | 0.60 | 0.64 | 0.64 | **0.85** |

根因：`js/train/bots.js` 的 `mpLeader` / `mpSaver` / `mpKillable` 都按 `mpOpps()` **升序**迭代、
用 `>`/`<` 比较 ⇒ **并列时取最低 pid** ⇒ 低 pid 系统性挨打更多、名次更差。
（`pickTargetN` 本身是 pid 中性的——并列时用 `state.rng.next()`；只有 `pickTarget2N` 用 `opps[0]`，
仅双枪/镜面走。）

### 4. 修法
**(a) `rankOf(st, seat, seed)`：加第 4 键 `dealt` + 每局种子洗牌**
- 第 4 键 **累计造成伤害降序**（`damage` 事件现已带 `source`；地雷伤害 `source=null` 按规则不归属）。
  实测可拆开 13% 对称场 / 55% 真实场的并列组。
- 仍完全并列者 → **先用本局种子洗牌、再稳定排序**：key 的先后被严格遵守，并列者保留随机序。
  名次仍是 **1..N 的严格排列**（所有调用方与历史指标口径不变），但不再给座位 0 送分。

**两个被否掉的方案（都实测过，写在这里免得再走一遍）**：
- ✗ 消耗 `st.rng` 洗牌 ⇒ **不幂等**：同一局问多个座位会得到互相矛盾的名次；
- ✗ 用 `(round, events.length)` 派生种子 ⇒ 对称场里大量对局取值相同，**少数几个排列被反复使用**，
  偏置重现（实测极差 18.7pt，而每局种子只有 3.5pt）。

**(b) `mpPickOne(state, arr)`：并列时用本局 rng 随机选一个**，`mpLeader`/`mpSaver`/`mpKillable` 全部改用它
⇒ 同时修好 `pickMultiEasy/Med/Strong`（游戏内多人难度档）、`pickProtoMine/Transfer`、`pickDeepSaver`。

**(c) 6 处 `rankOf` 调用点全部显式传本局种子**（`evo.js`×3 + 5 个工具）；
`tools/eval-3p.mjs` 补 `[冠军文件]` 参数以便 A/B（此前它硬编码线下路径，**差点让我拿同一个文件做对比**）。

### 5. 结果（800 局/场）
| 场 | 旧规则极差 | 坏种子版 | **修后 `rankOf`** | 中性对照 |
|---|---|---|---|---|
| 对称场（全同 chooser） | 54.4pt | 18.7pt | **7.5pt** | 3.5pt |
| 真实考卷场（1+4 脚本） | 12.9pt | — | **4.4pt** | 3.8pt |

均名次分别 2.91–3.12 / 2.89–3.08（几乎平）。**幂等 ✔、严格排列 ✔。**

**钉成用例 L7**（`np-test`）：构造"五人完全并列"终局，断言 rank1 在座位间大致均匀
（400 次里极差 ≤80；旧实现是 `[400,0,0,0,0]`）。
**反证**：旧比较器在同一构造下顺序恒为 `01234`、rank1 恒为座位 0 ⇒ L7 在旧实现上必红。

### 6. 首个 5 人冠军（按用户要求走 **server 路径** = 与浏览器同一机制）
5 人局基线（考卷 700 局）：`tankline` **37.1%** / `heavyfire` 37.9% / 旧冠军 div555 **29.3%** / 对照 10.4%。

第一次训练：`n=5, pop=16, gpo=8, gens=100, 冷启动, seed=1`（281s），产物 meta 已带
`seed:1, fresh:true, hotstartFrom:null, workers:16`。

| | 3P 1st | 3P top2 | 5P 1st（考卷） |
|---|---|---|---|
| 旧 div555 | 45.1% | 72.6% | 29.3% |
| **新（100 代 n=5 冷启动）** | **46.0%** | **81.1%** | **34.7%** |

**两边都更好、没有取舍。** 这也解释了原因：**div555 是 v5 存档补零升级**（meta 写着
`v5->v6 无损升级（新特征权重置 0）`），**从没用过 v6 特征**；冷启动重训自然更强。
仍低于 `tankline` 的 37.1%，续训见 v1.3.58。

**回归**：spec 37/37、np-test **49/49**（新 L7）。

## v1.3.56 — 查实「浏览器训练不可复现」的真因（**并纠正我自己在 P3.3 写错的归因**）+ 补推送

### 0. 先修一次交付疏漏：v1.3.54/55 **提交了但没推**
用户指出"GitHub 上看不到 v1.3.55"。查证：`origin/main` 停在 `4c8746e`(v1.3.53)，本地 `main`
**领先 2 个提交**（`0c18e2d` v1.3.54、`f0017f7` v1.3.55）——我提交后从未 `git push`。
已推送 `main` 与 tag `v1.3.45 / v1.3.54 / v1.3.55`。
（另注：origin 侧 tag 有缺口——`v1.3.11/12/13/14/39/40/45` 此前不在远端；v1.3.46–v1.3.53 本地也没有 tag。）

### 1. 结论先行：**「不可复现」的根因就是"输入变了"**（用户记忆正确，实测证实）
`js/train/trainer.js` 与 `js/ui/ui.js` **自 v1.3.23 起就没被改过** —— v1.3.46–55 的全部播种/
复现性修复都只落在 CLI 与 server 上，浏览器这条路从未被碰过。查清了三件事：

**(a) 浏览器按钮根本不走浏览器训练器。** `#btn-train` → `ui.js:777`
`new EventSource(base + '/train?gens=…')` → **Node 服务**。
`trainer.js:57 startTraining` 是**死代码**（全仓 grep 只有定义与导出，**零调用方**）。

**(b) 所以 P3.3 的归因是错的。** 我此前写"`trainer.js` 的 `budgetMs` 分帧预算 ⇒ 浏览器里
点『开始训练』本质上不可复现"——**两处都站不住**：那段代码没被使用；而且 `budgetMs` 只决定
**何时让出帧**（`while (… Date.now()-t0 < budgetMs)`），不改变算出来的东西。
真正的机制是**默认热启动**：

```
index.html:75   <input type="checkbox" id="tr-fresh">   ← 默认不勾
ui.js:718       const fresh = $('tr-fresh') && $('tr-fresh').checked;   → false
                → URL 不带 fresh=1
server:         const fresh = url.searchParams.get('fresh') === '1';    → false
                const seedP = cfg.fresh ? null : loadSeedN();          → 读 js/bundled-champion-3p.js
                1/3 种群由它变异而来（seedChampion）
```
那个冠军文件**每训练一次就变** ⇒ **下一次运行的输入不同**。与 v1.3.50 在 CLI 定位到的
"热启动读自己产物"是**同一根因**，只是这里的"产物"换成了 `js/bundled-champion-3p.js`。

**(c) 实测证明：给定 (seed, 输入冠军)，浏览器默认配置是可复现的。**
同一份输入冠军、同 `?seed=5`、**不传 `fresh=1`**、两次之间**还原输入冠军**：

| | 结果 |
|---|---|
| 20 代 SSE 记录（`gen` 事件逐字节） | **完全一致** |
| 整个 SSE 流唯一差异 | `done` 的 `secs`（墙上时钟 9.5s / 12.1s） |
| 产物 meta | `seed:5, fresh:false, hotstartFrom:"037b2f71597ed6bc", workers:16` |

且 `hotstartFrom` **精确等于输入冠军的权重 sha** ⇒ 记录是有意义的、可核对的。

### 2. 修法：把"可复现输入"记进产物（与 v1.3.50 给 CLI 定的规矩对齐）
此前 server 的 meta 只有 `source/n/gens/games/pop/ts/胜率` ⇒ **从产物上既看不出是不是热启动、
也看不出输入是哪一版冠军**，于是热启动训练**无法被别人复现**。新增：
- `weightsId(params)`：只哈希**权重数组**（沿用 v1.3.36 的教训——整文件 sha 会被 META 的 `ts` 污染）；
- 2P 与 N **两条路径**的 meta 都记 `seed` / `hotstartFrom`（N 另记 `fresh` 与 `workers`）。

**钉成用例**（`np-test` 的 L3）：server 必须存在 `weightsId`、meta 必须记 `hotstartFrom`（两条路径）
与 `seed` —— 否则这条规矩会像 P3.1 一样再次丢失。

### 3. 顺带查实的第二（潜在）成因：浏览器训练器**完全没有播种**
`setRng` 的调用方只有 `tools/train-3p.mjs` / `train-best.mjs` / `train-fast.mjs` /
`server/train-server.mjs` —— `trainer.js`/`ui.js` **从不调用**，所以 `policy.js` 的 `__rng`
保持默认 `Math.random`；`evo.js:665-667,712`（繁殖选父/交叉/随机个体注入）也是裸 `Math.random`。
**今天不影响结果**（那条路是死代码），但谁把它复活谁就会踩到。

### 4. 仍缺（未做，如实列出）
1. **UI 不发 `seed`**（`openRemote` 的 URL 里没有 `&seed=`）⇒ 浏览器所有运行都用 `seed0=0`。
   好处是种子恒定、只差输入；代价是**没法做配对设计**。修它要改 `js/ui/ui.js`（页面产物，
   需要浏览器端视觉复核），未做。
2. **`repro-check` 仍不覆盖 server 的"非 fresh"路径**（它 A/B/C 全用 `fresh=1`）——
   而"非 fresh"正是**浏览器默认配置**。v1.3.52 已把它列作未做的 **F**，本轮仍未做
   （我已用手工两次运行 + SSE 逐行 diff 代替，见 §1(c)）。
3. P3.1 的"worker 数写进 meta"本轮**已在 N 路径做了**（`workers: poolN.workers`）。

**回归**：spec 37/37、np-test **48/48**（L3 扩容）；手工验证见 §1(c)。
工作区两次实验后已还原（冠军与 `index.html` 均未被留下改动）。

## v1.3.55 — 搭好 5 人局评测（P3.5「改考卷」），而**它否掉了「等 5 人局」这个方向**

> 用户要求："搭一下五人局的评测吧。" 评测搭好了，而且它给出的第一个答案就是
> **"5 人局不是那张正确的考卷"** —— 这正好是评测该干的事。

### 1. 新工具 `tools/eval-5p.mjs`
不复用 `tools/eval-3p.mjs`，因为它有两个结构性缺陷（正是 P3.5 说的考卷问题）：
- 它只取**2 个脚本**并循环填充所有对手座位 ⇒ 5 人局会把同一对脚本重复填满 4 个座位；
- 它的对手池里**没有深经济对手**（见 §3）。

新工具：**4 个互不相同**的脚本组成对手场（默认池 7 个：random/defend/antidef/wall/farmer/heavyfire/deepsaver，
`--pool=all` 可扩到 12 个 ⇒ 495 组合）；对手槽位随局**相对座位旋转**（combo 长 4、座位 5、
`gcd(4,5)=1` ⇒ 20 局后 (槽位,座位) 恰好全覆盖，否则"座位偏置"与"脚本强度"混淆）；
附 **`pickRandom` 对照行**（同考卷基线，否则百分比无法解释）；报 1st..5th 名次分布、
各座位 1st 率、**含/不含深经济对手的拆分**、决策时 **ep 分带**、技能分布带费用。

### 2. 核心读数（35 组合 × 20 局 = 700 局，同种子）

| 主体 | 1st 率 |
|---|---|
| `tankline`（只出坦克的三行脚本） | **38.0%** |
| `heavyfire` | 38.4% |
| **神经冠军**（div555 升级版） | **30.7%** |
| `deepsaver`（攒到 5 放大雷） | 15.0% |
| 对照 `pickRandom` | 10.4% |
| `farmer`（只攒不还手） | 1.7% |

**⇒ 5 人局里神经冠军被一个三行脚本打败（30.7% vs 38.0%）。** 3 人局训出来的冠军
**不迁移**到 5 人局，而且不只是"缺深经济"：它 **74.4% 的时间在出ジ**（太消极），
`坦克` 只占 14.2%。同时它的 **`cost>=3` 出手只占 0.5%**。

### 3. 「深经济够不到」这个解释在 5 人局**被直接否掉**
冠军在 5 人局的决策时 ep：`10+ = 27.3%`、`5-9 = 18.6%`（合计 46%），**到达过的最高 ep = 58**。
⇒ **5 人局里钱根本不缺**（3 人局的 max ep ≤2 是"穷"造成的假象）——
约束不是"够不到钱"，而是"**有钱不买**"。这把"货架问题"从猜测变成了有机制读数的结论。

### 4. **干净消融**：同架构、只换弹头 ⇒ 大雷在 5 人局也**不**值它的价
`--payload=<弹头>`：同一条 "攒到买得起 → 打 → 回到攒" 的架构，只换 payload。
`tank` 这一档与 `tankline` 脚本**逐位相同（38.0%）** ⇒ 架构自洽；
`railgun`/`laserEye`（需要电珠，攒钱流永远凑不出）落到 **2.3%**，与 `farmer` 的 1.7% 同档
⇒ **死弹头负对照**，证明这套口径能区分"打了"和"永远打不出来"。

| 弹头 | 费用 | 1st 率 |
|---|---|---|
| **坦克** | 2 | **38.0%** |
| 真正的落雷 | 5 | **18.6%** |
| 狙击枪 | — | 18.6% |
| 双枪射手 | 3 | 16.0% |
| 电磁炮 / 激光眼 | 需珠 | 2.3%（死弹头） |

**⇒ `大雷 − 坦克 = −19.4pt`（5 人局）。** 两者的伤害相同（都是 2 点），
但大雷要多攒 3 回合 ⇒ **伤害/ジ效率只有坦克的 40%**；它的传导/失效/禁用必须补上这 2.5 倍
差距才值得，实测补不上。

**⚠️ 这与千问的实测相反**：他给的是 `大雷−坦克 = −1.7pt(3P) → −3.3pt(4P) → +26.7pt(5P)`，
以及"5 人局坦克掉到随机基线以下（19.2% vs 20%）"。本轮 700 局消融给出**相反符号**，
且坦克是 38.0%（远高于基线 20%）。他那个 5 人局杠杆**没有被我复现**；
`heavyfire` 不能用来复核它（ep≤2 时它只够坦克、**从不攒钱放大雷**，所以 `tankline ≡ heavyfire`
正是 §1-D 记录的那条等价），必须用本节的 save-spend 消融。

**⇒ 结论：「等 5 人局」这个方向被否掉了。** `3~5 ジ 档没有可兑换的价值`是**规则/平衡**性质，
不是考卷性质——它在 3/4/5 人局都成立。

### 5. 顺手挖出两个"没人发现的静默退化"
- **`pickDeepSaver` 被静默删除**：v1.3.27 加入（实测最高 ep=5、ep≥3 占比 47.1%），
  **v1.3.30（N20 地雷 AoE 重写）把它删了，那个 commit 的 CHANGELOG 只字未提**，
  之后 24 个版本没人发现；而 `REVIEW-3P` §1-D 把对手池变动记成"去重（项目卫生）"，恰好掩盖了它。
  后果是考卷里 **"不攒钱"没有任何惩罚来源**（`farmer` 只攒不还手、`heavyfire` 会还不攒）。
  **已恢复**（`js/train/bots.js`，含它自己的设计教训：一买得起就放地雷会让最高 ep 停在 3，
  3 ジ 与 5 ジ 互斥，故选保住大雷）。
- **`wrapBotN` 丢弃脚本自己选的目标**：它无条件用 `pickTargetN` 重算，把返回 `{key,target}`
  的脚本（protomine / prototransfer / focusfire / deepsaver）的瞄准**整个丢掉**。
  **已修**；对现有训练口径**零影响**（`OPP_NAMES` 那 9 个脚本都只返回技能名）。
- 另导出 `pickTarget2N`（此前只导出了 `pickTargetN`，不对称）。

**钉成用例 L6**：`pickDeepSaver` 必须在 `EpirusBots` 里（别再被静默删掉）；
`wrapBotN` 必须保留显式 `target`；行为反证——`ep=5` 时它必须真的打出大雷。

### 6. 已知未解决
- **5 人局仍有残余座位效应**：冠军 P0..P4 = 25.7/36.4/26.4/30.0/35.0%，对照 = 10.7/17.9/10.0/10.0/3.6%
  （各 140 局）。3 人局是平的（45/46/42%），故这是 5 人局特有的；疑似 `rankOf` 平局按 pid 断。
  **未查**。
- `subsidy-diag` 在 n=5 只有 30 局（sd≈8.9pt）⇒ 功效不足，`+10pt` 不显著；主证据用上面的 700 局消融。

**回归**：spec 37/37、np-test **48/48**（新 L6）、3P 基准锚 1st=44.4%（基线 33.3%，座位 45/46/42% 平）。

## v1.3.54 — 深经济"货架问题"从猜测变成**有效证据**；(c) 方案落地但门槛默认关闭

> 承接用户的"尝试做一下 C 和 A"。**A 已完成**（v6 / `FEAT_S=123` / `PACK_VERSION=6`，
> 2P 冠军已是 v6）。本轮做 (c)，结论是 **(c) 的门槛在 3 人局上不成立**，但过程里
> 挖出一个真问题、修掉两处残留通道、并给一条历史结论补上了它一直缺的证据。

### 1. 先做测量，再写机制 —— 结果推翻了 (c) 的前提
新增 `tools/subsidy-diag.mjs`：同一批种子（配对设计）测"**钱到底买不买到东西**"。
对线下 3P 冠军（div555 升级版）：

| 档位 | 夺 1 率 | 深技能（cost>=3）/局 |
|---|---|---|
| 原生 ep=0 | 26.7% | 0.00 |
| 开局白给 ep=6 | **46.7% (+20pt)** | **0.00** |
| 承诺 h=2 + 白给 4 | 36.7% (+10pt) | 0.00 |
| regen=2（永久） | 43.3% (+16.7pt) | 0.00 |
| **正对照：脚本鲸鱼** | — | **3.00**（20 局打出 bigT 60 次） |

正对照是关键：它证明"深技能"这个口径**可被触达**，所以冠军的 0.00 是**策略的选择**，
不是测量死角。⇒ **钱能买到胜率，但买不到贵技能**：冠军变强靠的是更频繁地出便宜技能。
这正是千问说的「**不是探索问题，是货架问题**」——现在它有了**有效证据**（此前只有
"3 人局里只出坦克 96~100%"这类间接观察，而那条的考卷本身可疑）。

**⇒ (c) 的门槛设计因此不成立**：补贴局夺 1 率反映的是**补贴强度**，
不是候选的深经济能力，拿它当门槛只会往选择里注入噪声。故
`SUBSIDY_MIN` **默认 0（关闭）**，行为与改动前一致；`EPIRUS_SUBSIDY_MIN` 可显式启用，
留给有货架的 5 人局（千问实测 大雷−坦克 = **+26.7pt @5P**）重新标定。

### 2. (c) 的机制仍然落地了（5 人局要用），并且**接线有实测证据**
- `js/train/evo.js` `scoreMemberN(..., hGeneIn)`：h 从"每局随机抽的噪声"升为**个体基因**；
  每 3 局 1 局跑承诺口径（`ep < 1+h` 只出ジ）+ 补贴经济；**承诺局一分不进 fit**
  （fit 必须仍代表线上原生强度），只作 h 基因的存活依据 + 终局门槛输入。
- `server/{paralleltrain,train-worker}.mjs`：h 随个体进 worker（漏传 = 基因从未到达适应度函数）。
- `server/train-server.mjs` + `tools/train-3p.mjs`：`hGenes` 种群、**分巢精英**
  （每个 h 值保留它自己承诺局夺 1 率最高的个体 —— 这是 h 参与选择的**唯一**机制）、
  基因随父代继承 + 15% 概率 ±1 突变。
- **接线证据**（不是"没崩"）：真跑一次 server 训练，SSE 的 `gen` 事件带 `hDist`/`commitBest`，
  **30/30 代 `commitBest>0`** ⇒ 承诺局真的在 worker 里跑了；`hDist` 逐代漂移
  （`0,0,0,2,0,0,3,0,0,4` → … → `4,0,3,1,2,2,2,1,4,0`）⇒ 分巢精英真的在保住承诺支系。
- 新增 `evalSubsidyProbe`：判据绑**胜负**（补贴局夺 1 率），不绑"贵技能落地次数"。
  千问的警告：双枪现在真能读 `def.dmg` 了，**任何按落地计分的判据都会选出"乱挥双枪"的冠军**
  （实测强制双枪 = **−27pt**）。

### 3. 挖出一个真问题：`play.js` 的 `affordable` 时序（差点被我写成一条错误更正）
`js/core/play.js:50-53`：`legal = legalActions(...)` 在**调用 chooser 之前**就算好
`affordable`，chooser 返回后又用**那个旧值**把买不起的选择强制改回ジ。
我据此一度判定 `evalEconProbe`（v1.3.18）的"构造上不可满足"是**测量假象**，
**差点写进代码和文档**。实测（新增 `tools/grant-mech-diag.mjs`）**否定了它**：

```
实验1（只在 chooser 内补 ep）: RING 的 affordable = [false,false,false,true,true,true]
                               贵/环技能实际出手 5 次
```

⇒ 只对**每局第 1 回合**（3 个座位各一次）无效，因为改的是**真实 state**，会留到下一回合。
所以那个历史结论**只有首回合是盲的，其余仍然有效**。教训依旧是项目自己那条：
**先验证你依赖的前置条件**。修法（重算同一个 `legal` 数组对象的 `affordable`）
消掉了那一个回合的盲区，让"白给资源"当回合就成立。

**钉成用例 L4**（`np-test`）：断言 (1) chooser 内补 ep 当回合 `affordable` 仍为 false、
次回合起变 true、且真的出现贵技能出手；(2) 引擎通道 `opts.regen` 在 `startTurn` 发 ep，
早于 `legalActions` ⇒ 当回合就可负担。两种解读对历史结论含义相反，必须由用例钉住。

### 4. 修两处"测试跑给 shipped 文件留残留"的通道（v1.3.48 事故的同类）
- `tools/repro-check.mjs` 备份还原了 `js/bundled-champion-3p.js`，却**没还原 `index.html`**：
  `writeBundleMP` 顺带调 `bumpChampionVersion()` 把所有 `?v=` 缓存戳改成新值
  ⇒ **每次跑测试都给一个 shipped 文件留 diff**，而 `git add -A` 会连测试残留一起提交。
  已改为对称备份/还原。
- `.gitignore` 未覆盖 `docs/artifacts/*-out.js`（v1.3.48 给 CLI 训练工具定的非发布落点）
  ⇒ 测试产物照样会被 `git add -A` 收进去。已加忽略规则。
- **钉成用例 L5**：查"残留通道"而不是某个具体文件 —— `.gitignore` 必须覆盖 `-out.js`；
  用了该落点的工具必须有 `EPIRUS_PUBLISH` 门槛；`repro-check` 必须还原 `index.html`。

### 5. 文档完整性
`CHANGELOG` 缺了 **v1.3.50 / v1.3.51 / v1.3.53** 三条（提交有、文档没有），本轮按提交信息补齐。
另：`index.html`/`README.md` 的版本号停在 **v1.3.45**（v1.3.46 之后都是训练/工具链改动，未动规则）。

**回归**：spec 37/37、np-test **47/47**（新 L4/L5）、repro-check OK（A/B/C/D/E 全绿，
跑完工作区零残留）、`train-3p` 同 seed 两次权重 sha 逐位一致（`a7c843d8344a64d6`）。
线下 3P 冠军仍为 div555 无损升级版（未被覆写，已核）。

## v1.3.53 — 按 Claude 的复核：把能自动化的教训全部改写成会失败的用例

Claude 的判断：那些错误编号在增长（"同类第 5、6 次"、"栽了四次"），规矩被写下来但**没被内化**；
而本项目恰好已做对了应对 —— `repro-check` 看门狗、`REPRO` 断言、反证要求。
> **每一次它写"沉淀"或"教训"，真正留下来的那一份都是一个测试，不是一条笔记。
> 笔记会随上下文消失，测试不会。**
⇒ 追问应是"**这条能不能变成一个会失败的用例**"。

新增三条 LESSON→TEST（`tools/np-test.mjs`）：
- **L1** 断言不得是恒真式（空断言 / 近乎恒真，本项目出现过 3 次）；
- **L2** 事件字段名必须与发射端一致（筛错字段 ⇒ 把"没发生"当结论）：钉住 `transfer` 必有
  `to`/`from` 且 `to !== from`、`mine` 伤害 `source` 必须为 null；
- **L3** 反向检查关键约束：`WR_TOL` 不得在引擎内读 env、必须有 `setWrTol`；
  `repro-check` 必须含冷/热启动断言。

**反证即时生效**：L1/L3 第一版**当场就红** —— 红的不是修复没做，而是**我自己的注释撞上了 lint**
（L1 的注释里列举了坏模式；`evo.js` 的注释里写了 env 名）⇒ 证明它们是**活的**断言。
已改写注释（lint 是对的，是注释该改）。

`docs/REVIEW-3P.md` 增 **§3b**：教训→用例对照表 + 明确列出两类**仍无法自动化**的
（① 补丁后未验证改动真的生效；② 自造断言 —— L1 只能抓恒真式，抓不到"断言内容与裁定不符"）。
回归：spec 37/37、np-test 45/45。

## v1.3.52 — WR_TOL 改显式参数 + repro-check 补 D/E 断言（千问建议）

### 1. `WR_TOL` 不再依赖 env（千问指出两条路可能取到不同值）
原实现把 `process.env.EPIRUS_WR_TOL` **读在沙箱内部**，两个问题：
- **CLI 沙箱没有 `process`** ⇒ 走 CLI 永远拿默认 0.03（哪怕你设了环境变量）；
- **server 沙箱可能暴露 `process`** ⇒ 同一常量在两条路径上可能不同。

改法：`evo.js` 里改为 `let WR_TOL = 0.03` + `setWrTol(v)` 显式导出，
**调用方**（server 入口）负责读一次 env 并传入。引擎内不再读 env。

### 2. repro-check 补 **D / E** 断言（把这一轮的坑变成自动判据）
千问的总结：**"不可复现"要先分诊成两种**——
(1) **随机源未播种** vs (2) **输入状态未固定**（如热启动读自己的产物）。
**(2) 对"同 seed 两遍一致"是不可见的**：它把"输入变了"误报成"有随机源"。
我们为找一个**不存在**的随机源排除掉了 4 个假设，还差点怀疑掉 `__seedSandbox` 机制本身。

新增：
- **D 冷启动可复现**：跑两遍，**两遍之间删掉产物**，要求 sha 相同；
- **E 热启动必须被报告**：跑两遍不删，若 sha 不同则必须在 meta 里看到 `hotstartFrom`，
  否则判失败 —— **专防"不可复现"被误当成 bug 再查一轮随机源**。

**实测**（`node tools/repro-check.mjs 40 3 42 8842`）：
```
✔ A 同 seed 同 worker 数：两次权重一致
✔ B 同 seed 跨 worker 数(2 vs 8)：权重一致
✔ C 不同 seed：权重应不同
  PASS D cold start: two runs with the artifact deleted agree
  PASS E hot start is reported in meta
REPRO-CHECK OK
```

### 未做
- **F 覆盖 server 非 `fresh` 路径**（现在只测了 `fresh=1`）；
- E 目前因**热启动默认关闭**而走的是"两遍一致、无需报告"分支；
  严格测 E 的报告路径需要跑 `EPIRUS_HOTSTART=1` 一次（**未做**）。

回归：spec 37/37、np-test 42/42；线下冠军仍为 div555。

## v1.3.51 — 修过期日志文本：打印真实 `OUT_PATH`（原硬编码线下路径，**正是它误导我**）

原代码 `console.log('\n已写入 js/bundled-champion-3p.js')` 是**硬编码**的，
即使实际写到 `docs/artifacts/` 也照样打印线下路径 —— 这正是我一度以为"训练跑覆写了线下冠军"
的原因之一，也让我多查了一轮。现改为打印**真实** `OUT_PATH` + `coldStart` + `seed`。

验证输出：`已写入 docs/artifacts/train-3p-out.js  (coldStart=true, seed=11)`；
线下冠军仍为 div555（`source=upgrade-pack`）。回归：spec 37/37、np-test 42/42。

## v1.3.50 — 修 CLI 不可复现的真正根因（千问定位）：**热启动读自己的产物**

千问定位：根本不是随机源 —— `train-3p` 的**热启动读的是自己的产物路径** `OUT_PATH`。

| 第 1 遍（无产物） | 第 2 遍（产物已存在） | 第 3 遍（先删产物） |
|---|---|---|
| `e5fe6a05…` | `a1208cc0…` | `e5fe6a05…`（与第 1 遍**逐位相同**） |

⇒ 训练是**确定性但有状态的**，而"同 seed 两遍一致"这个断言**永远测不到它**：
它把"**输入变了**"误报成"**有随机源**"。我为找这个**不存在**的随机源正确地排除了 4 个假设
（worker 池 / `vm` 上下文 Math 替换 / `mulberry32`+`setRng` 可取性 / `seedOfGen` 确定性）。

**修法**：
1. `OUT_PATH` 换名（原名 `__OUT` 被我的替换脚本弄成自引用 `const __OUT = … ? __OUT : …`，即 TDZ）；
2. **默认冷启动**；热启动须显式 `EPIRUS_HOTSTART=1`，种子来源独立（`EPIRUS_SEEDPACK`，默认线下冠军）；
3. meta 记录 **`seed` / `coldStart` / `hotstartFrom`** —— 让"从哪个冠军长出来的"成为可复现输入；
4. 日志打印**真实 `OUT_PATH`**（v1.3.51）；
5. **硬保险**：启动快照线下冠军，非 `EPIRUS_PUBLISH=1` 时退出还原（此前已被训练跑静默覆写两次）。

**验证**：同 `EPIRUS_SEED=11` 连跑两遍（不删任何文件）→ 权重 sha 逐位一致 `90c61dcb80f7c29e`；
跑一次后线下冠军 sha 前后一致（guard 生效）。回归：spec 37/37、np-test 42/42。

> ⚠️ **v1.3.49 的结论已被本条取代**：v1.3.49 写的是"CLI 复现性只解决了一半、根因未找到"，
> 那是在**错误的假设空间**里找随机源；真根因是"有状态"，已由本条修掉并加断言。

## v1.3.49 — train-3p 不可复现：排除三个假设（**仍未找到根因**）

### 已用实验排除的假设（都有实测证据，不是推测）
| # | 假设 | 实验 | 结论 |
|---|---|---|---|
| 1 | **worker 池引入顺序依赖** | `train-3p.mjs:98` 是**同步** `T.scoreMemberN(...)`，不用 worker | ✗ 排除 |
| 2 | **`vm.runInNewContext` 不认加载后对 `sb.Math` 的替换** | 先建上下文再 `__seedSandbox`，两次重播种取 `[Math.random(), Math.random()]` → **完全一致**，且与宿主 Math 不同 | ✗ 排除（替换**生效**） |
| 3 | **`T.mulberry32` / `P.setRng` 取不到 ⇒ 那句 `if` 恒 false、`setRng` 静默不执行** | 沙箱实测 `typeof T.mulberry32 === 'function'`、`typeof P.setRng === 'function'` ⇒ 条件为 `true` | ✗ 排除 |

另确认：`seedOfGen(gen, idx, tag)` 是**纯哈希**（`h=17; h=h*31+charCode` 循环），按构造确定性；
`scoreMemberN` 的每局种子 = `seedOfGen(gen, idx, 'n') + g*7919`。

### 未完成的定位（我预算耗尽）
我试图二分"差异在评估层还是 train-3p 外层循环"，但诊断脚本崩在
**我自己没核实的前置条件**上（`T.buildOpps(null, 0.05)` 的签名/所需参数）——
**又是本会话第 12 次"没验证依赖的前置条件"**。

**下一步最省力的做法**（留给下一轮）：
1. 先读 `buildOpps` 的签名与前置条件，再写二分脚本；
2. 或在 `train-3p` 里把**第 1 代之后**的 `pop` 前 4 个参数打印出来，
   两次同 seed 跑对比 —— 若第 1 代就不同，则问题在初始化（`makePolicy`/`buildOpps`），
   若第 1 代相同而后面分叉，则问题在繁殖循环。

### 结论（不变）
**CLI 训练的复现性只解决了一半。在 `train-3p` 通过验证之前，任何走 CLI 的对照数字仍不可信。**
（生产路径 `server/train-server.mjs` 已由 `repro-check.mjs` 验证可复现，那条路是可用的。）

回归：spec 37/37、np-test 42/42。

## v1.3.48 — 任务2 完成：训练工具不再覆写线下冠军；任务1（train-3p 可复现）**未解决**

### 任务 2 ✅ 输出保护（起因是一次真实事故）
一次 60 代/40 代的**测试跑把 `js/bundled-champion*.js` 覆写成测试冠军**，
并被 `git add -A` 提交 ⇒ **线下冠军就这么被换掉了**，而我据此在 REVIEW 文档里写错过
（"3P 冠军 = div555" 与实际文件 `source=server, gens=200` 不符）。

**规则**：`tools/train-{3p,fast,best}.mjs` 默认写 `docs/artifacts/<tool>-out.js`；
**只有显式 `EPIRUS_PUBLISH=1` 才写线下路径**。

**验证**：不加 `EPIRUS_PUBLISH` 跑一次 `train-3p 20 代` ——
线下冠军 sha 前/后均为 `decaf647cf46`（**未被覆写** ✔），产物落在 `docs/artifacts/train-3p-out.js`。

### 任务 1 ❌ `train-3p` 仍不可复现（已排除三个假设，未找到根因）
```
EPIRUS_SEED=11 跑两次 40 代：
  a0049a2f67920ff7  /  9e83020dac732cdf     ← 仍不一致
```
**已排除**：
1. ✗ **worker 池** —— `train-3p.mjs:98` 是**同步** `T.scoreMemberN(...)`，根本不用 worker；
2. ✗ **`seedOfGen(gen, idx, 'n')`** —— `scoreMemberN` 的每局种子由它派生，按构造是确定性的；
3. ✗ **`__seedSandbox` 的调用时机** —— 我假设"vm 上下文不反映加载前的属性替换"，
   把播种移到引擎加载**之后**重测，**仍然不一致** ⇒ 假设被否定。

**未排除的嫌疑**（我预算耗尽，未验证）：
- `vm.runInNewContext(readFileSync(f), sb)` 建出的上下文是否真的共享 `sb.Math` 的**后续替换**
  （server 侧可复现，但 server 的繁殖已改用显式 `breedRng`，所以它可能根本没走 `Math.random`）；
- `T.mulberry32` 是否被 `train-3p` 正确取到（若 `P.setRng` 静默失败，`policy.js` 会退回宿主 `Math.random`）；
- `buildOpps` / `pickTargetN` 内是否还有别的随机源。

**⇒ 结论：CLI 训练的复现性只解决了一半。播种加上了、断言扩面了，但 `train-3p` 本身未通过验证。**
**在它可复现之前，任何走 CLI 的对照数字仍不可信**（这正是千问那条复核的核心）。

### 第四次同类失误
Python 补丁里的中文引号再次导致**补丁未写入**（前三次见 v1.3.41/43/46）。
已改为**补丁里的 JS 文本一律只用 ASCII 引号**。

回归：spec 37/37、np-test 42/42。

## v1.3.45 — N23 激光眼单发即破整个防御族（用户裁定）

### 用户裁定
- **纠正**：**小雷无法失效最基础的防御、反弹和原型制御** —— 与现有实现一致
  （`rules.js` desc 本就写"防御/反弹/原型制御豁免"），**无需改动**。
- **改动**：激光眼是同费的防御类专用反制，但原实现**要求两个**才能破原型制御/全息。
  用户认为"**单个激光眼还打不破原型制御有点拉了**" ⇒
  **单个激光眼即可失效所有防御类技能 + 转移伤害。**

### 实现
- `resolve.js`：去掉 `ta.key !== SK.PROTO && ta.key !== SK.HOLO` 例外，
  只留 `R.GUARD_FAMILY.indexOf(ta.key) >= 0` 一个分支；`laserNoEffect` 路径成为死代码
  （**已 grep 确认全局无其它引用**）。
- `rules.js` desc 同步为"无效化目标**任意**防御类(含原型制御/全息)/转移伤害"。

### 用例与反证
`N23`：单个激光眼打原型制御 → `laserNoEffect` 计数必须为 0。
**反证已验**：恢复例外 ⇒ `✘ got=1 want=0` ✔

**同时清理了我自己写的草率断言**：N23 第一版含
`some(e => e.type==='laserNoEffect' ? false : true)`（**近乎恒真**）与 `ok(true,'')`（**空断言**），
已删除——只保留**可被反证**的那条判据。这是本轮"自造/空断言"问题的第三次出现。

回归：spec 37/37、np-test **41/41**。

## v1.3.44 — 修 v1.3.43 的回归：`BIG_T_EXEMPT` 扩张撞坏 R23'（spec 36/37 → 37/37）

### 回归（我在 v1.3.43 犯的）
我把 `BIG_T_EXEMPT` 从 `[GUARD, REFLECT, JINSHIELD, JI, MINI_T]` 扩成
`R.GUARD_FAMILY.concat([JI, MINI_T])`（即整个防御族豁免 3 回合禁用）。
**直接把 `spec` 从 37/37 打成 36/37** —— 失败的是
`R23' 大雷 vs 原型制御：2电被挡但禁用目标用的原型制御仍生效`（`tests/spec.js:166`）。

### 根因：我把用户的话读宽了
用户说「**防御族可以防住大雷**」——指的是**格挡那 2 点伤害**；
**并没有说**防御族豁免 3 回合禁用。而 **R23' 明确"2 电被挡，但禁用目标用的原型制御仍生效"**。
⇒ 格挡 ≠ 豁免禁用，两者是**正交**的。我已撤回扩张。

更糟的是：**`N22c` 里那条"防御族不进 3 回合禁用"的断言是我自己发明的**，
不是用户规则——**我用一条自造断言去"验证"自己的实现，然后把既有规则撞坏了。**
（已把该断言改为与 R23' 一致：防御族**仍会被禁用**。）

### 修正后
- `BIG_T_EXEMPT` 恢复 `[GUARD, REFLECT, JINSHIELD, JI, MINI_T]`（保留 v1.3.42 加的小雷）；
- `N22c` 断言改为 `cooldown[ARMOR] > 0`（与 R23' 同口径）；
- 反证仍有效：关掉防御豁免 ⇒ 失败 1 条 ✔
- **回归：spec 37/37、np-test 40/40。**

### 本轮教训（第 3 类"看起来对了"）
**"自造断言"和"假通过"是同一枚硬币的两面**：
- 空用例（N22/N22b）——测不到被测代码；
- **自造断言（N22c）**——测了一条**并不存在**的规则，并把真规则撞坏。
⇒ 断言必须**来自裁定原文**，不能来自我对实现的想象；写完后**反证**。

（另：`N22c` 的字符串里我写了 `R23'`，撇号截断 JS 单引号串 → 语法错误。已改。）

## v1.3.43 — N22 大雷效果传导：按用户裁定实现（防御族格挡 / 沿作用目标传导 / 反弹只挡不反击）

### 用户裁定（最终口径）
- **所有防御类理论上都可以挡住大雷**（但都挡不住小雷）；防御者本人不吃那 2 点。
- **大雷属"反弹可以格挡但不触发反弹"**的技能。
- **金刚盾/藤甲的特殊性在于"它们有作用目标"**（金刚盾→判定攻击、藤甲→上火弱）
  ⇒ 使用者本人免伤，但**那个作用目标会吃到传导伤害**。
- **八卦阵照常判定**；**ジ 本回合失效（不给 ep）但不进 3 回合禁用**。
- 受影响集合 = **单次传导**：大雷目标 T 本人 + T 的转移对象 + **本回合攻击 T 的人**（不扩散）。

### 实现（`resolve.js`）
1. **反弹改为"可格挡但不反击"**：去掉大雷伤害的 `pierce: { reflect: true }`
   （那个字段的语义是"反弹整个失效、连格挡都没了"），改用
   **`BIG_T ∉ REFLECTABLE = [GUN, TANK]`** 让反弹**只格挡、反击不成立**。
2. **传导链成员**：
   - 防御族**不失效**（`qIsDef` 判定）——原先无条件 `setVoid`；
   - 防御者**本人不吃伤害**；若其技能**有作用目标**（`snapT[q] != null`）⇒
     **2 点落到那个目标身上**；无目标的自守防御则完全挡住。
3. **大雷伤害补 `pierce: { transfer: true }`**（不可被转移）。
4. **`BIG_T_EXEMPT` 扩为 `R.GUARD_FAMILY.concat([SK.JI, SK.MINI_T])`** ——
   原先只列 `GUARD/REFLECT/JINSHIELD`，**漏了藤甲/八卦阵/原型制御/无极变速/全息**，
   而规则是"失效/禁用的对象为**非防御类**" ⇒ 豁免应覆盖整个防御族。

### spec 用例（全部做了反证）
| 用例 | 内容 | 反证 |
|---|---|---|
| **N22** | 用户原例 a,b,c,d：b/c/d 各受 2；c 的ジ失效不给 ep；b 的转移与 d 的枪禁 3 回合；ジ不禁用 | — |
| **N22b** | 直接目标用反弹 → 格挡那 2 点、且**不被反击** | — |
| **N22c** | **传导链成员**用藤甲 → 本人免伤、2 点落到其作用目标 | 关掉豁免 ⇒ **失败 1 条** ✔ |

### ⚠️ 反证法在本轮抓出两次"假通过"（这是本轮最有价值的产出）
1. **N22/N22b 都测不到新代码**：把防御豁免直接改成 `false`，两条**仍然通过**
   —— 因为 N22 的链条里没有防御者，N22b 测的是**直接目标**的防御（走另一条分支）。
   ⇒ 补了 **N22c** 专门覆"链上成员用防御"，反证才失败。
2. **N22c 自己又漏了一条**：`cooldown[ARMOR] > 0`（藤甲被禁用），
   暴露出 `BIG_T_EXEMPT` 漏了整个防御族 —— 这是**用例替实现抓出来的真 bug**。

**沉淀**：**"写完断言就做反证"这条规矩，本轮两次避免了"看起来测了、其实没测"。**

回归：spec 37/37、np-test **40/40**。

## v1.3.42 — 修大雷结算：豁免小雷（旧代码对照 + 用户裁定）+ **发现并修正一个空用例**

### 规则（用户裁定 + 老代码对照）
用户指出：**大雷可以失效目标当前的非防御类技能**——包括**能量类**技能和**除小雷（优先级更高）外**的特殊技能。
对照老代码 `D:\code\Epirus\EpirusN.py`：
```python
if(player[i].skill == lightning):
    player[num].last_skill = 0
    if(player[num].skill > 2):                 # 非 ジ/防御/反弹
        player[num].no[player[num].skill] = 3  # 禁用 3 回合
    if player[num].last_skill >= power:        # power = 蓄能 的下标
        player[num].no[player[num].last_skill] = 3
```

当前实现**其实已经做了"失效非防御类技能"**：
```js
if (!bothBig && ta && R.GUARD_FAMILY.indexOf(ta.key) < 0) setVoid(state, t, SK.BIG_T);
```
**错误在于缺了「小雷」的豁免**：小雷 = `SK.MINI_T`，**pri 5 高于大雷的 pri 4**
⇒ 大雷结算时小雷**早已放完**，此时再把它作废/禁用是**追溯性的错误**。

### 修法（两处）
1. `BIG_T_EXEMPT` 加 `SK.MINI_T`（不禁用 3 回合）；
2. 失效判定加 `&& ta.key !== SK.MINI_T`。
（`GUARD_FAMILY` 那条本就覆盖防御族，满足"非防御类"口径。）

### ⚠️ 本轮最重要的方法发现：**我写的 N21 第一版是空用例**
第一版让**被大雷打的人自己**用了小雷。结果：小雷(pri5) **先作废大雷本身** ⇒ 大雷压根不结算
⇒ 禁用与否都是 0 ⇒ **用例恒过**。
**我是靠"反证法"发现的**：把豁免去掉、重跑，如果用例**仍然通过**就说明它测不到东西。
```
第一版：带豁免 ✔  /  去掉豁免 ✔     ⇒ 空用例
第二版：带豁免 ✔  /  去掉豁免 ✘ got=4 want=0   ⇒ 有效用例
```
**第二版改法**：让小雷**打第三方**，大雷才不会被作废、才会真的走到"要不要禁用目标技能"这一步；
并加一条 `ok(ban 事件存在)` 作为**"本用例没有空过"的自检**。

### 沉淀（准备长期执行）
**新增/修改的断言一律做一次反证：把被测的修复撤掉，断言必须失败。**
否则就是"写了测试但测的不是那件事"——本轮的 N21 第一版、
以及 v1.3.35 那个"可证明无效的干预却造出 2/3 vs 0/3"都是同一类错误的两种形态。

### 另：本轮 Python 补丁**连续两次因中文引号语法错误而未写入**
（表现为"命令输出正常"但代码没变）。已改为**补丁里的字符串一律只用 ASCII 引号**。

### 仍未裁定
大雷的伤害投递仍是 `pierce:{reflect:true}`（**无 `pierce.transfer`**），
按 `resolve.js:240` **目前会被转移伤害拦下并转给施法者**，与用户所述
"大雷无法被转移（会失效），但大雷伤害会沿转移伤害传导"**不符**，且后者语义在代码中无对应实现。

回归：spec 37/37、np-test **37/37**。

## v1.3.41 — 集火脚本 `pickFocusFire` + 转移伤害的**决定性**结论：当前 meta 下不可用

### 新增 `pickFocusFire`（集火脚本，用于制造 N≥2 这一前置条件）
按用户提醒**特别处理目标设置**：
- 必须**显式返回 `{key,target}`**（只返回技能名会回落到 `pickTargetN`，集火就散了）；
- 目标必须是**存活且非自己**的对手；
- 选「**血量最低**」是刻意的——让**多个集火脚本各自独立计算都能得到同一个目标**（确定性共识），
  否则两个脚本各挑各的，N 永远到不了 2。

### ✅ 前置条件**这次真的成立**（上一轮就是在这里失败的）
```
集火局里 seat0 的每回合承伤分布：N=1: 72.5%  N=2: 17.5%  N=3: 10.0%
N>=2 占比 = 27.5%   ← 转移伤害赚的充要条件（v1.3.40 算术：赚 ⟺ N>=2）
```

### 实测：即使条件成立，转移伤害仍然**完全不可用**
```
对手 = 两个集火脚本，我方 seat0，60 局
  对照(只用枪)   1st = 45.0%   出转移 = 0    转移事件 = 0   转移伤害 = 0
  攒2出转移      1st =  0.0%   出转移 = 159  转移事件 = 59  转移伤害 = 59
```
**`攒2出转移` 拿到 0.0%**（对照 45.0%）。它确实出了 159 次转移、成功转了 59 次、
总转移伤害 **59** ⇒ **平均每次只转 1 点**（因为 N=1 占 72.5%）。

### 根因（这才是关键）
**为了凑够 2 ジ，必须先放弃 2 个回合的进攻；而在被集火时对手每回合打你 2 点，
3 点血撑不过那两回合。** 储蓄的机会成本 > 转移的收益。
⇒ 这与深经济问题**同源**：任何需要"攒到 2+"的动作，都要先付出"两回合不还手"的代价，
而当前 3 点血 / 1 点伤害的节奏下这个代价**付不起**。

### ⚠️ 顺带发现一处**规则与实现不符**（请用户裁定）
用户指出：**大雷无法被转移伤害（会被失效），但大雷的伤害会沿着转移伤害传导出去**。
但当前实现里，大雷的伤害投递是：
```js
{ amt: 2, type: R.DMG.ELECTRIC, source: c, via: SK.BIG_T, pierce: { reflect: true } }
```
**没有 `pierce.transfer`、也没有 `bypassGuards`**，而 `resolve.js:240` 的拦截条件是
`!dmg.bypassGuards && !pierce.transfer && tAct.key === SK.TRANSFER`
⇒ **大雷目前是会被转移伤害拦下并转给施法者的**，与用户所述规则相反。
（"大雷伤害沿转移伤害传导"这条语义我也没有在代码里找到对应实现。）
**本轮不动规则，登记待裁定。**

### 本轮我犯的两个错误（同类第 5、6 次）
1. 集火验证脚本第一版用 `e.from===0` 过滤 transfer 事件——但该事件的字段是
   `{to: 转移方, from: 攻击者}`，**我筛错了字段**，于是把"转移从未发生"当成结论；
2. 强制转移脚本第一版"ep≥1 就开枪" ⇒ **ep 永远到不了 2**、转移永远买不起
   （这是脚本自身的死锁，不是规则问题）。

回归：spec 37/37、np-test 36/36。

## v1.3.40 — 转移伤害诊断：**能赚的充要条件是"单回合承伤 N≥2"**（用户直觉被算术证实）

### 用户的判断（本轮的起点）
> 转移伤害在面对单点伤害的时候是很亏的，只有在碰到高伤害（比如集火或电磁炮）的时候才会赚

### 算术核对（比训练更可靠，这里先给结论）
`转移伤害`：**费用 2 ジ**，效果 = 本回合所受**可转移**伤害转给目标（坦克不可转移）
⇒ 若本回合承伤 **N**，则**省下 N + 转出 N = 净摆幅 2N，代价 2 ジ** ⇒ **每ジ收益 = N**。
对照 `枪`：1 点伤害 / 1 ジ ⇒ **每ジ收益 = 1**。
⇒ **转移伤害赚 ⟺ N ≥ 2**；N=1 时与枪打平（且枪不需要挨打）；**N=0（没被打）时纯亏 2 ジ**。

**用户的直觉完全正确**，且可以精确写成一句话：**它的盈亏平衡点就是"单回合承伤 2 点"。**

### 实测（3 人 60 局/格，座位轮换，强制出转移 vs 对照）
| 环境 | 强制转移 | 对照 | 说明 |
|---|---|---|---|
| 低伤（balanced+aggro） | 45.0% | 45.0% | **完全相同 ⇒ 转移从未出手，空测** |
| 中伤（tankline+reflectmix） | 33.3% | 33.3% | **同上，空测** |
| 重装（heavyfire+breakdef） | **15.0%** | 33.3% | 转移出手了，但 **−18.3pt** |

### ⚠️ 这个实验**没有检验到用户的假设**（设计失败，如实记录）
- 前两格完全相同 ⇒ 2 ジ 的转移伤害**根本买不起**（经济锁 ep≤2）⇒ **那两格是空测，无信息**；
- 第三格虽出手，但**"重装"环境的实际伤害并没有更高**：`电磁炮` 需 **1 电珠 + 2 ジ**，
  经济锁下几乎放不出；`大雷` 要 5 ジ ⇒ **我没能构造出"高伤害对手"**；
- 因此三格的差异**不能归因于 N 的大小**，第三格的 −18.3pt 更可能来自
  **"每回合花 2 ジ 等挨打 ⇒ 变成被动挨打"**，而不是转移本身的收益/亏损。

### 结论
1. **算术层面**：转移伤害赚 ⟺ **N ≥ 2**。用户判断正确。
2. **实战层面**：当前 3 人 meta 里 **N≥2 的唯一常见来源是"集火"**（两个对手同回合都打你）——
   `电磁炮`（单发 2 点）因经济锁基本不可达，**所以转移伤害的收益完全押在"被集火"这个条件事件上**。
3. **可验证的下一步**（本实验没做）：构造**主动集火**的对手脚本（两个对手都优先打同一个目标），
   那才能真正造出 N=2 并检验——**这是"制造考卷"而不是"改规则"**，成本低且可配对。

### 未采纳任何规则改动
本轮纯诊断，**spec 37/37、np-test 36/36 不变**。

## v1.3.39 — 新增 `tools/repro-check.mjs`：可复现性看门狗（千问建议）

### 它做什么
一条命令检查三件事（跑完自动还原冠军文件）：
- **A** 同 seed 连跑两次 → 权重哈希应**相同**
- **B** 同 seed、worker 数 2 vs 8 → 权重哈希应**相同**
- **C** 不同 seed → 权重哈希应**不同**（这条专防"种子其实没生效"的**空验证**）

⚠️ 只比对**权重数组 `a`** 的哈希，**不比对整文件**——冠军 META 里有 `ts: new Date().toISOString()`，
整文件哈希每次必然不同。这正是我早先把"训练不可复现"误判成真问题的原因（v1.3.36）。

### 首次运行结果
```
A1/A2 同 seed 两遍     权重sha=f5735e5637572a7c  ✔
B1/B2 workers=2 vs 8   权重sha=f5735e5637572a7c  ✔
C     不同 seed        权重sha=0786d1f021f60ec9  ✔ 种子确实生效
REPRO-CHECK OK
```

### 写这个工具时连踩三个坑（都记下来，都是"看起来对但没生效"的同类）
1. **Node `spawn` + 管道 stdio 在沙箱里失败**（环境限制：confined 模式禁止 pipe）
   → 改用本会话一直验证可靠的 **`pwsh Start-Process`** 启停；收尸按命令行匹配端口，**不做全局杀进程**。
2. **`curWorkers` 初始化为 `null`**，而第一项的 `workers` 也是 `null`
   ⇒ `run.workers !== curWorkers` 恒为 false ⇒ **`startServer` 从未被调用**（表现为 ECONNREFUSED，
   很容易误判成"服务器起不来"）。
3. **SSE 流在 `done` 之后不关闭** ⇒ `res.text()` 永远等不到结尾、body timeout
   → 改为**增量读 + 见到 `done`/`error` 立即 abort**。

**这三条都是同一类错误**：现象（ECONNREFUSED / timeout）指向 A，真因在 B。
本轮我已在同类问题上栽了 **四次**（`opts2` 笔误、Python 中文引号致补丁未写入、汇总脚本读错文件、
`curWorkers` 哨兵）。**教训：排错时要先验证"我依赖的前置条件真的成立了吗"。**

### 意义
从此"改动是否破坏可复现性"有了**常设自动判据**。
配合 v1.3.36/37 的显式种子，**A/B 实验可以跑同一批种子、用配对差分**——
这是唯一不依赖"噪声到底多大"的做法（v1.3.38 已论证噪声大小我们测不准）。

回归：spec 37/37、np-test 36/36。

## v1.3.38 — 噪声下限复测：**千问的预期没有应验**，但这个比较本身有缺陷

### 做法
worker 分种子修好（v1.3.37）后，同一配置跑 **4 个显式种子**（`gens=200 n=3 pop=16 gpo=8 fresh=1`）。
这次种子是**真起作用**的，结果可复现、可配对。

```
seed=1  0.4514      seed=2  0.5556      seed=3  0.3972      seed=4  0.5083
n=3 极差 = 15.9pt            n=4 极差 = 15.8pt
对照：修复前同配置 n=3 极差 = 7.4pt
```

### 结论一：极差**没有变小，反而翻倍** ⇒ 千问的预期（会明显变小）**没有应验**
（他推测原先的"相关样本"低估了个体差异、压低了噪声；数据显示不是这样。）

### 结论二（更重要）：**这个比较本身不成立，所以不能据此推翻他的判断**
1. **n 不同不能让区间直接对比**：n=4 的极差**天然**大于 n=3（更多样本更容易抽到极端值）
   —— 这本身就是统计假象；
2. **3 个样本算出的"极差"本身极不稳定**，两次 3 样本极差之比没有判别力。
⇒ 严格说：**这次复测对"噪声是否变小"这个问题是"无结论"**，而不是"否证"。

### 结论三：无论噪声是否变小，**它都不小**（≥7pt，本次观测 ~16pt）
⇒ **"规则改动前后对比必须多跑 + 配对"这条判断不是被削弱，而是被强化。**

### 真正的收获：**配对设计现在可行了**
种子已可显式指定且可复现（v1.3.36/37）⇒ A/B 两组可以**跑同一批种子**，
用**配对差分**（每对之内的差）代替"两组均值之差"。配对能把个体差异从误差项里消掉，
**这比"多跑几个种子"有效得多**，也是唯一不依赖"噪声到底多大"的做法。

### 附：一个自己踩的坑（记录）
汇总脚本对所有 4 个文件读出了同一个值（都等于 seed=4 的值）——**脚本有 bug**，
结论改用逐条 echo 的可靠数值。**本轮我已在同类问题上栽过三次**
（`opts2` 笔误、Python 中文引号致补丁未写入、"空验证"）：**命令输出"看起来对"不等于改动生效。**

### 下一步建议
写 `tools/repro-check.mjs`（千问早先建议）：**同 seed 跑两遍 + 跨 worker 数各跑一遍，
只比对权重哈希**，作为"改动是否破坏可复现性"的常设看门狗。
然后即可开始**配对设计**的正式实验（藤甲 4 人局 / 转移伤害）。

回归：spec 37/37、np-test 36/36。

## v1.3.37 — worker 分种子：结果与 worker 数无关 + 个体间独立（千问跨机问题 ①②）

### 千问指出的两个跨机问题
1. **worker 数改变结果**：`num = cpus().length - 1`、`chunk = ceil(pop/num)`，
   而所有 worker **共用同一个 `EPIRUS_SEED0`** ⇒ 每个个体拿到的随机流偏移随 worker 数变化（8 核 ≠ 18 核）。
2. **同种子 worker 是相关样本** ⇒ 系统性低估个体间差异、**放大假信号**。
   （他怀疑 §3 那个"同设置内极差 7.4~7.6pt"里相当一部分来自这里。）

### 修法：按 **(seed0, gen, 个体下标)** 播种
比千问建议的 `SEED0*1000 + workerIndex` **更彻底**——
按 worker 号播种仍与"哪个个体分到哪个 worker"绑定（仍受 worker 数影响）；
而按**个体下标**播种：
- 与"哪个 worker 跑它"无关、**与 worker 数无关**；
- **每个个体拿到独立随机流** ⇒ 同时修掉"相关样本"那条。

实现：`train-worker.mjs` 在 `evalN` 前调用
`__seedSandbox(sb, S0*100003 + (msg.gen+1)*1009 + (m.idx+1))`（`m.idx` 是全局个体下标）。
另加 `EPIRUS_WORKERS` 覆盖 worker 数（**用于验证这条性质**）。

### 验证
```
?seed=42，同一命令、仅改 worker 数：
  workers=2  仅权重sha=f5735e5637572a7c
  workers=8  仅权重sha=f5735e5637572a7c   ← 完全一致
```
（注：本次验证在补丁**真正写入后**重跑——第一次我因 Python 中文引号导致补丁未写入，
"workers=2/8 一致"是**空验证**，已发现并重做。）

### 仍未做（千问列表第 ③ 条）
- `train-server:311` 的 30 分钟上限（在生成循环内，建议改按代数计）；
- `trainer.js:74-76` 的 `budgetMs` 分帧预算 ⇒ **浏览器里点"开始训练"本质上不可复现**。

### 下一步（千问的预期，值得直接检验）
**给每个 worker 不同种子后重测噪声下限** —— 他预期那个"同设置内极差 7.4~7.6pt"会**明显变小**。
若确实变小，则 v1.3.34/v1.3.35 里基于"方差很大"的判断需要**重新校准（可能偏悲观）**。

回归：spec 37/37、np-test 36/36。

## v1.3.36 — 训练可复现：千问定位到两处 Node 作用域的随机/时间源（**我的假阳性也被证实**）

### 最终结果
```
两次 ?seed=42：整文件sha 不同（fe73de50… / 5a01db5f…），
              仅权重sha **相同**（cd32f2407ab7244c）
```
⚠️ **我上一轮"训练不可复现"的结论是假阳性**：我哈希的是**整个文件**，
而 `writeBundleMP` 把 `ts: new Date().toISOString()` 写进 META ⇒ 每次必然不同、与权重无关。
**千问预判了这一点并提醒只哈希 `a` 数组 —— 他是对的。**

### 真正的根因（两处，都在 **Node 作用域**，`__seedSandbox` 够不到）
1. **`runTrainN` 的繁殖段**用裸 `Math.random()`（3 处）：选父代、选母代、决定是否 crossover。
   ⇒ 我在 vm 沙箱里修好的 5 处（`evo.js` 4 + `bots.js` 1）**全都生效了**，
   **唯独这三处从未被播种**。已改 `breedRng = T.mulberry32((SEED0||1)*100003 + gen)`。
2. **`runTrain`(2P) 的 `seedBase = (r+1)*7919 + (Date.now()%1000)`**：
   决定"选哪个冠军"的评测种子来自**墙上时钟** ⇒ 即使 ① 修好，2P 也永远不可复现。
   已改 `cfg.seed0*100003 + (r+1)*7919`（并把 `seed0` 补进 2P 调用点）。

### 千问排除的嫌疑（记录，避免重查）
- **worker 聚合确定性 ✓**：`paralleltrain.mjs` 的 `evalAll` 用 `by[r.idx]=r` 回填、按 `i` 顺序写回，
  `Promise.all` 的完成顺序不参与决策。
- **评估层与引擎干净 ✓**：`scoreMemberN` 同参数两次调用逐位一致
  ⇒ 地雷 AoE 重写没有引入不确定性。

### 新增防回归断言（千问建议的"硬规矩"）
`np-test.mjs` 加 **REPRO 用例**：扫描 `server/*.mjs`，禁止裸 `Math.random(` / `Date.now(`
（白名单：`mulberry32` 兜底、`const t0 = Date.now()` 纯计时、超时、时间戳、cache token、注释）。
**本轮它先抓出 3 条误报（2 计时 + 1 我自己的注释），修正后已能真正把关。**

### 仍未处理（千问列的"跨机不可复现"三条）
1. **worker 数改变结果**：所有 worker 共用同一个 `EPIRUS_SEED0`，个体的随机流偏移随
   `cpus().length-1` 变化（8 核 ≠ 18 核）。建议 `seed = SEED0*1000 + workerIndex` 并把 worker 数写进 meta。
2. **同种子 worker 是相关样本** ⇒ 系统性低估个体间差异、**放大假信号**。
   v1.3.35 §3 那个"同设置内极差 7.4~7.6pt"可能主要来自这里，**给每个 worker 不同种子后应重测**。
3. **两处墙上时钟**：`train-server:311` 的 30 分钟上限（建议改按代数计）；
   `trainer.js:74-76` 的 `budgetMs` 分帧预算 ⇒ **浏览器里点"开始训练"本质上不可复现**。

回归：spec 37/37、np-test **36/36**（新增 REPRO 用例）。

## v1.3.35 — 火弱持续性：训练侧模拟（3×200）→ **无可测效果**，且意外成为噪声下限标定

### 做法（**shipped 规则一字未改**）
`resolve.js` 加训练侧开关 `EPIRUS_FIREWEAK_PERSIST`（默认关，浏览器里 `process` 不存在故恒 false）：
- 关（现状）：`Next` 在 `startTurn` 被消费成 `Now` 并清空 ⇒ 火弱只活一个回合；
- 开：`Next` **不清除**（每回合重新武装），直到被火焰伤害真正兑现时才清除。
按用户建议用 **3×200 代**（而非 1×600）。

### 结果一：胜率无可测效果
| | firstRate（3 次） | 均值 |
|---|---|---|
| 基线 | 0.463 / 0.389 / 0.458 | **0.4366** |
| 持续 | 0.393 / 0.469 / 0.439 | **0.4338** |
差值 **−0.3pt**，而同设置内极差就有 7.4~7.6pt。

### 结果二（更重要）：**藤甲在全部 6 个冠军里都是 0.0%**
⇒ **这个开关是"可证明的 no-op"**：没人出藤甲 → 没人被挂 `fireWeakNext` → 开关永远不会
被读到有效分支。**它不可能改变任何行为。**

### 结果三：正因为它不可能有效果，差异就是**纯噪声**——这是罕见的标定机会
| | 最高 ep | 贵技能出手 |
|---|---|---|
| 基线 3 个种子 | 1 / 1 / 1 | 0% / 0% / 0% |
| 持续 3 个种子 | 1 / **2** / **2** | 0% / **33.3%** / **23.8%** |

**一个可证明无效的干预，让"2/3 种子达到 maxEp=2 且 24~33% 贵技能" vs 基线"0/3"** ——
如果我们不知道它是 no-op，**这看起来完全像一次成功的规则改动**。

⇒ **这是对 v1.3.34 那条方法论最直接的实证**：在 n=3 × 200 代下，
**噪声足以制造出"2/3 vs 0/3"量级的假信号**。任何规则改动的前后对比都必须：
① 更多种子；② 更强的是**配对设计**（同一批种子跨条件配对）——
而当前 `train-server` 的种子来自时间基，**两次运行无法配对**，这是工具链的真实缺口。

### 结论
- **火弱持续性暂不采纳**：既无胜率收益，也因为**藤甲根本没人用**而谈不上提升其强度。
- **真正卡住藤甲的不是火弱的时长，是它被免费 `反弹` 严格支配**（v1.3.10 已证：
  强制把藤甲换成反弹，avg 97.6% → 98.3%）。**先解决支配关系，火弱时长才有意义。**
- 开关与存档保留：`docs/artifacts/fw-base-s{1,2,3}.bak` / `fw-persist-s{1,2,3}.bak`。

回归：spec 37/37、np-test 35/35。

## v1.3.34 — WR_TOL 多跑对照（各 3 次）：**0.03 胜出，我之前的收紧是错的**

### 方法修正
v1.3.33 我用"每种设置跑一次"对比 `WR_TOL`，结论不可用（跑间方差 7.6pt > 待测效应）。
本轮按正确方法：**每种设置 3 次独立训练**（每次种子由服务器时间基产生），
主要看**确定性指标**（技能分布 / ep 分布），兼看 server 报的 firstRate。

### 结果（600 代 / pop16 / gpo8 / fresh × 3）
| | WR_TOL = 0.01 | WR_TOL = 0.03 |
|---|---|---|
| server firstRate | 0.385 / 0.386 / 0.471 → **均值 0.414** | 0.544 / 0.465 / 0.618 → **均值 0.542** |
| 有效技能数 | 2.00 / 2.00 / 2.00 → **均值 2.00** | 2.00 / 3.17 / 2.17 → **均值 2.45** |
| 技能种类 | 2 / 2 / 2 | 2 / 5 / 4 |
| 最高 ep | 1 / 1 / 1 | 1 / 1 / **2** |
| 贵技能出手 | 0% / 0% / 0% | 0% / 0% / **30.7%** |

**三个种子方向一致**（0.03 的最小 firstRate 0.465 ≈ 0.01 的最大 0.471），
且 **0.03 在胜率上高 12.8pt、多样性也更好**，唯一次出现"30.7% 贵技能"的也是它。

### 结论：v1.3.12 把 WR_TOL 从 0.03 收紧到 0.01 是**错误决定**
当时的依据是**单次 3P 头对头 17:62（n=24）**——那在 24 局下就是噪声。
**已把默认值改回 0.03**（仍可 `EPIRUS_WR_TOL` 覆盖）。

### 方法论沉淀（这是本轮真正的收获）
**凡涉及训练随机性的对比，单跑一次都不足以归因**：
- 本轮：同一设置 3 次的 firstRate 极差可达 **0.086**（0.544 vs 0.618）；
- 上一轮：两种设置单跑的差是 0.076 —— **比同设置的极差还小**。
⇒ 我之前若干"改前 vs 改后"的单跑对比（新规则重训、多目标择优前后）**结论都要降级**。
**唯一一直可靠的是确定性指标**（技能分布 / ep 分布 / 高 n 的头对头）——而我做决策时
恰好主要用的就是这些，算是没有系统性走偏。

回归：spec 37/37、np-test 35/35。

## v1.3.33 — WR_TOL 单变量对照：**单跑一次分不出差异，方差盖过效应**

### 做法
把 `WR_TOL` 改为可由 `EPIRUS_WR_TOL` 覆盖，同一批参数（1500 代 / pop16 / gpo8 / fresh）
各跑一次：**A = 0.01**、**B = 0.03**（其余完全不动）。

### 结果
| 指标 | A (0.01) | B (0.03) |
|---|---|---|
| 容差带大小 | band=3 | band=3 |
| pickedWr | 0.540 | 0.464 |
| divNorm / distinct | 0.312 / 4 | 0.241 / 6 |
| probeSc | 0 | **0.75** |
| 最大 ep | **2** | 1 |
| 贵技能出手 | **15.9%** | 0.0% |

头对头（24 局/方向，座位轮换）：
```
B×2 vs A×1 → B 68.4%
B×1 vs A×2 → B 84.2%
```
⇒ **B 头对头明显更强**，但它更浅（max ep 1、贵技能 0%）；A 更深但更窄（4 种）。

### 结论：**我原来的假设被否定，而且这个实验设计本身不成立**
- 我推测"收紧 WR_TOL(0.03→0.01) 把发散度压回去了"——**容差带大小两次都是 3，没有变化**，
  假设不成立。
- 更要紧的是：**每种设置只跑了一次，而两次的 `pickedWr` 差了 7.6pt**（0.540 vs 0.464）。
  **同一设置的跑间方差本身就大于我要测的效应** ⇒ 单跑对比无法归因。
  **要判定 WR_TOL 的影响，必须每种设置跑 ≥3 个种子、比较均值与离散度**（这正是
  `train-best.mjs` 早就支持的 `seeds` 参数的用途，我这次没用它，是方法错误）。

### 处置
- 冠军恢复为 **div555**（此前基于"经济深度 + 多样性"两个确定性指标的决定不变；
  本轮的 A/B 都不足以推翻它，因为都无法与 div555 做同口径比较）。
- A/B 两版存档：`docs/artifacts/wt-a.bak`（0.01）、`docs/artifacts/wt-b.bak`（0.03），可复现。
- **转移伤害的调研本轮未做**（预算耗尽）。

回归：spec 37/37、np-test 35/35。

## v1.3.32 — 补齐 N20 用例（35/35）+ 用新规则重训一次 AI 的实测结论

### 1. 补齐两条欠的 spec 用例（**未改任何规则**）
| 用例 | 结果 |
|---|---|
| **N20d 火弱逐目标**：地雷 AoE 只对挂了藤甲 debuff 的那一个 +1（b 吃 2 点，其他人 1 点） | ✔ |
| **N20e 狙击枪豁免只针对那一次攻击**：狙击打持雷者不触发、雷仍装着、无任何雷伤害事件 | ✔ |
- N20e 初版我把狙击伤害写成固定 1 点而失败——狙击枪有「**无影响时判定爆头**」规则，
  点数随局面变化。已改为**只锁豁免不变量**（雷仍 armed / 无雷事件 / 旁人未波及），不锁具体点数。
- 回归：spec **37/37**、np-test **35/35**。

### 2. 用新地雷规则重训 3 人冠军（1500 代）
```
[multiObj] band=2 pickedWr=0.453 divNorm=0.240 distinct=5   probeSc=0.25
```
**`probeSc` 从恒为 0 变成 0.25** —— 深经济探针**第一次变得可满足**（此前 v1.3.18 判定它
"构造上不可满足"：只有大雷会造成 ≥3 ジ 伤害；现在地雷 AoE 也进了这一档）。

**但被选中的冠军仍然很窄**：
| | 本轮重训 | 盘上保留的 div555 |
|---|---|---|
| 最大 ep | **1** | **5** |
| 贵技能出手 | **0.0%** | **11.8%** |
| 技能种类 | **5** | **10** |
| 分布 | ジ48.8 / 枪48.8 | ジ40.7 / 枪12.9 / 坦克11.8 / 反弹11.1 / 八卦阵11.0 / 防御10.0 |

⇒ **新规则没有让"重训"变好**。处置：冠军恢复为 div555；本版存
`docs/artifacts/champion-3p-n20-retrain.bak`。

### 3. 一条值得记录的推测（未验证）
div555 出自 **`WR_TOL = 0.03`** 时期（容差带宽 → 选出更发散的候选）；
后来为了修 3P 头对头问题把 `WR_TOL` **收紧到 0.01**，容差带变窄 → **选出的冠军又变窄了**
（本轮 `band=2`）。**即：修头对头的那个改动，可能同时把发散度压回去了。**
下次重训应做一次 `WR_TOL` 单变量对照（0.01 vs 0.03）来验证。

## v1.3.31 — 修 v1.3.19 引入的双枪 `via` 回归（N20b 关闭）+ N20 全绿

### 根因：我在 v1.3.19 做「双枪走数据表」时**误改了 `via`**
```js
if (key === SK.DUAL_GUN) return { amt: ..., pierce: ..., via: SK.DUAL_GUN };  // ← 错
```
`MINE_TRIGGER = [GUN, SWORD, TANK, DRAIN, RAILGUN, BIG_T, LASER_EYE, CANNON]` **不含 `dualGun`**，
而 `REFLECTABLE` 同样按枪口径查表。所以 `via: SK.DUAL_GUN` 导致：
- **双枪第一枪不触发地雷**（第二枪仍是旧的 `via: SK.GUN`，所以只有第二枪触发）；
- **第一枪也不被反弹**（同一张表）。
注释里我当时明明写着"via 仍按枪口径保留可被反弹/地雷语义"，**实现却改了**——是自查不彻底。

**症状**（N20b 事件流）：
```
damage to=0 via=dualGun source=3    ← a 中枪，但 via 不在 MINE_TRIGGER
damage to=1 via=gun     source=3
mine from=1 kind=direct              ← 只有 b 的雷直接触发
mine from=[0,2] kind=indirect        ← a 反被算作"间接"
```

### 修法
`via` 一律用 `SK.GUN`（两枪都按枪口径），**数据表只供 `amt`/`type`/`pierce` 数值**。
这正是 v1.3.19 的原始意图，只是当时实现写错了。

### 结果
```
N20a 地雷直接触发（4 人，间接触发合并一波）      ✔
N20b 直接触发无上限（双枪打 a,b → 共 3 波）      ✔
N20c 地雷伤害无来源 source=null 不被铁索共享     ✔
```
回归：spec **37/37**、np-test **33/33**。

### 顺带结论
**`MINE_TRIGGER` / `REFLECTABLE` 这类"按 `via` 查表"的机制，与"把技能改成走数据表"是两件事。**
以后改数据表只动数值字段，**`via` 是语义键，不能顺手改**。这条已写进代码注释。

## v1.3.30 — N20 地雷 AoE 重写（用户裁定）+ 3 条 N≥3 spec 用例

### 规则偏差（用户指出，确认）
`mineChain` 一直是 2 人时代口径——**只让"攻击者"受 1 点火伤**；`rules.js` 的 desc 同口径。
但 2 人局里「全场其他角色」与「攻击者」**恰好是同一人** ⇒ **两种读法在 2 人下无法区分**，
multi 框架加入时没回头核对 ⇒ 该偏差**只在 N≥3 显形**。

### 正确规则（用户裁定，例子核对过）
- **直接触发**（X 被非狙击枪攻击）：除 X 外**所有存活角色**各受 1 火伤，**无次数上限**。
- **间接触发**（挨了地雷伤害且自己装着雷）：所有间接触发者**合并成一波**，
  打**除这些间接触发者之外**的所有存活角色；间接触发只发生一次。
- **火弱**逐目标计算；**地雷伤害无来源**（`source=null`）→ 不被铁索共享 / 不被转移 / 不被大雷传导。

### 实现（`resolve.js`）
- `mineChain` → `mineHit` + `mineResolveAll`；
- 伤害阶段**只记 `_minePending`**，在 `resolveActions` 末尾统一结算——为了做到
  **「直接优先」**：d 双枪同时打 a、b 时，若边打边结，a 的雷波会把 b 的雷**提前**当成
  间接消耗掉，b 的直接触发就永远不发生（实测 b 只掉 2 血，应为 3）。
- `rules.js` 的 desc 同步改为 AoE 口径。

### spec 用例（N≥3，2 人用例天然测不出）
| 用例 | 结果 |
|---|---|
| N20a 4 人 a/b/c 装雷、d 打 a → 除 a 外各 1；b/c 间接触发合并一波打 a/d | ✔ |
| N20c 地雷伤害 `source=null` → 不被铁索共享 | ✔ |
| **N20b d 双枪打 a,b → a/b 各直接一波 + c 间接触发合并一波（共 3 波）** | **✘ 待查** |

**N20b 的现象**：a 只掉 2 血（应 3）。**我的假设是双枪的 `target2` 没有生效**（即 b 没被
第二枪打中，于是 b 没有"直接"记录、不发直接波），属**测试接线**问题而非雷逻辑问题——
但**未证实**。测试已标 `[待查]`，保持可见而不是隐藏。

回归：spec 37/37、np-test **32/33**（含 1 条待查）。

## v1.3.29 — 地雷触发改为「近 5 回合架势频率 + 对手当前 ジ 数」（用户方案）

### 用户纠正（对的）
上一版用"最近 3 回合 ≥2 次架势"**不符合实际**：**原型制御要 1 ジ，不是免费技能**，
连续摆架势本身罕见，阈值过高等于没触发。改为：窗口 **5 回合**，并把**对手当前 ジ 数**纳入判据。

### 实测三种对手环境（40 局 × 座位轮换）
| 变体 | 常态 | 架势型 | 防守型 | 大雷 | 最高ep / ep≥3 |
|---|---|---|---|---|---|
| **仅 5 回合 + `foeEp>=1`（采用）** | 地雷 2.4% | 地雷 **0.0%** | 地雷 1.0% | **11.2 / 15.3 / 6.9%** | 5 / 40~49% |
| 补 `最近2回合刚摆过` 放宽（试后撤回） | 地雷 12.6% | 地雷 23.7% | 地雷 14.8% | **3.6 / 0 / 3.6%** | 4~5 / 25~33% |

### 两个必须记录的结论
1. **「结合ジ数量」是双刃的**：它正确过滤了"摆不起"的对手，但**恰好也排除了最爱摆架势的
   对手**——`guardspam`/`protowall` 把ジ 全花在架势上，`ep` 常为 0 ⇒ 地雷对该类对手 **0.0%**。
2. **地雷与大雷在本脚本里互斥**：地雷 3 ジ、大雷 5 ジ，**花了 3 就再也到不了 5**。
   放宽地雷 ⇒ 大雷从 11~15% 掉到 0~3.6%；收紧地雷 ⇒ 地雷掉到 0~2.4%。
   **这是脚本内部的设计冲突，不是阈值问题。**
   按 P1 的目的（深经济对手，让 3~5 ジ 档有独占价值）**选择保住大雷**，地雷只作稀有补充；
   若要让地雷当主武器，应**另写一个"地雷流"脚本**，而不是在这一个里兼顾。

回归：spec 37/37、np-test 30/30。

## v1.3.28 — 地雷改用「历史频率」触发（方案 a）；`lastSkill` 滞后问题的确认

### 问题（用户提问引出）
`deepsaver` 原先的地雷条件是 `state.p[tl].lastSkill === SK.PROTO`。
- **不是学习**，是硬编码的单字段等值比较。
- `lastSkill` 本身是好的（真实对局采样 2976 个决策，**非 null 占比 69.6%**；
  我最初的"恒为 null"测试是自己手搓 `attemptAction` 绕过了正常记录路径，测试错了）。
- **但那个分支从未触发**：120 次检查 **0 次命中**，deepsaver 实际分布 `ジ83.3 / 大雷16.7`，**地雷是死代码**。
- 根因：**拿"上一回合出的招"当"这一回合的意图"**。引擎在决策时刻不提供任何对手意图信息
  （所有人同时声明），所以滞后字段做不了"应对"。

### 改法（用户建议 a）：用**可观测的历史频率**，不假装预知
仿 `bots.js` 既有的 `__mem` 模式（模块级 + `round===1` 重置），新增 `__dsMem`：
统计**领先者最近 3 回合里出架势的次数**，`>=2` 才放地雷。脚本记参考数据不增加训练压力。

### 实测（40 局 × 座位轮换）
| 对手环境 | 分布 |
|---|---|
| 常态（random/balanced） | ジ59.6 / 防御25.9 / **地雷 9.8** / 大雷 4.7 |
| **架势型（guardspam/protowall）** | ジ76.3 / **地雷 23.7** / 大雷 **0.0** |
⇒ 频率触发**生效**，且对架势型加码（9.8% → 23.7%）。

### ⚠️ 但暴露出一个新矛盾（未解决）
在**架势型对局**里地雷占了 23.7%、**大雷掉到 0.0%**——因为地雷要 3 ジ、大雷要 5 ジ，
一旦开始放地雷就再也攒不到 5。**即：这个修法恰好在我们最需要深经济的那类对局里
牺牲了深经济。** 两个候选方向（未做）：
- 要求 `ep >= 5 才优先大雷`（现已如此）+ **`ep < 5` 时只有架势分满分才放地雷**；
- 或把地雷改为"对手连续 3 回合架势"才放（更严），保留大雷主线。

### 附带确认：`lastSkill` 滞后是**所有脚本反应逻辑的天花板**
`bots.js` 的 `stanceOf` / `reactDefense` / `oppInDefense` 读的都是同一个滞后字段，
影响 `pickBalanced` / `pickAdaptive` / `pickBreakDef` 等核心脚本。
⇒ 它们的"对手在摆架势"分支很可能**长期在空转**（与 `pickMultiMed` 当初实测
"反应式穿透 55.8% < 直接穿透 62.5%" 的现象一致）。**影响面待评估。**

回归：spec 37/37、np-test 30/30。

## v1.3.27 — P1：深经济对手 `deepsaver`（先写脚本 → 实测它真的会攒）

### 做法（按我自己提的顺序：先验证脚本会攒，不先接进池子）
新增 `pickDeepSaver`（"攒钱流"）：能一击必杀 → 大雷；够得着大雷 → 大雷；血少 → 守；否则**只出 ジ 攒钱**。
并给 `tools/bot-audit.mjs` **加了两列**：`最高ep` / `ep>=3占比`——这就是"是否真会攒"的判据。

### ⚠️ 第一次设计**没通过验证**（正是我预警的"设计 vs 实际"落差）
初版无条件放地雷(3 ジ) → **一买得起就花掉，最高 ep 只到 3**，大雷那一支永不触发：
```
deepsaver  1st=3%   最高ep=3   ep>=3占比 15.9%   ジ53 / 防御31 / 地雷16
```
修法：地雷只在"对手正在用原型制御"时才放（它唯一的独占价值），否则继续攒到大雷。改后：
```
deepsaver  1st=27%  最高ep=5   ep>=3占比 47.1%   ジ70 / 防御18 / 真正的落雷12
```
⇒ **1st 从 3% → 27%**，与 `breakdef`(30%) 同一档，且它是**会还手**的攒钱型。

### 同时纠正千问一处判断（有数据）
千问说"对手池里现在没有任何一个对手在 3+ ジ 档"——**不准确**。加了 ep 列后可见：
| 对手 | 最高ep | ep≥3占比 | 1st | 主线 |
|---|---|---|---|---|
| **breakdef** | **5** | **43.4%** | 30% | ジ86 / 大雷14 |
| **farmer** | **10** | **54.8%** | 0% | ジ68 / 蓄能32 |
| heavyfire | 2 | 0.0% | 37% | ジ70 / 坦克30 |
| deepsaver（新） | **5** | **47.1%** | 27% | ジ70 / 防御18 / 大雷12 |

⇒ 池子里**本来就有两个会攒的**（`breakdef`/`farmer`），只是 `farmer` 完全不还手（1st 0%）；
缺的是**「会攒 + 会还手」**那一个——`deepsaver` 补的正是这个位置。
（`heavyfire` 最高 ep=2、ep≥3 占比 0.0%，**再次确认它不是深经济探针**。）

### 未完成的部分
- **"考卷"还没改**：`deepsaver` 已进 `BOT_NAMES`（评估池），但**评估口径**仍是对 45 个对手对取平均，
  深经济对手只占少数 ⇒ "不攒钱"依然几乎不受罚。这正是千问说的 §3.5「门槛局须同时满足
  补贴 + 架势/控制型对手」要解决的问题。
- **架势/控制型对手未新增**：池中已有 `guardspam`/`protowall`/`reflecttank`/`reflectmix`，
  但都是 2 人时代逻辑；是否要按 N 意识重写、以及是否要给它们更高权重，待定。

回归：spec 37/37、np-test 30/30。

## v1.3.26 — 冠军换成 div555（v5→v6 无损升级）+ 工具回归确认

### 1. P0 后其余工具回归（用户要求的第 1 步）
| 工具 | 结果 |
|---|---|
| `seat-diag` | ✓ 坏行修好（`未使用技能数 = 6 种被用过`） |
| `bot-audit` | ✓ 退出码 0；**仍显示 tankline ≡ heavyfire**（15.0 回合/1.86/ジ69+坦克31 完全一致）→ 印证千问"heavyfire 不是深经济探针" |
| `skill-report` | ✓ 正常写出 HTML；判定 `枪 −24.1pt`、**`藤甲 −75.9pt`**（被免费反弹支配，代价极大）、没学会的强招 `激光剑 +5.6 / 真正的落雷 +2.8 / 电磁炮 +0.9` |

### 2. 冠军换成 div555（用户要求的第 2 步）
前提问题：**游戏侧 `checkPack` 严格拒绝 v5 包**，不能直接复制存档。
解法：**v5→v6 无损升级**（新增 `tools/upgrade-pack.mjs`）——
v6 相对 v5 只多一维状态特征，只要在 `W1` 每一行的对应位置**插入 0 权重**，
该维贡献恒为 0 ⇒ 网络行为与 v5 **完全等价**，但包形状变成 v6、游戏侧接受。

实测转换：`新增特征在状态特征中的下标 = 18`；输出 `v=6 f=123 len=3337 checkPack ok`；
**等价性自检 `max|Δvalue| = 0.000e+0`（完全等价）**。

### 3. 换冠军后的实测
```
[A 经济画像]  决策=3197  最大ep=5  贵技能(ep>=2)出手 11.8%  用到 10 种
  ep 累积: ep0:51% ep1:85% ep2:98% ep3:99% ep4:100%
  分布: ジ40.7 枪12.9 坦克11.8 反弹11.1 八卦阵11.0 防御10.0 蓄能1.9 藤甲0.4 贴贴0.2 净化0.0
```
对比被替换掉的 v6 窄版：`最大 ep 1 / 贵技能 0.0% / 6 种`。**经济深度与多样性都是净提升。**

### 4. head-to-head（诚实标注：样本太小，不足以定论）
```
A(div555)×2 vs B(v6窄)×1 → A 77.3%   （等强度期望 66.7%）
A(div555)×1 vs B(v6窄)×2 → A 18.2%   （等强度期望 33.3%）
```
n=24/方向，噪声约 ±9pt。**两个方向互相矛盾**（2 座位侧偏高、1 座位侧偏低），
且换位前的同口径测试也呈现同样形态 ⇒ **这个 2v1 口径本身可能不适合判强弱**
（两座位的一方本就占优，偏离期望的符号在两个方向上不一致）。
**结论：本次换冠军的依据是"经济深度 + 多样性"这两个确定性指标，不是头对头。**
若要严格判强弱，需要 n≥200 且改用「同座位 1v1v1 轮换」口径。

处置：旧窄版存 `docs/artifacts/champion-3p-v6-narrow.bak`。
回归：spec 37/37、np-test 30/30。

## v1.3.25 — P0：存档版本兼容（形状自适应）→ A/B 证据链恢复

### 问题（千问指出，我确认）
升 `PACK_VERSION 5→6` 后，**7 个实验存档全变砖**：
`checkPack` 要求 v=6/f=123/len=3337，而存档是 v=5/f=122/len=3313 → 全部拒绝，
`econ-eval ③ head-to-head` 直接抛"冠军 B 解包失败"。
⇒ **五次失败实验的结论无法复现、无法对照**，而且以后每次改特征都会重演一次。
根因：`FEAT_S`/`HID`/`PACK_VERSION` 是**模块全局**，一个进程只能存在一种网络形状。

### 修法（千问方案）：形状从 `params.length` 反推
`paramCount = HID*FEAT_N + HID + HID + 1` ⇒ `FEAT_N = (len - 2*HID - 1)/HID`
- v5: `(3313-49)/24 = 136` ⇒ `FEAT_S = 136 - FEAT_A(14) = 122`
- v6: `(3337-49)/24 = 137` ⇒ `FEAT_S = 123`

具体改动（`js/train/policy.js`）：
1. 原 `features` 改名 `featuresV6`；新增 `features(state,pid,featS)` 包装——
   当 `featS < 123` 时**去掉"自己跨得过环启动线"那一维**。
   **下标用运行时探测而非硬编码**：把 `me.ringStreak` 从 0 改到 3，
   唯一"由 1 变 0"的那一维就是它（`ringSelfIdx()`）。
2. 新增 `shapeOf(params)`：按长度反推 `{featS, featA, featN, legacy}`；
   `value()`/`forward()` 改用它（**v6 路径行为完全不变**，仅在长度不匹配时走 legacy）。
3. `unpack(o, allowLegacy)`：`allowLegacy=true` 时按包内长度重建；
   **游戏侧仍调用严格版 `unpack(o)`，checkPack 一字未改**。
4. `tools/econ-eval.mjs` 的两处 unpack 改传 `allowLegacy=true`。
5. `tools/seat-diag.mjs:78` 坏行修复（残留 `(R.ACT_KEYS ? 0 : 0) +` 拼出"06 种"）。

### 验证
```
champion-3p-div555.bak    v=5 f=122 len=3313  严格=✗  工具侧=✓  反推 featS=122 featN=136
champion-3p-curriculum.bak v=5 f=122 len=3313  严格=✗  工具侧=✓  反推 featS=122 featN=136
bundled-champion-3p.js    v=6 f=123 len=3337  严格=✓  工具侧=✓  反推 featS=123 featN=137
```
`econ-eval ③` 恢复可用（回归：spec 37/37、np-test 30/30）。

### ⚠️ P0 修好后立刻暴露的重要事实
A/B 一恢复，就发现**当前 v6 冠军比 v5 存档差**：

| | A（当前 v6 冠军） | **B（v5 存档 div555）** |
|---|---|---|
| 最大 ep | **1** | **5** |
| 贵技能出手 | **0.0%** | **11.8%** |
| 技能种类 | 6 | **10** |
| 分布 | ジ39.5/枪27.3/藤甲11.3 | ジ40.7/枪12.9/**坦克11.8**/反弹11.1/八卦阵11.0/防御10.0 |
| head-to-head | A×2:B×1 = 71.4%；A×1:B×2 = **20.7%** | — |

⇒ **v6 那次重训（含 (a) 特征 + 探针门槛 + 名次改动）是净负向的**：
经济更浅、种类更少、头对头更弱。这也印证了千问的判断
（"加了 (a) 特征的那次重训在经济深度上是负向的"）。
**此前因为存档变砖，这个对比根本做不出来** —— 这就是 P0 的价值。

回归：spec 37/37、np-test 30/30。

## v1.3.24 — 修 v1.3.23 引入的回归：冠军包版本不兼容 + 模仿默认关闭

### 回归（我在 v1.3.23 犯的）
v1.3.23 里我把 3P 冠军回滚到 **v1.3.11 的包（v5）**，但 `PACK_VERSION` 早在 v1.3.19 已升到 **6**
→ `checkPack` 拒绝 → **困难档会静默掉回脚本 AI**。
被 `tools/np-test.mjs` 的「N19 多人冠军包：可加载且维度兼容」抓到（30/30 → **29/30**）。

### 修法
1. **重训 v6 冠军**（1500 代 / 75s）：`band=4 pickedWr=0.49 divNorm=0.460 distinct=6`。
2. **模仿默认关闭**：C 方案实测未奏效（只做动作级模仿，学不到跨回合轨迹），
   故 `imitGens` 默认取 `EPIRUS_IMIT_FRAC=0`；需要复验时设 `EPIRUS_IMIT_FRAC=0.35` 打开。

### 教训
**升 `PACK_VERSION` 之后，任何回滚旧冠军的"安全操作"都会造成静默降级。**
回滚冠军前必须先看包版本；本轮是靠 np-test 的版本断言才发现的——
这条断言（v1.3.12 加的）价值在这一刻兑现了。

回归：spec 37/37、np-test **30/30**（恢复）。

