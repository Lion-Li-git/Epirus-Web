## v1.3.6 — 多人 AI 学会攒钱（打破"1 ジ 枪黑洞"）

**问题**（千问诊断 + 本地复现）：3P 冠军 6701 个决策点 ep 中位数 0 / 90 分位 1 / **最大 1**，
永远在 `ジ→枪→0` 循环，2 ジ 以上技能**永久不可负担** → 28 个技能只用到 6 个，
v5 的 122 维特征里"前摇/蓄能珠/大雷"相关维度基本是死输入。

**关键教训：这是探索瓶颈，不是奖励问题。** 连续三轮实验：
1. 加 `save`（奖励"攒到过的最高 ep"）→ 失败，反而学会**囤积**（max ep 冲到 58）而永不花钱。
   期间还暴露一个自身 bug：`R.byKey[key].cost` 是**数字**不是对象，`cost.ep` 恒为 undefined，
   导致"贵技能出手"计数恒 0——修成 `S.computeCost(state,pid,key).ep`。
2. 加 `conv`（奖励贵技能出手，0.15 主力）+ 压低旧口径 → 仍失败，max ep 死守 1。
3. 加 `hold`（奖励"ep≥1 时不花钱"，本该是稠密信号）→ 仍失败。
   原因：`policyChooserN` 只在**可负担**技能里 softmax 采样，而策略在 ep≥1 时 99.8% 选枪，
   "不花钱攒钱"这个动作**根本采样不到** —— 奖励再大也没有梯度。

**解法：ε-探索。** `policyChooserN(params, temp, eps)` 新增 ε-greedy（训练 eps=0.15，
评测/UI 不传 eps → 行为不变）；fitness 配平 `save 0.02`（饱和于 ep=2，不可靠囤积刷分）
+ `hold 0.03` + `conv 0.15`，旧的 proact 0.06→0.02、deal 0.03→0.01（旧口径实质在惩罚攒钱）。

**结果**（1500 代 × pop16 × gpo8，fresh，**89 秒**，16 worker）：
- **max ep 1 → 2；贵技能(≥2 ジ)出手占比 0% → 32.3%**；ep 分布 ep0 34% / ep1 68% / ep2 100%。
- 自发出「ジ 攒钱 → 激光剑（2 ジ，穿反弹）」循环（ジ 67.7% + 激光剑 32.3%）。
- 对旧冠军 head-to-head：**A×2 vs B×1 = 49:0（100%）**、A×1 vs B×2 = 28:22（56%）。
- 对 45 个脚本对手对 1st 51.9%（旧 53.3%，在 ±3pt 噪声内），服务器终局验证 1st **60.1%**。
- **仍存**：只用 2 种技能（多样性反而下降）；3/5 ジ 档（大雷、电磁炮）仍未触达。
  更深经济需分层奖励（按费用分档）或更高 ε —— 列为下一步。

**新增工具** `tools/econ-eval.mjs`：冠军经济画像（ep 分布 / 技能分布 / 种类数）
+ 对脚本对手对 1st 率 + **新旧冠军同局 head-to-head**（避免拿不同对手对的数字横比）。

**回归**：2 人 spec 36/36、N 人 np-test 30/30、smoke OK、np-probe 16/16。
## v1.3.5 — 多人同优先级改为「快照同时结算」+ 规则文档对齐

- **座位偏置根因修复**（`js/core/resolve.js`）：原本同优先级内一律按座位号（pid）升序结算，
  先结算者会把后者作废 → 低 pid 在"同归"局面里系统性免伤。
  - 大雷层与 `clashPass` 改为**层入口拍快照、层内按快照判定、最后统一落地** void 与伤害。
  - 实测：三方互放大雷（P0→P1 / P1→P2 / P2→P0）旧口径 HP **3/1/1**（P0 毫发无伤）→ 新口径三人各吃
    直击 2 + 连带 2 = 全灭；3× 同一策略（破防脚本）座位 1st 率 **73.5 / 14.5 / 0.0 → 32.9 / 31.5 / 35.7**。
  - 结算起点仍按回合轮换（`turnOrder`）；本轮 A/B 证明快照落地后轮换已是 no-op，保留作为其余层的保险。
  - **2 人局保持恒等顺序 → 行为不变，spec 仍 36/36**（v1.0 冻结不受影响）。
- **回归测试**（`tools/np-test.mjs` 28 → **30**）：新增「N2 同优先级同时结算：三方互放大雷 → 人人挨打」
  与「N2 座位偏置：3× 同一策略各座位 1st 率接近均等」。
- **新增诊断工具** `tools/seat-diag.mjs`：同策略座位 1st 率 / 三方互放大雷事件流 / 冠军 ep 与技能分布。
- **规则文档修正**（`docs/RULES-NP.md`）：
  - **N2**：删去"同优先级按 pid 升序、除小雷链外不影响结果"的错误说法，改为快照同时结算并附实测数据。
  - **N6**：连带伤害由"各受 **1 点**"改为与代码一致的 **2 点电伤 + 行动作废 + 禁用 3 回合**
    （按用户裁定：大雷强度与 5 ジ 费用不动，以代码为准），并标注"被 T 使用技能者是否该作废"的可争议点。
  - **N12**：补"全员 HP≤0 但有人挂 `reviveNext` → 该人判胜"的边界裁定（约 2/1500 局命中）。
  - **N19**：加"表格每行对手组合不同、不可横向比大小"的读数警告；说明座位率是跨对手对平均、
    掩盖单组摆动；补镜像对局全平对样本的筛选效应；双枪/镜面 0% 的真实原因是**经济锁死**而非权衡放弃。
- **新发现（待修，已写入 N19）**：3P 冠军 6701 个决策点 **ep 中位数 0 / 90 分位 1 / 最大 1**，
  永远在 `ジ→枪→0` 循环，2 ジ 以上技能永久不可负担 → 28 个技能只用到 **6 个**；
  成因是 fitness 的 `deal`/`offensive` 项。富裕经济 A/B：双枪 −26.8pt、镜面 −31.3pt（净负），
  大雷被喂饱时最强（77.4% vs 基线 52.8%）。
## v1.3.4 — 网页训练场支持多人训练（3/4/5 人）

- 训练场新增「人数」下拉（2 人 / 3 人 / 4 人 / 5 人）。选 2 人走原 1v1 训练，选 3~5 人走多人名次制训练。
- `server/train-worker.mjs` 新增 `{type:'evalN'}` 分支，worker 内解析对手函数名后调 `T.scoreMemberN`。
- `server/paralleltrain.mjs` 新增 `makeParallelEvalN(T, opts)` → `{ evalPopN(pop, gen, games, n, oppNames), workers, close() }`，**种群按 worker 数均分并发**，池不可用时回退串行。
- `server/train-server.mjs`：`/train?…&n=`（n>2 走 `runTrainN`）、`/champion?n=`、`/reset` 一并清多人状态；`BUNDLE_MP = 'js/bundled-champion-3p.js'`（2/3/4/5 人共用同一网络，特征与人数无关）；`writeBundleMP` 写 `window.EPIRUS_CHAMPION_3P*` 并刷新 index.html 全部 `?v=`。
- `js/ui/ui.js`：远程训练读 `#tr-n`，多人时 pop 默认 32 / gpo 默认 20；SSE 新增 `start/gen/done` 的 `n>2` 分支（图表显示 best 单曲线 + 1st/top2 实时值），done 时写入 3P 冠军并清 localStorage，困难档立即生效。
- 修复：对手函数名映射原先用字符串拼接生成，`antidef` → `pickAntidef` 大小写错误，导致终局验证抛 `sel is not a function`。改为显式 `BOT_FN_N` 表。
- 实测（16 worker）：3 人 8 代 × pop8 × gpo6 含终局验证 7.5 秒完成，SSE 事件流正常，冠军正确写盘。
# 更新日志

## v1.3.3 — 多人困难档真的用上 3P 冠军了 + 本局复盘

**2026-09-10**

用户反馈「这两个 AI 仍然在互相对打」——查证后发现是真 bug，且是我们自己的两个疏漏叠在一起。

### 根因（两个疏漏叠加）

1. **上一轮的补丁没写进文件**：把「多人困难档改用 3P 冠军」的代码加进 `js/ui/ui.js` 时，
   辅助函数 `rep(s, old, new)` 的返回值**忘了赋值回 `s`** —— 断言通过、脚本打印 "ok"，但改动被丢弃。
   于是多人的困难档一直还是 `Bots.pickAdaptive`（脚本），脚本用的 `pickTargetFor` 又是**没有反锁逻辑**的
   「打血量最低的对手」→ 又回到两家互为目标、每回合互相抵消。
2. **探针检查太弱**：`np-probe` 只断言了「`window.EPIRUS_CHAMPION_3P` 已定义」（= 脚本加载了），
   没断言「AI 真的在用它」→ 没抓到上面这个问题。
3. **附带的缓存风险**：`js/bundled-champion-3p.js` 是唯一**没有 `?v=` 缓存戳**的脚本，
   浏览器可能继续用旧的 v4 冠军包（f=62）→ `checkPack` 拒绝 → 回退脚本。

### 修复

- `js/ui/ui.js`：`chooseAIMulti` 困难档改为**真正加载并使用 3P 冠军**（`loadMultiChamp`）；
  localStorage 里的旧包若不兼容，**回退内置包**（此前会直接判失败）。
- `pickTargetFor` 改为委托训练器 `EpirusTrainer.pickTargetN`（反锁 + 必杀优先 + 打领先者），
  **脚本回退路径也一并享受反锁**。
- `index.html`：3P 冠军包加 `?v=` 缓存戳。
- **新增「对手 AI」显示**：新对局日志会写明 `对手AI=3P 冠军` 或 `脚本·自适应（冠军包缺失/不兼容）`，
  训练钩子 `window.EpirusUI.aiInfo()` 可供探针断言。

### 本局复盘（用户第二个需求）

- 结束面板新增 **「📜 查看本局复盘」**：不重开对局，弹出可滚动逐回合记录，附「导出记录 / 关闭 / 再来一局」。
- 对战工具栏新增 **「上局记录」** 按钮：从 localStorage 读上一局文本（**点过「再来一局」也还在**）。

### 探针加强（防止同类问题再溜过）

- 断言 **困难档实际使用 3P 冠军**（`aiInfo().champ === true`），不再只看「包已加载」。
- 断言对局日志里 **「相抵」出现次数 ≤ 2**（不出现互相抵消死循环）。
- 断言 **本局复盘面板可打开**。
- `np-probe` 11 → **16/16**。

### 真实浏览器实测（3 人 · 困难）

`results/epirus-3p-hard-live.txt`（玩家全程只出ジ）：**相抵 0 次**；
第 7 回合两家同时枪击玩家1 → 各中一枪、玩家1 死亡（☠ 有事件）；结果 22 回合玩家2 获胜；
显示统一为「玩家1/玩家2/玩家3」，不再出现「电脑」或数字 `2`。

## v1.3.2 — AI 状态特征 v5（对手槽位 + 技能历史）+ 显示/目标修正

**2026-09-10**

用户复查对局记录后提出三件事，全部处理。

### 显示统一

- 事件行 `nm(pid)` 原先对 pid1 写死「电脑」、pid2 直接打数字 `2` → 改为统一取玩家真实名字。
- `buildBattleText` / `rebuildLog` 原先用 2 人模板（`你=【…】 电脑=【…】`）拼多人回合行 → 多人改用整行记录。

### 目标选择 v2（消除「互相抵消死循环」）

- 记录里两个困难 AI 都用「打血量最低的对手」→ 血量错开时彼此成为对方的最低血 → **互为目标 → 每回合互相抵消**，
  第三家白嫖ジ（60 回合 0 推进）。新口径：① **反锁**（上回合与该对手互为目标而相抵 → 本回合不再打他）；
  ② 能一击必杀先杀；③ 否则打**血量最高的领先者**；并列随机。
- 实测（玩家只ジ + 两个困难 AI）：相抵 **1 次**、消极玩家第 **9 回合**被打死、**14 回合**结束（原 60 回合）。

### 大雷连带修正（按原文效果 3）

- 连带伤害 1 → **2 点电伤**（与直击同口径）；被连带者的技能同样**作废 + 禁用 3 回合**（防御/反弹/金刚盾/ジ 豁免）。

### AI 状态特征 v5（PACK_VERSION 4 → 5）

- 原 62 维是 2 人形状：对手信息全是聚合量（minHp/maxEp/any/sum），只有「ジ最多的那个对手」的 lastSkill 可见，
  且**完全看不到历史**。
- 新增 **每个对手一个槽位**（4 槽 × 10 维 = 40）：存活、各自 HP、各自ジ、各自电/爆珠、lastSkill(索引+类别+空位)、
  穿透/大雷前摇；槽位按「存活 → 血量升序 → pid」稳定排序，槽 0 永远是最可能被杀的。
- 新增 **最近 3 步技能历史**（自己 + 4 槽位，各 3 步 + 「连续同招比例」= 20 维）。
- `FEAT_S` 62 → **122**，参数 1873 → **3313**；2 人 / 5 人同长度。旧冠军包（f=62）不兼容 → **2 人与 3P 冠军均已重训**。
- 对手池新增 `farmer`（只攒资源不进攻）。

### 重训结果

- 2 人冠军：19 个基准 **avg 97%**（wall / defend 100%，最低 mix 75%）。
- 3 人冠军：1st **49.9% / top2 71.8%**（36 对 × 20 局 = 720 局）；出招明显转攻
  （枪 40.2%、ジ 44.0%、防御架势合计 ~13%，此前 ~29%）。
- 脚本基线（同赛制）：aggro 60.4% / balanced 51.0% / mix 43.9% / defend 26.8% / random 9.0% / **farmer 2.4%**。

### 自测

- 2 人 `tools/spec-run.mjs` 36/36；N 人 `tools/np-test.mjs` 28/28；多人页面 `np-probe` 14/14；2 人 `smoke` OK。

## v1.3.1 — 多人规则修正（目标/相抵/大雷连带/死亡）+ 3P 冠军重训

**2026-09-09**

用户复查 3 人局对局记录（`results/`）后指出「技能目标存在巨大问题」。定位到 3 个引擎缺陷并修复，
多人生态随之大幅改变，3P 冠军已重训。

### 引擎修正

- **N4 相抵必须“互为目标”**（原文档口径，代码漏实现）：初版 `clashPass` 只比优先级、**完全不看目标**，
  导致「两家同时打第三人」的攻击互相抵消（记录里 P2/P3 同时枪击玩家1 → 双方攻击相抵、玩家1 毫发无伤）。
  现在只有 A→B 且 B→A 的攻击才交锋（同优先级相抵、高优先级阻止）；两家同打同一第三人 → **两击都落地**。
  2 人时“互为目标”天然成立 → 行为与 v1.0 完全一致（回归 36/36）。
- **N6 大雷连带伤害**（原文效果 3，此前误裁定为“不产生连带”）：
  与目标 T 发生交互的第三方也吃连带 ——「对 T 使用技能者」受 1 点电伤且该攻击被无效化；
  「被 T 使用技能者」受 1 点电伤；施法者自身不参与。2 人时无第三方 → 行为不变。
- **N12 死者不再行动**：`attemptAction` 直接拒绝已淘汰者；`startTurn` 每回合重置行动槽
  （防死者沿用上回合行动）；`autoGameN` / UI 的 AI 跳过死者；死亡写 `death` 事件便于复查。

### 训练

- 规则变化后重训 3P 冠军（`node tools/train-3p.mjs 500 3 20 32`）。

### 实测（3 人，28 对手对，座位轮换，temp0.15）

| 主体 | 1st | top2 |
|---|---|---|
| **3P 冠军** | **60.9% / 68.1%（独立复核）** | **78.0% / 81.9%** |
| 脚本 aggro | 58.6% | 84.1% |
| 脚本 balanced | 55.0% | 85.5% |
| 脚本 mix / defend | 39.6% / 29.1% | 69.5% / 68.6% |
| 脚本 random | 8.4% | 52.1% |

- 生态对比（修规则前 → 后）：random 20.4%→8.4%、aggro 49.6%→58.6%、**冠军 51.3%→68.1%**。
  集火不再自我抵消后，比赛更致命、进攻型主体全面上移。
- 出招分布：ジ 37.1%、枪 22.4%、贴贴 10.6%、反弹 9.7%、防御 9.3%、八卦阵 9.2%。

### 自测

- `tools/np-test.mjs` 19 → **26/26**（新增 N4 / N4b / N4c / N12 / N12b / N6 / N6b）。
- 2 人 `tools/spec-run.mjs` 仍 **36/36**；2 人页面 `smoke` OK；多人页面 `np-probe` 全绿。

## v1.3.0 — 多人（3 人）自对战训练 + 3P 冠军

**2026-09-09**

按 N19 落地多人训练：多对手聚合特征 + 名次适应度 + 座位轮换 + 名人堂验证择优。

### 训练

- `js/train/policy.js`：`oppAgg(state, pid)` 把“对手”改成**多对手聚合**（minHp / maxEp / any() / sum()），
  特征向量长度与顺序不变（N=2 退化为原值 → **2 人冠军包仍兼容** f=62 / 1873 参数）；
  `ACT_KEYS` 扩到全 30 技能（含双枪 / 镜面），参数维度不变；修复 `chainLink` 改名后失效的铁索特征。
- `js/train/evo.js`：新增 `scoreMemberN` / `oneGameN` / `rankOf` / `evalN` / `policyChooserN` / `wrapBotN` / `pickTargetN`；
  **名次适应度 1.0 / 0.3 / 0.0**（初版 0.6 / 0.2 会奖励“苟到第二”，实测把 1st 率从 52% 压到 42%）；
  目标选择在血量并列时**随机**（消除脚本固定打 pid 最小者造成的座位假优势）。
- `tools/train-3p.mjs`（新）：N 人进化训练，支持热启动、名人堂、终局用全部 28 对手对重新验证择优。
- `tools/eval-3p.mjs`（新）：28 对手对 × 座位轮换 → 1st / top2 率 + 出招分布。

### 结果

- **3 人冠军：1st 51.3% / top2 74.3%**（28 对 × 25 局 = 700 局，座位轮换，temp0.15）。
- 同赛制脚本基线：aggro 49.6% / balanced 41.1% / mix 39.1% / wall 27.9% / defend 26.3% / random 20.4%
  → **冠军已超过最强脚本**；随机基线 33.3% / 66.7%。
- 座位 1st 率 P0 54% / P1 47% / P2 53%（无座位偏差）。
- 出招分布：ジ 25.4%、枪 22.6%、反弹 17.0%、防御 16.8%、八卦阵 16.7%。
  **双枪 / 镜面 0%** —— 名次制 3 人局里“3 ジ 换 2 点分散伤害”不划算（涌现结果，非 bug）。

### UI

- 多人局**困难档改用 3P 冠军**（包缺失 / 不兼容时回退脚本自适应）；`index.html` 加载 `js/bundled-champion-3p.js`。

### 自测

- `tools/np-test.mjs` 18 → **19/19**（新增 3P 冠军包兼容性）。
- `tools/np-probe.mjs` 12 → **14/14**（新增 3P 冠军加载 + 困难档多人对局）。
- 2 人 `tools/spec-run.mjs` 仍 **36/36**。

## v1.2.0 — 多人技能补齐（镜面反射 / 聚光炮 / 反复横跳 / 合二为一）

**2026-09-09**

多人（3~5 人）的**可选技能与隐藏技能全部实现**；2 人对局行为不变（引擎自测仍 36/36）。

### 引擎

- `js/core/rules.js`：新增复合伤害类型 `DMG.FIRELIGHT`（光&火）；`AVAILABLE_MULTI` 恢复全技能（含镜面反射）。
- `js/core/resolve.js`：
  - **镜面反射**（N14）：两个目标（复制对象 t1 / 输出对象 t2），复制 t1 本回合的**伤害**技能并由使用者打向 t2；
    复制双枪只算一枪；架势 / 自增益 / 状态类不可复制；复制体来源 = 使用者（反弹 / 地雷按使用者结算）；
    在优先级 3 之后结算，不重排优先级。
  - **聚光炮**（N16）：两人互镜且输出同一人 → 该人受 1 点光&火。
  - **反复横跳**（N15）：镜面 (t1→t2) 边成环 → 所有使用者各受 1 点光&火（与聚光炮互斥）。
  - **合二为一**（N17）：≥2 人同时用小雷打同一目标 → 目标额外 1 点电伤。
  - 藤甲火弱 / 吸血鬼光弱统一在 `rawDamage` 结算（聚光炮 / 反复横跳等直扣血也吃加成）。

### UI

- 镜面反射**两级目标弹窗**（“选择复制对象 / 选择输出对象”）；新增 mirror / mirrorNoEffect 与隐藏技能日志文案。
- 多人专用技能标签由「未实现」回到「多人模式」。
- 新增测试钩子 `window.EpirusUI`（供 `tools/np-probe.mjs` 驱动）。

### 自测

- `tools/np-test.mjs` 12 → **18/18**（新增 N14 / N14b / N15 / N16 / N17 / N18 用例）。
- `tools/np-probe.mjs` 11 → **12/12**（新增镜面反射两级弹窗）。
- 2 人 `tools/spec-run.mjs` 仍 **36/36**（回归不变）。

## v1.1.0 — 多人（3~5 人）框架

**2026-09-09**

2 人对战行为不变（引擎自测 36/36 回归全绿），引擎与 UI 推广到 3~5 人。

### 引擎

- `createState(modeKey, rng, n)`：N 人状态（`p[]` / `actions[]` / `n`），`n` 缺省 = 2。
- **显式目标**：技能目标写在 `action.target` / `target2`；`resolveTarget` 在 2 人时 = 另一个，N 人时取指定目标（非法则退化为第一个存活对手）。
- `resolve.js`：全部 `1 - pid` → 显式目标；结算管线遍历 N 个行动；`clashPass` 改**两两相抵**；铁索改**边集合**（`p.chains`，不递归）；`checkOver` 改 N 人（最后存活胜 / 全灭平局 / 回合上限血最高者胜）。
- `rules.js`：新增 `multi` 模式（3~5 人，3 血，全技能）。
- **双枪射手**：实现（两个目标各 1 点，按枪口径可被反弹 / 触发地雷）。
- 镜面反射 / 聚光炮 / 合二为一 / 反复横跳：**本版未实现**（技能表置灰，见 RULES-NP N11）。

### UI

- 对局面板新增**人数下拉（2/3/4/5）**；N>2 时强制 `multi` 模式。
- 多人面板堆叠渲染（每人 HP/ジ/珠/状态）；**目标选择弹窗**（双枪为两级）。
- 人类玩家被淘汰后**自动观战到结束**；结算面板支持 N 人。

### 自测

- 新增 `tools/spec-run.mjs`（2 人引擎 36/36，Node 桩）、`tools/np-test.mjs`（N 人 12/12：目标/小雷逐边/铁索不递归/双枪/胜负/fuzz）、`tools/np-probe.mjs`（真实 Chrome 3 人 + 5 人 11/11，无 JS 异常）。

### 文档

- `docs/RULES-NP.md` 升为 v0.2（已实现），记录 N1..N13 裁定与实现状态。

## v1.0.0 — 2 人对战完整版（功能冻结）

**2026-09-09**

首个正式版本。2 人模式功能冻结，后续开发转向多人（3~5 人）框架。

### 引擎

- 28 个可用技能 + 4 个隐藏技能（铁索连环 / 吸血鬼公爵 / 回魂 / 梦魇）全部实现；
  多人专用技能（双枪射手 / 镜面反射 / 聚光炮 / 合二为一 / 反复横跳）置灰保留。
- 裁定版规则 `docs/RULES-2P.md`（R1..R55 + 子证），代码注释逐条对应。
- 优先级管线 / 防御矩阵（防御·反弹·八卦阵·金刚盾·原型制御·藤甲·全息屏障）/ 地雷链 / 转移伤害 /
  挑衅义务 / 符咒引爆窗口 / 大雷只禁"目标当回合使用的技能"。
- **无贷款**：资源不足 → 技能作废，不扣血、不消耗资源。
- **蓄能珠只供下一回合**：回合结束时未使用即清空（R9'，原规则"供给下一回合"）。
- **铁索连环**按文档口径共享火焰（天火）与爆头伤害。
- **绝不空放兜底**：脚本/AI 返回买不起或不合法的招一律改按ジ，避免对手 `lastSkill=null` 掉进未训练区；
  同时 `policy.js` 为"上一招无效"补了显式特征，池内 `whiff` 对手专门训练该状态。

### 对局

- 标准模式（3 血 · 28 技能），三档难度：简单 / 中等 / 困难（冠军策略）。
- 同时行动、回合日志逐条结算、技能按钮按资源实时置灰、「悔一步」（每局一次）、导出对局记录。

### 训练

- 零依赖 Node 训练服务（`worker_threads` 多核并行 + SSE 实时进度），页面一键启动。
- 对抗进化（类 GAN 思想）：19 个脚本基准，含三条人类式硬门槛（坦克线 / 重火力 / 防御+枪三拍）、
  原型制御墙、空放型、三条节奏型（反弹→枪→坦克 / 反弹→反弹→坦克 / 防御→反弹→枪）。
- 冠军择优：任一基准不高于 50% 直接失去资格，过门后按 `0.5×平均 + 0.5×最差对局` 排序。
- 多种子 × 多轮 + 种子谱系（保留前 ⌈√n⌉ 名作父代，按"越靠前分得越多"分配新种子）。
- 评测口径与实战一致（双方均基于行动前状态决策）。
- 内置冠军：19 个基准全部 ≥80%，平均 96%。

### 自测

- `tests/spec.html`：**36 条规则场景用例 + 500 场随机对局无异常**。
- `node tools/smoke.mjs`：CDP 驱动真实 Chrome 走完整对局与训练页。
