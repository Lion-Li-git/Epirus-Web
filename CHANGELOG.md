## v1.3.20 — A 方案：多人专用难度档（三档 N 意识脚本 + 显式兜底）

### 问题（用户反馈："ai 搞崩了都没法玩"）
`ui.js` 的 `chooseAIMulti` 原本是：
| 档 | 原实现 | 问题 |
|---|---|---|
| 简单 | `DIFFICULTY.easy` = 60% random + 40% `pickBalanced` | 逻辑是 2 人的 |
| 中等 | `Bots.pickBalanced` | **与 2 人中等同一个函数**，不认识多人 |
| 困难 | 3P 冠军；unpack 失败 → **静默**回退 `Bots.pickAdaptive` | 冠军一坏就掉到 2 人脚本，且无提示 |

根因：**全套 23 个脚本 bot 都是 2 人时代写的**。最典型的是 `oppPidOf` 只找**血量最低**的对手
——永远打残血、无视领先者，也不认识"有人正在攒钱（ep≥3）"这个 3 人局的核心威胁。

### 改动
1. `js/train/bots.js` 新增多人专用档 `DIFFICULTY_N = { easy, medium, hard }`：
   - `pickMultiEasy`（简单）：随机 + 50% 枪打领先者，**故意不架势、不穿透**（留破绽）
   - `pickMultiMed`（中等）：能杀就杀 → **压制攒钱者** → 对领先者架势做穿透反应 → 攒ジ/开枪
   - `pickMultiStrong`（困难·脚本兜底）：上述 + 低血守、被两人压时反弹、
     **ep≥3 且环未启动 → 开环**（2 人脚本从没做过的经济行为）
   - 三档都返回 `{key, target}` → **"打谁"也成为决策**（老脚本只管"用什么"）
2. `js/ui/ui.js` `chooseAIMulti` 改用 `DIFFICULTY_N`；支持脚本返回 `{key,target}`；
   困难档冠军不可用时**显式标记 `B.aiFallback=true`**，`aiInfo()` 报
   `脚本·多人强档（冠军包缺失/不兼容，已回退）`，**不再静默降级**。

### 实测（3 人，三档各坐三个位置 × 6×6 对手对 × 8 局 = 288 局）
| 档位 | 1st 率 |
|---|---|
| 简单 | 36.5%（接近随机线 33.3%，够弱 ✓） |
| 中等 | **57.3%** |
| 困难·脚本兜底 | **52.1%** ← ⚠️ **比中等弱** |

⚠️ **困难档还不合格**。原因分析：`pickMultiStrong` 相对中等新增的分支
（`ep>=3 开环`、`大雷`、`被两人压 → 反弹`）在当前经济下**几乎不触发**
（经济锁在 ep≤2，大雷要 5 ジ）→ 困难 ≈ 中等 + 几个不生效的分支，
而 `被两人压 → 反弹` 反而可能白费一回合。**下一轮需要：给困难档补上真正吞吐量的差别
（例如更积极的穿透选择、更早的守势判定），而不是堆不触发的分支。**

回归：spec 37/37、np-test 30/30、页面 SMOKE OK、无 JS 错误。

## v1.3.19 — 千问纠正后的基础设施修复（让所有数字可信）+ (a) 特征

### 0. 前提纠正（千问自查，重要）
千问上一轮的"2P 冠军 ep≥3 占 94.9%、最高 ep=59"**是坏测量**：他把 2P 对手用 `wrapBotN` 包了一层，
而 `wrapBotN` 返回**对象**、2P 的 `autoGame` 直接把它当技能名传给 `attemptAction`
→ **对手全程无效出招**，冠军在无对抗环境里空转到 60 回合。
用正确接线重测：**2P 冠军最高 ep 也只有 2**（ジ53.9 / 枪19.4 / 藤甲19.0 / 坦克6.3 / 狙击枪1.4）。

⇒ **「2P 能攒钱而 3P 不能」这个前提不成立，两边都攒不到 3。** 这不是 3 人局特有的病。
本文档 §0 与旧版 CHANGELOG 的说法据此更正；§2.2 的算术约束 `ジ占比 = c/(c+1)` 仍然成立
（观测 53.9~59.8% 贴着上限）。

### 1. `damage` 事件补 `source`（千问指出：6 处发射点全只有 to/amt/reason/via）
`rawDamage` 的 damage 事件加 `source`（从 `opts.source` 透传）；`deliverDamage` 调用 `rawDamage` 时带上 `dmg.source`。
验证：`枪` 命中后事件 `{"type":"damage","to":1,"amt":1,"via":"gun","source":0}` ✓。
顺带解锁：名次第 4 键「累计造成伤害」现在可用了（原先因缺 source 不可用）。

### 2. `tools/skill-diag.mjs` 上下文错接（千问指出，**会污染历史数字**）
第 30 行用 `sb.EpirusState`，但引擎/冠军装在另一个沙箱对象 `W` 里 → `Cannot read properties of undefined`；
且 `S` 根本没声明。已修为 `const S = W.EpirusState`。
⚠️ 该工具此前在费用表处崩溃（分布数字本身没问题，但费用列缺失）——§2.1 引用的"有效技能数 3.21"
来自能正常打印的分布部分，已用修好的工具复核。

### 3. (a) 自己的「跨得过聚能环启动线」离散特征（千问方案，1 行）
`policy.js` 原先只有**对手**版本 `anyOp(o => o.ep>=3 && o.ringStreak===0)`；
补上 **自己**的 `(me.ep >= 3 && me.ringStreak === 0) ? 1 : 0`。
价值（千问原话）：不是"告诉它能攒"，而是让"跨过 3"成为**可被 value 区分的离散事件**——
否则 ep 是连续输入，网络只能学出单调的"钱越多越好"，学不到"3 是质变点"。
⇒ `FEAT_S 122 → 123`，`PACK_VERSION 5 → 6`，paramCount **3337**。

### 4. 两个冠军按 v6 重训（否则 v5 包被 checkPack 拒、困难档会掉回脚本 AI）
- **2P**：`train-fast 600` 代 197s（score=1.067, attackShare=0.27）
- **3P**：`train-server 1500` 代 75s（multiObj band=2, pairSc=0.844, **probeSc=0**, 1st 47.6%）
- 验证：两个包 `v=6 f=123 len=3337 unpack=true checkPack.ok` ✓；页面 **SMOKE OK**、无 JS 错误
- 回归：spec **37/37**、np-test **30/30**

### 5. 千问对四个方向的裁决（记录，供后续）
- **(b) 排除**：环启动费 3→2 **不会改变行为**。问价值函数：ep=1→防御100%、ep=2/3→激光剑100%、
  ep=5→聚能环100%。**ep=2 时它选激光剑而不是环**，且 `Q(ジ) > Q(枪)` 在所有 ep 档成立（差 +2.4~+17.8）
  ⇒ 它不排斥攒钱，排斥的是"为了环多等一轮"——**是定价错误，不是门槛错误**。
- **(a) 近乎免费且必要**（已做，见上）。
- **(c) 必需**：探索的单位必须从"动作"升到**轨迹**——把储蓄视界 h 做成**基因**并参与选择，
  每个 h 保留子群（固定 ε 只保证偶尔试一次，不保证"试成功的那次会被记住"）。
- **(d) 理由反了**：千问实测补到 ep=5 时冠军夺1率 **84.4%**（不补 41.1%）⇒ 深经济**很强但进不去**，
  (d) 只在"决定不修"时成立，措辞应为"**当前不可达**"而非"无价值"。

### 6. 千问指出的**未试过的组合**（下一轮）
```
sc_gate = 补贴局（regen 或起手 ep=3~5）里的夺1率 ≥ 阈值
排序键   = 原生局夺1率
```
理由：深经济能力在**被选出的那一刻可见**（补掉实验 2 的"选择时不可见"），
且打分用**结果**而非行为计数（补掉实验 3 的"空放刷分"）；天然免疫漂移（评测永不退火）。

⚠️ **新坑警告**（千问）：双枪修好后，≥3 ジ 档终于有了"能落地的伤害技能"，
偏偏就是那个**净负收益**的（强制用双枪 = −27pt）。
⇒ 任何"奖励贵技能落地"的探针现在会把冠军选成"爱空放双枪"。**探针口径必须挂到胜负上**。

## v1.3.18 — 探针两处修正（只奖励落地 / 改门槛）+ 探针不可满足的根因

按上轮记录的两条修正一起做（互相依赖）：
1. **探针只奖励「落地」**：`score = min(1, 落地的≥3ジ技能数/2)`，出手不再给分。
   上一版 `0.4*casts/3` 奖励的是"花钱"本身 → 被"空放"刷分（castPerGame≥3 但 landPerGame=0）。
2. **探针是门槛不是分数**：`PROBE_MIN = 0.5`，`probeSc ≥ 门槛` 才进排序，排序仍按 `pairSc`。
   上一版 `min(pairSc, probeSc)` 跨量纲（[0,1.5] vs [0,1]）→ min 几乎永远取到探针。

⚠️ 过程事故：`PROBE_MIN` 我声明在了 `if (candsN.length)` 块内，却在 `hall` 循环里先使用
→ `PROBE_MIN is not defined`，**训练跑完 1500 代才在最后一步报错**（前 1500 个 gen 事件正常）。
已修为函数作用域。教训：长训练脚本的收尾阶段错误会被前面的正常流掩盖。

### 实测（3 人 1500 代，67s）
```
[multiObj] band=1 pickedWr=0.486 divNorm=0.443 distinct=5  pairSc=0.841  probeSc=0
原生实测：ジ28.6 / 枪27.2 / 反弹15.8 / 防御14.5 / 八卦阵13.8   最高 ep=1   有效技能数 4.75
```
- **所有候选 probeSc 均为 0** → 过门槛数=0 → 优雅退化为全体候选并按 pairSc 排序（设计如此，无崩溃、无 hack）。
- **副作用**：本版冠军 **有效技能数 4.75，是 3 人侧历史最高**（v1.3.11 为 3.21）——
  熵移出目标函数 + 完整名次 + 回放切片的组合效果。但最高 ep 仍为 1。

### 根因：我的探针**在构造上不可满足**
"落地的 ≥3 ジ 技能"在现有技能表里**只有大雷（5 ジ）会造成伤害**——
地雷3/净化3/双枪3/镜面3 都不是伤害技能，避雷针4 是防御。
而千问实测：喂到 ep=5 时冠军 **81% 选坦克、大雷 0%**。⇒ 探针恒为 0，门槛形同虚设。
**探针只能"守住"已有能力，无法"创造"能力**；而当前种群从未发展出 ep≥3 的落地能力，
所以门槛没有任何东西可筛。这轮等于验证了：**千问的漂移结论（探针能防漂移）成立，
但探针不能当"创造者"用**——创造仍必须靠"那个状态被走到并被奖励"。

处置：冠军回滚 v1.3.11（强版本）；本版存 `docs/artifacts/champion-3p-div475.bak`（最发散）。
回归：spec 37/37、np-test 30/30。

## v1.3.17 — 深经济探针进「选择」（Q3 完整版）→ **反噬，探针被 hack**

按千问 Q3 补上最后一半：把「深经济探针」从诊断提升为**选择依据**。
- `evo.js` 新增 `evalEconProbe(params, games, n, seedBase)`：把冠军喂到 ep=5 并补珠，
  **只打分"是否用出贵技能"、不打分胜负**。口径（本人定义，已写入文档）：
  `0.6 * min(1, 落地的高费技能数/2) + 0.4 * min(1, 出手的高费技能数/3)`，其中"高费" = 费用 ≥ 3 ジ。
- `train-server.mjs` 终局择优改为 **`sc = min(pairSc, probeSc)`**（按类别取最差），
  `multiObj` 事件新增 `minSc/pairSc/probeSc` 便于诊断。

### 实测（3 人 1500 代，66s）：❌ 反噬
```
[multiObj] minSc=0.400  pairSc=0.810  probeSc=0.400  → 探针成为绑定项
原生实测：反弹 32.7% | 防御 30.9% | 八卦阵 30.3% | ジ 3.1% | 枪 3.0%   最高 ep=1
```
冠军**退化成纯防御 spammer**（三种免费架势占 94%，几乎不出ジ、不进攻）。

### 根因（两个都是我的设计错误）
1. **`casts/3` 奖励"出手"而不是"用对"**：它靠不断放高费技能拿分，**即使全被挡下、一次没落地**
   （探测显示 `landPerGame=0`、`castPerGame≥3`）→ 这正是"奖励打伤害却打不赢"的同类过拟合，
   只是这次奖励的是"花钱"本身。
2. **`min` 跨不同量纲的类别是错的**：`pairSc∈[0,1.5]`、`probeSc∈[0,1]`，min 几乎永远取到探针，
   于是"对手对打得怎么样"完全不影响排序 → 冠军为了探针分牺牲实战。
   千问说"按类别取最差"方向对，但前提是**各类别可比**；直接 min 会被量纲绑架。

### 修正方向（下一轮）
- 探针**只奖励"落地"**，出手本身不给分（或给极小权重）；
- 选择口径从 `min` 改为 **约束式**：先要求 `probeSc ≥ 阈值`，再用 `pairSc` 排序择优
  （即"探针是门槛不是分数"），避免量纲绑架。

处置：冠军回滚 v1.3.11；本版存 `docs/artifacts/champion-3p-probe-hacked.bak`。
回归：spec 37/37、np-test 30/30。

## v1.3.16 — 千问漂移实验后的方案：完整名次 / 永久回放切片 / 熵移出目标函数

千问的漂移实验（重要）：只加变异不加选择、σ=0.06 的情况下，**深经济能力在 15~20 代内掉 26~40pt**，
而原生强度基本不动。⇒ 不是"选错了人"，是**没人看管的分支被 σ 噪声漂掉**，且衰减尺度只有 ~20 代。
（这也解释了 v1.3.15 课程为什么必然失败：150 代退火窗口 ≫ 20 代衰减尺度。）

### 1. Q2(i) 完整名次 `rankOf`（纯训练/评测侧，**不动 checkOver**）
原来"并列最高 HP ⇒ 平局"，实测镜像平局里 89~93.5% 三家 HP 向量本身可分 → 白扔一半样本。
改为完整排序打破平局：**存活优先 → HP 降序 → 累计承伤升序**（第 4 键"累计造成伤害"暂不可用，
因为 `resolve.js` 的 `damage` 事件只带 `to`/`via`，不带 `source`）。
⚠️ 严守千问的警告：**胜/平/负分值一律不变**，次级键只用来在"原来判平的地方"分出 1/2/3 名，
绝不拿来加分（历史上 deal/proact"奖励打伤害"养出过"打伤害不赢"的过拟合）。

### 2. Q1(d) 永久回放切片（比课程退火更根本）
`regenForGame(g)`：每 12 局固定留 1 局带补贴（约 **8%**），**永不退火**。
动机：不靠时间安排躲漂移，而是给深经济分支一条**永久的评价通道**。

### 3. Q3 覆盖熵移出目标函数（只作诊断）+ 归一化修正
`divBonus = 0`（熵不再进适应度）；`divNorm` 改为按「**当时可负担的动作数**」归一化
（千问：现在 22/28 个技能对主体不可达，拿 28 归一化是在量一个恒为 0 的量）。

### 实测：❌ 仍然 max ep=2、有效技能数 2.40
3 人 1500 代（68s）：`band=1 pickedWr=0.458 divNorm=0.253 distinct=7`；
原生实测 ジ49.5 / **枪46.9** / 八卦阵1.0 / 防御0.9 / 反弹0.9 / 激光剑0.7，最高 ep **2**。

**根因（我漏做了千问方案的另一半）**：冠军是在**原生经济**下被选出来的
（`runTrainN` 终局 `T.evalN(...)` 跑无补贴局）→ **深经济能力在选择时完全不可见**。
切片只让它在 8% 的局里"被看到"，但没有任何机制让那 8% 影响"谁当冠军"。
⇒ 必须补上 **Q3 的「深经济探针类别」**：把"补到 ep=3/5 时你怎么应对"做成独立评测类别，
只打分"是否用对贵技能"、不打分胜负，并让终局择优把该类别纳入（按类别取最差 / min）。

处置：冠军回滚 v1.3.11；本版存 `docs/artifacts/champion-3p-replay.bak`。
回归：spec 37/37、np-test 30/30。

## v1.3.15 — 千问方案落地（双枪数据表 / 课程式经济补贴 / 承诺级 ε）+ 课程实测否决

按千问分析，**优先做不改 shipped 规则**的三项：

### 1. 双枪走数据表（千问指出的代码问题，已修）
`resolve.js` 原先在 `copyEffect` 与 `SK.DUAL_GUN` 分支**硬编码 `amt: 1`**，
而 `rules.js:76` 的 `dmg` 字段根本没被读 → **以后调双枪/镜面平衡会静默无效**。
已改为读 `def.dmg` / `def.pierce`（`via` 仍按枪口径保留"可被反弹/地雷"语义）。

### 2. 课程式经济补贴（千问方案，纯训练侧）
`evo.js` 新增 `setRegenTotal(n)` + `regenForGen(gen)`：前 30% 代数 `regen=2`（富经济），
30~50% `regen=1`，之后 `regen=0` 回到线上口径。`train-server.mjs` 两个训练入口自动挂钩。
动机（千问原话）：**2P 能攒钱而 3P 不能，不是规则更严，而是 3P 没人替它把"攒到 3"这条路走通过一次。**

### 3. 承诺级 ε（千问方案）
`makeCommitChooser(params, temp, h)`：每局以 30% 概率抽「储蓄视界 h∈1..4」，
本局 `ep < 1+h` 时**只准出 ジ**。比动作级 ε 强在：3~5 回合的连续攒钱轨迹被**真正走到**，
而不是靠多次独立抽样撞运气。

### 实测结果：❌ 课程**没有**打开经济深度
3 人 1500 代（74s）：`[multiObj] 巅峰带=1 pickedWr=0.490 divNorm=0.275 distinct=10`，
终局 1st 49.0% / top2 75.7%。但原生经济实测：

| | 最高 ep | 有效技能数 | 分布 |
|---|---|---|---|
| 课程训练后 | **2（仍锁死）** | 2.56 | ジ50.1 / **枪45.0** / 防御1.2 / 坦克1.1 / … |
| v1.3.11 旧 3P | 3 | 3.21 | ジ59.8 / 激光剑26.0 / … |

**诊断**：补贴期学到的行为在**后 50% 无补贴阶段被冲掉**（灾难性遗忘），
而冠军正是从**退火后**的名人堂里选出的 → 拿到的是"退回原样"的策略。
**未验证的改进方向**：把补贴保持到接近结尾再快速退火；或从补贴期的名人堂里选冠军；
或采用千问另一条（降聚能环启动费 3→2 **并**给连用收益设上限）——那条是规则改动，尚未执行。

处置：冠军回滚到 v1.3.11 强版本；课程版存 `docs/artifacts/champion-3p-curriculum.bak`。
回归：spec 37/37、np-test 30/30。

## v1.3.14 — 过载炮新规 + 训练分析报告 harness 修正

- **过载炮规则**（用户设计）：原设计是"强制消耗对手ジ"，但 AI 没学会攒ジ → 完全无效（强度 Δ −2.2pt）。
  新规：**可打穿防御与反弹，但任何攻击类技能回击都能抵消它**。
  - `rules.js` desc 更新；`resolve.js` 伤害加 `pierce {defense, reflect}`。
  - ⚠️ **关键实现细节**：反制必须放在 `clashPass` **之前**。过载炮 pri=3 > 枪 pri=2，
    若先走相抵流程，炮会先把枪作废，反制永远轮不到（实测 hp 3/2，与预期相反）。
  - 新增 spec 用例（36→**37**）：打穿防御 / 打穿反弹不回打 / 被枪抵消时目标不掉血。
- **报告 harness 修正（我自己的 bug）**：`tools/skill-report.mjs` 的 `makeSel` 把冠军硬编码在座位 0，
  而 `runCondition` 是**轮换座位**的 → 冠军坐 1/2 号位时决策被套在对手身上，测量完全错位。
  这是富经济基线只有 **10%**（低于 3 人随机线 33%）的主因；修正后 **91.7%**。
  另修：只给冠军补资源（原先给所有人补）、珠类技能同时补珠（原先 Δ 全为 0 的假信号）。
- **判定口径修正**：费用 0 的基础动作（ジ）强制 mono-spam 必然大幅为负，标为「基础动作」而非「坑」。

## v1.3.13 — 头对头验收 + AI 训练可视化报告

- **多目标择优加头对头验收**（`H2H_MIN = 0.5`）：容差带内候选必须与最高分候选对拼 ≥50% 才允许选中。
  原因：`1st + 0.5*top2` 不能完整代表头对头强度（3 人侧实测：带内最发散候选被 2 倍数量的强候选打成 17:62）。
- **新增 `tools/skill-report.mjs`** → 自包含 HTML 报告（`docs/skill-report-3p.html`）：
  并排画「实际使用率（原生经济）」×「实际强度（富裕经济 A/B：自由发挥 vs 强制只用某技能）」，
  自动判定：坑（常用却亏）/ 主力 / 没学会的强招 / 死技能。

## v1.3.12 — 多人侧也改多目标择优 + 收紧容差

- `server/train-server.mjs` 两条路径都改多目标：`runTrain`（2 人）逐种子收集 `{wr, div}` 后按容差带取最发散；
  `runTrainN`（多人）名人堂 `evalN` 后同处理，新增 `multiObj` SSE 事件 + 服务端日志。
- `WR_TOL` 0.03 → **0.01**。
- **实测（3 人 1500 代，<120s）**：`[multiObj] 候选=6 容差带=5 选中 divNorm=0.517 种类=9`；
  多样性 **有效技能数 3.21 → 5.55（+73%）**，最高 ep 3 → 7；
  分布 ジ41.2 / 枪12.2 / 反弹12.1 / 坦克12.1 / 防御10.1 / 八卦阵9.4。
- ⚠️ **但这是真实权衡**：头对头 A×2:B×1 = 43:45（持平）、A×1:B×2 = **17:62（明显输）**、1st 40.6%。
  故：3P 冠军回滚到 v1.3.11 的强版本，多样性版存 `docs/artifacts/champion-3p-div555.bak`。

## v1.3.11 — 多目标择优（胜率容差带内取最发散）+ 2 人冠军重训

- **根因定位**：窄化不在 fitness（`scoreMember` 与 v4-era **逐字未变**），而在**「按胜率择优」这一步**——
  argmax(胜率) 必然挑中最强也最窄的个体。实测同一份 fitness：按胜率择优 2.52 有效技能 / 按适应度分选 3.25。
- `evo.js` 新增 `champEntropy(params, temp, games, seedBase, n)`（动作分布 H/ln#技能，2P/N 通用）；
  `pickChampionByWinRate` 改为多目标：按胜率分圈出「≥ 最高分 − `WR_TOL`」的容差带，带内取 `divNorm` 最高者。
- `tools/train-best.mjs` 同口径；打印候选对照表 + 实际胜率损失；`divNorm`/`distinct`/`wrTol` 写进 meta。
- **2 人冠军重训（3 候选 × 400 代）**：新 **avg 99% / 有效技能数 3.82 / 种类 8**，
  旧 97.4% / 2.52 / 6；**head-to-head 新 86 : 14 旧** → **两轴同时改善，无权衡**。
  分布：ジ56.8 / 狙击枪19.5 / 藤甲11.1 / 反弹3.1 / 八卦阵3.0 / 防御2.9 / 枪2.8 / 坦克0.9
  （旧：ジ54.8 / 枪36.5 / 狙击枪8.4 —— **枪 36.5%→2.8%**，腾出的份额分给了长尾）。

## v1.3.10 — 定位窄化根因（按胜率择优）+ 藤甲质疑的验证

- 用户要求"按新计划做前三步"：
  1. **diff v4-era `scoreMember` vs 当前 → 逐字未变**，窄化不是 fitness 改的（我上一轮的判断错了）。
  2. 用当前代码复训 600 代（`train-fast`，按 shaped 分选）：有效技能数 **2.52 → 3.25**，种类 6 → 8，
     且**自发学会聚能环（18%，最高 ep 25）**。
  3. 结论：**窄化来自「按胜率择优的冠军选择」**——它必然挑中最强也最窄的个体。
- **藤甲质疑验证**（用户："冠军常用藤甲，但不用坦克/天火，那 1 ジ 白费"）：
  查清 `rules.js:51` **反弹费用 0 ジ**、`rules.js:55` **藤甲费用 1 ジ 且防御效果同反弹**（目标为敌人，额外给对手火弱）。
  → **藤甲在防御上严格劣于反弹**，多付的 1 ジ 只买到"对手火弱"，而冠军坦克仅 0.9% → **几乎从不兑现，确实是浪费**。
  决策性验证：强制把藤甲换成反弹，avg **97.6% → 98.3%**（+0.7pt，在 ~1.8pt 噪声内但方向一致）。
  成因是**策略盲点**（网络学不到"反弹严格支配藤甲"），不是 fitness 问题。

## v1.3.9 — 分段 ep shaping（用户的奖励设计）+ 更正"环支配"结论

### 更正：v1.3.8 说的"聚能环是支配策略"是**我的奖励被 hack**，结论作废
v1.3.8 我加的 `engine = 0.06 * min(1, epGain/12)` 按**"获得的 ep 总量"**发钱；
而聚能环 +3/回合、ジ +1 → **等于给"刷环"发三倍工资**；再叠加 `save`（max ep）项，
囤到 ep=204 也照样满分。用户指出后才定位到：所谓"永动刷环"不是环支配，是**奖励被 hack**。

### 按用户设计改为分段 shaping
```
0 ~ 4 ep   → 攒钱奖励（鼓励手上有货，够放 2~5 ジ 技能）   STOCK_BONUS = 0.05
5 ~ 10 ep  → 不奖不罚（中间区交给策略自己权衡）
11+ ep     → 囤积惩罚（资源积着不转化 = 浪费）           HOARD_PEN = 0.12
```
`engine`（ep 收入奖励）已撤除。

### 重测（regen=2，1500 代，同经济评测）
| 指标 | 旧 engine 奖励 | 分段 shaping |
|---|---|---|
| 最高 ep | **204** | **2** |
| 环最长连用 | 16 | **0** |
| 贵技能(≥2 ジ)出手占比 | 16% | **100%** |
| 有效技能数 | 1.73 | 1.84 |

- **囤积治好**（204→2），且**每次出手都是 ≥2 ジ 的技能**：资源真正转化成攻势。
- **环不再被使用**：分段下"攒够就花"优于"投资环永动"，反证前一轮的刷环是奖励 hack。

### 口径修正（重要）
服务器终局验证 `evalN` 走**标准经济**（未传 `opts`），而该轮训练用 regen=2
→ 报出的 1st（20% / 36.8%）**不可读**。要正经评估 regen 经济须让评测与训练同经济，
并给 UI 加 regen 开关。

### 本版状态
- 机制（`opts.regen` / `state.epRegen` / `startTurn` 回能）与分段 shaping **保留**，
  默认 `TRAIN_REGEN = 0` 与线上规则一致；2 人局不受影响（spec 36/36、np-test 30/30）。
- **冠军回滚到 v1.3.7 的 β=0.60 版**（对脚本对手对 1st 59.4%）。
- 探针临时 profile 目录现在**退出时自删**（`rmSync(udd)`），不再堆在 %TEMP%。
## v1.3.8 — 每回合回 ep / 聚能环实测否决 + 探针进程卫生修复

### 1. 探针进程卫生（用户报告：我一直在误伤机器上的其它 chrome/node）
- **停止全局杀进程**。`Get-Process chrome|node | Stop-Process -Force` 会误伤用户自己的服务，
  以后一律不用。
- 新增 `tools/cleanup-epirus.ps1`：**定向**清理 —— 只杀命令行含 `epirus-` 临时 profile 的 chrome，
  以及监听探针 CDP 端口（9349/9351/9353/9337）**且命令行确为 epirus** 的进程；顺带清 `%TEMP%\epirus-*`。
  首次执行清掉 **79 个**残留临时目录，孤儿 chrome 已为 0。
- 四个开 Chrome 的探针（`smoke.mjs` / `np-probe.mjs` / `remote-probe.mjs` / `multi-train-probe.mjs`）
  改为**脚本内自限时** `--max-ms=N`（默认 240s）+ `taskkill /PID <pid> /T /F` 收进程树。
  根因：以前用 shell `timeout` 包 node，超时是杀 node 本身 → `finally` 里的 `proc.kill()` 根本没执行
  → 探针起的 headless Chrome 变孤儿。现在到点由脚本自己先收尸再退出。

### 2. 每回合回 ep（机制就绪，默认关闭）
- `js/core/state.js` `createState(modeKey, rng, n, opts)` 支持 `opts.regen` → `state.epRegen`；
- `js/core/resolve.js` `startTurn` 按 `state.epRegen` 给存活者发 ep；
- `js/train/evo.js` `oneGameN(..., opts)` 透传 + `TRAIN_REGEN`（默认 0）。
- **2 人局不受影响**（默认 0）→ spec 36/36、np-test 30/30。

### 3. 聚能环：能学会，但它是支配策略（实测否决默认启用）
机制（已核实）：`state.js:95` 首次 3 ジ、**之后永久 0 费**；`resolve.js:508` 收益 `ringStreak` 递增
（+1→+2→+3→+3…）。**第 3 次起每回合白拿 3 ep = ジ 的 3 倍**，第 4 回合起严格领先。

| regen | 最高 ep | 环最长连用 | 有效技能数 | 分布 |
|---|---|---|---|---|
| 0（标准） | 1 | 0 | 3.02 | ジ49 / 枪35 / 藤甲14 / … |
| 1 | 1 | 0 | **1.00** | **藤甲 100%** |
| 2 | 204 | **16** | 1.73 | ジ82 / 聚能环16 / 雷击之枪2 |

- **regen=1 无效**：+1/回合只够每回合放一个 1 ジ 技能，经济锁换了个不动点（藤甲 100%），仍攒不起 3 点启动环。
- **regen=2 能解锁**：AI 确实学会聚能环并连用到 16 连 —— 证明**可学**。
- **但环是严格支配策略**：学会后就是"永动刷环"，有效技能数 3.02 → **1.73**，**与拓宽分布的目标冲突**。
- 故默认不启用 regen、不采用 regen 混合训练的冠军（混合版 1st 51.9% < 57~59%），**已回滚到 v1.3.7 冠军**。
- **建议**：要让环成为"可选项"而非必然最优，需先动平衡——加持续费用 / 限制连用收益上限 / 降 +3 档。
## v1.3.7 — 技能覆盖熵：抑制策略坍缩，拓宽技能分布

**问题**：v1.3.6 的冠军只用 2 种技能（ジ 67.7% + 激光剑 32.3%），有效技能数 exp(H)=1.88。
用户指出：训练池里冠军是权重 1.5 的对手，**冠军窄 → 其他对战 AI 也窄**，会自我强化掉进坑里。

**诊断先行**（新增 `tools/skill-diag.mjs`）——分清"买不起"与"买得起也不选"：
强制把 ep 补到 6 后，冠军只是**换一招 spam**（激光剑→狙击枪 74.2%），有效技能数 1.88→2.06。
⇒ **不是经济问题，是策略坍缩**：softmax 动作价值 + 低温贪心，目标函数里只有胜负、
没有任何"广度"压力 ⇒ argmax 必塌到一招。

**确诊的三个加重因素（含自认的错）**：
1. 上一版我加的 `hold`+`conv` 奖励**literally 在为「ジ→激光剑」这一个循环付钱** —— 窄是我塑造的；
2. 训练池 `buildOpps` 里 `champion` 权重 **1.5** ⇒ 自己打自己，越练越尖；
3. 播放温度 0.15 的贪心放大坍缩。

**改动**：目标函数加**技能覆盖熵** `div = DIV_BETA * H(个体动作分布)/ln(#技能)`（唯一不指向特定循环的
广度信号）；经济奖励由"特定循环"改为**按费用分档**（`0.08*heavy≥2 + 0.08*heavy≥4`，让大雷比激光剑更值钱）；
拆掉 `hold`。

**效果**（DIV_BETA=0.60，1500 代 × pop16 × gpo8，fresh，**76 秒**）：
- **有效技能数 1.88 → 3.22；用到技能种类 2 → 9**
  （ジ 59.7 / 激光剑 26.1 / 反弹 3.6 / 枪 3.4 / 防御 3.0 / 八卦阵 2.7 / 坦克 1.1 / 雷击之枪 0.3）。
- 多样性代价约 4pt（1st 60.1%→56.5%），但 β=0.60 对 β=0.30 head-to-head **88.9% / 64.7%**、
  对脚本对手对 1st **59.4%**，是三者最强。
- 回归：spec 36/36、np-test 30/30、smoke OK、np-probe 16/16。

**天花板（关键结论，供规则决策）**：β 翻倍后有效技能数几乎不动（3.39→3.22）⇒ 熵奖励在 ~3.3 饱和。
两个原因**都不是奖励能解决的**：
1. **ジ 占比 ~60% 是算术必然**：`js/core/resolve.js:501` 的 `case SK.JI: me.ep += 1` 是唯一 ep 收入
   （无每回合自动回能），稳态下每次出手花 c ジ就要先出 c 次ジ ⇒ ジ 占 c/(c+1)。
2. **富经济下少数攻击技碾压**（坦克 92.2% / 激光剑 63.3%+聚能环 28.4%）⇒ 长尾技能不会被大量使用，
   属**平衡性问题**。

**建议的下一步（需用户裁决，未擅自改规则）**：每回合自动 +1 ep（或 ジ 一次 +2）以压低ジ占比；
或调整坦克/狙击枪/激光剑的富经济强度；训练侧可把 `champion` 自对弈权重从 1.5 降到 ~1.0。
## v1.3.6 — 多人 AI 学会攒钱（打破"1 ジ 枪黑洞"）

**问题**（千问诊断 + 本地复现）：3P 冠军 6701 个决策点 ep 中位数 0 / 90 分位 1 / **最大 1**，
永远在 `ジ→枪→0` 循环，2 ジ 以上技能**永久不可负担** → 28 个技能只用到 6 个，
v5 的 122 维特征里"前摇/蓄能珠/大雷"相关维度基本是死输入。

**关键教训：这是探索瓶颈，不是奖励问题。** 连续三轮实验：
1. 加 `save`（奖励"攒到过的最高 ep"）→ 失败，反而学会**囤积**（max ep 冲到 58）而永不花钱。
   期间还暴露一个自身 bug：`R.byKey[key].cost` 是**数字**不是对象，`cost.ep` 恒为 undefined，
   导致"贵技能出手"计数恒 0——修成 `S.computeCost(state,pid,key).ep`。
2. 加 `conv`（奖励贵技能出手，0.15 主力）+ 压低旧口径 → 仍失败，max ep 死守 1。
3. 加 `hold`（奖励"ep≥1 时不花钱"，本该是稠密信号）→ 仍失败。
   原因：`policyChooserN` 只在**可负担**技能里 softmax 采样，而策略在 ep≥1 时 99.8% 选枪，
   "不花钱攒钱"这个动作**根本采样不到** —— 奖励再大也没有梯度。

**解法：ε-探索。** `policyChooserN(params, temp, eps)` 新增 ε-greedy（训练 eps=0.15，
评测/UI 不传 eps → 行为不变）；fitness 配平 `save 0.02`（饱和于 ep=2，不可靠囤积刷分）
+ `hold 0.03` + `conv 0.15`，旧的 proact 0.06→0.02、deal 0.03→0.01（旧口径实质在惩罚攒钱）。

**结果**（1500 代 × pop16 × gpo8，fresh，**89 秒**，16 worker）：
- **max ep 1 → 2；贵技能(≥2 ジ)出手占比 0% → 32.3%**；ep 分布 ep0 34% / ep1 68% / ep2 100%。
- 自发出「ジ 攒钱 → 激光剑（2 ジ，穿反弹）」循环（ジ 67.7% + 激光剑 32.3%）。
- 对旧冠军 head-to-head：**A×2 vs B×1 = 49:0（100%）**、A×1 vs B×2 = 28:22（56%）。
- 对 45 个脚本对手对 1st 51.9%（旧 53.3%，在 ±3pt 噪声内），服务器终局验证 1st **60.1%**。
- **仍存**：只用 2 种技能（多样性反而下降）；3/5 ジ 档（大雷、电磁炮）仍未触达。
  更深经济需分层奖励（按费用分档）或更高 ε —— 列为下一步。

**新增工具** `tools/econ-eval.mjs`：冠军经济画像（ep 分布 / 技能分布 / 种类数）
+ 对脚本对手对 1st 率 + **新旧冠军同局 head-to-head**（避免拿不同对手对的数字横比）。

**回归**：2 人 spec 36/36、N 人 np-test 30/30、smoke OK、np-probe 16/16。
## v1.3.5 — 多人同优先级改为「快照同时结算」+ 规则文档对齐

- **座位偏置根因修复**（`js/core/resolve.js`）：原本同优先级内一律按座位号（pid）升序结算，
  先结算者会把后者作废 → 低 pid 在"同归"局面里系统性免伤。
  - 大雷层与 `clashPass` 改为**层入口拍快照、层内按快照判定、最后统一落地** void 与伤害。
  - 实测：三方互放大雷（P0→P1 / P1→P2 / P2→P0）旧口径 HP **3/1/1**（P0 毫发无伤）→ 新口径三人各吃
    直击 2 + 连带 2 = 全灭；3× 同一策略（破防脚本）座位 1st 率 **73.5 / 14.5 / 0.0 → 32.9 / 31.5 / 35.7**。
  - 结算起点仍按回合轮换（`turnOrder`）；本轮 A/B 证明快照落地后轮换已是 no-op，保留作为其余层的保险。
  - **2 人局保持恒等顺序 → 行为不变，spec 仍 36/36**（v1.0 冻结不受影响）。
- **回归测试**（`tools/np-test.mjs` 28 → **30**）：新增「N2 同优先级同时结算：三方互放大雷 → 人人挨打」
  与「N2 座位偏置：3× 同一策略各座位 1st 率接近均等」。
- **新增诊断工具** `tools/seat-diag.mjs`：同策略座位 1st 率 / 三方互放大雷事件流 / 冠军 ep 与技能分布。
- **规则文档修正**（`docs/RULES-NP.md`）：
  - **N2**：删去"同优先级按 pid 升序、除小雷链外不影响结果"的错误说法，改为快照同时结算并附实测数据。
  - **N6**：连带伤害由"各受 **1 点**"改为与代码一致的 **2 点电伤 + 行动作废 + 禁用 3 回合**
    （按用户裁定：大雷强度与 5 ジ 费用不动，以代码为准），并标注"被 T 使用技能者是否该作废"的可争议点。
  - **N12**：补"全员 HP≤0 但有人挂 `reviveNext` → 该人判胜"的边界裁定（约 2/1500 局命中）。
  - **N19**：加"表格每行对手组合不同、不可横向比大小"的读数警告；说明座位率是跨对手对平均、
    掩盖单组摆动；补镜像对局全平对样本的筛选效应；双枪/镜面 0% 的真实原因是**经济锁死**而非权衡放弃。
- **新发现（待修，已写入 N19）**：3P 冠军 6701 个决策点 **ep 中位数 0 / 90 分位 1 / 最大 1**，
  永远在 `ジ→枪→0` 循环，2 ジ 以上技能永久不可负担 → 28 个技能只用到 **6 个**；
  成因是 fitness 的 `deal`/`offensive` 项。富裕经济 A/B：双枪 −26.8pt、镜面 −31.3pt（净负），
  大雷被喂饱时最强（77.4% vs 基线 52.8%）。
## v1.3.4 — 网页训练场支持多人训练（3/4/5 人）

- 训练场新增「人数」下拉（2 人 / 3 人 / 4 人 / 5 人）。选 2 人走原 1v1 训练，选 3~5 人走多人名次制训练。
- `server/train-worker.mjs` 新增 `{type:'evalN'}` 分支，worker 内解析对手函数名后调 `T.scoreMemberN`。
- `server/paralleltrain.mjs` 新增 `makeParallelEvalN(T, opts)` → `{ evalPopN(pop, gen, games, n, oppNames), workers, close() }`，**种群按 worker 数均分并发**，池不可用时回退串行。
- `server/train-server.mjs`：`/train?…&n=`（n>2 走 `runTrainN`）、`/champion?n=`、`/reset` 一并清多人状态；`BUNDLE_MP = 'js/bundled-champion-3p.js'`（2/3/4/5 人共用同一网络，特征与人数无关）；`writeBundleMP` 写 `window.EPIRUS_CHAMPION_3P*` 并刷新 index.html 全部 `?v=`。
- `js/ui/ui.js`：远程训练读 `#tr-n`，多人时 pop 默认 32 / gpo 默认 20；SSE 新增 `start/gen/done` 的 `n>2` 分支（图表显示 best 单曲线 + 1st/top2 实时值），done 时写入 3P 冠军并清 localStorage，困难档立即生效。
- 修复：对手函数名映射原先用字符串拼接生成，`antidef` → `pickAntidef` 大小写错误，导致终局验证抛 `sel is not a function`。改为显式 `BOT_FN_N` 表。
- 实测（16 worker）：3 人 8 代 × pop8 × gpo6 含终局验证 7.5 秒完成，SSE 事件流正常，冠军正确写盘。
# 更新日志

## v1.3.3 — 多人困难档真的用上 3P 冠军了 + 本局复盘

**2026-09-10**

用户反馈「这两个 AI 仍然在互相对打」——查证后发现是真 bug，且是我们自己的两个疏漏叠在一起。

### 根因（两个疏漏叠加）

1. **上一轮的补丁没写进文件**：把「多人困难档改用 3P 冠军」的代码加进 `js/ui/ui.js` 时，
   辅助函数 `rep(s, old, new)` 的返回值**忘了赋值回 `s`** —— 断言通过、脚本打印 "ok"，但改动被丢弃。
   于是多人的困难档一直还是 `Bots.pickAdaptive`（脚本），脚本用的 `pickTargetFor` 又是**没有反锁逻辑**的
   「打血量最低的对手」→ 又回到两家互为目标、每回合互相抵消。
2. **探针检查太弱**：`np-probe` 只断言了「`window.EPIRUS_CHAMPION_3P` 已定义」（= 脚本加载了），
   没断言「AI 真的在用它」→ 没抓到上面这个问题。
3. **附带的缓存风险**：`js/bundled-champion-3p.js` 是唯一**没有 `?v=` 缓存戳**的脚本，
   浏览器可能继续用旧的 v4 冠军包（f=62）→ `checkPack` 拒绝 → 回退脚本。

### 修复

- `js/ui/ui.js`：`chooseAIMulti` 困难档改为**真正加载并使用 3P 冠军**（`loadMultiChamp`）；
  localStorage 里的旧包若不兼容，**回退内置包**（此前会直接判失败）。
- `pickTargetFor` 改为委托训练器 `EpirusTrainer.pickTargetN`（反锁 + 必杀优先 + 打领先者），
  **脚本回退路径也一并享受反锁**。
- `index.html`：3P 冠军包加 `?v=` 缓存戳。
- **新增「对手 AI」显示**：新对局日志会写明 `对手AI=3P 冠军` 或 `脚本·自适应（冠军包缺失/不兼容）`，
  训练钩子 `window.EpirusUI.aiInfo()` 可供探针断言。

### 本局复盘（用户第二个需求）

- 结束面板新增 **「📜 查看本局复盘」**：不重开对局，弹出可滚动逐回合记录，附「导出记录 / 关闭 / 再来一局」。
- 对战工具栏新增 **「上局记录」** 按钮：从 localStorage 读上一局文本（**点过「再来一局」也还在**）。

### 探针加强（防止同类问题再溜过）

- 断言 **困难档实际使用 3P 冠军**（`aiInfo().champ === true`），不再只看「包已加载」。
- 断言对局日志里 **「相抵」出现次数 ≤ 2**（不出现互相抵消死循环）。
- 断言 **本局复盘面板可打开**。
- `np-probe` 11 → **16/16**。

### 真实浏览器实测（3 人 · 困难）

`results/epirus-3p-hard-live.txt`（玩家全程只出ジ）：**相抵 0 次**；
第 7 回合两家同时枪击玩家1 → 各中一枪、玩家1 死亡（☠ 有事件）；结果 22 回合玩家2 获胜；
显示统一为「玩家1/玩家2/玩家3」，不再出现「电脑」或数字 `2`。

## v1.3.2 — AI 状态特征 v5（对手槽位 + 技能历史）+ 显示/目标修正

**2026-09-10**

用户复查对局记录后提出三件事，全部处理。

### 显示统一

- 事件行 `nm(pid)` 原先对 pid1 写死「电脑」、pid2 直接打数字 `2` → 改为统一取玩家真实名字。
- `buildBattleText` / `rebuildLog` 原先用 2 人模板（`你=【…】 电脑=【…】`）拼多人回合行 → 多人改用整行记录。

### 目标选择 v2（消除「互相抵消死循环」）

- 记录里两个困难 AI 都用「打血量最低的对手」→ 血量错开时彼此成为对方的最低血 → **互为目标 → 每回合互相抵消**，
  第三家白嫖ジ（60 回合 0 推进）。新口径：① **反锁**（上回合与该对手互为目标而相抵 → 本回合不再打他）；
  ② 能一击必杀先杀；③ 否则打**血量最高的领先者**；并列随机。
- 实测（玩家只ジ + 两个困难 AI）：相抵 **1 次**、消极玩家第 **9 回合**被打死、**14 回合**结束（原 60 回合）。

### 大雷连带修正（按原文效果 3）

- 连带伤害 1 → **2 点电伤**（与直击同口径）；被连带者的技能同样**作废 + 禁用 3 回合**（防御/反弹/金刚盾/ジ 豁免）。

### AI 状态特征 v5（PACK_VERSION 4 → 5）

- 原 62 维是 2 人形状：对手信息全是聚合量（minHp/maxEp/any/sum），只有「ジ最多的那个对手」的 lastSkill 可见，
  且**完全看不到历史**。
- 新增 **每个对手一个槽位**（4 槽 × 10 维 = 40）：存活、各自 HP、各自ジ、各自电/爆珠、lastSkill(索引+类别+空位)、
  穿透/大雷前摇；槽位按「存活 → 血量升序 → pid」稳定排序，槽 0 永远是最可能被杀的。
- 新增 **最近 3 步技能历史**（自己 + 4 槽位，各 3 步 + 「连续同招比例」= 20 维）。
- `FEAT_S` 62 → **122**，参数 1873 → **3313**；2 人 / 5 人同长度。旧冠军包（f=62）不兼容 → **2 人与 3P 冠军均已重训**。
- 对手池新增 `farmer`（只攒资源不进攻）。

### 重训结果

- 2 人冠军：19 个基准 **avg 97%**（wall / defend 100%，最低 mix 75%）。
- 3 人冠军：1st **49.9% / top2 71.8%**（36 对 × 20 局 = 720 局）；出招明显转攻
  （枪 40.2%、ジ 44.0%、防御架势合计 ~13%，此前 ~29%）。
- 脚本基线（同赛制）：aggro 60.4% / balanced 51.0% / mix 43.9% / defend 26.8% / random 9.0% / **farmer 2.4%**。

### 自测

- 2 人 `tools/spec-run.mjs` 36/36；N 人 `tools/np-test.mjs` 28/28；多人页面 `np-probe` 14/14；2 人 `smoke` OK。

## v1.3.1 — 多人规则修正（目标/相抵/大雷连带/死亡）+ 3P 冠军重训

**2026-09-09**

用户复查 3 人局对局记录（`results/`）后指出「技能目标存在巨大问题」。定位到 3 个引擎缺陷并修复，
多人生态随之大幅改变，3P 冠军已重训。

### 引擎修正

- **N4 相抵必须“互为目标”**（原文档口径，代码漏实现）：初版 `clashPass` 只比优先级、**完全不看目标**，
  导致「两家同时打第三人」的攻击互相抵消（记录里 P2/P3 同时枪击玩家1 → 双方攻击相抵、玩家1 毫发无伤）。
  现在只有 A→B 且 B→A 的攻击才交锋（同优先级相抵、高优先级阻止）；两家同打同一第三人 → **两击都落地**。
  2 人时“互为目标”天然成立 → 行为与 v1.0 完全一致（回归 36/36）。
- **N6 大雷连带伤害**（原文效果 3，此前误裁定为“不产生连带”）：
  与目标 T 发生交互的第三方也吃连带 ——「对 T 使用技能者」受 1 点电伤且该攻击被无效化；
  「被 T 使用技能者」受 1 点电伤；施法者自身不参与。2 人时无第三方 → 行为不变。
- **N12 死者不再行动**：`attemptAction` 直接拒绝已淘汰者；`startTurn` 每回合重置行动槽
  （防死者沿用上回合行动）；`autoGameN` / UI 的 AI 跳过死者；死亡写 `death` 事件便于复查。

### 训练

- 规则变化后重训 3P 冠军（`node tools/train-3p.mjs 500 3 20 32`）。

### 实测（3 人，28 对手对，座位轮换，temp0.15）

| 主体 | 1st | top2 |
|---|---|---|
| **3P 冠军** | **60.9% / 68.1%（独立复核）** | **78.0% / 81.9%** |
| 脚本 aggro | 58.6% | 84.1% |
| 脚本 balanced | 55.0% | 85.5% |
| 脚本 mix / defend | 39.6% / 29.1% | 69.5% / 68.6% |
| 脚本 random | 8.4% | 52.1% |

- 生态对比（修规则前 → 后）：random 20.4%→8.4%、aggro 49.6%→58.6%、**冠军 51.3%→68.1%**。
  集火不再自我抵消后，比赛更致命、进攻型主体全面上移。
- 出招分布：ジ 37.1%、枪 22.4%、贴贴 10.6%、反弹 9.7%、防御 9.3%、八卦阵 9.2%。

### 自测

- `tools/np-test.mjs` 19 → **26/26**（新增 N4 / N4b / N4c / N12 / N12b / N6 / N6b）。
- 2 人 `tools/spec-run.mjs` 仍 **36/36**；2 人页面 `smoke` OK；多人页面 `np-probe` 全绿。

## v1.3.0 — 多人（3 人）自对战训练 + 3P 冠军

**2026-09-09**

按 N19 落地多人训练：多对手聚合特征 + 名次适应度 + 座位轮换 + 名人堂验证择优。

### 训练

- `js/train/policy.js`：`oppAgg(state, pid)` 把“对手”改成**多对手聚合**（minHp / maxEp / any() / sum()），
  特征向量长度与顺序不变（N=2 退化为原值 → **2 人冠军包仍兼容** f=62 / 1873 参数）；
  `ACT_KEYS` 扩到全 30 技能（含双枪 / 镜面），参数维度不变；修复 `chainLink` 改名后失效的铁索特征。
- `js/train/evo.js`：新增 `scoreMemberN` / `oneGameN` / `rankOf` / `evalN` / `policyChooserN` / `wrapBotN` / `pickTargetN`；
  **名次适应度 1.0 / 0.3 / 0.0**（初版 0.6 / 0.2 会奖励“苟到第二”，实测把 1st 率从 52% 压到 42%）；
  目标选择在血量并列时**随机**（消除脚本固定打 pid 最小者造成的座位假优势）。
- `tools/train-3p.mjs`（新）：N 人进化训练，支持热启动、名人堂、终局用全部 28 对手对重新验证择优。
- `tools/eval-3p.mjs`（新）：28 对手对 × 座位轮换 → 1st / top2 率 + 出招分布。

### 结果

- **3 人冠军：1st 51.3% / top2 74.3%**（28 对 × 25 局 = 700 局，座位轮换，temp0.15）。
- 同赛制脚本基线：aggro 49.6% / balanced 41.1% / mix 39.1% / wall 27.9% / defend 26.3% / random 20.4%
  → **冠军已超过最强脚本**；随机基线 33.3% / 66.7%。
- 座位 1st 率 P0 54% / P1 47% / P2 53%（无座位偏差）。
- 出招分布：ジ 25.4%、枪 22.6%、反弹 17.0%、防御 16.8%、八卦阵 16.7%。
  **双枪 / 镜面 0%** —— 名次制 3 人局里“3 ジ 换 2 点分散伤害”不划算（涌现结果，非 bug）。

### UI

- 多人局**困难档改用 3P 冠军**（包缺失 / 不兼容时回退脚本自适应）；`index.html` 加载 `js/bundled-champion-3p.js`。

### 自测

- `tools/np-test.mjs` 18 → **19/19**（新增 3P 冠军包兼容性）。
- `tools/np-probe.mjs` 12 → **14/14**（新增 3P 冠军加载 + 困难档多人对局）。
- 2 人 `tools/spec-run.mjs` 仍 **36/36**。

## v1.2.0 — 多人技能补齐（镜面反射 / 聚光炮 / 反复横跳 / 合二为一）

**2026-09-09**

多人（3~5 人）的**可选技能与隐藏技能全部实现**；2 人对局行为不变（引擎自测仍 36/36）。

### 引擎

- `js/core/rules.js`：新增复合伤害类型 `DMG.FIRELIGHT`（光&火）；`AVAILABLE_MULTI` 恢复全技能（含镜面反射）。
- `js/core/resolve.js`：
  - **镜面反射**（N14）：两个目标（复制对象 t1 / 输出对象 t2），复制 t1 本回合的**伤害**技能并由使用者打向 t2；
    复制双枪只算一枪；架势 / 自增益 / 状态类不可复制；复制体来源 = 使用者（反弹 / 地雷按使用者结算）；
    在优先级 3 之后结算，不重排优先级。
  - **聚光炮**（N16）：两人互镜且输出同一人 → 该人受 1 点光&火。
  - **反复横跳**（N15）：镜面 (t1→t2) 边成环 → 所有使用者各受 1 点光&火（与聚光炮互斥）。
  - **合二为一**（N17）：≥2 人同时用小雷打同一目标 → 目标额外 1 点电伤。
  - 藤甲火弱 / 吸血鬼光弱统一在 `rawDamage` 结算（聚光炮 / 反复横跳等直扣血也吃加成）。

### UI

- 镜面反射**两级目标弹窗**（“选择复制对象 / 选择输出对象”）；新增 mirror / mirrorNoEffect 与隐藏技能日志文案。
- 多人专用技能标签由「未实现」回到「多人模式」。
- 新增测试钩子 `window.EpirusUI`（供 `tools/np-probe.mjs` 驱动）。

### 自测

- `tools/np-test.mjs` 12 → **18/18**（新增 N14 / N14b / N15 / N16 / N17 / N18 用例）。
- `tools/np-probe.mjs` 11 → **12/12**（新增镜面反射两级弹窗）。
- 2 人 `tools/spec-run.mjs` 仍 **36/36**（回归不变）。

## v1.1.0 — 多人（3~5 人）框架

**2026-09-09**

2 人对战行为不变（引擎自测 36/36 回归全绿），引擎与 UI 推广到 3~5 人。

### 引擎

- `createState(modeKey, rng, n)`：N 人状态（`p[]` / `actions[]` / `n`），`n` 缺省 = 2。
- **显式目标**：技能目标写在 `action.target` / `target2`；`resolveTarget` 在 2 人时 = 另一个，N 人时取指定目标（非法则退化为第一个存活对手）。
- `resolve.js`：全部 `1 - pid` → 显式目标；结算管线遍历 N 个行动；`clashPass` 改**两两相抵**；铁索改**边集合**（`p.chains`，不递归）；`checkOver` 改 N 人（最后存活胜 / 全灭平局 / 回合上限血最高者胜）。
- `rules.js`：新增 `multi` 模式（3~5 人，3 血，全技能）。
- **双枪射手**：实现（两个目标各 1 点，按枪口径可被反弹 / 触发地雷）。
- 镜面反射 / 聚光炮 / 合二为一 / 反复横跳：**本版未实现**（技能表置灰，见 RULES-NP N11）。

### UI

- 对局面板新增**人数下拉（2/3/4/5）**；N>2 时强制 `multi` 模式。
- 多人面板堆叠渲染（每人 HP/ジ/珠/状态）；**目标选择弹窗**（双枪为两级）。
- 人类玩家被淘汰后**自动观战到结束**；结算面板支持 N 人。

### 自测

- 新增 `tools/spec-run.mjs`（2 人引擎 36/36，Node 桩）、`tools/np-test.mjs`（N 人 12/12：目标/小雷逐边/铁索不递归/双枪/胜负/fuzz）、`tools/np-probe.mjs`（真实 Chrome 3 人 + 5 人 11/11，无 JS 异常）。

### 文档

- `docs/RULES-NP.md` 升为 v0.2（已实现），记录 N1..N13 裁定与实现状态。

## v1.0.0 — 2 人对战完整版（功能冻结）

**2026-09-09**

首个正式版本。2 人模式功能冻结，后续开发转向多人（3~5 人）框架。

### 引擎

- 28 个可用技能 + 4 个隐藏技能（铁索连环 / 吸血鬼公爵 / 回魂 / 梦魇）全部实现；
  多人专用技能（双枪射手 / 镜面反射 / 聚光炮 / 合二为一 / 反复横跳）置灰保留。
- 裁定版规则 `docs/RULES-2P.md`（R1..R55 + 子证），代码注释逐条对应。
- 优先级管线 / 防御矩阵（防御·反弹·八卦阵·金刚盾·原型制御·藤甲·全息屏障）/ 地雷链 / 转移伤害 /
  挑衅义务 / 符咒引爆窗口 / 大雷只禁"目标当回合使用的技能"。
- **无贷款**：资源不足 → 技能作废，不扣血、不消耗资源。
- **蓄能珠只供下一回合**：回合结束时未使用即清空（R9'，原规则"供给下一回合"）。
- **铁索连环**按文档口径共享火焰（天火）与爆头伤害。
- **绝不空放兜底**：脚本/AI 返回买不起或不合法的招一律改按ジ，避免对手 `lastSkill=null` 掉进未训练区；
  同时 `policy.js` 为"上一招无效"补了显式特征，池内 `whiff` 对手专门训练该状态。

### 对局

- 标准模式（3 血 · 28 技能），三档难度：简单 / 中等 / 困难（冠军策略）。
- 同时行动、回合日志逐条结算、技能按钮按资源实时置灰、「悔一步」（每局一次）、导出对局记录。

### 训练

- 零依赖 Node 训练服务（`worker_threads` 多核并行 + SSE 实时进度），页面一键启动。
- 对抗进化（类 GAN 思想）：19 个脚本基准，含三条人类式硬门槛（坦克线 / 重火力 / 防御+枪三拍）、
  原型制御墙、空放型、三条节奏型（反弹→枪→坦克 / 反弹→反弹→坦克 / 防御→反弹→枪）。
- 冠军择优：任一基准不高于 50% 直接失去资格，过门后按 `0.5×平均 + 0.5×最差对局` 排序。
- 多种子 × 多轮 + 种子谱系（保留前 ⌈√n⌉ 名作父代，按"越靠前分得越多"分配新种子）。
- 评测口径与实战一致（双方均基于行动前状态决策）。
- 内置冠军：19 个基准全部 ≥80%，平均 96%。

### 自测

- `tests/spec.html`：**36 条规则场景用例 + 500 场随机对局无异常**。
- `node tools/smoke.mjs`：CDP 驱动真实 Chrome 走完整对局与训练页。
