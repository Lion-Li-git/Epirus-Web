## v1.3.39 — 新增 `tools/repro-check.mjs`：可复现性看门狗（千问建议）

### 它做什么
一条命令检查三件事（跑完自动还原冠军文件）：
- **A** 同 seed 连跑两次 → 权重哈希应**相同**
- **B** 同 seed、worker 数 2 vs 8 → 权重哈希应**相同**
- **C** 不同 seed → 权重哈希应**不同**（这条专防"种子其实没生效"的**空验证**）

⚠️ 只比对**权重数组 `a`** 的哈希，**不比对整文件**——冠军 META 里有 `ts: new Date().toISOString()`，
整文件哈希每次必然不同。这正是我早先把"训练不可复现"误判成真问题的原因（v1.3.36）。

### 首次运行结果
```
A1/A2 同 seed 两遍     权重sha=f5735e5637572a7c  ✔
B1/B2 workers=2 vs 8   权重sha=f5735e5637572a7c  ✔
C     不同 seed        权重sha=0786d1f021f60ec9  ✔ 种子确实生效
REPRO-CHECK OK
```

### 写这个工具时连踩三个坑（都记下来，都是"看起来对但没生效"的同类）
1. **Node `spawn` + 管道 stdio 在沙箱里失败**（环境限制：confined 模式禁止 pipe）
   → 改用本会话一直验证可靠的 **`pwsh Start-Process`** 启停；收尸按命令行匹配端口，**不做全局杀进程**。
2. **`curWorkers` 初始化为 `null`**，而第一项的 `workers` 也是 `null`
   ⇒ `run.workers !== curWorkers` 恒为 false ⇒ **`startServer` 从未被调用**（表现为 ECONNREFUSED，
   很容易误判成"服务器起不来"）。
3. **SSE 流在 `done` 之后不关闭** ⇒ `res.text()` 永远等不到结尾、body timeout
   → 改为**增量读 + 见到 `done`/`error` 立即 abort**。

**这三条都是同一类错误**：现象（ECONNREFUSED / timeout）指向 A，真因在 B。
本轮我已在同类问题上栽了 **四次**（`opts2` 笔误、Python 中文引号致补丁未写入、汇总脚本读错文件、
`curWorkers` 哨兵）。**教训：排错时要先验证"我依赖的前置条件真的成立了吗"。**

### 意义
从此"改动是否破坏可复现性"有了**常设自动判据**。
配合 v1.3.36/37 的显式种子，**A/B 实验可以跑同一批种子、用配对差分**——
这是唯一不依赖"噪声到底多大"的做法（v1.3.38 已论证噪声大小我们测不准）。

回归：spec 37/37、np-test 36/36。

## v1.3.38 — 噪声下限复测：**千问的预期没有应验**，但这个比较本身有缺陷

### 做法
worker 分种子修好（v1.3.37）后，同一配置跑 **4 个显式种子**（`gens=200 n=3 pop=16 gpo=8 fresh=1`）。
这次种子是**真起作用**的，结果可复现、可配对。

```
seed=1  0.4514      seed=2  0.5556      seed=3  0.3972      seed=4  0.5083
n=3 极差 = 15.9pt            n=4 极差 = 15.8pt
对照：修复前同配置 n=3 极差 = 7.4pt
```

### 结论一：极差**没有变小，反而翻倍** ⇒ 千问的预期（会明显变小）**没有应验**
（他推测原先的"相关样本"低估了个体差异、压低了噪声；数据显示不是这样。）

### 结论二（更重要）：**这个比较本身不成立，所以不能据此推翻他的判断**
1. **n 不同不能让区间直接对比**：n=4 的极差**天然**大于 n=3（更多样本更容易抽到极端值）
   —— 这本身就是统计假象；
2. **3 个样本算出的"极差"本身极不稳定**，两次 3 样本极差之比没有判别力。
⇒ 严格说：**这次复测对"噪声是否变小"这个问题是"无结论"**，而不是"否证"。

### 结论三：无论噪声是否变小，**它都不小**（≥7pt，本次观测 ~16pt）
⇒ **"规则改动前后对比必须多跑 + 配对"这条判断不是被削弱，而是被强化。**

### 真正的收获：**配对设计现在可行了**
种子已可显式指定且可复现（v1.3.36/37）⇒ A/B 两组可以**跑同一批种子**，
用**配对差分**（每对之内的差）代替"两组均值之差"。配对能把个体差异从误差项里消掉，
**这比"多跑几个种子"有效得多**，也是唯一不依赖"噪声到底多大"的做法。

### 附：一个自己踩的坑（记录）
汇总脚本对所有 4 个文件读出了同一个值（都等于 seed=4 的值）——**脚本有 bug**，
结论改用逐条 echo 的可靠数值。**本轮我已在同类问题上栽过三次**
（`opts2` 笔误、Python 中文引号致补丁未写入、"空验证"）：**命令输出"看起来对"不等于改动生效。**

### 下一步建议
写 `tools/repro-check.mjs`（千问早先建议）：**同 seed 跑两遍 + 跨 worker 数各跑一遍，
只比对权重哈希**，作为"改动是否破坏可复现性"的常设看门狗。
然后即可开始**配对设计**的正式实验（藤甲 4 人局 / 转移伤害）。

回归：spec 37/37、np-test 36/36。

## v1.3.37 — worker 分种子：结果与 worker 数无关 + 个体间独立（千问跨机问题 ①②）

### 千问指出的两个跨机问题
1. **worker 数改变结果**：`num = cpus().length - 1`、`chunk = ceil(pop/num)`，
   而所有 worker **共用同一个 `EPIRUS_SEED0`** ⇒ 每个个体拿到的随机流偏移随 worker 数变化（8 核 ≠ 18 核）。
2. **同种子 worker 是相关样本** ⇒ 系统性低估个体间差异、**放大假信号**。
   （他怀疑 §3 那个"同设置内极差 7.4~7.6pt"里相当一部分来自这里。）

### 修法：按 **(seed0, gen, 个体下标)** 播种
比千问建议的 `SEED0*1000 + workerIndex` **更彻底**——
按 worker 号播种仍与"哪个个体分到哪个 worker"绑定（仍受 worker 数影响）；
而按**个体下标**播种：
- 与"哪个 worker 跑它"无关、**与 worker 数无关**；
- **每个个体拿到独立随机流** ⇒ 同时修掉"相关样本"那条。

实现：`train-worker.mjs` 在 `evalN` 前调用
`__seedSandbox(sb, S0*100003 + (msg.gen+1)*1009 + (m.idx+1))`（`m.idx` 是全局个体下标）。
另加 `EPIRUS_WORKERS` 覆盖 worker 数（**用于验证这条性质**）。

### 验证
```
?seed=42，同一命令、仅改 worker 数：
  workers=2  仅权重sha=f5735e5637572a7c
  workers=8  仅权重sha=f5735e5637572a7c   ← 完全一致
```
（注：本次验证在补丁**真正写入后**重跑——第一次我因 Python 中文引号导致补丁未写入，
"workers=2/8 一致"是**空验证**，已发现并重做。）

### 仍未做（千问列表第 ③ 条）
- `train-server:311` 的 30 分钟上限（在生成循环内，建议改按代数计）；
- `trainer.js:74-76` 的 `budgetMs` 分帧预算 ⇒ **浏览器里点"开始训练"本质上不可复现**。

### 下一步（千问的预期，值得直接检验）
**给每个 worker 不同种子后重测噪声下限** —— 他预期那个"同设置内极差 7.4~7.6pt"会**明显变小**。
若确实变小，则 v1.3.34/v1.3.35 里基于"方差很大"的判断需要**重新校准（可能偏悲观）**。

回归：spec 37/37、np-test 36/36。

## v1.3.36 — 训练可复现：千问定位到两处 Node 作用域的随机/时间源（**我的假阳性也被证实**）

### 最终结果
```
两次 ?seed=42：整文件sha 不同（fe73de50… / 5a01db5f…），
              仅权重sha **相同**（cd32f2407ab7244c）
```
⚠️ **我上一轮"训练不可复现"的结论是假阳性**：我哈希的是**整个文件**，
而 `writeBundleMP` 把 `ts: new Date().toISOString()` 写进 META ⇒ 每次必然不同、与权重无关。
**千问预判了这一点并提醒只哈希 `a` 数组 —— 他是对的。**

### 真正的根因（两处，都在 **Node 作用域**，`__seedSandbox` 够不到）
1. **`runTrainN` 的繁殖段**用裸 `Math.random()`（3 处）：选父代、选母代、决定是否 crossover。
   ⇒ 我在 vm 沙箱里修好的 5 处（`evo.js` 4 + `bots.js` 1）**全都生效了**，
   **唯独这三处从未被播种**。已改 `breedRng = T.mulberry32((SEED0||1)*100003 + gen)`。
2. **`runTrain`(2P) 的 `seedBase = (r+1)*7919 + (Date.now()%1000)`**：
   决定"选哪个冠军"的评测种子来自**墙上时钟** ⇒ 即使 ① 修好，2P 也永远不可复现。
   已改 `cfg.seed0*100003 + (r+1)*7919`（并把 `seed0` 补进 2P 调用点）。

### 千问排除的嫌疑（记录，避免重查）
- **worker 聚合确定性 ✓**：`paralleltrain.mjs` 的 `evalAll` 用 `by[r.idx]=r` 回填、按 `i` 顺序写回，
  `Promise.all` 的完成顺序不参与决策。
- **评估层与引擎干净 ✓**：`scoreMemberN` 同参数两次调用逐位一致
  ⇒ 地雷 AoE 重写没有引入不确定性。

### 新增防回归断言（千问建议的"硬规矩"）
`np-test.mjs` 加 **REPRO 用例**：扫描 `server/*.mjs`，禁止裸 `Math.random(` / `Date.now(`
（白名单：`mulberry32` 兜底、`const t0 = Date.now()` 纯计时、超时、时间戳、cache token、注释）。
**本轮它先抓出 3 条误报（2 计时 + 1 我自己的注释），修正后已能真正把关。**

### 仍未处理（千问列的"跨机不可复现"三条）
1. **worker 数改变结果**：所有 worker 共用同一个 `EPIRUS_SEED0`，个体的随机流偏移随
   `cpus().length-1` 变化（8 核 ≠ 18 核）。建议 `seed = SEED0*1000 + workerIndex` 并把 worker 数写进 meta。
2. **同种子 worker 是相关样本** ⇒ 系统性低估个体间差异、**放大假信号**。
   v1.3.35 §3 那个"同设置内极差 7.4~7.6pt"可能主要来自这里，**给每个 worker 不同种子后应重测**。
3. **两处墙上时钟**：`train-server:311` 的 30 分钟上限（建议改按代数计）；
   `trainer.js:74-76` 的 `budgetMs` 分帧预算 ⇒ **浏览器里点"开始训练"本质上不可复现**。

回归：spec 37/37、np-test **36/36**（新增 REPRO 用例）。

## v1.3.35 — 火弱持续性：训练侧模拟（3×200）→ **无可测效果**，且意外成为噪声下限标定

### 做法（**shipped 规则一字未改**）
`resolve.js` 加训练侧开关 `EPIRUS_FIREWEAK_PERSIST`（默认关，浏览器里 `process` 不存在故恒 false）：
- 关（现状）：`Next` 在 `startTurn` 被消费成 `Now` 并清空 ⇒ 火弱只活一个回合；
- 开：`Next` **不清除**（每回合重新武装），直到被火焰伤害真正兑现时才清除。
按用户建议用 **3×200 代**（而非 1×600）。

### 结果一：胜率无可测效果
| | firstRate（3 次） | 均值 |
|---|---|---|
| 基线 | 0.463 / 0.389 / 0.458 | **0.4366** |
| 持续 | 0.393 / 0.469 / 0.439 | **0.4338** |
差值 **−0.3pt**，而同设置内极差就有 7.4~7.6pt。

### 结果二（更重要）：**藤甲在全部 6 个冠军里都是 0.0%**
⇒ **这个开关是"可证明的 no-op"**：没人出藤甲 → 没人被挂 `fireWeakNext` → 开关永远不会
被读到有效分支。**它不可能改变任何行为。**

### 结果三：正因为它不可能有效果，差异就是**纯噪声**——这是罕见的标定机会
| | 最高 ep | 贵技能出手 |
|---|---|---|
| 基线 3 个种子 | 1 / 1 / 1 | 0% / 0% / 0% |
| 持续 3 个种子 | 1 / **2** / **2** | 0% / **33.3%** / **23.8%** |

**一个可证明无效的干预，让"2/3 种子达到 maxEp=2 且 24~33% 贵技能" vs 基线"0/3"** ——
如果我们不知道它是 no-op，**这看起来完全像一次成功的规则改动**。

⇒ **这是对 v1.3.34 那条方法论最直接的实证**：在 n=3 × 200 代下，
**噪声足以制造出"2/3 vs 0/3"量级的假信号**。任何规则改动的前后对比都必须：
① 更多种子；② 更强的是**配对设计**（同一批种子跨条件配对）——
而当前 `train-server` 的种子来自时间基，**两次运行无法配对**，这是工具链的真实缺口。

### 结论
- **火弱持续性暂不采纳**：既无胜率收益，也因为**藤甲根本没人用**而谈不上提升其强度。
- **真正卡住藤甲的不是火弱的时长，是它被免费 `反弹` 严格支配**（v1.3.10 已证：
  强制把藤甲换成反弹，avg 97.6% → 98.3%）。**先解决支配关系，火弱时长才有意义。**
- 开关与存档保留：`docs/artifacts/fw-base-s{1,2,3}.bak` / `fw-persist-s{1,2,3}.bak`。

回归：spec 37/37、np-test 35/35。

## v1.3.34 — WR_TOL 多跑对照（各 3 次）：**0.03 胜出，我之前的收紧是错的**

### 方法修正
v1.3.33 我用"每种设置跑一次"对比 `WR_TOL`，结论不可用（跑间方差 7.6pt > 待测效应）。
本轮按正确方法：**每种设置 3 次独立训练**（每次种子由服务器时间基产生），
主要看**确定性指标**（技能分布 / ep 分布），兼看 server 报的 firstRate。

### 结果（600 代 / pop16 / gpo8 / fresh × 3）
| | WR_TOL = 0.01 | WR_TOL = 0.03 |
|---|---|---|
| server firstRate | 0.385 / 0.386 / 0.471 → **均值 0.414** | 0.544 / 0.465 / 0.618 → **均值 0.542** |
| 有效技能数 | 2.00 / 2.00 / 2.00 → **均值 2.00** | 2.00 / 3.17 / 2.17 → **均值 2.45** |
| 技能种类 | 2 / 2 / 2 | 2 / 5 / 4 |
| 最高 ep | 1 / 1 / 1 | 1 / 1 / **2** |
| 贵技能出手 | 0% / 0% / 0% | 0% / 0% / **30.7%** |

**三个种子方向一致**（0.03 的最小 firstRate 0.465 ≈ 0.01 的最大 0.471），
且 **0.03 在胜率上高 12.8pt、多样性也更好**，唯一次出现"30.7% 贵技能"的也是它。

### 结论：v1.3.12 把 WR_TOL 从 0.03 收紧到 0.01 是**错误决定**
当时的依据是**单次 3P 头对头 17:62（n=24）**——那在 24 局下就是噪声。
**已把默认值改回 0.03**（仍可 `EPIRUS_WR_TOL` 覆盖）。

### 方法论沉淀（这是本轮真正的收获）
**凡涉及训练随机性的对比，单跑一次都不足以归因**：
- 本轮：同一设置 3 次的 firstRate 极差可达 **0.086**（0.544 vs 0.618）；
- 上一轮：两种设置单跑的差是 0.076 —— **比同设置的极差还小**。
⇒ 我之前若干"改前 vs 改后"的单跑对比（新规则重训、多目标择优前后）**结论都要降级**。
**唯一一直可靠的是确定性指标**（技能分布 / ep 分布 / 高 n 的头对头）——而我做决策时
恰好主要用的就是这些，算是没有系统性走偏。

回归：spec 37/37、np-test 35/35。

## v1.3.33 — WR_TOL 单变量对照：**单跑一次分不出差异，方差盖过效应**

### 做法
把 `WR_TOL` 改为可由 `EPIRUS_WR_TOL` 覆盖，同一批参数（1500 代 / pop16 / gpo8 / fresh）
各跑一次：**A = 0.01**、**B = 0.03**（其余完全不动）。

### 结果
| 指标 | A (0.01) | B (0.03) |
|---|---|---|
| 容差带大小 | band=3 | band=3 |
| pickedWr | 0.540 | 0.464 |
| divNorm / distinct | 0.312 / 4 | 0.241 / 6 |
| probeSc | 0 | **0.75** |
| 最大 ep | **2** | 1 |
| 贵技能出手 | **15.9%** | 0.0% |

头对头（24 局/方向，座位轮换）：
```
B×2 vs A×1 → B 68.4%
B×1 vs A×2 → B 84.2%
```
⇒ **B 头对头明显更强**，但它更浅（max ep 1、贵技能 0%）；A 更深但更窄（4 种）。

### 结论：**我原来的假设被否定，而且这个实验设计本身不成立**
- 我推测"收紧 WR_TOL(0.03→0.01) 把发散度压回去了"——**容差带大小两次都是 3，没有变化**，
  假设不成立。
- 更要紧的是：**每种设置只跑了一次，而两次的 `pickedWr` 差了 7.6pt**（0.540 vs 0.464）。
  **同一设置的跑间方差本身就大于我要测的效应** ⇒ 单跑对比无法归因。
  **要判定 WR_TOL 的影响，必须每种设置跑 ≥3 个种子、比较均值与离散度**（这正是
  `train-best.mjs` 早就支持的 `seeds` 参数的用途，我这次没用它，是方法错误）。

### 处置
- 冠军恢复为 **div555**（此前基于"经济深度 + 多样性"两个确定性指标的决定不变；
  本轮的 A/B 都不足以推翻它，因为都无法与 div555 做同口径比较）。
- A/B 两版存档：`docs/artifacts/wt-a.bak`（0.01）、`docs/artifacts/wt-b.bak`（0.03），可复现。
- **转移伤害的调研本轮未做**（预算耗尽）。

回归：spec 37/37、np-test 35/35。

## v1.3.32 — 补齐 N20 用例（35/35）+ 用新规则重训一次 AI 的实测结论

### 1. 补齐两条欠的 spec 用例（**未改任何规则**）
| 用例 | 结果 |
|---|---|
| **N20d 火弱逐目标**：地雷 AoE 只对挂了藤甲 debuff 的那一个 +1（b 吃 2 点，其他人 1 点） | ✔ |
| **N20e 狙击枪豁免只针对那一次攻击**：狙击打持雷者不触发、雷仍装着、无任何雷伤害事件 | ✔ |
- N20e 初版我把狙击伤害写成固定 1 点而失败——狙击枪有「**无影响时判定爆头**」规则，
  点数随局面变化。已改为**只锁豁免不变量**（雷仍 armed / 无雷事件 / 旁人未波及），不锁具体点数。
- 回归：spec **37/37**、np-test **35/35**。

### 2. 用新地雷规则重训 3 人冠军（1500 代）
```
[multiObj] band=2 pickedWr=0.453 divNorm=0.240 distinct=5   probeSc=0.25
```
**`probeSc` 从恒为 0 变成 0.25** —— 深经济探针**第一次变得可满足**（此前 v1.3.18 判定它
"构造上不可满足"：只有大雷会造成 ≥3 ジ 伤害；现在地雷 AoE 也进了这一档）。

**但被选中的冠军仍然很窄**：
| | 本轮重训 | 盘上保留的 div555 |
|---|---|---|
| 最大 ep | **1** | **5** |
| 贵技能出手 | **0.0%** | **11.8%** |
| 技能种类 | **5** | **10** |
| 分布 | ジ48.8 / 枪48.8 | ジ40.7 / 枪12.9 / 坦克11.8 / 反弹11.1 / 八卦阵11.0 / 防御10.0 |

⇒ **新规则没有让"重训"变好**。处置：冠军恢复为 div555；本版存
`docs/artifacts/champion-3p-n20-retrain.bak`。

### 3. 一条值得记录的推测（未验证）
div555 出自 **`WR_TOL = 0.03`** 时期（容差带宽 → 选出更发散的候选）；
后来为了修 3P 头对头问题把 `WR_TOL` **收紧到 0.01**，容差带变窄 → **选出的冠军又变窄了**
（本轮 `band=2`）。**即：修头对头的那个改动，可能同时把发散度压回去了。**
下次重训应做一次 `WR_TOL` 单变量对照（0.01 vs 0.03）来验证。

## v1.3.31 — 修 v1.3.19 引入的双枪 `via` 回归（N20b 关闭）+ N20 全绿

### 根因：我在 v1.3.19 做「双枪走数据表」时**误改了 `via`**
```js
if (key === SK.DUAL_GUN) return { amt: ..., pierce: ..., via: SK.DUAL_GUN };  // ← 错
```
`MINE_TRIGGER = [GUN, SWORD, TANK, DRAIN, RAILGUN, BIG_T, LASER_EYE, CANNON]` **不含 `dualGun`**，
而 `REFLECTABLE` 同样按枪口径查表。所以 `via: SK.DUAL_GUN` 导致：
- **双枪第一枪不触发地雷**（第二枪仍是旧的 `via: SK.GUN`，所以只有第二枪触发）；
- **第一枪也不被反弹**（同一张表）。
注释里我当时明明写着"via 仍按枪口径保留可被反弹/地雷语义"，**实现却改了**——是自查不彻底。

**症状**（N20b 事件流）：
```
damage to=0 via=dualGun source=3    ← a 中枪，但 via 不在 MINE_TRIGGER
damage to=1 via=gun     source=3
mine from=1 kind=direct              ← 只有 b 的雷直接触发
mine from=[0,2] kind=indirect        ← a 反被算作"间接"
```

### 修法
`via` 一律用 `SK.GUN`（两枪都按枪口径），**数据表只供 `amt`/`type`/`pierce` 数值**。
这正是 v1.3.19 的原始意图，只是当时实现写错了。

### 结果
```
N20a 地雷直接触发（4 人，间接触发合并一波）      ✔
N20b 直接触发无上限（双枪打 a,b → 共 3 波）      ✔
N20c 地雷伤害无来源 source=null 不被铁索共享     ✔
```
回归：spec **37/37**、np-test **33/33**。

### 顺带结论
**`MINE_TRIGGER` / `REFLECTABLE` 这类"按 `via` 查表"的机制，与"把技能改成走数据表"是两件事。**
以后改数据表只动数值字段，**`via` 是语义键，不能顺手改**。这条已写进代码注释。

## v1.3.30 — N20 地雷 AoE 重写（用户裁定）+ 3 条 N≥3 spec 用例

### 规则偏差（用户指出，确认）
`mineChain` 一直是 2 人时代口径——**只让"攻击者"受 1 点火伤**；`rules.js` 的 desc 同口径。
但 2 人局里「全场其他角色」与「攻击者」**恰好是同一人** ⇒ **两种读法在 2 人下无法区分**，
multi 框架加入时没回头核对 ⇒ 该偏差**只在 N≥3 显形**。

### 正确规则（用户裁定，例子核对过）
- **直接触发**（X 被非狙击枪攻击）：除 X 外**所有存活角色**各受 1 火伤，**无次数上限**。
- **间接触发**（挨了地雷伤害且自己装着雷）：所有间接触发者**合并成一波**，
  打**除这些间接触发者之外**的所有存活角色；间接触发只发生一次。
- **火弱**逐目标计算；**地雷伤害无来源**（`source=null`）→ 不被铁索共享 / 不被转移 / 不被大雷传导。

### 实现（`resolve.js`）
- `mineChain` → `mineHit` + `mineResolveAll`；
- 伤害阶段**只记 `_minePending`**，在 `resolveActions` 末尾统一结算——为了做到
  **「直接优先」**：d 双枪同时打 a、b 时，若边打边结，a 的雷波会把 b 的雷**提前**当成
  间接消耗掉，b 的直接触发就永远不发生（实测 b 只掉 2 血，应为 3）。
- `rules.js` 的 desc 同步改为 AoE 口径。

### spec 用例（N≥3，2 人用例天然测不出）
| 用例 | 结果 |
|---|---|
| N20a 4 人 a/b/c 装雷、d 打 a → 除 a 外各 1；b/c 间接触发合并一波打 a/d | ✔ |
| N20c 地雷伤害 `source=null` → 不被铁索共享 | ✔ |
| **N20b d 双枪打 a,b → a/b 各直接一波 + c 间接触发合并一波（共 3 波）** | **✘ 待查** |

**N20b 的现象**：a 只掉 2 血（应 3）。**我的假设是双枪的 `target2` 没有生效**（即 b 没被
第二枪打中，于是 b 没有"直接"记录、不发直接波），属**测试接线**问题而非雷逻辑问题——
但**未证实**。测试已标 `[待查]`，保持可见而不是隐藏。

回归：spec 37/37、np-test **32/33**（含 1 条待查）。

## v1.3.29 — 地雷触发改为「近 5 回合架势频率 + 对手当前 ジ 数」（用户方案）

### 用户纠正（对的）
上一版用"最近 3 回合 ≥2 次架势"**不符合实际**：**原型制御要 1 ジ，不是免费技能**，
连续摆架势本身罕见，阈值过高等于没触发。改为：窗口 **5 回合**，并把**对手当前 ジ 数**纳入判据。

### 实测三种对手环境（40 局 × 座位轮换）
| 变体 | 常态 | 架势型 | 防守型 | 大雷 | 最高ep / ep≥3 |
|---|---|---|---|---|---|
| **仅 5 回合 + `foeEp>=1`（采用）** | 地雷 2.4% | 地雷 **0.0%** | 地雷 1.0% | **11.2 / 15.3 / 6.9%** | 5 / 40~49% |
| 补 `最近2回合刚摆过` 放宽（试后撤回） | 地雷 12.6% | 地雷 23.7% | 地雷 14.8% | **3.6 / 0 / 3.6%** | 4~5 / 25~33% |

### 两个必须记录的结论
1. **「结合ジ数量」是双刃的**：它正确过滤了"摆不起"的对手，但**恰好也排除了最爱摆架势的
   对手**——`guardspam`/`protowall` 把ジ 全花在架势上，`ep` 常为 0 ⇒ 地雷对该类对手 **0.0%**。
2. **地雷与大雷在本脚本里互斥**：地雷 3 ジ、大雷 5 ジ，**花了 3 就再也到不了 5**。
   放宽地雷 ⇒ 大雷从 11~15% 掉到 0~3.6%；收紧地雷 ⇒ 地雷掉到 0~2.4%。
   **这是脚本内部的设计冲突，不是阈值问题。**
   按 P1 的目的（深经济对手，让 3~5 ジ 档有独占价值）**选择保住大雷**，地雷只作稀有补充；
   若要让地雷当主武器，应**另写一个"地雷流"脚本**，而不是在这一个里兼顾。

回归：spec 37/37、np-test 30/30。

## v1.3.28 — 地雷改用「历史频率」触发（方案 a）；`lastSkill` 滞后问题的确认

### 问题（用户提问引出）
`deepsaver` 原先的地雷条件是 `state.p[tl].lastSkill === SK.PROTO`。
- **不是学习**，是硬编码的单字段等值比较。
- `lastSkill` 本身是好的（真实对局采样 2976 个决策，**非 null 占比 69.6%**；
  我最初的"恒为 null"测试是自己手搓 `attemptAction` 绕过了正常记录路径，测试错了）。
- **但那个分支从未触发**：120 次检查 **0 次命中**，deepsaver 实际分布 `ジ83.3 / 大雷16.7`，**地雷是死代码**。
- 根因：**拿"上一回合出的招"当"这一回合的意图"**。引擎在决策时刻不提供任何对手意图信息
  （所有人同时声明），所以滞后字段做不了"应对"。

### 改法（用户建议 a）：用**可观测的历史频率**，不假装预知
仿 `bots.js` 既有的 `__mem` 模式（模块级 + `round===1` 重置），新增 `__dsMem`：
统计**领先者最近 3 回合里出架势的次数**，`>=2` 才放地雷。脚本记参考数据不增加训练压力。

### 实测（40 局 × 座位轮换）
| 对手环境 | 分布 |
|---|---|
| 常态（random/balanced） | ジ59.6 / 防御25.9 / **地雷 9.8** / 大雷 4.7 |
| **架势型（guardspam/protowall）** | ジ76.3 / **地雷 23.7** / 大雷 **0.0** |
⇒ 频率触发**生效**，且对架势型加码（9.8% → 23.7%）。

### ⚠️ 但暴露出一个新矛盾（未解决）
在**架势型对局**里地雷占了 23.7%、**大雷掉到 0.0%**——因为地雷要 3 ジ、大雷要 5 ジ，
一旦开始放地雷就再也攒不到 5。**即：这个修法恰好在我们最需要深经济的那类对局里
牺牲了深经济。** 两个候选方向（未做）：
- 要求 `ep >= 5 才优先大雷`（现已如此）+ **`ep < 5` 时只有架势分满分才放地雷**；
- 或把地雷改为"对手连续 3 回合架势"才放（更严），保留大雷主线。

### 附带确认：`lastSkill` 滞后是**所有脚本反应逻辑的天花板**
`bots.js` 的 `stanceOf` / `reactDefense` / `oppInDefense` 读的都是同一个滞后字段，
影响 `pickBalanced` / `pickAdaptive` / `pickBreakDef` 等核心脚本。
⇒ 它们的"对手在摆架势"分支很可能**长期在空转**（与 `pickMultiMed` 当初实测
"反应式穿透 55.8% < 直接穿透 62.5%" 的现象一致）。**影响面待评估。**

回归：spec 37/37、np-test 30/30。

## v1.3.27 — P1：深经济对手 `deepsaver`（先写脚本 → 实测它真的会攒）

### 做法（按我自己提的顺序：先验证脚本会攒，不先接进池子）
新增 `pickDeepSaver`（"攒钱流"）：能一击必杀 → 大雷；够得着大雷 → 大雷；血少 → 守；否则**只出 ジ 攒钱**。
并给 `tools/bot-audit.mjs` **加了两列**：`最高ep` / `ep>=3占比`——这就是"是否真会攒"的判据。

### ⚠️ 第一次设计**没通过验证**（正是我预警的"设计 vs 实际"落差）
初版无条件放地雷(3 ジ) → **一买得起就花掉，最高 ep 只到 3**，大雷那一支永不触发：
```
deepsaver  1st=3%   最高ep=3   ep>=3占比 15.9%   ジ53 / 防御31 / 地雷16
```
修法：地雷只在"对手正在用原型制御"时才放（它唯一的独占价值），否则继续攒到大雷。改后：
```
deepsaver  1st=27%  最高ep=5   ep>=3占比 47.1%   ジ70 / 防御18 / 真正的落雷12
```
⇒ **1st 从 3% → 27%**，与 `breakdef`(30%) 同一档，且它是**会还手**的攒钱型。

### 同时纠正千问一处判断（有数据）
千问说"对手池里现在没有任何一个对手在 3+ ジ 档"——**不准确**。加了 ep 列后可见：
| 对手 | 最高ep | ep≥3占比 | 1st | 主线 |
|---|---|---|---|---|
| **breakdef** | **5** | **43.4%** | 30% | ジ86 / 大雷14 |
| **farmer** | **10** | **54.8%** | 0% | ジ68 / 蓄能32 |
| heavyfire | 2 | 0.0% | 37% | ジ70 / 坦克30 |
| deepsaver（新） | **5** | **47.1%** | 27% | ジ70 / 防御18 / 大雷12 |

⇒ 池子里**本来就有两个会攒的**（`breakdef`/`farmer`），只是 `farmer` 完全不还手（1st 0%）；
缺的是**「会攒 + 会还手」**那一个——`deepsaver` 补的正是这个位置。
（`heavyfire` 最高 ep=2、ep≥3 占比 0.0%，**再次确认它不是深经济探针**。）

### 未完成的部分
- **"考卷"还没改**：`deepsaver` 已进 `BOT_NAMES`（评估池），但**评估口径**仍是对 45 个对手对取平均，
  深经济对手只占少数 ⇒ "不攒钱"依然几乎不受罚。这正是千问说的 §3.5「门槛局须同时满足
  补贴 + 架势/控制型对手」要解决的问题。
- **架势/控制型对手未新增**：池中已有 `guardspam`/`protowall`/`reflecttank`/`reflectmix`，
  但都是 2 人时代逻辑；是否要按 N 意识重写、以及是否要给它们更高权重，待定。

回归：spec 37/37、np-test 30/30。

## v1.3.26 — 冠军换成 div555（v5→v6 无损升级）+ 工具回归确认

### 1. P0 后其余工具回归（用户要求的第 1 步）
| 工具 | 结果 |
|---|---|
| `seat-diag` | ✓ 坏行修好（`未使用技能数 = 6 种被用过`） |
| `bot-audit` | ✓ 退出码 0；**仍显示 tankline ≡ heavyfire**（15.0 回合/1.86/ジ69+坦克31 完全一致）→ 印证千问"heavyfire 不是深经济探针" |
| `skill-report` | ✓ 正常写出 HTML；判定 `枪 −24.1pt`、**`藤甲 −75.9pt`**（被免费反弹支配，代价极大）、没学会的强招 `激光剑 +5.6 / 真正的落雷 +2.8 / 电磁炮 +0.9` |

### 2. 冠军换成 div555（用户要求的第 2 步）
前提问题：**游戏侧 `checkPack` 严格拒绝 v5 包**，不能直接复制存档。
解法：**v5→v6 无损升级**（新增 `tools/upgrade-pack.mjs`）——
v6 相对 v5 只多一维状态特征，只要在 `W1` 每一行的对应位置**插入 0 权重**，
该维贡献恒为 0 ⇒ 网络行为与 v5 **完全等价**，但包形状变成 v6、游戏侧接受。

实测转换：`新增特征在状态特征中的下标 = 18`；输出 `v=6 f=123 len=3337 checkPack ok`；
**等价性自检 `max|Δvalue| = 0.000e+0`（完全等价）**。

### 3. 换冠军后的实测
```
[A 经济画像]  决策=3197  最大ep=5  贵技能(ep>=2)出手 11.8%  用到 10 种
  ep 累积: ep0:51% ep1:85% ep2:98% ep3:99% ep4:100%
  分布: ジ40.7 枪12.9 坦克11.8 反弹11.1 八卦阵11.0 防御10.0 蓄能1.9 藤甲0.4 贴贴0.2 净化0.0
```
对比被替换掉的 v6 窄版：`最大 ep 1 / 贵技能 0.0% / 6 种`。**经济深度与多样性都是净提升。**

### 4. head-to-head（诚实标注：样本太小，不足以定论）
```
A(div555)×2 vs B(v6窄)×1 → A 77.3%   （等强度期望 66.7%）
A(div555)×1 vs B(v6窄)×2 → A 18.2%   （等强度期望 33.3%）
```
n=24/方向，噪声约 ±9pt。**两个方向互相矛盾**（2 座位侧偏高、1 座位侧偏低），
且换位前的同口径测试也呈现同样形态 ⇒ **这个 2v1 口径本身可能不适合判强弱**
（两座位的一方本就占优，偏离期望的符号在两个方向上不一致）。
**结论：本次换冠军的依据是"经济深度 + 多样性"这两个确定性指标，不是头对头。**
若要严格判强弱，需要 n≥200 且改用「同座位 1v1v1 轮换」口径。

处置：旧窄版存 `docs/artifacts/champion-3p-v6-narrow.bak`。
回归：spec 37/37、np-test 30/30。

## v1.3.25 — P0：存档版本兼容（形状自适应）→ A/B 证据链恢复

### 问题（千问指出，我确认）
升 `PACK_VERSION 5→6` 后，**7 个实验存档全变砖**：
`checkPack` 要求 v=6/f=123/len=3337，而存档是 v=5/f=122/len=3313 → 全部拒绝，
`econ-eval ③ head-to-head` 直接抛"冠军 B 解包失败"。
⇒ **五次失败实验的结论无法复现、无法对照**，而且以后每次改特征都会重演一次。
根因：`FEAT_S`/`HID`/`PACK_VERSION` 是**模块全局**，一个进程只能存在一种网络形状。

### 修法（千问方案）：形状从 `params.length` 反推
`paramCount = HID*FEAT_N + HID + HID + 1` ⇒ `FEAT_N = (len - 2*HID - 1)/HID`
- v5: `(3313-49)/24 = 136` ⇒ `FEAT_S = 136 - FEAT_A(14) = 122`
- v6: `(3337-49)/24 = 137` ⇒ `FEAT_S = 123`

具体改动（`js/train/policy.js`）：
1. 原 `features` 改名 `featuresV6`；新增 `features(state,pid,featS)` 包装——
   当 `featS < 123` 时**去掉"自己跨得过环启动线"那一维**。
   **下标用运行时探测而非硬编码**：把 `me.ringStreak` 从 0 改到 3，
   唯一"由 1 变 0"的那一维就是它（`ringSelfIdx()`）。
2. 新增 `shapeOf(params)`：按长度反推 `{featS, featA, featN, legacy}`；
   `value()`/`forward()` 改用它（**v6 路径行为完全不变**，仅在长度不匹配时走 legacy）。
3. `unpack(o, allowLegacy)`：`allowLegacy=true` 时按包内长度重建；
   **游戏侧仍调用严格版 `unpack(o)`，checkPack 一字未改**。
4. `tools/econ-eval.mjs` 的两处 unpack 改传 `allowLegacy=true`。
5. `tools/seat-diag.mjs:78` 坏行修复（残留 `(R.ACT_KEYS ? 0 : 0) +` 拼出"06 种"）。

### 验证
```
champion-3p-div555.bak    v=5 f=122 len=3313  严格=✗  工具侧=✓  反推 featS=122 featN=136
champion-3p-curriculum.bak v=5 f=122 len=3313  严格=✗  工具侧=✓  反推 featS=122 featN=136
bundled-champion-3p.js    v=6 f=123 len=3337  严格=✓  工具侧=✓  反推 featS=123 featN=137
```
`econ-eval ③` 恢复可用（回归：spec 37/37、np-test 30/30）。

### ⚠️ P0 修好后立刻暴露的重要事实
A/B 一恢复，就发现**当前 v6 冠军比 v5 存档差**：

| | A（当前 v6 冠军） | **B（v5 存档 div555）** |
|---|---|---|
| 最大 ep | **1** | **5** |
| 贵技能出手 | **0.0%** | **11.8%** |
| 技能种类 | 6 | **10** |
| 分布 | ジ39.5/枪27.3/藤甲11.3 | ジ40.7/枪12.9/**坦克11.8**/反弹11.1/八卦阵11.0/防御10.0 |
| head-to-head | A×2:B×1 = 71.4%；A×1:B×2 = **20.7%** | — |

⇒ **v6 那次重训（含 (a) 特征 + 探针门槛 + 名次改动）是净负向的**：
经济更浅、种类更少、头对头更弱。这也印证了千问的判断
（"加了 (a) 特征的那次重训在经济深度上是负向的"）。
**此前因为存档变砖，这个对比根本做不出来** —— 这就是 P0 的价值。

回归：spec 37/37、np-test 30/30。

## v1.3.24 — 修 v1.3.23 引入的回归：冠军包版本不兼容 + 模仿默认关闭

### 回归（我在 v1.3.23 犯的）
v1.3.23 里我把 3P 冠军回滚到 **v1.3.11 的包（v5）**，但 `PACK_VERSION` 早在 v1.3.19 已升到 **6**
→ `checkPack` 拒绝 → **困难档会静默掉回脚本 AI**。
被 `tools/np-test.mjs` 的「N19 多人冠军包：可加载且维度兼容」抓到（30/30 → **29/30**）。

### 修法
1. **重训 v6 冠军**（1500 代 / 75s）：`band=4 pickedWr=0.49 divNorm=0.460 distinct=6`。
2. **模仿默认关闭**：C 方案实测未奏效（只做动作级模仿，学不到跨回合轨迹），
   故 `imitGens` 默认取 `EPIRUS_IMIT_FRAC=0`；需要复验时设 `EPIRUS_IMIT_FRAC=0.35` 打开。

### 教训
**升 `PACK_VERSION` 之后，任何回滚旧冠军的"安全操作"都会造成静默降级。**
回滚冠军前必须先看包版本；本轮是靠 np-test 的版本断言才发现的——
这条断言（v1.3.12 加的）价值在这一刻兑现了。

回归：spec 37/37、np-test **30/30**（恢复）。

