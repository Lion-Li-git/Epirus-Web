## v1.5.89 — 修"经济/熵奖励 env 到不了 worker"的**静默半开**（单一真源 + 回执 + 守门 D77）+ 两条被作废的臂拿到真读数

> 全部证据与读数见 **附录 H**（H1 反证 / H2 修法 / H3 修后判据 / H4 复跑真读数 / H5 顺手发现的门缺陷）。

### 病（第八轮复核 §2，[代码事实] + 实测）
`EPIRUS_DIV_W / DIV_K / DIV_FORCE_GENS / WALL_FILTER` 原先只在 `train-server.mjs` 内联读，而 fitness 是在
**16 个 worker 线程**里算的，worker 只读 `EPIRUS_ECO_TARGET/CAP/DIVW` ⇒ 这四个开关在 worker 里**从未被读**：
日志完全正常，臂的 A/B 实际是 **A/A**。反证：`git show HEAD:server/train-worker.mjs` 里 `EPIRUS_DIV_*` 出现 **0 次**。
这是"开关看着接上了、实际作用在没跑的那条路径上"的第 N 次（同族：v1.5.18 的 `EPIRUS_FIGHT_*`、v1.5.79 的 `TGT_W`）。

### 修（单一来源，而不是"在 worker 里再补一遍"）
- 新增 `server/econ-env.mjs`（`readEconEnv / hasEconOverride / econEcho` + 键名表），与 `server/fight-env.mjs` 同一套规矩；
- `train-server.mjs` 删掉四行内联 + 重复解析；`train-worker.mjs` 改走同一份 + 新增 **`[econ] worker 生效值: …` 回执**；
- 语义：`EPIRUS_WALL_FILTER` 仍只认 `'1'`；旧名 `EPIRUS_ECO_DIVW` 仍生效（新名优先）；**新增**"空串＝未设"；
- 守门 **D77**（静态单一读取点 + 两端都导入 + worker 有回执 + setter 契约 + 真模块行为自检）；**D76** 的接线断言改指
  `econ-env.mjs` —— 它原先断言的是"train-server 里有这个 env 名"，而**名字存在 ≠ 开关进得了算 fitness 的进程**。

### 证
`EPIRUS_DIV_W=0.5` 起真服务跑 1 代 ⇒ **16 个 worker 各打一行 `divW: 0.5`**（复核 §2-2 给的判定标准）；
真臂同样（`divK2` 48 行回执 `divW 0.3 / divK 6`，`forceF` 32 行 `divForceGens 60 / wallFilter true`）。
门禁：np-test **125/125**（原 124 + D77）· spec **36/36** · smoke OK · ui-probe **10/10** · 规则指纹 **8dda2677 未变** ·
`js/` 零改动（免重记两个包指纹）· 线上包全程 **`e4b4f780dda2`**。

### 两条臂的真读数（250 代 · 默认 16 对手池 · 考卷 40 局）
| 臂 | env（真生效）| seed | 产物 | A 考卷 | G | 反弹墙伤害/局 |
|---|---|---|---|---|---|---|
| K2 `v7divK2` | `DIV_W=0.3 DIV_K=6` | 31 | 无（健康门禁）| — | — | — |
| K2 | 同上 | 81 | `v7divK2-81.bak` | 43.0% | **5.28** | **8.95**（零伤害局 0%）|
| K2 | 同上 | 82 | `v7divK2-82.bak` | 39.1% | 3.70 | 0.00（零伤害局 100%）|
| 甲 `v7forceF` | `FORCE=60 WALL_FILTER=1` | 31 | 无（健康门禁）| — | 4.42（种类 10）| — |

- ⇒ "T×5 必然 0 产物"不成立（真 `DIV_W=0.3` 下 **2/3 出产物**），且 seed 81 的 **G 5.28 > 线上包 4.22** 并**能破反弹墙**；
- 两个产物仍 **G4/G5 行为门双 FAIL** ⇒ `promote-champion --dry` **rc=6**，**未换包**、未 `--force`；
- 甲真开之后仍 0 产物，但被拦理由变了：**"自对局回合 40.3 > 40"**（宽度已起来 G=4.42，代价是回合变长）；
- **归因作废**：附录 D/E/F/G 与 `HANDOFF §5.4` 里"臂 K = `DIV_W=0.3`""臂甲 = 强迫 60 代 + 破墙过滤"的归因不再成立；
  **仍然成立**的是 `DIV_K=6` 固定分母那条因果（它是**改代码默认值** —— 代码改动确实进 worker）。

### ⚠ 未修（待裁定，见附录 H5）
`tools/promote-champion.mjs:230` 把 gate-drafts 输出里**任何** `G4[`/`G5[` FAIL 都当阻断项，而该输出
**同时含"线上包"这一对照行** ⇒ 只要在位的线上包是红的（补了"只枪"那一行之后 `G4[线上包/long]` 就是红的），
**任何候选都会被连坐**，换包实际只能靠 `--force`；且 FAIL 行括号里打的是**基线**，读起来像"这条过了"。

## v1.5.88 — 退火强迫 + 破墙硬过滤（默认关）+ 落盘阻断开关

- `js/train/evo.js`：`DIV_FORCE_GENS`（默认 0，**只包计分对局**的 chooser）+ `WALL_FILTER_ON`（默认 false）
  + `makeDiversityForce` / `wallProbe`（**必须**在真 4 席 reflectspam 里量破墙 —— 自对局用量不能预测破墙能力，成本 ≈80 秒/臂）；
- `server/train-server.mjs`：`EPIRUS_FEASIBILITY_BLOCK=1` 时不可行候选**不写盘**（默认关，D73）；守门 **D75/D76**。
- ⚠ **本条与 v1.5.86/87 的臂读数都受 v1.5.89 那个 env 静默半开影响**（`DIV_FORCE_GENS` / `WALL_FILTER` 当时没进 worker）
  ⇒ 相关归因作废；真读数见 v1.5.89 与附录 H4。

## v1.5.87 — 熵口径与门禁对齐（用户裁定 A）+ "固定分母"的因果验证

- 进 fit 的熵改用**自对局·成功非ジ动作**（与门禁 `G` 同源）：`spUse` → `spH/spDivNorm`，`divBonus = DIV_W × spH/ln(DIV_K)`；
  旧的 `coverageEntropy(agg.use, agg.aff, DIV_K)` 只留作对照量。守门 **D74**（"进 fit 的项必须与门禁同一个量"）。
- 因果验证（附录 F）：同一 `T×5` 下，旧分母 **0/3 产物**（G=1.24/1.90 被拦）、固定分母 **3/3 健康**（G=3.02~4.42）
  ⇒ **分母就是那个病因**（当时 `DIV_W` 实为空操作、等效默认 0.06 —— 见 v1.5.89 的更正）。

## v1.5.86 — 熵项固定分母（病因 + 修法）+ 对手注册表第 3 次"加名字漏一处"

- 病（附录 D6）：`divNorm = H(非ジ直方图) / ln(可负担技能数)` ⇒ **把菜单变穷就能刷分**；
  实测反证：`EPIRUS_DIV_W=0.3` 的产物 G=**1.24 / 1.90**（比默认臂被拒的 2.3~2.8 更低）、整臂 0 产物；
- 修：`coverageEntropy(agg.use, agg.aff, DIV_K)`，`DIV_K = 6`（固定目标；env `EPIRUS_DIV_K` 经 `setEconomyReward` 注入）；
- 对手注册表：`server/opp-pool.mjs` 补 `protomine` / `prototransfer`（`train-server` 的 `BOT_FN` 早有、注册表没有），
  新增守门 **D72**（runner 池子里的名字必须全部在册）—— 这是本项目第 3 次同类漏加；
- 踩坑（附录 E1）：env 读取**不得写进 `evo.js` 的模块作用域**（`evo.js` 在 np-test / audit-lib 的 vm 沙箱里加载，
  沙箱**没有 `process`** ⇒ 加载即崩）。

## v1.5.85 — 温度 T（熵权重）的现状与曲线；顺带撤回一组无效的 2P 读数

> 用户两点：(1) 规则不用改，怀疑我写的存钱脚本本身有问题（"攒大雷的脚本曾经有相当胜率"）；
> (2) 冠军强度已很高，为多样性稍微降胜率无妨；(3) 要一个**可调的温度权重**把熵奖励写进最终目标。

### 1. 你要的"S 项"**已经在目标函数里**（读代码）
`js/train/evo.js`：`const cov = coverageEntropy(agg.use, agg.aff); const divBonus = DIV_W * divNorm;`
- 即 `G = H − T·S` 里的 `T` = **`DIV_W`**（默认 **0.06**，env `EPIRUS_DIV_W` 可调，早已在最终目标中）；
- `coverageEntropy` **只统计非ジ动作**（v1.5.8 用户裁定：否则攒钱会把熵压低、反过来惩罚攒钱）；
- 历史证据：`EPIRUS_DIV_W` 提到 **10×** 仍无法打破"G<3 与 F(活跃场进攻) 的互斥"⇒ 只调这一个权重不是万能钥匙。

### 2. 新工具 `tools/probe-temp.mjs`：**多给熵要付多少胜率**（只读）
同一权重、只改**采样温度**，中立场（random/balanced/aggro/defend 各 1 席，基线 20%），40 局/格：
| T | 1st% | effSkills | 伤害/局 | 出手前 5 |
|---|---|---|---|---|
| 0.05 | 30.0% | 4.28 | 3.1 | 枪 41 / 狙击 33 / 全息 15 |
| 0.15 | 27.5% | 4.25 | 3.1 | 枪 43 / 狙击 31 / 全息 14 |
| 0.30 | **37.5%** | 4.53 | 3.4 | 枪 40 / 狙击 32 / 全息 14 |
| 0.50 | 25.0% | **5.28** | 2.8 | 狙击 36 / 枪 27 / 全息 17 / 八卦阵 9 |
| 0.80 | 35.0% | **6.29** | 3.5 | 狙击 34 / 枪 25 / 全息 11 / 八卦阵 10 / 反弹 8 |
- **多样性随 T 单调上升（4.28→6.29），强度在 [0.05, 0.8] 内没有显著下降**（n=40、±15pt）⇒
  "为多样性牺牲胜率"在这个区间里**可能根本不用付**（这也支持用户的判断：可玩性可以白拿）。
- ⚠ 本表是**采样温度**的曲线；训练侧 `DIV_W` 的曲线要开臂才能量（见 §5）。

### 3. 仓库自带脚本的"联赛"（回应"我的脚本可能写坏了"）
**5 人 multi，0 号位=脚本（逐局轮换），其余 4 席=线上冠军**（40 局/个，基线 20%）：
- 强：**`pickProtoMine` / `pickProtoTransfer` 52.5%**、`pickMultiMed`/`pickMultiStrong` 47.5%、`pickFocusFire` 32.5%；
- 弱：**`pickDeepSaver`（真·攒大雷，5 ジ打领跑者）7.5%**、`pickHeavyFire` 15%、`pickRingSpam` 2.5%、`pickSnipeSpam` 5%。
**中立场**（其余 4 席 = random/balanced/aggro/defend）：`ProtoMine/ProtoTransfer` 45%、`GuardGun` 32.5%、
`DeepSaver` **17.5%**（≈基线 20%）、`HeavyFire`/`TankLine` 5%。
⇒ **在 5 人 multi 下，我无法复现"攒大雷有相当胜率"**（仓库自己的 `deepSaver` 也只有 17.5% ≈ 基线）；
⇒ 但"vs 冠军 7.5%"另有原因：**冠军的训练池里就有 deepsaver/heavyfire** ⇒ 它被专门选出来克制攒钱流 ⇒
用克星当标尺量不出这个策略本身的价值（这是我这轮实验设计的一个缺陷）。
⇒ 顺带发现一个**真空白**：**冠军从不使用原型制御**（候选表里 proto 0.0%），而原型系脚本在中立场最强（45%）。

### 4. 撤回一组无效读数（2 人局）
我先前那张"2 人局"表**无效**，原因：`R.MODES` 只有 `standard`(**2人**)/`multi`/`long`，而我先用 `multi`+2 人（非法配置），
换成 `standard` 后**镜像自检仍然 0%** ⇒ 查下去发现：**镜像局双方 60 回合 0 伤害、都满血、`winner='draw'`** ——
`standard` 模式**没有终局收缩**，防御型互相对峙永远打不完 ⇒ 那张表里绝大多数是**平局**，被我当成失败统计了。
⇒ **2 人局的结论暂缺**；要在沙盘里量 2P，必须先按页面口径打开终局收缩（已记为待办）。

### 5. 下一步（待你定）
1. **训练侧 T 的曲线**：开 2 条小臂对比 `EPIRUS_DIV_W=0.06`（现状）vs `0.3`（各 2 seed）⇒ 量 (A, G) 的移动；
2. **2P 沙盘补齐**（打开 suddenDeath 后重跑一遍脚本联赛）——这更接近你线下的体验；
3. **原型制御**这条空白值得研究（冠军 0%、脚本 45% 起）：它是"组合/协同"类机制，可能正是"多样性"最有价值的方向。

## v1.5.84 — 结论：本规则下"上环 / 存钱放大招"**更弱**（先验收益，未训练）

> 用户方向："先研究怎么让冠军用聚能环以及高ep技能，因为先有用聚能环产生优势了，
> 冠军的集火或打断聚能环才更有用。" ⇒ 我先验证**收益是否真的存在**（v1.5.82 §4 的复盘顺序：
> 先修条件/确认收益，再谈奖励），**没有**先烧一条训练臂。

### 1. 引擎事实（读代码，不猜）
- `state.js:105-107`：`ring` 的花费 = `p.ringStreak === 0 ? 3 : 0` ⇒ **只有第一环花 3 ジ，续环免费**；
- `resolve.js:609`：`ringStreak += 1; g = ringStreak >= 3 ? 3 : ringStreak` ⇒ 收益 +1/+2/+3；
- `resolve.js:1036`：该回合没成功出环 ⇒ `ringStreak = 0` ⇒ **断一次清零**。
- ⇒ 净额：第 1 次 −2、第 2 次 +2、**第 3 次起 +3/回合**（ジ 只有 +1）。

### 2. 新工具 `tools/probe-ringpay.mjs`（只读"规则实验室"）
0 号位换成一行脚本（座位逐局轮换），1~4 号位是被测冠军，量夺冠率 + 引擎是否真转起来 + 它造成的伤害。
配置可用 `RINGPAY_N` / `RINGPAY_MODE` 切换 ⇒ 换一条规则假设重跑只要约 30 秒。

### 3. 读数（每格 40 局，随机基线 20%）
| 打法 | multi 5P | multi 3P | long 5P | 伤害/局（5P multi）|
|---|---|---|---|---|
| sustain 永远续环 | **0/40 (0%)** | — | — | **0.0**（仓堆到 ep=123 却零输出）|
| cycle 攒→花→再进 | **0/40 (0%)** | — | — | 0.0（我的写法有 bug：一次都没转起来）|
| burst 环到第 3 次再输出 | 5/40 (12.5%) | — | — | 2.2 |
| **saver 存到 4 才放大招** | 5/40 (12.5%) | 11/40 (27.5%) | 1/40 (2.5%) | 2.2 |
| **sniper 一直用便宜卡压制** | **11/40 (27.5%)** | **19/40 (47.5%)** | **10/40 (25.0%)** | **4.5** |

⇒ **每一种配置下"存钱/上环"都输给"持续压制"**，且伤害只有一半左右。

### 4. 机制（为什么必然如此）
**一回合只能出一手** ⇒ 上环（即使免费）与存钱都等于**这一回合不输出**；
而 3~5 人混战**惩罚不输出**（别人在互相削血、你在攒钱）⇒ 攒出来的仓还没花完就先出局。
`long` 模式更长 ⇒ 反而更差（2.5%），说明问题不是"赛程太短"而是**出手机会的机会成本**。

### 5. 结论与建议（这是**产品/规则**决定，归你）
1. **不要训练它**：在现规则下"让它用环/存钱放高 ep 技能"= 训练一个**更弱**的打法（数据在 §3）⇒ 我不会为它开臂。
2. 同理，**集火/打断环**也没有猎物（环不构成威胁）⇒ 用户推理的后半句在当前环境下不成立。
3. 若确实想让"环/投资"成为一条真路线，改动必须发生在**规则层**（不是训练层），可选项：
   - ① 续环的当回合**仍可输出**（把"投资占用出手"这个惩罚去掉）；
   - ② 提高环的收益 / 降低首环成本 / 让仓可以直接转化为伤害；
   - ③ 引入"储备不被清零"这类机制。
   任一条我都能用 `probe-ringpay.mjs` 在**30 秒内**验一遍（改的是假设，不是训练）⇒ 建议先在沙盘里试规则，再决定要不要动正式规则与本文件之外的文档。
4. 元教训（值得保留）：**先验收益结构，再烧训练**（对比"靶向"那条：先烧臂、事后才发现奖励是死的）。

## v1.5.83 — 珠子闭环诊断（只读）：**机会几乎不出现，出现了又选不中**

> 触发：用户更正"**激光眼也吃珠子**（爆破能），它的主要目标是破防御，而破防御要**别人也进攻**才起效 ⇒ 偏结盟用"。
> 我上一版把它当成天火，写错了 ⇒ 见 §1 的更正。

### 1. 规则事实（已从 rules.js 核）
| 卡 | 费用 | 珠子 | 效果 |
|---|---|---|---|
| **电磁炮** `railgun` | 2 | **电珠** | 2 点电伤害，攻破反弹与防御（`pierce:{defense:true,reflect:true}`）|
| **激光眼** `laserEye` | **null（变费）** | **爆破能** | 首次 **1 ジ + 1 爆珠**、连续使用 **2 ジ**；无效化目标任意防御类（含原型制御/全息）/转移伤害，否则 1 点光伤害 |
| 天火 `firestorm` | 2 | —（**符咒**，与珠子无关）| 引爆自己贴出且停留 ≤3 回合的符咒 |

- **更正**：v1.5.82 与 `tools/probe-candslot.mjs` 里的"唯一的珠子消费卡是电磁炮 / 珠子消费=电磁炮+天火"**都是错的**
  ⇒ 珠子卡是**两张**（电磁炮=电珠、激光眼=爆珠），天火与珠子无关。工具里的错句已改。
- ⚠️ `laserEye.cost === null`（变费）⇒ 任何"按 cost 过滤"的统计都会漏掉它（我上一版就是这么漏的）。

### 2. 新探针 `tools/probe-beadloop.mjs`（只读）
包一层 0 号座 chooser，在 `Play.autoGameN` 的真实对局里记录每个决策点，并做**蒙特卡洛**
（把 `state.rng` 临时换成**独立流**，问完还原 ⇒ 绝不消耗对局随机流；这是 v1.5.51 的教训）。
候选表用与线上**同一份** `T.econBase`（v1.5.83 抽成单一真源，避免探针各写一份又量错）。

### 3. 读数（线上包，12 局 multi，105 个决策点，蒙特卡洛 24 次/点）
- **电磁炮可负担 = 0 / 105**；**激光眼可负担 = 1 / 105**。
- 那唯一一次：**在候选表里** ✓（1/1）但采样 **24/24 全选【全息屏障】** ✗（选中率 0.0%）。
- **持有珠子 = 1 / 105**（ep=1）⇒ 珠子只活 1 回合、收入约 1/回合 ⇒ **闭环窗口极小**。
- 蓄能可负担 = 54（ep1: 45 / ep2: 9）⇒ 新门槛压掉了 ep=1 那 45 个（v1.5.82 生效 ✓）。
- 附带观察：网络在那个点上选的是**全息屏障（100% 套给别人）** —— 它已经会一种"偏结盟"行为（护人），
  但不会另一种（**拆防御**，即用户指出的激光眼用法）。

### 4. 结论与下一步（未做）
两层，缺一不可：
1. **供给层**：机会几乎不出现（1/105）⇒ 得让它"持珠且付得起"的那一刻变多（门槛已把蓄能收到 ep≥2 ✓，
   但珠子 1 回合寿命 + 收入 1/回合 ⇒ 窗口天生很窄）；
2. **选择层**：机会出现时网络给 0% ⇒ 这是**价值估计**问题（在候选表里、可负担、却不选）。
⇒ 按 v1.5.82 §4 的复盘，正确顺序是"**先修条件、再谈奖励**"：
   - 若走奖励：必须是**闭环**（得珠 → **打出去且命中**）✓ + 可归因 ✓ + **退火** ✓ + "行为不许消失"守卫 ✓；
   - 验收要量**闭环率**，不要量"浪费率 = 0"（P1 的教训）。
   - 另一条待查（低成本）：`laserEye` 的收益依赖"之后有人真的打上去"（结盟结构）⇒
     先量"防御被拆掉之后，是否真有人跟进攻击"，再决定这个能力值不值得练。

## v1.5.82 — 蓄能经济门槛（用户裁定）：ep<2 不许蓄能；顺带更正"天火是珠子卡"的错误

> 用户原话："ep=1 的时候本来就不应该蓄能，因为用到蓄能花费最低的激光眼也要额外一个 ep，
> 至少要让他 ep>=2 才开始用，不然就是浪费。"

### 1. 规则核准（据此把"浪费"精确化）
- 蓄能 `charge`：cost=1，描述是"获得 1 枚能量珠……**只保留到下一回合**" => **珠子只活 1 回合**。
- 唯一的珠子消费卡是**电磁炮 `railgun`**（2 ジ + 1 电珠）=> 付掉 1 点蓄能后要在**下一回合**凑出 2 ジ；
  收入约 1 ジ/回合 => **ep=1 蓄能必然过期**。
- ⚠ **更正我自己的错误**：**天火 `firestorm` 不是珠子卡**（它引爆自己贴出且停留 <=3 回合的**符咒**）。
  `tools/probe-candslot.mjs` 里那句"珠子消费技能：电磁炮/天火"是我手写的错话 => 已改。
- ⚠ 这是**启发式**不是定律：收入 >1/回合（聚能环第 3 次起 +3、避雷针 +4）时 ep=1 蓄能也可能成立
  => 要放宽就按"预期收入"分档。

### 2. 实现（零指纹成本）
放在**共享入口** `js/train/evo.js` 的 `policyChooserN`（页面 `pickChampion` 与考卷/训练都走它），
**只作用于 v7 分支** —— legacy（v5/v6）保持旧口径，历史基线才可比。`evo.js` 不在规则指纹内 => 不引 D16 抖动。

### 3. 实测（改前 -> 改后；线上包 40 局 multi 的 chargeProfile + 12532 次决策的考卷）
| 指标 | 改前 | 改后 |
|---|---|---|
| 蓄能/局 | 0.57 | **0.10** |
| 得珠 / 过期 / 花掉 | 23 / 23 / **0** | 4 / 4 / **0** |
| 标准考卷 1st（20 局） | 40.7% | **41.3%**（严胜 40.6%->41.0%）|
| 蓄能占出手比 | 0.9% | 0.1% |
| ep 分带 0-1 | 80.7% | 80.0% |

- **门槛生效且不损分**（+0.6pt 在 n=20 局属噪声量级，不构成"变强"的结论）。
- **珠子闭环仍未建立**：得珠 4 颗全部过期、花掉 0 => 只解决了"别浪费"这半边。

### 4. 参考（用户要求复盘：以前那个会用聚能环的冠军是怎么来的）
- **自然路线**：线上冠军 `long-33`（v1.5.7 上线、v1.5.8 被 eco-34 换掉）出手分布里**聚能环 42%**，
  它的环**不是奖励练出来的**，而是**长程模式里环真的赚钱**（首次 +1、连续第 2 次 +2、**第 3 次起 +3ジ/回合**
  vs ジ 的 +1）=> 进化自己找到的。更早在 `7a4cd0e`（v1.5.1）就结案过 `P0-d`：**"环是 3 血赛程太短才没长出来"**。
- **奖励路线**（v1.5.23-v1.5.45，八次尝试）：成功配方三要素**缺一不可** ——
  ① 奖励可归因（`setVoid` 带 `byPid`）；② 开环判据用**动作事件**（该座真的施放聚能环，含首次；
  旧 `delta>=2` 漏首次且误判避雷针）；③ **退火强迫**（ε=1 只在前 40 代，产物取自无强迫的后段）。
- **它的问题**：唯一"成功样本" `v7r2-35` 复核后是**第 4 回合仪式**（小雷 187 次里 160 次在第 4 回合、
  **砸中滚环者 0/187**）=> 收益是假的、已回退；命中率 ≈**1/24**；"逃过冻结 ≠ 学会"（18 种子：3 逃逸 0 学会）；
  代价 = 标准考卷 **-3.8pt**、活跃场进攻率 **11%**（偏被动）；根因是**选择哲学**（强迫让形状分变差 =>
  探索期个体永远不算 better => 提升链不留它们）。
- **可参考的三条**：① **先看收益结构与条件，再谈奖励**（环在长程里赚钱才长出来；珠子的问题在**条件**
  —— 只活 1 回合，所以我先加 `ep>=2` 门槛）；② 真要用奖励就必须"可归因 + 动作事件判据 + 退火"三件套；
  ③ 验收要量**闭环**（得珠且**花掉**），别量"浪费率 = 0"（P1 的教训：那是把行为删掉刷绿）。

## v1.5.81 — 诊断：冠军"放不出强技能"的机制（**只诊断，未改行为**）

> 用户提问："当前冠军对攒 ji 的威胁性已经很高了…我担心威胁太多了之后会导致以后冠军反而放不出强技能"。
> 这条担心指向的是**当前状态**，所以先只做只读诊断（不改行为、不训练）。

### 1. 更正我自己引用的一个**死指标**
- `mirrorHealth.heavyPerGame` 读 0.00 **对所有包恒零** ✗：`rec.heavy` 只在训练用的 econ chooser 下计数
  （`js/train/evo.js:605` `if (c.ep >= 2) rec.heavy++`），而 `mirrorHealth` 不挂那个 chooser ⇒ 恒 0。
- 复核 §15-4 与我先前给你的"重击/局 = 0.00"**都建立在这个死指标上** ⇒ 作废。
- 另一个读数 **`cost>=3 出手占比 = 0%` 是真指标**（`tools/eval-5p.mjs:312` 按 `c.ep >= 3` 计数）⇒ 结论仍成立。

### 2. 真读数（官方考卷经济段，12571 次决策，线上包）
- **决策时 ep 分带：0-1 = 80.7% · 2-4 = 18.0% · 5-9 = 0.5% · 10+ = 0.8%**
- `ep>=3 的决策占比 = 1.8%` · `cost>=3 出手占比 = 0.0%`
- 出手分布：ジ 58.8% · 狙击枪 16.7% · 枪 14.0% · 全息屏障 4.3% · 反弹/防御/八卦阵 各 1.4% · 蓄能 0.9% · 坦克 0.4% · 激光剑 0.3%
- 费用 ≥3 的技能共 7 个（`drain bigT rod mine purify dualGun mirror`，最高 5）。

### 3. 机制（三层，按贡献排序）
1. **经济（主因）**：收入约 1 ジ/回合，却每回合花在 1~2 费的卡上（枪/狙击枪）⇒ **80.7% 的决策发生在 ep 0-1**
   ⇒ `ep>=3` 只有 1.8% ⇒ 那 7 张 3+ 费卡**几乎从不可负担**、压根不进候选表
   （`tools/probe-candslot.mjs` 的中期决策表里 7 张一张都没出现 ✓）。
2. **偏好（次因）**：极少数可负担时刻，网络仍给约 0%（`proto/charge/guard/reflect/bagua/jinshield` 一律 0.0%）。
   连**蓄能**都一样：它在 ep=1 时 24/24 次出现在候选里、却 **0.0%** 被选 ⇒ 这是**价值估计**问题，不是可负担性问题。
3. **珠子链彻底断**：蓄能 0.9%、得珠不花（`pierceMissingCards: ["railgun"]`）⇒ 电磁炮/天火（各 2 费）一次都没打出去。

### 4. 顺带确认与存疑
- 深经济对手确实在扣它分：含深经济对手 39.2% vs 不含 50.0%（Δ−10.8pt）⇒ 压力存在，缺的是**信号**。
- 候选槽位结构仍在（`gun 3.33 槽 / holo 3.33 槽` vs 自指向技能各 1 槽）；但本次探针的"聚合概率"是否已按
  v1.5.74 的两级采样（F2）口径计算**未验证**（表中 gun 74.2% / holo 24.2% 可能仍是旧口径）⇒ 列为待查，不据此下结论。
- **已查清（同日补记）**：`tools/probe-candslot.mjs` 的"聚合概率"取自 `P.forwardCands(...).probs`
  —— 那是**条目级** softmax；而 v1.5.74 的 F2 之后，真正决定出手的是 `chooseCandidates` 的**两级口径**
  ⇒ 该表的 `gun 74.2% / holo 24.2%` **不等于**冠军的真实采样分布（真实分布是考卷给的
  ジ 58.8% / 狙击枪 16.7% / 枪 14.0%）⇒ **"槽位结构仍在"这一条不成立**；
  该探针要在 F2 口径下重写后才能再谈结构偏差。

### 5. 结论 / 下一步（未做）
这是**奖励设计**任务（与刚失败的"靶向"同类），但有一个关键差别：**蓄能已经在被选中（0.9%）**，
不是"从不发生的动作"⇒ 窄奖励**有可能**给它定向加压。可选路线（按"能反证"排序）：
1. **闭环奖励**（得珠且**花掉**才算，沿用 v1.5.76 的 `countBeadSpent`）——必须配"行为不许消失"的守卫
   （复核 §11 的教训：P1 的"浪费 0%"曾是把行为删掉刷绿的）；
2. **暴露度**：池里加"会惩罚廉价消耗战"的对手（反弹墙/tankline 已在池内，但压力没变成信号）；
3. **槽位口径复核**：先确认 F2 之后聚合概率怎么算，再谈"结构偏差是否还在"。

## v1.5.80 — UI 三条（第八轮复核 §5）：先在真页面复现，再按证据修 —— ① 可取消 ② 有反馈 ③ 不可复现

> 新增探针 `tools/ui-probe.mjs`（与 `battle-test.mjs` 同源的 CDP 骨架，**只读**、
> 截图落 `docs/artifacts/ui-out/`）。复核 §5 的三条我逐条跑过，**修复前**的读数：

| 项 | 修复前实测（真页面） | 修复后 |
|---|---|---|
| ①-甲 弹窗开着时的提示 | 「第 3 回合准备 —— **请出招**（出招前可"悔一步"）。」✗ | 「请为【激光剑】选择目标（Esc 或「取消」可放弃）。」✓ |
| ①-乙 弹窗开着时改点别的技能 | **静默替换**成新技能的弹窗，第一个选择消失、无任何提示 ✗ | 日志留痕「（已放弃【激光剑】的目标选择）」+ 切到新技能 ✓ |
| ①-丙 弹窗开着时点自指向技能 | **立即落子**（round 2 → 3）= 误触白扔一回合 ✗ | 先留痕再落子 ✓ |
| ② 结算期间（`B.locked`）点击 | 无任何反馈（提示仍是上一句）✗ | 「本回合正在结算…**你刚才的点击没有生效**。」✓ 且**不排队**（不会出现"迟到的动作"）✓ |
| ③ 连续"ジ不足·未发动" | **在当前构建复现不出来**：10+ 回合按座归因 = `{}` ✗ | 保持（格子早已把买不起的置灰；已加回归断言）✓ |

### 1. 修法（`js/ui/ui.js`）
- 新增待选状态 `B.picking`：目标弹窗/蓄能弹窗打开时登记，选择或取消时清除 ✓。
- **必须能取消**：弹窗里加「↩ 取消」按钮 + **Esc** + **点遮罩**三条出口（`cancelPick()`）；
  取消后明确提示「已取消【X】，请重新出招。」且**不消耗回合** ✓（探针 ①-丙 验过 round 不变）。
- 结算期间点击 ⇒ `pickSkill` 提前返回并**明确告知**（不做排队：排队会引入"迟到的动作"这类新状态，风险大于收益）。
- 改主意/误触 ⇒ 在回合日志里**留痕**，玩家不再"点了却没反应"或"莫名其妙白扔一回合"。
- 新局重置 `B.picking`（防止上一局的待选状态跨局泄漏）。

### 2. 守门
- `tools/ui-probe.mjs`：**10/10 全过**（需要 Chrome ⇒ 不进 np-test；断言的是"修复后的正确行为"，PASS = 正确）。
- np-test **D70**：只锁**接线**（`picking` 登记/清除、`cancelPick`、Esc/遮罩、提示文案、
  「你刚才的点击没有生效」、「↩ 取消」按钮、新局重置、以及"请出招"仍保留给回合开始）✓。
- `docs/artifacts/ui-out/` 已进 `.gitignore`。

### 3. 关于复核 §5-③（"ジ不足"）的结论
它建议"按状态泄漏（pending-target 未清）查"。实测：**格子早就不允许点买不起的招**（`btn.disabled`，
代码注释写着"不再'能点但显示无效'"）⇒ 真人座根本触发不到 `outcome:'insufficient'`；
10+ 回合里按座统计 = `{}`（连 AI 座也没有 —— AI 走 `legalActions` 的 `affordable` 过滤）。
⇒ 该条**在当前构建不可复现**；探针保留这条断言，一旦回归就会红。若复核手上有当时的复现步骤，请给我。

## v1.5.79 — 威胁靶向奖励的第一版是**静默空操作**（已修）：v7tgt4 整臂读数作废

> 复核 §15-1 建议"把靶向当能力练（窄奖励或教师示范）"。我选了窄奖励并写了 `TGT_W`，
> 跑完 `v7tgt4` 后四个产物的靶向率**全都低于线上包**（11.0 / 8.8 / 14.4 / 17.7% vs 线上 27.8%）——
> 按纪律先查"奖励到底有没有真的发出去"，结果发现**它一次都没发过**。

### 1. 三条我自己的 bug（按发现顺序）
1. **回合重建漏了重置 `seen`** ⇒ `countThreatHits ≡ 0`：`countPressRounds` 在换回合时 `seen = {}`，
   我这一版只 `cur++` 没清空 ⇒ 五个 pid 都出现过之后**每个动作事件都被判成新回合** ⇒ 回合重建变垃圾 ⇒
   威胁与我方伤害永远对不上 ⇒ **奖励恒 0**（与 v1.5.68 的 `seatPen ≡ 0` 完全同型）。
2. **标度错**：即便修好 (1)，威胁命中真实频率只有 **0.30 次/局**（线上包实测）⇒ `min(1, hits/2)`
   几乎每局都落在 0~0.15 ⇒ 奖励退化成**常数级微扰**、梯度≈0。改成 `min(1, hits/1)`：
   0 次得 0、1 次即满额 ⇒ 约 **30% 的局吃满**（方差大 = 真有梯度）。
3. **诊断与奖励的封顶不同步**：`threatCapRate` 一度还按旧的 `hits >= 2` 统计 ⇒ 修了标度仍报 0%（假红）。

### 2. 教训（方法论，已加进本文件的写法习惯）
- **门槛太弱 = 没门槛**：D68 第一版只用 **≤2 回合**的合成序列 ⇒ 恰好绕开了 bug (1)。
  已加硬：**≥6 回合重复序列** + 一条源码断言（"换回合处必须 `seen = {}`"）。
- **搜索锚点只用 ASCII**：本轮我连续多次 patch"匹配 0 次"，根因是**bash heredoc 里的中文在搜索串中被损坏**
  （错误信息里就是乱码），而中文出现在**替换文本**里没问题 ⇒ 以后 python 打补丁：锚点 ASCII、正文可中文。
- **行内 `//` 会吃掉右花括号**（本轮又踩一次）：带块结尾的行不能追加行内注释。

### 3. 修好之后的读数（这次是真的）
- 支付诊断（新增在 `mirrorHealth`，只读、不改行为）：`threatHitsPerGame` / `threatCapRate`。
- `multi` 20 局：线上包 **0.30 次/局 · 30% 局吃满** · `v7tgt2-31` 0.35 · 25% · `v7tgt4-62` 0.05 · 5% · `v7tgt4-64` 0.00 · 0%
  ⇒ **跨策略从 0% 到 30% 有区分** ⇒ 奖励现在真的能当梯度用（这才是这个思路的第一份有效证据）。
- 顺手体检了**已上线**的两条奖励是否也在空发（它们同样依赖 `e.source === seat`）：
  `pressRounds/局 = 2.05`（封顶 /3）、`pierceHits/局 = 1.00`（封顶 /2）⇒ **都在发** ✓（没有历史性空发）；
  但 press 对多数策略已接近封顶（1.85~2.05）⇒ 梯度偏弱，列为后续可调项。

### 4. 裁定与后续
- **`v7tgt4` 整臂读数作废**（它测的奖励是死的）⇒ "窄奖励不能让能力长出来"**不是**本轮结论；
  用修好的奖励重跑 `v7tgt5`（同池 E、同 seed、`TGT_W=0.05`），验收仍是复核的**双条件**：
  **G6 靶向率 ≥40% 且混合场不低于线上包**（防为窄指标牺牲综合）。
- `tools/promote-champion.mjs` 现在也打印**珠经济双向口径**与 G4/G5 阻断结论；`champ-audit` 增加裸参数点名警告。

## v1.5.78 — 第七轮复核（`docs/AUDIT-RESPONSE-v1.5.77.md`）的采纳、独立复现与自我更正

> 复核对象 = 线上包 `v7tgt2-52` + 新臂 `v7f3`（91/93/94）。**我独立复跑了他们的量具**（`tools/gate-drafts.mjs`），
> 关键格逐一吻合 ⇒ 下面标 `[复现✓]` 的是我自己跑出来的。

### 1. 我接受的更正（三条是我的过度声称）
1. **"它终于会瞄人了" ✗** —— 定量否掉：靶向率 **22.5%**（选择口径，均匀 25%、"只瞄最肥" 4.1%、
   一行代码的"瞄威胁者" 100%）`[复现✓]`；我自己的命中口径同时给出 29.4% `[复现✓]`。
   两个独立口径同向 ⇒ 准确说法是"**会对当场作死的人出手（反狙击/反环的收益真实），但没有优先清除威胁的策略**"。
   那句话是我自己定的 40% 阈值、又是我自己声称达标 ✗。
2. **91 的"最强"框架 ✗** —— A 考卷 53.1%（历史最高）**与真实强度脱钩**：四个一行脚本在长程能打它 **78~82%**
   `[复现✓：G4 FAIL]`。机制查清：它**练出了 `pierce.reflect`、没练出 `pierce.defense`**（面对"只防御"7.0 次攻击里
   只 2.6 次带穿防），出手里坦克/激光剑≈0 ⇒ 我上一轮"91 最强"的说法只反映**拟合了那张卷**。
3. **P1 的"浪费 0%"是把行为删掉刷绿的 ✗** —— 新包连"蓄能"都不做了（0.00~0.06 次/局），
   而整条珠经济（蓄能→电磁炮/天火）**四代包一次都没闭环** ⇒ 只看 `wasteRate` 它永远绿。
   ⇒ 采纳：判据改**双向**（"得珠>0 且花珠率>0"才算闭环），`promote-champion` 已按此打印。

### 2. 我采纳为**阻断门**的（复核 §15-1）与理由
- **G4 克制表 + G5 破防反射 ⇒ 进阻断面**（`promote-champion` 调 `tools/gate-drafts.mjs`；可用 `--force` 越过并留痕）。
  理由（**判别力检验**）：线上包 G4 两模式 PASS（最克 22%/18%）、G5 PASS（防席 0%）；
  `v7f3-91` FAIL（G4 78~82% / G5 65~75%）、`v7f3-94` FAIL（G4 67~68%）`[复现✓]` ⇒ **能分开已知好与已知坏** ✓。
- **G6（靶向率 ≥40%）⇒ 只记录不阻断**：四代包（含线上）全部 **3.5~22.5%** `[复现✓]` ⇒ 是"能力未长出"，
  做成阻断会把所有候选一起挡死；正确路线是把它当**能力**练（窄奖励/教师示范），验收还要加"不许牺牲综合"（H 混合场 ≥ 线上）。
- **G3（座位 ≤15pt）⇒ 不采纳**：该阈值**连线上包自己都过不了** ⇒ 不满足判别力检验（座位仍用 `audit-lib.seatSymmetry`）。

### 3. 我对他们量具的三处修正（采用前必须先能自己维护）
1. `seatSpread`（G3）**完全没设 `slotSalt`** ✗ ⇒ 所有对局共用一个盐，"身份通道"会被放大而不是洗掉；已补 avalanche 盐（与轮座去相关）。
2. `loadBakParams` 用 `slice(indexOf('{"v":'), lastIndexOf('}'))` ⇒ 传 **bundle**（含 meta）会解析失败 ✗；已改为"先认 bundle 正则，再退回 .bak"。
3. 他们的说明里"0 号座 66~76%"**与本轮 §12 自己的表矛盾**（91/multi P0 = 0%）⇒ 是盐修好前的陈词 ✗；已按本轮分级改写。
   （另：`h32` 原本两份实现已合并；G3 局数改为 `GATE3_GAMES` 可调，便于门禁快速跑。）

### 4. 顺手采纳的两条工具/判据修正（复核 §8/§11）
- `tools/champ-audit.mjs`：**裸参数点名警告** —— `flag()` 只认 `--k=v`，`--games 80` 会被静默忽略、
  按默认 20 局跑完且无警告（复核第一版表因此整表重跑）⇒ 现在会打印"这些参数已被忽略"。
- `tools/promote-champion.mjs`：珠经济改**双向打印**（得珠/过期/**花掉**/浪费率/**花珠率**），
  并显式标注"未闭环 = 没长出能力"而不是"指标好看"。

### 5. 复核没有改变、我独立确认的两条事实
- **用户四连败输得合法**：`ui.js:517` 人类落子前克隆 `preState`、AI 从 `preState` 决策 ⇒ 无决策泄漏；
  门禁 114/114、指纹 `8dda2677` 一致；且它把"王八流/装死流"从旧包 20~28% 治到 **0%**（真能力）。
- **"AI 专瞄我"体感是否**：40 局轮座里人类席承接攻击落点 12~19%、其余四席 20~22%。

### 6. 未做（留作下一轮，按复核 §15 的"能反证"排序）
1. **把"靶向"当能力练**（窄奖励或 `targeter` 教师示范）；验收 = **G6 ≥40% 且 H 混合场 ≥ 线上包**（防为窄指标牺牲综合）。
2. **G5 的破防反射**：以 **94** 为底继续训（结构最干净：座位对称 + 会破防），91 不建议在其上加分（缺整条反射）。
3. **UI 三条**（复核 §5，真页面可复现）：① 目标弹窗还开着却提示"请出招"（真人点技能被吞）；
   ② 动画期间点击**静默丢弃**（无排队无反馈）；③ 日志连续四行"ジ不足·未发动" ⇒ **按状态泄漏类查**（pending-target 未清）。
4. `chargeProfile` 的"重击/局 > 0"配套指标（同一链的另一端，四代包全是 0.0）。
5. 座位：接受"每包方向不同"，只做行为对称统计（n≥150），停止找"第 8 处 pid 顺序"。

### 7. 读数的 `pre-F2 / post-F2` 分界（复核 §7 的方法论警告）
`v1.5.74` 的两级采样改变了**动作分布的采样口径** ⇒ A 考卷/G/反弹墙/场A/B 全是口径的函数。
**本文件 v1.5.74 之前的读数一律视为 `pre-F2`，不得与之后直接相比。**
（实测线上包 A 考卷 pre-F2 39.9% / post-F2 39.9% 恰好没变，但这是巧合，不是普遍规律。）

## v1.5.77 — 快记：带 P1 的重训（`v7f3`）结果 —— **P1 目标达成（蓄能白扔消失），但强度/座位仍是取舍**

> 用户要求："14 点之后得到结果就快速记录一下，等晚上再做" ⇒ 本文只**记录事实**，裁定与完整对照留到晚上。

### 1. 臂 `v7f3`（F1+F2+P1 · 500 代 · 池 E · seeds 91–94）
- 汇总：**有提升 3 / 零提升 0 / 缺产物 1**；产物 `v7f3-91`（哈希未记）、`v7f3-93`（`ebeefc73e10d`）、`v7f3-94`（`914e04859cc5`）。

### 2. P1 的验收：**"没 ジ 却用蓄能"这个现象消失了** ✓
`chargeProfile`（40 局，长程）：

| 包 | 蓄能/局 | 得珠 | 过期 | **花掉** | 浪费率 |
|---|---|---|---|---|---|
| `v7f3-91` | **0.00** | 0.00 | 0.00 | 0.00 | —（不再蓄能）|
| `v7f3-93` | **0.03** | 0.03 | 0.03 | 0.00 | 100%（几乎不蓄能）|
| `v7f3-94` | **0.03** | 0.03 | 0.03 | 0.00 | 100%（几乎不蓄能）|
| 线上包（对照） | 0.30 | 0.30 | 0.30 | 0.00 | 100% |
| 旧候选 `v7f2b-81/82`（对照） | 1.75 / 1.15 | — | — | 0.00 | 94% / 100% |

⇒ P1 只给"**花掉**"记分 ⇒ "蓄能后扔掉"相对"不蓄能"永远是亏的（白丢 1 ジ）⇒ 演化直接**放弃了蓄能**
（0.00~0.03 次/局）。**闭环没学会**（仍 0 次花掉），但**浪费现象本身消失了** ✓。
（晚上的问题：要不要再补"经济侧"——蓄能只在"下回合拿得出 2 ジ"时才值得，属于时机特征/教师示范那条线。）

### 3. 五道门与 A 考卷（快记，未做完整对照）
| 候选 | A 考卷 1st / 严胜 | 座位(n=150) | 反弹墙/局 | 场A | 场B 清场 | G | 五道门 |
|---|---|---|---|---|---|---|---|
| `v7f3-91` | **51.9% / 51.4%**（历史最好）| **34.5pt** ✗ | **17.9** ✓ | 0.31 ✓ | 4.0 ✓ | 3.94 ✓ | ✗ 座位 |
| `v7f3-93` | 40.6% / 39.8% | 37.5pt ✗ | 9.2 ✓ | 0.336 ✓ | 0.00 ✗ | **2.16** ✗ | ✗ 座位+G+场B |
| `v7f3-94` | 32.3% / 31.3% ✗ | **19.6pt** ✓ | 4.4 ✓ | 0.311 ✓ | 1.5 ✓ | 4.54 ✓ | ✅ **唯一全过** |
| 线上包 | 39.9% / 39.5% | 20pt | 14.35 | 0.31 | 0.57 | 4.17 | ✅ |

⇒ 又是取舍：**91 最强（A 51.9% + 墙 17.9）但偏座 34.5pt** ✗；**94 全过门却最弱（32.3%）** ✗。
**未换包**（等晚上做完整对照与裁定）。

### 4. 晚上的待办
1. 对 91 与 94 做完整对照：靶向场、狙击场（mixed/wall）、全息-vs-原型制御复测、`chargeProfile` 复核；
2. 裁定是否换包（91 强但偏座 / 94 过门但弱 / 都不换）；
3. 91 的座位问题定性：是老的"0 号座诅咒"（结构性）还是可修的（与 `v7f2b-81` 的 33pt 同族）。

## v1.5.76 — P1：给"把蓄能珠花掉"加窄奖励（只认花掉，两次封顶）

### 1. 动机（用户 4 局实测第二问的后半）
- 复查结论（v1.5.75）：F1 已消除"买不起也进候选 ⇒ 被引擎静默降级成ジ"的**幻影动作** ✓，
  但**珠子仍在浪费**：`chargeProfile`（40 局）浪费率 **94% / 100% / 100%**（`v7f2b-81` / `v7f2b-82` / 线上包），
  且新候选蓄能**更频繁**（1.75 / 1.15 次/局 vs 线上 0.30）。
- 机制（两条同时成立）：
  1. **经济**：珠子消费卡（电磁炮/天火）都要 **2 ジ**，而蓄能常发生在 `ep = 1`（花掉唯一 1 ジ）⇒ 下回合必然凑不出；
  2. **信号**：**没有任何奖励为"把珠子花掉"记分**（窄奖励不能 bootstrap 闭环的老问题）。
  珠子本身**在状态特征里看得见**（`policy.js:203` 的 `Math.min(me.elec,1)/Math.min(me.boom,1)`）⇒
  **缺的是信号，不是输入**。

### 2. 落法（`js/train/evo.js`，与 `RING_W / PRESS_W / PIERCE_W` 同族）
- `countBeadSpent(events, seat)`（纯函数）：真源 = `{type:'bead', pid, kind, delta}`，
  **只认 `delta < 0`（花掉）** —— 得珠（+1）、过期（`beadExpire`）、别人的珠子、无 delta 一律不计。
- `BEAD_W = 0.05` + `setBeadReward/beadReward`；每局计数后 `min(1, spent/2)`（**两次封顶**）并进 `gFit`。
- ⚠️ `evo.js` **不在规则指纹里** ⇒ 不触发 D16 churn —— 这一点被实测验证（D16 仍然绿 ✓）。

### 3. 守门
**D66**：只认自己花掉的（得珠/过期/别人的/无 delta 四类反例）+ 空事件安全 + 默认权重为正 + 可调 +
源码结构断言（`beadBonus` 必须真的并进 `gFit`，漏了就是静默空操作）。

### 4. 下一步
带 P1 的重训臂 `v7f3`（500 代、池 E、seeds 91–94）已启动 ⇒ 验收 = ① `chargeProfile` 浪费率显著下降；
② 反弹墙/F/G/座位不再互相顶（当前 `v7f2b-82` 就是墙 1.15 ✗ 那一档）。

## v1.5.75 — F1+F2 后的重训裁定：**250 代不够、500 代治好了 G 塌缩**；全息屏障的偏爱消失，珠子浪费仍在

### 1. 两臂对比（都是池 E、F1+F2 已生效）
| 臂 | 代数 | 产物 | 健康门槛读数 |
|---|---|---|---|
| `v7f2` | 250 | **0/4** ✗ | 全被拦：G **1.38 / 2.20 / 2.69 / 2.91** ✗（+ 一个自对局回合 45 ✗）|
| `v7f2b` | **500** | **2/4** ✓ | G **3.59 / 4.54 / 4.59 / 4.84**（4/5 达标）✓ |

⇒ **诊断成立**：旧规则的"多目标技能 4 个槽位"其实是个**意外的探索助推器**（攻击类技能每步 4 倍试错率），
F2 把它拿掉后 250 代的搜索预算不够；500 代把 G 恢复到 3.5~4.8 ✓。
⚠️ 但 G 阈值本身**没有被 F2 弄坏**（标定：种子 2.97/3.03 · 线上 4.17/4.06 · v17-146 1.00 ⇒ 仍分得开）。

### 2. 两个产物的读数（五道门 + 关键指标）
| 候选 | A 考卷 1st/严胜 | 座位(n=150) | 反弹墙/局 | 穿透 | F | G | 五道门 |
|---|---|---|---|---|---|---|---|
| `v7f2b-81` | **43.4% / 43.2%** | **33pt** ✗ | **18.00** ✓ | 360 | 28% | 3.48 | ✗ 偏座 |
| `v7f2b-82` | **44.7% / 44.7%** | 28pt ✓ | **1.15** ✗ | 23 | 32% | 4.49 | ✅ **通过**（墙 1.15 过 >0.5 的线，但比线上差 12 倍）|
| 线上包（v7tgt2-52） | 39.9% / 39.5% | 20pt | 14.35 | 287 | 31% | 4.17 | ✅ |

⇒ 又是"某一项精彩、另一项崩掉"（与 v1.5.70 的裁定同型）⇒ **不换包**。

### 3. 用户两问的复查结论
- **① 全息屏障 vs 原型制御**：**已治好** —— F2 生效后重训的产物里 `holo` 的**聚合概率降到 0.0%**
  （旧规则下 5.9~7.1%、单条最高 46~62%）⇒ 槽位放大移除 + 重训 = 不再偏爱这张劣势卡。
  ⚠️ 诚实标注：本次采样的状态被 `ji` 100% 主导，所以这条只能表述为"**不再有证据显示它仍被偏爱**"，
  更强结论需要改进探针触发器（已修 `tools/probe-candslot.mjs`：旧触发器写死 `round === TR+(g%4)`，
  快战型候选（82：99% 分出胜负）在到达该回合前就打完了 ⇒ 探针空转 0 个决策）。
- **② 蓄能**：**只治好一半**。F1 消除了"买不起也进候选、被引擎静默降级成ジ"的**幻影动作** ✓，
  但**珠子仍在浪费**：`chargeProfile`（40 局）浪费率 **94% / 100% / 100%**（81 / 82 / 线上），
  且新候选蓄能**更频繁**（1.75 / 1.15 次/局 vs 线上 0.30）。
  机制：珠子消费卡（电磁炮/天火）都要 **2 ジ**，而蓄能常发生在 `ep=1`（花掉唯一 1 ジ）⇒ 下回合必然凑不出；
  且**没有任何奖励为"把珠子花掉"记分**（窄奖励不能 bootstrap 闭环的老问题）。
  珠子本身**在状态特征里看得见**（`policy.js:203` `Math.min(me.elec,1)/Math.min(me.boom,1)`）⇒
  缺的是**信号**不是输入。

### 4. 下一步建议（待裁定）
1. **P1：给"花掉珠子"加窄奖励**（`js/train/evo.js`，与 `RING_W/PRESS_W/PIERCE_W` 同族：
   由 `bead` 事件（`{type:'bead', kind, delta:-1}`）归因、`min(1, spent/2)` 封顶）。
   ⚠️ `evo.js` **不在规则指纹里** ⇒ 不触发 D16 churn ✓。
2. P1 落地后**再训一臂**（500 代、池 E）：这样重训同时修"全息偏爱 + 珠子闭环"，
   比再多跑纯种子更值得。
3. 冠军是否换：`v7f2b-82` 五道门过、A 考卷 44.7% ✓，但反弹墙 1.15 ✗（线上 14.35）
   ⇒ 建议**等 P1 重训**出来再一起比，而不是现在用 --force 换。

## v1.5.74 — F1 + F2：候选表只收买得起的招 + **两级采样**（技能层 → 条目层），槽位不再等于概率

> 起因：用户 4 局实测两问（爱用全息屏障却不用原型制御 / 没 ジ 却用蓄能），机制见 v1.5.73。
> 用户裁定"规则没问题（全息屏障是**可见的结盟信号**，多人局有用，只是 AI 不会结盟）"⇒
> **F3 撤销**，只做输入/采样侧的 F1 + F2。

### 1. F1：`candidatesFor` 只收**买得起**的招（`js/train/policy.js`）
- 病：`legal` 每项都带 `affordable`（play.js:17/21），而这里只判 `l.key` ⇒ 实测 `ep=0` 时蓄能仍在候选里
  （`{0:12,1:14,2:2}`），被选中后引擎**静默降级成「ジ」**（play.js:60/93）⇒ **幻影动作** + 错误归因。
- 落法：`if (l.affordable === false && !opts.allowUnaffordable) continue;`
  ⚠️ **只过滤显式 `false`** —— 页面/工具/测试夹具自造的 `legal` 通常不带该字段，若按 `!l.affordable`
  过滤会把候选表清空（冠军只出ジ）。这是 **D51 在我第一版实现上抓到的真实回归**，已写进注释与 D65。

### 2. F2：采样改成**两级**（`forwardCands`）
- ① 技能层：每个技能取其**条目里的最大值**（"该技能最好的一种打法"）⇒ 每个技能恰好一个槽位；
- ② 条目层：在该技能内部按其自身 softmax 选目标/珠类型（**目标学习性保留**）；
- `probs[i]` 仍是**条目 i 的最终概率**（两级相乘，全表严格和为 1）⇒ 采样/工具/单测无需改；
- `argmax`/`greedy` 仍取**全表最大对数几率**（两级后"条目少"的条目概率反而更高 ⇒ 不能再用 probs 取最大）。

### 3. 实测（线上包，改动前后同口径）
| 观测 | 改动前 | 改动后 |
|---|---|---|
| 候选槽位/决策（枪 / 全息 / 蓄能 / 原型制御） | 3.36 / 3.36 / 2.00 / 1.00（**含买不起的虚高**） | **1.93 / 1.93 / 1.14 / 1.00** |
| 技能聚合概率（ji / gun / snipe / holo） | — | 51.3% / 35.7% / 7.1% / **5.9%** |
| 原型制御聚合概率 | ≈0%（1 槽位被 3.36:1 压制） | **仍 ≈0%** ⇒ 槽位放大已消除，**价值错误要靠重训** |
| A 考卷 1st / 严胜（60 局） | 39.9% / 39.5% | **39.9% / 39.5%**（**未退化** ✓） |
| 蓄能（`chargeProfile` 40 局） | 0.30 次/局 · 100% 浪费 | 待重训后复测（候选中不再出现买不起的蓄能 ✓） |

### 4. 守门与夹具修正
- 新增 **D65**：① 显式买不起不进表 + 逃生口；② 不带 `affordable` 字段的 `legal` 不得被清空；
  ③ 零权重下"4 条槽位 vs 1 条槽位"的技能概率必须相等（**旧实现会 4:1**）；④ probs 严格归一；
  ⑤ greedy 语义不变；⑥ 源码结构断言。
- **D54**（引擎层对称性烟测）夹具改用**脚本**（`wrapBotN(pickAggro)`）+ 用**引擎自己的裁定**判胜负：
  旧夹具用随机权重神经网络，F2 后条目少的技能拿回份额 ⇒ 局面更平（17/120）；而"有人活着且血量唯一最高"
  的口径又把**全员阵亡**（v1.5.65 起按累计伤害判胜）全算成平局（实测只剩 12/120 判"分出胜负"）。

### 5. 后续（未完成，按顺序）
1. **重训**（臂 `v7f2`，池 E，seeds 71–74）：旧权重是在旧采样规则下选出来的 ⇒ 必须在新规则下重选。
2. 重训产物过五道门后换包；**两个包指纹必须重记**（`promote-champion` + `promote-champion2p`），
   否则 D16 红（指纹 `d42ec70a` → `8dda2677`）。

## v1.5.73 — 两个探索性问题的机制级答案（用户实测 4 局后提出）：**候选"槽位数"直接变成采样概率** + **买不起的招也在候选表里**

> 用户 4 局实测（`results/epirus-battle-{21,25,41,60}回合.txt`）：强度"完全够了"，但
> ① 冠军**爱用全息屏障、从不用原型制御**；② **没 ジ 却用蓄能**；并怀疑"是不是你原始输入带来的偏好"。
> **两条都成立，且都在我这边** —— 下面是可复证的机制，不是猜测。

### 1. 病根 A：`chooseCandidates` 是在**候选条目**上采样，不是在**技能**上
`js/train/policy.js:644-647`：`for (i…) { acc += f.probs[i]; if (r < acc) return cands[i]; }`
⇒ softmax 分布在候选**条目**上 ⇒ **同一个技能的目标/珠类型是互相竞争的独立选项**，

| 技能 | 目标类 | 候选条数（实测：中期每决策均值） |
|---|---|---|
| 狙击枪 / 枪 / 剑 / 坦克 | enemy | **3.36**（每个存活对手 1 条） |
| **全息屏障** | other | **3.36** |
| 双枪/镜面 | enemy×2 | **8.29** |
| **蓄能** | self（两种珠） | **2.00** |
| **原型制御 / 聚能环 / 防御 / 八卦阵 / 金刚盾** | self | **1.00** |

实测（`tools/probe-candslot.mjs`，14 个中期决策，线上包）：`snipe` 槽位 3.36 → 聚合概率 **63.9%**；
`sword` 3.36 → 14.2%；**`holo` 3.36 → 7.1%（单条最高 46.3%）**；`proto` **1.00 → ≈0%（单条≈0）**。
⇒ **两件事同时成立**：(i) 槽位 3.36:1 的结构性放大器（**我的枚举设计**）；
(ii) 网络确实给 `holo` 的单条打分远高于 `proto` ✗ —— 而 `holo`（同费用 1 ジ、同效果"原型制御架势"，
但施加给**别人**）在 5 人混战里被 `proto` **严格支配** ⇒ 这是一个**价值估计错误**，
技能报告独立印证：全息屏障 `Δ_lost = −3.3pt`（**拿掉它反而更强**）。

### 2. 病根 B：候选表**不按 `affordable` 过滤** + 引擎**静默降级**
- `candidatesFor`（policy.js:562）遍历 `legal` 时只判 `l.key`，**不判 `l.affordable`** ✗ ——
  而 `legal` 里每项都带 `affordable: loan === 0`（play.js:17/21）。
  实测：**`ep = 0` 时蓄能仍在候选表里**（探针：蓄能出现的 ep 分布 `{0:12, 1:14, 2:2}`）。
- 引擎侧（play.js:60/93）：`(l && l.affordable) ? raw.key : R.SK.JI` ⇒ **买不起的选择被静默降级成「ジ」**，
  而 UI/日志仍显示那个技能名 ⇒ **幻影动作**（网络以为放了招、实际只是攒 1 ジ）。
- 与"蓄能 100% 浪费"合起来才是用户看到的现象：`chargeProfile`（40 局）线上包 **蓄能 0.30 次/局 ·
  得珠 0.30 · 过期 0.30 · 花掉 0.00 ⇒ 浪费率 100%**；珠子消费卡（电磁炮/天火）都要 **2 ジ**，
  而蓄能常发生在 `ep = 1`（花掉唯一 1 ジ）⇒ **下一回合必然凑不出** ⇒ 珠子必定过期。
  日志实证（21 回合局）：`第10回合 玩家4=【蓄能】` → `✨ 完成蓄能` → `⏳ 蓄能珠过期`（全程未使用）。
- 根因之二：**没有任何奖励为"把珠子花掉"记分**（窄奖励不能 bootstrap 闭环的老问题）。

### 3. 建议（都动 `js/train/policy.js` ⇒ 需重记两个包指纹 + 重训才生效，故未擅自改）
1. **F1（低风险）**：`candidatesFor` 里跳过 `!l.affordable` ⇒ 概率质量不再分给幻影动作。
2. **F2（结构性）**：选择改成**按技能两级**（先按技能聚合/归一，再在该技能的条目里选），
   抹平 3.36:1 的槽位放大器；不升 `PACK_VERSION`（不改参数形状，旧包仍可加载）。
3. **F3（规则问题，用户裁定）**：5 人混战里 `全息屏障`（把保护送给对手）被 `原型制御` 严格支配 ——
   与 v1.5.7"2P 里去掉全息屏障"同源。要么禁止它对自己以外的人用，要么在多人局移除该卡。
4. 若采纳 F1/F2，**必须重训再上线**（否则线上包行为会变而成绩未复测）。

## v1.5.72 — 线上冠军换成 `v7tgt2-52`：**三条臂里唯一全过门的候选**（且首次未用 `--force`）

### 1. 来源与可复现配方
- 臂 `v7tgt2`（`RING2_POOL=E RING2_SEEDS=51,52,53,54 RING2_TAG=v7tgt2`）—— 池 E = 池 C +
  **1 席"会瞄人的对手"**（`pickTargeter`，见 v1.5.71 §5/§8）。产物 `docs/artifacts/v7tgt2-52.bak`。
- 三条臂共 13 个 seed：`v7tgt`（1 产物）、`v7tgt2`（2 产物）、`v7tgt3`（**0 产物**，4/4 被健康门槛拦下：
  G 1.39/1.55/2.07 与"自对局回合 43.1 > 40"）⇒ 全过门者仅 `v7tgt2-52`。

### 2. 逐项读数（60 局考卷 / n=150 座位 / 20 局反弹墙）
| 指标 | **v7tgt2-52（新）** | 线上包（旧） |
|---|---|---|
| A 考卷 1st | 39.9% | 39.5% |
| 反弹墙伤害/局 · 穿透命中 | **14.35 · 287** | 12.25 · 242 |
| F 被集火还手率 | **31%** | 25% |
| E 无威胁摆架势 | **11%** | 16% |
| 场B 清场/局 | **0.57** | 0.00 |
| 狙击场（1 狙击+3 被动） | 12.88 伤害/局 · 打过 3.92 人 · 存活/胜率 **50%** | 0.47 · 1.3 人 · 0% |
| 狙击场（4 席狙击） | 存活/胜率 **60%/60%** | 8%/23% |
| 靶向场（4 席会瞄人） | **16.7%** | 1.7% |
| G 有效技能数 | 4.12 | 4.14 |
| 座位极差（n=150） | 19pt（现行判据 ok） | **9pt** |

⇒ **用户最初抱怨的三件事全部改善**：一直出防御（E 16%→11%）、被集火不还手（F 25%→31%）、
清不掉人（场B 0.00→0.57）；墙与两个压力场也一并变好；A/G 持平。

### 3. 两条**已知代价**（如实记录，不掩饰）
1. **座位极差 19pt vs 9pt** —— 现行判据（占比≥70% 或 n≥50 且 ≥30pt）判过，但按 n=150 标定
   （已知好 8.3~8.8pt / 已知坏 28.6~35.7pt，15~20pt 有宽空档）它落在空档上沿 ⇒ **偏弱但真实**。
2. **全息屏障 4.9 次/局、100% 套给别人**（硬门槛 >6 故放行）—— 与 v7wall-33 那次（18.7 次/局）
   同族，量级小得多，但方向一致；是否把硬门槛收到 >3 留作后续决定。

### 4. 出厂门槛状态（包内 meta 可自查）
`auditFails = []` · `auditForced = false`（**本会话首次不用 `--force` 的换包**）·
`examScoreAtBuild = 39.9`（`examGames = 60`，与上表同口径）· `feasibility.ok = true`（五道全过）·
`rulesFingerprint` 随包写入（规则未改 ⇒ 与当前指纹一致）。

## v1.5.71 — 第五轮复核的**三条更正全部接受**：门槛收进单一真源 + 座位判据的样本量守卫 + 场B 判据钉死为清场数

复核（`docs/AUDIT-RESPONSE-v1.5.70.md`）给了三条更正，逐条落成代码。以下按"病 → 判据 → 证据"记。

### 1. 场B 的"100% 胜"是**规则红利**，不是能力（复核 §2-1）⇒ 判据只认清场数
- 病：v1.5.65 给 multi 加了终局收缩 + 全灭按累计伤害判胜 ⇒ **打 1 点就赢**。线上包场B
  **严格胜率 100%**，而清场 **0.00/局**、伤害 1.0/局 ⇒ 任何以胜率为准的场B 门槛都是**白送**。
- 落法：五道判据里场B 那一行**只读 `fieldB.clearedPerGame`**（< 0.3/局 ⇒ 拒），胜率只作为
  `fieldBWinRate` 记录在案、**永不参与判定**；并加守门 **D60**（构造"胜率 100% + 清场 0"的探针
  ⇒ 必须 FAIL 且失败文案是"清场"，不得出现"胜率"）。

### 2. v1.5.66 标题降级：目标枚举顺序是**通道之一**，不是"成因"（复核 §2-2）
- 证据：v1.5.66 修完枚举顺序后，v17 臂**仍有 3~4/6 个 seed 偏 30~100pt** ⇒ "这才是成因"是
  over-claim；更站得住的是 v1.5.68 的解释（**训练从不评估"5 席同策略"**，固定目标偏好在池上能赢、
  在 5 席测量里表现为某座通吃，演化惩罚不到）。标题已就地降级并标注。

### 3. 座位判据的**样本量守卫**（复核 §2-3）—— 我自己上一轮的"决赛圈 n=20~30 + 极差<30pt"是错的
- 复核给的蒙特卡洛（5 席对称假设下胜率极差，均值/p90/p99）：**n=6 → 41/50/67pt** ·
  n=20 → 23/35/45pt · n=30 → 19/27/37pt · n=50 → 15/22/28pt · n=100 → 10/16/20pt。
  ⇒ n=30 配 30pt 阈值仍 **5~8% 假红**、n=6 配 30pt **约一半时间假红** ⇒ 我拟的"20~30 局+极差<30pt"
  就是用噪声当判据（与 v1.5.69 已认的错同型）。
- 落法（`tools/audit-lib.mjs` 的 `seatSymmetry`）：判偏 = **占比判据（某座 ≥70%，~0.2% 假红，任意 n 稳健）**
  **或** 大样本极差（**n ≥ 50** 且 ≥30pt）；`n<50` 且未通吃时返回第三态 **`'underpowered'`（不知道）**，
  并回报 `basis` 说明是哪条判据触发 —— **`underpowered` 绝不能被读成"均衡"**。

### 4. 判据收进**单一真源** + 出厂换包也写可行性记录（复核 §4-6）
- 病（一个）：训练落盘（`server/train-server.mjs`）与出厂换包（`tools/promote-champion.mjs`）各写一份
  阈值 ⇒ 两头会漂；实测**线上包 meta 里根本没有 `feasibility` 字段** —— 因为它是经
  `tools/upgrade-pack.mjs` 换回来的、**绕过了训练落盘那一步**。
- 落法：新增纯函数 **`feasibilityOf({seat, G, wall, aggr})`**（`tools/audit-lib.mjs`）作为五道门槛的
  唯一真源，落盘处与换包处**都调它**；`promote-champion` 把结果写进 `meta.feasibility` 并打印一行
  （含 `fieldBWinRate` 与"规则红利、不作判据"的标注）。守门 **D61**（源头断言：两处都必须走
  `feasibilityOf`，阈值不得再各写一份）、**D62**（`underpowered` 不得判 fail，但必须留 note）。
- **明确不做的事**：没有把 `feasibility` 升级为**阻断**。原因是它会连带否决"换回历史冠军"这条路径
  （eco-34 场B 清场 = 0.00 会被它挡）。"五道门是否升为硬门槛"是独立决策，等用户裁定。

### 5. ① **"会瞄人"的对手**进池：给三条断掉的线补梯度（复核 §4-1）
- 病（复核 §3-2 的合成结论）：池里**没有任何对手会瞄人** —— 脚本走 `pickTargetN`（击杀优先 → 血最多），
  而"满血、不出头"的狙击手永远进不了任何人的候选集。三件事因此同时断梯度：反狙击、反聚能环、
  场B 里主动清场。复核的反向验证把这条钉死了：给 4 席对手加一条"谁用狙击就瞄谁"的规则 ⇒
  候选 v17-146 的夺冠率立刻 **30% → 0%**。
- 落法：`js/train/bots.js` 新增 **`pickTargeter`**（威胁 = `ringStreak > 0`（在滚环）→
  `lastSkill === snipe`（刚放冷枪）→ `ep >= 5`（攒满大雷）；都没有就**攒钱**——
  它是"惩罚者"，不是又一个乱打的激进派；濒死先保命，能开枪就用最便宜的手指他）。
  登记进 `server/opp-pool.mjs` 的 `OPP_SPECS`（单一来源，`OPP_DEFAULT` 不受影响）；
  实验臂用 `RING2_POOL=E`（= C + 1 席 targeter）。
- **关键前提已验证**：`wrapBotN` 自 v1.3.55 起保留脚本自选目标 ⇒ **不需要动引擎**（也就没有指纹churn）。
  守门 **D63** 盯住这条前提 + 判别力对照（默认口径 `pickTargetN` 必须选领先者、targeter 必须选滚环者）。
- **判决实验（复核那条"现成期望值"被精确复现，用真实候选跑，不靠训练）**——
  `tools/eval-5p.mjs` 新增 `--field=targeter`（4 席会瞄人）：

| 包 | 默认 A 考卷 1st | `--field=targeter` | 结论 |
|---|---|---|---|
| **`v17-146`**（复核点名的狙击专精候选） | **51.0%** | **0.0%** | 4th 51.7% / 5th 41.7%、场均承伤 **3.00** = 恰好 3 血 ⇒ **每局都被打死** |
| 线上包（对照组） | 39.5% | **63.3%** | 该场对稳健包是**加分**的 ⇒ 双向判别力，不是"把所有人打死"的坏场 |

  ⇒ 复核对"狙击优势是假的"的判断**完全成立**（他们的 30%→0%，我们 51%→0%）；
  同时说明**靶向压力场值得进训练池**（这正是本节 ① 的目的）。
  附带发现（待更大样本复核）：线上包在该场里各座 1st 相差 41.7~91.7pt（60 局 ⇒ 每座 12 局，噪声 ±14pt）
  ⇒ 疑似座位依赖行为被这个场**照出来**了，值得后续用 `EPIRUS_SEAT_GAMES` 拉大样本看。

### 6. ② **狙击场探针**：复核建议的判据被数据否掉，换成真正分得开的口径（复核 §4-2）
- 先做**独立探针** `tools/probe-sniper.mjs` + `audit-lib.sniperField`：
  - `mixed`（默认）= **1 席狙击 + 3 席只攒不还手** vs 冠军（双方座位逐局轮换）；
  - `wall` = 4 席狙击（只量生存/胜率）。⚠️ **wall 没有靶向判别力**（人人都是狙击手 ⇒ 比例恒 ~1）——
    这正是我上一轮 `ringWallProbe` 犯过的错，已写进注释与工具输出，防止再把恒 1 当成果。
- **口径限制（写在代码里）**：`action` 事件**不带目标**（`state.js:156` 只有 pid/key/outcome），
  所以"指向谁"只能按**伤害事件**（`damage.to` + `damage.source`，字段是 `source` **不是** `from`）
  归因 ⇒ 只统计命中，被挡/被无效的尝试不计 ⇒ 是**下界**。归因抽成纯函数 `attackAttribution`（守门 D64）。
- **标定（40~60 局/包，长程）**：

| 包 | 靶向率（均匀 25%） | 自身伤害/局 | 平均打过几人 | 打过 1 人的局 | 存活/胜率 | 4 狙击场出手 |
|---|---|---|---|---|---|---|
| 种子 `champion-5p-v1.3.58` | 45.3% | **2.73** | 3.2 | 9/40 | 5% / **13%** | 244 |
| `eco-34`（原线上） | 47.7% | 0.53 | 1.4 | **27/40** | 0% / 0% | 245 |
| `eco-34-v7c`（v7 嵌入） | 47.7% | 0.53 | 1.4 | 27/40 | 0% / 0% | 245 |
| 线上包（60 局） | 43.8% | 0.47 | 1.3 | **37/60** | 0% / 0% | 408（6.8/局）|

- **结论（两处修正复核、一处证实）**：
  1. **复核 §4-2 建议的判据（靶向率 ≥20%）没有判别力** —— 所有包都在 43.8~47.7%（远高于均匀 25%），
     线上包并不是"接近 0"（复核自己也标了这条是待验证的假设）⇒ **不能用**。
  2. 真正分得开的是**低压力场里的出手量与目标多样性**：种子 2.73 伤害/局、平均打过 3.2 个人；
     eco-34/线上 0.47~0.53/局、**27~37 局里只打过 1 个人**（"只盯一个靶子"= 身份映射家族的另一面）。
     ⇒ 判据候选改为 `dmgPerGame` / `spreadAvg`（**先记录、暂不阻断**，等更多包标定）。
  3. 顺带**再次独立证实** v1.5.64 的嵌入等价性：`eco-34.bak`（原生 v6）与 `eco-34-v7c.bak`（v7 嵌入）
     在这次探针上**逐场读数完全相同**（含 `targetSpreadHist` 的分布）✓。
  4. 反证了"狙击手不进入候选集 ⇒ 冠军从不打它"这个推论：线上包对狙击席的命中占其攻击的 **43.8%**，
     **不是 0** ⇒ 病根不是"从不瞄"，而是"**低压力场里整体不出手**"（62 回合只造成 0.83 点伤害）。

### 7. 顺带修掉两条**门禁自身**的毛病（不是功能改动）- **D13 偶发红（实测 5 次里 1 次）**：它断言 `fit - fitNoDiv == divBonus + w*styleRate`，但 `fit` 里
  还有 v1.5.68 加的 **`- seatPen`** 项（`js/train/evo.js:996`）⇒ 惩罚一触发就红。
  修法不是放宽阈值，而是把 **fit 的全部加项**纳入恒等式（并要求 `seatPen` 照实报告）⇒ 连跑 6 次全绿。
  教训：门禁里的恒等式必须覆盖**全部**加项，否则它自己就是一条偶发噪声源。
- **`promote-champion.mjs` 加 `--dry`**：只做体检与自检、**一个文件都不写**。起因是这次要验证新增的
  `meta.feasibility` 块需要**真实运行证据**，但又不能顺手改写线上包与缓存戳。
  实测输出（线上包）：`✗ 场B 清场 0.00 < 0.3/局（… 胜率 100% —— 规则红利，不作判据）` ⇒
  **在真产物上复现了复核 §2-1**，且 `--dry` 下 `js/bundled-champion-3p.js` 与 `index.html` 未被改动 ✓。
- **另一个我自己踩的坑（记下来）**：门禁用 `node … | tail` 取尾部时，`$?` 是 **tail** 的退出码 ⇒
  会把红灯读成 0。改用 `> file 2>&1; rc=$?` 取真实退出码（铁律第 2 条再次生效）。
- **D16 的"训练进行中"分支是假红（v1.5.48 隔离之前的旧假设）**：它要求训练期间线上包**逐字节等于
  `champion-5p-v1.3.58.bak`**（自跑器"`copyFileSync(BASE, BUNDLE)`"那个年代的行为）。但隔离之后
  自跑器只把基线拷到 staging（`docs/artifacts/.training-in-3p.js`）、产物从 `.training-out-3p.js` 取，
  线上包**只在臂首备份、臂尾还原，训练期间不写** —— 实测铁证：本臂运行中
  `js/bundled-champion-3p.js` 的 mtime 仍是**前一天**。于是这条把**每一条臂**在训练期间都打红
  （我用它提交时被挡了一次），属"门禁在正常运行时乱响"（与 D13 同类）。
  改成**真断言**：臂首写 `docs/artifacts/.training-live.sha1`，D16 在训练期间逐字节比对
  （不等 ⇒ 真有人在写线上包 —— 用户被这个坑过两次）；臂尾自跑器也自查一遍并大声报错。
- **`tools/eval-5p.mjs` 的第四份名字表被合并掉**：`--field=targeter` 第一次跑直接崩栈 ——
  该文件自己持有一份"名字→函数"表，我加对手时漏了它（`opp-pool.mjs` 文件头记录过三次同类事故：
  v1.3.59 漏 worker、v1.4.8 漏 server、v1.4.14 漏 eval-5p）。现在 `ALL` 从 `OPP_SPECS` **派生**
  （再加只属于评测的 focusfire/minespam/cursestorm），并加两条硬守卫：
  场里出现未登记名字 ⇒ **点名报错**（不许崩栈）；登记了名字却取不到函数 ⇒ 报错退出（不许静默剔除）。

### 8. 靶向对手的**反噬**与修正（`v7tgt` 臂实测发现的，我自己的设计缺陷）
- 第一版写成"没有威胁就攒钱" ⇒ 两个后果：① `--field=targeter` 变成**弱场**（随机基线 **78.3%**、
  新候选 100% ⇒ 场在"白送"）；② 更糟的是它给训练送了一条**反向梯度** ——
  "别开环 / 别用狙击 / 别攒到 5 ジ 就不会挨打" = 正好喂大我一直在打的**低压力场瘫**（E/场A/场B 同族）。
- 修正：有威胁 ⇒ 掐威胁（原设计）；**没有威胁 ⇒ 压领先者**（与其他进攻脚本一致，不再躺平）。
  修正后随机基线回到 **25.0%**（5 人局期望 ≈20%）✓ 场不再白送。
- 修正后的判别力（60 局）：v17-146 **1.7%** · 线上包 **1.7%** · `v7tgt-44`（在含 targeter 的池里训出）
  **20.0%** ⇒ 场能分开"抗压制"与"被压制"。守门 **D63** 的 ③ 相应改成"无威胁也必须出手压领先者"。

### 9. G（有效技能数）阈值标定：判据成立，但阈值**贴着已知好**
新增 `tools/g-calib.mjs`（与落盘处**同一调用** `mirrorHealth`，并同时报 n=20/n=40）：

| 包 | G(n=20) | G(n=40) | 种数 |
|---|---|---|---|
| 已知好·热启动种子 | **2.97** | **3.03** | 5/6 |
| 已知好·线上包 / eco-34（原生与 v7 嵌入逐位相同） | 4.14 | 4.04 | 9/11 |
| 已知坏·v17-146 | 1.00 | 1.00 | 1/1 |
| 已知坏·v17-141 | 2.74 | 2.63 | 3/3 |
| 新候选·v7tgt-44 | **5.40** | **5.10** | 11/11 |

- ⇒ **阈值能分开**（好 3.03~4.04 / 坏 1.00~2.63）⇒ G **不是**假红源，最近几批"无产物"是候选真的坍缩。
- ⚠️ 但**阈值贴边**：种子在 n=20 是 **2.97 < 3**、n=40 才 3.03 ⇒ 同一包换个样本量结论就翻
  ⇒ 记入"阈值不许定在噪声带里"的同类清单（E/F/holo/座位之后的第五例）；后续考虑给 G 加余量或样本量说明。

### 10. ① 的训练侧结果（臂 `v7tgt`：池 E = 池 C + 1 席 targeter，seeds 41–44）
- 健康门槛拦下 3/4（G 1.36~2.74），**seed 44 逃逸并产出 `docs/artifacts/v7tgt-44.bak`** ✓
  （评测日志确认 `opps=16` ⇒ 池 E 里真的含 targeter）。
- `v7tgt-44` 三项优于线上包：**A 考卷 44.9% vs 39.5%** ✓ · **G 5.10 vs 4.04** ✓ ·
  **靶向场 20.0% vs 1.7%** ✓ ⇒ 复核 §4-1"往池里加会瞄人的对手"**有效**（不只体现在评测场里）。
- 训练隔离再次实证：臂运行中线上包 mtime 仍是**前一天**、sha1 臂首臂尾**完全一致**（`c6cc0c9f`）✓。

### 11. ① 的第二臂（`v7tgt2`，用**修好后**的 targeter）+ 座位判据标定 + 首个"五道全过"候选

**臂 `v7tgt2`**（池 E、seeds 31/51–54）：有提升 1 / 缺产物 3；产物 `v7tgt2-31.bak`、`v7tgt2-52.bak`。

| 指标 | 线上包 | v7tgt-44（旧 targeter） | v7tgt2-31 | **v7tgt2-52** |
|---|---|---|---|---|
| A 考卷 1st | 39.5% | 44.9% | — | 39.9% |
| 反弹墙伤害/局 | 12.25 | **1.75** ✗ | 13.80 | **14.35** |
| 穿透命中 | 242 | 35 ✗ | 276 | **287** |
| 被集火还手 F | 25% | 31% | 21% | **31%** |
| G | 4.14 | 5.40 | 3.50 | 4.12 |
| E 无威胁摆架势 | 16% | 5% | — | **11%** |
| 场B 清场/局 | 0.00 | 2.1 | 0.00 | **0.57** |
| 靶向场 1st | 1.7% | 20.0% | 41.7% | 16.7% |
| 狙击场（mixed） | 0.47 伤害/局·1.3 人·0% 存活 | 12.55·3.85·65% | 9.07·3.67·48% | 12.88·3.92·**50%** |
| 狙击场（wall 4 席） | 8% 存活 / 23% 胜 | — | 18%/35% | **60%/60%** |
| 座位极差（n=150） | **8.8pt** ✓ | 28.6pt ✗ | **35.7pt** ✗ | **18.9pt**（现行判据 ok，但高于已知好带 ✗） |
| 出厂体检 | 五道：场B ✗ | 座位 ✗ | 座位 ✗ | **✅ 五道全过（首次，且未用 --force）** |

- **`v7tgt2-52` 是本条线上第一个不是"取舍"的候选**：六项（墙/F/E/场B/狙击场/靶向场）优于线上包、
  A 与 G 持平，**只退化座位（19pt vs 9pt）** ⇒ 首次通过五道门槛（含场B 清场 0.57/局）。
- ⚠️ 但它有一条**行为缺陷**（未达硬门槛、但同类）：**全息屏障 4.9 次/局、100% 套给别人** ——
  与 v7wall-33 那次（18.7 次/局、100% 送人）同族，只是量级小得多。硬门槛是 >6，故放行。
- **座位判据标定（n=150，5 席同策略）**：已知好 **8.3/8.8/8.8pt**（种子/eco-34/线上包）vs
  候选 **28.6/35.7pt**（两者 P0 恒为 0% = 老的"0 号座诅咒"）⇒ **中间有 15~20pt 的宽空档**，
  而现行阈值 30pt **放过了 28.6pt** ✓✗。⇒ 建议把座位做成**决赛圈的硬过滤**（n≈150、阈值落在空档里，
  例如 15~20pt），而不是"n≥50 且 ≥30pt"—— 后者已在标定里被证明分不开中间那档。
- 训练隔离第三次实证：臂运行中 `champ-audit` 自己打出"⚠️ 训练进行中：bundle 行不可信" ✓，
  臂尾 sha1 与臂首快照一致 ✓。

## v1.5.70 — v17 裁定入档：座位惩罚生效后的真实结果 + 设计结论（**未再盲改**）

### 1. v17（惩罚真正生效）逐 seed 裁定
| seed | 座位极差 | G | 反弹墙 | 场A | 场B 清场 | 失败项 |
|---|---|---|---|---|---|---|
| v17-141 | **30.5pt**（差 0.5）| 2.63 | 16.8 | 42% | **4.0** | 座位+G |
| v17-142 | 100pt | 1.00 | 20 | 30% | 3.75 | 座位+G |
| v17-143 | 100pt | 1.37 | 18 | 26% | **4.0** | 座位+G |
| v17-144 | 76.6pt | 1.64 | 18.7 | 29% | 3.4 | 座位+G |
| v17-145 | 61.4pt | 2.32 | 7.6 | 22% | 3.15 | 座位+G |
| **v17-146** | **8.9pt** ✓ | **1.00** | 15.9 | 29% | 3.05 | **只差 G** |

### 2. 三条可判读的结论
1. **场B 能力已成常态**：6/6 都清场 3.0~4.0 个/局（线上包 0.00）⇒ `EPIRUS_PASSIVE_FIELD` 暴露度 + 规则修（v1.5.65）是**持久收益**。
2. **G 覆盖坍缩成为普遍项**（6/6 <3，1.00~2.63；比 v15 的 1.96~3.38、v16 的 1.21~3.05 更差）⇒
   座位与 G **互相顶**：座位达标的（146 = 8.9pt）G 只有 1.00；G 较好的（141 = 2.63）座位差 0.5pt。
3. **6 局座位探针本身是噪声源**：极差在 6 局下的噪声地板 40~60pt（`multinomial(6,1/5)` 期望极差≈50pt）。
   即使触发条件已改成"某座 ≥70% 通吃"（对称下 ~0.2%），把它作为**每成员每代**的评分项仍会把选择**部分推向噪声**
   —— G 的全面恶化很可能与此有关（"用噪声当梯度"）。

### 3. 设计结论（下一步的方向，不再盲改权重）
两条剩余约束都**不适合**做成"每成员每代的小样本软惩罚"：
- **座位**：应做成**决赛圈的硬过滤 + 大样本探针**（例如每位决赛者 20~30 局，只对少数决赛者跑 ⇒ 成本可控）；
- **G 覆盖**：同族（用"自对局 distinct keys"当**决赛圈硬约束**，而不是奖励项；`DIV_W` ×10 已证压不动互斥）。
⇒ 即：**把约束放回"决定产物的那一行"，但用足够大的样本 + 硬判定**；软惩罚 + 小样本只会引入噪声。

### 4. 门禁
`np-test 107/107`。

---

## v1.5.69 — 修 v1.5.68 的**静默空操作**：座位惩罚从未触发（我自己的错）+ 噪声地板标定

### 1. 我自己的静默空操作（v1.5.68 的惩罚等于没加）
`v1.5.68` 想"蹭 `MIRROR_GAMES`（=**2**）的局零成本统计座位分布，但守卫写的是 `mirDec >= 3`
⇒ **永不满足** ⇒ `seatSpreadMirror = null`、`seatPen ≡ 0`。实测复核：
```
mirrorDecisive=2  seatSpreadMirror=null  seatPen=0      ← 惩罚从未生效
```
⇒ **v16 那 6 个 seed 是在"惩罚关闭"下跑的**，其读数对惩罚效果没有信息量（0/6 五道全过 ≠ 惩罚无效，而是惩罚根本没跑）。

### 2. 修
- 新增**独立座位探针**：`SEAT_GAMES`（`EPIRUS_SEAT_GAMES`，默认 **6** 局，5 席同一策略）⇒ 各座胜场；
  守卫放宽到 `>= 4`（6 局样本下正常满足）。座位统计不再蹭 mirror 局（也就不会重复计数）。
- 导出 `seatGames()/setSeatGames()` 供守门与审计读取。

### 3. 噪声地板标定（这一步同样必要）
6 局样本下"极差"的**噪声地板就有 40~60pt**（对称假设下 `multinomial(6, 1/5)` 的期望极差≈50pt）——
实测一个**未训练**策略就报"极差 50pt"。⇒ 若以"极差 > 35pt"触发，等于**按噪声随机扣分**。
故触发条件改为**明显通吃**：某座占 **≥70%** 的分胜负局（对称假设下概率约 0.2%，不是噪声），
权重 `1.0 × (maxPct−70)/100`（100% 通吃 ⇒ 扣 0.30，相对成员 fit 0.2~0.5 是重罚；70% 以下一分不扣）。
实测：未训练（对称）策略 ⇒ `极差 50 / 最高座 50% / seatPen = 0` ✓

### 4. 守门 **D59 加硬**（本版的关键）
新增两条**能挡住空操作**的断言：必须有独立探针 `SEAT_GAMES`、且 `T.seatGames() >= 4`；
触发条件必须按 `maxPct`（不是极差）。⇒ v1.5.68 那种"惩罚恒 0"的版本在这里会直接红。

### 5. 门禁
`np-test 107/107`（`evo.js` 不在指纹集 ⇒ 无需重记）。
**待验证（v17 臂）**：惩罚真正生效后，是否首次出现 `meta.feasibility.ok === true`。

---

## v1.5.68 — 让演化**看得见偏置**：阈值式座位惩罚（用已经在打的 mirror 局，零额外成本）

### 病（v15 臂逐 seed 裁定给出的精确定位）
`v15` 6 个 seed **0/6 五道全过**，但两个 seed **各只差一项**：
`v15-123` 只差座位（33.9pt vs <30，其余全过）· `v15-124` 只差 G（2.29 vs ≥3，其余全过）
⇒ 瓶颈不是"某项能力不会"，而是**演化看不到自己偏座**：训练的对局永远是"受训者 vs 脚本机器人"，
**从不评估"5 席同策略"这一配置** ⇒ 固定目标偏好的策略在机器人池上能赢、在 5 席测量里却某座通吃。

### 修（只改训练侧；引擎与 `PACK_VERSION` 不动）
1. `mirrorHealth`（**已经在跑 5 席同策略**）顺便统计各座胜场 ⇒ 回报 `seatWins / seatDecisive / seatSpread`（**零额外成本**）。
2. `scoreMemberN` 里**已经在打的 mirror 局**顺便统计各座胜场 ⇒ 加一项**阈值式座位惩罚**进 `fit`：
   `极差 > 35pt 才扣分`（`seatPen = 0.5 × (极差−35)/100`），低于阈值**一分不扣**。
   ⇒ 不改变"好个体之间的相对次序"，只在偏置真的大时把它压下去。
   ⇒ 这是**约束处理**（只罚违规、不奖励任何东西），不是又一项目标权重 ——
   加权奖励已被两次实验证伪（先手激励 −14pt、破墙奖励让混合场 40%→17%）。
3. 成员评分回报 `seatPen / seatSpreadMirror / mirrorDecisive`（供审计追溯）。

### 守门 **D59**
源码契约（常量存在、`- seatPen` 真进 fit、回报字段存在、`mirrorHealth` 回报各座胜场）+ 行为断言
（线上包已知均衡：`mirrorHealth(20)` 的 5 席极差必须 <40pt；包不可读时跳过并打印，与 D16/D58 同一守卫）。

### 门禁
`np-test 107/107`（`evo.js`/`ui.js` 不在指纹集 ⇒ **无需重记指纹**）。

### 待验证（v16 臂）
判据：是否首次出现 `meta.feasibility.ok === true` 的 seed，且 **A严胜 / H混合场 / 反弹墙伤害不回退**。

---

## v1.5.67 — 五道上线门槛做成"**决定产物的那一行**"的可行性判定（复核 §4 的 ④）

### 病（本轮实验证）
训练的选择目标是"池上胜率 + 奖励"，而上线按**另外五道门槛**筛 ⇒ 五批 30+ 候选（v9/v11/v12/v13/v14）
全是"某一项精彩、其余崩掉"；其中最致命的是训练**从不评估"5 席同策略"这一配置**（它的对局永远是
受训者 vs 脚本机器人）⇒ 任何"固定目标偏好"的策略在机器人池上能赢、在 5 席测量里却表现为某座通吃，
而训练**永远惩罚不到它**。

### 修（只改训练侧；引擎与 `PACK_VERSION` 不动）
在 `server/train-server.mjs` 的**唯一产物决定点**（落盘前）跑一次五道检查，结论写进 meta：
`feasibility = { ok, fails[], seatSpread, seatDecisive, G, Gkeys, wallDmg, fieldA, fieldBClears }`，
并打印 `[feasible]` 行 + SSE 事件。探针复用 `tools/audit-lib.mjs`（**单一真源**：座位 `seatSymmetry` /
G `mirrorHealth(40)` / 反弹墙 `reflectWall(20, long)` / 场A 与场B `aggressionProfile(20)`）。
- 阈值：座位极差 <30pt（前置判胜≥30%）· G ≥3 · 反弹墙伤害 >0.5/局 · 场A 还手 ≥20% · 场B 清场 ≥0.3/局。
- **默认不阻断落盘**（`EPIRUS_FEASIBILITY=0` 可关）：避免重演 v1.5.56 的"整臂拦下 ⇒ 实验没有样本"；
  **出厂门槛仍由 `tools/promote-champion.mjs` 强制** —— 那里才是"能不能上线"的判定。

### 实测样例（feastest，30 代）
```
[feasible] ✗ 座位极差 37pt ≥30；G 1.99 < 3（座位 36.8pt · G 1.99（3种）· 墙 17.15/局 · 场A 33% · 场B 4/局）
[feasible] ✗ 座位极差 40pt ≥30；G 2.38 < 3；场B 清场 0.05 < 0.3/局（… 墙 18.1/局 · 场A 28% · 场B 0.05/局）
```

### 门禁
`np-test 106/106`。

---

## v1.5.66 — L7 第七处：候选**目标枚举顺序**也是身份通道 —— ⚠️ **v1.5.71 降级**：它是"通道之一"，不是成因

> **标题更正（v1.5.71，第五轮复核 §2-2）**：本条原写作"……**这才是**'引擎无偏、冠军仍偏'的成因"。
> 修完之后 v17 臂**仍有 3~4/6 个 seed 偏 30~100pt** ⇒ 该断言是 over-claim。更站得住的原因是训练
> **从不评估"5 席同策略"**这个配置（见 v1.5.68 的说明）：固定目标偏好的策略在对手池上能赢，
> 在 5 席测量里就表现为某座通吃，而演化惩罚不到它。

### 1. 为什么"环境和特征都对称了，新训的冠军还是偏 30~100pt"
- **引擎确实无偏**（镜像实验，60 局）：脚本"每回合打**索引最小**的存活对手" ⇒ **P4 通吃 60/60**；
  镜像规则"打**索引最大**" ⇒ **P0 通吃 60/60**；全员只ジ ⇒ 全平。⇒ **镜像规则给出镜像结果**，引擎/相位/盐都没问题。
- **但"无偏的环境"不等于"无偏的策略"**：任何"按固定 pid 顺序选目标"的策略**本身就是偏置规则**（见上）。
- **v7 候选口径恰好会系统性产生这种规则**：`js/train/policy.js` 的 `candidatesFor` 按 `S.opponentsOf`（**pid 升序**，
  `state.js:73`）枚举目标，而 `forwardCands` 的 argmax **并列取第一个下标** ⇒ 网络对目标"无所谓"时，
  实际行为退化成"**打最小 pid 的那个**" ⇒ 网络再围着这个事实学习 ⇒ 偏置自我强化、
  且**对特征完全不可见**（它在动作选择里，不在状态编码里）。
- **证据吻合**：偏置候选的胜局是「0 号座全程满血、其余四席全灭」（`v13-106`/`v13-103` = P0 58~60/60）——
  正是"打最大索引"那条规则的镜像形态。
- **为什么老种子 v1.3.58 均衡**：它是 **v6 包**，走**旧口径**（键 + `pickTargetN` 启发式选目标），
  **不经过候选枚举** ⇒ 没有这条通道 ✓ ⇒ 这也解释了"新训的都偏、老种子不偏"。
  ⇒ 结论：该通道是 **v1.5.19 引入 v7 候选口径时新增的**，与特征/引擎无关。

### 2. 修（只动目标枚举顺序，不碰引擎规则、不升 `PACK_VERSION`）
- 新增 `poolOrder(state, pid, pool)`：按**每局盐**哈希排序目标池（纯函数、决策内一致、**不消耗任何随机流**；
  **盐 0 保持旧序** ⇒ 历史基线不变）。三处枚举（单目标 / 双目标有序对 / `lockTarget` 备选）全部改走它。
  实测目标首项顺序：`salt=0 → 1,2,3,4`；`12345 → 4,1,2,3`；`98765 → 2,4,1,3` ✓
- **页面也每局带盐**（`js/ui/ui.js` 建局处）：此前只有页面缺省 0 ⇒ 产品仍走确定性顺序（训练/评测早就带盐）。

### 3. 守门 **D58**（L7 第七处的可反证形式）
- 源码契约：`poolOrder` 存在且三处枚举走它；页面每局带盐；
- **镜像对称**：脚本"打最小索引"必须让最高 pid 通吃、镜像规则必须让最低 pid 通吃（10 局，最强形式）；
- **5 席不得通吃**：用**仓库内线上包**跑 60 局 5 席同一策略，最高座不得超过 55% 的分胜负局
  （修前形态 `v13-106` = 60/60 会直接红；现线上包 13/9/13/9/8 通过）。
  （训练中包被临时改写时跳过并打印，避免误报——与 D16 同一守卫。）

### 4. 门禁
`np-test 106/106`（唯一需重记的是指纹）；指纹 `57bac9a5 → d42ec70a`。

---

## v1.5.65 — 场 B 的两块前置：**训练暴露度**（①）+ **multi 终局收缩与全灭判胜**（②）

起因（第五轮复核 §4）：场 B（惩罚纯攒钱）从 v1.5.17 到 v1.5.63 一次都没练好，因为**训练分布里根本没有这个局面**。

### ① 训练暴露度：把"4 席全被动"注入评估分布
- 诊断（复核给出、我已核实）：`js/train/evo.js:814` 的对手是按 `opps[oi % opps.length]` **连续取不同项**，
  而池子 17 项里被动攒钱型只有 `farmer` / `deepsaver` 两项 ⇒ **"4 席全被动"概率为 0（结构上不可能）**
  ⇒ 过去所有"惩罚攒钱"类奖励（惩罚被动 / 先手激励 / 密集分 / `EPIRUS_DIV_W`）都在**样本量为 0 的分布**上优化。
- 修：新增 `PASSIVE_FIELD`（环境旋钮 `EPIRUS_PASSIVE_FIELD`，**默认 1/8 局**）与 `passiveFieldAt(g)`；
  命中局的 **4 个对手席全部**为被动型（`farmer`/`deepsaver` 轮换）。只改"评估哪些局面"，
  **不动参数量、不升 `PACK_VERSION`**。

### ② multi 的平局兜底：收缩必须落进回合上限内 + 全灭按伤害判胜
- 病（实测坐实）：`MODES.multi` 不设 `suddenDeath` ⇒ 走全局 `SUDDEN_DEATH = 100`，而上限 `MAX_ROUNDS = 60`
  ⇒ **收缩在多人局永不触发** ⇒ "不打"零代价（守到哨声即可），场 B 严格胜率上限恒为 **0**（实测 40 局 0 胜 / 40 平）。
- 修 a：`MODES.multi.suddenDeath = 45`（常规局平均 22~30 回合 ⇒ 几乎不影响正常对局，只惩罚拖到 45 回合不清场）。
- 修 b（复核给的另一半，必须做）：**全灭改按"累计造成伤害"判胜**（并列才平局；`damage` 事件的 `source`，
  与 `evo.rankOf` 第 4 键同源）。原因：收缩是**同时**清场 ⇒ 只做修 a 仍是"全员同归于尽⇒平局"
  （实测 40/40 平局、回合恒 47、5/5 阵亡）。

### 验收（可反证，修前修后对比）
| "4 席只ジ vs 1 席冠军" ×40 局 | 修前 | **修后** |
|---|---|---|
| 分出胜负 | **0/40** | **40/40** |
| 平局 / 打满上限 | 40/40 · 40 | **0 · 0** |
| 回合 | 60（恒打满）| **47（恒）** |
| 场 B 严格胜率 | **0%** | **100%** |

### 守门（新增/更新）
- **D57**（新）：multi 收缩必须 >0 且 < `MAX_ROUNDS`；`passiveFieldAt` 契约；"4 席只ジ"场必须出现胜者且不撞上限。
- **D3 / D19**：旧断言记录的是"multi 走全局 100（实际不触发）"这一**病态现状** ⇒ 改断言 multi=45、
  并新增"44 不触发 / 45 全员 −1"的用例；`opts` 覆盖的**污染检查**改成断言"全局仍是模式自身的 45"（效力不变）。
- **D55**：场 B 的"不该挨打"改为按**对手造成的伤**（`takenByOpponentPerGame`）—— 终局收缩的伤 `source=null`，不可格挡。
- 因此本轮 `np-test` 从 104 升到 **105** 条。

### 未做（下一轮，复核 §5 的顺序）
场 B 的**阻断式门槛**（起步：严格胜率 ≥10%）、以及验收 A严胜 / H混合场 / 反弹墙伤害不回退；
之后才是"珠浪费 100%"（暂只打印）与环 exploit（长程滚环者 68%）的节拍随机化。

---

## v1.5.64 — 修"换回来的不是 eco-34"（容器显式旧口径标记 + 等价性守门）+ 训练零暴露度诊断

### 1. 病（第五轮复核发现，我认）：v1.5.63 发出去的是**行为更弱的**策略
`LEGACY(params)` 靠 `shapeOf(params).legacy` 反推，而它按 **`params.length`** 判断 ⇒
`upgrade-pack` 把 3337 撑成 5689 之后 `legacy=false` ⇒ 旧冠军被切到**它从没训练过的 v7 候选口径**。
更深的机制（本版补充）：`embedLegacy` 只保证**新增维度乘 0 ⇒ 前向逐位等价**，
但**动作侧 (目标/珠) 的权重恰好在新增维度里** ⇒ 嵌入后网络**分不出目标** ⇒ 目标退化成枚举顺序。

| 版本 | 座位极差 | 反弹墙（伤害/命中）| 场A | G |
|---|---|---|---|---|
| `eco-34.bak`（原生 v6）| 8pt | 12.25 / 242 | 25% | 4.17 |
| `eco-34-v7.bak`（v1.5.63 发出去的坏嵌入）| 11pt | **7.70 / 152** ✗ | 25% | 4.55 |
| **`eco-34-v7c.bak`（带标记嵌入 = 现线上）** | **8pt** | **12.25 / 242** ✓ | 25% | **4.17** |

### 2. 修法（复核建议 ①，已实现）
- 容器**显式标记**：`pack()` 写 `lv`（来源版本）、`unpack()` 把标记挂到 `params.legacyFrom`、
  新增 `isLegacyChooser(params)`；`js/train/evo.js` 的 `LEGACY()` **优先认标记**（不再认长度）。
- `tools/upgrade-pack.mjs` 输出带 `lv` 的容器；**V7 训练产线不受影响**（训练包无 `lv`）。
- 守门 **D56**（复核建议 ②）：源码契约 + **行为等价断言**（同一份旧形状权重：原生 vs 嵌入+标记 ⇒ 逐场结果必须相同）。
  ⇒ 这条在修好之前**必然红**，正好把问题钉住。

### 3. 线上包（本版重上线，严格体检、无 `--force`）
A 考卷 **42.6%**（坏嵌入是 49.1% —— 口径退化的策略反而在这张卷上分高，说明单指标不够）·
反弹墙 **12.25/局 · 242 命中** · 场A 还手 **25%**（造成 1.63/局、胜率 10%）· G **4.17** ·
E 新口径 **16%**（旧口径 87%）· 座位极差 **9pt** · 自对局 13.1 伤害/局 · 平局 0%。
指纹重记 `53f064c5 → 3459758e`（`policy.js`/`evo.js` 属指纹集）；`np-test 104/104`。

### 4. **训练零暴露度诊断**（复核 §4，已核实 ⇒ 记入附录 B3-7）
训练对手池 `server/opp-pool.mjs`（17 项）在 `js/train/evo.js:814` 是**连续取不同项**：
```js
else { choosers.push(wrapBotN(opps[oi % opps.length].sel)); oi++; }
```
而被动攒钱型只有 `farmer` / `deepsaver` **两项** ⇒ **"4 席全被动"在训练分布里概率为 0（不是少，是不可能）**
⇒ 过去所有奖励尝试（惩罚被动 / 先手激励 / 密集分 / `EPIRUS_DIV_W`）都在**样本量为 0 的分布**上做优化
⇒ 这解释了 v1.5.17 → v1.5.63 场 B 一次都没被练好。**顺序必须是"先改暴露度，再谈奖励"**。

### 5. 场 B 的后续计划（复核 §4 的四步，未做，待办）
1. **暴露度**：把"4 席同型被动"作为一类对手组合进训练评估（哪怕 1/8 局；不动参数量、不升 `PACK_VERSION`）；
2. **平局兜底**：multi 没有 `maxRounds` ⇒ 落到 `MAX_ROUNDS=60` 而 `SUDDEN_DEATH=100` ⇒ **收缩在多人局永不触发**
   ⇒ 该规则下"不打"零代价、场 B 严格胜率上限为 0（要么把 multi 收缩起点降到 ~45，要么"终局全员存活"判负）⇒ 规则改动需重记指纹；
3. **判据**用"清场数 / 严格胜率"，不用进攻率（1.0→3.0 伤害/局可以让进攻率变好而依然一个人打不死）；
4. **做成阻断式门槛**（起步 ≥10%）而不是 fitness 一项 —— 已有两次证据说明加权会牺牲综合（先手激励掉 14pt、破墙奖励让混合场 40%→17%）；
5. 验收同时看 **A严胜 / H混合场 / 反弹墙伤害不回退**。
另两条顺带可见（暂只打印、未做门槛）：**珠浪费 100%**（线上包与 eco-34 都是每局蓄 1.1~1.4 颗、一颗未花；`v9-31` 是 0%）；
**环 exploit 未变**（长程 1 席滚环 vs 4 席冠军：滚环者夺冠 68%、落地伤害 0.0、小雷 1 次/40 局）。

---

## v1.5.63 — 换回 **eco-34**（v7 嵌入版，用户裁定）+ 旧形状包上线通路打通 + CHANGELOG 精简

### 1. 换包（严格体检，**未用 `--force`** —— 线上包此前连续三轮都是 force 越过的）
| 项 | 线上包 v7wall-31 | **新：eco-34（v7 嵌入）** |
|---|---|---|
| A 考卷（40 局）| 41.4% | **49.1%** |
| 座位极差（5 席同策略）| **61pt** ✗ | **15pt** ✓（19/26/23/11/22）|
| F 场A 被集火还手 | **13%** ✗ | **25%** ✓（造成 1.85/局、承受 2.80/局、胜率 18%）|
| 反弹墙（长程 20 局）| 7.25/局 · 84 命中 | 7.70/局 · 152 命中 ✓ |
| G 有效技能数 | 5.15 | **4.55** ✓ |
| E 新口径 · 无威胁摆架势 | 3% | 23% ✓（旧口径 89% = 当年误判它"病态摆架势"的原因）|
| 自对局 | 14.1 伤害/局 · 0% 平局 | 13.5 伤害/局 · 0% 平局 |
| 场B 无压（惩罚攒钱）| 2% · 1.00 伤害/局 | 2% · 1.00 伤害/局（**共同缺口**，待训练解决）|

### 2. 过程修复：旧形状冠军的**上线通路**（本次被挡了三次才发现）
- `tools/promote-champion.mjs` 两处 `unpack(json)` 缺原生标志 ⇒ 对 v5/v6 包返回 **null**（体检直接崩：
  `shapeOf(null)`）；实测 `unpack(j)=null` / `unpack(j,true)=3337`。改为 `unpack(json, true)`（与
  `audit-lib.loadChamp` 注释的规矩一致："历史形状必须原生读取，不要嵌入"）。
- `checkPack` 只校验 **v7 容器**（`o.v !== PACK_VERSION` 即判 not-ok）⇒ 旧包会被回读自检挡死；
  改为"v7 才走 checkPack，旧形状走原生读取 + 长度自检"。
- **仓库契约确认**：线上 3P 包**必须是 v7 容器**（np-test N19 卡 `checkPack`）⇒ 新增
  **`tools/upgrade-pack.mjs`**（`unpack(j,true)` → `embedLegacy` → `pack`，3337 ⇒ 5689 位，`checkPack ok`）。
  ⚠️ 注意嵌入会**改变行为**（chooser 从旧路径切到 v7 候选口径）：实测反弹墙 12.25/242 → **7.70/152**、
  G 4.17 → 4.55、座位 8 → 11pt（嵌入前后都过四道硬门槛）。

### 3. CHANGELOG 精简（用户要求：杂项太多）
3963 行 → **918 行（23%）**：条数不变（105 条），改为「**一行版本索引**（最近 20 条保留要点）+ 附录」：
- **附录 A 当前有效契约/常量**（指纹、形状、v7 容器契约、上线体检门槛、训练隔离、实验开关、口径常量）；
- **附录 B 训练问题复盘**（流水线 / 门槛指标 / 训练动力学 三类，逐条带版本号与实测证据）；
- **附录 C 方法论规则**（踩坑 → 规则 10 条）。
精简前的全文保存在 git 历史：`git show <精简提交的父提交>:CHANGELOG.md` 可取回。

### 4. 门禁
`np-test 103/103` · `smoke OK` · `battle OK`；规则指纹 `53f064c5` 与包内一致；技能报告已自动重跑（`docs/skill-report.html`）。

---

# 变更记录（精简版）

> **本文件是索引 + 当前契约 + 复盘**：每条版本保留"一行决策/结论"，最近 20 条另附要点。
> 精简前的全文（每版的完整证据、表格、日志）保存在 git 历史里，随时可取：
> `git log --oneline -- CHANGELOG.md` 找到精简提交的**父提交**，再 `git show <父提交>:CHANGELOG.md`。
> 精简原则：保留**决策与其理由、门槛/指纹/常量、踩过的坑**；删掉过程复述、重复表格与已经过期的中间读数。

## v1.5.62 — **F 正式换成"场 A 被集火还手率"（≥20%）**（用户裁定）+ eco-34 历史记录核查

### 1. 换口径（工具链三处同步，单一真源）
`tools/audit-lib.mjs` 新增 **`aggressionProfile(W, params, GAMES)`**（场 A/场 B 的单一真源；
冠军座位逐局轮换 `g % 5`；伤害归因用事件真字段 **`source`**）：
- **场 A（被集火）**：4 席脚本猛攻 vs 1 席冠军 ⇒ 还手率 / 造成伤害 / 承受伤害 / 胜率；
- **场 B（无压）**：4 席只ジ vs 1 席冠军 ⇒ "面对纯攒钱者会不会主动打"。

三处消费者：`promote-champion`（**判据 = 场 A ≥20%**，旧的 `<25%` 判据删除，旧值仅作历史对照打印）、
`screen-champ`（筛选器同步）、`champ-audit`（F 列换成场 A）。新守门 **D55**（源码契约 + 行为断言：
场 A 对手出手必须远多于场 B、场 B 里冠军不该挨打）；D41 里过期的"F=25%"正断言改为**反断言**（防回退）。

### 2. 判别力（60 局口径）
| 候选 | 座位极差 | 反弹墙伤害/命中 | **场A 还手** | G | 判定 |
|---|---|---|---|---|---|
| 线上包 | **61pt** ✗ | 7.25 / 84 | **13%** ✗ | 5.15 | ✗ 偏座 + 被集火不还手 |
| **eco-34** | **8pt** ✓ | 12.25 / 242 ✓ | **25%** ✓ | 4.17 ✓ | **✅ 通过** |
| **种子 v1.3.58** | 13pt ✓ | 16.95 / 339 ✓ | 27% ✓ | 3.02 ✓ | **✅ 通过** |
| v12-85 | 33pt ✗ | 16.40 / 313 ✓ | 33% ✓ | 3.40 ✓ | 只差座位 3pt |
| v9-36 | 25pt ✓ | 17.75 / 355 ✓ | 34% ✓ | 1.70 ✗ | 只差 G |
⇒ **修正口径后首次有两个候选全过**（旧口径下所有冠军 22~35% 全被挡，只能 `--force`）。

### 3. eco-34 历史记录核查（用户提问："当初为什么换下来"）
**没有 bug 类问题**，当年被换下的两条理由都是**后来被证明标定错误**的指标：
- **E 89% ✗（判"病态摆架势"）** —— 同一份记录里新口径是 **16% ✓**，并注明"**旧口径误判**：它主要在'对手攒过 5 ジ 之后'才防，合理"；
- **F 22% < 25%** —— 就是本版换掉的这条（`CHANGELOG` 当时写着"待用户裁定：(a) 回退到 eco-34…但 F 22% < 25% 仍差 3pt"）。
它**真实存在的弱点**（`docs/AUDIT-RESPONSE-v1.5.17.md:150`）：面对 4 席只ジ的对手 **0% 胜 / 100% 和 / 主动伤害 1.0/局**
（long 打满 104 回合被终局收缩耗死）—— 正是用户最初的抱怨，也是本版**场 B** 量到的东西。
另有两条背景：它是"**两个杠杆都关**"的老配方训出来的；`eco-34.bak` 的 meta 缺 `ecoOverride/fightOverride/…` 四键。

### 4. **换包仍未执行**（等用户裁定）
场 A 修正后 `eco-34` 与**种子**都过四道硬门槛；但两者在**场 B（惩罚攒钱）**都仍不合格（2% 进攻 / 1.0 伤害/局 / 100% 平局）
⇒ 若要以"能惩罚纯攒钱"为目标，那需要训练而不是换包。换包前我会先出全量体检对照表（A 考卷 / E / H / 蓄能 / 珠浪费 / 场B）。

### 5. 门禁
`np-test 103/103`（新增 D55）。

---

## v1.5.61 — F 的口径重做（双场实测；**门槛未改**，据用户质疑）

### 1. 用户质疑成立：旧 F 的考核依据有问题（代码证据）
`tools/audit-lib.mjs:196-247` 的 `fieldRate`：
```js
const ch = [human];                                   // 0 号座 = 唯一脚本进攻者
for (let i = 1; i < 5; i++) ch.push(policyChooserN(params, 0.15));   // 1~4 号座 = 冠军自己 ×4
const ATK = [GUN, SWORD, SNIPE, TANK, RAILGUN, DRAIN];               // 分子是硬编码 6 张卡
```
① **场地名不副实**：所谓"活跃场"只有 **1 席**进攻，另外 4 席是冠军自己的副本 ⇒ 量到的是**自对局均衡**，
不是"面对猛攻不还手"。② **分子是手工白名单**：数据驱动看真正带 `dmg` 的还有 **`bigT` / `dualGun`**（不在名单里）。
③ 门槛史佐证：CHANGELOG 自己记过"**所有冠军 F 都在 22%~35%**"，门槛 35%→25% 后仍靠 `--force` 越过
⇒ 一个只能靠 force 过的门槛，问题在门槛。

### 2. 新探针 `tools/probe-aggr.mjs`：**两个对置场 + 结果型指标**
冠军座位逐局轮换（`g % 5`）；伤害归因用事件真字段 **`source`**（已打印确认，此前猜 `from` 得 0）。

**场 A 被集火（4 席脚本猛攻 vs 1 席冠军，40 局）**
| 冠军 | 进攻率 | 造成伤害/局 | 承受/局 | 胜率 | 平局 |
|---|---|---|---|---|---|
| **线上包** | **13%** | **0.85** | 2.92 | 8% | 35% |
| 种子 v1.3.58 | 27% | 1.85 | 3.00 | 0% | 38% |
| eco-34 | 25% | 1.63 | 2.88 | 10% | 40% |
| v12-85 | **33%** | 1.98 | 2.80 | 15% | 35% |
| v9-36 | **34%** | **2.08** | 2.88 | 8% | 48% |
⇒ **该口径能把"已知好"与"已知坏"分开**（线上包 13% ✗ / 种子 27% ✓ / eco-34 25% ✓）——
若定 **≥20%**，这是第一个有判别力的 F 阈值（不再需要 `--force`）。

**场 B 无压（4 席只ジ vs 1 席冠军，40 局）**
| 冠军 | 进攻率 | 造成伤害/局 | 平局 | 回合 |
|---|---|---|---|---|
| 线上包 / 种子 / eco-34 | 2% | **1.00** | **100%** | 60.0（打满）|
| v12-85 | 5% | 3.20 | 88% | 60.0 |
| v9-36 | 5% | 3.08 | 100% | 60.0 |
⇒ **所有冠军面对纯攒钱者都不主动进攻** ⇒ 这正是用户最初的抱怨（"有人攒 ji，冠军就一直出防御类技能"），
且它是**真实能力缺口**（不是口径问题）。

### 3. 本版**不改任何门槛**（`promote-champion` / `screen-champ` 一行未动）
新口径的阈值（场 A ≥20%？场 B 要不要设？）待用户裁定后再改；本版只交付探针与读数。

### 4. 门禁
`np-test 102/102`。

---

## v1.5.60 — 覆盖熵权重旋钮 `EPIRUS_DIV_W`（训练侧；默认不设 => 行为一字不变）

动因：本晚筛选发现 **G≥3 与 F≥25% 在 25 个候选里几乎互斥**（打得凶的只用 2~3 张卡；用卡多的不够凶）
⇒ 覆盖度这一维在演化目标里被压得过低（`js/train/evo.js` 的注释声称覆盖熵能压低"只剩两三张卡"的个体，
实测权重不足以对抗 fitness 主力项）。`server/train-server.mjs` 读该环境变量并调 `T.setEconomyReward({divW})`。
实测（`EPIRUS_DIV_W` = 默认×10 的 `v12` 臂，12 seed）：**没有打破互斥**（高 G 的仍不进攻、进攻的仍 G<3）
⇒ 说明这是**当前 fitness/探针定义下的结构性张力**，不是权重调不够（→ 促成 v1.5.61 的口径重做）。

---

## v1.5.59 — 蓄能空转量化（`wastedChargeRate`）+ 反弹墙真信号 + 候选筛选器；换冠军**有据暂缓**

### 1. 新探针 `chargeProfile`（`tools/audit-lib.mjs`）+ 体检列 `蓄能/局` / `珠浪费`
事件是真源（先打印过，未猜字段）：`{type:'action',key:'charge',outcome:'ok'}` / `{type:'bead',kind,delta:±1}` / `{type:'beadExpire'}`。
`wasteRate = 过期珠 / 得珠` ⇒ 用户实感的"蓄能→珠过期→再蓄能"变成可量化指标：

| 候选 | 蓄能/局 | 珠浪费率 |
|---|---|---|
| **线上包**（v7wall-31）| **17.2** | **84%** |
| `v7seat2-51` | **1.8** | 83% |
⇒ 线上冠军每局蓄能 17 次、**84% 的珠白攒**；候选只蓄能 1.8 次/局（基数小 10 倍）。

### 2. D 列（反弹墙）的口径真相
`1st` 在这个场里几乎全是并列：线上包 **1st=100% 但严胜仅 12.5%（并列 87.5%）**、候选 1st=32.5%/严胜 0%。
⇒ 真正有判别力的是**反弹墙伤害/局 + 穿透命中数**（新增列），而不是 1st 或严胜。

### 3. 新工具 `tools/screen-champ.mjs`（候选筛选器）
一次量完全部硬指标：座位极差（`seatSymmetry`）/ 反弹墙伤害与穿透命中（`reflectWall`）/ 活跃场进攻（`fieldRate`）/ G。
**判据**：座位 ≥30pt 判偏 · 反弹墙伤害 ≤0.5/局 = "反弹墙瘫" · 活跃场 <25% = 不进攻 · G <3 = 打法坍缩。

### 4. 换冠军：**有据暂缓**（实测表见下）
| 候选 | 座位极差 | 反弹墙伤害/局 | 穿透命中 | 活跃场 | G | 结论 |
|---|---|---|---|---|---|---|
| 线上包 v7wall-31 | **61pt** ✗ | 7.25 | 84 | 22% ✗ | 5.15 | 偏座 + 不进攻 |
| v7seat2-51 | **9pt** ✓ | **0.00** ✗ | 0 | 29% ✓ | 3.47 | 反弹墙瘫 |
| v9-36 | 25pt ✓ | 17.75 ✓ | 355 | 37% ✓ | **1.70** ✗ | 只差 G |
| v9-31 | 29pt ✓ | 17.80 ✓ | 356 | 32% ✓ | **2.46** ✗ | 只差 G |
| v9-46 | 39pt ✗ | 20.00 ✓ | 400 | 33% ✓ | 1.67 ✗ | 偏座 + G |
⇒ **无候选三全**：高 D 的候选全部 G<3（打法坍缩）；G 高的线上包偏座且不进攻。故**暂缓换包**，先训练找三全候选。

### 5. 附带证据：健康门槛确实在"整臂拦下"
`v10` 臂（6 seed，未开 `EPIRUS_ALLOW_HEALTH_FAIL`）**6/6 全被拦、0 产物**；`v9` 臂（开开关）12/12 有产物
⇒ 训练候选池的可得性完全取决于该开关（`v1.5.56` 引入，默认关）。

### 6. 门禁
`np-test 102/102`。

---

## v1.5.58 — 体检并报**严格胜率**（第五轮复核 §4-1/§4-2）：`1st` 把并列算第一，已可见

### 1. 病（复核实测 + 我复现）
`tools/eval-5p.mjs:311` 的 `1st` 走 `T.rankOf`，其排序键 `(存活, hp, taken, dealt)` 在平票时按本局种子洗牌
⇒ **被动场里"多打出 1 点伤害"就能拿第一**。我在 farmerwall 上实测：

```
[冠军] 1st=97.5%   严胜=0.0%   并列=97.5%
```
⇒ **"I 对被动 95~97%"整列都是并列洗牌的产物**，不代表"会打被动场"（引擎真正判胜 0~4%）。

### 2. 修（便宜、不动历史列）
`eval-5p.mjs` 每列并报三个数：`1st(并列计) / 严胜(引擎判胜) / 并列(名次第一但没判赢)`；
`tools/audit-lib.mjs` 的 `exam()` 解析出 `strict`/`tie`；`tools/champ-audit.mjs` 体检表新增 **A严胜** 列。

### 3. 读数（本版实测）
| 场 | 1st | 严胜 | 判断 |
|---|---|---|---|
| A 考卷（standard）| 41.4% | **40.4%** | 差别小 ⇒ **A 列基本可信** |
| farmerwall（I 列）| 97.5% | **0.0%** | ⇒ **该列在"熬满"场里不可用** |
| mix4 / reflectwall | 同类机制（复核上轮已发现"100% 但 52/60 局全灭平局"）| 待逐场重测 | 与 I 列同族 |

### 4. 未做（诚实登记）
**硬门槛改走严格口径**这一步本轮没做：先让三列并联可见（避免历史列不可比），下一轮再把门槛切到严格口径。

### 5. 门禁
`np-test 102/102`。

---

## v1.5.57 — 第五轮独立复核（Qoder）的修正：**座位对称性进体检** + 体检口径/表头修复 + 我的一处单位错误

### 0. 我先认错：上一轮"多数候选已修好"的结论**不成立**
我那张表的"极差"列算的是**胜场数差**，却拿去和**百分点阈值 18** 比 ⇒ 推出"中位 18 ⇒ 多数已修好"。
按百分点重算：`97/2/2/0/0` 的极差是 **97pt**（我印 57）、`100/0/0/0/0` 是 **100pt**（我印 39）。
正确中位约 **33pt** ⇒ **多数候选仍然偏座**（复核的判断对）。本版把口径钉死在百分点上。

### 1. 核实（实测，100 局、5 席同策略、引擎判胜）
| 对象 | long 各座胜率 | 极差 | multi 极差 |
|---|---|---|---|
| **线上包 = `v7wall-31`**（`v7\|0d987a62\|5689`，与 `docs/artifacts/v7wall-31.bak` 一致）| 84% 4% 6% 4% 2% | **82pt** | **51pt** |
| `v7seat2-51` | 13% 26% 26% 20% 15% | 12pt | 16pt |
| `v9-31` | 12% 16% 21% 26% 25% | 14pt | 17pt |
⇒ **玩家真正遇到的对手（线上包）才是最偏的那个**，而此前所有体检项都看不见这件事。

### 2. 新探针 `seatSymmetry(W, params, mode, GAMES)`（`tools/audit-lib.mjs`）
5 席同一策略、每局带盐、**回报一律百分点**；附带判据：**前置"分出胜负 ≥30%"**（平局过多时"各座≈0%"是空读数，
如某候选 93% 平局时的"极差 3pt"）+ **极差 ≥30pt ⇒ 判偏**；`max/min` 比值一并回报供参考。
`tools/champ-audit.mjs` 新增 **座位极差** 列，默认 **100 局**（20 局时噪声足以淹没 30pt 阈值）。

### 3. **座位对称性进上线体检**（`tools/promote-champion.mjs`）
按复核 §6 的"比值 + 前置条件"思路：`verdict==='biased'` ⇒ **阻断上线**；`unjudgeable`（平局过多）⇒ 警告并明确
"别把它当均衡"，不计入阻断。⇒ 现行线上包在新门槛下 **FAIL（60pt）**，即复核要求的 **G3 接成已知红**。

### 4. 复核指出的其它两处，已修
- **体检表头与列错位**（`champ-audit`）：H/I 原先印在 D 之后、表头却排在末尾 ⇒ 按表头读整体错两格；现按表头顺序印。
- **`1st` 口径把并列算第一**（`eval-5p.mjs:311` 走 `rankOf`）：复核的 farmerwall 实测（引擎判胜 4% / 判平 96% /
  `rankOf==1` 99%）确认 ⇒ **A/D/H/I 四列在"熬到上限全员存活"的场里都会虚高**。
  本版先记录（本轮不改口径，避免历史列不可比）：**下一轮**给每列并报"严格胜率 / 并列率"，硬门槛改用严格口径。

### 5. 换冠军的取舍（实测表，见本节末）
没有候选能同时满足：线上包 D=100%/G=5.15 但座位 60pt；`v7seat2-51` A=50.9%/座位 14pt/F=30% 但 D=25%；
`v9-31` D=100%/座位 18pt 但 G=2.46（过不了 G≥3）。⇒ 换冠军**暂缓**，先跑带座位门槛的臂找"三全"候选。

### 6. 门禁
`np-test 102/102`（D50–D54 不变）。

---

## v1.5.54 — 引擎兜底改为**按每局盐轮换**（L7 第四次）：无显式目标时不再恒定取最小索引

### 1. 病（三处，全部在 `js/core/resolve.js`）
```js
oppOf:      const o = aliveOpps(state, pid); return o.length ? o[0] : null;   // 恒定最小索引
mirrorRefs: if (t1 == null || ...) t1 = opps.length ? opps[0] : null;         // 同上
            for (const o of opps) { if (o !== t1) { t2 = o; break; } }        // 恒取最小 ≠ t1
```
⇒ 无显式目标（self 类效果贴在对手身上、镜面反射的 t1/t2）**一律落到索引最小的存活玩家** = **0 号座**
⇒ "0 号座是所有人的默认靶子"（实测 0 号座每局必死 60/60 的一部分来源），也是"多目标技能第二目标不可表达"那条的另一半。

### 2. 修法
新增纯函数 `saltPick(state, arr, seq)`：**盐 0 ⇒ 保持旧口径 `[0]`**（页面与既有对比基线不变）；
有盐 ⇒ 按 `(salt, round, seq)` 轮换（**不消耗任何随机流** —— 借流的三个坑 v1.5.51 已踩过）。
三处兜底全部改用它；导出 `oppOf` / `saltPick` 供守门直测。

### 3. 新守门 **D53**
盐 0/无盐 ⇒ 旧口径 · 有盐 ⇒ 默认作用者覆盖全部候选且大致均匀（40 盐 × 5 回合）· `oppOf` 行为同验 ·
源码断言三处兜底必须已移除 + `saltPick` 必须存在。

### 4. 门禁
`np-test 101/101`；指纹按铁律重记（`fc20996a → 53f064c5`）。

### 5. 下一步（本轮同时启动）
12 seed 大验收臂（v7l7）：同时检验"引擎层三条 L7 修复 + 特征层随机槽位"能否消掉训练诱导的座位偏置；
样本量从上一轮的 2 个不同策略提上去。

---

## v1.5.53 — 结算起点改为**每局随机相位**（L7 的第三次应用）+ 决定性诊断：**偏置是训练造出来的**

### 1. 修：`turnOrder` 的确定性相位
`js/core/resolve.js` 原为 `start = (round - 1) % n` —— **确定性映射** ⇒ 第 1 回合**永远**从 0 号座开始。
（该行上方注释本身就写着上一次修"永远按座位升序"时也是改成另一个**确定性**映射。）
修法：`start = ((salt % n) + round - 1) % n`，盐取 `state.slotSalt`（每局不同、纯函数、**不消耗任何随机流**）。
**盐 0 保持旧口径**（页面与既有对比基线不变）；2 人局恒等顺序（v1.0 冻结）。导出 `turnOrder` 供守门直测。

### 2. 新守门 **D52**
25 个不同盐 ⇒ 第 1 回合起点**覆盖全部 5 座**且大致均匀 · 盐 0 ⇒ 第 1 回合 = 0 号座、第 3 回合 = 2 号座 · 2 人局 = `0,1` · 源码断言（用盐、旧相位必须已移除）。

### 3. 🔑 决定性诊断（本版最重要的一条）
| 对象 | 各座夺冠率（60 局）| 各座死亡数 | 结论 |
|---|---|---|---|
| **热启动种子 v1.3.58** | **22% 16% 22% 16% 24%** | 43/44/43/49/38 | **本身几乎均衡** |
| 训练产物 `v7phase-31..34`（同一权重）| 39% 15% 15% 15% 17% | 39/51/50/49/46 | 强烈偏座 |
| 训练产物 `v7phase-35..36`（同一权重）| 0% 30% 26% 23% 21% | 60/42/47/45/46 | 反向极端 |
⇒ **偏置不是引擎层、也不是特征层造成的，而是训练过程造出来的**（从均衡种子出发、产出偏座策略）。
⇒ 6 个 seed 只产出 **2 个不同策略**（探索近乎冻结）⇒ 验收样本量实际只有 2，**不足以判定修复效果**。

### 4. 已知尚未修的确定性身份映射（下一项）
引擎 `oppOf` / `aliveOpps` 兜底 = **"索引最小的存活对手"**（无显式目标时的默认作用者、镜面反射 t2 兜底）。
它正是"多目标技能两个目标不可调换"那条的另一半。**注**：本版尝试统计"无目标出手比例"的探针**无效**
（猜错了事件字段名 ⇒ 报 100% null ⇒ 实际什么都没测到）—— 下一轮先**打印真实事件对象**再统计，不再猜。

### 5. 门禁
`np-test 100/100`（含 D50/D51/D52）；指纹按铁律重记（`d44d68b3 → 7902661f`）。

---

## v1.5.52 — 槽位顺序改为**纯函数哈希**（不再借任何随机流）；**双目标技能有序成对进入决策**

### 1. `slotRand` 重做：`__slotHash(salt, round, i)`，`salt = state.slotSalt`（调用方输入，缺省 0）
同时满足四条约束：① **不消耗任何随机流**；② **可复现**（同种子同结果）；③ 同一决策内天然一致；
④ 跨局不可预测由调用方"每局传不同盐"提供（L7：并列不许有任何确定性身份映射）。

**过程中查出并钉死三个真缺陷**（每个都配了反证型守门）：
| 缺陷 | 后果 | 守门 |
|---|---|---|
| v1.5.51 借 `__rng()` 当随机键 | 消耗采样流 + 特征对隐藏状态敏感 ⇒ **D22 红**（蓄能珠类型泄漏 [66,76]）| D50：**槽位键不得使用任何随机流** |
| 从 `state.rng` 抽盐 | 改变对局行为 ⇒ **D26 红** | D50：禁 `state.rng`/`rng.next` |
| 计数器盐（每 state 递增）| **破坏"同种子同结果"** ⇒ **D13 红** | 盐改为调用方输入、缺省 0 |
（旧版"缓存随机键 `SLOT_RAND`"一并移除；D50 改为断言其**不存在** + 纯函数哈希存在。）

### 2. 双目标技能：`target2` 声明 + **有序成对枚举**
- `rules.js`：`mk(SK.DUAL_GUN,…)` / `mk(SK.MIRROR,…)` 经 **`opts.extra`**（`mk` 唯一注入通道）声明 `target2: 'enemy'`；
- `candidatesFor`：**有序成对枚举** `(t1,t2)` ⇒ 5 人局 **12 个候选**（4×3）、两目标必不同、**(1→2) 与 (2→1) 是两个不同候选**
  ⇒ **"两个目标不可调换"的语义可表达**（镜面反射 t1=复制对象 / t2=输出对象）；
- 此前 `target2` 恒 `null` ⇒ 第二个目标由引擎兜底（`mirrorRefs` 的"索引最小对手"）填
  ⇒ **这是 0 号座每局必死 60/60 的一部分来源**，也是"角色不可表达"的根因；
- 新守门 **D51**：数据声明 + 全部有序组合 n×(n−1) + 两目标不同 + (1→2)/(2→1) 都在 + 单目标技能口径不变。

### 3. 过时测试假设修正
D26 原先硬编码"玩家 1 在槽 1"（pid 升序时代遗留）⇒ 改为**按 pid 反查槽位**（T/B 块布局：槽 0 = 我，槽 1..4 = 对手槽顺序）。

### 4. 确认
`choose` 采样是**单次抽样 + CDF 扫描**（`policy.js:534-537`/`633`）⇒ 候选数 4→12 **不扰动随机流**（已排除第二个坑）。
指纹按铁律重记（`300ad368 → d44d68b3`）。门禁：`np-test 99/99`。

### 5. 紧随其后（未完成，按序）
① 每局盐接进训练主循环（`state.slotSalt` 由每局种子派生）⇒ 训练里槽位顺序才真正跨局不可预测；
② 去掉引擎兜底（`mirrorRefs` 自动填 t2 / `oppOf` 索引最小兜底）⇒ 无显式目标即不生效；
③ 重训一臂后**复测验收**（各座夺冠率 ~20%、0 号座死亡数与其他座持平）。

---

## v1.5.51 — 槽位顺序改为**每次决策内一次随机洗牌**（破"确定性身份映射"；本会话第三次同类修正）

### 1. 完整诊断链（三层排除 + 一层定位）
| 层 | 测试 | 结果 |
|---|---|---|
| 引擎 | **随机策略** 5 座自对局 | 无偏（各座 23/15/21/25/16，极差 7 局）|
| **状态特征** | 完全对称局面下逐维比较五座 | **0/213 维不对称** ⇒ 无座位编码 |
| **开局策略** | 5 个同一冠军坐满 5 座，看第 1 回合选择（20 局 × temp 0/0.15）| 各座都是 `ji` 主导、分布同形 ⇒ 开局无座位差异 |
| **中后期动力学** | 各座死亡次数 + 提高温度重测 | **0 号座死亡 45~48/50（其他 34~42）、夺冠 3~9%**；temp 0.5 仍偏（甚至更明显）⇒ **结构性、非协调假象** |

### 2. 机制（指向 v1.5.46 我自己的修复）
- 旧版并列按 **pid 升序** ⇒ 开局同血时"槽位 0"永远是 0 号座 ⇒ 上线冠军 **0 号座夺冠 81%**；
- **v1.5.46 改成"按回合起点轮转"** ⇒ 仍然是一个**确定性映射**：每回合"回合起点"占所有人的槽位 0，
  策略学出"打槽位 0"⇒ **回合起点被集火**；而**第 1 回合起点 = 0 号座**、且对局在前几回合就定局 ⇒ 0 号座先死 ✓
- ⇒ **L7 教训的完整含义：并列不许有任何确定性的身份映射**（"更对称的确定性映射"也不行）。

### 3. 修法
`oppSlots` 的并列分支改用 **`slotRand`**：**每次决策内一次随机洗牌**，
按 `state + round` 缓存 ⇒ 同一决策内所有调用（features/actionFeatures/多候选打分）**特征一致**，
跨座位/回合**不可预测** ⇒ "打槽位 0"不再是可利用习惯。
守门 **D50** 同步：必须用 `slotRand`、**旧 `_rot` 必须已移除**（反断言）、随机键必须按 `state+round` 缓存。

### 4. 可证伪验收（正在跑）
重训一臂（6 seed、无强迫）后量新候选：**各座夺冠率应回到约 20%**（现在 3~9%）
且 **0 号座死亡次数应与其他座位持平**（现在 45~48 vs 34~42）。两条都满足才算修好。

### 5. 连带
`policy.js` 属指纹集 ⇒ 按铁律重记两个包（`be74b3d3 → 300ad368`），3P 权重不变。

---

## v1.5.50 — 座位偏置二分（6 seed/格）：**泄漏不在三个特征块里**，转向训练动力学 + 新线索"0 号座 3%"

### 1. 读数（`EPIRUS_FEAT_MASK`，各格 6 seed，long·120 局）
| 格 | 掩掉 | 典型各座夺冠率 | 极差 | 判决 |
|---|---|---|---|---|
| `v7m2_noTarget` | `target` 块 | **3%** 23% 25% 21% 28% | 24~30 | 仍偏 |
| `v7m2_noEffects` | `effects` 块 | 3% 26% 19% 26% 25%；另有 4 个 **98%** 1% 1% 1% 0% | 24 / **117** | 仍偏（且更极端）|
| `v7m2_noBead` | `bead` 块（对照）| **3%** 23% 25% 21% 28%；另有 50% 15% 15% 11% 10% | 30 / 44 | 仍偏 |

⇒ 三格都仍偏 ⇒ **泄漏不在 `target`/`effects`/`bead` 这三个特征块**（掩掉它们并不能去掉偏置）⇒ 按事先定的判读规则**转向训练动力学**。

### 2. 新线索（更锋利，下一步就查它）
反复出现的模式是"**0 号座只有 3%**" —— 这是 **0 号座的系统性劣势**，**方向与上线冠军那个 81% 优势相反**。
⇒ 说明**训练或评测路径里有某处系统性削弱 0 号座**。候选排查点（按可疑度）：
① `rankOf` 的并列洗牌在**训练打分**中的使用；② 评测/训练里**各座 chooser 的构造顺序**与温度；
③ `featuresV7` base 段是否存在与座位相关的量；④ 训练里"只给被测座补资源"的那段评测逻辑（`seat = g % N`）在**跨座比较**时的口径。

### 3. 意外收获：**冻结不是普遍现象，是强迫/环奖励臂特有的**
| 臂类型 | 逃逸率 |
|---|---|
| 带强迫（`EPIRUS_RING_FORCE_EPS`）/ 环奖励窗口 | 约 **1/6** |
| **本轮三格（无强迫、无环奖励）** | **6/6 全部有提升** |
⇒ "训练静默冻结"应记为**那些实验臂特有**的现象，而不是流水线的常态。

### 4. 待办（顺序）
1. 查 **0 号座劣势**（上表列的四个候选点）；2. **G3 固化成真门槛**；3. 替换无判别力的 `ringWallProbe` + 新增 `wastedChargeRate`；
4. 环 exploit：开环节拍随机化 + 反证型守门。

---

## v1.5.47 — 换冠军 `v7seat2-51`：**去座位偏置** + 首个"零 `--force`"的干净换冠军

> 起因：第三方复核 §3 发现**座位偏置**（5 席同冠军自对局 **0 号座夺冠 81%**，其余 3~7%，期望 20%），用户要求优先修。

### 1. 偏置的调查链（每一步都有"能失败"的对照）
| 步骤 | 测试 | 结果 |
|---|---|---|
| 引擎/代码在**对局**层有偏？ | **随机策略** 5 席自对局 | ❌ **无偏**（各座 23/15/21/25/16，极差 **7 局**）|
| 训练时**被测座位固定**？ | 查 `scoreMemberN` | ❌ 座位是**轮转**的（`seat = g % n`）|
| `oppSlots` 并列按 pid 升序（v1.5.46 已修）？ | 修 + 重训 | **部分有效**：仍有 4/5 候选严重偏（87/78/59/21 局）|
| **未训练的种子**是否偏？ | 冻结产物（= 热启动种子） | ✅ **均衡（4 局）** ⇒ 偏置是**训练中学出来的** |

⇒ 收窄为：**偏置可学习、来源在特征/训练信号**（不在引擎、不在被测座位）。**二分计划**（`setFeatMask`，下一步）：
逐块掩掉各训 2 seed，看哪块掩掉后 G3 极差塌回 ⇒ 优先怀疑**动作特征里的"目标槽位下标"**（同一目标在不同座位下槽位不同 ⇒ 可学"打槽位 0"），
其次每对手槽位块；base 聚合块作对照（应对称、预期无变化）。

### 2. 换冠军：`v7seat2-51` → `js/bundled-champion-3p.js`（**未用 `--force`**）

| 指标 | **新：v7seat2-51** | 旧：v7wall-31 |
|---|---|---|
| **座位夺冠极差（G3）** | **4 局 ✅** | **91 局 ❌** |
| 标准考卷 1st（200 局） | **48.5%**（项目最高）| 44.0% |
| **F 活跃场进攻率** | **29% ✓（无需 `--force`）** | 24% ✗ |
| E 无威胁摆架势 | 0% ✓ | 0% ✓ |
| G 有效技能 / 回合 / 平局 | 4.92（**13 种**）/ 23.9 / 0% | 5.40（10 种）/ 30.1 / 0% |
| H 混合场 / I 对被动 | 80% / 100% | 80% / 95% |
| **D 长程反弹墙** | **23%**（已知退化，如实记录）| **100%** |

- **首次出现"全过门槛、不需要 `--force`"的换冠军**（`auditForced` 应为 false）；
- **代价（如实记录）**：D 反弹墙 100% → 23%（复核 §3-1 那条能力线又变弱 —— 但注意 D 的 100% 部分来自"同归于尽后按血量第一"的口径）。

### 3. 待办
1. **把千问的 G3 固化成真门槛**（`tools/gate-drafts.mjs` 作单一来源）：它已证明有判别力
   （在 45/46/48/49 上红、在 51 与种子模型上绿）⇒ 可作提交前的硬门，防止再引入带偏置的冠军；
2. **偏置来源二分**（`setFeatMask`，见 §1）；
3. **替换我那个没有判别力的 `ringWallProbe`**（G1 已证明它对"会瞄/仪式"策略都报 40 次）⇒ 环课题改用 `probeField`/`probeFieldNoRinger`；
4. 环 exploit 本身（节拍随机化 + 反证型守门）继续排队。

---

## v1.5.46 — **修座位偏置**（复核 §3，最高优先级）：并列不再按 pid 升序

> 复核实测：同种子族、5 席全部加载**同一个**冠军包、temp 0.15、200 局 ⇒
> **0 号座夺冠 81%（long）/ 59%（multi）**，其余座位 3~7%（对称期望 20%）。
> 且 `resolve.js` 的 `turnOrder()` 每回合轮转行动起点 ⇒ **不是"先手"造成的**。

### 1. 根因（一行代码的并列分支）
```js
// js/train/policy.js  oppSlots(state, pid)  —— 修前
a.sort(function (x, y) { const d = state.p[x].hp - state.p[y].hp; return d !== 0 ? d : x - y; });
```
开局全员同血 ⇒ 并列全部走后一项 ⇒ 1~4 号座看别人时 **"槽位 0"永远是 0 号座**，
而 `FEAT_S` 里 4 槽 × 10 维是**硬编码下标** ⇒ 网络完全可以学出"槽位 0 该怎么对待"
⇒ **与实力无关的座位身份泄漏**（即本项目 `L7` 教训"并列不许按 pid 升序"在另一处的复现）。

### 2. 修法（v1.5.46）
并列分支改用**与 pid 无关、且每回合变化**的轮转：
```js
const _ts = ((((state.round || 1) - 1) % _n) + _n) % _n;
const _rot = function (i) { return (((i - _ts) % _n) + _n) % _n; };
a.sort(function (x, y) { const d = state.p[x].hp - state.p[y].hp; return d !== 0 ? d : _rot(x) - _rot(y); });
```
实测：`pid=1` 的"槽位 0"从**恒为 0** 变为随回合变化 `[0,2,2,3,4]` ✓。
守门 **D50**（行为 + 源码双断言）：不同回合下槽位 0 不得恒为同一人；必须用 `_rot`；旧的 `x - y` 必须已移除。

### 3. 影响面（为什么这条优先级最高）
它给**一整类"5 座同一冠军"的指标**打上折扣：B 伤害/局、平局率、`mirrorHealth`（健康门槛的数据源）、D 长程反弹墙，
以及复核自己上一轮的 D 项对照。

### 4. 连带：指纹重记（铁律）
`policy.js` 属指纹集 ⇒ `57108d06 → **be74b3d3**` ⇒ 重测重记两个包（3P 权重不变仍为 `v7wall-31`）⇒ D16 转绿，`np-test 98/98`。

### 5. 偏置修复后现役冠军的新读数（**基本不变** ⇒ 纯缺陷修复，无连带行为变化）
A 考卷 42% · B 伤害/局 13.8 · 回合 30.1 · **D 反弹墙 100%** · **E 无威胁 0% ✓** · **F 24% ✗（`--force`）** ·
G 5.40（10 种）· H 混合场 80% · I 对被动 95% · 平局 0%。

### 6. 仍未做（复核文档 §2/§4 指出的，按优先级）
1. **环 exploit 一点没动**（回退后的现状）：长程 1 席连开环 + 4 席冠军 ⇒ **滚环者夺冠 75%、落地伤害 0.0**，
   而冠军 40 局只出 **2 次**小雷 —— 用户那两局就是这么输的；
2. **两条反证型守门**（换人反证：把唯一滚环者换成不滚环 ⇒ 小雷出手必须显著下降；判别场：1 环 + 3 快攻 + 1 冠军 ⇒ 出手必须 > 0）；
3. **训练侧解耦**：环流对手**开环节拍随机化**，打断"环 ↔ 回合数"共线；并确认 `opps` 里 `ringspam` ×3 是否有意；
4. `auditForced=true` 仍是 F 24% < 25%。

---

## v1.5.45 — **回退冠军到 `v7wall-31`**：v7r2-35 的"会打断环"被复核证实是**第 4 回合仪式**

> 依据：用户实测两局（`results/epirus-battle-20回合.txt` / `-21回合.txt`）+ 第三方复核复刻。
> **实测证据（原文）**：20 回合局里 **玩家1 出【聚能环】**，四个冠军座**全部【雷击之枪】打彼此**（两次"互雷成环"自废），
> **唯一在开环的玩家1 一枪没挨**；21 回合局里 **玩家1 出的是【ジ】（无人开环）**，四座**照样四发齐射**。
> 复刻场（1 席连开环 + 4 席冠军，long，40 局）：小雷 187 次（**第 4 回合 ×160**、第 8 回合 ×20、其余 7），
> **砸在滚环者身上 0 次**、160 次出手时**场上根本没有环**；判别场（1 环 + 3 快攻 + 1 冠军）小雷 **0 次**；
> 自对局（无环）小雷 **560 次 = 9.7% 出手** ⇒ 把反环卡当常规动作在烧。

### 1. 处置：回退冠军
`v7r2-35` 的收益（"自然小雷 25"）是**假验收**产物 ⇒ 回退到 **`v7wall-31`**（标准 44.0% / D 100% / E 0% ✓ / 全息套别人 2.0 ✓）。
用 `--force` 越过 F 24%（差 1pt 属噪声带），meta 留痕。

### 2. ⚠️ 我的验收标准**第二次**失效（必须记进方法论）
探针场地是**固定节拍**的滚环场（每回合无脑开环）⇒ "**环出现**"与"**回合数**"**共线** ⇒
只要在第 4 回合砸小雷就必然"命中"⇒ `miniTCasts > 0` **由场地构造保证、不可能失败**。
- 第一次同类错误：`miniTCasts ≥ 5` 可被 ε-强迫自证（复核 §5-5 已警告过，我没把它贯彻到**探针场地构造**）；
- **新规矩**：验收断言必须能**失败**，且**探针必须自带判别臂**（同回合数、但把环换成非环 ⇒ 出手数必须显著下降）。

### 3. 下一步（已排好序，先做前两条再做训练）
1. **两条反证型守门（np-test）**：
   - **换人反证**：把场上唯一的滚环者换成不滚环 ⇒ 小雷出手数**必须显著下降**（现在不降 ⇒ 它读的是回合数）；
   - **判别场**：1 滚环者 + 3 快攻 + 1 冠军 ⇒ 小雷出手数必须 **> 0**（现在 = 0 ⇒ 一有真实压力动作就消失）。
2. **训练侧解耦**：把环流对手的**开环节拍随机化**（先攒够再等几回合 / 中途断一环），
   打断"环 ↔ 回合数"的共线 —— 否则**换任何奖励形状都会重新拟合回合数**；
   并确认 `opps` 里 `ringspam` 出现 **3 次**是有意还是拼接产物（它放大共线）。
3. 之后才谈是否重训。

---

## v1.5.44 — **换冠军 `v7r2-35`：首个"会打断环"的冠军**（供用户/复核实测）

> 用户裁定：先换上，让用户与第三方复核**实测**该行为，再决定是否继续投资这条线。

### 1. 新老对照

| 指标 | **新：v7r2-35** | 旧：v7wall-31 |
|---|---|---|
| **自然小雷次数**（20 局对 4 席环流，**关掉强迫后量**）| **25** | 0 |
| **由我造成的作废**（`voided` 且 `byPid === 我`）| **25** | 0 |
| 环墙 1st（long·200 局）| **42.5%** | 26.0% |
| 标准考卷 1st（200 局）| 40.2% | **44.0%** |
| D 长程反弹墙 | 100%（穿透 **344**）| 100%（穿透 84）|
| E 无威胁摆架势 / G / 平局 | 0% ✓ / 5.11（8 种）/ 0% | 0% ✓ / 5.15（11 种）/ 0% |
| **F 活跃场进攻率** | **11% ✗（`--force` 越过）** | 24% ✗ |
| 全息套给别人（硬门槛）| 0 ✓ | 2.0 ✓ |

### 2. 成功配方（三要素，缺一不可；前七次失败的原因见 v1.5.31~v1.5.43）
1. **奖励可归因**（v1.5.37）：`setVoid` 带 `byPid` —— 否则"我打断"与"别人打断、我恰好放过小雷"同分；
2. **开环判据 = 动作事件**（同版）：该座**真的施放了聚能环**（含首次）—— 旧 `delta ≥ 2` 漏首次、还把避雷针 +4 ジ 误判；
3. **退火强迫**（v1.5.40）：格内 ε=1.0 只在前 **40** 代生效 ⇒ 产物来自**无强迫的后段**，验收才有意义。

### 3. 已知代价与风险（**如实记录，供复核重点验证**）
- **标准考卷 −3.8pt**（44.0% → 40.2%）；
- **F 11% < 25%**（活跃场进攻率）⇒ 常态对局可能**偏被动**；
- **能力命中率 ≈ 1/24**（"逃过冻结" ≠ "学会"：18 种子批量里 3 个逃逸、0 个学会）⇒ 本候选是**确定性样本**，不代表配方已稳健；
- 根因（v1.5.42 的负结果）：**堵点在选择哲学** —— 强迫让形状分变差 ⇒ 探索期个体永远不算 `better` ⇒ 提升链不留它们。

### 4. 请实测时重点记录的三类现象
① 我方开环时它**没**出小雷的回合；② **该打断却没打断**的场面特征（是否满血/是否已连开多回合）；
③ **该进攻却摆架势**的回合（对应已知短板 F 11%）。这三类可直接喂给下一步"改选择哲学"的课题。

---

## v1.5.43 — ② 的负结果 + 18 种子批量："逃过冻结"≠"学会打断环"，堵点在**选择哲学**

### 1. ②（探索期不被健康门槛惩罚）实测：**没治好脆弱性**
| | 修 ② 前（窗口 40）| 修 ② 后（v7r2）|
|---|---|---|
| 逃逸 seed | 1/6（35）| **1/6（还是 35）** |
| 该 seed 的自然小雷 / 可归因作废 | 25 / 25 | **25 / 25（完全复现）** |
⇒ **堵点不在健康门槛**：`finishStep` 要求 `better`（形状分/胜率变好），而**被强迫出来的小雷让形状分变差**
⇒ 探索期个体**从来不是 better** ⇒ 连进入提升候选的机会都没有（健康与否都轮不到）。

### 2. 18 种子批量（41~58，同一配置）：**逃逸 3 个（45/46/52），学会 0 个**
```
逃逸 3 / 零提升 15      v7r3-45 小雷 0 / 作废 0 / 穿透 291
                        v7r3-46 小雷 0 / 作废 0 / 穿透 257
                        v7r3-52 小雷 0 / 作废 0 / 穿透 359
```
⇒ **"逃过冻结"与"学会打断环"是两件事**：逃逸是必要不充分条件。

### 3. 全部证据汇总（窗口 × 种子）
| 配置 | 种子 | 逃逸 | 学会（自然小雷>0）|
|---|---|---|---|
| 窗口 20 | 31–36 | 0 | 0 |
| **窗口 40** | 31–36 | 1（35）| **1（35：小雷 25 / 作废 25）** |
| 窗口 40 | 41–58 | 3 | **0** |
| 窗口 80 | 31–36 | 1 | 0 |
⇒ **能力命中率 ≈ 1/24**（且是确定性的：同 seed 复现）。

### 4. 结论（B 的目标用"多种子"这条路走不通）
- "先做稳再换"**无法靠堆种子达成**（1/24 的产率，且 3/3 逃逸者都没学会）；
- 要稳健，必须动**选择哲学**：探索期允许"变差仍能推进"（例如按**新颖度**入档/保留 explorer 分支，
  或探索期把 `better` 判据从形状分换成行为新颖度）—— 这是独立课题、风险更高；
- 现有可上线的**唯一确定性样本**：`v7r2-35`（= `v7force2-35` 同配置复现）——
  标准 40.2%（现役 44.0%）· 环墙 42.5%（现役 26.0%）· D 100%（穿透 344）· E 0% ✓ · **F 11% ✗** · 自然小雷 25 / 可归因作废 25。

### 5. 待用户裁定（三选一）
(a) **换 `v7r2-35`**（`--force` 过 F）：用 −3.8pt 标准分换"会打断环"+环墙 +16.5pt；
(b) **立独立课题**：改选择哲学（新颖度入档/探索期换判据）后再重训；
(c) **维持现役**，把本课题归档为"能力可学、当前搜索留不住"的研究结论（七次尝试谱系已完整入库）。

---

## v1.5.41 — "打断环"**配方确认但很脆弱**（窗口 20/40/80 对照）+ 七次尝试谱系

### 1. 成功配方（首次真正学会"打断环"，v1.5.40 实测）
三条**缺一不可**：
1. **奖励可归因**：`setVoid` 带 `byPid`（v1.5.37）—— 否则"我打断"与"别人打断"同分（反例 A=1.5 / B=0.5）；
2. **开环判据 = 动作事件**（该座真的施放了聚能环，含首次）—— 旧 `delta >= 2` 漏首次、还把避雷针 +4 ジ 误判；
3. **退火强迫**：格内 ε=1.0 只在前 **40** 代生效 ⇒ 产物来自**无强迫的后段**，验收才有意义。
   - 验收（复核 §5-5 要求，非恒真）：**关掉强迫后的自然小雷 > 0** 且 **`voided` 且 `byPid === 我` > 0**。

### 2. 窗口对照（每臂 6 seed，池 D）
| 窗口 | 零提升检测 | 自然小雷 | 判决 |
|---|---|---|---|
| 前 **20** 代 | **0/6 有提升** | 全 0 | 作废（冻死更彻底）|
| 前 **40** 代 | 1/6 有提升 | **seed 35: 25 / 由我作废 25** | ✅ 唯一成功 |
| 前 **80** 代 | 1/6 有提升 | 全 0 | 逃过冻结的也没学会 |
⇒ **窗口很窄、可复现率低（1/6）**：不是"配方不对"，而是**提升闸门会把探索期的个体整个拒掉**。

### 3. 脆弱的机制（与 v7both/v7soft 的"奖励冻死"同源）
强迫出来的小雷降低了成员的**形状分/体检指标** ⇒ 提升被拒 ⇒ 冠军冻在热启动种子 ⇒ 整臂零提升。
⇒ 结构性修法（下一步）：**让提升闸门不惩罚"探索期"**（例如只按**强迫窗口之后**的世代判定健康/择优，
或允许冠军来自后段的世代），而不是继续调 ε/窗口。

### 4. 已知最佳候选 `v7force2-35`（**尚未上线**，待更多候选对比）
标准考卷 40.2%（现役 44.0%）· 环墙 **42.5%**（现役 26.0%）· D 反弹墙 100%（穿透 344）·
E 无威胁摆架势 **0% ✓** · F 活跃场进攻 **11% ✗** · G 5.11（8 种）· 平局 0% · 自然小雷 25 / 可归因作废 25。
⇒ 用户选 **(B) 先做稳再换**：先凑出"标准不掉 + 会打断环"的候选，再动冠军。

---

## v1.5.39 — 定向强制探索：**两臂都因"算术陷阱/训练冻结"作废**（结论：探索问题仍未测到）+ 结构障碍入库

### 1. 实现（只影响"有滚环者且我付得起小雷"那一格）
- `ringForceTarget(state,pid,legal)`：仅当**有人 `ringStreak ≥ 1` 且我付得起小雷**时返回该目标，否则 `-1`；
- 包装层：**只在概率门内**改动作，其余一律**原样返回学习到的动作**（**刻意不复用教师槽做常开 ε** —— 那会用 heavyfire 扰动全局）；
- env 通道：`EPIRUS_RING_FORCE_EPS` → 主线程 + **worker 独立进程**都接；并加**审计轨迹**
  `[ringforce] worker eps=... (env=...)` —— 实测打印 `eps=1 (env=1)` ✓（**通道无误**）；
- 守门 **D47**（7 条：目标格判定四例 + 必须带概率门 + 非目标格必须原样交回 + worker 必须能走 env）⇒ `np-test 95/95`。

### 2. ⚠️ 两臂都**不能作为证据**（各自的原因都实测钉死了）
| 臂 | 设定 | 零提升检测 | 判决 |
|---|---|---|---|
| `v7force` | 格内 ε=5% | 2 有提升 / **4 零提升** | **算术陷阱**：目标格只占决策 **0.15%**（复核实测 7/4549），又乘 5% ⇒ 实际强制 ≈ **0.0075%**，整跑几乎没强迫 ⇒ 那 2 个提升的 seed 也等于没测 |
| `v7force1` | 格内 ε=1.0 | **6/6 零提升** | **训练整体冻死**：产物 = 热启动种子 ⇒ 探针量到的是**种子**，`miniTCasts=0` **无效** |

⇒ **"探索是不是瓶颈"这个问题，到现在仍然没有被有效测试过**（不能下任何结论）。

### 3. 新发现的结构性障碍（值得单列）
**在当前"健康门槛 + 提升闸门"下，强制探索会把训练冻死**：被强迫出来的小雷降低了成员的形状分/体检指标
⇒ 提升被拒 ⇒ 冠军冻在种子（与 v7both/v7soft 的"奖励冻死"同源，只是触发者从"奖励"换成"强迫"）。
⇒ 正确的做法应是**强迫也要退火**（早期强制把动作示范出来，之后关掉、交给奖励），
且**验收必须同时看 `zero-promote-check`**（否则会在"量到种子"的情况下误判成"学不会"——本次差点如此）。

### 4. 方法论新增
**"在稀有格子上再乘 ε" = 等于没做**：`P(格子) × ε` 才是实际干预率，稀有格子上 ε 必须取 1 甚至配合退火。
（同族教训：阈值不能定在噪声带里、门槛要按已知好/坏样本标定。）

---

## v1.5.38 — "打断环"第五次尝试：**信号层修好后依然是 0**（干净的否定结果，进入最后一枪）

### 1. 本臂做了什么（把审计指出的三堵墙**全部拆掉**之后的第一臂）
- 奖励**可归因**：`setVoid` 带 `byPid`，反例 A=1.5 / **B=0.5**（此前同分）；
- 开环判据 = **"该座真的施放了聚能环"的动作事件**：反例 **D=0.5**（首次已识别）、**E=0**（避雷针 +4 ジ不再误触发）；
- 教师条件 `ep ≥ 2` → **`ringStreak ≥ 1`**（审计实测触发率 45% → 0.15%）；
- 池 D（**4 席 ringspam**）保证暴露度；示范期 0.8 + 退火环奖励。

### 2. 结果（`v7attr`，6 seed，20 局对 4 席环流）

| 冠军 | 自然小雷次数 | **由我造成的作废** | 对开环者伤害/局 | 反弹墙穿透命中 |
|---|---|---|---|---|
| v7attr-31 … -36（全部）| **0** | **0** | 2.00~2.45 | **339~372** ✓ |

- **验收两条（自然小雷 > 0、`byPid===我` 的作废 > 0）全部不达标**；破墙线完好（说明本臂没"换偏"）。
- 探针同步增强：`ringWallProbe` 新增 **`voidedByMe`**（由我造成的作废数）—— 旧工具只数 `voided` 总数（含别人造成的），
  在 §5-1 修好之前**根本量不出归因**。

### 3. 结论（五次尝试的完整谱系）
| 手段 | 自然小雷 |
|---|---|
| 奖励常开 / 退火 / 密集 | 0 |
| 池子（1 席 / 4 席 ringspam）| 0 |
| 课程示范（0.5 / 0.8）| 1 → 0 |
| 暴露度 + 示范 + 接力 | 0 |
| **再加上"可归因 + 判据含首次 + 教师条件正确"** | **0** |

⇒ **信号层正确是必要条件、但不足够**：在奖励已经"该给的都给对"的情况下，这个动作**依然没有被采样到**。
⇒ 剩下的只有两条：**① 探索**（训练期对"有滚环者且我付得起小雷"这一格做 ε-强迫，**但验收必须在关掉强迫后量自然行为**）；
**② 表达力**（按审计 §5-4：每对手槽位加 1 维 `ringStreak/3`，**+5 维**，不新造概念）。
顺序：先 ① （便宜、且是审计认可的最后一枪），不行再 ②。

---

## v1.5.37 — 按第三方第三轮复核修"可归因"与"全息幻影候选"（**都不动参数量、不升 PACK_VERSION**）

> 依据：`docs/AUDIT-RESPONSE-v1.5.35.md` §5-1/2/3（打断环失败的硬障碍）与 §2-1（全息修复没生效且前提错）。

### 1. 打断环：奖励终于**可归因**（复核 §5-1 的三个硬障碍全修）
| 障碍（复核实测） | 修法 | 验证 |
|---|---|---|
| `voided` 事件**不记施法者**（`by` 是原因字符串）⇒ "我打断"与"别人打断、我恰好放过小雷"**同分** | `setVoid(state, pid, by, **byPid**)` 加第 4 参；小雷/大雷作废目标的调用点补施法者 | 反例 **A=1.5 / B=0.5**（此前 1.5 与 1.5）|
| 教师条件 `对手 ep ≥ 2` 在真实自对局里 **45%** 的决策都成立，而"有人真在滚环"只有 **0.15%** ⇒ 教的是"见人兜里有 ジ 就砸小雷" | 教师条件改 **`ringStreak ≥ 1`** | D43 新增"只是兜里有ジ ⇒ 不得砸小雷"的反例 |
| 开环判据 `delta ≥ 2` **漏掉环的第一次**（delta 是 1 然后 2）⇒ 越早打断越没分；且把"避雷针 +4 ジ"误当开环 | 判据改为**"该座真的施放了聚能环"的动作事件** | 反例 **D=0.5（首次已识别）**、**E=0（非环不触发）** |

- 守门 **D39/D43 按新事实重写**（D39 直接用复核给的 A~E 五反例）。
- **验收标准同步修正**（复核 §5-5）：原先 `miniTCasts ≥ 5` 是**恒真式**（自己加 ε-强迫必然满足）⇒ 改为
  **关掉强迫后的自然小雷数 > 0** 且 **`voided` 且 `byPid === 我` 的次数 > 0**。

### 2. 全息屏障：删掉**幻影"自己"候选**（复核 §2-1）
- 复核证明 v1.5.34 那条修复**没生效且前提错**：`holoShieldFrom` 明写"自己给自己套不算"（**这张卡本来就不能给自己**，
  自保用原型制御），而 `targetOf()` 在**声明阶段**就把 null/自己退成"第一个存活对手" ⇒ 我加的"自己"候选是**幻影**：
  实测被选中 **35/330 次**，每次都把**错误归因**喂回训练。
- 修：**候选只给真实对手**（holo 候选 5 → 4，无 `null`）；**撤销 v1.5.34 的 holo 特例**（恢复引擎原语义）。
- **C 门槛处置（写清理由）**：撤掉旧的"全息 > 2 次/局"普通条件（送盾是这张卡的**定义**、不是 bug），
  只保留**硬门槛"套给别人 > 6 次/局"（不可 `--force`）**，并留档实测分布（v7wall-33 18.7/局 vs v7wall-31 2.0~2.9/局）。
- 守门 **D45 重写**（候选不得含幻影 + 引擎"不能给自己"语义仍在 + v1.5.34 特例必须已撤销）。

### 3. 连带：指纹重记（铁律）
动了 `js/core/resolve.js` ⇒ 规则指纹推进 ⇒ 重测重记两个包（3P 权重不变仍为 `v7wall-31`；2P 重跑考卷）。

---

## v1.5.36 — 新增审计建议的两列 H/I（4 风格同场 / 1 冠军 vs 4 只ジ），**并标记口径待对齐**

## v1.5.35 — 终局平局口径修正（用户裁定）+ battle-test 看门狗止血

## v1.5.34 — 根因修复："全息屏障送盾"不是学坏了，是**目标没进决策**（用户质疑换来的正解）

## v1.5.33 — 训练"静默冻结"检测（`tools/zero-promote-check.mjs`）：零提升不再无声无息

## v1.5.32 — 换冠军 `v7wall-31`（换掉"送盾给对手"的 v7wall-33）+ **新增不可 `--force` 越过的硬门槛**

## v1.5.31 — "打断环"课题：**动作级手段全部打满**（奖励 / 池子 / 课程示范 / 暴露度），结论转"结构路"

## v1.5.30 — 换冠军 `v7wall-33`（修掉 D 列退化）+ 破墙奖励（方案 b）+ **"打断环"三次尝试的实测结论**

## v1.5.28 — 修门禁（方案 1+2）：**反弹墙穿透卡零命中 = 阻断** + 自对局穿透条件降级为提示

## v1.5.27 — **换冠军：v7press-36**（首次"标准持平 + 打环翻倍 + 多样性翻倍"的干净提升）+ F 门槛重标定

## v1.5.26 — **重定义 E 指标**（方案 B）：只把"没有大雷威胁时还一直摆架势"算病

## v1.5.25 — 惩罚被动的窄奖励（方案①）+ **出现首个"两个都要"的候选 v7press-36**

## v1.5.24 — 环奖励**退火**（方案 A）+ 实验结论：打环能力练出来了，但仍是"偏科"

## v1.5.23 — 修"死亡结算跑到伤害之前" + **训练信号：教它惩罚开环的人**（首次真正生效）

## v1.5.22 — 结算事件行的**显示顺序**（用户澄清的"防御优先"真意）+ 技能格按钮回到规则顺序

## v1.5.21 — 页面回合列表/导出三处修正 + 底部技能格恢复"防御优先"

## v1.5.20 — UI 两处修正（连带电伤文案 / 技能格子顺序）+ 指纹重记 + README 瘦身

## v1.5.19 — 一次性加全参数量（v7 特征）：候选 =(技能,目标,珠类型) + 关系块 + 效果快照块

## v1.5.18 — 处置第三方独立复核：A 批（工程/门槛）+ B 批（规则层定稿）

## v1.5.17 — 镜面反射"生效时机"改回优先级口径（用户二次裁定：大雷先结算 ⇒ 复制不会发生）

## v1.5.16 — 镜面反射回归原始规则：**没有"无可复制"**（非伤害类复制到自己身上 + 对 t2 空指）

## v1.5.15 — 修蓄能珠类型的信息泄漏（三处）+ 技能格子显示顺序改成防御类优先

## v1.5.14 — 先手激励实测（能治"集体防御"但代价太大）+ 冠军体检新增 E/F 两列

## v1.5.13 — 修狙击枪"被干扰"判定（多人局 bug，用户对局记录发现）+ 口径澄清（冠军 vs 风格脚本）

## v1.5.12 — 回滚"长程默认开哨声惩罚"（实测是**空操作**，证据：两条臂产出逐字节相同）

## v1.5.11 — 终局收缩参数化（起点/每回合扣血可在页面调）+ 长程训练默认开哨声惩罚 + 换冠军自动跑技能报告

## v1.5.10 — 终局收缩升级为**全局规则**（100 回合后每回合全员 −1 血）+ 页面新增「用内置冠军」按钮

## v1.5.9 — 研究结论入库（哨声惩罚实测 + 防御型池子在 multi 下不划算）+ 对战测试覆盖长程模式

## v1.5.8 — 熵只统计非ジ + 反摆烂哨声惩罚 + 浏览器对战测试 / 冠军体检（上线冠军 long-33 → eco-34）

## v1.5.7 — 线上冠军换成 `long-33`（修 v1.5.4 造成的成绩崩塌）+ 全息屏障在 2P 灰掉 + 规则指纹守门

## v1.5.6 — 经济 shaping 门槛按 (人数,模式) 参数化（用户锚点）+ 恢复技能熵奖励；2P 体检

## v1.5.5 — 全息屏障第二轮修正：**被小雷无效化的时机**（漏读 `voided`）

## v1.5.4 — 规则修正：**全息屏障是"给别人套的盾"**（此前被做成了原型制御的重复品）

## v1.5.3 — 复合适应度（把风格表现写进 fit）；以及那条"已被记过一次"的错再犯一次 + 更正技能报告的假信号

## v1.5.2 — 训练池能放「风格化冠军」当对手；以及"加名字漏一处"的第三次

## v1.5.1 — 技能报告补上「模式」口径；它顺手揭开了挂很久的 P0-d 悬案

## v1.5.0 — 长程（5 血）冠军终于能训练了（接线 + 两道守门 + 一个新坑）

## v1.4.14 — 第三次『加名字漏一处』：对手池补 `ringspam` + 未知名字**开跑前中止**

## v1.4.13 — 三件卫生事项（用户批准）

## v1.4.12 — 多 seed 配对（n=6）：加反弹墙无显著影响 ⇒ 不改默认池、不换冠军

## v1.4.10 — 消融（`--ban`）成为 report 的主口径

## v1.4.11 — 用户纠正：**小雷不是陷阱卡**，它的价值是反制（我错在考卷里没有"环"）

## v1.4.9 — 对手池抽成单一来源（静默漂移坑）+ 反弹墙实验的诚实结论

## v1.4.8 — 终局收缩替代硬截断（用户方案）+ 查清"反弹墙为什么不在训练池里"

## v1.4.7 — 处置第十轮独立复核：确认反弹墙(P0-c) + 更正我两处结论 + 修 3 处小问题

## v1.4.6 — report 自己说清"哪些测不了、该用什么命令" + 一键入口 + REVIEW-5P

## v1.4.5 — 补齐"测不了"的四个口径：消融/开珠/连招/条件注入

## v1.4.4 — 多 seed 配对 A/B（n=4）：12 对手均值 +4.9pt 且方差更小；skill-report 支持多版本对比

## v1.4.3 — 对手池 A/B（从零冷启动）：符号 2/2 支持加对手，但**幅度未定**

## v1.4.2 — 更正 v1.4.1 的归因：38.4% → 21.8% **不是**轮换造成的

## v1.4.0 — 长程模式（5 血）：拉长赛程**只能解冻一部分**被锁死的卡

## v1.3.60 — 条件触发型技能的专用器材 + 用户纠正的符咒机制

## v1.3.59 — skill-report「一堆死技能」主要是**报告自己的测量失败**

## v1.3.58 — 首个 5 人冠军：3 人与 5 人**同时**变强，且座位中性

## v1.3.57 — 座位效应与平局问题：查出**两个方向相反的**成因并都修掉 + 首个 5 人冠军

## v1.3.56 — 查实「浏览器训练不可复现」的真因（**并纠正我自己在 P3.3 写错的归因**）+ 补推送

## v1.3.55 — 搭好 5 人局评测（P3.5「改考卷」），而**它否掉了「等 5 人局」这个方向**

## v1.3.54 — 深经济"货架问题"从猜测变成**有效证据**；(c) 方案落地但门槛默认关闭

## v1.3.53 — 按 Claude 的复核：把能自动化的教训全部改写成会失败的用例

## v1.3.52 — WR_TOL 改显式参数 + repro-check 补 D/E 断言（千问建议）

## v1.3.51 — 修过期日志文本：打印真实 `OUT_PATH`（原硬编码线下路径，**正是它误导我**）

## v1.3.50 — 修 CLI 不可复现的真正根因（千问定位）：**热启动读自己的产物**

## v1.3.49 — train-3p 不可复现：排除三个假设（**仍未找到根因**）

## v1.3.48 — 任务2 完成：训练工具不再覆写线下冠军；任务1（train-3p 可复现）**未解决**

## v1.3.45 — N23 激光眼单发即破整个防御族（用户裁定）

## v1.3.44 — 修 v1.3.43 的回归：`BIG_T_EXEMPT` 扩张撞坏 R23'（spec 36/37 → 37/37）

## v1.3.43 — N22 大雷效果传导：按用户裁定实现（防御族格挡 / 沿作用目标传导 / 反弹只挡不反击）

## v1.3.42 — 修大雷结算：豁免小雷（旧代码对照 + 用户裁定）+ **发现并修正一个空用例**

## v1.3.41 — 集火脚本 `pickFocusFire` + 转移伤害的**决定性**结论：当前 meta 下不可用

## v1.3.40 — 转移伤害诊断：**能赚的充要条件是"单回合承伤 N≥2"**（用户直觉被算术证实）

## v1.3.39 — 新增 `tools/repro-check.mjs`：可复现性看门狗（千问建议）

## v1.3.38 — 噪声下限复测：**千问的预期没有应验**，但这个比较本身有缺陷

## v1.3.37 — worker 分种子：结果与 worker 数无关 + 个体间独立（千问跨机问题 ①②）

## v1.3.36 — 训练可复现：千问定位到两处 Node 作用域的随机/时间源（**我的假阳性也被证实**）

## v1.3.35 — 火弱持续性：训练侧模拟（3×200）→ **无可测效果**，且意外成为噪声下限标定

## v1.3.34 — WR_TOL 多跑对照（各 3 次）：**0.03 胜出，我之前的收紧是错的**

## v1.3.33 — WR_TOL 单变量对照：**单跑一次分不出差异，方差盖过效应**

## v1.3.32 — 补齐 N20 用例（35/35）+ 用新规则重训一次 AI 的实测结论

## v1.3.31 — 修 v1.3.19 引入的双枪 `via` 回归（N20b 关闭）+ N20 全绿

## v1.3.30 — N20 地雷 AoE 重写（用户裁定）+ 3 条 N≥3 spec 用例

## v1.3.29 — 地雷触发改为「近 5 回合架势频率 + 对手当前 ジ 数」（用户方案）

## v1.3.28 — 地雷改用「历史频率」触发（方案 a）；`lastSkill` 滞后问题的确认

## v1.3.27 — P1：深经济对手 `deepsaver`（先写脚本 → 实测它真的会攒）

## v1.3.26 — 冠军换成 div555（v5→v6 无损升级）+ 工具回归确认

## v1.3.25 — P0：存档版本兼容（形状自适应）→ A/B 证据链恢复

## v1.3.24 — 修 v1.3.23 引入的回归：冠军包版本不兼容 + 模仿默认关闭

## 附录 A：当前有效契约 / 常量（改这些必须同步守门）

- **规则指纹**：`53f064c5`。改动 `js/core/rules.js` / `resolve.js` / `state.js` / `play.js` / `js/train/policy.js`
  之后必须**重记两个包的指纹**（`tools/promote-champion.mjs` + `tools/promote-champion2p.mjs`），否则 D16 红。
- **网络形状**：`PACK_VERSION=7`、`FEAT_S=213`、`FEAT_A=22`、`FEAT_N=235`、`paramCount=5689`；
  旧形状 v6=3337 / v5=3313 仍可原生读取（`unpack(json, true)`）。
- **线上 3P 包必须是 v7 容器**（NP-test N19 用 `checkPack` 卡 `o.v === PACK_VERSION`）；
  旧冠军要上线先用 `node tools/upgrade-pack.mjs <旧包> <新包>` 嵌入 v7（旧前缀逐位保留，新块零填）。
- **上线体检门槛**（`tools/promote-champion.mjs`，任一不过即阻断，除非 `--force` 并记入 meta）：
  `伤害/局 ≥ 5` · `平局率 ≤ 20%` · `E 无威胁摆架势 ≤ 60%` · **`F 场A 被集火还手 ≥ 20%`** · `G 有效技能数 ≥ 3` ·
  `反弹墙穿透命中 > 0` · **`座位极差 < 30pt`**（前置：判胜 ≥ 30%，否则记"不可判"而非"均衡"）。
- **训练隔离**：产物写 `EPIRUS_BUNDLE_OUT`、热启动读 `EPIRUS_BUNDLE_IN`（`.training-*`，已 gitignore）；
  训练期间不 bump `index.html` 版本号；`.training.lock` 存在时禁止换包。
- **实验开关**（默认全关 ⇒ 出厂行为一字不变）：`EPIRUS_ALLOW_HEALTH_FAIL=1`（门槛拦下的产物仍落盘）、
  `EPIRUS_DIV_W`（覆盖熵权重）、`EPIRUS_FEAT_MASK`（特征块掩码）、`EPIRUS_IMIT_GENS` / `EPIRUS_IMIT_TEACHER`（课程/模仿）。
- **口径常量**：座位对称性用**百分点**（20 局噪声会淹没 30pt 阈值 ⇒ 至少 100 局）；
  `1st` 走 `rankOf`（并列算第一）⇒ 体检同时报 `严胜`/`并列`，判据用严格口径与结果型指标（伤害/命中）。

## 附录 B：训练问题复盘（2026-09 全期；用户要求）

### B1 流水线 / 工具链
1. **并发训练请求被静默丢弃**（`train-server.mjs` 的 `if (!runningN) { … } return;`）⇒ 12 个 seed 拷到**同一个包**
   （sha1 `dfb399dab9` × 12），所有"多种子对比 / 座位验收"的**有效样本恒为 1**。修于 v1.5.55（明确报错 +
   `start` 事件带 seed + runner「产物串味守卫」），并补修"被健康门槛拦下"与"真串味"的混淆。
2. **健康门槛整臂拦下候选**：v10 臂 6/6、v8fix 臂 11/12 无产物 ⇒ 候选池可得性完全取决于实验开关
   `EPIRUS_ALLOW_HEALTH_FAIL`（v1.5.56 引入，默认关）。
3. **训练覆写线上包**（早期两次把页面搞崩）⇒ v1.5.48 训练隔离（`EPIRUS_BUNDLE_IN/OUT`）。
4. **可复现性脆弱**：槽位随机键一度借用共享 `__rng()` ⇒ 任何"特征调用时机/次数"变化都会扰动随机流，
   引发 D22（蓄能珠泄漏）/D26/D13 三连红 ⇒ v1.5.52 改成**纯函数哈希 + 调用方输入盐**（不消耗任何随机流）。
5. **旧形状包读不出来**：`unpack(json)` 对 v5/v6 返回 `null`（必须 `unpack(json,true)`）；`checkPack` 只认 v7；
   表现为"换回历史冠军时体检直接崩"⇒ v1.5.63 修 + 新增 `tools/upgrade-pack.mjs`。

### B2 门槛 / 指标（错指标把合格产物挡在门外）
1. **F 口径错**（v1.5.61/62）：所谓"活跃场"只有 1 席进攻者、另外 4 席是**冠军自己的副本**（量到的是自对局均衡）；
   分子还是硬编码 6 张卡。门槛 35%→25% 后仍靠 `--force` 越过 ⇒ 换成"场 A 被集火还手率（≥20%）"后
   **首次出现两个候选全过**（eco-34、种子 v1.3.58）。
2. **E 口径错**（v1.5.26）：旧口径把"看到对手攒 5 ジ 才防"的**合理防御**判成病（eco-34 89% ✗）⇒ 新口径 16% ✓；
   **这条错指标是 eco-34 当年被换下的直接原因之一**。
3. **`1st` 把并列算第一**（v1.5.58）：farmerwall `1st=97.5%` 但 `严胜=0.0%`；反弹墙 `100%` 但 `严胜=12.5%`
   （87.5% 并列）⇒ A/D/H/I 四列在"熬到上限全员存活"的场里系统性虚高。
4. **座位偏置此前无门槛**（v1.5.57）：线上包 5 席同策略时极差 **61pt**（long 82pt），而所有旧体检项都看不见。
5. **小样本把噪声当结论**：同一个 eco-34 的 G 在 n5=3.08 / n10=2.99 / n20=4.24（v1.5.18 记录）⇒
   用 n=10 卡 `G<3` 等于用噪声淘汰个体。
6. **反复出现"阈值把所有人挡住"**（F、座位、自对局穿透卡、E）⇒ 规则：**阈值必须能分开"已知好"与"已知坏"**，
   分不开就先怀疑指标，而不是继续下调数字。

### B3 训练动力学 / 配方
1. **探索冻结（零提升）**：多数臂产出与热启动种子**逐字节相同**的包（`v7both`/`v7soft` 6/6 = 种子；逃逸率≈1/6）；
   且**逃过冻结 ≠ 学会**（18 seed 批量：3 个逃逸、0 个学会）。
2. **窄奖励从 gen 0 打开会冻结训练**：环奖励一开就压制演化（成功样本 `v7wall` 靠**退火**才出），
   学到的"被动攒环"个体过不了健康门槛 ⇒ 落盘被拒。
3. **冻结是实验臂特有**：带强迫/环奖励的臂逃逸率≈1/6，而**无强迫的普通臂 6/6 全部有提升**（v1.5.50 实测）。
4. **覆盖坍缩（G<3）**：v9/v11/v12 共 25+ 候选里，"打得凶"的全部只用 2~3 张卡；把覆盖熵权重 `DIV_W` 提到 10×
   **也没有打破**这个互斥（v12 臂 12/12 仍不合格）⇒ 是当前 fitness/探针定义下的**结构性张力**。
5. **偏置是训练造出来的**：热启动种子 v1.3.58 自身均衡（各座 22/16/22/16/24%），训练产物却偏到 39~100pt ⇒
   四条 L7 修复（槽位并列 / 结算起点相位 / 默认靶子 / 多目标第二目标）只能保证"引擎不系统性偏"，
   阻止不了策略自选座位套利 ⇒ 需要**上线体检的座位门槛**（v1.5.57 已加）。
6. **候选来源单一**：几乎所有产物都从同一个热启动种子出发，多样性只靠 seed 抖动 ⇒ 容易收敛到同一吸引子；
   健康门槛又会把"探索期"的候选整批判死（B1-2）。

## 附录 C：方法论规则（踩过的坑 → 规则）
1. **门槛与提交必须用 `&&` 串联**；`;` 接 `echo` 会**掩盖真实退出码**（一次臂失败被伪装成"作业成功"）。
2. **改动指纹集必须重记包指纹**（`promote-champion` + `promote-champion2p`），否则 D16 红。
3. **不要猜字段名**：先打印真实对象再写探针（`e.from` 应为 `source`；事件无 `round` 字段；技能键不是 `GUN/SWORD`）。
4. **阈值必须有判别力**：先用"已知好 vs 已知坏"验证，再定数字（B2-6）。
5. **单位必须钉死**（胜场数差 ≠ 百分点；20 局噪声 > 30pt 阈值 ⇒ 100 局起）。
6. **纯函数优先**：凡是"身份/顺序/随机性"，都不要借用共享随机流（B1-4）；需要不可预测就由**调用方输入**（盐）。
7. **版本号替换必须硬断言**（`assert old in text`）—— 静默 no-op 会让三方版本漂移而门禁仍绿（v1.5.55/56 就发生过）。
8. **修补脚本先写后检**：python 里"先插用法再判是否已导入"会让守卫误判；引号嵌套会 SyntaxError 且**静默不写盘**。
9. **发布作业跑完再跑门禁**：发布中途跑 np-test 会与写包互相污染（本次踩过）。
10. **训练与线上物理隔离**：产物只写 `docs/artifacts/.training-*`；`.training.lock` 存在时禁止换包。

## 附录 D — 熵/温度 T 与"原型制御空白"（v1.5.86 的分析与读数，已折进上面的版本条目）

### D1 用户的疑问：为什么加了熵、胜率不掉，冠军却还是不会用更多技能？
三层机制，都有读数支撑：
1. **熵项不改变"动作质量"，只改分布形状**：采样温度升高时，它仍是在**近似等价值**的候选里换着用；
   在这个对手场（random/balanced/aggro/defend）里很多选择"够用就行" ⇒ 噪声成本小到看不见
   （`tools/probe-temp.mjs`：effSkills 4.28→6.29，1st% 30/27.5/37.5/25/35 无趋势；n=40、±15pt）。
2. **训练侧的 T（`DIV_W`）与"学会一张卡"不是一回事**：它只给**已实现直方图**加分，
   不告诉网络"这张卡为什么值钱" ⇒ 对**需要两步连招**的卡（原型制御/转移伤害）没有信用分配。
3. **在固定对手池里，"窄而深"胜过"宽而广"**：实测 `pickProtoMine`（几乎只用一招）中立场 **45%**、
   对冠军 **52.5%**，而冠军 13 种技能的组合分只有 **40.7%** ⇒ **技能多本身不是强度**；
   宽度只在**遇到没见过的对手**时才值钱 —— 而训练池是固定且有限的那 16 个（见 D2）。
⇒ 结论：多给熵能拿到**可玩性**，拿不到"更强的广度"；要拿到后者必须让**环境**包含需要广度的压力。

### D2 原型制御空白（本轮最有价值的发现）
- 实测（`tools/probe-beadloop.mjs`，105 个决策点，蒙特卡洛 24 次/点）：
  **proto 可负担 54/105（51.4%）、在候选表里 100%、采样选中率 0.0%**；
  transfer 同样（9/105、100%、0.0%）⇒ **不是枚举问题，是价值估计问题**（在候选里、付得起、就是不给分）。
- 原因：池子里**没有原型系对手**（`BOT_PICKS` 里只有 `protowall`/`combocounter`；最强的
  `pickProtoMine`/`pickProtoTransfer` **未注册**）⇒ 冠军从没见过"原型连招被用好" ⇒ 价值头学不到那条回报。
- ⇒ 这正是"让更多技能成为应对环境更强的因素"的正确落点：**不是奖励宽度，而是把宽度所需的压力放进池子**。

### D3 本轮启动的两条臂（后台；验收写在下面）
- A `v7div3`：默认池 + `EPIRUS_DIV_W=0.3`（T 权重 5×）⇒ 量 (A 考卷, G) 的移动；
  验收：**G 上升而 A 不显著下降**。
- B `v7proto`：默认 16 对手 **+ `protowall` + `combocounter`**（proto 系进池）⇒ 验收：
  **proto 使用率 > 0**（学会用它/防它）且 A 考卷不低于线上包（不为多样性牺牲强度）。
- 两臂各 2 seed（71/72）+ 常备 31；沿用隔离约定（不碰线上包与 index.html）。

### D4 用户的产品决定（记录以免我再纠结）
- 线下主要是 **4~7 人**（3 人场都少）；**2P 是最初的简化版**（东西少好做）。
- 一旦"多人场明确优于冻结的 2P 版"得到验证 ⇒ **删掉旧的 2P 模式、统一并入多人场**。
- ⇒ 因此"补 2P 沙盘"从待办里**降级**（不再为 2P 花时间）；上一条 2P 读数继续保持作废状态。

### D5 更正（用户指出，已用数据核实）：全息屏障是**旧产物特征**，不是现役风格
- 用户："全息屏障的问题不是已经修了吗？线上冠军因为是在修之前做的所以很经常用全息屏障，
  新训练的应该不会那么常用。"
- **成立** ✓：`v1.5.74` 的 **F2 两级采样**就是那个修（`v1.5.75` 已记「全息屏障的偏爱消失」），
  而**线上冠军 `v7tgt2-52` 是 v1.5.72 提上来的、早于 F2** ⇒ 它的 4.2% 全息是**历史特征** ✗。
- 实测对照（官方考卷，20 局，同一场地）：

| 包 | 1st | 全息屏障 | 原型制御 | 出手种类 |
|---|---|---|---|---|
| 线上包（F2 之前）| 41.3% | **4.2%** | 0% | 12 |
| `v7f3-91`（F2 之后）| 52.9% | **0.4%** | 0.2% | 12 |
| `v7f3-94`（F2 之后）| 30.7% | **0.0%** | 0.2% | 15 |
- ⇒ 我先前"冠军爱用全息"的说法**只对旧包成立**，不能用来描述现役策略 ✗（已更正）。
- ⚠ 但 **proto 在 F2 之后仍是 0.2%** ⇒ 它**不由** F2 负责，缺的是**池子里的原型系对手**（见 D2）✓。
  这同时说明：不能把"F2 治好了全息"当成"proto 也顺带修好了" ✗。
- 另：`probe-temp.mjs` 那张 T 曲线是在**旧包**上量的 ⇒ 想描述现役形状要在**新产物**上重量（待臂产出）。

### D6 熵项的机制缺陷（本轮最重要的因果发现，臂 A 的第一手证据）
- 臂 A（`EPIRUS_DIV_W=0.3`，即 T×5）跑出的候选**全被健康门禁拦下**，而且真实多样性**更差** ✗：
  `[health] G 有效技能数 1.24 < 3（种类=4）` · `1.90 < 3（种类=3）`；
  对照历史默认臂（`DIV_W=0.06`）的同批拒绝是 **G=2.77 / 2.54 / 2.32** ⇒ **T 调大反而更低**。
- 根因（`js/train/evo.js:750` 的 `coverageEntropy`）：
  `divNorm = H(非ジ直方图) / ln(可负担技能数)` —— **分母是"当时可负担的技能数"** ⇒
  **把菜单变穷（少可负担项）就能把这个比值刷高** ⇒ 目标奖励的是"机会集合变小"，不是"用得更广" ✗。
- 健康门禁用真实实现分布 `G = exp(H)` ⇒ **抓到了这个退化** ✓（这次门禁是对的）。
- ⇒ 这也回答了用户的问题"为什么加熵不掉胜率却学不会更多技能"：
  **不是网络不愿多样，而是这一项可以被"缩小分母"刷分** ✗。
- 修法（待臂结束后实施，改 evo.js ⇒ 零指纹成本）：
  ① 分母改成**固定目标 K**（如 6）⇒ 只有"把直方图铺开到 ~6 种非ジ技能"才能拿满 ⇒ **不可靠变穷刷分**；
  ② `coverageEntropy` **已有** `capDiv` 形参 ⇒ 接线即可（无需改函数体）；
  ③ 之后重跑 T 曲线（`EPIRUS_DIV_W` 0.06 / 0.3 / 0.6）在**新产物**上量 (A, G)。
- ⚠ 同时保留健康门禁：它是这轮唯一挡住退化的东西（`EPIRUS_ALLOW_HEALTH_FAIL` 只用于观察，不用于出厂）。

## 附录 E — 熵项分母修正落地 + 对手注册表修复（v1.5.86 的细节与读数）

### E1 修正一：熵项的固定分母（D6 的落地）
- `js/train/evo.js`：`coverageEntropy(agg.use, agg.aff, DIV_K)`，`let DIV_K = 6`（env `EPIRUS_DIV_K` 可调，
  经既有的 `setEconomyReward({divK})` 由服务端注入 ⇒ 与 `EPIRUS_DIV_W` 同一模式）。
- 语义变化：`divNorm = H(非ジ直方图) / ln(6)` ⇒ **只有把直方图铺开到 ~6 种非ジ技能才能拿满**，
  不再能靠"把菜单变穷（少可负担项）"刷分。
- ⚠ 踩坑记录（值得记）：我第一版把 `Number(process.env.EPIRUS_DIV_K || 6)` 直接写在 **evo.js 的模块作用域** ⇒
  **加载即抛** ✗ —— 因为 `evo.js` 在 np-test / audit-lib 的 **vm 沙箱**里加载，沙箱**没有 `process`**。
  正确做法：evo.js 只留常量 + setter，env 读取放 `server/train-server.mjs`（沿用 `EPIRUS_DIV_W` 的既有模式）。

### E2 修正二：最强脚本没进池子（"加名字漏一处"第 3+ 次）
- 事故：臂 B 用 `RING2_POOL='…,protomine,prototransfer'` 启动，**0.4 秒即失败** ✗：
  `未知/缺失对手: combocounter`，并列出服务器真正认识的名单 ⇒ 服务器用的是 **`server/opp-pool.mjs`** 的注册表，
  而它**没有** `protomine`/`prototransfer`（只有 `protowall`），尽管 `train-server.mjs` 的 `BOT_FN` 里**早有**这两个映射 ✗。
- `server/opp-pool.mjs` 自己就写着「**这是第三次"加名字漏一处"**」⇒ 所以这次顺带加了一道守门
  **D72**：解析 `opp-pool` 注册表与 runner 的 `POOL_*` 名单，断言 **runner 池子里的名字都必须在册**
  （正是本轮踩到的形态）。
- ⚠ 诚实记录：D72 的第一版我写得太严（要求 `opp-pool` 每个名字都在 `train-server` 的 `BOT_FN` 里）⇒ 它**红了** ✓，
  但那是**我的断言错**（`antidef/farmer/deepsaver/ringspam/targeter/snipespam` 走的是另一张表）⇒ 已收窄到真实形态。

### E3 臂 A 的最终读数（T×5，零产物）
`v7div3`（`EPIRUS_DIV_W=0.3`）：seed 31/71/72 **全部被健康门禁拦下**（G=1.24 / 1.90 < 3）⇒
**整臂 0 产物** ✓ —— 这是 D6 的独立证据：把 T 调大在当前实现下**连候选都交不出来**，
原因是分母可被"变穷"刷分，而不是"网络不愿多样"。

### E4 本轮新开的两条臂（后台，各 2 seed + 常备 31）
- **P `v7proto2`**：默认 16 对手 **+ `protomine` + `prototransfer`**（两者已在册 ✓）⇒
  验收：产物的 **proto 使用率 > 0**（学会用它/防它）且 A 考卷不低于线上包。
- **K `v7divK`**：`EPIRUS_DIV_W=0.3` **+ 固定分母 `DIV_K=6`** ⇒ 验收：**至少产出健康候选**（G ≥ 3），
  再看 (A, G) —— 用来回答"修好分母后，调 T 是否真的买到多样性而不掉强度"。

## 附录 F — 固定分母的**因果验证**：同样 T×5，旧分母 0 产物、新分母 2/2 健康产物

### F1 对比（同一 `EPIRUS_DIV_W=0.3`，唯一差别是分母）
| 臂 | 分母 | 产物 | 健康门禁 |
|---|---|---|---|
| A `v7div3` | 旧（=可负担技能数）| **0 / 3 seed** ✗ | 全被拦：G=1.24 / 1.90 < 3 |
| K `v7divK` | **新（固定 6）** | **2 / 2 seed** ✓（`v7divK-81` `5dd88649a1aa`、`-82` `a4aa28ca0ad4`）| 通过：G=3.72 / 4.42 ≥ 3 |

### F2 读数（官方考卷 20 局，同一场地；G 来自自对局 20 局）
| 包 | A 考卷 1st | G（有效技能数）| 出手种类 | 伤害/局 | 全息屏障 |
|---|---|---|---|---|---|
| 线上包（F2 之后前一批）| 41.3% | 4.22 | 9 | 13.8 | 4.2% |
| `v7divK-81` | **50.4%** | 3.72 | 7 | 14.3 | — |
| `v7divK-82` | **42.7%** | **4.42** | **16** | 14.3 | 0.0% |
- ⇒ **T×5 在分母修好后：强度不降（42.7~50.4% vs 41.3%）且多样性不降反升（G 4.42 vs 4.22、种类 16 vs 12）** ✓
  —— 这是"熵权重要调对了才有用"的第一个正面证据 ✓（用户最初的问题"为什么加了熵却学不到更多技能"由此闭环）。
- ⚠ 口径：A 考卷 20 局（±7~10pt）⇒ **"不低于"有支撑，"更强"未证明** ✗；且只 2 个 seed ✓。
- ⚠ `A=50.4%` 这一档要警惕"考卷拟合"（对比 v7f3-91：A 52.9% 却被一行脚本打 78~82% ✗）⇒
  **必须过 G4/G5 与可行性门槛才算数**（已在跑 ✓）。

### F3 臂 P（池子加 `protomine`/`prototransfer`）被健康门禁拦下
- 三轮体检：`G=2.12（种类=3）` · `1.88（种类=3）` · `1.95（种类=5）` ⇒ 全部 < 3 ⇒ **0 产物** ✗。
- 与历史默认臂被拒的 2.32~2.77 相比**更低** ⇒ 一个**假设**（未证实）：把**很强的对手**放进池子，
  可能把种群推向更窄的"最佳应对" ⇒ **降低**多样性 ✓ —— 若成立，它对"要多样性"的目标是**反效果** ✓。
- ⇒ proto 使用率的问题**仍未答**（产物没写盘）⇒ 下一步用 `EPIRUS_ALLOW_HEALTH_FAIL=1` 放行**只为观察行为**
  （出厂口径不放行 ✓）。

### F4 下一步（顺序）
1. 跑 `gate-drafts`（G4 克制表 / G5 破防反射 / G6 靶向）在 `v7divK-81/82/31` 上 ⇒ 决定"能不能上线"✓；
2. 若 K 候选过门 ⇒ 与线上包做**同场对比 + 换包流程**（`promote-champion --dry` 先看五道门槛 ✓）；
3. P 的观察跑（allow-health-fail）⇒ 看 proto 使用率有没有从 0.2% 起来 ✓。

## 附录 G — 固定分母修好后的候选：**过全部行为门 + 多样性更高 + 强度不掉**

### G1 三个 `v7divK` 候选 vs 线上包（考卷 20 局；G/座位来自自对局 20 局；G4/G5 各 60 局）
| 候选 | A 考卷 1st | **G（多样性）** | 出手种类（考卷）| **G4**（克制表）| **G5**（破防反射）| 座位极差 |
|---|---|---|---|---|---|---|
| 线上包 `v7tgt2-52` | 41.3% | 4.22 | 12 | PASS | PASS | 35.3pt |
| **`v7divK-82`** | **42.7%** | **4.42** ✓ | **16** ✓ | **PASS** ✓ | **PASS** ✓ | **29.4pt** ✓ |
| `v7divK-81` | 50.4% | 3.72 | 7 | PASS | **FAIL**（防席 33%/28%）✗ | — |
| `v7divK-31` | **51.0%** | 3.02 | 6 | PASS | PASS | 20.0pt |
- ⇒ **`v7divK-82`：两个阻断门双模式全过、多样性比线上包更高（G 4.42 vs 4.22、种类 16 vs 12）、
  A 考卷不低于（42.7% vs 41.3%，n=20 ±7~10pt）、座位极差更小（29.4pt vs 35.3pt）** ✓ ——
  这正是用户要的"为可玩性要多样性、但别掉强度"，而且**它是靠修分母拿到的**。
- `-81` 再次演示"考卷高分 ≠ 能上线"（A=50.4% 但 G5 红 ✗），门禁起作用 ✓。
- `-31` A=51.0% 但 G=3.02（贴着健康门下限）⇒ 多样性收益小 ✗。

### G2 因果链（同一 T×5，唯一差别是分母）
旧分母（=可负担技能数）⇒ **0/3 seed 产物**（G 1.24/1.90 被拦）✗；
新分母（固定 6）⇒ **3/3 seed 产物**（G 3.02~4.42 全过）✓ ⇒ **分母就是那个病因** ✓。

### G3 下一步（决定权在用户）
1. 若要上线：走 `promote-champion`（含五道 feasibility 门槛）→ 换 `js/bundled-champion-3p.js` → bump v1.5.86；
   ⚠ 换包是**用户决定**（会改变他实际在玩的那个对手）。
2. 臂 P（池子加 proto）的"proto 使用率"问题仍开放 ⇒ 需 `EPIRUS_ALLOW_HEALTH_FAIL=1` 观察跑（出厂不放行）。
3. 若要继续调 T：现在可以安全地在 `EPIRUS_DIV_K=6` 下扫 `EPIRUS_DIV_W`（0.06/0.3/0.6）——
   分母修好后，高 T 臂不再必然 0 产物 ✓。

## 附录 H — v1.5.89：经济/熵奖励 env 的**静默半开**已修（第八轮复核 §2 的落地）+ 两条被作废的臂拿到**真读数**

> 背景：`docs/AUDIT-RESPONSE-v1.5.85.md` §2 判定"四个开关到不了算 fitness 的 16 个 worker"。
> 按本仓库纪律**先反证、再改**。版本号**未动**（收尾时臂在跑，沿用既有约定），折叠进正式条目留给下次。

### H1 反证（修前）——复核是对的
- `git show HEAD:server/train-worker.mjs`：`EPIRUS_DIV_*` 出现 **0 次**（`EPIRUS_ECO_TARGET/CAP/DIVW` 各 2 次）
  ⇒ 这四个开关在 worker 里**从未被读**。
- 修前实跑 `EPIRUS_DIV_W=0.5`：服务端 `[econ] 生效值` 显示 0.5，而 worker 侧**没有任何回执** ⇒
  "开关看着接上了、实际作用在没跑的那条路径上"，与 v1.5.18 的 `EPIRUS_FIGHT_*`、v1.5.79 的 `TGT_W` 同族。
- ⇒ 附录 D/E/F/G 与 `HANDOFF §5.4` 里 **"臂 K = `DIV_W=0.3`"、"臂甲 = 强迫 60 代 + 破墙过滤"的归因作废**。
  **仍然成立**的是 `DIV_K=6` 固定分母那条因果 —— 那是**改代码默认值**，代码改动确实进 worker（附录 F1 的对比有效）。

### H2 修法：单一来源（不是"在 worker 里再补一遍"）
- 新增 `server/econ-env.mjs`（`readEconEnv / hasEconOverride / econEcho` + `ECON_ENV_KEYS / ECON_REWARD_KEYS`），
  与 `server/fight-env.mjs` 同一套规矩、同一族守门（D24 ↔ D77）。
- `server/train-server.mjs`：删掉原先 :337-340 的四行内联与 :387-389 的重复解析，改走 `readEconEnv`（一次解析，
  同一份喂 `ecoOverride` meta）；`server/train-worker.mjs`：改走同一份 + 新增
  **`[econ] worker 生效值: …` 回执**（照 `[ringforce] worker eps=` 的样子 —— 本轮若早有这一行，第三方复核就不必靠代码考古）。
- 语义：`EPIRUS_WALL_FILTER` 仍只认 `'1'`；旧名 `EPIRUS_ECO_DIVW` 仍生效（新名优先）；
  **新增**"空串＝未设"（旧代码里 `EPIRUS_DIV_W=` 会被静默当成 `divW=0`，一个手滑的启动命令就能改口径）。
- 守门 **D77**：静态（这些 env 名只允许出现在 `econ-env.mjs`，用**整名**匹配以免误伤 `EPIRUS_WALL_MS`）
  + 两端都导入 + worker 有回执 + `readEconEnv` 返回的每个键 `setEconomyReward` 都必须接受 + 真模块行为自检。
  **D76** 的接线断言改指 `econ-env.mjs`：它原先断言的是"train-server 里有这个 env 名"——
  而**名字存在 ≠ 开关进得了算 fitness 的进程**，这正是本轮的教训。
- 门禁：np-test **125/125**（原 124 + D77）· spec **36/36** · 规则指纹 **8dda2677 未变** · ui-probe **10/10** ·
  `js/` **零改动**（免重记两个包指纹）· 线上包全程 `e4b4f780dda2`（runner 收尾自检 + 人工复核）。

### H3 修后反证（判据 = 复核 §2-2"打印 0.5 ⇒ env 到了"）
- `EPIRUS_DIV_W=0.5` 起真服务跑 1 代：**16 个 worker 各打一行 `divW: 0.5`** ✓（修前是"读都没读"）。
- 真臂里同样：`ring2-server-divK2.log` = `divW 0.3 / divK 6`（48 行回执）；
  `ring2-server-forceF.log` = `divForceGens 60 / wallFilter true`（32 行回执）⇒ 三条开关在真臂里都已生效 ✓。

### H4 复跑真读数（`tools/ring2-run.mjs`，默认 16 对手池，250 代，考卷 40 局）
| 臂 | env（**真生效**）| seed | 产物 | 自评 | A 考卷 | G | 反弹墙伤害/局 | 座位极差 | 花珠率 |
|---|---|---|---|---|---|---|---|---|---|
| K2 `v7divK2` | `DIV_W=0.3 DIV_K=6` | 31 | **无**（健康门禁）| — | — | — | — | — | — |
| K2 | 同上 | 81 | `v7divK2-81.bak` `84375e87b4d8` | 0.3981 | **43.0%** | **5.28** | **8.95**（零伤害局 0%）| 28pt | 8% ✓闭环 |
| K2 | 同上 | 82 | `v7divK2-82.bak` `ed3fd324cbe0` | 0.4314 | 39.1% | 3.70 | **0.00**（零伤害局 100%）| 8pt | 0% 未闭环 |
| 甲 `v7forceF` | `DIV_W=0.06 FORCE=60 WALL_FILTER=1` | 31 | **无**（健康门禁）| — | — | **4.42**（种类 10）| — | — | — |
| 对照 p12 | — | 31 | — | — | 33.6% | — | — | — | — |

判读（与附录 F/G 的旧读数对比）：
1. **"T×5 必然 0 产物"不成立**：真 `DIV_W=0.3` 下 **2/3 seed 出产物**，且 seed 81 的 **G=5.28 > 线上包 4.22**、
   **反弹墙 8.95/局（零伤害局 0%）** —— 附录 G 当初说"多样性更高、墙更差"其实是在 `DIV_W=0.06` 下量的；
   **现在才是真的 T×5**，而它这两项**都**更好 ⇒ 用户要的"为可玩性要多样性"这条线**值得继续**。
2. **但两个产物都过不了行为门**：G4（长程/多席）与 G5（对"只防御"清场）双 FAIL ⇒ `promote-champion --dry` **rc=6**，
   **未换包**、未 `--force`；82 另有可行性硬伤（反弹墙 0.00/局、场B 清场 0.00）。
3. **甲真开之后仍 0 产物，但被拦的理由变了**：旧臂是 `G=1.06~2.71`（宽度没起来），现在是
   **`G=4.42 种类=10`，撞的是"自对局回合 40.3 > 40"** ⇒ 强迫确实把宽度走出来了，代价是回合变长。

### H5 顺手发现的门缺陷（**未修，需裁定**）
`tools/promote-champion.mjs:230` 把 gate-drafts 输出里**任何** `G4[`/`G5[` FAIL 都当阻断项，而
`tools/gate-drafts.mjs` 的输出**同时含"线上包"这一对照行**（它读 `js/bundled-champion-3p.js`，与候选无关）。
上一会话按复核 §6 往 `COUNTERS` 补了"只枪(1ジ压制·打最肥)"那行（**未提交**）之后：
- `G4[线上包/long]` 变成 **FAIL**（长程被最便宜的一张卡打穿 = 复核 §6 的结论）⇒
  **只要在位的线上包是红的，任何候选都会被这条对照行连坐**（哪怕候选自己 G4 全过）⇒ 换包实际只能靠 `--force`；
- 且 FAIL 行括号里打的是**基线**（"基线 23%/60局"），读起来像"这条过了" —— 实测时连我都先被它误导一次 ⇒
  应改成打印**最克那一格的值**。
- 建议（门禁口径，属产品决定，未擅改）：① 对照行**只记录不阻断**（阻断只看候选自己的行）；或
  ② 保留阻断，但把它当成"候选必须先把 只枪 那格打回去"的硬要求。

### H6 工作区里另有**上一会话遗留、未提交**的两处（非本轮改动）
- `tools/gate-drafts.mjs`：按复核 §6 补的"只枪"克制行（M，未提交）；
- `docs/AUDIT-RESPONSE-v1.5.85.md`：第八轮复核全文（??，未跟踪）。
