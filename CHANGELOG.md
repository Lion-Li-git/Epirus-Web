## v1.5.68 — 让演化**看得见偏置**：阈值式座位惩罚（用已经在打的 mirror 局，零额外成本）

### 病（v15 臂逐 seed 裁定给出的精确定位）
`v15` 6 个 seed **0/6 五道全过**，但两个 seed **各只差一项**：
`v15-123` 只差座位（33.9pt vs <30，其余全过）· `v15-124` 只差 G（2.29 vs ≥3，其余全过）
⇒ 瓶颈不是"某项能力不会"，而是**演化看不到自己偏座**：训练的对局永远是"受训者 vs 脚本机器人"，
**从不评估"5 席同策略"这一配置** ⇒ 固定目标偏好的策略在机器人池上能赢、在 5 席测量里却某座通吃。

### 修（只改训练侧；引擎与 `PACK_VERSION` 不动）
1. `mirrorHealth`（**已经在跑 5 席同策略**）顺便统计各座胜场 ⇒ 回报 `seatWins / seatDecisive / seatSpread`（**零额外成本**）。
2. `scoreMemberN` 里**已经在打的 mirror 局**顺便统计各座胜场 ⇒ 加一项**阈值式座位惩罚**进 `fit`：
   `极差 > 35pt 才扣分`（`seatPen = 0.5 × (极差−35)/100`），低于阈值**一分不扣**。
   ⇒ 不改变"好个体之间的相对次序"，只在偏置真的大时把它压下去。
   ⇒ 这是**约束处理**（只罚违规、不奖励任何东西），不是又一项目标权重 ——
   加权奖励已被两次实验证伪（先手激励 −14pt、破墙奖励让混合场 40%→17%）。
3. 成员评分回报 `seatPen / seatSpreadMirror / mirrorDecisive`（供审计追溯）。

### 守门 **D59**
源码契约（常量存在、`- seatPen` 真进 fit、回报字段存在、`mirrorHealth` 回报各座胜场）+ 行为断言
（线上包已知均衡：`mirrorHealth(20)` 的 5 席极差必须 <40pt；包不可读时跳过并打印，与 D16/D58 同一守卫）。

### 门禁
`np-test 107/107`（`evo.js`/`ui.js` 不在指纹集 ⇒ **无需重记指纹**）。

### 待验证（v16 臂）
判据：是否首次出现 `meta.feasibility.ok === true` 的 seed，且 **A严胜 / H混合场 / 反弹墙伤害不回退**。

---

## v1.5.67 — 五道上线门槛做成"**决定产物的那一行**"的可行性判定（复核 §4 的 ④）

### 病（本轮实验证）
训练的选择目标是"池上胜率 + 奖励"，而上线按**另外五道门槛**筛 ⇒ 五批 30+ 候选（v9/v11/v12/v13/v14）
全是"某一项精彩、其余崩掉"；其中最致命的是训练**从不评估"5 席同策略"这一配置**（它的对局永远是
受训者 vs 脚本机器人）⇒ 任何"固定目标偏好"的策略在机器人池上能赢、在 5 席测量里却表现为某座通吃，
而训练**永远惩罚不到它**。

### 修（只改训练侧；引擎与 `PACK_VERSION` 不动）
在 `server/train-server.mjs` 的**唯一产物决定点**（落盘前）跑一次五道检查，结论写进 meta：
`feasibility = { ok, fails[], seatSpread, seatDecisive, G, Gkeys, wallDmg, fieldA, fieldBClears }`，
并打印 `[feasible]` 行 + SSE 事件。探针复用 `tools/audit-lib.mjs`（**单一真源**：座位 `seatSymmetry` /
G `mirrorHealth(40)` / 反弹墙 `reflectWall(20, long)` / 场A 与场B `aggressionProfile(20)`）。
- 阈值：座位极差 <30pt（前置判胜≥30%）· G ≥3 · 反弹墙伤害 >0.5/局 · 场A 还手 ≥20% · 场B 清场 ≥0.3/局。
- **默认不阻断落盘**（`EPIRUS_FEASIBILITY=0` 可关）：避免重演 v1.5.56 的"整臂拦下 ⇒ 实验没有样本"；
  **出厂门槛仍由 `tools/promote-champion.mjs` 强制** —— 那里才是"能不能上线"的判定。

### 实测样例（feastest，30 代）
```
[feasible] ✗ 座位极差 37pt ≥30；G 1.99 < 3（座位 36.8pt · G 1.99（3种）· 墙 17.15/局 · 场A 33% · 场B 4/局）
[feasible] ✗ 座位极差 40pt ≥30；G 2.38 < 3；场B 清场 0.05 < 0.3/局（… 墙 18.1/局 · 场A 28% · 场B 0.05/局）
```

### 门禁
`np-test 106/106`。

---

## v1.5.66 — L7 第七处（**本轮新发现**）：候选**目标枚举顺序**也是身份通道 —— 这才是"引擎无偏、冠军仍偏"的成因

### 1. 为什么"环境和特征都对称了，新训的冠军还是偏 30~100pt"
- **引擎确实无偏**（镜像实验，60 局）：脚本"每回合打**索引最小**的存活对手" ⇒ **P4 通吃 60/60**；
  镜像规则"打**索引最大**" ⇒ **P0 通吃 60/60**；全员只ジ ⇒ 全平。⇒ **镜像规则给出镜像结果**，引擎/相位/盐都没问题。
- **但"无偏的环境"不等于"无偏的策略"**：任何"按固定 pid 顺序选目标"的策略**本身就是偏置规则**（见上）。
- **v7 候选口径恰好会系统性产生这种规则**：`js/train/policy.js` 的 `candidatesFor` 按 `S.opponentsOf`（**pid 升序**，
  `state.js:73`）枚举目标，而 `forwardCands` 的 argmax **并列取第一个下标** ⇒ 网络对目标"无所谓"时，
  实际行为退化成"**打最小 pid 的那个**" ⇒ 网络再围着这个事实学习 ⇒ 偏置自我强化、
  且**对特征完全不可见**（它在动作选择里，不在状态编码里）。
- **证据吻合**：偏置候选的胜局是「0 号座全程满血、其余四席全灭」（`v13-106`/`v13-103` = P0 58~60/60）——
  正是"打最大索引"那条规则的镜像形态。
- **为什么老种子 v1.3.58 均衡**：它是 **v6 包**，走**旧口径**（键 + `pickTargetN` 启发式选目标），
  **不经过候选枚举** ⇒ 没有这条通道 ✓ ⇒ 这也解释了"新训的都偏、老种子不偏"。
  ⇒ 结论：该通道是 **v1.5.19 引入 v7 候选口径时新增的**，与特征/引擎无关。

### 2. 修（只动目标枚举顺序，不碰引擎规则、不升 `PACK_VERSION`）
- 新增 `poolOrder(state, pid, pool)`：按**每局盐**哈希排序目标池（纯函数、决策内一致、**不消耗任何随机流**；
  **盐 0 保持旧序** ⇒ 历史基线不变）。三处枚举（单目标 / 双目标有序对 / `lockTarget` 备选）全部改走它。
  实测目标首项顺序：`salt=0 → 1,2,3,4`；`12345 → 4,1,2,3`；`98765 → 2,4,1,3` ✓
- **页面也每局带盐**（`js/ui/ui.js` 建局处）：此前只有页面缺省 0 ⇒ 产品仍走确定性顺序（训练/评测早就带盐）。

### 3. 守门 **D58**（L7 第七处的可反证形式）
- 源码契约：`poolOrder` 存在且三处枚举走它；页面每局带盐；
- **镜像对称**：脚本"打最小索引"必须让最高 pid 通吃、镜像规则必须让最低 pid 通吃（10 局，最强形式）；
- **5 席不得通吃**：用**仓库内线上包**跑 60 局 5 席同一策略，最高座不得超过 55% 的分胜负局
  （修前形态 `v13-106` = 60/60 会直接红；现线上包 13/9/13/9/8 通过）。
  （训练中包被临时改写时跳过并打印，避免误报——与 D16 同一守卫。）

### 4. 门禁
`np-test 106/106`（唯一需重记的是指纹）；指纹 `57bac9a5 → d42ec70a`。

---

## v1.5.65 — 场 B 的两块前置：**训练暴露度**（①）+ **multi 终局收缩与全灭判胜**（②）

起因（第五轮复核 §4）：场 B（惩罚纯攒钱）从 v1.5.17 到 v1.5.63 一次都没练好，因为**训练分布里根本没有这个局面**。

### ① 训练暴露度：把"4 席全被动"注入评估分布
- 诊断（复核给出、我已核实）：`js/train/evo.js:814` 的对手是按 `opps[oi % opps.length]` **连续取不同项**，
  而池子 17 项里被动攒钱型只有 `farmer` / `deepsaver` 两项 ⇒ **"4 席全被动"概率为 0（结构上不可能）**
  ⇒ 过去所有"惩罚攒钱"类奖励（惩罚被动 / 先手激励 / 密集分 / `EPIRUS_DIV_W`）都在**样本量为 0 的分布**上优化。
- 修：新增 `PASSIVE_FIELD`（环境旋钮 `EPIRUS_PASSIVE_FIELD`，**默认 1/8 局**）与 `passiveFieldAt(g)`；
  命中局的 **4 个对手席全部**为被动型（`farmer`/`deepsaver` 轮换）。只改"评估哪些局面"，
  **不动参数量、不升 `PACK_VERSION`**。

### ② multi 的平局兜底：收缩必须落进回合上限内 + 全灭按伤害判胜
- 病（实测坐实）：`MODES.multi` 不设 `suddenDeath` ⇒ 走全局 `SUDDEN_DEATH = 100`，而上限 `MAX_ROUNDS = 60`
  ⇒ **收缩在多人局永不触发** ⇒ "不打"零代价（守到哨声即可），场 B 严格胜率上限恒为 **0**（实测 40 局 0 胜 / 40 平）。
- 修 a：`MODES.multi.suddenDeath = 45`（常规局平均 22~30 回合 ⇒ 几乎不影响正常对局，只惩罚拖到 45 回合不清场）。
- 修 b（复核给的另一半，必须做）：**全灭改按"累计造成伤害"判胜**（并列才平局；`damage` 事件的 `source`，
  与 `evo.rankOf` 第 4 键同源）。原因：收缩是**同时**清场 ⇒ 只做修 a 仍是"全员同归于尽⇒平局"
  （实测 40/40 平局、回合恒 47、5/5 阵亡）。

### 验收（可反证，修前修后对比）
| "4 席只ジ vs 1 席冠军" ×40 局 | 修前 | **修后** |
|---|---|---|
| 分出胜负 | **0/40** | **40/40** |
| 平局 / 打满上限 | 40/40 · 40 | **0 · 0** |
| 回合 | 60（恒打满）| **47（恒）** |
| 场 B 严格胜率 | **0%** | **100%** |

### 守门（新增/更新）
- **D57**（新）：multi 收缩必须 >0 且 < `MAX_ROUNDS`；`passiveFieldAt` 契约；"4 席只ジ"场必须出现胜者且不撞上限。
- **D3 / D19**：旧断言记录的是"multi 走全局 100（实际不触发）"这一**病态现状** ⇒ 改断言 multi=45、
  并新增"44 不触发 / 45 全员 −1"的用例；`opts` 覆盖的**污染检查**改成断言"全局仍是模式自身的 45"（效力不变）。
- **D55**：场 B 的"不该挨打"改为按**对手造成的伤**（`takenByOpponentPerGame`）—— 终局收缩的伤 `source=null`，不可格挡。
- 因此本轮 `np-test` 从 104 升到 **105** 条。

### 未做（下一轮，复核 §5 的顺序）
场 B 的**阻断式门槛**（起步：严格胜率 ≥10%）、以及验收 A严胜 / H混合场 / 反弹墙伤害不回退；
之后才是"珠浪费 100%"（暂只打印）与环 exploit（长程滚环者 68%）的节拍随机化。

---

## v1.5.64 — 修"换回来的不是 eco-34"（容器显式旧口径标记 + 等价性守门）+ 训练零暴露度诊断

### 1. 病（第五轮复核发现，我认）：v1.5.63 发出去的是**行为更弱的**策略
`LEGACY(params)` 靠 `shapeOf(params).legacy` 反推，而它按 **`params.length`** 判断 ⇒
`upgrade-pack` 把 3337 撑成 5689 之后 `legacy=false` ⇒ 旧冠军被切到**它从没训练过的 v7 候选口径**。
更深的机制（本版补充）：`embedLegacy` 只保证**新增维度乘 0 ⇒ 前向逐位等价**，
但**动作侧 (目标/珠) 的权重恰好在新增维度里** ⇒ 嵌入后网络**分不出目标** ⇒ 目标退化成枚举顺序。

| 版本 | 座位极差 | 反弹墙（伤害/命中）| 场A | G |
|---|---|---|---|---|
| `eco-34.bak`（原生 v6）| 8pt | 12.25 / 242 | 25% | 4.17 |
| `eco-34-v7.bak`（v1.5.63 发出去的坏嵌入）| 11pt | **7.70 / 152** ✗ | 25% | 4.55 |
| **`eco-34-v7c.bak`（带标记嵌入 = 现线上）** | **8pt** | **12.25 / 242** ✓ | 25% | **4.17** |

### 2. 修法（复核建议 ①，已实现）
- 容器**显式标记**：`pack()` 写 `lv`（来源版本）、`unpack()` 把标记挂到 `params.legacyFrom`、
  新增 `isLegacyChooser(params)`；`js/train/evo.js` 的 `LEGACY()` **优先认标记**（不再认长度）。
- `tools/upgrade-pack.mjs` 输出带 `lv` 的容器；**V7 训练产线不受影响**（训练包无 `lv`）。
- 守门 **D56**（复核建议 ②）：源码契约 + **行为等价断言**（同一份旧形状权重：原生 vs 嵌入+标记 ⇒ 逐场结果必须相同）。
  ⇒ 这条在修好之前**必然红**，正好把问题钉住。

### 3. 线上包（本版重上线，严格体检、无 `--force`）
A 考卷 **42.6%**（坏嵌入是 49.1% —— 口径退化的策略反而在这张卷上分高，说明单指标不够）·
反弹墙 **12.25/局 · 242 命中** · 场A 还手 **25%**（造成 1.63/局、胜率 10%）· G **4.17** ·
E 新口径 **16%**（旧口径 87%）· 座位极差 **9pt** · 自对局 13.1 伤害/局 · 平局 0%。
指纹重记 `53f064c5 → 3459758e`（`policy.js`/`evo.js` 属指纹集）；`np-test 104/104`。

### 4. **训练零暴露度诊断**（复核 §4，已核实 ⇒ 记入附录 B3-7）
训练对手池 `server/opp-pool.mjs`（17 项）在 `js/train/evo.js:814` 是**连续取不同项**：
```js
else { choosers.push(wrapBotN(opps[oi % opps.length].sel)); oi++; }
```
而被动攒钱型只有 `farmer` / `deepsaver` **两项** ⇒ **"4 席全被动"在训练分布里概率为 0（不是少，是不可能）**
⇒ 过去所有奖励尝试（惩罚被动 / 先手激励 / 密集分 / `EPIRUS_DIV_W`）都在**样本量为 0 的分布**上做优化
⇒ 这解释了 v1.5.17 → v1.5.63 场 B 一次都没被练好。**顺序必须是"先改暴露度，再谈奖励"**。

### 5. 场 B 的后续计划（复核 §4 的四步，未做，待办）
1. **暴露度**：把"4 席同型被动"作为一类对手组合进训练评估（哪怕 1/8 局；不动参数量、不升 `PACK_VERSION`）；
2. **平局兜底**：multi 没有 `maxRounds` ⇒ 落到 `MAX_ROUNDS=60` 而 `SUDDEN_DEATH=100` ⇒ **收缩在多人局永不触发**
   ⇒ 该规则下"不打"零代价、场 B 严格胜率上限为 0（要么把 multi 收缩起点降到 ~45，要么"终局全员存活"判负）⇒ 规则改动需重记指纹；
3. **判据**用"清场数 / 严格胜率"，不用进攻率（1.0→3.0 伤害/局可以让进攻率变好而依然一个人打不死）；
4. **做成阻断式门槛**（起步 ≥10%）而不是 fitness 一项 —— 已有两次证据说明加权会牺牲综合（先手激励掉 14pt、破墙奖励让混合场 40%→17%）；
5. 验收同时看 **A严胜 / H混合场 / 反弹墙伤害不回退**。
另两条顺带可见（暂只打印、未做门槛）：**珠浪费 100%**（线上包与 eco-34 都是每局蓄 1.1~1.4 颗、一颗未花；`v9-31` 是 0%）；
**环 exploit 未变**（长程 1 席滚环 vs 4 席冠军：滚环者夺冠 68%、落地伤害 0.0、小雷 1 次/40 局）。

---

## v1.5.63 — 换回 **eco-34**（v7 嵌入版，用户裁定）+ 旧形状包上线通路打通 + CHANGELOG 精简

### 1. 换包（严格体检，**未用 `--force`** —— 线上包此前连续三轮都是 force 越过的）
| 项 | 线上包 v7wall-31 | **新：eco-34（v7 嵌入）** |
|---|---|---|
| A 考卷（40 局）| 41.4% | **49.1%** |
| 座位极差（5 席同策略）| **61pt** ✗ | **15pt** ✓（19/26/23/11/22）|
| F 场A 被集火还手 | **13%** ✗ | **25%** ✓（造成 1.85/局、承受 2.80/局、胜率 18%）|
| 反弹墙（长程 20 局）| 7.25/局 · 84 命中 | 7.70/局 · 152 命中 ✓ |
| G 有效技能数 | 5.15 | **4.55** ✓ |
| E 新口径 · 无威胁摆架势 | 3% | 23% ✓（旧口径 89% = 当年误判它"病态摆架势"的原因）|
| 自对局 | 14.1 伤害/局 · 0% 平局 | 13.5 伤害/局 · 0% 平局 |
| 场B 无压（惩罚攒钱）| 2% · 1.00 伤害/局 | 2% · 1.00 伤害/局（**共同缺口**，待训练解决）|

### 2. 过程修复：旧形状冠军的**上线通路**（本次被挡了三次才发现）
- `tools/promote-champion.mjs` 两处 `unpack(json)` 缺原生标志 ⇒ 对 v5/v6 包返回 **null**（体检直接崩：
  `shapeOf(null)`）；实测 `unpack(j)=null` / `unpack(j,true)=3337`。改为 `unpack(json, true)`（与
  `audit-lib.loadChamp` 注释的规矩一致："历史形状必须原生读取，不要嵌入"）。
- `checkPack` 只校验 **v7 容器**（`o.v !== PACK_VERSION` 即判 not-ok）⇒ 旧包会被回读自检挡死；
  改为"v7 才走 checkPack，旧形状走原生读取 + 长度自检"。
- **仓库契约确认**：线上 3P 包**必须是 v7 容器**（np-test N19 卡 `checkPack`）⇒ 新增
  **`tools/upgrade-pack.mjs`**（`unpack(j,true)` → `embedLegacy` → `pack`，3337 ⇒ 5689 位，`checkPack ok`）。
  ⚠️ 注意嵌入会**改变行为**（chooser 从旧路径切到 v7 候选口径）：实测反弹墙 12.25/242 → **7.70/152**、
  G 4.17 → 4.55、座位 8 → 11pt（嵌入前后都过四道硬门槛）。

### 3. CHANGELOG 精简（用户要求：杂项太多）
3963 行 → **918 行（23%）**：条数不变（105 条），改为「**一行版本索引**（最近 20 条保留要点）+ 附录」：
- **附录 A 当前有效契约/常量**（指纹、形状、v7 容器契约、上线体检门槛、训练隔离、实验开关、口径常量）；
- **附录 B 训练问题复盘**（流水线 / 门槛指标 / 训练动力学 三类，逐条带版本号与实测证据）；
- **附录 C 方法论规则**（踩坑 → 规则 10 条）。
精简前的全文保存在 git 历史：`git show <精简提交的父提交>:CHANGELOG.md` 可取回。

### 4. 门禁
`np-test 103/103` · `smoke OK` · `battle OK`；规则指纹 `53f064c5` 与包内一致；技能报告已自动重跑（`docs/skill-report.html`）。

---

# 变更记录（精简版）

> **本文件是索引 + 当前契约 + 复盘**：每条版本保留"一行决策/结论"，最近 20 条另附要点。
> 精简前的全文（每版的完整证据、表格、日志）保存在 git 历史里，随时可取：
> `git log --oneline -- CHANGELOG.md` 找到精简提交的**父提交**，再 `git show <父提交>:CHANGELOG.md`。
> 精简原则：保留**决策与其理由、门槛/指纹/常量、踩过的坑**；删掉过程复述、重复表格与已经过期的中间读数。

## v1.5.62 — **F 正式换成"场 A 被集火还手率"（≥20%）**（用户裁定）+ eco-34 历史记录核查

### 1. 换口径（工具链三处同步，单一真源）
`tools/audit-lib.mjs` 新增 **`aggressionProfile(W, params, GAMES)`**（场 A/场 B 的单一真源；
冠军座位逐局轮换 `g % 5`；伤害归因用事件真字段 **`source`**）：
- **场 A（被集火）**：4 席脚本猛攻 vs 1 席冠军 ⇒ 还手率 / 造成伤害 / 承受伤害 / 胜率；
- **场 B（无压）**：4 席只ジ vs 1 席冠军 ⇒ "面对纯攒钱者会不会主动打"。

三处消费者：`promote-champion`（**判据 = 场 A ≥20%**，旧的 `<25%` 判据删除，旧值仅作历史对照打印）、
`screen-champ`（筛选器同步）、`champ-audit`（F 列换成场 A）。新守门 **D55**（源码契约 + 行为断言：
场 A 对手出手必须远多于场 B、场 B 里冠军不该挨打）；D41 里过期的"F=25%"正断言改为**反断言**（防回退）。

### 2. 判别力（60 局口径）
| 候选 | 座位极差 | 反弹墙伤害/命中 | **场A 还手** | G | 判定 |
|---|---|---|---|---|---|
| 线上包 | **61pt** ✗ | 7.25 / 84 | **13%** ✗ | 5.15 | ✗ 偏座 + 被集火不还手 |
| **eco-34** | **8pt** ✓ | 12.25 / 242 ✓ | **25%** ✓ | 4.17 ✓ | **✅ 通过** |
| **种子 v1.3.58** | 13pt ✓ | 16.95 / 339 ✓ | 27% ✓ | 3.02 ✓ | **✅ 通过** |
| v12-85 | 33pt ✗ | 16.40 / 313 ✓ | 33% ✓ | 3.40 ✓ | 只差座位 3pt |
| v9-36 | 25pt ✓ | 17.75 / 355 ✓ | 34% ✓ | 1.70 ✗ | 只差 G |
⇒ **修正口径后首次有两个候选全过**（旧口径下所有冠军 22~35% 全被挡，只能 `--force`）。

### 3. eco-34 历史记录核查（用户提问："当初为什么换下来"）
**没有 bug 类问题**，当年被换下的两条理由都是**后来被证明标定错误**的指标：
- **E 89% ✗（判"病态摆架势"）** —— 同一份记录里新口径是 **16% ✓**，并注明"**旧口径误判**：它主要在'对手攒过 5 ジ 之后'才防，合理"；
- **F 22% < 25%** —— 就是本版换掉的这条（`CHANGELOG` 当时写着"待用户裁定：(a) 回退到 eco-34…但 F 22% < 25% 仍差 3pt"）。
它**真实存在的弱点**（`docs/AUDIT-RESPONSE-v1.5.17.md:150`）：面对 4 席只ジ的对手 **0% 胜 / 100% 和 / 主动伤害 1.0/局**
（long 打满 104 回合被终局收缩耗死）—— 正是用户最初的抱怨，也是本版**场 B** 量到的东西。
另有两条背景：它是"**两个杠杆都关**"的老配方训出来的；`eco-34.bak` 的 meta 缺 `ecoOverride/fightOverride/…` 四键。

### 4. **换包仍未执行**（等用户裁定）
场 A 修正后 `eco-34` 与**种子**都过四道硬门槛；但两者在**场 B（惩罚攒钱）**都仍不合格（2% 进攻 / 1.0 伤害/局 / 100% 平局）
⇒ 若要以"能惩罚纯攒钱"为目标，那需要训练而不是换包。换包前我会先出全量体检对照表（A 考卷 / E / H / 蓄能 / 珠浪费 / 场B）。

### 5. 门禁
`np-test 103/103`（新增 D55）。

---

## v1.5.61 — F 的口径重做（双场实测；**门槛未改**，据用户质疑）

### 1. 用户质疑成立：旧 F 的考核依据有问题（代码证据）
`tools/audit-lib.mjs:196-247` 的 `fieldRate`：
```js
const ch = [human];                                   // 0 号座 = 唯一脚本进攻者
for (let i = 1; i < 5; i++) ch.push(policyChooserN(params, 0.15));   // 1~4 号座 = 冠军自己 ×4
const ATK = [GUN, SWORD, SNIPE, TANK, RAILGUN, DRAIN];               // 分子是硬编码 6 张卡
```
① **场地名不副实**：所谓"活跃场"只有 **1 席**进攻，另外 4 席是冠军自己的副本 ⇒ 量到的是**自对局均衡**，
不是"面对猛攻不还手"。② **分子是手工白名单**：数据驱动看真正带 `dmg` 的还有 **`bigT` / `dualGun`**（不在名单里）。
③ 门槛史佐证：CHANGELOG 自己记过"**所有冠军 F 都在 22%~35%**"，门槛 35%→25% 后仍靠 `--force` 越过
⇒ 一个只能靠 force 过的门槛，问题在门槛。

### 2. 新探针 `tools/probe-aggr.mjs`：**两个对置场 + 结果型指标**
冠军座位逐局轮换（`g % 5`）；伤害归因用事件真字段 **`source`**（已打印确认，此前猜 `from` 得 0）。

**场 A 被集火（4 席脚本猛攻 vs 1 席冠军，40 局）**
| 冠军 | 进攻率 | 造成伤害/局 | 承受/局 | 胜率 | 平局 |
|---|---|---|---|---|---|
| **线上包** | **13%** | **0.85** | 2.92 | 8% | 35% |
| 种子 v1.3.58 | 27% | 1.85 | 3.00 | 0% | 38% |
| eco-34 | 25% | 1.63 | 2.88 | 10% | 40% |
| v12-85 | **33%** | 1.98 | 2.80 | 15% | 35% |
| v9-36 | **34%** | **2.08** | 2.88 | 8% | 48% |
⇒ **该口径能把"已知好"与"已知坏"分开**（线上包 13% ✗ / 种子 27% ✓ / eco-34 25% ✓）——
若定 **≥20%**，这是第一个有判别力的 F 阈值（不再需要 `--force`）。

**场 B 无压（4 席只ジ vs 1 席冠军，40 局）**
| 冠军 | 进攻率 | 造成伤害/局 | 平局 | 回合 |
|---|---|---|---|---|
| 线上包 / 种子 / eco-34 | 2% | **1.00** | **100%** | 60.0（打满）|
| v12-85 | 5% | 3.20 | 88% | 60.0 |
| v9-36 | 5% | 3.08 | 100% | 60.0 |
⇒ **所有冠军面对纯攒钱者都不主动进攻** ⇒ 这正是用户最初的抱怨（"有人攒 ji，冠军就一直出防御类技能"），
且它是**真实能力缺口**（不是口径问题）。

### 3. 本版**不改任何门槛**（`promote-champion` / `screen-champ` 一行未动）
新口径的阈值（场 A ≥20%？场 B 要不要设？）待用户裁定后再改；本版只交付探针与读数。

### 4. 门禁
`np-test 102/102`。

---

## v1.5.60 — 覆盖熵权重旋钮 `EPIRUS_DIV_W`（训练侧；默认不设 => 行为一字不变）

动因：本晚筛选发现 **G≥3 与 F≥25% 在 25 个候选里几乎互斥**（打得凶的只用 2~3 张卡；用卡多的不够凶）
⇒ 覆盖度这一维在演化目标里被压得过低（`js/train/evo.js` 的注释声称覆盖熵能压低"只剩两三张卡"的个体，
实测权重不足以对抗 fitness 主力项）。`server/train-server.mjs` 读该环境变量并调 `T.setEconomyReward({divW})`。
实测（`EPIRUS_DIV_W` = 默认×10 的 `v12` 臂，12 seed）：**没有打破互斥**（高 G 的仍不进攻、进攻的仍 G<3）
⇒ 说明这是**当前 fitness/探针定义下的结构性张力**，不是权重调不够（→ 促成 v1.5.61 的口径重做）。

---

## v1.5.59 — 蓄能空转量化（`wastedChargeRate`）+ 反弹墙真信号 + 候选筛选器；换冠军**有据暂缓**

### 1. 新探针 `chargeProfile`（`tools/audit-lib.mjs`）+ 体检列 `蓄能/局` / `珠浪费`
事件是真源（先打印过，未猜字段）：`{type:'action',key:'charge',outcome:'ok'}` / `{type:'bead',kind,delta:±1}` / `{type:'beadExpire'}`。
`wasteRate = 过期珠 / 得珠` ⇒ 用户实感的"蓄能→珠过期→再蓄能"变成可量化指标：

| 候选 | 蓄能/局 | 珠浪费率 |
|---|---|---|
| **线上包**（v7wall-31）| **17.2** | **84%** |
| `v7seat2-51` | **1.8** | 83% |
⇒ 线上冠军每局蓄能 17 次、**84% 的珠白攒**；候选只蓄能 1.8 次/局（基数小 10 倍）。

### 2. D 列（反弹墙）的口径真相
`1st` 在这个场里几乎全是并列：线上包 **1st=100% 但严胜仅 12.5%（并列 87.5%）**、候选 1st=32.5%/严胜 0%。
⇒ 真正有判别力的是**反弹墙伤害/局 + 穿透命中数**（新增列），而不是 1st 或严胜。

### 3. 新工具 `tools/screen-champ.mjs`（候选筛选器）
一次量完全部硬指标：座位极差（`seatSymmetry`）/ 反弹墙伤害与穿透命中（`reflectWall`）/ 活跃场进攻（`fieldRate`）/ G。
**判据**：座位 ≥30pt 判偏 · 反弹墙伤害 ≤0.5/局 = "反弹墙瘫" · 活跃场 <25% = 不进攻 · G <3 = 打法坍缩。

### 4. 换冠军：**有据暂缓**（实测表见下）
| 候选 | 座位极差 | 反弹墙伤害/局 | 穿透命中 | 活跃场 | G | 结论 |
|---|---|---|---|---|---|---|
| 线上包 v7wall-31 | **61pt** ✗ | 7.25 | 84 | 22% ✗ | 5.15 | 偏座 + 不进攻 |
| v7seat2-51 | **9pt** ✓ | **0.00** ✗ | 0 | 29% ✓ | 3.47 | 反弹墙瘫 |
| v9-36 | 25pt ✓ | 17.75 ✓ | 355 | 37% ✓ | **1.70** ✗ | 只差 G |
| v9-31 | 29pt ✓ | 17.80 ✓ | 356 | 32% ✓ | **2.46** ✗ | 只差 G |
| v9-46 | 39pt ✗ | 20.00 ✓ | 400 | 33% ✓ | 1.67 ✗ | 偏座 + G |
⇒ **无候选三全**：高 D 的候选全部 G<3（打法坍缩）；G 高的线上包偏座且不进攻。故**暂缓换包**，先训练找三全候选。

### 5. 附带证据：健康门槛确实在"整臂拦下"
`v10` 臂（6 seed，未开 `EPIRUS_ALLOW_HEALTH_FAIL`）**6/6 全被拦、0 产物**；`v9` 臂（开开关）12/12 有产物
⇒ 训练候选池的可得性完全取决于该开关（`v1.5.56` 引入，默认关）。

### 6. 门禁
`np-test 102/102`。

---

## v1.5.58 — 体检并报**严格胜率**（第五轮复核 §4-1/§4-2）：`1st` 把并列算第一，已可见

### 1. 病（复核实测 + 我复现）
`tools/eval-5p.mjs:311` 的 `1st` 走 `T.rankOf`，其排序键 `(存活, hp, taken, dealt)` 在平票时按本局种子洗牌
⇒ **被动场里"多打出 1 点伤害"就能拿第一**。我在 farmerwall 上实测：

```
[冠军] 1st=97.5%   严胜=0.0%   并列=97.5%
```
⇒ **"I 对被动 95~97%"整列都是并列洗牌的产物**，不代表"会打被动场"（引擎真正判胜 0~4%）。

### 2. 修（便宜、不动历史列）
`eval-5p.mjs` 每列并报三个数：`1st(并列计) / 严胜(引擎判胜) / 并列(名次第一但没判赢)`；
`tools/audit-lib.mjs` 的 `exam()` 解析出 `strict`/`tie`；`tools/champ-audit.mjs` 体检表新增 **A严胜** 列。

### 3. 读数（本版实测）
| 场 | 1st | 严胜 | 判断 |
|---|---|---|---|
| A 考卷（standard）| 41.4% | **40.4%** | 差别小 ⇒ **A 列基本可信** |
| farmerwall（I 列）| 97.5% | **0.0%** | ⇒ **该列在"熬满"场里不可用** |
| mix4 / reflectwall | 同类机制（复核上轮已发现"100% 但 52/60 局全灭平局"）| 待逐场重测 | 与 I 列同族 |

### 4. 未做（诚实登记）
**硬门槛改走严格口径**这一步本轮没做：先让三列并联可见（避免历史列不可比），下一轮再把门槛切到严格口径。

### 5. 门禁
`np-test 102/102`。

---

## v1.5.57 — 第五轮独立复核（Qoder）的修正：**座位对称性进体检** + 体检口径/表头修复 + 我的一处单位错误

### 0. 我先认错：上一轮"多数候选已修好"的结论**不成立**
我那张表的"极差"列算的是**胜场数差**，却拿去和**百分点阈值 18** 比 ⇒ 推出"中位 18 ⇒ 多数已修好"。
按百分点重算：`97/2/2/0/0` 的极差是 **97pt**（我印 57）、`100/0/0/0/0` 是 **100pt**（我印 39）。
正确中位约 **33pt** ⇒ **多数候选仍然偏座**（复核的判断对）。本版把口径钉死在百分点上。

### 1. 核实（实测，100 局、5 席同策略、引擎判胜）
| 对象 | long 各座胜率 | 极差 | multi 极差 |
|---|---|---|---|
| **线上包 = `v7wall-31`**（`v7\|0d987a62\|5689`，与 `docs/artifacts/v7wall-31.bak` 一致）| 84% 4% 6% 4% 2% | **82pt** | **51pt** |
| `v7seat2-51` | 13% 26% 26% 20% 15% | 12pt | 16pt |
| `v9-31` | 12% 16% 21% 26% 25% | 14pt | 17pt |
⇒ **玩家真正遇到的对手（线上包）才是最偏的那个**，而此前所有体检项都看不见这件事。

### 2. 新探针 `seatSymmetry(W, params, mode, GAMES)`（`tools/audit-lib.mjs`）
5 席同一策略、每局带盐、**回报一律百分点**；附带判据：**前置"分出胜负 ≥30%"**（平局过多时"各座≈0%"是空读数，
如某候选 93% 平局时的"极差 3pt"）+ **极差 ≥30pt ⇒ 判偏**；`max/min` 比值一并回报供参考。
`tools/champ-audit.mjs` 新增 **座位极差** 列，默认 **100 局**（20 局时噪声足以淹没 30pt 阈值）。

### 3. **座位对称性进上线体检**（`tools/promote-champion.mjs`）
按复核 §6 的"比值 + 前置条件"思路：`verdict==='biased'` ⇒ **阻断上线**；`unjudgeable`（平局过多）⇒ 警告并明确
"别把它当均衡"，不计入阻断。⇒ 现行线上包在新门槛下 **FAIL（60pt）**，即复核要求的 **G3 接成已知红**。

### 4. 复核指出的其它两处，已修
- **体检表头与列错位**（`champ-audit`）：H/I 原先印在 D 之后、表头却排在末尾 ⇒ 按表头读整体错两格；现按表头顺序印。
- **`1st` 口径把并列算第一**（`eval-5p.mjs:311` 走 `rankOf`）：复核的 farmerwall 实测（引擎判胜 4% / 判平 96% /
  `rankOf==1` 99%）确认 ⇒ **A/D/H/I 四列在"熬到上限全员存活"的场里都会虚高**。
  本版先记录（本轮不改口径，避免历史列不可比）：**下一轮**给每列并报"严格胜率 / 并列率"，硬门槛改用严格口径。

### 5. 换冠军的取舍（实测表，见本节末）
没有候选能同时满足：线上包 D=100%/G=5.15 但座位 60pt；`v7seat2-51` A=50.9%/座位 14pt/F=30% 但 D=25%；
`v9-31` D=100%/座位 18pt 但 G=2.46（过不了 G≥3）。⇒ 换冠军**暂缓**，先跑带座位门槛的臂找"三全"候选。

### 6. 门禁
`np-test 102/102`（D50–D54 不变）。

---

## v1.5.54 — 引擎兜底改为**按每局盐轮换**（L7 第四次）：无显式目标时不再恒定取最小索引

### 1. 病（三处，全部在 `js/core/resolve.js`）
```js
oppOf:      const o = aliveOpps(state, pid); return o.length ? o[0] : null;   // 恒定最小索引
mirrorRefs: if (t1 == null || ...) t1 = opps.length ? opps[0] : null;         // 同上
            for (const o of opps) { if (o !== t1) { t2 = o; break; } }        // 恒取最小 ≠ t1
```
⇒ 无显式目标（self 类效果贴在对手身上、镜面反射的 t1/t2）**一律落到索引最小的存活玩家** = **0 号座**
⇒ "0 号座是所有人的默认靶子"（实测 0 号座每局必死 60/60 的一部分来源），也是"多目标技能第二目标不可表达"那条的另一半。

### 2. 修法
新增纯函数 `saltPick(state, arr, seq)`：**盐 0 ⇒ 保持旧口径 `[0]`**（页面与既有对比基线不变）；
有盐 ⇒ 按 `(salt, round, seq)` 轮换（**不消耗任何随机流** —— 借流的三个坑 v1.5.51 已踩过）。
三处兜底全部改用它；导出 `oppOf` / `saltPick` 供守门直测。

### 3. 新守门 **D53**
盐 0/无盐 ⇒ 旧口径 · 有盐 ⇒ 默认作用者覆盖全部候选且大致均匀（40 盐 × 5 回合）· `oppOf` 行为同验 ·
源码断言三处兜底必须已移除 + `saltPick` 必须存在。

### 4. 门禁
`np-test 101/101`；指纹按铁律重记（`fc20996a → 53f064c5`）。

### 5. 下一步（本轮同时启动）
12 seed 大验收臂（v7l7）：同时检验"引擎层三条 L7 修复 + 特征层随机槽位"能否消掉训练诱导的座位偏置；
样本量从上一轮的 2 个不同策略提上去。

---

## v1.5.53 — 结算起点改为**每局随机相位**（L7 的第三次应用）+ 决定性诊断：**偏置是训练造出来的**

### 1. 修：`turnOrder` 的确定性相位
`js/core/resolve.js` 原为 `start = (round - 1) % n` —— **确定性映射** ⇒ 第 1 回合**永远**从 0 号座开始。
（该行上方注释本身就写着上一次修"永远按座位升序"时也是改成另一个**确定性**映射。）
修法：`start = ((salt % n) + round - 1) % n`，盐取 `state.slotSalt`（每局不同、纯函数、**不消耗任何随机流**）。
**盐 0 保持旧口径**（页面与既有对比基线不变）；2 人局恒等顺序（v1.0 冻结）。导出 `turnOrder` 供守门直测。

### 2. 新守门 **D52**
25 个不同盐 ⇒ 第 1 回合起点**覆盖全部 5 座**且大致均匀 · 盐 0 ⇒ 第 1 回合 = 0 号座、第 3 回合 = 2 号座 · 2 人局 = `0,1` · 源码断言（用盐、旧相位必须已移除）。

### 3. 🔑 决定性诊断（本版最重要的一条）
| 对象 | 各座夺冠率（60 局）| 各座死亡数 | 结论 |
|---|---|---|---|
| **热启动种子 v1.3.58** | **22% 16% 22% 16% 24%** | 43/44/43/49/38 | **本身几乎均衡** |
| 训练产物 `v7phase-31..34`（同一权重）| 39% 15% 15% 15% 17% | 39/51/50/49/46 | 强烈偏座 |
| 训练产物 `v7phase-35..36`（同一权重）| 0% 30% 26% 23% 21% | 60/42/47/45/46 | 反向极端 |
⇒ **偏置不是引擎层、也不是特征层造成的，而是训练过程造出来的**（从均衡种子出发、产出偏座策略）。
⇒ 6 个 seed 只产出 **2 个不同策略**（探索近乎冻结）⇒ 验收样本量实际只有 2，**不足以判定修复效果**。

### 4. 已知尚未修的确定性身份映射（下一项）
引擎 `oppOf` / `aliveOpps` 兜底 = **"索引最小的存活对手"**（无显式目标时的默认作用者、镜面反射 t2 兜底）。
它正是"多目标技能两个目标不可调换"那条的另一半。**注**：本版尝试统计"无目标出手比例"的探针**无效**
（猜错了事件字段名 ⇒ 报 100% null ⇒ 实际什么都没测到）—— 下一轮先**打印真实事件对象**再统计，不再猜。

### 5. 门禁
`np-test 100/100`（含 D50/D51/D52）；指纹按铁律重记（`d44d68b3 → 7902661f`）。

---

## v1.5.52 — 槽位顺序改为**纯函数哈希**（不再借任何随机流）；**双目标技能有序成对进入决策**

### 1. `slotRand` 重做：`__slotHash(salt, round, i)`，`salt = state.slotSalt`（调用方输入，缺省 0）
同时满足四条约束：① **不消耗任何随机流**；② **可复现**（同种子同结果）；③ 同一决策内天然一致；
④ 跨局不可预测由调用方"每局传不同盐"提供（L7：并列不许有任何确定性身份映射）。

**过程中查出并钉死三个真缺陷**（每个都配了反证型守门）：
| 缺陷 | 后果 | 守门 |
|---|---|---|
| v1.5.51 借 `__rng()` 当随机键 | 消耗采样流 + 特征对隐藏状态敏感 ⇒ **D22 红**（蓄能珠类型泄漏 [66,76]）| D50：**槽位键不得使用任何随机流** |
| 从 `state.rng` 抽盐 | 改变对局行为 ⇒ **D26 红** | D50：禁 `state.rng`/`rng.next` |
| 计数器盐（每 state 递增）| **破坏"同种子同结果"** ⇒ **D13 红** | 盐改为调用方输入、缺省 0 |
（旧版"缓存随机键 `SLOT_RAND`"一并移除；D50 改为断言其**不存在** + 纯函数哈希存在。）

### 2. 双目标技能：`target2` 声明 + **有序成对枚举**
- `rules.js`：`mk(SK.DUAL_GUN,…)` / `mk(SK.MIRROR,…)` 经 **`opts.extra`**（`mk` 唯一注入通道）声明 `target2: 'enemy'`；
- `candidatesFor`：**有序成对枚举** `(t1,t2)` ⇒ 5 人局 **12 个候选**（4×3）、两目标必不同、**(1→2) 与 (2→1) 是两个不同候选**
  ⇒ **"两个目标不可调换"的语义可表达**（镜面反射 t1=复制对象 / t2=输出对象）；
- 此前 `target2` 恒 `null` ⇒ 第二个目标由引擎兜底（`mirrorRefs` 的"索引最小对手"）填
  ⇒ **这是 0 号座每局必死 60/60 的一部分来源**，也是"角色不可表达"的根因；
- 新守门 **D51**：数据声明 + 全部有序组合 n×(n−1) + 两目标不同 + (1→2)/(2→1) 都在 + 单目标技能口径不变。

### 3. 过时测试假设修正
D26 原先硬编码"玩家 1 在槽 1"（pid 升序时代遗留）⇒ 改为**按 pid 反查槽位**（T/B 块布局：槽 0 = 我，槽 1..4 = 对手槽顺序）。

### 4. 确认
`choose` 采样是**单次抽样 + CDF 扫描**（`policy.js:534-537`/`633`）⇒ 候选数 4→12 **不扰动随机流**（已排除第二个坑）。
指纹按铁律重记（`300ad368 → d44d68b3`）。门禁：`np-test 99/99`。

### 5. 紧随其后（未完成，按序）
① 每局盐接进训练主循环（`state.slotSalt` 由每局种子派生）⇒ 训练里槽位顺序才真正跨局不可预测；
② 去掉引擎兜底（`mirrorRefs` 自动填 t2 / `oppOf` 索引最小兜底）⇒ 无显式目标即不生效；
③ 重训一臂后**复测验收**（各座夺冠率 ~20%、0 号座死亡数与其他座持平）。

---

## v1.5.51 — 槽位顺序改为**每次决策内一次随机洗牌**（破"确定性身份映射"；本会话第三次同类修正）

### 1. 完整诊断链（三层排除 + 一层定位）
| 层 | 测试 | 结果 |
|---|---|---|
| 引擎 | **随机策略** 5 座自对局 | 无偏（各座 23/15/21/25/16，极差 7 局）|
| **状态特征** | 完全对称局面下逐维比较五座 | **0/213 维不对称** ⇒ 无座位编码 |
| **开局策略** | 5 个同一冠军坐满 5 座，看第 1 回合选择（20 局 × temp 0/0.15）| 各座都是 `ji` 主导、分布同形 ⇒ 开局无座位差异 |
| **中后期动力学** | 各座死亡次数 + 提高温度重测 | **0 号座死亡 45~48/50（其他 34~42）、夺冠 3~9%**；temp 0.5 仍偏（甚至更明显）⇒ **结构性、非协调假象** |

### 2. 机制（指向 v1.5.46 我自己的修复）
- 旧版并列按 **pid 升序** ⇒ 开局同血时"槽位 0"永远是 0 号座 ⇒ 上线冠军 **0 号座夺冠 81%**；
- **v1.5.46 改成"按回合起点轮转"** ⇒ 仍然是一个**确定性映射**：每回合"回合起点"占所有人的槽位 0，
  策略学出"打槽位 0"⇒ **回合起点被集火**；而**第 1 回合起点 = 0 号座**、且对局在前几回合就定局 ⇒ 0 号座先死 ✓
- ⇒ **L7 教训的完整含义：并列不许有任何确定性的身份映射**（"更对称的确定性映射"也不行）。

### 3. 修法
`oppSlots` 的并列分支改用 **`slotRand`**：**每次决策内一次随机洗牌**，
按 `state + round` 缓存 ⇒ 同一决策内所有调用（features/actionFeatures/多候选打分）**特征一致**，
跨座位/回合**不可预测** ⇒ "打槽位 0"不再是可利用习惯。
守门 **D50** 同步：必须用 `slotRand`、**旧 `_rot` 必须已移除**（反断言）、随机键必须按 `state+round` 缓存。

### 4. 可证伪验收（正在跑）
重训一臂（6 seed、无强迫）后量新候选：**各座夺冠率应回到约 20%**（现在 3~9%）
且 **0 号座死亡次数应与其他座位持平**（现在 45~48 vs 34~42）。两条都满足才算修好。

### 5. 连带
`policy.js` 属指纹集 ⇒ 按铁律重记两个包（`be74b3d3 → 300ad368`），3P 权重不变。

---

## v1.5.50 — 座位偏置二分（6 seed/格）：**泄漏不在三个特征块里**，转向训练动力学 + 新线索"0 号座 3%"

### 1. 读数（`EPIRUS_FEAT_MASK`，各格 6 seed，long·120 局）
| 格 | 掩掉 | 典型各座夺冠率 | 极差 | 判决 |
|---|---|---|---|---|
| `v7m2_noTarget` | `target` 块 | **3%** 23% 25% 21% 28% | 24~30 | 仍偏 |
| `v7m2_noEffects` | `effects` 块 | 3% 26% 19% 26% 25%；另有 4 个 **98%** 1% 1% 1% 0% | 24 / **117** | 仍偏（且更极端）|
| `v7m2_noBead` | `bead` 块（对照）| **3%** 23% 25% 21% 28%；另有 50% 15% 15% 11% 10% | 30 / 44 | 仍偏 |

⇒ 三格都仍偏 ⇒ **泄漏不在 `target`/`effects`/`bead` 这三个特征块**（掩掉它们并不能去掉偏置）⇒ 按事先定的判读规则**转向训练动力学**。

### 2. 新线索（更锋利，下一步就查它）
反复出现的模式是"**0 号座只有 3%**" —— 这是 **0 号座的系统性劣势**，**方向与上线冠军那个 81% 优势相反**。
⇒ 说明**训练或评测路径里有某处系统性削弱 0 号座**。候选排查点（按可疑度）：
① `rankOf` 的并列洗牌在**训练打分**中的使用；② 评测/训练里**各座 chooser 的构造顺序**与温度；
③ `featuresV7` base 段是否存在与座位相关的量；④ 训练里"只给被测座补资源"的那段评测逻辑（`seat = g % N`）在**跨座比较**时的口径。

### 3. 意外收获：**冻结不是普遍现象，是强迫/环奖励臂特有的**
| 臂类型 | 逃逸率 |
|---|---|
| 带强迫（`EPIRUS_RING_FORCE_EPS`）/ 环奖励窗口 | 约 **1/6** |
| **本轮三格（无强迫、无环奖励）** | **6/6 全部有提升** |
⇒ "训练静默冻结"应记为**那些实验臂特有**的现象，而不是流水线的常态。

### 4. 待办（顺序）
1. 查 **0 号座劣势**（上表列的四个候选点）；2. **G3 固化成真门槛**；3. 替换无判别力的 `ringWallProbe` + 新增 `wastedChargeRate`；
4. 环 exploit：开环节拍随机化 + 反证型守门。

---

## v1.5.47 — 换冠军 `v7seat2-51`：**去座位偏置** + 首个"零 `--force`"的干净换冠军

> 起因：第三方复核 §3 发现**座位偏置**（5 席同冠军自对局 **0 号座夺冠 81%**，其余 3~7%，期望 20%），用户要求优先修。

### 1. 偏置的调查链（每一步都有"能失败"的对照）
| 步骤 | 测试 | 结果 |
|---|---|---|
| 引擎/代码在**对局**层有偏？ | **随机策略** 5 席自对局 | ❌ **无偏**（各座 23/15/21/25/16，极差 **7 局**）|
| 训练时**被测座位固定**？ | 查 `scoreMemberN` | ❌ 座位是**轮转**的（`seat = g % n`）|
| `oppSlots` 并列按 pid 升序（v1.5.46 已修）？ | 修 + 重训 | **部分有效**：仍有 4/5 候选严重偏（87/78/59/21 局）|
| **未训练的种子**是否偏？ | 冻结产物（= 热启动种子） | ✅ **均衡（4 局）** ⇒ 偏置是**训练中学出来的** |

⇒ 收窄为：**偏置可学习、来源在特征/训练信号**（不在引擎、不在被测座位）。**二分计划**（`setFeatMask`，下一步）：
逐块掩掉各训 2 seed，看哪块掩掉后 G3 极差塌回 ⇒ 优先怀疑**动作特征里的"目标槽位下标"**（同一目标在不同座位下槽位不同 ⇒ 可学"打槽位 0"），
其次每对手槽位块；base 聚合块作对照（应对称、预期无变化）。

### 2. 换冠军：`v7seat2-51` → `js/bundled-champion-3p.js`（**未用 `--force`**）

| 指标 | **新：v7seat2-51** | 旧：v7wall-31 |
|---|---|---|
| **座位夺冠极差（G3）** | **4 局 ✅** | **91 局 ❌** |
| 标准考卷 1st（200 局） | **48.5%**（项目最高）| 44.0% |
| **F 活跃场进攻率** | **29% ✓（无需 `--force`）** | 24% ✗ |
| E 无威胁摆架势 | 0% ✓ | 0% ✓ |
| G 有效技能 / 回合 / 平局 | 4.92（**13 种**）/ 23.9 / 0% | 5.40（10 种）/ 30.1 / 0% |
| H 混合场 / I 对被动 | 80% / 100% | 80% / 95% |
| **D 长程反弹墙** | **23%**（已知退化，如实记录）| **100%** |

- **首次出现"全过门槛、不需要 `--force`"的换冠军**（`auditForced` 应为 false）；
- **代价（如实记录）**：D 反弹墙 100% → 23%（复核 §3-1 那条能力线又变弱 —— 但注意 D 的 100% 部分来自"同归于尽后按血量第一"的口径）。

### 3. 待办
1. **把千问的 G3 固化成真门槛**（`tools/gate-drafts.mjs` 作单一来源）：它已证明有判别力
   （在 45/46/48/49 上红、在 51 与种子模型上绿）⇒ 可作提交前的硬门，防止再引入带偏置的冠军；
2. **偏置来源二分**（`setFeatMask`，见 §1）；
3. **替换我那个没有判别力的 `ringWallProbe`**（G1 已证明它对"会瞄/仪式"策略都报 40 次）⇒ 环课题改用 `probeField`/`probeFieldNoRinger`；
4. 环 exploit 本身（节拍随机化 + 反证型守门）继续排队。

---

## v1.5.46 — **修座位偏置**（复核 §3，最高优先级）：并列不再按 pid 升序

> 复核实测：同种子族、5 席全部加载**同一个**冠军包、temp 0.15、200 局 ⇒
> **0 号座夺冠 81%（long）/ 59%（multi）**，其余座位 3~7%（对称期望 20%）。
> 且 `resolve.js` 的 `turnOrder()` 每回合轮转行动起点 ⇒ **不是"先手"造成的**。

### 1. 根因（一行代码的并列分支）
```js
// js/train/policy.js  oppSlots(state, pid)  —— 修前
a.sort(function (x, y) { const d = state.p[x].hp - state.p[y].hp; return d !== 0 ? d : x - y; });
```
开局全员同血 ⇒ 并列全部走后一项 ⇒ 1~4 号座看别人时 **"槽位 0"永远是 0 号座**，
而 `FEAT_S` 里 4 槽 × 10 维是**硬编码下标** ⇒ 网络完全可以学出"槽位 0 该怎么对待"
⇒ **与实力无关的座位身份泄漏**（即本项目 `L7` 教训"并列不许按 pid 升序"在另一处的复现）。

### 2. 修法（v1.5.46）
并列分支改用**与 pid 无关、且每回合变化**的轮转：
```js
const _ts = ((((state.round || 1) - 1) % _n) + _n) % _n;
const _rot = function (i) { return (((i - _ts) % _n) + _n) % _n; };
a.sort(function (x, y) { const d = state.p[x].hp - state.p[y].hp; return d !== 0 ? d : _rot(x) - _rot(y); });
```
实测：`pid=1` 的"槽位 0"从**恒为 0** 变为随回合变化 `[0,2,2,3,4]` ✓。
守门 **D50**（行为 + 源码双断言）：不同回合下槽位 0 不得恒为同一人；必须用 `_rot`；旧的 `x - y` 必须已移除。

### 3. 影响面（为什么这条优先级最高）
它给**一整类"5 座同一冠军"的指标**打上折扣：B 伤害/局、平局率、`mirrorHealth`（健康门槛的数据源）、D 长程反弹墙，
以及复核自己上一轮的 D 项对照。

### 4. 连带：指纹重记（铁律）
`policy.js` 属指纹集 ⇒ `57108d06 → **be74b3d3**` ⇒ 重测重记两个包（3P 权重不变仍为 `v7wall-31`）⇒ D16 转绿，`np-test 98/98`。

### 5. 偏置修复后现役冠军的新读数（**基本不变** ⇒ 纯缺陷修复，无连带行为变化）
A 考卷 42% · B 伤害/局 13.8 · 回合 30.1 · **D 反弹墙 100%** · **E 无威胁 0% ✓** · **F 24% ✗（`--force`）** ·
G 5.40（10 种）· H 混合场 80% · I 对被动 95% · 平局 0%。

### 6. 仍未做（复核文档 §2/§4 指出的，按优先级）
1. **环 exploit 一点没动**（回退后的现状）：长程 1 席连开环 + 4 席冠军 ⇒ **滚环者夺冠 75%、落地伤害 0.0**，
   而冠军 40 局只出 **2 次**小雷 —— 用户那两局就是这么输的；
2. **两条反证型守门**（换人反证：把唯一滚环者换成不滚环 ⇒ 小雷出手必须显著下降；判别场：1 环 + 3 快攻 + 1 冠军 ⇒ 出手必须 > 0）；
3. **训练侧解耦**：环流对手**开环节拍随机化**，打断"环 ↔ 回合数"共线；并确认 `opps` 里 `ringspam` ×3 是否有意；
4. `auditForced=true` 仍是 F 24% < 25%。

---

## v1.5.45 — **回退冠军到 `v7wall-31`**：v7r2-35 的"会打断环"被复核证实是**第 4 回合仪式**

> 依据：用户实测两局（`results/epirus-battle-20回合.txt` / `-21回合.txt`）+ 第三方复核复刻。
> **实测证据（原文）**：20 回合局里 **玩家1 出【聚能环】**，四个冠军座**全部【雷击之枪】打彼此**（两次"互雷成环"自废），
> **唯一在开环的玩家1 一枪没挨**；21 回合局里 **玩家1 出的是【ジ】（无人开环）**，四座**照样四发齐射**。
> 复刻场（1 席连开环 + 4 席冠军，long，40 局）：小雷 187 次（**第 4 回合 ×160**、第 8 回合 ×20、其余 7），
> **砸在滚环者身上 0 次**、160 次出手时**场上根本没有环**；判别场（1 环 + 3 快攻 + 1 冠军）小雷 **0 次**；
> 自对局（无环）小雷 **560 次 = 9.7% 出手** ⇒ 把反环卡当常规动作在烧。

### 1. 处置：回退冠军
`v7r2-35` 的收益（"自然小雷 25"）是**假验收**产物 ⇒ 回退到 **`v7wall-31`**（标准 44.0% / D 100% / E 0% ✓ / 全息套别人 2.0 ✓）。
用 `--force` 越过 F 24%（差 1pt 属噪声带），meta 留痕。

### 2. ⚠️ 我的验收标准**第二次**失效（必须记进方法论）
探针场地是**固定节拍**的滚环场（每回合无脑开环）⇒ "**环出现**"与"**回合数**"**共线** ⇒
只要在第 4 回合砸小雷就必然"命中"⇒ `miniTCasts > 0` **由场地构造保证、不可能失败**。
- 第一次同类错误：`miniTCasts ≥ 5` 可被 ε-强迫自证（复核 §5-5 已警告过，我没把它贯彻到**探针场地构造**）；
- **新规矩**：验收断言必须能**失败**，且**探针必须自带判别臂**（同回合数、但把环换成非环 ⇒ 出手数必须显著下降）。

### 3. 下一步（已排好序，先做前两条再做训练）
1. **两条反证型守门（np-test）**：
   - **换人反证**：把场上唯一的滚环者换成不滚环 ⇒ 小雷出手数**必须显著下降**（现在不降 ⇒ 它读的是回合数）；
   - **判别场**：1 滚环者 + 3 快攻 + 1 冠军 ⇒ 小雷出手数必须 **> 0**（现在 = 0 ⇒ 一有真实压力动作就消失）。
2. **训练侧解耦**：把环流对手的**开环节拍随机化**（先攒够再等几回合 / 中途断一环），
   打断"环 ↔ 回合数"的共线 —— 否则**换任何奖励形状都会重新拟合回合数**；
   并确认 `opps` 里 `ringspam` 出现 **3 次**是有意还是拼接产物（它放大共线）。
3. 之后才谈是否重训。

---

## v1.5.44 — **换冠军 `v7r2-35`：首个"会打断环"的冠军**（供用户/复核实测）

> 用户裁定：先换上，让用户与第三方复核**实测**该行为，再决定是否继续投资这条线。

### 1. 新老对照

| 指标 | **新：v7r2-35** | 旧：v7wall-31 |
|---|---|---|
| **自然小雷次数**（20 局对 4 席环流，**关掉强迫后量**）| **25** | 0 |
| **由我造成的作废**（`voided` 且 `byPid === 我`）| **25** | 0 |
| 环墙 1st（long·200 局）| **42.5%** | 26.0% |
| 标准考卷 1st（200 局）| 40.2% | **44.0%** |
| D 长程反弹墙 | 100%（穿透 **344**）| 100%（穿透 84）|
| E 无威胁摆架势 / G / 平局 | 0% ✓ / 5.11（8 种）/ 0% | 0% ✓ / 5.15（11 种）/ 0% |
| **F 活跃场进攻率** | **11% ✗（`--force` 越过）** | 24% ✗ |
| 全息套给别人（硬门槛）| 0 ✓ | 2.0 ✓ |

### 2. 成功配方（三要素，缺一不可；前七次失败的原因见 v1.5.31~v1.5.43）
1. **奖励可归因**（v1.5.37）：`setVoid` 带 `byPid` —— 否则"我打断"与"别人打断、我恰好放过小雷"同分；
2. **开环判据 = 动作事件**（同版）：该座**真的施放了聚能环**（含首次）—— 旧 `delta ≥ 2` 漏首次、还把避雷针 +4 ジ 误判；
3. **退火强迫**（v1.5.40）：格内 ε=1.0 只在前 **40** 代生效 ⇒ 产物来自**无强迫的后段**，验收才有意义。

### 3. 已知代价与风险（**如实记录，供复核重点验证**）
- **标准考卷 −3.8pt**（44.0% → 40.2%）；
- **F 11% < 25%**（活跃场进攻率）⇒ 常态对局可能**偏被动**；
- **能力命中率 ≈ 1/24**（"逃过冻结" ≠ "学会"：18 种子批量里 3 个逃逸、0 个学会）⇒ 本候选是**确定性样本**，不代表配方已稳健；
- 根因（v1.5.42 的负结果）：**堵点在选择哲学** —— 强迫让形状分变差 ⇒ 探索期个体永远不算 `better` ⇒ 提升链不留它们。

### 4. 请实测时重点记录的三类现象
① 我方开环时它**没**出小雷的回合；② **该打断却没打断**的场面特征（是否满血/是否已连开多回合）；
③ **该进攻却摆架势**的回合（对应已知短板 F 11%）。这三类可直接喂给下一步"改选择哲学"的课题。

---

## v1.5.43 — ② 的负结果 + 18 种子批量："逃过冻结"≠"学会打断环"，堵点在**选择哲学**

### 1. ②（探索期不被健康门槛惩罚）实测：**没治好脆弱性**
| | 修 ② 前（窗口 40）| 修 ② 后（v7r2）|
|---|---|---|
| 逃逸 seed | 1/6（35）| **1/6（还是 35）** |
| 该 seed 的自然小雷 / 可归因作废 | 25 / 25 | **25 / 25（完全复现）** |
⇒ **堵点不在健康门槛**：`finishStep` 要求 `better`（形状分/胜率变好），而**被强迫出来的小雷让形状分变差**
⇒ 探索期个体**从来不是 better** ⇒ 连进入提升候选的机会都没有（健康与否都轮不到）。

### 2. 18 种子批量（41~58，同一配置）：**逃逸 3 个（45/46/52），学会 0 个**
```
逃逸 3 / 零提升 15      v7r3-45 小雷 0 / 作废 0 / 穿透 291
                        v7r3-46 小雷 0 / 作废 0 / 穿透 257
                        v7r3-52 小雷 0 / 作废 0 / 穿透 359
```
⇒ **"逃过冻结"与"学会打断环"是两件事**：逃逸是必要不充分条件。

### 3. 全部证据汇总（窗口 × 种子）
| 配置 | 种子 | 逃逸 | 学会（自然小雷>0）|
|---|---|---|---|
| 窗口 20 | 31–36 | 0 | 0 |
| **窗口 40** | 31–36 | 1（35）| **1（35：小雷 25 / 作废 25）** |
| 窗口 40 | 41–58 | 3 | **0** |
| 窗口 80 | 31–36 | 1 | 0 |
⇒ **能力命中率 ≈ 1/24**（且是确定性的：同 seed 复现）。

### 4. 结论（B 的目标用"多种子"这条路走不通）
- "先做稳再换"**无法靠堆种子达成**（1/24 的产率，且 3/3 逃逸者都没学会）；
- 要稳健，必须动**选择哲学**：探索期允许"变差仍能推进"（例如按**新颖度**入档/保留 explorer 分支，
  或探索期把 `better` 判据从形状分换成行为新颖度）—— 这是独立课题、风险更高；
- 现有可上线的**唯一确定性样本**：`v7r2-35`（= `v7force2-35` 同配置复现）——
  标准 40.2%（现役 44.0%）· 环墙 42.5%（现役 26.0%）· D 100%（穿透 344）· E 0% ✓ · **F 11% ✗** · 自然小雷 25 / 可归因作废 25。

### 5. 待用户裁定（三选一）
(a) **换 `v7r2-35`**（`--force` 过 F）：用 −3.8pt 标准分换"会打断环"+环墙 +16.5pt；
(b) **立独立课题**：改选择哲学（新颖度入档/探索期换判据）后再重训；
(c) **维持现役**，把本课题归档为"能力可学、当前搜索留不住"的研究结论（七次尝试谱系已完整入库）。

---

## v1.5.41 — "打断环"**配方确认但很脆弱**（窗口 20/40/80 对照）+ 七次尝试谱系

### 1. 成功配方（首次真正学会"打断环"，v1.5.40 实测）
三条**缺一不可**：
1. **奖励可归因**：`setVoid` 带 `byPid`（v1.5.37）—— 否则"我打断"与"别人打断"同分（反例 A=1.5 / B=0.5）；
2. **开环判据 = 动作事件**（该座真的施放了聚能环，含首次）—— 旧 `delta >= 2` 漏首次、还把避雷针 +4 ジ 误判；
3. **退火强迫**：格内 ε=1.0 只在前 **40** 代生效 ⇒ 产物来自**无强迫的后段**，验收才有意义。
   - 验收（复核 §5-5 要求，非恒真）：**关掉强迫后的自然小雷 > 0** 且 **`voided` 且 `byPid === 我` > 0**。

### 2. 窗口对照（每臂 6 seed，池 D）
| 窗口 | 零提升检测 | 自然小雷 | 判决 |
|---|---|---|---|
| 前 **20** 代 | **0/6 有提升** | 全 0 | 作废（冻死更彻底）|
| 前 **40** 代 | 1/6 有提升 | **seed 35: 25 / 由我作废 25** | ✅ 唯一成功 |
| 前 **80** 代 | 1/6 有提升 | 全 0 | 逃过冻结的也没学会 |
⇒ **窗口很窄、可复现率低（1/6）**：不是"配方不对"，而是**提升闸门会把探索期的个体整个拒掉**。

### 3. 脆弱的机制（与 v7both/v7soft 的"奖励冻死"同源）
强迫出来的小雷降低了成员的**形状分/体检指标** ⇒ 提升被拒 ⇒ 冠军冻在热启动种子 ⇒ 整臂零提升。
⇒ 结构性修法（下一步）：**让提升闸门不惩罚"探索期"**（例如只按**强迫窗口之后**的世代判定健康/择优，
或允许冠军来自后段的世代），而不是继续调 ε/窗口。

### 4. 已知最佳候选 `v7force2-35`（**尚未上线**，待更多候选对比）
标准考卷 40.2%（现役 44.0%）· 环墙 **42.5%**（现役 26.0%）· D 反弹墙 100%（穿透 344）·
E 无威胁摆架势 **0% ✓** · F 活跃场进攻 **11% ✗** · G 5.11（8 种）· 平局 0% · 自然小雷 25 / 可归因作废 25。
⇒ 用户选 **(B) 先做稳再换**：先凑出"标准不掉 + 会打断环"的候选，再动冠军。

---

## v1.5.39 — 定向强制探索：**两臂都因"算术陷阱/训练冻结"作废**（结论：探索问题仍未测到）+ 结构障碍入库

### 1. 实现（只影响"有滚环者且我付得起小雷"那一格）
- `ringForceTarget(state,pid,legal)`：仅当**有人 `ringStreak ≥ 1` 且我付得起小雷**时返回该目标，否则 `-1`；
- 包装层：**只在概率门内**改动作，其余一律**原样返回学习到的动作**（**刻意不复用教师槽做常开 ε** —— 那会用 heavyfire 扰动全局）；
- env 通道：`EPIRUS_RING_FORCE_EPS` → 主线程 + **worker 独立进程**都接；并加**审计轨迹**
  `[ringforce] worker eps=... (env=...)` —— 实测打印 `eps=1 (env=1)` ✓（**通道无误**）；
- 守门 **D47**（7 条：目标格判定四例 + 必须带概率门 + 非目标格必须原样交回 + worker 必须能走 env）⇒ `np-test 95/95`。

### 2. ⚠️ 两臂都**不能作为证据**（各自的原因都实测钉死了）
| 臂 | 设定 | 零提升检测 | 判决 |
|---|---|---|---|
| `v7force` | 格内 ε=5% | 2 有提升 / **4 零提升** | **算术陷阱**：目标格只占决策 **0.15%**（复核实测 7/4549），又乘 5% ⇒ 实际强制 ≈ **0.0075%**，整跑几乎没强迫 ⇒ 那 2 个提升的 seed 也等于没测 |
| `v7force1` | 格内 ε=1.0 | **6/6 零提升** | **训练整体冻死**：产物 = 热启动种子 ⇒ 探针量到的是**种子**，`miniTCasts=0` **无效** |

⇒ **"探索是不是瓶颈"这个问题，到现在仍然没有被有效测试过**（不能下任何结论）。

### 3. 新发现的结构性障碍（值得单列）
**在当前"健康门槛 + 提升闸门"下，强制探索会把训练冻死**：被强迫出来的小雷降低了成员的形状分/体检指标
⇒ 提升被拒 ⇒ 冠军冻在种子（与 v7both/v7soft 的"奖励冻死"同源，只是触发者从"奖励"换成"强迫"）。
⇒ 正确的做法应是**强迫也要退火**（早期强制把动作示范出来，之后关掉、交给奖励），
且**验收必须同时看 `zero-promote-check`**（否则会在"量到种子"的情况下误判成"学不会"——本次差点如此）。

### 4. 方法论新增
**"在稀有格子上再乘 ε" = 等于没做**：`P(格子) × ε` 才是实际干预率，稀有格子上 ε 必须取 1 甚至配合退火。
（同族教训：阈值不能定在噪声带里、门槛要按已知好/坏样本标定。）

---

## v1.5.38 — "打断环"第五次尝试：**信号层修好后依然是 0**（干净的否定结果，进入最后一枪）

### 1. 本臂做了什么（把审计指出的三堵墙**全部拆掉**之后的第一臂）
- 奖励**可归因**：`setVoid` 带 `byPid`，反例 A=1.5 / **B=0.5**（此前同分）；
- 开环判据 = **"该座真的施放了聚能环"的动作事件**：反例 **D=0.5**（首次已识别）、**E=0**（避雷针 +4 ジ不再误触发）；
- 教师条件 `ep ≥ 2` → **`ringStreak ≥ 1`**（审计实测触发率 45% → 0.15%）；
- 池 D（**4 席 ringspam**）保证暴露度；示范期 0.8 + 退火环奖励。

### 2. 结果（`v7attr`，6 seed，20 局对 4 席环流）

| 冠军 | 自然小雷次数 | **由我造成的作废** | 对开环者伤害/局 | 反弹墙穿透命中 |
|---|---|---|---|---|
| v7attr-31 … -36（全部）| **0** | **0** | 2.00~2.45 | **339~372** ✓ |

- **验收两条（自然小雷 > 0、`byPid===我` 的作废 > 0）全部不达标**；破墙线完好（说明本臂没"换偏"）。
- 探针同步增强：`ringWallProbe` 新增 **`voidedByMe`**（由我造成的作废数）—— 旧工具只数 `voided` 总数（含别人造成的），
  在 §5-1 修好之前**根本量不出归因**。

### 3. 结论（五次尝试的完整谱系）
| 手段 | 自然小雷 |
|---|---|
| 奖励常开 / 退火 / 密集 | 0 |
| 池子（1 席 / 4 席 ringspam）| 0 |
| 课程示范（0.5 / 0.8）| 1 → 0 |
| 暴露度 + 示范 + 接力 | 0 |
| **再加上"可归因 + 判据含首次 + 教师条件正确"** | **0** |

⇒ **信号层正确是必要条件、但不足够**：在奖励已经"该给的都给对"的情况下，这个动作**依然没有被采样到**。
⇒ 剩下的只有两条：**① 探索**（训练期对"有滚环者且我付得起小雷"这一格做 ε-强迫，**但验收必须在关掉强迫后量自然行为**）；
**② 表达力**（按审计 §5-4：每对手槽位加 1 维 `ringStreak/3`，**+5 维**，不新造概念）。
顺序：先 ① （便宜、且是审计认可的最后一枪），不行再 ②。

---

## v1.5.37 — 按第三方第三轮复核修"可归因"与"全息幻影候选"（**都不动参数量、不升 PACK_VERSION**）

> 依据：`docs/AUDIT-RESPONSE-v1.5.35.md` §5-1/2/3（打断环失败的硬障碍）与 §2-1（全息修复没生效且前提错）。

### 1. 打断环：奖励终于**可归因**（复核 §5-1 的三个硬障碍全修）
| 障碍（复核实测） | 修法 | 验证 |
|---|---|---|
| `voided` 事件**不记施法者**（`by` 是原因字符串）⇒ "我打断"与"别人打断、我恰好放过小雷"**同分** | `setVoid(state, pid, by, **byPid**)` 加第 4 参；小雷/大雷作废目标的调用点补施法者 | 反例 **A=1.5 / B=0.5**（此前 1.5 与 1.5）|
| 教师条件 `对手 ep ≥ 2` 在真实自对局里 **45%** 的决策都成立，而"有人真在滚环"只有 **0.15%** ⇒ 教的是"见人兜里有 ジ 就砸小雷" | 教师条件改 **`ringStreak ≥ 1`** | D43 新增"只是兜里有ジ ⇒ 不得砸小雷"的反例 |
| 开环判据 `delta ≥ 2` **漏掉环的第一次**（delta 是 1 然后 2）⇒ 越早打断越没分；且把"避雷针 +4 ジ"误当开环 | 判据改为**"该座真的施放了聚能环"的动作事件** | 反例 **D=0.5（首次已识别）**、**E=0（非环不触发）** |

- 守门 **D39/D43 按新事实重写**（D39 直接用复核给的 A~E 五反例）。
- **验收标准同步修正**（复核 §5-5）：原先 `miniTCasts ≥ 5` 是**恒真式**（自己加 ε-强迫必然满足）⇒ 改为
  **关掉强迫后的自然小雷数 > 0** 且 **`voided` 且 `byPid === 我` 的次数 > 0**。

### 2. 全息屏障：删掉**幻影"自己"候选**（复核 §2-1）
- 复核证明 v1.5.34 那条修复**没生效且前提错**：`holoShieldFrom` 明写"自己给自己套不算"（**这张卡本来就不能给自己**，
  自保用原型制御），而 `targetOf()` 在**声明阶段**就把 null/自己退成"第一个存活对手" ⇒ 我加的"自己"候选是**幻影**：
  实测被选中 **35/330 次**，每次都把**错误归因**喂回训练。
- 修：**候选只给真实对手**（holo 候选 5 → 4，无 `null`）；**撤销 v1.5.34 的 holo 特例**（恢复引擎原语义）。
- **C 门槛处置（写清理由）**：撤掉旧的"全息 > 2 次/局"普通条件（送盾是这张卡的**定义**、不是 bug），
  只保留**硬门槛"套给别人 > 6 次/局"（不可 `--force`）**，并留档实测分布（v7wall-33 18.7/局 vs v7wall-31 2.0~2.9/局）。
- 守门 **D45 重写**（候选不得含幻影 + 引擎"不能给自己"语义仍在 + v1.5.34 特例必须已撤销）。

### 3. 连带：指纹重记（铁律）
动了 `js/core/resolve.js` ⇒ 规则指纹推进 ⇒ 重测重记两个包（3P 权重不变仍为 `v7wall-31`；2P 重跑考卷）。

---

## v1.5.36 — 新增审计建议的两列 H/I（4 风格同场 / 1 冠军 vs 4 只ジ），**并标记口径待对齐**

## v1.5.35 — 终局平局口径修正（用户裁定）+ battle-test 看门狗止血

## v1.5.34 — 根因修复："全息屏障送盾"不是学坏了，是**目标没进决策**（用户质疑换来的正解）

## v1.5.33 — 训练"静默冻结"检测（`tools/zero-promote-check.mjs`）：零提升不再无声无息

## v1.5.32 — 换冠军 `v7wall-31`（换掉"送盾给对手"的 v7wall-33）+ **新增不可 `--force` 越过的硬门槛**

## v1.5.31 — "打断环"课题：**动作级手段全部打满**（奖励 / 池子 / 课程示范 / 暴露度），结论转"结构路"

## v1.5.30 — 换冠军 `v7wall-33`（修掉 D 列退化）+ 破墙奖励（方案 b）+ **"打断环"三次尝试的实测结论**

## v1.5.28 — 修门禁（方案 1+2）：**反弹墙穿透卡零命中 = 阻断** + 自对局穿透条件降级为提示

## v1.5.27 — **换冠军：v7press-36**（首次"标准持平 + 打环翻倍 + 多样性翻倍"的干净提升）+ F 门槛重标定

## v1.5.26 — **重定义 E 指标**（方案 B）：只把"没有大雷威胁时还一直摆架势"算病

## v1.5.25 — 惩罚被动的窄奖励（方案①）+ **出现首个"两个都要"的候选 v7press-36**

## v1.5.24 — 环奖励**退火**（方案 A）+ 实验结论：打环能力练出来了，但仍是"偏科"

## v1.5.23 — 修"死亡结算跑到伤害之前" + **训练信号：教它惩罚开环的人**（首次真正生效）

## v1.5.22 — 结算事件行的**显示顺序**（用户澄清的"防御优先"真意）+ 技能格按钮回到规则顺序

## v1.5.21 — 页面回合列表/导出三处修正 + 底部技能格恢复"防御优先"

## v1.5.20 — UI 两处修正（连带电伤文案 / 技能格子顺序）+ 指纹重记 + README 瘦身

## v1.5.19 — 一次性加全参数量（v7 特征）：候选 =(技能,目标,珠类型) + 关系块 + 效果快照块

## v1.5.18 — 处置第三方独立复核：A 批（工程/门槛）+ B 批（规则层定稿）

## v1.5.17 — 镜面反射"生效时机"改回优先级口径（用户二次裁定：大雷先结算 ⇒ 复制不会发生）

## v1.5.16 — 镜面反射回归原始规则：**没有"无可复制"**（非伤害类复制到自己身上 + 对 t2 空指）

## v1.5.15 — 修蓄能珠类型的信息泄漏（三处）+ 技能格子显示顺序改成防御类优先

## v1.5.14 — 先手激励实测（能治"集体防御"但代价太大）+ 冠军体检新增 E/F 两列

## v1.5.13 — 修狙击枪"被干扰"判定（多人局 bug，用户对局记录发现）+ 口径澄清（冠军 vs 风格脚本）

## v1.5.12 — 回滚"长程默认开哨声惩罚"（实测是**空操作**，证据：两条臂产出逐字节相同）

## v1.5.11 — 终局收缩参数化（起点/每回合扣血可在页面调）+ 长程训练默认开哨声惩罚 + 换冠军自动跑技能报告

## v1.5.10 — 终局收缩升级为**全局规则**（100 回合后每回合全员 −1 血）+ 页面新增「用内置冠军」按钮

## v1.5.9 — 研究结论入库（哨声惩罚实测 + 防御型池子在 multi 下不划算）+ 对战测试覆盖长程模式

## v1.5.8 — 熵只统计非ジ + 反摆烂哨声惩罚 + 浏览器对战测试 / 冠军体检（上线冠军 long-33 → eco-34）

## v1.5.7 — 线上冠军换成 `long-33`（修 v1.5.4 造成的成绩崩塌）+ 全息屏障在 2P 灰掉 + 规则指纹守门

## v1.5.6 — 经济 shaping 门槛按 (人数,模式) 参数化（用户锚点）+ 恢复技能熵奖励；2P 体检

## v1.5.5 — 全息屏障第二轮修正：**被小雷无效化的时机**（漏读 `voided`）

## v1.5.4 — 规则修正：**全息屏障是"给别人套的盾"**（此前被做成了原型制御的重复品）

## v1.5.3 — 复合适应度（把风格表现写进 fit）；以及那条"已被记过一次"的错再犯一次 + 更正技能报告的假信号

## v1.5.2 — 训练池能放「风格化冠军」当对手；以及"加名字漏一处"的第三次

## v1.5.1 — 技能报告补上「模式」口径；它顺手揭开了挂很久的 P0-d 悬案

## v1.5.0 — 长程（5 血）冠军终于能训练了（接线 + 两道守门 + 一个新坑）

## v1.4.14 — 第三次『加名字漏一处』：对手池补 `ringspam` + 未知名字**开跑前中止**

## v1.4.13 — 三件卫生事项（用户批准）

## v1.4.12 — 多 seed 配对（n=6）：加反弹墙无显著影响 ⇒ 不改默认池、不换冠军

## v1.4.10 — 消融（`--ban`）成为 report 的主口径

## v1.4.11 — 用户纠正：**小雷不是陷阱卡**，它的价值是反制（我错在考卷里没有"环"）

## v1.4.9 — 对手池抽成单一来源（静默漂移坑）+ 反弹墙实验的诚实结论

## v1.4.8 — 终局收缩替代硬截断（用户方案）+ 查清"反弹墙为什么不在训练池里"

## v1.4.7 — 处置第十轮独立复核：确认反弹墙(P0-c) + 更正我两处结论 + 修 3 处小问题

## v1.4.6 — report 自己说清"哪些测不了、该用什么命令" + 一键入口 + REVIEW-5P

## v1.4.5 — 补齐"测不了"的四个口径：消融/开珠/连招/条件注入

## v1.4.4 — 多 seed 配对 A/B（n=4）：12 对手均值 +4.9pt 且方差更小；skill-report 支持多版本对比

## v1.4.3 — 对手池 A/B（从零冷启动）：符号 2/2 支持加对手，但**幅度未定**

## v1.4.2 — 更正 v1.4.1 的归因：38.4% → 21.8% **不是**轮换造成的

## v1.4.0 — 长程模式（5 血）：拉长赛程**只能解冻一部分**被锁死的卡

## v1.3.60 — 条件触发型技能的专用器材 + 用户纠正的符咒机制

## v1.3.59 — skill-report「一堆死技能」主要是**报告自己的测量失败**

## v1.3.58 — 首个 5 人冠军：3 人与 5 人**同时**变强，且座位中性

## v1.3.57 — 座位效应与平局问题：查出**两个方向相反的**成因并都修掉 + 首个 5 人冠军

## v1.3.56 — 查实「浏览器训练不可复现」的真因（**并纠正我自己在 P3.3 写错的归因**）+ 补推送

## v1.3.55 — 搭好 5 人局评测（P3.5「改考卷」），而**它否掉了「等 5 人局」这个方向**

## v1.3.54 — 深经济"货架问题"从猜测变成**有效证据**；(c) 方案落地但门槛默认关闭

## v1.3.53 — 按 Claude 的复核：把能自动化的教训全部改写成会失败的用例

## v1.3.52 — WR_TOL 改显式参数 + repro-check 补 D/E 断言（千问建议）

## v1.3.51 — 修过期日志文本：打印真实 `OUT_PATH`（原硬编码线下路径，**正是它误导我**）

## v1.3.50 — 修 CLI 不可复现的真正根因（千问定位）：**热启动读自己的产物**

## v1.3.49 — train-3p 不可复现：排除三个假设（**仍未找到根因**）

## v1.3.48 — 任务2 完成：训练工具不再覆写线下冠军；任务1（train-3p 可复现）**未解决**

## v1.3.45 — N23 激光眼单发即破整个防御族（用户裁定）

## v1.3.44 — 修 v1.3.43 的回归：`BIG_T_EXEMPT` 扩张撞坏 R23'（spec 36/37 → 37/37）

## v1.3.43 — N22 大雷效果传导：按用户裁定实现（防御族格挡 / 沿作用目标传导 / 反弹只挡不反击）

## v1.3.42 — 修大雷结算：豁免小雷（旧代码对照 + 用户裁定）+ **发现并修正一个空用例**

## v1.3.41 — 集火脚本 `pickFocusFire` + 转移伤害的**决定性**结论：当前 meta 下不可用

## v1.3.40 — 转移伤害诊断：**能赚的充要条件是"单回合承伤 N≥2"**（用户直觉被算术证实）

## v1.3.39 — 新增 `tools/repro-check.mjs`：可复现性看门狗（千问建议）

## v1.3.38 — 噪声下限复测：**千问的预期没有应验**，但这个比较本身有缺陷

## v1.3.37 — worker 分种子：结果与 worker 数无关 + 个体间独立（千问跨机问题 ①②）

## v1.3.36 — 训练可复现：千问定位到两处 Node 作用域的随机/时间源（**我的假阳性也被证实**）

## v1.3.35 — 火弱持续性：训练侧模拟（3×200）→ **无可测效果**，且意外成为噪声下限标定

## v1.3.34 — WR_TOL 多跑对照（各 3 次）：**0.03 胜出，我之前的收紧是错的**

## v1.3.33 — WR_TOL 单变量对照：**单跑一次分不出差异，方差盖过效应**

## v1.3.32 — 补齐 N20 用例（35/35）+ 用新规则重训一次 AI 的实测结论

## v1.3.31 — 修 v1.3.19 引入的双枪 `via` 回归（N20b 关闭）+ N20 全绿

## v1.3.30 — N20 地雷 AoE 重写（用户裁定）+ 3 条 N≥3 spec 用例

## v1.3.29 — 地雷触发改为「近 5 回合架势频率 + 对手当前 ジ 数」（用户方案）

## v1.3.28 — 地雷改用「历史频率」触发（方案 a）；`lastSkill` 滞后问题的确认

## v1.3.27 — P1：深经济对手 `deepsaver`（先写脚本 → 实测它真的会攒）

## v1.3.26 — 冠军换成 div555（v5→v6 无损升级）+ 工具回归确认

## v1.3.25 — P0：存档版本兼容（形状自适应）→ A/B 证据链恢复

## v1.3.24 — 修 v1.3.23 引入的回归：冠军包版本不兼容 + 模仿默认关闭

## 附录 A：当前有效契约 / 常量（改这些必须同步守门）

- **规则指纹**：`53f064c5`。改动 `js/core/rules.js` / `resolve.js` / `state.js` / `play.js` / `js/train/policy.js`
  之后必须**重记两个包的指纹**（`tools/promote-champion.mjs` + `tools/promote-champion2p.mjs`），否则 D16 红。
- **网络形状**：`PACK_VERSION=7`、`FEAT_S=213`、`FEAT_A=22`、`FEAT_N=235`、`paramCount=5689`；
  旧形状 v6=3337 / v5=3313 仍可原生读取（`unpack(json, true)`）。
- **线上 3P 包必须是 v7 容器**（NP-test N19 用 `checkPack` 卡 `o.v === PACK_VERSION`）；
  旧冠军要上线先用 `node tools/upgrade-pack.mjs <旧包> <新包>` 嵌入 v7（旧前缀逐位保留，新块零填）。
- **上线体检门槛**（`tools/promote-champion.mjs`，任一不过即阻断，除非 `--force` 并记入 meta）：
  `伤害/局 ≥ 5` · `平局率 ≤ 20%` · `E 无威胁摆架势 ≤ 60%` · **`F 场A 被集火还手 ≥ 20%`** · `G 有效技能数 ≥ 3` ·
  `反弹墙穿透命中 > 0` · **`座位极差 < 30pt`**（前置：判胜 ≥ 30%，否则记"不可判"而非"均衡"）。
- **训练隔离**：产物写 `EPIRUS_BUNDLE_OUT`、热启动读 `EPIRUS_BUNDLE_IN`（`.training-*`，已 gitignore）；
  训练期间不 bump `index.html` 版本号；`.training.lock` 存在时禁止换包。
- **实验开关**（默认全关 ⇒ 出厂行为一字不变）：`EPIRUS_ALLOW_HEALTH_FAIL=1`（门槛拦下的产物仍落盘）、
  `EPIRUS_DIV_W`（覆盖熵权重）、`EPIRUS_FEAT_MASK`（特征块掩码）、`EPIRUS_IMIT_GENS` / `EPIRUS_IMIT_TEACHER`（课程/模仿）。
- **口径常量**：座位对称性用**百分点**（20 局噪声会淹没 30pt 阈值 ⇒ 至少 100 局）；
  `1st` 走 `rankOf`（并列算第一）⇒ 体检同时报 `严胜`/`并列`，判据用严格口径与结果型指标（伤害/命中）。

## 附录 B：训练问题复盘（2026-09 全期；用户要求）

### B1 流水线 / 工具链
1. **并发训练请求被静默丢弃**（`train-server.mjs` 的 `if (!runningN) { … } return;`）⇒ 12 个 seed 拷到**同一个包**
   （sha1 `dfb399dab9` × 12），所有"多种子对比 / 座位验收"的**有效样本恒为 1**。修于 v1.5.55（明确报错 +
   `start` 事件带 seed + runner「产物串味守卫」），并补修"被健康门槛拦下"与"真串味"的混淆。
2. **健康门槛整臂拦下候选**：v10 臂 6/6、v8fix 臂 11/12 无产物 ⇒ 候选池可得性完全取决于实验开关
   `EPIRUS_ALLOW_HEALTH_FAIL`（v1.5.56 引入，默认关）。
3. **训练覆写线上包**（早期两次把页面搞崩）⇒ v1.5.48 训练隔离（`EPIRUS_BUNDLE_IN/OUT`）。
4. **可复现性脆弱**：槽位随机键一度借用共享 `__rng()` ⇒ 任何"特征调用时机/次数"变化都会扰动随机流，
   引发 D22（蓄能珠泄漏）/D26/D13 三连红 ⇒ v1.5.52 改成**纯函数哈希 + 调用方输入盐**（不消耗任何随机流）。
5. **旧形状包读不出来**：`unpack(json)` 对 v5/v6 返回 `null`（必须 `unpack(json,true)`）；`checkPack` 只认 v7；
   表现为"换回历史冠军时体检直接崩"⇒ v1.5.63 修 + 新增 `tools/upgrade-pack.mjs`。

### B2 门槛 / 指标（错指标把合格产物挡在门外）
1. **F 口径错**（v1.5.61/62）：所谓"活跃场"只有 1 席进攻者、另外 4 席是**冠军自己的副本**（量到的是自对局均衡）；
   分子还是硬编码 6 张卡。门槛 35%→25% 后仍靠 `--force` 越过 ⇒ 换成"场 A 被集火还手率（≥20%）"后
   **首次出现两个候选全过**（eco-34、种子 v1.3.58）。
2. **E 口径错**（v1.5.26）：旧口径把"看到对手攒 5 ジ 才防"的**合理防御**判成病（eco-34 89% ✗）⇒ 新口径 16% ✓；
   **这条错指标是 eco-34 当年被换下的直接原因之一**。
3. **`1st` 把并列算第一**（v1.5.58）：farmerwall `1st=97.5%` 但 `严胜=0.0%`；反弹墙 `100%` 但 `严胜=12.5%`
   （87.5% 并列）⇒ A/D/H/I 四列在"熬到上限全员存活"的场里系统性虚高。
4. **座位偏置此前无门槛**（v1.5.57）：线上包 5 席同策略时极差 **61pt**（long 82pt），而所有旧体检项都看不见。
5. **小样本把噪声当结论**：同一个 eco-34 的 G 在 n5=3.08 / n10=2.99 / n20=4.24（v1.5.18 记录）⇒
   用 n=10 卡 `G<3` 等于用噪声淘汰个体。
6. **反复出现"阈值把所有人挡住"**（F、座位、自对局穿透卡、E）⇒ 规则：**阈值必须能分开"已知好"与"已知坏"**，
   分不开就先怀疑指标，而不是继续下调数字。

### B3 训练动力学 / 配方
1. **探索冻结（零提升）**：多数臂产出与热启动种子**逐字节相同**的包（`v7both`/`v7soft` 6/6 = 种子；逃逸率≈1/6）；
   且**逃过冻结 ≠ 学会**（18 seed 批量：3 个逃逸、0 个学会）。
2. **窄奖励从 gen 0 打开会冻结训练**：环奖励一开就压制演化（成功样本 `v7wall` 靠**退火**才出），
   学到的"被动攒环"个体过不了健康门槛 ⇒ 落盘被拒。
3. **冻结是实验臂特有**：带强迫/环奖励的臂逃逸率≈1/6，而**无强迫的普通臂 6/6 全部有提升**（v1.5.50 实测）。
4. **覆盖坍缩（G<3）**：v9/v11/v12 共 25+ 候选里，"打得凶"的全部只用 2~3 张卡；把覆盖熵权重 `DIV_W` 提到 10×
   **也没有打破**这个互斥（v12 臂 12/12 仍不合格）⇒ 是当前 fitness/探针定义下的**结构性张力**。
5. **偏置是训练造出来的**：热启动种子 v1.3.58 自身均衡（各座 22/16/22/16/24%），训练产物却偏到 39~100pt ⇒
   四条 L7 修复（槽位并列 / 结算起点相位 / 默认靶子 / 多目标第二目标）只能保证"引擎不系统性偏"，
   阻止不了策略自选座位套利 ⇒ 需要**上线体检的座位门槛**（v1.5.57 已加）。
6. **候选来源单一**：几乎所有产物都从同一个热启动种子出发，多样性只靠 seed 抖动 ⇒ 容易收敛到同一吸引子；
   健康门槛又会把"探索期"的候选整批判死（B1-2）。

## 附录 C：方法论规则（踩过的坑 → 规则）
1. **门槛与提交必须用 `&&` 串联**；`;` 接 `echo` 会**掩盖真实退出码**（一次臂失败被伪装成"作业成功"）。
2. **改动指纹集必须重记包指纹**（`promote-champion` + `promote-champion2p`），否则 D16 红。
3. **不要猜字段名**：先打印真实对象再写探针（`e.from` 应为 `source`；事件无 `round` 字段；技能键不是 `GUN/SWORD`）。
4. **阈值必须有判别力**：先用"已知好 vs 已知坏"验证，再定数字（B2-6）。
5. **单位必须钉死**（胜场数差 ≠ 百分点；20 局噪声 > 30pt 阈值 ⇒ 100 局起）。
6. **纯函数优先**：凡是"身份/顺序/随机性"，都不要借用共享随机流（B1-4）；需要不可预测就由**调用方输入**（盐）。
7. **版本号替换必须硬断言**（`assert old in text`）—— 静默 no-op 会让三方版本漂移而门禁仍绿（v1.5.55/56 就发生过）。
8. **修补脚本先写后检**：python 里"先插用法再判是否已导入"会让守卫误判；引号嵌套会 SyntaxError 且**静默不写盘**。
9. **发布作业跑完再跑门禁**：发布中途跑 np-test 会与写包互相污染（本次踩过）。
10. **训练与线上物理隔离**：产物只写 `docs/artifacts/.training-*`；`.training.lock` 存在时禁止换包。
